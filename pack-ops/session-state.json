{
  "schema": "pack-session-state/1",
  "boundary_commit": "ed7956a",
  "checkpoint": "2026-07-31T00:00:00Z",
  "active": ["BD-236"],
  "in_flight_agents": [
    "planner-bd236-impl -- BD-236 IN-FLIGHT (RO, isolation=full; large-BD pipeline STAGE 6): commit-sequenced plan for the USER-APPROVED design. Planning: Change A Check-57 per-tier reshape (trinity 8 / defs 13 / launcher 13) + test-57 update [MANDATORY, pack-only]; Change B launcher git tag (agent-run.sh array+prose) [D5, project-only, prerequisite for Tier-C-13]; Change C Check-11 docstring fix [pack-only]; Change D resolve the 9-toml stash/restore transposition [project-only]; NEW test-27.sh + test-11.sh [OI-6, pack-only]. Scope-keyword purity, rule-10 wave map, per-commit CI-green, push-time manifest note. ON REPORT: adversarial planner -> reconciliation (if findings) -> USER planner-to-coder gate -> coder waves."
  ],
  "queue": ["BD-210", "BD-205", "BD-093"],
  "parallelization": "SERIAL: BD-236 LARGE-BD pipeline -- design APPROVED at the user gate; planner in flight -> adversarial planner -> reconciliation (if findings) -> USER planner-to-coder gate -> parallel worktree coder waves (bounded cycle per commit). Then BD-210 -> BD-205 -> BD-093.",
  "wave": "BD-136/276/277/278 + BD-275 SHIPPED+RESOLVED+PUSHED+CI-GREEN. BD-236 design APPROVED at the user design-review gate (itemized walk). Planner turning it into a commit sequence now. UNPUSHED: dashboard + several session-state commits (ride with the BD-236 push).",
  "pending_decisions": [
    "USER-APPROVED BD-236 design decisions (locked at the design-review gate): D1 ADOPT per-tier Check 57 (trinity 8 / defs 13 / launcher 13); D2 keep Check 11 informational + fix docstring; D3 (a)-only (NO new content check, NO Check-27 phrase change); D5 add git tag to launcher; D4 RESOLVE the 9-toml transposition; OI-6 ADD test-27.sh + test-11.sh (close pre-existing gap); OI-9 BD-236 before BD-109 (+ flag BD-109 stale .gemini path). Plan these EXACTLY.",
    "NEXT after the planner: adversarial planner -> reconciliation planner (if findings) -> USER planner-to-coder gate (surface the plan for approval) -> parallel worktree coder waves under the bounded review/fix cycle -> commits (pack-only for the checks/tests; project-only for agent-run.sh + the 9 tomls; scope-keyword purity per Check 36) -> push (manifest regen at push-time since Change B/D shift project-template/* inputs).",
    "BD-205 carries the user-directed pre-merge main->v11-dev reconciliation (per-commit user-approved keep/drop; keepers merged INTO v11-dev first so the final merge is a clean overwrite).",
    "Standing: push USER-APPROVED then Pack-Chat-executed; NO new BDs without user ask; pack-chat-only trees Pack-Chat-direct; intervention commit-gate live (fresh token, separate step) EXCEPT autonomous session-state refreshes; session-state COMPACT (one SHA boundary only, no other SHAs, no dates outside checkpoint, no hex agent IDs, <4096 B); modes review=itemized, intervention=full, isolation=full; scratch root $XDG_STATE_HOME/optiquity-pack-handoff/."
  ],
  "cycle_position": "BD-236 design APPROVED (itemized gate). Planner IN-FLIGHT (planner-bd236-impl). On report -> adversarial planner -> reconcile (if findings) -> USER planner-to-coder gate. Then BD-210."
}
