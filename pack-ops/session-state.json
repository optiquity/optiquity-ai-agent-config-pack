{
  "schema": "pack-session-state/1",
  "boundary_commit": "30bda2a",
  "checkpoint": "2026-07-24T00:00:00Z",
  "active": ["BD-257"],
  "in_flight_agents": [
    "coder-bd257-a1 — BD-257 A1 keystone commit: client help separation + de-ship pack-help.sh/detect.sh + _SANCTIONED_PACK_SIDE_SHIPPED=() + the Check-47 empty-invariant assertion (code-enforcing the (c) hard line) (RW, isolated worktree)"
  ],
  "queue": ["BD-136", "BD-236", "BD-210", "BD-205", "BD-093"],
  "parallelization": "BD-257 coder waves are SERIAL (B-waves serialize on PLATFORM-SKILLS.md per Check 31): order A1 -> B1 -> B2 -> B4 -> B3 -> C1 -> C2; each commit runs its own bounded review/fix cycle + per-commit user approval.",
  "wave": "No-dual-use (c) hard-line rule LANDED (dependency-direction-placement strengthened: no dual-use, empty allowlist, growth FORBIDDEN — trinity + rationale; CI green). BD-257 implementation BEGUN. A1 keystone in flight: client help separation (/pack-help -> /pm-help with its OWN pm-help.sh + fragment), de-ship pack-help.sh + detect.sh, empty the allowlist, and ADD the Check-47 empty-invariant assertion so any regrowth fails CI (code-enforced (c)). Seq-1 done -> A1 drops the pack-root trinity value-flip (the rule already owns it) and instead reconciles the Check-47 comments to (c). NEXT after A1: B1 (modes) -> B2 (status/refresh) -> B4 (promotions) -> B3 (dashboard, heaviest) -> C1 (fragment rows + startup) -> C2 (Check 90 parity + count 86->87).",
  "pending_decisions": [
    "BD-257 runs 7 serial coder-wave commits (A1->B1->B2->B4->B3->C1->C2), each with a bounded review/fix cycle and a per-commit user commit-approval gate. B3 (dashboard schema-retarget) is the heaviest — its own review budget.",
    "Standing: NO new BDs without explicit user ask; pack-chat-only trees are Pack-Chat-direct; MAJOR edits route to pack-coder; push is user-manual (v11-dev in sync with origin); the intervention commit-gate is live (write a fresh pack-ops/.commit-approval-token in a SEPARATE step before each approved commit) EXCEPT routine session-state refreshes, which the user authorized Pack Chat to perform autonomously; session-state COMPACT — one SHA in boundary_commit only, no other SHAs, no hex agent IDs, <4096 bytes."
  ],
  "cycle_position": "BD-257 A1 coder in flight (first of 7 serial waves); then reviewer -> user commit-approval -> B1. No-dual-use rule fully landed + CI green."
}
