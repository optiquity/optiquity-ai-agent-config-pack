{
  "schema": "pack-session-state/1",
  "boundary_commit": "53564d2",
  "checkpoint": "2026-07-30T00:00:00Z",
  "active": ["BD-276", "BD-275"],
  "in_flight_agents": [
    "coder-bd276-sweep-guard -- BD-276 (pack-only, RW isolated): sweep every REAL mktemp [-d] -t <prefix>.XXXXXX invocation (~60 tracked files; NOT comment/doc mentions) to the portable full-template form + add a NEW validate-pack guard check (git-tracked, O(lines), invocation-vs-mention aware, allowlist=legit doc mentions) + atomic count 88->89 (Check 59 gate) + per-check test proving bite-real/spare-mention. BD-275 sequenced AFTER (both touch scripts/tests/)."
  ],
  "queue": ["BD-236", "BD-210", "BD-205", "BD-093"],
  "parallelization": "SERIAL mini-batch: BD-276 (mktemp sweep + guard) IN FLIGHT -> then BD-275 (order-dependent test flake; reproduce-in-fresh-clone-first then fix the shared test-fixtures/ state). User chose 275+276 before BD-236 (both now; BD-276 = sweep + guard so the guard protects later BDs from re-introducing the class). BD-136 confirmed portable (its 2 -t hits are portability COMMENTS, false positives).",
  "wave": "BD-136 SHIPPED + CI-GREEN + RESOLVED. Now the off-spine chores (user-directed, both now): BD-276 (mktemp sweep + regression guard) coder IN FLIGHT; BD-275 (serial-suite flake, reproduction-blocked) NEXT. Then the ruled spine BD-236 -> BD-210 -> BD-205 -> BD-093.",
  "pending_decisions": [
    "BD-276 returns -> CI-verify (guard bites real invocation + spares mentions; count 89; no sweep regression) + review -> user commit approval (pack-only). Then BD-275: reproduce the serial-suite flake in a FRESH CLONE (fixture is gitignored — must build) against the CURRENT post-BD-136 suite, diagnose the shared test-fixtures/ vector (hypothesis: a fixture-mutating test flips Check 61/62 that test-v11-realistic-ot.sh reads), fix (general shared-state isolation / per-test reset), review, commit. Then push (user-approved). Then BD-236 (rebases the count -> 90).",
    "BD-205 carries the user-directed pre-merge main->v11-dev reconciliation (per-commit user-approved keep/drop; keepers merged INTO v11-dev first so the final merge is a clean overwrite).",
    "Standing: push USER-APPROVED then Pack-Chat-executed; NO new BDs without user ask; pack-chat-only trees Pack-Chat-direct; intervention commit-gate live (fresh token, separate step) EXCEPT autonomous session-state refreshes; session-state COMPACT (one SHA boundary_commit only, no other SHAs/dates in prose, no hex agent IDs, <4096 bytes); modes review=itemized, intervention=full, isolation=full; canonical scratch root $XDG_STATE_HOME/optiquity-pack-handoff/ (pack side)."
  ],
  "cycle_position": "BD-136 done. Off-spine chores in progress: BD-276 sweep+guard coder in flight; BD-275 next (reproduce+fix). Then BD-236 per the ruled spine."
}
