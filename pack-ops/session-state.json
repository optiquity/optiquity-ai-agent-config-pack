{
  "schema": "pack-session-state/1",
  "boundary_commit": "bd07ee0",
  "checkpoint": "2026-07-01T00:00:00Z",
  "active": [],
  "in_flight_agents": [
    {
      "name": "reviewer-bd261-cm2-2",
      "role": "pack-reviewer",
      "purpose": "CM2 review, fresh instance (launch-pad; in the CM2 worktree)",
      "output": "/Users/david/Developer/_tmp/pack-handoff-bd261/REVIEW-BD261-CM2.md"
    }
  ],
  "queue": [
    "BD-189",
    "BD-224",
    "BD-257",
    "BD-136",
    "BD-236",
    "BD-210",
    "BD-205",
    "BD-093"
  ],
  "parallelization": "serial",
  "wave": "BD-261 complete pending the CM4 commit + the batch push. On landing: the BD-189 fold pass (the P2 spec + the two routed riders + all the held-plan folds) -> the BD-189 plan re-approval -> C0 opens the groupings children.",
  "pending_decisions": [
    "The CM4 closing package (flip commit + batch push) awaiting approval.",
    "SESSION-SETTINGS JSON (user heads-up, for BD-224 pack + BD-257 project slash-command BDs): user will introduce a separate settings JSON (distinct from session-state.json) governing chat/agent session behavior, per-side, readable by any agent/actor — like Claude's settings. Track as a design input for BD-224/BD-257; user introduces the mechanism when those BDs start.",
    "restore GitHub Actions minutes at monthly reset if capped (not currently) — re-run CI as needed",
    "SESSION DIRECTIVE (user, until revoked; not a rule change — no memory/BD): ALL spawns use isolation:worktree (the async-task channel). RO agents work in their own worktree. Reviewers + fix-coders get a LAUNCH-PAD worktree they must ABANDON: first instruction = cd into the target live worktree (the commit under review/fix) + verify pwd/HEAD/dirty-set before any work; the unused launch worktree auto-cleans. The substantive placement rules are unchanged (review/fix happens IN the commit worktree); re-engagement via SendMessage-to-agentId (transcript resume)."
  ],
  "cycle_position": "At the CM4 gate (the flip commit + the batch push with manifest-sync). Then the BD-189 fold pass."
}
