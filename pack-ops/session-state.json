{
  "schema": "pack-session-state/1",
  "boundary_commit": "55f479c",
  "checkpoint": "2026-07-30T00:00:00Z",
  "active": ["BD-278", "BD-275"],
  "in_flight_agents": [],
  "queue": ["BD-236", "BD-210", "BD-205", "BD-093"],
  "parallelization": "SERIAL client mini-batch: BD-277 (CLIENT mktemp fix + validate-shell.sh guard leg) SHIPPED + RESOLVED (project-only). NEXT BD-278 (CLIENT validate.sh enforcement: opt-in pre-push hook installer + pm-startup/pm-refresh detect-and-suggest; needs a focused architect assessment). Then BD-275 (order-dependent serial-suite flake; reproduce-in-fresh-clone then fix shared test-fixtures state). Then the ruled spine. BD-277's fixture manifest is pending a push-time regen (separate pack-only commit).",
  "wave": "BD-136 + BD-276 + BD-277 SHIPPED + RESOLVED. Client mini-batch continuing: BD-278 (validate.sh push-hook enforcement) NEXT -> architect-spawn gate; then BD-275. Then the ruled spine BD-236 -> BD-210 -> BD-205 -> BD-093. Push user-approved + batched (regen the BD-277 fixture manifest at push).",
  "pending_decisions": [
    "BD-278 (opened, user-approved to open): AWAITING user approval to spawn pack-architect (per architect-spawn-protocol) for the enforcement design -- pre-push vs pre-commit, the pm-startup + pm-refresh detect-and-suggest, the opt-in installer (precedents: client install-immutable-hook.sh + pack /pack-startup Step 5); all client-audience + OPT-IN + user-consent-gated.",
    "BD-275 after BD-278: reproduce the serial-suite flake in a FRESH CLONE (fixture is gitignored - must build) against the current post-BD-136/276/277 suite, diagnose the shared test-fixtures state vector, fix (general shared-state isolation), review, commit.",
    "PUSH-time (batched, user-approved): regenerate test-fixtures/manifest.txt for BD-277 (fixture inputs shifted by the client project-template edits) via manifest-sync.sh -> a SEPARATE pack-only commit (BD-274 precedent); then push + confirm CI-green.",
    "BD-205 carries the user-directed pre-merge main->v11-dev reconciliation (per-commit user-approved keep/drop; keepers merged INTO v11-dev first so the final merge is a clean overwrite).",
    "Standing: push USER-APPROVED then Pack-Chat-executed; NO new BDs without user ask; pack-chat-only trees Pack-Chat-direct; intervention commit-gate live (fresh token, separate step) EXCEPT autonomous session-state refreshes; session-state COMPACT (one SHA boundary_commit only, no other SHAs, no dates outside checkpoint, no hex agent IDs, no dated handoff paths; <4096 bytes); modes review=itemized, intervention=full, isolation=full; canonical scratch root $XDG_STATE_HOME/optiquity-pack-handoff/ (pack side)."
  ],
  "cycle_position": "BD-277 done + committed + Resolved; worktree torn down. BD-278 (push-hook enforcement) next -> architect-spawn gate. Then BD-275, then the ruled spine BD-236."
}
