{
  "schema": "pack-session-state/1",
  "boundary_commit": "f3eac18",
  "checkpoint": "2026-07-30T00:00:00Z",
  "active": ["BD-276", "BD-275"],
  "in_flight_agents": [
    "coder-bd276-sweep-guard -- BD-276 (RW, alive worktree; SendMessage-reused under isolation_mode=full): the fix bundle + -p synonym are DONE and re-review CLEAN on the core delta (Check 92 covers 3 classes: -t/-dt literal-XXXXXX, GNU-only --tmpdir + -p; skill x3 byte-identical + correct note; sweep 318 intact; battery + DEEP green; count 89). Now applying two reviewer-surfaced pack-side one-liners the user approved folding in: OPEN-1 test-fixtures/README.md still taught the buggy form (a second root-cause teaching surface) -> portable swap; OPEN-3 ARCHITECTURE-BD-219-CI-FAILURE-DIAGNOSIS.md stale 'portable' claim -> one-line reconcile. Then a FRESH FINAL reviewer (final pass of the bounded cycle) -> commit.",
    "reviewer-bd276-refix -- BD-276 (RO): re-review DONE, verdict CLEAN on the core delta + 3 open-items (OPEN-1/OPEN-3 fix in BD-276; OPEN-2 client agent-run.sh -> BD-277)."
  ],
  "queue": ["BD-236", "BD-210", "BD-205", "BD-093"],
  "parallelization": "SERIAL mini-batch: BD-276 (pack: mktemp sweep + Check 92 guard + verification-harness skill ROOT-CAUSE fix) -> BD-277 (CLIENT companion, user-approved to open, not yet authored: project-template/agent-run.sh line 347 is the SOLE client mktemp -t occurrence + assess/add a client-side guard; the client ships validators but NO CI workflow, so guard placement is a P-missed-7 architect assessment) -> BD-275 (order-dependent serial-suite flake; reproduce-in-fresh-clone then fix shared test-fixtures state).",
  "wave": "BD-136 SHIPPED + CI-GREEN + RESOLVED. Off-spine chores (user-directed): BD-276 sweep verified complete; guard review found 5 completeness/root-cause findings, ALL user-approved FIX (incl NIT-4); fix-coder IN FLIGHT in the sweep worktree. Scope EXPANDED to fix the pack verification-harness skill root cause + widen Check 92 to two portability classes (-t + --tmpdir). BD-277 (client-only companion) + BD-275 next. Then the ruled spine BD-236 -> BD-210 -> BD-205 -> BD-093.",
  "pending_decisions": [
    "BD-276 next: fix-coder returns -> re-review (fix-pass 1 of the bounded cycle, fresh RO reviewer, no prior-review injected) -> if clean, produce patch from the worktree -> replace the stale applied copy on the v11-dev checkout with the reviewed version -> re-verify (validate + DEEP + Check 92 clean + count 89 + check-92 test incl new bites + no regression) -> user commit approval (pack-only) -> tear down worktree after landed. Entry gains the skill-fix + two-class-guard scope clause + Resolved line at commit.",
    "BD-277 (client-only companion, user-approved to open): author entry (next BD number) then architect assessment (USER-APPROVED spawn per architect-spawn-protocol) of the client-guard placement (client has validators, no CI workflow) before any coder.",
    "BD-205 carries the user-directed pre-merge main->v11-dev reconciliation (per-commit user-approved keep/drop; keepers merged INTO v11-dev first so the final merge is a clean overwrite).",
    "Standing: push USER-APPROVED then Pack-Chat-executed; NO new BDs without user ask; pack-chat-only trees Pack-Chat-direct; intervention commit-gate live (fresh token, separate step) EXCEPT autonomous session-state refreshes; session-state COMPACT (one SHA boundary_commit only, no other SHAs/dates, no hex agent IDs, <4096 bytes); modes review=itemized, intervention=full, isolation=full; canonical scratch root $XDG_STATE_HOME/optiquity-pack-handoff/ (pack side)."
  ],
  "cycle_position": "BD-276 sweep+guard clean; guard review in flight; skill root-cause fix + client BD-277 + BD-275 queued. Then BD-236 per the ruled spine."
}
