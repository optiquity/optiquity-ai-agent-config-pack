# ADVERSARIAL-PLAN-REVIEW-BD-291 — adversarial pass over PLAN-BD-291.md

- Agent: pack-planner ADVERSARIAL instance (fresh — not the plan's author), read-only; worktree `…/.claude/worktrees/agent-a65248df308b6c6a7` @ HEAD `0c2709e7e59241a39b7892d31c219d3f5ec4cefd` (== injected canonical HEAD; `git worktree list` cross-checked: canonical + this worktree both at `0c2709e`); date 2026-08-25; `isolation_mode: full` per spawn contract.
- Plan under review: `/Users/david/.local/state/optiquity-pack-handoff/bd291-planner-20260825T223908Z/PLAN-BD-291.md` (383 lines, read in full).
- Authority chain read in full: `…/bd291-reconcile-20260825T221529Z/ARCHITECTURE-BD-291.md` (335 lines) + `RECONCILIATION-BD-291.md` (99 lines); `backlog/BD-291.md`; census artifacts at `…/bd291-census-20260825T210547Z/` consulted for grep-count evidence only (read-only).
- Every finding below carries independently captured evidence from THIS session at `0c2709e`. Where I re-measured a plan claim and it held, that is recorded in §3, not manufactured into a finding.

## §1 Verdict summary

**0 BLOCKER, 1 MUST, 3 SHOULD, 3 NIT. The plan survives.** Coder-wave execution as specified would land: every design §4 row maps to exactly one commit (re-derived 20/20, zero drops/duplicates); both declared deltas (OI-P2, OI-P3) are evidence-backed and verified; every spot-checked locator and quoted anchor is byte-accurate against the tree; the corrected constants (400 / 68 = 61+7 / 72 = 61+7+4 / 4-out-of-enum by name / 0 missing-payload / 13 phase-N) appear consistently with zero stale 403/65 carriers; every intermediate commit is green by construction (Check 62's structural-only scope verified in source; the A1 no-test-breaks claim independently re-proven on axes the plan did not measure); all seven Check-36 keyword claims re-derived VALID against `boundary_refs.py`'s actual path rules; the §7 workspace ritual matches the landed isolation contract verbatim; all mutation-bite obligations (m1–m5, r0–r2, s1–s5, P13 case 6) bite regressable behavior; the cross-BD delta EEB and Wave-D close-out are sound. The one MUST is a completeness defect, not a design defect: the c3 "Files (complete)" list omits a measured encoding surface (`scripts/persona-contracts/contract-migration.sh`) whose in-file rationale is falsified by P9 and whose hedge in §4.6 names the wrong file — state that was verifiable at plan time. Correcting it is a one-file addition to the c3 spec; no re-planning is required.

## §2 Findings (most severe first)

### F-1 — MUST — c3's "complete" file list omits `scripts/persona-contracts/contract-migration.sh`, a measured encoding surface of the P9-changed fixture state; the §4.6 hedge names the wrong file and the wrong contact shape

**Plan claims (quoted).** §1 table header: "Files (complete)"; c3 row lists exactly `scripts/migrate-v10-to-v11.sh`, `scripts/lib/migrate-v10-to-v11/decompose.sh`, `test-fixtures/build.sh`, `scripts/tests/test-migrate-v10-to-v11-decompose.sh`, `scripts/tests/test-migrate-v10-to-v11.sh`. §4.6: "`test-persona-contracts.sh` green (if a v10 contract asserts docs/project absence, update it in this commit and report the assertion delta)."

**Evidence (this session, `0c2709e`).**
1. `test-fixtures/build.sh` L1134–1142 (`_materialize_for_contract`, `migration` arm): the migration persona contract's sandbox is a full copy of `v10-realistic-ot` — the exact persona P9 enriches.
2. `scripts/persona-contracts/contract-migration.sh` L7/L48: "The contract drives `migrate-v10-to-v11.sh` against the BD-120 …" / `MIGRATE_SH="$PACK_ROOT/scripts/migrate-v10-to-v11.sh"` — it runs the REAL migrator (the c3-changed pipeline) over the P9-enriched sandbox at c3's own gate.
3. Same file, L504–513 (comment, verbatim): "The current v10-realistic-ot fixture has no docs/project/{BACKLOG,IMPLEMENTATION-PLAN,CHANGELOG}.md input files, so the migrator legitimately produces no _toc.md files for that fixture. Adding the _toc.md surface here would force the contract to fail on the canonical fixture even though the migrator behavior is correct." — post-c3 (P9 + P5 + P6) this statement is FALSE: the fixture HAS the three inputs, the migrator DOES produce `_toc.md` (+ `_index.md` + `MIGRATION-TRIAGE.md`) on the canonical fixture, and the stated justification for omitting those assert surfaces inverts.
4. `grep -n "docs/project" scripts/persona-contracts/contract-migration.sh` → hits ONLY in the L444–513 region (installed-template asserts + the stale comment): there is NO "asserts docs/project absence" case — so the §4.6 conditional, evaluated honestly by the c3 coder, fires on NOTHING and the false comment lands in the tree unreconciled.
5. The hedge's named file is the WRAPPER: `scripts/tests/fixture-dependent/test-persona-contracts.sh` (87 lines, read in full) contains no fixture assertions at all — it only rosters `scripts/persona-contracts/{contract-greenfield,contract-mid-dev,contract-migration,contract-existing-source}.sh`.
6. Referencer census closed to grep-zero: `grep -rln "v10-realistic-ot"` over `scripts/ test-fixtures/ pack-ops/ supporting-docs/ .github/` → 11 files; 6 are plan-covered (c3/c4 files + the G1 gate item), 3 are name-only refs (`test-migrator-capability-translation.sh` L15, `test-validate-pack-check-61.sh` L182, `v11-trinity-marker-prepped/README.md` L83), leaving exactly TWO uncovered content-encoders: `contract-migration.sh` (this finding) and `test-fixtures/README.md` (F-4).
7. (Corroborating graph pass: `graphify query "which files assert or describe the content of the v10-realistic-ot fixture persona …" --graph <injected> --backend claude-cli --budget 1500` → 15 nodes, all unrelated check-fixture READMEs — no signal; G2 grep census above is the authority.)

**Why wrong.** The plan's own instrument (§4.6) cannot reach the actual contact: the conditional tests a predicate ("asserts absence") that is false, in a file (the wrapper) that holds no assertions, so the coder-wave lands a committed test contract carrying a now-false factual rationale — exactly the stale-reference class the planner role is charged to name, and a violation of the state-query duty (the contact was fully measurable at plan time) and of `enumerate-encoding-surfaces` (a TEST surface encoding the fixture's expected docs/project state is not updated in lock-step with the state change). Secondary effect the plan never names: post-c3, contract-migration.sh becomes a de-facto end-to-end instrument (the full accounting/synthesis/TRIAGE pipeline runs inside c3's gate against the built persona, BEFORE c4's T5.a) — the coder should know this is expected, not incidental.

**Minimal correction.** Add `scripts/persona-contracts/contract-migration.sh` to the c3 file set with a spec line: reconcile the L504–513 rationale comment to the post-P9 reality and (now that the stated blocker is gone) add the `_toc.md`/`_index.md`/`MIGRATION-TRIAGE.md` presence asserts — or record a measured zero-edit justification; drop the wrapper-named conditional.

### F-2 — SHOULD — PE-7's "A1 leaves every existing wired test green" is broader than its evidence: the H1-break and fence behavior changes apply to ALL streams, and PE-7 measured only project-changelog date shapes

**Plan claim (quoted, §0 PE-7).** "`grep -rn "2026-\d\d-\d\d-" scripts/tests/ test-fixtures/build.sh` → 3 relevant hits … `grep -rn "### 20" …` → hits only in `-decompose.sh` … CONCLUSION: the A1 commit (P1–P4, P11, P12) leaves every existing wired test green; no bare-date heading exists in any test fixture."

**Evidence.** P1's H1-break (§2.1.3) and fence-state (§2.1.1) change the walker for EVERY stream, and `per_entry_decompose` has SIX wired non-migrator consumers the date-greps cannot see: `grep -rln "per_entry_decompose" scripts/tests/ scripts/*.sh` → `test-per-entry.sh`, `test-migrate-v10-to-v11-decompose.sh`, `tracker-bd204-lossless-roundtrip-test.sh` (allowlisted OUT), `tracker-migrate-roundtrip-test.sh`, `tracker-bd134-close-retry-test.sh`, `tracker-migrate-forward-test.sh` (pack-backlog decompose at L110/L1819/L2048/L2231) — plus `test-fixtures/build.sh` L516. A mid-file `# ` line or a fence in any of those monolith fixtures would change A1 behavior; PE-7 never measured that axis. My independent census (awk heredoc scans + context reads of every call site's monolith input: `test-per-entry.sh` heredocs L76/127/148/170/318/422+, decompose-test heredocs L108/153/180, bd134 L159–188, roundtrip/forward BD-only prefilters + `BD108_MONO`/`CR_MONO`/`CY_MONO` heredocs, `build.sh` `.bd206-fixture-input.md` content L465–470 + `td_entries` L381–437, `scripts/tests/fixtures/tracker-migrate/BACKLOG.md` head) → every `# ` H1 is a LINE-1 title (pre-first-anchor preamble — H1-break inert) and ZERO fenced blocks exist in any decompose input. **The claim is TRUE — but the plan proves it only for the changelog-naming axis.**

**Why it matters.** An EEB conclusion ("every existing wired test green") resting on a measurement that does not cover the mechanism under change is the exact defect class `empirical-evidence-blocks` exists to stop; had any tracker fixture carried a mid-file H1, c1 would have failed its gate with no plan warning. **Minimal correction:** extend PE-7 with the H1/fence census over the `per_entry_decompose` consumer set (one grep/awk pass; results above can be cited).

### F-3 — SHOULD — §10's collision-delta EEB omits the plan's own conditional c3 contact with `scripts/tests/test-migrate-v10-to-v11-gates.sh`, a file in open BD-172's declared set (v11.0)

**Plan claims (quoted).** §10: "This plan ADDS exactly ONE touched file beyond the design's §4 blast radius, plus the Wave-D bookkeeping files"; §4.6: "`test-migrate-v10-to-v11-{dry-run,gates,interactive,pre-reconcile}.sh` green (banner/advisory-sensitive — any assertion contacted by P5/P6 wording changes is updated in THIS commit and reported)."

**Evidence.** `backlog/BD-172.md` File/Symbol (read this session): "`scripts/tests/test-migrate-v10-to-v11-gates.sh` — nine new test cases (3 helpers × 3 cases each …)"; BD-172 is `Status: Open`, "Lands in v11.0". The plan's §4.6 explicitly authorizes editing `-gates.sh` inside c3 when P5/P6 wording contacts it, yet §10's intersection records only `test-dry-run-migration.sh` + d1 bookkeeping as plan-added paths and asserts "delta introduces ZERO new co-editing conflicts."

**Why wrong.** A conditionally-authorized edit is part of the plan's blast radius for collision purposes; if it fires, c3 and BD-172 co-edit the same file inside the same minor version — a COORDINATE signal §10 never records (the design's BD-172 row covers only the checkpoint-verifier sequencing note). **Minimal correction:** add the conditional `-gates.sh` (and `-dry-run/-interactive/-pre-reconcile`) contact to §10 with a BD-172 COORDINATE note (serialize; the d1 BD-172 note already lands).

### F-4 — SHOULD — `test-fixtures/README.md`'s v10-realistic-ot row becomes a stale fixture description after P9, and it is in no commit's file list (and is in BD-171's declared set)

**Plan claim (quoted).** §1 c3 row "Files (complete)" (no `test-fixtures/README.md`); §8.1 NO-EDIT trailer and §10 do not mention it.

**Evidence.** `test-fixtures/README.md` L29 (read this session) describes `v10-realistic-ot` as: "Fake-OT shape: project-name fills (`FakeOT`); … 5-entry TD-* `BACKLOG.md`; trinity v10 self-label intact" — after P9 the persona additionally carries `docs/project/{BACKLOG.md, IMPLEMENTATION_PLAN.md, CHANGELOG.md}` with the D-1/D-2/D-3/D-7 hazard shapes; the row's content inventory is then incomplete/stale. `backlog/BD-171.md` File/Symbol includes "EXTEND `test-fixtures/README.md` table" — so an edit here is also a BD-171-set contact worth a COORDINATE note.

**Why it matters.** The planner brief explicitly includes "cross-references in docs that mention modified files"; this is the only fixture-description doc for the persona P9 rewrites. **Minimal correction:** add a one-line c3 spec item updating the L29 row (neutral vocabulary), and note the BD-171-set contact in §10.

### F-5 — NIT — §5.1's exit-code non-collision rationale mis-states the framework set: `EXIT_GATE_FAILED=31` exists outside the quoted "20..30" range

**Plan claim (quoted, §5.1.2).** "new local exit code `DRY_EXIT_SANDBOX_VERIFY=8` (does not collide with framework 10..16/20..30/99 or local 2/4/5/6/7 — PE-8)."

**Evidence.** `scripts/lib/migrator-core.sh` L68–79: `EXIT_PACK_INVALID=10` … `EXIT_ALREADY_MIGRATED=16`, `EXIT_GATE_FAILED=31`, `EXIT_INTERNAL=99`; fail_stage formula 20+N (e.g. rc=24 asserted at `test-migrate-v10-to-v11.sh` Group 5). The framework set includes 31, outside "20..30". The CONCLUSION (8 is collision-free) still holds — only the quoted range is wrong. **Minimal correction:** state the range as 10..16/20..31/99.

### F-6 — NIT — Wave D run (ii)'s failure action is implicit only

**Plan claim (quoted, §9.2).** "ACCEPTANCE: rc==0 under §5.1(a)–(e) … This run is a MEASURED precondition of the Resolved flip." No explicit red-path action is stated for run (ii) itself; §7.8 covers post-landing defects generically ("a `fix: v11 — BD-291 …` follow-up within the batch") and OI-P9 sequences T5.a first, but a coder/orchestrator reading §9 alone gets no named action for a red run (ii). **Minimal correction:** one sentence in §9.2 — on red, no flip; diagnose from the report, land the fix via the §7.8 in-batch `fix:` path, re-run (ii).

### F-7 — NIT — the bare-date fail-loud guard is specified against an anchor regex that cannot itself see the bare-date shape (bite-covered, but the spec invites a mis-wiring)

**Plan claim (quoted, §2.1.4).** "anchor detect `^### (\d{4}-\d{2}-\d{2}) — (.+)$` (date + MANDATORY ` — ` + suffix) … Fail-loud … (a) a `^### \d{4}-\d{2}-\d{2}` line with NO ` — ` suffix or an empty/unslugifiable suffix → error naming the monolith line number."

**Evidence.** Under the strict anchor regex, a bare `### 2026-01-01` line matches NEITHER the anchor NOR project-changelog's `section_break_re` (`^## `, current `decompose.sh` L176 region) — in a naive implementation it would glue in-span or fall to preamble, and guard (a) (if wired only inside `id_extract`) would never execute. The plan's P11 case 9 ("bare-date anchor → decompose FAILS naming the line") BITES this, so a mis-wiring cannot land — but the spec never says the guard must be a SEPARATE date-prefix classification evaluated before/alongside anchor matching. **Minimal correction:** one clause: "detect the `^### \d{4}-\d{2}-\d{2}` prefix independently of the strict anchor regex; the guard fires on prefix-match without a valid suffix."

## §3 Per-axis verdicts

**A. DESIGN COVERAGE — CLEAN (with F-1's file-list caveat).** Re-derived the full row→commit map independently: P1/P2/P3/P4/P11/P12→c1; C1/C2→c2; P5/P6/P9/P13/P16→c3; P7/P8+m4/m5→c4; P14→c5; P10→m*; P15→d1; C3→generated; m1–m3/r0–r2 in P12, s1–s5 in c2, P13 cases 1–7 incl. gate-refusal — 20/20 design rows, zero dropped, zero duplicated, zero re-scoped beyond the two declared deltas. NO-EDIT list honored (no commit touches `index-generate.sh`, `core.py`, `per_entry_sync.py`, `boundary_refs.py`, `init-project.sh`, `tracker-migrate-reverse.sh`, the pack `_rules.md`, the project `backlog/_rules.md`, `implementation-plan/_rules.md`, `groupings/_rules.md`, METHODOLOGY Part 7, `_intro.md` templates). OI-P2 evidence HOLDS: workflow L69–75 mandates fixture-dependent location cohesion; `test-dry-run-migration.sh` exists there and owns T1–T4 (read: content-tolerant, rc/report asserts). OI-P3 evidence HOLDS: `build.sh` v10 arm writes root `BACKLOG.md` only (L448–459); v11 decomposes only the backlog input (L516); P13's runner self-builds under /tmp (L44–50 verbatim); nothing in A1's tests needs P9. F-1 is the one coverage defect (an affected file missing from c3's "complete" list).

**B. LOCATOR + CONSTANT TRUTH — CLEAN (one NIT, F-5).** Spot-checked every load-bearing locator against the tree this session: `decompose.sh` 310 lines, extractor/walk/`os.replace`/summary regions as cited; `_lib.sh` slug-optional block ~L114–121 + entry-regex; `toc-regenerate.sh` project-changelog regex ~L98; `index-generate.sh` `parse_phase_refs`/`field_value`/L155–163 dependency folds; `migrate-v10-to-v11.sh` L205–260 S4a with all four asserted substrings (`S4a (rename)`, `nothing to rename`, `git mv hint`, `renamed (untracked)`) + collision block; migrator `decompose.sh` 198 lines with spec table/skip/fail_stage/delete-gate/sourcing at the cited regions; `validate-docs.sh` 2588 lines, `--self-test` L62/L85, `_conf_check_backlog_entry` L602, groupings misname L1407, `run_selftest` L1924, conformance self-test L2054+, `good_td` L2080–2092 (`Marker: TODO` + `Scope: feature`), walk-`continue` L1618–1630, `_CONF_ENTRY_REGEX["changelog"]` L503, grep-zero on `severity-enum|scope-enum|payload` legs; `changelog/_rules.md` 94 lines, L28–29 fourth example + L61–70 three mapping examples; `MIGRATION-v10-to-v11.md` 887 lines, all six P14 anchors at exactly L314/L325/L336/L359/L370/L492/L619; `DRY-RUN-MIGRATION.md` section headers exact; harness exit codes 0/2/4/5/6/7 + `--report-out/--tmp-dir/--pack` + L395–397 `--dry-run`-only; `manifest-inputs.sh` INPUT/DENY globs verbatim; `fixtures.py` Check 62 structural-only docstring L196–208; `boundary_refs.py` L224/L236–238 + `PACK_CHECK_36_RANGE`; workflow header + `ci-shard-plan.py` `DEFAULT_WEIGHT_S=3.0` L91 + allowlist file (1 entry); `pe_strip_backpointer_stdin` L290; `test-pm-dashboard-render.sh` L184 slugged filename; decompose-test L44–50/L187/L195/L275; Group 5 at L555+ with rc=24. Constant audit: plan-wide grep → zero `403`, zero stale `65`, `68` only as 61+7 (OI-A3 recap), `72 = 61+7+4` consistent at §0/§8.2/§9.2, carrier sets 4/0/13 with TD-057/058/070/071 by name. Current-extractor regex re-derivation confirms PE-7's same-filename claim for `2026-04-15-phase-1-milestone.md` (backtracking forces the slug branch; whole-suffix rule yields the identical id).

**C. SEQUENCING + GREEN-BY-CONSTRUCTION — CLEAN (with F-2's evidence-gap SHOULD).** Check 62's structural-only scope verified in source ("deliberately does NOT assert SHA-CORRECTNESS"), so c2's and c3's in-workspace stale-SHA manifests keep validate-pack green while `--verify` diverges exactly as the plan declares, closed by the push-time chore (manifest-sync token semantics verified: SKIP/NOOP=0, CHANGED=10, union-range screen). c1's MANIFEST-NOOP expectation verified end-to-end: `scripts/lib/per-entry/*` are input-glob members (screen fires) but the only build-time decompose consumer is the v11 backlog input whose content (line-1 H1 title, no fences, no mid-file H1s, no project-changelog entries) is behaviorally inert under P1/P2/P3. A1 no-test-breaks re-proven beyond the plan's own evidence (F-2). c3's dual dependency (c1 capture/gate/id-model + c2 shipped-validator leg for the D-7 end-to-end case) is real and correctly serialized; c4/c5 after c3; run (ii) after the full battery. A1∥A2 workspace bases are pre-c1 for c2 — safe because the file sets are disjoint (patch applies cleanly post-c1).

**D. COMMIT HYGIENE — CLEAN.** All seven keyword claims re-derived against `_PROJECT_SIDE_PATH_PREFIXES = ("project-template/", "supporting-docs/")` and `_SCOPE_NEUTRAL_GENERATED_PATHS = {"test-fixtures/manifest.txt"}`: c1 (6× `scripts/`) pack-only VALID incl. the two NEW files (Check 36 keys on touched paths, not tracked-status); c2 (2× `project-template/`) project-only VALID; c3 (`scripts/`×4 + `test-fixtures/build.sh`) pack-only VALID; c4 (`scripts/`×2 + `pack-ops/`) pack-only VALID; c5 (`supporting-docs/`×1) project-only VALID; m* scope-neutral; d1 (`backlog/` + `maintenance-docs/`) pack-only VALID. Subject shapes match the approved `feat:`/`docs:` forms and the landed precedents (`0c2709e`, `fa06e36` verified in `git log`); the `chore:` shape is correctly surfaced as OI-P4 rather than silently assumed; exactly one keyword per subject; HEAD-only walk + the `PACK_CHECK_36_RANGE` audit option verified in source.

**E. CYCLE MECHANICS — CLEAN.** §7's ritual matches the LANDED contract verbatim — the current pack-root `CLAUDE.md` §Sub-agent behavior and `pack-ops/OPTIONAL-FEATURES.md` L110–135/L255–275 both specify the orchestrator-created out-of-prefix commit workspace (`git worktree add --detach <WS> HEAD`), injected absolute path, `cd <WS> &&` fusion, no up-front patch, SendMessage patch-emit after review-clean, teardown only after confirmed landing, failed-commit workspace retention. New-file conveyance enumeration verified complete: only c1 carries untracked files (`accounting.sh`, `test-per-entry-fidelity.sh` — both find-unique at `0c2709e`, re-run); c3's TRIAGE/personas are runtime-`/tmp`/gitignored artifacts (CI header L66–68), c4's mutation hooks are test-runtime `mktemp` scripts, c2/c5 edit tracked files only, d1 is orchestrator-placed. A1∥A2 disjointness re-derived: c1 ∩ c2 = ∅.

**F. VERIFICATION GATES — CLEAN (one NIT, F-6).** Every commit's gate names validate-pack + DEEP + fixture build/`--verify` (with the two declared divergence states and their closure path) + the full wired set (the disk-derived KEEP set minus the 1-entry allowlist — glob shape verified against the allowlist header) + commit-specific legs; §9.1 re-runs on HEAD + pushed CI. Bites all verified to bite a regressable mechanism: m1/m2/m3 hit the three multiset directions of the gate; r0–r2 bite reduced-mode in both `declare-verify-backing` directions; m4/m5 (T5.b/c) bite the harness's §5.1(b)/(d) records-style legs through the REAL legs (OI-P6's hook avoids re-implementation forks); s1–s5 bite the two new validator legs incl. both reached-assertions and the phase-N template PASS direction; P13 case 6 bites the delete-gate; P11 cases 1–10 map one-to-one onto P1's five edit intents. Auto-wiring verified: matrix is disk-derived (`--emit-matrix`), `DEFAULT_WEIGHT_S=3.0` covers the un-weighted new test, fixture-dependent cohesion pins the extended harness test to the fixture-owning shard. Wave-D acceptance is mechanically specified (exact command, rc==0, per-stream PASS + set-equality report sections, 72 as re-derived expected cardinality); only the red-path action is implicit (F-6).

**G. CROSS-BD + CLOSE-OUT — CLEAN (two SHOULDs, F-3/F-4).** Open set re-verified: 17 `Status: Open` BD files, identical list to PE-16. BD-289's File/Symbol re-read: names Check 95/96 surfaces + "`supporting-docs/` (the 45 bareness qualifications)" — no `maintenance-docs/` membership; no plan commit touches `core.py` or adds a check (ledger/IDs untouched). BD-171's declared set re-read: ∅ overlap with the plan's files; non-absorption preserved. BD-172's `_cp_verify_bd104_rename_outcome` verified in its entry (the d1 note is accurate); BD-254 is the live graphify-adoption anchor (OI-A14 note fits). Wave D is complete and correctly gated: battery → user-approved run (ii) as the flip's MEASURED precondition → d1 (preservation + flip + `_toc.md` regen + BD-172/BD-254 notes) → OI-A11 relay (present, §9.5) → launch-note naming the USER-run real migration (§9.6, matching BD-291's Unblocks line). The two SHOULDs are delta-EEB completeness gaps (conditional `-gates.sh` ∩ BD-172; `test-fixtures/README.md` ∩ BD-171), not conflicts.

**H. OPEN-ITEM QUALITY — CLEAN.** All nine OI-P items carry context + the planner's own options + an evidence-cited recommendation; none recommends from memory; none defers work out of v11.0 (OI-P3 re-sequences within the batch; OI-P9's contingency lands in c4's bounded cycle). OI-P5 verified against `no_leak.py` source: leg 1 scans EVERY git-tracked file (docstring + L97: "leg 1 scans every git-tracked file, THIS module included") with an EMPTY leg-1 allowlist, and the census artifacts measurably carry the banned literal (grep -ci → 15/4/3 hits across the three files) — committing them to `maintenance-docs/` would fail Check 93; option (a) is the only clean option, correctly recommended. OI-P9 is coherent with OI-A4: the harness compares DERIVED sets (never hard-codes 72); the fixture's own class set is exercised at T5.a first, so a fixture-shell surprise surfaces in the cheap loop, with the fix landing inside c4's cycle — no allowlist, no deferral.

## §4 Open items (context → options → recommendation)

- **AOI-1 — scope of the F-1 correction: reconcile-only vs reconcile+assert.** Context: once `contract-migration.sh`'s stale rationale is fixed, its own stated reason for omitting `_toc.md` asserts is gone; the contract could now assert the migrator's full new output surface (`_toc.md` ×3, `_index.md`, `MIGRATION-TRIAGE.md`) on the canonical fixture, duplicating part of c4's T5.a at the contract layer. Options: (a) reconcile the comment only (minimal, keeps the contract's original scope: install-surface assertions); (b) reconcile + add the presence asserts (the contract becomes a second independent instrument of the c3 pipeline on the built persona, at near-zero runtime — the migrator already runs there). Recommendation: **(b)** — `enumerate-encoding-surfaces` favors symmetric coverage, the asserts are presence-only (cheap, deterministic), and they convert the contract's accidental post-c3 pipeline execution (F-1 evidence item 2) into deliberate coverage; the fix belongs in c3 (same commit as the state change), not a later batch.
- **AOI-2 — where the `test-fixtures/README.md` row update lands.** Context: F-4's stale-row fix touches a file in BD-171's declared set. Options: (a) fold into c3 (the commit that changes the fixture content; one table-cell edit; pack-only keyword unaffected — `test-fixtures/` is not a project-side prefix); (b) leave to BD-171 (which plans to extend the same table). Recommendation: **(a)** — (b) is deferral of unblocked work to another BD (excluded by the deferral rules); the BD-171 contact is note-grade (different row/purpose), recorded in §10 per F-3's pattern.
- No other open items: every remaining question I hit was state-verifiable and is answered above with measurements (nothing requires a MAINTAINER CHECK).

## §5 Rules-Applied Verification Block

1. **agents-never-commit** — Evidence: git this session = `rev-parse HEAD`, `worktree list`, `status --porcelain`, `log --oneline -8` — all read-only, all in this worktree; the platform additionally REFUSED one compound for-loop command ("worktree-isolated agent … Refusing to run", captured in-transcript), confirming enforcement; zero state-changing verbs; no patch emitted (RO). **COMPLIANT**
2. **per-action-approval-sub-agents** — Evidence: writes this session = `ADVERSARIAL-PLAN-REVIEW-BD-291.md` in the owned dir `…/bd291-advplan-20260825T230325Z/` ONLY (1 Write + 1 Edit-append); all four input handoff dirs read-only; nothing deleted anywhere; no scratchpad files needed. **COMPLIANT**
3. **preflight-stop-means-stop** — Evidence: emitted `PREFLIGHT: adversarial plan pass complete; about to Write ADVERSARIAL-PLAN-REVIEW-BD-291.md to /Users/david/.local/state/optiquity-pack-handoff/bd291-advplan-20260825T230325Z` in the message immediately preceding the Write; no stop message received; writes chunked (2 calls, each ≤~300 lines). **COMPLIANT**
4. **graph-first-context** — Evidence: my one genuinely NEW discovery question (surfaces encoding the v10-realistic-ot persona's content) ran `graphify query … --graph /Users/david/Developer/optiquity-ai-agent-config-pack/graphify-out/graph.json --backend claude-cli --budget 1500` → 15 nodes, all unrelated check-fixture README headings — no signal; per G2 the grep census (`grep -rln "v10-realistic-ot"` → 11 files, each classified) is the authority. All other reads were P2 verification reads of surfaces the plan/design had already named (carve-out (i)); the `per_entry_decompose` consumer census verified the design's own E-5 enumeration (carve-out (i) — an already-identified set). Injected path used verbatim; never recomputed. **COMPLIANT**
5. **empirical-evidence-blocks** — Evidence: every finding and every axis verdict above quotes the command + captured output (file, line, verbatim text, counts) at HEAD `0c2709e`, 2026-08-25, including my REFUTATION-side measurements (the H1/fence census that CONFIRMS the plan's greenness claim; the keyword re-derivations; the exit-code table). **COMPLIANT**
6. **ci-guard-measure-then-bound + declare-verify-backing** — Evidence: axis B re-measured the carrier sets' encoding surfaces independently (validator grep-zero on payload legs; `good_td` bytes; `_CONF_ENTRY_REGEX`); axis F verified each mutation-bite maps to a regressable mechanism rather than accepting the plan's assertion (m1–m3 = the three multiset directions; m4/m5 through the REAL legs via the hook seam; s1–s5 incl. both reached-assertions); F-1/F-7 are exactly absence-of-backing catches (a hedge whose predicate reaches nothing; a guard whose host regex cannot see its trigger). **COMPLIANT**
7. **verify-full-ci-suite** — Evidence: axis F verdict — checked each commit gate (§2.7/§3.3/§4.6/§5.1/§5.2/§9.1) names validate-pack + DEEP (the `validate` job's two runs), the disk-derived wired test set incl. fixture-dependent-after-build (the `tests` job's substance), and the manifest `--verify` leg; verified the wired-set glob shape and the 1-entry allowlist against `scripts/ci-test-wiring-allowlist.txt`. **COMPLIANT**
8. **operating-docs-no-history-no-bloat** — Evidence: audited the plan's doc-edit intents — §2.1.6/§2.2 (present-tense headers, comment replaced with a one-line contract pointer), §3.1 C1, §5.1 P8 (OI-P6 keeps the test seam out of the operator doc), §5.2 P14 ("PROCEDURAL, present-tense, zero incident narration"), §8.2 item 9 reviewer grep — all preserve the constraint; F-1's correction direction likewise reconciles a stale comment rather than annotating history around it. **COMPLIANT**
9. **dependency-direction-placement** — Evidence: audited §1's file lists — `accounting.sh` pack-side (migrator runtime dep, never shipped; no commit touches the install map or `_SANCTIONED_PACK_SIDE_SHIPPED`); MIGRATION-TRIAGE.md migrator-GENERATED client output (no template file); pack/client twins separate (C2 vs P-rows); zero dual-use files in any commit. **COMPLIANT**
10. **ci-check-runtime-compounding** — Evidence: verified the plan adds NO validate-pack check (§8.1 NO-EDIT row; `core.py` untouched); §2.4 gate is O(lines)/Counter/no-subprocess-per-entry per spec; §3.2/§3.2b legs ride the existing conformance pass; T5's 3× migrator runs are confined to the fixture-owning shard; my F-1 AOI-1(b) recommendation adds presence-asserts only (O(1) stat calls) to a contract that already runs the migrator. **COMPLIANT**
11. **open-item-surfacing** — Evidence: §4 above — both open items carry context, my own options, and an evidence-based recommendation; neither defers work (AOI-1 lands in c3; AOI-2 explicitly rejects the defer-to-BD-171 option); axis H verified the plan's nine items meet the same contract. **COMPLIANT**
12. **memory-not-an-ssot** — Evidence: every rule/contract claim above cites a live in-repo path read THIS run (pack-root `CLAUDE.md` §Sub-agent behavior, `pack-ops/OPTIONAL-FEATURES.md`, `pack-ops/OPERATING-MODES.md`, `boundary_refs.py`, `no_leak.py`, `fixtures.py`, `manifest-sync.sh`/`manifest-inputs.sh`, the per-entry libs, the migrator + `migrator-core.sh`, `validate-docs.sh`, the four `_rules.md`/doc anchors, `backlog/BD-{171,172,254,289,291}.md`, `.claude/skills/{planning,commit-discipline,architecture-review}/SKILL.md`) or a handoff artifact by absolute path; zero claims from CLI memory. **COMPLIANT**
13. **public-bound-no-leak** — Evidence: this report lives out-of-repo; target-app tokens appear only as the injected read-only checkout path and grep COUNTS against census artifacts (no monolith content quoted); every fixture-content discussion uses the repo's own neutral vocabulary (`FakeOT`, synthetic TD text) quoted from committed files. **COMPLIANT**
14. **no-deferral-without-user-direction + deferral-is-scope-creep** — Evidence: audited the plan for silent deferral — none found (§8.1 20/20 rows in v11.0 commits; Wave D's "later" items are user-owned by design); my own corrections all land inside the existing batch (F-1/F-4 in c3, F-3/F-5/F-6/F-7 as plan-text amendments; AOI-2 rejects deferral to BD-171). **COMPLIANT**
15. **rules-applied-verification-block** — Evidence: this block — 15/15 rules from the spawn prompt's "Rules in force" list, each with quoted evidence and a terminal conclusion; it closes the document. **COMPLIANT**

