# RECONCILIATION-BD-290 — adjudication of the adversarial review against the BD-290 design

**Agent:** architect-bd290-reconcile (pack-architect, RO; fresh instance — neither the design's author nor the adversarial reviewer). **Date:** 2026-08-25.
**Tree:** /Users/david/Developer/optiquity-ai-agent-config-pack; `pwd` verified; HEAD `3cf436ec46433556a218e086364871882c84490f` (matches expected 3cf436e); working tree ` M pack-ops/session-state.json` only (orchestrator-owned).
**Adjudicated:** design `…/bd290-architect-20260825T154529Z/ARCHITECTURE-BD-290.md` (read in full, §0–§12) vs adversarial review `…/bd290-advarch-20260825T161351Z/ADVERSARIAL-REVIEW-BD-290.md` (read in full; 0 BLOCKER / 5 MUST / 6 SHOULD / 4 NIT). Evidence base read: `backlog/BD-290.md`; census / probe 1 / probe 2 IMPL-REPORTs (targeted re-reads of every passage either party cites).
**Method:** every finding re-measured against the tree THIS run — no ruling rests on either party's word. EEB-n references below are to the RECONCILED design's §0 ledger (same dir), where each measurement's full command + output is quoted.
**Reconciled design:** `ARCHITECTURE-BD-290.md` (same dir) — complete, self-contained, all ACCEPTed corrections folded.

---

## §1 Adjudication table

| ID | Sev | Ruling | My independent evidence (command → captured result) | Design delta |
|---|---|---|---|---|
| M-1 | MUST | **ACCEPT** | `sed -n '108,150p' pack-ops/OPTIONAL-FEATURES.md` → :122–123 "the fix-coder REUSES the same worktree", :130 "they cd in + verify pwd/HEAD", :134–139 "could then not cd-REUSE the first coder's worktree" — retired-contract text on a pack operating surface; no original E-P4 row's range touches :117–140, while E-C8 rewrites the exact client twin (EEB-20-a). | New row **E-P4-f** (§4.1): rewrite :117–140 to the workspace model, mirroring E-C8; W1 scope updated (§10). |
| M-2 | MUST | **ACCEPT** | `sed -n '44,55p' project-template/docs/pack/prompts/reviewer.md` → :48–51 "`cd` into that worktree and VERIFY your pwd/HEAD… before reviewing"; `sed -n '25,35p' .claude/skills/implementation-report/SKILL.md` → :29–31 `worktree-agent-*` base-HEAD semantics; `cmp` → `.codex`/`.agents` copies byte-IDENTICAL (EEB-20-b/c). Census grep: neither file appears in the census report. My own closure sweep (graph-first, mis-seeded, then corrected `-E` greps — EEB-10, EEB-16) found NO third stratum: the only additional hits classify CLEAN (`boundary_refs.py:4670` unrelated "REUSES" sense; dashboard render scripts cosmetic; prompts/pm-chat.md:462 Swift "Domain isolation" false positive). | New rows **E-IR** (implementation-report ×3, W1, Check-71 same-commit) and **E-C12** (reviewer.md, W2); EEB-10 rebuilt as the closed carrier universe (§0). |
| M-3 | MUST | **ACCEPT** | `grep -n "Check 71\|check_pack_skill_mirror_identity\|_CHECK_71_SKILL_MIRROR_DIRS" scripts/lib/validate_checks/boundary_refs.py` → `:4660` Check 71 header, `:4671` `_CHECK_71_SKILL_MIRROR_DIRS = (".claude/skills", ".codex/skills", ".agents/skills")`, `:4680` the check function (EEB-19). The design's "no such check exists in `validate_checks/`" is factually false. | §6 Guard 2 claim corrected; 6-file tuple KEPT with an accurate defense-in-depth rationale (adversarial OI-R2 option 2 — the 2 transitively-covered reads are O(file) and keep the leg self-contained); **Check 71 named as a W1 commit-boundary constraint** for E-P9/E-IR/E-CD (§4.1, §9, §10). |
| M-4 | MUST | **ACCEPT** | Probe 1 §4 re-read verbatim: E1/E2 — a git-free, cd-free `echo "GIT_DIR=$GIT_DIR …"` BLOCKED twice with the "too complex to verify" denial (shape matcher false-positives measured). Probe 2 Step D: WT_OUT's path was the SESSION SCRATCHPAD (`/private/tmp/claude-501/…/scratchpad/…`), not the `~/.local/state` class §3 ships; only plain `rev-parse`/`log`/`status` forms were run — no battery-shaped or patch-emit compound ever exercised (EEB-21). The original's "one unmeasured instance" undercounts by its own §3 text. | §3 W0 extended to legs (e) realistic battery invocation + (f) patch-emit compound, scratch worktree pinned to the actual workspace root; the gated set enumerated as FOUR instances; §5 W0 row and §11 OI-A updated. |
| M-5 | MUST | **DRAFT-FOR-USER** | `backlog/BD-290.md:12` verbatim: "…a read-only agent spawns isolated, `cd`s to the target tree, runs read-only git verbs and writes its report…" (EEB-22) — the parenthetical instructs the measured-illegal operation (probe 1: 11/11 blocked) and can never pass as written. The finding is correct; the RESOLUTION is user governance, not a design edit. | §11 **OI-H**: exact replacement parenthetical drafted (verbatim in the design) for user approval; W4's Resolved flip GATED on the approved amendment (§10 W4, §4.1 ORCH row). NO repo edit made or planned by any agent. |
| S-1 | SHOULD | **ACCEPT** | Ran EEB-14's command as originally quoted (`git grep -cn "only in its own worktree\|…"` without `-E`) → exit 1, guaranteed-zero (literal pipe). Corrected `git grep -in "only in its own worktree"` → exit 1 (conclusion survives). Corrected EEB-18 (`git grep -lnE "isolation_mode|isolation:" -- supporting-docs/`) → `AGENT_KICKOFF_TEMPLATE.md`, inspected :117 "Domain isolation" = Swift-architecture vocabulary, false positive (conclusion survives on inspection). EEB-10 as quoted was elided/unreproducible; its recall misses are exactly what produced M-1/M-2. | §0: EEB-14 and EEB-18 REPAIRED with the real runnable commands + captured output; EEB-10 REBUILT from this run's corrected sweep with full classification. |
| S-2 | SHOULD | **ACCEPT** | Pattern 1 misses the other retired families — verified: `pack-ops/PACK-CHAT.md:335–340` "the RO agent `cd`s to the work's tree"; reuse-family grep → pack-coder ×3, `CLAUDE.md:476`, OPTIONAL-FEATURES :123/:138, PACK-CHAT :313/:321 (EEB-23). Sizing for the adversary's proposed `cd-REUSE` pattern measured by me: record streams carry NO literal `cd-REUSE` (backlog/changelog/maintenance-docs grep → only unrelated "REUSES" senses); all live literal carriers are in the edit map; `dashboard.html` is outside `_CHECK_53_SCAN_SUFFIXES` (`:695`). Post-W1 tracked scanned hits = ∅ → green-by-construction. | §6 Guard 1 gains **pattern 2 `cd-REUSE`** (adversarial OI-R1 option 1) with its own measure-then-bound block (EEB-23); the deliberate recall bound for the broader prose families RECORDED in §6 (a general pattern would false-positive on surviving legitimate prose and on measured unrelated "REUSES" senses); test-53 extension covers both patterns (§5). |
| S-3 | SHOULD | **ACCEPT** | Original §2 invariant reads "only in trees outside the main checkout's path"; E-P1-a row reads "only in the agent's own worktree or the injected commit workspace" — neither contains the literal `only in its own worktree`; `git grep -in` → 0 at HEAD (EEB-14). A check sized to bytes no draft carries invites a hollow-green or a W3-vs-W1 mismatch. | §2 invariant REDRAFTED to carry the exact token bytes ("runs git only in its own worktree or the orchestrator-injected commit workspace"); E-P1-a/E-P9/E-C1/E-C6 row texts specified token-bearing verbatim; §6 fixes casing (lowercase) at design level — no planner-time alignment. |
| S-4 | SHOULD | **ACCEPT** | W-V1 as originally specified proves param-absent→deny / param-present→allow for PER-SPAWN parameters only; no probe spawned a frontmatter-pinned def; the hooks reference documents neither (census §4.3). "A def-level pin is invisible to the payload check" is an unmeasured platform claim about the same undocumented field the design itself flags version-fragile (§8-4). | §4 **no-pin wording note**: all surfaces carry ONLY the measured claim; §5 W-V1 gains a **pinned-def leg** (temporary untracked scratch def, per-action user approval, orchestrator-created/removed) to convert the inference; doc wording independent of the leg's outcome. |
| S-5 | SHOULD | **ACCEPT** | `grep -a -o -n "REUSES[^.]*" pack-ops/dashboard-approvals/dashboard.html` → `110:REUSES that worktree, NEVER a new one`; `git ls-files` → tracked; `dashboard-shell.html` → 0 occurrences; `scripts/dashboard-render.py:374` — rules panel is "A live mirror of CLAUDE.md § Pack memory"; `.html` outside `_CHECK_53_SCAN_SUFFIXES` (EEB-25). A tracked launch-tree file would carry the retired text post-W1 with no wave refreshing it. | §10 **W4 gains the dashboard re-render step** (mechanical regeneration, commit with user approval, `pack-only`); §4.1 NO-EDIT row documents the render-machinery disposition; §9 blast-radius set includes `dashboard.html`. |
| S-6 | SHOULD | **ACCEPT** | `grep -n -B1 -A2 "isolated worktree" pack-ops/PACK-MEMORY-RATIONALE.md` → `:33–34` "its worktree edits (held in the commit's isolated worktree). The `git diff` patch is NOT emitted up front…" — a rationale body restating the old flow, at HEAD, today (EEB-26). The "if" was one grep from resolution. | E-P2's lockstep edit made **DEFINITE**, citing the :33–34 carrier; `PACK-MEMORY-RATIONALE.md` named in W1's scope and the §9 set. |
| N-1 | NIT | **ACCEPT** | `discipline_parity.py:892–895` — existing `_CHECK_54_OPTIONAL_FEATURES_SURFACES` = exactly 2 files (the two OPTIONAL-FEATURES); zero overlap with the 6 proposed mode-contract surfaces ⇒ 6 net-new reads, not 4 (EEB-24, EEB-12). | §12 rule-12 evidence corrected to **6** net-new single-file reads; §6 states the count with its measurement. |
| N-2 | NIT | **ACCEPT** | `core.py` `__all__` header (:479–) mandates every seam be listed; `:519` carries `"_CHECK_54_REQUIRED_TOKENS"`; `discipline_parity.py:58` imports via `from .core import (…)` (EEB-24). An unexported new tuple = ImportError at W3. | §6 Guard 2 spec now names the **`__all__` export step** explicitly; §10 W3 scope updated. |
| N-3 | NIT | **ACCEPT** | `project-template/.claude/agents/repo-ops.md:46` "fix-coder reuse. See…" (carrier past the mapped :28–45); `.claude/skills/commit-discipline/SKILL.md:78` + `:104` write-under-`pwd` directives outside the mapped :30–55; Check 54's existing tokens (`core.py:391–395`: `baseRef`/`bgIsolation`/`permissions.deny`) live inside the OPTIONAL-FEATURES sections being edited (EEB-27, EEB-12). | §4 **phrase-sweep rule** (binding, cross-row): line numbers are locators; whole-file sweep for the full retired-token family + post-edit re-verify of Check 54's existing tokens; E-C11 range corrected to :28–48; E-CD scope widened to the whole file. |
| N-4 | NIT | **ACCEPT** | `git grep -n "live worktree"` (record streams excluded) → trinity `CLAUDE.md` (rule 9 + RO-class bullet), 12 pack RO defs, commit-discipline ×3 at :40/:73/:95 (EEB-28). Sentences stay executable (the workspace IS a worktree) — terminology drift, exactly as the adversary classified it. | §4 **vocabulary rule**: one term ("commit workspace") decided at design level and applied in one pass; rule-9 trigger wording specified. |

---

## §2 Counts and consolidated user-decision list

**Rulings: 14 ACCEPT · 1 DRAFT-FOR-USER (M-5) · 0 REJECT.**

No finding was refuted: every one of the 15 re-measured TRUE against the tree at HEAD 3cf436e. No compromise rulings were made — where I diverged from a correction *direction* it was on my own evidence (e.g. keeping Guard 2's 6-file tuple after correcting M-3's false premise, per the measured cost/benefit; adopting OI-R1's pattern only after independently sizing its post-fix hit set to ∅).

**Items requiring USER decision at the design review** (consolidated; each carries context + options + recommendation in the reconciled design §11):

1. **OI-B — mode-invariant workspace** (replace the cycle tree in BOTH modes, not just `full`). Recommendation: yes — the default mode's fix-coder verify step is measured-illegal today (EEB-3); a `full`-only workspace doubles the contract surface.
2. **OI-F — `permissions.deny` posture** for the guard-uncovered out-of-prefix workspace. Recommendation: honest-bounds caveat only; the REQUIRED-recipe option is surfaced as a user-settings policy choice the pack deliberately does not write.
3. **OI-G — dogfood the workspace ritual** for BD-290's own W1–W3 coder cycles (decided at the W1 spawn gate). Recommendation: yes — running the measured-illegal old ritual to implement its replacement is self-defeating.
4. **OI-H (from M-5) — BD-290 AC amendment**, exact replacement parenthetical drafted in §11, approved BEFORE the W4 Resolved flip. Recommendation: approve — it aligns the AC's letter with the platform-legal probe (W-V2) and removes the pipeline's only silent-reinterpretation step. Pack-chat-only edit; no agent touches the entry.

(OI-A/OI-C/OI-D/OI-E are architect-resolved with recommendations standing; no user gate beyond the normal design review. W-V1's pinned-def leg carries one small per-action approval — a temporary untracked scratch def — noted in §5.)

---

## §3 Verdict on design readiness

**READY FOR USER REVIEW.** The core architecture was never in question — the adversary's own verdict, which I independently confirm: root cause grounded (EEB-2/-3/-6 re-cited; EEB-8 re-measured to the same 8 files), the workspace model the only alternative surviving the reviewer-battery requirement, axes E (containment) and F (cross-BD) clean under two independent derivations plus my §9 re-attestation of the 17-entry Open set. The 14 accepted corrections are folded: the edit map now covers the full measured carrier universe (closure sweep run and quoted, EEB-10); the guards are sized to measurements taken this run, with the false Check-71 claim corrected and the recall bound made explicit; the W0 gate now covers the complete unmeasured set including the two correctness-bearing command shapes; no EEB in the reconciled design quotes a command that does not run. The one governance divergence (AC letter vs design) is drafted for the user, gating the W4 flip. All corrections land inside the existing W0–W4 wave structure — no wave added, no work deferred out of v11.0.

---

## §4 Rules-Applied Verification Block

1. **agents-never-commit** — Evidence: complete git inventory this run: `git rev-parse HEAD` (×1), `git grep` (×12), `git ls-files` (×1), `git status` porcelain read — read-only verbs only, all in the canonical checkout; zero state-changing git verbs in any tree; working tree at end = ` M pack-ops/session-state.json` (orchestrator-owned, untouched). Conclusion: **COMPLIANT**.
2. **per-action-approval-sub-agents** — Evidence: writes this run = `ARCHITECTURE-BD-290.md` (1 Write + 2 Edit appends) + this file (1 Write), both inside the owned dir `/Users/david/.local/state/optiquity-pack-handoff/bd290-reconcile-20260825T163255Z/`; zero deletions/overwrites anywhere; no `rm`/`mv`/`truncate` invoked; the two input handoff dirs (`bd290-architect-…`, `bd290-advarch-…`) were READ-ONLY to me. Conclusion: **COMPLIANT**.
3. **preflight-stop-means-stop** — Evidence: emitted verbatim before the first Write: `PREFLIGHT: reconciliation complete; about to Write ARCHITECTURE-BD-290.md + RECONCILIATION-BD-290.md to /Users/david/.local/state/optiquity-pack-handoff/bd290-reconcile-20260825T163255Z`. No stop/halt message received at any point. Conclusion: **COMPLIANT**.
4. **graph-first-context** — Evidence: the run's one NEW discovery question (carrier-universe closure beyond both parties' sets) opened with `graphify query "isolation worktree contract carrier fix-coder reuse cd target tree reviewer prompt implementation report" --graph /Users/david/Developer/optiquity-ai-agent-config-pack/graphify-out/graph.json --backend claude-cli --budget 1500` → mis-seeded (19 nodes, resolve-merge-conflicts/PACK-FEEDBACK communities, captured in EEB-16) ⇒ G2 grep fallback. All other reads were named-surface verification of claims quoting specific files/lines (fall-through (i)/(ii)). Conclusion: **COMPLIANT**.
5. **empirical-evidence-blocks** — Evidence: every ruling in §1 quotes my own command + captured result; the reconciled design's §0 ledger (EEB-1..28) carries command/output/HEAD-date/verdict for every state claim, and the three EEBs flagged un-runnable were re-run in corrected form with real output (EEB-10/-14/-18). Conclusion: **COMPLIANT**.
6. **ci-guard-measure-then-bound** — Evidence: both Guard extensions re-measured from git-tracked enumeration this run — pattern 1: `git grep -lE "then .?cd.?(-s it)? to the target tree"` → the same 8 files; pattern 2: candidate + record-stream + suffix measurements (EEB-23, EEB-12: `.html` outside `_CHECK_53_SCAN_SUFFIXES`); allowlist loosening safety re-run (`-E` form → exit 1, EEB-13); Guard 2 token base = 0 (EEB-14), tuple sized 6 net-new (EEB-24); absence-of-backing direction verified for both guards (Guard 2 fails on token loss; Guard 1 mutation-bite specified in test-53 for both patterns). Conclusion: **COMPLIANT**.
7. **design-discipline-challenge** — Evidence: no split-the-difference ruling exists in §1 — each of the 15 rulings cites tree-measured evidence; the two places I exercised judgment beyond the adversary's direction (Guard-2 tuple kept at 6; battery-shape fail absorbable by decomposition inside W0) are argued from measured properties (O(file) read cost + seam self-containment; probe-1 contrast set), not compromise. Conclusion: **COMPLIANT**.
8. **verify-availability-not-existence** — Evidence: the M-4 adjudication makes the W0 gate list COMPLETE per the probes' actual coverage (EEB-21: shape false-positive verbatim; WT_OUT path class captured); the S-4 adjudication strips the unmeasured pin-invisibility claim from all doc wording and adds the measuring leg; every capability the folded design uses cites its 2.1.245 measurement or is inside the W0/W-V1 gate set. Conclusion: **COMPLIANT**.
9. **declare-verify-backing** — Evidence: verified the folded guards BITE — Guard 1 post-fix hit-set-vs-allowlist walked concretely for both patterns ({backlog/BD-290.md} ⊂ allowlist; ∅ for `cd-REUSE` on scanned suffixes); Guard 2's deny direction matches Check 54's implemented fail path with Check 71 byte-locking the mirrors (EEB-19); W-V1's deny-path proof retained as the hook's load-bearing wire test. Conclusion: **COMPLIANT**.
10. **dependency-direction-placement** — Evidence: the folded edit map ships no new file, keeps pack/client copies separate (E-P9 vs E-C6, E-IR pack-side, E-C12 client-side), adds nothing to `_SANCTIONED_PACK_SIDE_SHIPPED`, and makes no client deliverable a pack runtime dependency (the workspace roots are separate per-side conventions). Conclusion: **COMPLIANT**.
11. **ci-check-runtime-compounding** — Evidence: folded corrections keep Guard 1 as two regexes on the existing single `git ls-files` pass (no new subprocess/walk — verified in `discipline_parity.py` source) and Guard 2 as 6 bounded single-file substring reads (EEB-24); no folded correction introduces a whole-tree walk or per-entry subprocess. Conclusion: **COMPLIANT**.
12. **operating-docs-no-history-no-bloat** — Evidence: every NEW/changed edit row drafted in the reconciled design (E-P4-f, E-IR, E-C12, no-pin note text, vocabulary rule, E-P1-a row text) was authored and re-read history-free — zero version markers, bug numbers, BD provenance, or deferred-feature mentions in any proposed surface text (the design docs themselves are out-of-repo reference artifacts). Conclusion: **COMPLIANT**.
13. **fail-loud-delete-old-source** — Evidence: the folded design DELETES the retired text from every measured carrier (EEB-10 closure; M-1/M-2 carriers now mapped), archives nothing, flip-blocks two families, and reconciles single-stale-element docs in place (E-P4-b/c, Check-4 docstring). Conclusion: **COMPLIANT**.
14. **architect-doc-reality-reconciliation** — Evidence: the reconciled §12-14 preserves and extends the chain — check-header comments by file+symbol (no line numbers), the `maintenance-docs/v11-implementation/` addendum home, two-way IMPL-REPORT cross-references. Conclusion: **COMPLIANT**.
15. **open-item-surfacing** — Evidence: §11 OI-A..OI-H each carry context + options + an evidence-based recommendation; the DRAFT-FOR-USER item ships the exact proposed text, not a deferral; §2 above consolidates the user-decision list in one place; no item recommends from memory or moves work to another BD. Conclusion: **COMPLIANT**.
16. **memory-not-an-ssot** — Evidence: every rule/contract claim in both outputs cites a live in-repo path read this run (`discipline_parity.py:695/:700/:892–895`, `boundary_refs.py:4660–4680`, `core.py:209–210/:391–395/:519`, `modes-enforce.py:17/:50/:84/:116/:124`, `singletons.py:273/:277/:285`, `pack-ops/OPTIONAL-FEATURES.md:108–150`, `pack-ops/PACK-CHAT.md:313–340`, `pack-ops/PACK-MEMORY-RATIONALE.md:33–34`, `backlog/BD-290.md:12`, `backlog/_toc.md`, skill/def/prompt files) or a stage-1 artifact by path; no memory-cache claim is load-bearing. Conclusion: **COMPLIANT**.
17. **public-bound-no-leak** — Evidence: both outputs name no target-app or domain-vocabulary token; the near-tokens that appear ("Domain isolation", "SwiftUI") are verbatim quotes of internal-surface text inside evidence blocks, in out-of-repo handoff artifacts. Conclusion: **COMPLIANT**.
18. **no-deferral-without-user-direction + deferral-is-scope-creep** — Evidence: zero rulings resolve a finding by deferral — all 14 ACCEPTs land inside the existing v11.0 waves (mapping quoted in the design's §12-19); the DRAFT-FOR-USER ruling gates existing W4 work on user governance rather than moving any work later; the only post-v11.0 reference (BD-289) is pre-existing user-directed sequencing, untouched. Conclusion: **COMPLIANT**.
19. **rules-applied-verification-block** — Evidence: this block — 19 entries matching the prompt's rules-in-force list one-for-one, each with quoted/measured evidence and a terminal conclusion. Conclusion: **COMPLIANT**.

**Deliverables:** `/Users/david/.local/state/optiquity-pack-handoff/bd290-reconcile-20260825T163255Z/ARCHITECTURE-BD-290.md` (reconciled design, standalone) + this file (RO agent; owned-dir writes only; no patch; no repo edits).
