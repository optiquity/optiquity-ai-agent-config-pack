# RECONCILIATION ARCH REVIEW — BD-272 — final, coder-ready eradication design for the `pack td` cross-boundary violation

> **Part of the BD-272 design chain** (as-landed reference record) — see `backlog/BD-272.md`. Chain order: `RESEARCH-BD-272-BOUNDARY-BLAST-RADIUS.md` → `ARCHITECTURE-BD-272.md` → `ADVERSARIAL-ARCH-REVIEW-BD-272.md` → **RECONCILIATION ARCH REVIEW (this doc)** → `AUDIT-BD-272.md`. This is the authoritative reconciled design; the `pack td` eradication that shipped under BD-272 is its realized consumer.

**Status:** as-landed reference record (BD-272) · **Design HEAD:** `0d427f9` · **Branch:** v11-dev.
**Author:** `pack-architect` (fresh, INDEPENDENT reconciliation instance — NOT the original designer, NOT the adversarial reviewer).
**Date:** 2026-07-23.
**Canonical checkout read:** `/Users/david/Developer/optiquity-ai-agent-config-pack-v11-dev`.
**HEAD at read time:** `0d427f9` (verified: `git rev-parse --short HEAD` → `0d427f9`, branch `v11-dev`).
**Graph:** `/Users/david/Developer/optiquity-ai-agent-config-pack-v11-dev/graphify-out/graph.json` (25.9 MB present). This pass is VERIFICATION/PRECISION over surfaces already discovered by the three prior docs, so per `graph-first-context` P2 fall-through (i) — "a verification read of a named surface (exact bytes/counts, AFTER discovery named it)" — the correct tool is `git grep`/Read; every ruling below re-measures a NAMED surface. Discovery (graph-first) was already performed by the researcher/designer/adversary.
**Inputs reconciled (all three read in full):**
- Design — `maintenance-docs/v11-implementation/ARCHITECTURE-BD-272.md`
- Adversarial — `maintenance-docs/v11-implementation/ADVERSARIAL-ARCH-REVIEW-BD-272.md`
- Blast radius — `maintenance-docs/v11-implementation/RESEARCH-BD-272-BOUNDARY-BLAST-RADIUS.md`
**Deliverable:** ONE authoritative reconciled design — the original design AS AMENDED by the accepted findings. The user rules OI-1 (delete-now) and OI-2 (grain); a coder executes; I reconcile and do not edit the tree or make the value call.

---

## 0. Reconciliation verdict — all three findings ACCEPTED (2 fold-in, 1 with a scope amendment)

I independently re-ran every measurement behind F1/F2/F3 at HEAD `0d427f9` and did **not** defer to either prior doc. All three findings are **factually correct**. None flips the delete-now decision, the atomic-commit shape, or the BD sequencing — each reconciles cheaply.

| # | Sev | Ruling | One-line reconciling edit |
|---|---|---|---|
| **F1** | MUST | **ACCEPT** | Reclassify `dashboard.html` as a **TRACKED generated record** (Check-69/86-exempt; embeds BD-107 CLASS-3; regenerated by `scripts/dashboard-render.py`; never hand-edited) **and** add `':!pack-ops/dashboard-approvals/*'` to the §6.2 step-5 grep-to-zero gate. |
| **F2** | SHOULD | **ACCEPT (AMEND)** | Redefine OI-2 **Option X** to include a flat-file **reword of the surviving `promoted-to:`/`derived-from:`/`folded-into:` label mechanics** (they are deferred tracker-mode constructs — keeping them raw violates `operating-docs-no-history-no-bloat`); surface the label-coherence tradeoff in the OI-2 X-vs-Y ruling. |
| **F3** | NIT | **ACCEPT** | Drop the "README.md pack-chat-only" characterization for **E3**: only the README **version table** is pack-chat-only; the `scripts/` tree listing (L221) is ordinary **coder** content. Coder edits it directly (no scope-in-as-pack-chat-only needed). |

**No new surface is introduced by any finding** — F1 corrects a *classification* of an already-inventoried surface (`dashboard.html`, the 19th census file); F2 corrects the *grain* of two edits already in the manifest (E10/E11); F3 corrects the *governance label* on an edit already in the manifest (E3). `enumerate-encoding-surfaces` holds: the manifest's surface SET is complete and unchanged.

---

## 1. F1 (MUST) — ACCEPT. `dashboard.html` is TRACKED, and the §6.2 gate does not exclude it

### Empirical-Evidence Block (HEAD `0d427f9`, 2026-07-23)

- **Command / output — tracked?**
  - `git ls-files --error-unmatch pack-ops/dashboard-approvals/dashboard.html` → prints the path, **exit 0** ⇒ TRACKED.
  - `git check-ignore -v pack-ops/dashboard-approvals/dashboard.html` → **exit 1** ⇒ NOT gitignored.
  - `git log --oneline -1 -- …/dashboard.html` → `861834f feat: v11 — BD-224 track dashboard board (D1): commit all three approvals files; dashboard.html -diff -merge + Check 69 classify …`.
- **Command / output — embeds `pack td`?**
  - `git grep -iE "pack[ -]td|pack_td|tracker[-_]promote" -- ':!maintenance-docs/*' ':!backlog/*' | grep -i dashboard` → `Binary file pack-ops/dashboard-approvals/dashboard.html matches` (**exit 0**). It is a live match under the design's own §6.2 step-5 gate.
- **Command / output — the design's §6.2 step-5 gate flags it (verbatim):**
  - `git grep -iE "pack[ -]td|pack_td|tracker[-_]promote" -- ':!maintenance-docs/*' ':!backlog/*'` → returns `Binary file …/dashboard.html matches` among 19 files / 192 lines. The dir is **not** excluded by `:!maintenance-docs/*` `:!backlog/*`.
- **Command / output — the proposed exclusion CLOSES the gap:**
  - `git grep -iE "pack[ -]td|pack_td|tracker[-_]promote" -- ':!maintenance-docs/*' ':!backlog/*' ':!pack-ops/dashboard-approvals/*' | grep -i "dashboard\|binary"` → **exit 1, empty** ⇒ gap closed, no over-suppression of other survivors.
- **Command / output — the three checks that treat it as tracked (why "never hand-edit"):**
  - `grep -n "dashboard-approvals/dashboard" scripts/lib/validate_checks/boundary_refs.py` → **L3359** enumerates `pack-ops/dashboard-approvals/dashboard.html` in Check 69's EXEMPT set (with `-shell.html`, `-url.txt` at L3360-3361).
  - `grep -n "check_dashboard_approvals_file_cap" scripts/validate-pack.py scripts/lib/validate_checks/pack_ops_hygiene.py` → Check **86** (`validate-pack.py` L1310-1311; `pack_ops_hygiene.py` L115) caps the git-TRACKED dashboard set.
  - `git ls-files pack-ops/dashboard-approvals/` → exactly `dashboard-shell.html`, `dashboard-url.txt`, `dashboard.html` (the capped set of 3). Check **88** (`check_dashboard_approvals_spec_shell_sync`, `pack_ops_hygiene.py` L273) validates `dashboard-shell.html`'s spec-sha against `DASHBOARD-SPEC-PACK.md` — it never reads `dashboard.html`'s text.

**Interpretation.** Both prior docs assert `dashboard.html` is an *"untracked render artifact … NOT a source surface"* (research §1.4; design §2.D bullet 3, §2.E context). **That premise is FALSE at HEAD `0d427f9`** — the file is committed (861834f, BD-224), tracked, not ignored, and explicitly enumerated by Checks 69/86. The *disposition* (leave it untouched) is right; the *stated reason* is wrong, and the §6.2 step-5 gate as written surfaces it as an unexplained binary survivor.

**Two impacts (matching the adversary, re-verified):** (1) factual — the rationale rests on a false premise; (2) verification-gate GAP — a reviewer running the literal §6.2 command sees an unexplained `Binary file … matches` and will either wrongly flag the eradication incomplete, burn a review cycle, or worst, hand-edit a **generated** file (semantically wrong — it regenerates on the next render — and risks perturbing the Check 86/88 tracked-set invariants).

**Why MUST, not BLOCKER (re-verified).** No CI check fails: Check 69 exempts the file from doc-content gates; Checks 86/88 are file-cap and shell↔spec-sync (untouched — the eradication adds/removes no file in that dir and does not hand-edit `dashboard.html`); no check validates the dashboard's *text* against `pack-td` existence. The embedded `pack td promote --to=phase` text is an immutable consequence of preserving BD-107's `#state` snapshot (a fresh render re-embeds it) — effectively embedded CLASS-3.

**Ruling: ACCEPT — fold both remediations into the final manifest** (§4: §2.D reclassification + §6.2 step-5 gate `':!pack-ops/dashboard-approvals/*'` + the never-hand-edit note). **Conclusion: SUPPORTED.**

---

## 2. F2 (SHOULD) — ACCEPT (AMEND). Surviving label mechanics are deferred tracker-mode residue; Option X must reword them

### Empirical-Evidence Block (HEAD `0d427f9`, 2026-07-23)

- **Command / output — the label mechanics survive under a NARROW Option X (strip only the `pack td` verb tokens):**
  - `grep -nE "promoted-to|derived-from" project-template/docs/pack/PM-CHAT.md` → **L753** `Resolved with \`promoted-to:phase-N\` label`, **L754** `Resolved with \`promoted-to:phase-N.M\` label` (column 2 of the three-outcome table at L750-754 — survives if only column-3 verb tokens are stripped). Read of L756-757 also shows the Path-3 `folded-into:` label prose survives.
  - `grep -nE "promoted-to|derived-from" supporting-docs/METHODOLOGY.md` → **L1607** `derived-from:TD-NNN`, **L1608** `promoted-to:phase-N`, **L1613** `derived-from:TD-NNN`, **L1613-1614** `promoted-to:phase-N.M` (interleaved with the verb tokens at L1600/1606/1611 in the decision-tree). Read of L1623-1625 also shows the `folded-into:` label prose.
- **Command / output — these ARE GH-issue tracker-mode constructs (NOT flat-file):**
  - `grep -n "promoted-to\|derived-from" scripts/lib/tracker-labels.sh` → L35/L37/L55/L59/L220/L230/L234-235 — `derived-from:TD-NNN` and `promoted-to:phase-N[.M]` are LABELS *composed and applied to GH issues* by the tracker-labels library.
- **Command / output — flat-file per-entry is the SOLE supported mode; tracker mode is DEFERRED INDEFINITELY:**
  - `grep -n "sole supported mode" CLAUDE.md` → L726, L909 (`Flat-file per-entry is the sole supported mode`). Tracker mode (which owns these labels) is deferred indefinitely per BD-214 (`Status: Resolved`, verified §5).
- **Command / output — no CI check asserts (or forbids) the label prose in the CLIENT docs; the reword is CI-neutral:**
  - `grep -rln "promoted-to\|derived-from\|folded-into" scripts/lib/validate_checks/*.py scripts/validate-pack.py` → `boundary_refs.py`, `validate-pack.py`. Reading `boundary_refs.py` L65-180: the only ENFORCED invariant (Invariant 3, L129-180) forbids the literal `folded-into` in **EXECUTABLE code under `scripts/lib/`** — it is scoped to executables, NOT to `project-template/docs/pack/` or `supporting-docs/`. `validate-pack.py` L163 is only a descriptive comment. So the client-doc label prose is neither asserted-present nor forbidden by any check.
  - `echo "promoted-to:phase-7" | grep -iE "pack[ -]td|pack_td|tracker[-_]promote"` → **exit 1** ⇒ the §6.2 grep-to-zero pattern does NOT match `promoted-to`/`derived-from`, so under a narrow Option X these labels are INVISIBLE-but-surviving residue.

**Interpretation.** The adversary is correct: under OI-2 Option X as the design NARROWLY framed it ("strip the `pack td …` verb tokens, KEEP the three-outcome methodology"), the surviving prose still instructs the client to apply `promoted-to:`/`derived-from:`/`folded-into:` LABELS — GH-issue tracker-mode constructs that do not exist in flat-file mode (the sole supported mode) and belong to a feature deferred indefinitely (BD-214). That is worse than "semi-coherent": it is client guidance to perform an operation impossible in the only supported mode, and it is a **deferred-feature mention inside an operating doc** — which `operating-docs-no-history-no-bloat` prohibits ("ZERO description of a DEFERRED, unimplemented, or off-by-default feature — even to say it is deferred"). The narrow grep gate cannot see it (pattern miss), so it would ship silently.

**Two nuances that bound F2 correctly (my own measurement, not the adversary's):**
1. **CI-neutral.** No check asserts these labels present and none forbids them in docs (the `folded-into` forbid-check is scoped to `scripts/lib/` executables). So F2 is a coherence/quality finding, correctly rated **SHOULD** — it changes NO CI-green story.
2. **The execution prose is ALREADY flat-file-coherent.** PM-CHAT.md L811-814 already describes the manual workflow in flat-file terms ("writes the phase entry (`docs/project/implementation-plan/phase-N.md`). Re-keys the TD. Dependency edges … recorded in the flat-file entry bodies"). So a flat-file template for the reword sits inside the very same section — the label reword is bounded and in-scope for E10/E11 (which already edit these sections), not a scope-creep into the OI-3-scoped-out tracker family.

**Ruling: ACCEPT (AMEND).** Fold the label-coherence tradeoff into the OI-2 decision (§5), and redefine Option X so that "keep the methodology" means **keep the three-outcome DECISION model + the already-flat-file execution prose, strip the `pack td` verb tokens, AND translate the `promoted-to:`/`derived-from:`/`folded-into:` label mechanics to flat-file terms** (the `Resolved:` line + phase-entry-creation the section already describes). Option Y (delete the whole section) dissolves the question. **Conclusion: SUPPORTED.**

---

## 3. F3 (NIT) — ACCEPT. E3 edits ordinary coder content, not the pack-chat-only version table

### Empirical-Evidence Block (HEAD `0d427f9`, 2026-07-23)

- **Command / output — only the README version table is pack-chat-only:**
  - `grep -nE "README" pack-ops/PACK-AGENTS.md` → **L133** `| Writing to README.md version table | Pack chat only | Version-level decisions |` and **L200** `- \`README.md\` (version table)`. The pack-chat-only carve-out names the **version table** specifically.
- **Command / output — E3 edits the `scripts/` tree listing, not the version table:**
  - `sed -n '219,223p' README.md` → L221 = `├── pack-td.sh   TD orchestration — promote (Path 1/2) / resolve … (v11; project-side tool, pack-internal — not advertised in pack help)`; L220 = `pack-tracker.sh` (dormant), L222 = `tracker-migrate.sh` (dormant). This is the pack `scripts/` directory listing — ordinary README prose, not the version table.
- **Command / output — no validator gates the README `scripts/` tree against disk:**
  - `grep -rln "README" scripts/lib/validate_checks/*.py` lists several modules, but none validates the README `scripts/` directory listing against disk contents (the design and adversary both confirmed this; I re-confirm there is no readme-tree liveness check — removing the `pack-td.sh` line has no interlock).

**Interpretation.** The design's §6.2 states *"README.md (E3) is pack-chat-only. Scope it INTO the coder prompt."* That characterization is imprecise: only the **version table** is pack-chat-only; the L221 `scripts/` listing is coder-editable content. The prescribed *handling* (a coder edits it) is correct regardless — this is a governance-label defect, not a behavior defect, hence NIT.

**Ruling: ACCEPT — drop the pack-chat-only characterization for E3.** In the final manifest, E3 is ordinary coder content: the coder deletes the L221 `pack-td.sh` line directly as part of its one patch. No scope-in-as-pack-chat-only routing is needed (it may still be listed in the coder prompt, but not under a pack-chat-only carve-out). This SIMPLIFIES §6.2's routing note. **Conclusion: SUPPORTED.**

---

## 4. FINAL authoritative manifest — the design AS AMENDED (one doc for the coder)

Verified live set: **6 whole-file/dir DELETEs + 11 in-place EDITs** across pack and project surfaces (unchanged from the original design). CLASS-3 historical record is UNTOUCHED. **Deltas from the original design are flagged `[Δ Fn]`.**

### 4.A DELETE — whole file / whole dir (pack-side dead machinery) — UNCHANGED

| # | Path | Size | Why DELETE |
|---|---|---|---|
| D1 | `scripts/pack-td.sh` | 337 L | The `pack td` verb dispatcher. No runtime invoker. Un-backs every client advertisement. |
| D2 | `scripts/lib/tracker-promote.sh` | 1474 L | TD→phase orchestration library. Sourced ONLY by D1 + the 3 tests. Dead once D1+tests go. |
| D3 | `scripts/tests/test-tracker-promote-path1.sh` | 629 L | Path-1 test; sources D1. |
| D4 | `scripts/tests/test-tracker-promote-path2.sh` | 562 L | Path-2 test; sources D1. |
| D5 | `scripts/tests/test-tracker-promote-direct.sh` | 340 L | Direct-close test; sources D1. |
| D6 | `scripts/tests/fixtures/tracker-promote/` (dir) | dir | Test DATA for D3-D5; not in the wired-test glob. Delete with the tests. |

Deletion is self-contained (adversary re-verified: only `pack-td.sh:65` live-sources `tracker-promote.sh`; `tracker-edit.sh`/`tracker-migrate-forward.sh` "matches" are COMMENTS, not `source`/call; no external caller of the exported `tracker_promote_*` functions).

### 4.B EDIT — in place (pack-side)

| # | Path | Exact edit | Interlock / note |
|---|---|---|---|
| E1 | `scripts/ci-shard-weights.tsv` | DELETE rows **L89-91** (the 3 `test-tracker-promote-*` weight rows). | Check 60 hygiene. NOT CI-fatal if left (disk-derived KEEP set; orphan rows tolerated) but MUST go per `enumerate-encoding-surfaces`. |
| E2 | `scripts/tests/pack-help-test.sh` | FLIP the **2.2** assertion **L185-187** from the PRESENT form to the **ABSENT** form (`!= *"pack td promote"* && != *"pack td resolve"*` → `t_pass "2.2 client pack td rows absent"` / `t_fail "2.2 client pack td rows leaked"`). Update the header comment **L4-8** and inline comments **L142-143, L155-158, L167-170** to "absent on BOTH surfaces." | **Coupling A** (cross-surface) with E9 — inseparable, same commit. |
| E3 | `README.md` | DELETE the **`pack-td.sh`** line (**L221**) from the `scripts/` tree listing. Leave L220 (`pack-tracker.sh`, dormant) and L222 (`tracker-migrate.sh`, dormant) — the OI-3 scoped-OUT family. | **[Δ F3]** ORDINARY CODER CONTENT — **not** pack-chat-only (only the README version table is). Coder edits directly; no scope-in-as-pack-chat-only routing. No interlock (no README-tree validator). |
| E4 | `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md` | **L212** row 6 — replace the dead `` `pack td promote` `` idempotency example with a LIVE idempotent verb (e.g. `` `init-project.sh --update` `` / `` `activate-capability.sh` ``). | operating-docs: current-behavior only. |
| E5 | `scripts/lib/tracker-edit.sh` | **L35 + L491** — remove the dangling `tracker-promote.sh:801` file:line cite; reword to describe the `provider_update "$gh_id" "$payload"` payload shape without naming the deleted file (or point at `tracker-provider.sh`). | Dormant family — COMMENT-only. Not CI-fatal. |
| E6 | `scripts/lib/tracker-migrate-forward.sh` | **L72** — reword to drop `` `pack td promote --to=phase-N.M` `` (generalise to "a subsequent link-creating cycle" or remove the sentence). | Dormant family — COMMENT-only. Not CI-fatal. |
| E7 | `scripts/lib/validate_checks/help_fragments.py` | **L333** (OPTIONAL/COSMETIC) — the Check-89 comment's `` `scripts/pack-td.sh`-shape `` illustrative regex-anchor example → a live file shape (e.g. `scripts/pack-help.sh`). | Check 89 functionally unaffected. |

### 4.C EDIT — in place (project-side, client surfaces)

| # | Path | Exact edit | Interlock / note |
|---|---|---|---|
| E8 | `project-template/scripts/.docs-gate-allowlist.txt` | DELETE the two FALSE rows: **L504-505** (`target: scripts/pack-td.sh` + reason) and **L507-508** (`target: scripts/lib/tracker-promote.sh` + reason), with blank-line separators. | **Coupling B** with E10 — CI-fatally inseparable (dead-target L3 liveness). |
| E9 | `project-template/docs/pack/HELP-FRAGMENT.md` | DELETE the three `pack td …` rows in the Project-commands table (**L20-22**) AND the entire **`## TD promotion`** section (**L25-36**). KEEP the `pack help` row (L23) and `## See also`. | **Coupling A** with E2. |
| E10 | `project-template/docs/pack/PM-CHAT.md` | The `## TD resolution orchestration` section (**L745-862**). **MANDATORY in every option:** DELETE `### Verb shape` (**L843-852**) + `### Implementation reference` (**L854-861**, the two `scripts/…` path refs — the live corpus backing for E8). **GRAIN = OI-2 (§5):** Option X = strip `pack td` verb tokens from the table (L753-754 col 3) + prose (L796/L800/L823) **[Δ F2]** AND reword the surviving `promoted-to:phase-N[.M]` label mechanics (table L753-754 col 2) + the `folded-into:` Path-3 prose (L757) to flat-file terms (`Resolved:` + phase-entry creation, per the section's own L811-814), KEEP the three-outcome decision model. Option Y = delete the whole L745-862 section. | **Coupling B** with E8. |
| E11 | `supporting-docs/METHODOLOGY.md` (client-SHIPPED, installed at init-project S6) | **L1600, L1606, L1611, L1630** — strip the `pack td resolve`/`pack td promote --to=phase-N[.M]` verb tokens + the "`pack td promote` verb has no `--fold-into` flag" line. **[Δ F2]** ALSO reword the surviving label mechanics at **L1607-1608, L1613-1614** (`derived-from:TD-NNN`, `promoted-to:phase-N[.M]`) and the `folded-into:` Path-3 prose at **L1625** to flat-file terms — MIRROR the E10 grain so the two client docs stay consistent. | Grain follows OI-2; CI-neutral (no check asserts/forbids the doc labels). |

### 4.D UNTOUCHED — historical record (explicit boundary; do NOT scrub)

- **`maintenance-docs/**`** (79 files incl. `IMPLEMENTATION-REPORT-BD-107.md`, `PACK-REVIEW-BD-107.md`) — the audit trail. UNTOUCHED. (BD-210 may prune superseded docs on its own authority; not this fix.)
- **`backlog/BD-107.md`** (`Status: Resolved`, verified) — origin governance entry + BD-076/204/222/224/251 in-body refs. UNTOUCHED.
- **[Δ F1] `pack-ops/dashboard-approvals/dashboard.html`** — **TRACKED generated record** (committed 861834f, BD-224; Check-69/86-exempt; capped in the 3-file tracked set with `dashboard-shell.html` + `dashboard-url.txt`). Its `#state` JSON embeds BD-107/BD-224 history — **embedded CLASS-3**. Regenerated by `scripts/dashboard-render.py`; reflects the historical record; **NEVER hand-edited** (a hand-edit is semantically wrong — it regenerates on the next `/pack-dashboard` render — and risks perturbing the Check 86/88 tracked-set invariants). *Correction: the prior docs' "untracked render artifact" label was FALSE.*

### 4.E Verified NO-EDIT (research surfaces needing no change) — UNCHANGED

- `scripts/ci-test-wiring-allowlist.txt` — the 3 promote tests are comment-only narration (L22-43), not parsed entries; Check 42 skips comments. NO functional edit. *(Optional: trim the stale L22-43 narration; not required.)*
- `scripts/tests/test-validate-pack-check-89.sh` — asserts `/pack-td`/`/pack-tracker` non-regression via a SYNTHETIC `pack-testonly` fixture (C5), never the real file. NO edit.
- `pack-ops/HELP-FRAGMENT-PACK.md` — no live `pack td` refs (BD-224 removed them). NO edit.
- Check 89 logic — matches `/pack-*` slash spans, not `scripts/…` spans. NO functional edit (E7 cosmetic).

---

## 5. CI-interlock reconciliation — the battery stays GREEN (ci-guard-measure-then-bound)

The reconciliation changes NO CI check. F1 changes only the §6.2 VERIFICATION grep (a coder/reviewer command, not a wired check); F2's label reword and F3's E3 relabel touch NO check (measured §2, §3). The six interlocks are **identical to the original design**, plus the dashboard checks and the sanctioned set are re-confirmed untouched.

| Interlock | State now | Reconciling edit | After | Δ vs original |
|---|---|---|---|---|
| Check 60 shard coverage | 3 disk tests + 3 weight rows in `keep_set` | D3-D5 + E1 | GREEN (disk-derived; orphan-row-tolerant) | none |
| Check 89 `/pack-*` parity | synthetic-fixture non-regression | none (E7 cosmetic) | GREEN unchanged | none |
| Check 23 exec↔fragment | `pack-td.sh` passes via `# pack-internal` | D1 (drops out of `iterdir`) | GREEN | none |
| Check 42 wiring allowlist | 3 tests comment-only, not entries | none | GREEN unchanged | none |
| Client docs-gate L3 liveness | 2 `target:` rows backed only by PM-CHAT L856-857 | **E8+E10 (lockstep)** | GREEN (refs+rows gone together) | none |
| pack-help-test 2.2 | asserts client rows PRESENT | **E2+E9 (lockstep, cross-surface)** | GREEN (asserts ABSENT) | none |
| **Check 86 dashboard file-cap** | 3-file tracked set | none (dir untouched) | GREEN | **[F1] re-confirmed** |
| **Check 88 shell↔spec sync** | reads `dashboard-shell.html` only | none | GREEN | **[F1] re-confirmed** |
| **Check 47 sanctioned set** | `{detect.sh, pack-help.sh}` | none (neither deleted file is a member) | GREEN | re-confirmed |
| Check registry count (86) | `CHECK_REGISTRY_EXPECTED_COUNT = 86` | none (no CHECK added/removed) | GREEN | re-confirmed |

**Empirical-Evidence Block (HEAD `0d427f9`, 2026-07-23):** `grep -n "CHECK_REGISTRY_EXPECTED_COUNT" scripts/lib/validate_checks/core.py` → L190 `= 86`; `grep -n "_SANCTIONED_PACK_SIDE_SHIPPED" scripts/lib/validate_checks/boundary_refs.py` → L593 (tuple `{scripts/lib/detect.sh, scripts/pack-help.sh}`, neither deleted file a member); `git ls-files pack-ops/dashboard-approvals/` → exactly the 3 capped files. **Conclusion: the affected-check list is UNCHANGED; every affected check projects GREEN; the F1 gate change is a verification-command fix, not a check change. SUPPORTED.**

### 5.1 The corrected §6.2 step-5 grep-to-zero gate (F1 fold-in)

Replace the design's §6.2 step-5 command with:
```
git grep -iE "pack[ -]td|pack_td|tracker[-_]promote" \
  -- ':!maintenance-docs/*' ':!backlog/*' ':!pack-ops/dashboard-approvals/*'
```
**Expected post-edit result:** ONLY the intended survivors — the OI-3 dormant-tracker comment sites (E5/E6, if reworded to drop the token, they drop out entirely) and, under OI-2 Option X, the reworded flat-file methodology prose (no `pack td`/`tracker-promote` literal remains; the `promoted-to:`/`derived-from:` labels are reworded away). The `dashboard.html` binary match is excluded by the added pathspec (embedded CLASS-3, verified exit 1 in §1). Target: **zero LIVE `pack td` advertising/backing**.

Note (F2): the §6.2 grep pattern does NOT match `promoted-to`/`derived-from`/`folded-into` (verified exit 1, §2). Under Option X the coder must therefore ALSO eyeball-verify the E10/E11 label reword (the grep cannot catch a stale label); under Option Y the section deletion removes them wholesale.

---

## 6. Commit shape — ONE cross-surface commit, neutral framing (UNCHANGED, F3-simplified)

**Why one commit.** Coupling A (E2 `pack-help-test.sh` 2.2 ↔ E9 `HELP-FRAGMENT.md` rows) spans BOTH surfaces and must land atomically; Coupling B (E8 ↔ E10) is CI-fatally inseparable. Because Coupling A is inherently cross-surface, no clean pack-only/project-only split keeps the battery green at every boundary → **one commit**.

**Scope keyword (CI Check 36):** `pack-only` DENIED (touches `project-template/` E8/E9/E10 + `supporting-docs/` E11); `project-only` DENIED (touches `scripts/`, `README.md`, `pack-ops/…`). → **NO scope keyword** (neutral framing; Check 36 skipped — no claim to verify). Suggested subject: `feat: v11 — BD-272 eradicate pack td cross-boundary violation (dead pack-backed verb; cross-surface)`.

**[Δ F3] Routing.** The original design routed E3 (`README.md`) as a pack-chat-only scope-in. **Corrected:** E3 is ordinary coder content — the coder edits L221 directly inside its single patch. **The ENTIRE eradication is now one coder patch (D1-D6 + E1-E11) + Pack-Chat apply/commit with user approval — no pack-chat-only carve-out at all.** This removes the design's "scope README INTO the coder prompt" special-case.

**One coder wave.** The couplings force a single atomic commit → one coder + the bounded review/fix cycle. No rule-10 parallel worktree waves (same-commit serialization).

---

## 7. Open items for the USER (open-item-surfacing) — context + options + evidence recommendation

### OI-1 — Delete-now vs re-home — RE-AFFIRMED: DELETE now; a clean verb, if wanted, ships fresh under BD-257

**Context.** `pack td` is dead machinery — no `pack` dispatcher routes it, backing ships to no client (`_SANCTIONED_PACK_SIDE_SHIPPED = {detect.sh, pack-help.sh}` — neither file a member), only its own 3 tests exercise it. Re-verified independently by both prior passes and unchallenged by F1/F2/F3.

**Options.** (A) DELETE now (eradicate) — smallest surface, removes the CI-interlock burden, satisfies the user's goal, loses no methodology (OI-2 keeps the client decision model). (B) Re-home the backing to `project-template/scripts/` — revives a never-executed feature and pulls the BD-214-deferred tracker-lib subtree client-side or requires severing it; a *build-a-feature* decision, not a *fix-a-violation* one.

**Recommendation:** **DELETE now.** Evidence unchanged by the reconciliation. "Eradicate" and "a future clean project-side verb" are compatible: if the user later wants an executable TD-promotion verb it is authored fresh under **BD-257** (project-side, `pm-*`/project naming, in the install map, shipped + tested) — never a resurrection of `pack td`. **This is the user's value call.**

### OI-2 — The PM-CHAT.md / METHODOLOGY.md edit grain — RECOMMEND: Option X, AMENDED to reword labels to flat-file terms (F2)

**Context (now with F2 visible).** E10/E11 mandatorily delete the dead-verb blocks. The remaining three-outcome decision model (direct close / Path 1 / Path 2) is verb-independent client methodology whose *execution* prose (PM-CHAT L811-814) is ALREADY flat-file-coherent. **F2 exposes what the design's OI-2 framing hid:** the surviving methodology also carries `promoted-to:phase-N[.M]` / `derived-from:TD-NNN` / `folded-into:` LABEL mechanics — GH-issue tracker-mode constructs (`tracker-labels.sh`) that do NOT exist in flat-file mode (the sole supported mode) and belong to a feature deferred indefinitely (BD-214). Keeping them raw is a deferred-feature mention in an operating doc — prohibited by `operating-docs-no-history-no-bloat`.

**Options:**
- **(X) Strip verb tokens, KEEP the methodology — AMENDED:** additionally reword the surviving `promoted-to:`/`derived-from:`/`folded-into:` label mechanics to flat-file terms (the `Resolved:` line + phase-entry creation the section already describes at L811-814). Preserves the legitimate client decision model; leaves NO deferred-feature residue; no client-guidance gap. Cost: a careful in-section reword of PM-CHAT L753-754/L756-757 + METHODOLOGY L1607-1608/L1613-1614/L1625, in the same commit.
- **(Y) Delete the whole `## TD resolution orchestration` section** (+ the METHODOLOGY resolution-path block), handing ALL TD-resolution guidance to BD-257. Dissolves the label-coherence question entirely; smallest commit surface. Cost: a **transient client-guidance gap** — clients have no documented TD-resolution path until BD-257 lands (BD-272 → BD-257 is the immediate sequence, so the gap is bounded), and the verb-independent decision model must be re-authored under BD-257.

**Recommendation (evidence + logic): Option X, amended.** (1) The three-outcome decision model is a legitimate, user-affirmed client concept ("TD entries are CLIENT-ONLY"); the user's "no such thing as a pack TD" targets the pack-backed VERB, not the client's decision methodology. (2) The methodology stands complete on the manual flat-file workflow already present at L811-814 — a template for the reword sits in the same section. (3) The label reword is bounded and in-scope (E10/E11 already edit these sections) and CI-neutral (no check asserts/forbids the doc labels; the `folded-into` forbid is scoped to `scripts/lib/` executables). (4) Option X strands no gap; Option Y does. **Caveat / user's call:** if the user prefers BD-257 to own ALL surviving client TD-resolution prose (a single clean-surface authorship), **Option Y is clean too** — the choice is *which BD owns the methodology prose*, with the tradeoff being (X) no gap + a bounded reword now vs (Y) smaller commit + a transient gap until BD-257. **Either way E8/E9/E10-mandatory/E11-mandatory are identical; only the fate of the decision-model prose differs.** Recommend X-amended; flag Y as the clean alternative.

### OI-3 — Scope of the dormant tracker family — RE-AFFIRMED: scope OUT (BLOCKED test)

**Context.** `pack-tracker.sh` + `tracker-migrate.sh` + ~18 `scripts/lib/tracker-*.sh` share `pack td`'s root cause but are dormant/deferred (BD-214 Resolved; verbs refuse) and advertised on NO client surface.

**Recommendation:** **scope OUT** — passes the `deferral-is-scope-creep` BLOCKED test with file evidence (advertised nowhere; verbs refuse; BD-185 Deferred; no OPEN tracker-redesign BD at HEAD `0d427f9`). The fix edits only E5/E6 (the dangling COMMENT refs the DELETE creates), NOTES the shared root cause, and flags the family for the tracker-redesign group. **F2 does not change this** — the client-doc label reword (E10/E11) is squarely within the `pack td` surface, NOT the tracker-family libraries (which stay untouched).

### OI-4 — The false `.docs-gate-allowlist.txt` rows — RE-AFFIRMED: delete in lockstep (E8)

The two rows encode a false "present after install" claim (a `declare-verify-backing` failure, pattern-copied from the TRUE `pack-help.sh` sibling row) and are the interlock that MASKED the dangling reference. DELETE (E8), lockstep with E10 — leaving them fails L3 liveness once E10 removes the corpus backing.

---

## 8. Cross-BD sequencing — RE-AFFIRMED (cross-bd-collision-scan Empirical-Evidence Block)

**Command / output (HEAD `0d427f9`, 2026-07-23):**
- `ls backlog/BD-*.md | sed -E 's/.*BD-([0-9]+)\.md/\1/' | sort -n | tail -3` → `269 270 271` ⇒ highest is **271** → **BD-272** correct.
- `grep -m1 "^Status:" backlog/BD-{257,136,214,205,210,107,185}.md` → BD-257 **Open**, BD-136 **Open**, BD-214 **Resolved**, BD-205 **Open**, BD-210 **Open**, BD-107 **Resolved**, BD-185 **Deferred**. Every status the design cites is correct.

| Open BD | Collision | Signal | Sequencing |
|---|---|---|---|
| **BD-257** (Open) — client slash-commands + execution foundation | **DIRECT** — owns client `docs/pack/` + `project-template/scripts/`; E9/E10 co-edit `HELP-FRAGMENT.md`/`PM-CHAT.md`. | **COORDINATE / SEQUENCE (strongest).** | Eradication (BD-272) lands **BEFORE** BD-257 — the clean precursor. Hands BD-257 a settled client surface + the explicit re-home option (OI-1 B). **[F2 note]** Under OI-2 Option Y, BD-257 also inherits ownership of the deleted TD-resolution methodology prose. |
| **BD-136** (Open) — trinity marker-section preservation | Minimal — E10 removes a normal `## TD resolution orchestration` H2, not a `[CONDITIONAL]` marker section. | NOTE. | Serialize same-file commits if BD-136 concurrently edits `PM-CHAT.md`. No marker-grammar interaction. |
| **BD-214** (Resolved) — tracker-deferral cleanup | Root-cause sibling; established the dormant-tracker decision. | ALIGN. | OI-3 scope-out aligns; E5/E6 touch only its dormant family's dangling comments. |
| **BD-205** (Open) — v11.0 final readiness audit | Sequencing — land this boundary fix before the whole-tree audit. | COORDINATE (ordering). | Insert BD-272 before BD-257 in the closeout order. |
| **BD-210** (Open) — pre-launch maintenance-docs cleanup | Light — CLASS-3 (§4.D) is UNTOUCHED here; BD-210 prunes superseded docs on its own authority. | NOTE (no gate). | Independent. |

**Conclusion: SUPPORTED — sequence BD-272 → BD-257.** No tracker-redesign BD is OPEN (BD-185 Deferred), so OI-3's scope-out strands no active work. The F1/F2/F3 reconciliation does not alter any collision or the sequencing.

---

## 9. Untouched-history boundary — RE-AFFIRMED (fail-loud-delete-old-source)

The eradication DELETES dead live source (D1-D6) with no archive and PRESERVES the historical record: `maintenance-docs/**` (79 files) + `backlog/BD-107.md` (Resolved) are UNTOUCHED. **F1 refines — does not breach — this boundary:** `dashboard.html` is a TRACKED generated record whose embedded `pack td` text is BD-107 CLASS-3 history that regenerates on the next render; it is correctly classified as neither dead live source (do not delete) nor a hand-editable doc (do not scrub) — it is left untouched and EXCLUDED from the verification gate (§5.1). Scrubbing the BD-107 history to make the tree "read clean" remains a defect and is rejected.

---

## 10. What the coder produces (one wave, isolated worktree)

1. Apply **D1-D6** (deletes) + **E1-E11** (edits) — E10/E11 per the user-ruled OI-2 grain (X-amended or Y); **E3 edited directly as ordinary coder content** (no pack-chat-only routing); E7 optional-cosmetic.
2. Run the FULL wired battery (verify-full-ci-suite): `python3 scripts/validate-pack.py` (both jobs) + `PACK_VALIDATE_DEEP=1 …`; `python3 scripts/lib/ci-shard-plan.py --assert-coverage` + the `--emit-matrix` path; the full sharded suite (`pack-help-test.sh` 2.1+flipped-2.2 both pass; `test-validate-docs-template-fullscan.sh` L3 liveness 0 dead targets; `test-validate-pack-check-89.sh` C5 unchanged); `project-template/scripts/validate-docs.sh` scan on `project-template/`; and the **corrected** §5.1 grep-to-zero gate (with `':!pack-ops/dashboard-approvals/*'`) plus an eyeball check of the E10/E11 label reword (Option X).
3. Emit the IMPL-REPORT (PREFLIGHT line verbatim). NO patch on return; NO commit.
4. Reviewer (RO): re-run the full battery; run the corrected grep gate; verify CLASS-3 (incl. `dashboard.html`) untouched; verify Couplings A + B landed atomically. Then the sanctioned post-clean patch step (Pack Chat SendMessages the RW agent for `git diff > changes.patch`, applies + commits with user approval).

---

## 11. Rules-Applied Verification Block

**reconciliation-instance-independence.** Evidence: I am a fresh instance — I did not author the design, the research, or the adversarial review. Every ruling rests on my OWN re-run commands at HEAD `0d427f9` (§1 `git ls-files`/`git check-ignore`/`git log`/gate-verbatim; §2 `grep` of PM-CHAT/METHODOLOGY/tracker-labels.sh/boundary_refs.py; §3 `grep` of PACK-AGENTS.md/README/validators; §8 `ls`/`grep Status:`), and I ACCEPTED F1/F2/F3 on that evidence while independently re-forming the OI-2 recommendation (X-amended) rather than copying the adversary's phrasing. **Conclusion: COMPLIANT.**

**agents-never-commit.** Evidence: I ran only read-only verbs (`git rev-parse`, `git ls-files`, `git check-ignore`, `git log`, `git grep`, `ls`, `grep`, `sed`, `wc`, `echo`) + `mkdir` + `cat >`/`cat >>` to the OUTPUT report only. No `git add/commit/checkout/restore/reset/…`. HEAD unchanged at `0d427f9`. SOLE write: this reconciliation doc under `…/packtd-eradication-recon-20260723/`. **Conclusion: COMPLIANT.**

**memory-not-an-ssot.** Evidence: I re-verified each finding against the LIVE tree with quoted command output rather than deferring to the two prior docs — I confirmed `dashboard.html` tracked (exit 0) NOT from the adversary's assertion but from my own `git ls-files --error-unmatch`; I re-measured the label mechanics at PM-CHAT L753-754 / METHODOLOGY L1607-1614 and their tracker-labels.sh origin; I re-measured PACK-AGENTS.md L133/L200 for F3. No ruling rests on memory or on a prior doc's word. **Conclusion: COMPLIANT.**

**empirical-evidence-blocks.** Evidence: §1 (F1), §2 (F2), §3 (F3), §5 (CI), §8 (BD scan) each carry the command run + captured output + HEAD/date + interpretation + a SUPPORTED/GREEN conclusion for every state-claim. **Conclusion: COMPLIANT.**

**ci-guard-measure-then-bound.** Evidence: §5 re-measures every affected guard against the PROJECTED post-edit tree (disk-derived Check 60; L3 substring liveness; Check 23 `iterdir`; Check 86/88 dashboard tracked-set; Check 47 sanctioned membership; registry count 86) and projects GREEN; I verified the F1 gate change is a verification-command fix (not a check change) and the F2/F3 edits touch no check (label forbid scoped to `scripts/lib/` executables; no README-tree validator). No allowlist declared without measuring. **Conclusion: COMPLIANT.**

**enumerate-encoding-surfaces.** Evidence: §4 enumerates every surface (D1-D6, E1-E11, the 4 NO-EDIT surfaces, the CLASS-3 untouched set) in lockstep; F1 confirms `dashboard.html` adds no MISSED surface (it is the already-inventoried 19th census file, reclassified, not new); the two atomic couplings are pinned. **Conclusion: COMPLIANT.**

**fail-loud-delete-old-source.** Evidence: §4.A deletes dead live source with no archive; §4.D + §9 preserve the historical record and classify `dashboard.html` correctly as a TRACKED generated record (embedded CLASS-3) that is neither scrubbed nor a deletable live source. **Conclusion: COMPLIANT.**

**cross-bd-collision-scan.** Evidence: §8 is a keyed EEB — next number (271→272), all cited BD statuses re-grepped, BD-257 DIRECT / BD-136 minimal / BD-214 ALIGN / BD-205 order / BD-210 NOTE — with command + HEAD/date and the BD-272→BD-257 sequencing re-affirmed. **Conclusion: COMPLIANT.**

**open-item-surfacing.** Evidence: §7 carries OI-1 (delete-now), OI-2 (X-amended vs Y — the F2 label-coherence tradeoff made explicit with options + evidence recommendation + the user's-call caveat), OI-3 (scope-out on the BLOCKED test), OI-4 (delete E8) — each with context, my own options, and an evidence/logic recommendation; none deferred to a new/other BD. **Conclusion: COMPLIANT.**

**rules-applied-verification-block.** Evidence: this section — each in-force rule named as in MEMORY.md with quoted/pathed evidence + a terminal COMPLIANT conclusion; no AMBIGUOUS state. **Conclusion: COMPLIANT.**

---

*End of RECONCILIATION-ARCH-REVIEW-BD-272.md (handoff name: `RECONCILIATION-PACKTD-ERADICATION.md`). F1/F2/F3 ACCEPTED on independent evidence (F2 with a scope amendment to OI-2 Option X); the FINAL manifest (D1-D6 + E1-E11), the CI-green story (affected-check list unchanged; §5.1 gate corrected), the untouched-history boundary, and the BD-272→BD-257 sequencing are authoritative. The user rules OI-1 (delete-now, recommended) and OI-2 (X-amended, recommended; Y flagged clean); a coder executes.*
