# RECONCILIATION-BD-291 — adjudication of the adversarial review over ARCHITECTURE-BD-291

- Agent: pack-architect RECONCILIATION instance (`architect-bd291-reconcile`), read-only; FRESH — neither the design's author nor the adversarial reviewer (reconciliation-instance-independence honored)
- Regime: own worktree `…/.claude/worktrees/agent-ad313e07a831b5847` @ HEAD `0c2709e7e59241a39b7892d31c219d3f5ec4cefd` (== injected canonical HEAD; `git worktree list` cross-checked: canonical + this worktree, both at `0c2709e`); date 2026-08-25; `isolation_mode: full` per spawn contract
- Inputs read in full: the design (315 lines), the adversarial review (168 lines), `/backlog/BD-291.md`, census IMPL-REPORT (376 lines) + `ot-dropped-content-appendix.md` (table anchors verified)
- Method: every load-bearing dispute re-measured INDEPENDENTLY this session. The prior passes' scratch trees no longer exist on disk (verified: their session scratchpads are absent), so the reproduction was REBUILT from byte-copies of the three target monoliths into this agent's own scratchpad (`…/scratchpad/recon-bd291/`), using the real pack helpers at `0c2709e`. Input identity with the census's pinned `472d931` inputs verified exactly: `wc -l` → 1478 / 5342 / 2579; anchors 113 / 61 / 55; decompose reproduction → "wrote 113/61/55", files 113/61/53. All writes confined to the owned handoff dir + own scratchpad; the real target checkout received plain-`cp` reads only; zero state-changing git verbs anywhere.
- Deliverable pair: the reconciled standalone design is `ARCHITECTURE-BD-291.md` (this dir); this log carries the rulings, the consolidated user-decision list, and the readiness verdict.

## §1 Ruling summary

**13/13 findings ACCEPTED on independent evidence — 0 REJECTED, 0 DRAFT-FOR-USER as standalone rulings.** One correction discovered AT RECONCILIATION beyond both prior passes (**R-1**: the true post-synthesis validator set is 68, not the adversary's 61 nor the design's implied 65 — 7 phases also lack `Goal`), folded into the reconciled design. The adversary's three open items adopted: OI-R1 → option (a), OI-R2 → option (a), OI-R3 → option (a). Four items route to the user design review (consolidated list, §4). The BLOCKER is resolved WITHIN the existing design shape — no return to the author (§5).

| Finding | Severity | Ruling | Where folded |
|---|---|---|---|
| F-1 validator names-out-of-enum claim measured-false | BLOCKER | ACCEPT (+R-1 correction) | §3.2b, §5.1(d), §3.3, OI-A3/A4/A12, E-25/E-26 |
| F-2 missed defect class D-7 (contract↔validator payload divergence) | MUST | ACCEPT | §1 D-7, §3.2b, E-27 |
| F-3 E-9 "403" wrong | MUST | ACCEPT (400 re-measured) | E-9, §1 D-6 |
| F-4 gate oversold as routing backstop | MUST | ACCEPT (sim reproduced) | §2.1, §2.3.1, §5.2, E-29 |
| F-5 milestone-H1 second destroyed association | MUST | ACCEPT (6-file list reproduced) | §2.3.3, §3.1.4, §7, E-28 |
| F-6 capture lifecycle / re-entry double-append + refusal interplay | MUST | ACCEPT (refusal code verified) | §2.3.2, §2.5 step 1, §7 recovery, E-16 |
| F-7 two stale P14 in-doc claims | MUST | ACCEPT (both verified) | P14, §3.1, §7, E-31 |
| F-8 missing edit-map row `test-migrate-v10-to-v11.sh` | MUST | ACCEPT (ownership verified) | P16, §5.3, E-31 |
| F-9 duplicate-heading population unmeasured | SHOULD | ACCEPT (measured: safe) | E-30, §2.3.4 |
| F-10 no bite obligations for harness legs §5.1(b/d) | SHOULD | ACCEPT | §5.3 m4/m5 |
| F-11 §8 BD-289 row over-narrow ∅ | NIT | ACCEPT | §8 BD-289 row → COORDINATE (note) |
| F-12 gate line-split semantics unspecified | NIT | ACCEPT | §2.2 read-symmetry sentence |
| F-13 Wave C keyword eligibility unnoted | NIT | ACCEPT | §9 keyword note |

## §2 Per-finding rulings (independent evidence per ruling)

**F-1 — ACCEPT (BLOCKER confirmed by my own probes; both prior arithmetics corrected by R-1).**
My measurements: (i) `grep -n "severity-enum\|scope-enum\|Severity\|Verify-Source\|payload" project-template/scripts/validate-docs.sh` → **zero hits**; full read of `_conf_check_backlog_entry` (L602–646) → legs are Entry-Type / core-field presence / Marker∈marker-enum / Status∈status-enum / resolved-requires — **no payload leg exists**. (ii) Probes on my clean rebuilt trees: TD-002 + `Severity: dependency` (out-of-enum) → zero failures; TD-003 + `Scope: architecture` (out-of-enum) → zero failures; TD-004 + `Marker: TODO` with **no payload line** → zero failures; controls: TD-001 valid → zero, phase-6 minus Status → exactly `phase-epic missing field 'Status'`; probe-scan total 387 = 400 − 13 (arithmetic-consistent). The design's "the validator then names them (loud)" is measured-false; §5.1(d) fails on the design's own declared data. **R-1 (my correction to BOTH passes):** the FULL §3.3-recipe synthesis over all 113 TDs + 61 phases, then the shipped validator → **68 failures = 61 missing `Status` + 7 missing `Goal`** (phase-18…24; the monolith carries 54 `**Goal**:` labels for 61 phases — authored-without; phase-18 shows `**Prerequisite**:` but no Goal). The adversary's projected 61 was also short; the declared 65 and the projected 61 were BOTH wrong; the mismatch (and so the BLOCKER) stands a fortiori. **Fold (OI-R1 option (a)):** C2 gains the contract-declared payload legs (§3.2b — presence-by-marker, Scope/Severity enum with the `phase-\d+` template the 13 real phase-N scopes require, Verify-Source presence-only; measured populations 4 out-of-enum + 0 missing-payload; empty allowlist; three self-test bites incl. the reached-for-backlog direction); the manual-fill declaration is corrected to **72 = 61 Status + 7 Goal + 4 payload**; post-C2 the projected validator set is the same 72 → §5.1(d) exact set-equality restored. Option (b) (weaken the contract) rejected: it ships a weaker contract to dodge a small measured leg and contradicts the fail-loud direction the design already chose for D-5's misname hole.

**F-2 — ACCEPT.** `project-template/docs/project/backlog/_rules.md` L48–62 read this run: schema declares `payload-by-marker` / `scope-enum` / `severity-enum` and the enforcement paragraph literally states "the Marker-keyed payload field present + (for Scope/Severity) enum-valid" — vs the F-1 measurement that no such leg exists. A D-5-sibling contract↔code divergence on the backlog stream, inside the BD's blast radius. **Fold:** admitted as **D-7** in §1; fix = the same C2 edit (§3.2b); token blast radius measured bounded (`grep -rln "payload-by-marker\|severity-enum\|scope-enum"` → the contract + `backlog/BD-272.md` only) — the contract text itself needs NO edit (validator catches up, not contract down; the backlog `_rules.md` stays NO-EDIT in §4).

**F-3 — ACCEPT.** My clean rebuild (full `project-template/` shell + reproduced trees + `_index.md`): `grep -c "\[conformance\]"` → **400** exactly; breakdown `174 missing Entry-Type (113+61) + 113 missing 'ID' + 113 missing 'Marker'`; changelog 0; my fuller shell shows NO dangling artifacts (total = 400), confirming the census-shell's 5 dangling failures were shell artifacts, plausibly the source of the mis-recorded "403". The design's own component list summed to 400 — internal inconsistency independent of my run. **Fold:** E-9 corrected to 400 everywhere (E-9, §1 D-6); the shell-artifact class noted so run (ii) doesn't misread it.

**F-4 — ACCEPT.** My own Counter simulation (7-line fenced-entry monolith, fence-unaware walker WITH the capture sink): truncated tail routes to capture; `M == T + R : True` — the misroute PASSES the gate. The §2.3.2 claim "would then be caught by the §2.2 gate" was false under the design's own architecture. **Fold:** §2.1 gains the exact scope statement (no-loss ONLY; routing correctness = P11 walker tests + human triage; "no future actor may cite the gate to skip walker-routing tests"); the fence fix retained on the corrected justification (misplacement-prevention + precondition of the H1-break class); §5.2 cross-references the scope.

**F-5 — ACCEPT.** My reproduction: `grep -n '^# '` on the plan monolith → 8 H1s (L1 title + `# Milestone 1`…`# Milestone 7`, L2640–L4071); in my rebuilt tree `grep -l '^# Milestone' phase-*.md` → exactly **6 files: phase-26, 28, 31, 32, 33, 36** (Milestone 1 falls in a dropped block); BACKLOG/CHANGELOG have line-1 titles only. H1 matches neither anchor nor `^## ` → in-span glue; invisible to every prior §2/§3 mechanism; "the one real ASSOCIATION" claim false. **Fold (OI-R2 option (a)):** §2.3.3 H1-break class (fence-guarded, non-preamble `^# ` closes the entry, lines to capture) + §3.1.4 `## Derived: milestone membership` + §7 step (2) covers both maps + P9 fixture hazard + P11 walker case. Option (b) (hard-coded 6-file list in shipped instructions) rejected — data-dependent list on an operating surface; (c) rejected — contradicts the BD's preservation goal. Safety verified before adoption: pack monoliths' H1s are line-1 titles (preamble — behavior unchanged); the fence guard runs FIRST so fenced `# ` comment lines can never break an entry.

**F-6 — ACCEPT.** Verified this run: the design's §2.3.1 specifies only "append" (no truncation); `scripts/lib/migrator-stages.sh` L131–135 dies `EXIT_ALREADY_MIGRATED` when `dispositions.tsv` exists ("restore from the backup … first") and `_stage_backup` L149–151 `fail_stage`s when the backup dir exists — so the double-append hazard is real on any re-drive and the sanctioned plain re-run is refused, with no §7 recovery procedure shipped. **Fold (OI-R3 option (a)):** capture truncate-at-decompose-start (§2.3.2, one sentence + a P11 no-double-append test case) + the §7 gate-failure recovery paragraph (inspect UNACCOUNTED/FABRICATED output → restore from `<state-dir>-backup`, move the backup dir aside per the S1 message, re-run). Option (b) (per-run capture filenames) rejected — state-dir garbage + a naming convention for no added safety.

**F-7 — ACCEPT.** Verified both stale claims this run: `### What changes` bullet — "`_intro.md` — the preamble extracted from the v10 monolithic file (lines 1–20 of the source on first migration)" — false against measured behavior TODAY (preamble dropped, D-3; `_intro.md` is an installed template) and contradicts §3.1's chosen destination; stage-table S4a row — "rename … at project root" — stale under P5's two-location rename; the same list omits the two new migration outputs. **Fold:** P14's edit spec extended (E-31): fix the `_intro.md` bullet, add `_index.md` + MIGRATION-TRIAGE.md to the outputs list, update the S4a row to name both locations.

**F-8 — ACCEPT.** Verified this run: `grep -rn "IMPLEMENTATION_PLAN" scripts/tests/*.sh` → `test-migrate-v10-to-v11.sh` Group 5 (L556+) owns the four BD-104 rename branches with substring assertions `"S4a (rename)"` and `"nothing to rename"` that P5's per-location rewording can contact; the file appears nowhere in the design's §4. **Fold:** new edit-map row **P16** (Group 5 gains the second location's collision + untracked-fallback branches; substring assertions re-checked), wired into Wave B and §5.3.

**F-9 — ACCEPT (and the measurement now exists).** My run: slugified all dated anchors of the real CHANGELOG under the §2.3.4 rule → **55 anchors, 55 distinct ids, 0 empty-suffix, 0 within-run duplicates** — neither fail-loud guard fires on the real migration; the design's conclusion stands, now measured. **Fold:** E-30.

**F-10 — ACCEPT.** §5.1(b) and §5.1(d) are records-style checks (declare-verify-backing) and the original §5.3 proved bite for the accounting check only. **Fold:** m4 (mutate one recorded synthesis line → §5.1(b) must FAIL) + m5 (introduce one undeclared validator failure → §5.1(d) must FAIL) added to the fixture-mode run (§5.3).

**F-11 — ACCEPT.** `backlog/BD-289.md` File/Symbol read this run: includes "`supporting-docs/` (the 45 bareness qualifications)"; P14 edits `supporting-docs/MIGRATION-v10-to-v11.md`; new bare `MIGRATION-TRIAGE.md` prose references land in Check 95's future walk. Family-level intersection is non-empty; temporal separation (v11.1) keeps it note-grade. **Fold:** §8 BD-289 row re-verdicted **COORDINATE (note)**; conclusion updated to four COORDINATE signals.

**F-12 — ACCEPT.** Verified the walker's read semantics this run (`open(…, encoding="utf-8", newline="")` L77; `text.splitlines(keepends=True)` L199). An asymmetric byte-`\n` reader could produce a loud-but-confusing false FAIL on Unicode line boundaries. **Fold:** §2.2 read-symmetry sentence (the accounting reader uses the identical read + split semantics as the walker).

**F-13 — ACCEPT.** Per the pack-root `CLAUDE.md` Check-36 keyword table (read this run): `pack-only` denies `supporting-docs/`; Wave C touches `scripts/` + `pack-ops/` + `supporting-docs/MIGRATION-v10-to-v11.md` → no exclusive keyword fits; A1/B qualify `pack-only`, A2 qualifies `project-only`. **Fold:** §9 planner note (Wave C necessarily keyword-less).

**R-1 — reconciliation-discovered correction (beyond the adversarial review).** The full-tree synthesis projection had never been RUN by either prior pass (the design extrapolated per-file probes; the adversary projected 61 from per-phase Status alone). My run measured **68**, exposing the 7 Goal-less phases (phase-18…24, verified authored-without in the monolith: 54 `**Goal**:` labels / 61 phases). Folded: E-25; §1 D-6; §3.1.6 manual-fill class (b); §3.3 "Goal is NOT synthesized" for the authored-without phases; §5.1(d) corrected equality (72 = 61 + 7 + 4 post-C2); §7 step (4); OI-A3/OI-A4 amended. No fabrication path exists for a narrative field — same honest-red rationale as Status, same user decision (OI-A3).

## §3 Counts by ruling

- ACCEPT: **13** (1 BLOCKER, 7 MUST, 2 SHOULD, 3 NIT) — every one on independent evidence captured this session.
- REJECT: **0**. DRAFT-FOR-USER (standalone): **0** — but four items route to the user design review (§4).
- Reconciliation-discovered corrections folded: **1** (R-1).
- Adversary open items: OI-R1 → (a) adopted (§3.2b); OI-R2 → (a) adopted (§2.3.3 + §3.1.4); OI-R3 → (a) adopted (§2.3.2 + §7).
- Nothing deferred out of v11.0; BD-171 non-absorption preserved (§8 re-verified).

## §4 CONSOLIDATED user-decision list (the design review's single decision surface)

1. **OI-A2 — project-changelog naming rule (shipped-contract examples change).** Recommendation: always-full-slug. Evidence: the contract's own mapping collides 5 real same-date entries; the rule is a pure function of the heading; measured 55/55 distinct, 0 guard-fires on the real data (E-30); no client trees exist yet (E-19).
2. **OI-A3 (AMENDED) — phase `Status:` + `Goal:` fill policy.** Recommendation: leave absent → validator names all 68 (61 Status + 7 Goal); MIGRATION-TRIAGE ships the Status suggestion table + the Goal-less list; the client PM chat fills with knowledge. Fabrication alternatives measured-wrong (E-13/E-14; Goal is narrative). Shapes the client's first-run experience — user decision.
3. **OI-A4 (AMENDED) — the "end-to-end green" definition.** Recommendation: set-equality green (§5.1(d)) with the CORRECTED set: declared = measured = **72** post-C2 (E-25/E-26). Strict-zero forces fabrication. This operationalizes the BD's acceptance wording — user sign-off.
4. **D-7 / §3.2b — the shipped validator gains the payload legs (OI-R1(a), folded as the design position).** The contract already DECLARES these legs (E-27); the validator measured-lacks them (E-26); the fix restores the design's honest-loud chain and the §5.1(d) instrument. Surfaced for confirmation because it tightens shipped-validator strictness for every v11 client (a client-facing behavior change, same review tier as OI-A2/A3/A4). Alternative (weaken the contract text) is on the table but recommended AGAINST (fail-loud direction, D-5 precedent).
5. **OI-A11 — user relay (action, not decision):** relay the §7 Step-3 fidelity guarantee + verify-command to the target-app PM chat and the second pack chat to close U-1's stale observation.

## §5 Readiness verdict

**The BLOCKER is resolved within the existing design shape — the adversary's own assessment ("all corrections small, land inside the existing v11.0 waves, no redesign required") is CONFIRMED by measurement, with one arithmetic correction (R-1) that also lands wave-locally.** Concretely: §3.2b + the corrected declaration land in Wave A2 + B surfaces already in the map (C2, P6, P9, P13 + new P16); the walker corrections (capture truncation, H1-break, fence-first ordering) are Wave A1 (P1, P11); the TRIAGE milestone map + recovery instructions are Waves B/C (P6, P14); no wave is added, no dependency direction changes, A1∥A2 disjointness is preserved (re-verified against the expanded §4 tables — C2/C1 vs the per-entry set share no file). The reconciled design is standalone and internally consistent: no section carries "403", "65/61", or the refuted "gate catches routing" claim (all replaced, not annotated). **The design does NOT return to the author. Next stage per the deterministic pipeline: user design review over §4's five items, then planner.**

## §6 Rules-Applied Verification Block

1. **agents-never-commit** — Evidence: git this session = `git rev-parse HEAD` (→ `0c2709e7…`), `git worktree list`, `git status --porcelain` — all read-only, all in this worktree; the platform sandbox additionally REFUSED two compound commands ("worktree-isolated agent…"), confirming enforcement; the target checkout was read via plain `cp`/`grep` only; no harness executed; no patch emitted (RO). **COMPLIANT**
2. **per-action-approval-sub-agents** — Evidence: writes landed ONLY in the owned dir `…/bd291-reconcile-20260825T221529Z/` (this file + ARCHITECTURE-BD-291.md) and own scratchpad `…/scratchpad/recon-bd291/` (drive.sh, probe.py, sims.py, mono/, otproj/, snap-*, scan-*.txt); the three input handoff dirs were READ-only; nothing deleted anywhere (probe.py's restore step deleted only my OWN scratch copies inside my scratchpad). **COMPLIANT**
3. **preflight-stop-means-stop** — Evidence: emitted `PREFLIGHT: reconciliation complete; about to Write ARCHITECTURE-BD-291.md + RECONCILIATION-BD-291.md to /Users/david/.local/state/optiquity-pack-handoff/bd291-reconcile-20260825T221529Z` in the message immediately preceding the first Write; no stop message received. **COMPLIANT**
4. **graph-first-context** — Evidence: my one NEW discovery question (surfaces encoding the payload/enum contract) ran `graphify query … --graph /Users/david/Developer/optiquity-ai-agent-config-pack/graphify-out/graph.json --backend claude-cli --budget 1500` FIRST → 30 nodes, all unrelated shell test functions (`payload()` in `test-deletion-boundary.sh` etc.) — no signal; per G2 fell back to grep (`payload-by-marker|severity-enum|scope-enum` → 2 files). All other reads were P2 verification reads of surfaces the prior passes had already named. **COMPLIANT**
5. **empirical-evidence-blocks** — Evidence: E-1…E-31 in the reconciled design — every entry re-run this session with command + captured output + HEAD/date + conclusion; this log's §2 quotes the per-ruling measurements (400, 387, 68, 55/55, 6-file list, `M == T+R : True`, L131–135/L149–151, L602–646 grep-zero). **COMPLIANT**
6. **ci-guard-measure-then-bound + declare-verify-backing** — Evidence: both folded C2 legs re-measured from my own rebuilt trees (§3.2: population 0 post-fix; §3.2b: 4 out-of-enum by name TD-057/058/070/071, 0 missing-payload, 13 phase-N template carriers, fixture TDs `TODO(version)` in-enum); bites specified in both directions incl. reached-assertions; the harness's records-style legs gained m4/m5 bite proofs; the gate's misroute non-bite PROVEN (E-29) and its guarantee re-scoped. **COMPLIANT**
7. **design-discipline-challenge** — Evidence: no ruling was inherited — each was re-derived, two were CORRECTED beyond the adversary (R-1: 68 not 61; F-3's dangling-artifact attribution verified by my zero-artifact full shell); the H1-break adoption was property-fit-checked against pack-stream data and fence interaction before folding; OI-R3(b) and OI-R2(b)/(c) rejected on stated evidence. No split-the-difference ruling exists. **COMPLIANT**
8. **verify-availability-not-existence** — Evidence: every capability claim in the reconciled design rests on a RUN: shipped validator (400/387/68 scans), index generator (rc=0, 67 lines), synthesis recipes (probe-green), payload legs (measured ABSENT — the capability the original design assumed), slug rule (55/55). The one remaining unmeasured step (real-target run ii) stays an explicitly gated Resolved-precondition. **COMPLIANT**
9. **dependency-direction-placement** — Evidence: fold-audit of §4 — `accounting.sh` pack-side (migrator runtime dep, never shipped), TRIAGE generator pack-side / generated file client-side, no dual-use file introduced by any fold, `_SANCTIONED_PACK_SIDE_SHIPPED` untouched. **COMPLIANT**
10. **ci-check-runtime-compounding** — Evidence: folded legs ride the existing per-entry conformance pass (O(1) field lookups per entry — §3.2b); the gate stays O(lines)/Counter, migration-time-only; no validate-pack check added (§6 of the design). **COMPLIANT**
11. **fail-loud-delete-old-source** — Evidence: the reconciled design REPLACES every superseded claim outright ("403"→400; 65/61→72; the routing-backstop sentence deleted and re-stated; the stale `_intro.md` promise routed to P14 as a FIX) — grep of the reconciled doc for `403`, `routing backstop`, `lines 1–20` as live claims → present only as corrected/quoted-history inside E-31/§2 rulings, never as operative design text. **COMPLIANT**
12. **architect-doc-reality-reconciliation** — Evidence: the §3.3 reconciliation chain preserved verbatim in the reconciled design (docstring names `index-generate.sh` `field_value`/`parse_phase_refs`, file + symbol, no line numbers; IMPL-REPORT cross-link specified). **COMPLIANT**
13. **operating-docs-no-history-no-bloat** — Evidence: every doc/contract edit the reconciled design specifies is procedural + present-tense (§3.2 rewrite, §7 procedure incl. the recovery paragraph, P8, P14 repairs); the folds ADD no history narration to any operating surface; this log and the design doc are handoff reference docs, not operating docs. **COMPLIANT**
14. **open-item-surfacing** — Evidence: §4 consolidates every open item in ONE place with context + options + an evidence-based recommendation (items 1–4) or an explicit action framing (item 5); R-1 was surfaced and FOLDED, not deferred; no recommendation rests on memory or moves work to another BD. **COMPLIANT**
15. **memory-not-an-ssot** — Evidence: every rule/contract claim cites a live in-repo path read this session (`validate-docs.sh` L602–646/L108–121/L499–510/L1400–1416/L1618–1630; `backlog/_rules.md` L48–62; `changelog/_rules.md` mapping section; `implementation-plan/_rules.md` L41–45/L99–106; `per-entry/*.sh`; `migrator-stages.sh` L131–151; `migrate-v10-to-v11.sh`; `core.py` L55–75; `CLAUDE.md` keyword table; `backlog/BD-289.md`) or a handoff artifact by path; zero claims from CLI memory. **COMPLIANT**
16. **public-bound-no-leak** — Evidence: both outputs live out-of-repo in the owned handoff dir; target-app tokens appear only inside verbatim-quoted measurement evidence (headings, filenames, H1 titles); every in-repo deliverable the reconciled design specifies is neutral (synthetic fixtures mandated; contract examples are the shipped placeholder style). **COMPLIANT**
17. **no-deferral-without-user-direction + deferral-is-scope-creep** — Evidence: no ruling resolves a finding by deferral; the D-7 fix, R-1 class, and all 13 folds land in the existing v11.0 waves (§5); OI-A14 attaches evidence to the already-open BD-254 (its live scope, prior user direction); BD-171 remains strictly separate (∅ file overlap re-verified). **COMPLIANT**
18. **rules-applied-verification-block** — Evidence: this block — 18/18 rules from the spawn prompt's "Rules in force" list, each with quoted evidence and a terminal conclusion; the reconciled design carries its own §11 block. **COMPLIANT**
