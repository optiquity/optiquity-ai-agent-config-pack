# PLAN — BD-206: Project-side per-entry, no-monolith-mirror, form-family conversion

**Planner (author):** `planner-bd206-nomirror` (FRESH; not the architect, not any reviewer; anti-contamination: read NO prior BD-206 PLAN/RESTART/FINAL nor the adversarial-PLANNER review nor anything under `/tmp/bd206-REJECTED-DO-NOT-READ/`).
**Reconciled by:** `planner-bd206-reconcile` (FRESH reconciliation instance; NOT the author `planner-bd206-nomirror`, NOT the adversarial reviewer) — folds the adversarial plan review's 2 MUST + 1 SHOULD + 1 NIT into THIS single plan doc (Reconciliation log, §10). Re-verified MUST-1's control empirically (EE-C). Anti-contamination: read NO prior BD-206 PLAN/RESTART/FINAL, NO architecture-side review, nothing under `/tmp/bd206-REJECTED-DO-NOT-READ/`.
**Plans from (SSOT):** `ARCHITECTURE-BD-206.md` (1276 lines, read IN FULL) — APPROVED at the design gate (6 adversarial passes) + the SIX user-approved amendments below.
**Planned at:** HEAD = `66c833223c2c8e3b7657e3c24e7c4ddfb539a3d7`, branch `v11-dev`, 2026-06-26. Working tree: 8 uncommitted deletions (7 project sidecars + 1 maintenance RESEARCH doc) = the PARTIAL Wave-A state.
**Read-only.** Pack repo + OptiquityTrader (OT) read-only; the only write is this plan doc. Ran read-only git + read-only `validate-pack.py` + read-only shell tests (scratch tmpdirs) + read-only graphify query to ground the A-1 per-wave breakage analysis.

> **Provenance.** Plans the 27 deliverables (O0–O26) into COMMITS across 5 waves (A foundational atomic + B/C/D/E parallel) + the downstream BD-249 pack-only commit. The design's decisions are FACTS; this plan sequences them. The A-1 amendment REQUIRED an empirical full-battery breakage census — I ran the battery (not EE-9's sample) and independently determined the complete per-wave breaking-test set.

---

## 0. Goal + BD items addressed

**Goal:** convert the PROJECT side (the client-shipped `project-template/docs/project/` template + the machinery operating on a populated client project) from monolith-with-regenerated-mirror to **per-entry flat-file, NO-monolith-mirror**, with a **form-family schema** (backlog + impl-plan), a **structured schema** (changelog), full drift enforcement (rules + tests + CI) in BOTH repos, and overhaul of every mechanism that assumed the old model.

**BD items in scope:**
- **BD-206** (Open, v11.0) — the whole conversion (O0–O21, O22-proj, O23–O26). Acceptance criteria per `backlog/BD-206.md`: no monolithic mirror; project monoliths DELETED for converted streams with no regenerated mirror; impl-plan stream carries `_index.md`; client docs/rules state no-mirror flat-file standard; conversion preserves every entry; dual-use scripts (`detect.sh`/`pack-help.sh`) tree-aware on both surfaces; `project-template/skills/*/SKILL.md` masters match the corrected standard; no doc-vs-implementation gap; validate-pack green (Check 47 unchanged).
- **BD-249** (Open, v11.0; Blocked-by BD-206) — the PACK-side dead-mirror-subsystem removal (O22-pack), a SEPARATE `pack-only` commit sequenced DIRECTLY AFTER BD-206. Coordinated here (§6) but NOT a BD-206 wave.

**Commit count (authoritative; §3 is the SSOT, §6 the schedule):** BD-206 = **12 to 16 commits** depending ONLY on the orchestrator's Wave-D commit-granularity choice — **1** atomic Wave-A commit (A1) + **4** Wave-B commits (B1, B1b, B2, B3) + **4** Wave-C commits (C2, C3, C4, C5 — `validate-pack.py`'s O9 leg folded into A1; the C2-C5 enforcement legs SERIALIZE per §4 MUST-2 — they share `validate-pack.py` and `validate-docs.sh`, so they are NOT parallel) + the Wave-D commits (**2** if D2-D6 land combined: D1 + one combined D2-D6 → 12 total; up to **6** if D1..D6 fully split → 16 total) + **1** Wave-E commit (E1). Then **+1** downstream BD-249 commit (a SEPARATE BD, `pack-only`). So BD-206 ∈ {12 … 16}; BD-249 = 1. The Wave-C serialization (MUST-2) changes the C-wave SCHEDULE (parallel→serial) but NOT the C-wave commit COUNT (still 4).

---

## 1. APPROVED DESIGN AMENDMENTS (user-approved 2026-06-26; supersede the cited design-doc text; NOT open for re-debate)

> **Read this first, downstream agents (adversarial planner reviewer + coders):** the SIX deltas below are USER-APPROVED fixes that take precedence over the corresponding design-doc text. They are **SANCTIONED** — not plan/design drift, not a planner deviation. When you compare this plan to `ARCHITECTURE-BD-206.md` and find these differences, that is EXPECTED and CORRECT. Do NOT re-grade them SHOULD/NIT-vs-MUST; do NOT re-litigate. Evidence source: `CONFIRM2-ARCH-REVIEW-BD-206.md` (read for evidence detail only).

### A-1 (was S-2; THE important one) — per-wave verification runs the FULL battery, empirically determined; breaking-test inversions land in the wave that breaks them
**Supersedes:** EE-9's "baseline shell battery is green" claim (ARCHITECTURE-BD-206.md §2 EE-9 Command 2, lines 184; §9 "verify-full-ci-suite" row, line 1217). EE-9 SAMPLED (`test-per-entry.sh` + 4 tracker tests); it did NOT run the migration battery.
**Approved fix:** the plan's per-wave verification EMPIRICALLY runs the FULL battery (validate-pack.py + build.sh + ALL shell shards including the migration battery) and pulls any test that breaks at a wave's source changes INTO that wave's commit (the §15 "green at the first wave" guarantee). The design's CONCLUSION (Wave-A makes the full battery green) STANDS; only EE-9's baseline-sampling attestation is corrected.
**Planner's empirical determination (this plan, §5 + the Empirical-Evidence blocks):** I ran the full battery at the partial Wave-A state and found `test-migrate-v10-to-v11-decompose.sh` (33/12), `test-migrate-v10-to-v11.sh` (48/6), `test-migrate-v10-to-v11-dry-run.sh` (57/4), and `test-migrate-v10-to-v11-gates.sh` (80/7) ALL RED — beyond EE-9's sample. Root cause + per-wave assignment in §5. **APPROVED — do not re-litigate.**

### A-2 (was S-1) — EE-11 row-10/row-20 line-range overlap on `tracker-migrate-forward.sh` resolved
**Supersedes:** EE-11 row 10 (ARCHITECTURE-BD-206.md line 261) classifying `tracker-migrate-forward.sh:2142-2172` as M-mono REPOINT, where `:2142-2172` is INSIDE row 20's `:1992-2253` KEEP range (line 271).
**Approved fix:** `:2142-2172` is an **M-track tracker→file read-only-mirror staleness probe** (verified: `:2160-2173` reads `$repo_root/BACKLOG.md`, checks the `<!--` read-only-mirror header, reports `mirror_age` — the `tracker-mirror.sh` feature) → it is **KEEP**, NOT REPOINT. Row-10's REPOINT scope is bounded to the genuine M-mono surfaces (`:1395` stale comment + `:1387-1432` mirror-only short-circuit prose where it asserts the per-entry→monolith assumption); it does NOT cover the `:2142-2172` KEEP range. The Wave-E coder draws the M-mono/M-track boundary line-precisely (§3 Wave E). **APPROVED — do not re-litigate.**

### A-3 — grep-zero / allowlist completeness gates use DIRECT git pathspecs (not post-filtering)
**Supersedes:** the EE-11 "Command 2" reproduction recipe (ARCHITECTURE-BD-206.md line 247, 278) which returns ZERO run literally (unquoted `$FILES` glob + `mirror_required` not in the 52-file set).
**Approved fix:** ALL grep-zero / allowlist-sizing gates use direct git pathspecs, e.g. `git grep -nE '<pattern>' -- ':!maintenance-docs/' ':!changelog/' ':!backlog/'`, NOT a variable-expanded file list nor post-filtering. I verified the direct form works exactly (103 force-overwrite hits + 81 mirror-subsystem hits at HEAD; both reach zero by gate construction). `mirror_required` belongs in a `validate-pack.py`-scoped grep, not the 52-file tracker grep. **APPROVED — do not re-litigate.**

### A-4 (NIT) — EE-11 "Command 2" doc-precision correction (no plan-material change)
**Supersedes:** the implication (ARCHITECTURE-BD-206.md line 278) that a fourth reviewer re-running Command 2 verbatim reproduces the surfaces.
**Approved fix:** Command 2 returns empty run literally; the SUBSTANCE is reproducible via direct grep (A-3). Planner notes it; no plan-material change. **APPROVED — do not re-litigate.**

### A-5 (NIT) — `test-fixtures/build.sh:627-632` `[mirror]` table is KEEP (M-track), outside the 52-file tracker set
**Supersedes:** the "exhaustive" framing of EE-11's 52-file sweep (ARCHITECTURE-BD-206.md line 246) which did not enumerate `test-fixtures/build.sh:627-632` (outside the `git ls-files` 52-file set).
**Approved fix:** `test-fixtures/build.sh:627-632` is a tracker-mode (`mode.state="tracker"`, `:616`) M-track `[mirror]` fixture inside `_build_v11_tracker_on()` → **KEEP** (correct). Noted so the O23 coder does NOT mistake it for an in-scope M-mono surface when editing build.sh's per-stream round-trip block (`:510-578`). The two regions are disjoint. **APPROVED — do not re-litigate.**

### A-6 (NIT) — EE-11 row-17 rationale mischaracterizes `flat-file-mode.toml` (KEEP conclusion correct)
**Supersedes:** EE-11 row 17 rationale (ARCHITECTURE-BD-206.md line 268) calling `flat-file-mode.toml` a "tracker-mode/staleness-test fixture."
**Approved fix:** `flat-file-mode.toml` is `mode.state="flat-file"`, `prefix="BD"` (PACK), consumed only by `tracker-config-test.sh` as a **mode-resolution parse fixture** (never staleness-validated; Check 29 short-circuits when `mode != tracker`). Its `[mirror]` table is **KEEP** (correct conclusion); the rationale is corrected. Noted so the O25 coder does NOT touch this fixture. **APPROVED — do not re-litigate.**

---

## 2. Affected files (complete list, including cross-reference updates)

Drawn from the design's deliverables + EE-6/EE-7/EE-8/EE-10/EE-11 + my own grounding. Grouped by wave; same-file collisions flagged with [SF].

### Wave A (one atomic commit) — sources + their lock-step encoding surfaces
- **Template sidecars (rebuilt/created):** `project-template/docs/project/{backlog,implementation-plan,changelog}/_intro.md` (O2, 3 files); `…/{backlog,implementation-plan,changelog}/_rules.md` (O3, 3 files); `…/changelog/_format.md` (DELETED — FORBIDDEN, O1).
- **Installer:** `scripts/init-project.sh` [SF: O1/O4/O8] — `_CLIENT_INSTALLED_FILES` (~`:1268-1274`) + self-doc (~`:1415-1421`): keep 6 rebuilt sidecar rows, DELETE 2 `_format.md` rows; `cmd_update` S11 copy block drops `_format.md` copy + canonical-missing guard; remove greenfield mirror generation (`:1084-1121`); drop mirror middle field from install tuples (keep stream-dir); fix info string + comment.
- **Migrator copy-source [A-1, design under-attested]:** `scripts/migrate-v10-to-v11.sh:453` — the `support_basenames` changelog branch DROPS `_format.md` (becomes `_rules.md _intro.md`); fix the `:429-431,449-451` comments. (THIS is the A-1 root the design did not explicitly enumerate as a Wave-A edit.)
- **Per-entry lib:** `scripts/lib/per-entry/_lib.sh` [SF: O1 → O22-proj(B) → BD-249] — DROP `_format.md` from `:136` (project-changelog support); ADD `_index.md` to `:123` (project-implementation-plan support, MUST-4).
- **Validator (Python):** `scripts/validate-pack.py` [SF: O9 Check-43 + O25 Check-29 + O26-adjacent + C1 empty-template leg — ALL folded into Wave A, see §4] — Check-43 STRIP `:5503` `_format.md` sibling, STRIP `:5094` phantom `(see /backlog/_format.md)`, STRIP mirror rationale `:5504-5507,5590-5593` + DR-3 KEEP/STRIP the 3 basenames; REMOVE `:8225` changelog `_format.md` operating-doc glob member (KEEP `:8224` `*/_rules.md`); Check-29 flip `:2796` `mirror_required=True→False` + rationale `:2492-2497,2571-2573`; O20-prose per-line sweep (`:136,239,318,2495,2752,3145,5360,5625,5697,7819,8836,11491`); the NEW empty-template-validation leg (O9) + the `_rules.md` schema parser.
- **Operating-doc allowlists:** `pack-ops/.operating-doc-history-allowlist.txt` (REMOVE `:145,150,157`); `pack-ops/.operating-doc-deferred-feature-allowlist.txt` (REMOVE `:419,423`); `pack-ops/.dangling-ref-allowlist.txt` (REMOVE `:101,104` project-monolith tokens; KEEP `:95,98` pack-monolith names) (O26, SHOULD-A).
- **Tracker examples + emitter + Check-29 tests [O25, lock-step with Check-29 flip]:** `project-template/tracker.toml.project-example` (REMOVE `:37-44` `[mirror]` table); `project-template/tracker.toml.pack-example` (STRIP `:57-58` "until BD-206"; KEEP `:50`); `scripts/lib/tracker-init.sh` (DROP client `mirror_block` `:359-379`; rewrite `:185-192,318-333` comments, MUST-B); `scripts/tests/tracker-config-schema-test.sh` (invert Test 7 `:17-20,116-131,223-242`, BLOCKER-A); `scripts/tests/tracker-init-test.sh` (invert 3.5 `:296-302`; rewrite `:14-16,280-283` comments).
- **Build fixture:** `test-fixtures/build.sh` [O23, BLOCKER-2] — rewrite per-stream round-trip block `:510-578` (REMOVE `mirror-generate.sh` sourcing `:516`, monolith-missing `die :537-538`, `cp …orig :544`, `per_entry_regenerate_mirror :555`, round-trip `cmp`/`die :567-573`, `PE_FORCE_OVERWRITE_MIRROR :496,552,555`); install-map tuple `:528` drops mirror middle field; KEEP `:627-632` (M-track, A-5). `test-fixtures/README.md` (rewrite `:30,189-191` round-trip prose, SHOULD-B). `test-fixtures/manifest.txt` (regenerated at push, BD-228 — both build.sh + project-template are manifest inputs).
- **Wave-A EXISTING-test inversions (EE-9 + A-1):** `scripts/tests/test-per-entry.sh` [SF: O1 `:231-232` invert "1.9 …includes _format.md" → ABSENT + add `_index.md` admitted assert] ; `scripts/tests/test-init-project.sh` [O1/O4 invert 4.2/4.3, delete 4.4/4.5 byte-identity, drop 4.6 mirror rows, drop Group-5 mirror-regen leg `:386-394,416,427`] ; `scripts/persona-contracts/contract-greenfield.sh` (drop `_format.md` + 3 monoliths from arrays `:212,231,239,240-244`) ; `scripts/persona-contracts/contract-migration.sh` (drop `_format.md` `:422,458,471`) ; `scripts/tests/fixture-dependent/test-v11-realistic-ot.sh` (invert A.13 `:172`; comments `:143,183-188`) ; `scripts/tests/test-validate-pack-check-43.sh` [O24: required-set `:133-138` drop `_format.md`; T9 PASS fixture `:481-494` repoint to sanctioned sibling] ; `scripts/tests/fixtures/project-side-refs/project-side-pass-same-dir-skeleton.md` (`:13,21` drop `_format.md`) + `…/project-side-fail-per-entry-skeleton.md` (`:14`) [O24] ; **the migration battery** (`test-migrate-v10-to-v11.sh`, `-dry-run.sh`, `-gates.sh`, `-decompose.sh` sidecar-source-driven asserts) [A-1 — green restored when sidecars rebuilt; the FORCE-OVERWRITE asserts in `-decompose.sh` belong to Wave B, see §5].
- **Client-side bash validator allowlist:** `project-template/scripts/.docs-gate-allowlist.txt` (drop mirror entries `:393-397` + `_format.md` `:97-112`) — design places O10 in Wave C; but the `_format.md` FORBIDDEN grep-zero gate (EE-6) spans this file → see §7 GAP-1 (the planner places the `_format.md`/mirror allowlist drops with O10 in Wave C; the grep-zero gate is asserted JOINTLY across A+B+C+E, not Wave-A-alone).
- **Doc refs (drop `_format.md` / project-monolith prose):** `README.md` (`:149-154` diagram); `pack-ops/PACK-AGENTS.md` (`:213`); `pack-ops/MERGE-STRATEGY.md` (`:270` `_format.md` + `:267-280` project-monolith prose rewrite, SHOULD-C) [O1 doc share, Wave A].

### Wave B — migrator + conversion machinery + client reads
- **B1 (serial within):** `scripts/lib/migrate-v10-to-v11/decompose.sh` (remove regenerate-mirror `:93-95,195-197`; KEEP v10-monolith INPUT read `:146-148,162`; fix header `:4` + comment `:47`; remove the `_MIGRATOR_*→PE_*` bridge `:27,57,117,120,125`); `scripts/lib/migrator-core.sh` (remove `_MIGRATOR_FORCE_OVERWRITE_MIRROR` flag/parser/help/default + `:326` comment); `scripts/migrate-v10-to-v11.sh` (remove the flag wiring — separate from the `:453` Wave-A copy-source edit); their tests: `scripts/tests/test-migrate-v10-to-v11-decompose.sh` (invert 2.2a/b/c `:300-307` to NO regenerated mirror; Group-4 force-overwrite asserts `4.1a/b/c,4.2a,2.0b`), `scripts/test-migrator-core.sh` (flag tests). `scripts/tests/fixture-dependent/test-v11-realistic-ot.sh` (DELETE Group B byte-identity `:236-241,287`); `scripts/persona-contracts/contract-migration.sh` (align `:475` comment).
- **B1b (O22-proj):** `scripts/lib/per-entry/_lib.sh` [SF — A first] (remove `:109,121,129` project mirror constants); `scripts/tests/test-per-entry.sh` [SF — A first; BD-249 after] (remove `:219-221` project mirror-filename asserts).
- **B2 (O5):** `scripts/lib/detect.sh` (add client-surface per-entry probe parallel to pack-surface `:56-65`; keep legacy-monolith fallback inside DENY-LIST-CONTENT markers; client branch `:66-72`); `scripts/pack-help.sh` (inherits; no independent monolith read); `scripts/test-detect.sh` (add client-tree-probe assert); `scripts/tests/pack-help-test.sh`.
- **B3 (O6):** `scripts/lib/recommendation.sh` (recompute signals from the tree `:166-188`; relabel `:415`); `scripts/tests/recommendation-test.sh` (fixtures seed a per-entry tree).

### Wave C — enforcement legs (INTERNALLY SERIAL C2→C3→C4→C5 [MUST-2]: all share `validate-docs.sh`; C3/C4/C5 also share `validate-pack.py`; read A's schema)
- **C2 (O10):** `project-template/scripts/validate-docs.sh` (extend with populated-client per-entry schema conformance; parse the `_rules.md` schema block; drop `:88` `_format.md` + exclude-category comments `:9,24-25,319`); `project-template/scripts/.docs-gate-allowlist.txt` (drop mirror `:393-397` + `_format.md` `:97-112`).
- **C3 (O11):** `_index.md` generator + MANDATORY validation script (both repos: pack leg in `scripts/validate-pack.py` [SF], client leg in `validate-docs.sh` [SF]) — new script(s) under `scripts/lib/per-entry/` (e.g. `index-generate.sh` + an index validator) + the two validator legs; tests. (NOTE `_lib.sh:123` admission lands in Wave A.)
- **C4 (O12):** changelog conformance check (both repos: `scripts/validate-pack.py` [SF] pack leg + `validate-docs.sh` [SF] client leg; parses changelog `_rules.md` `## Entry structure`) + tests.
- **C5 (O13):** phase/part/task naming-conformance guard (both repos: `scripts/validate-pack.py` [SF] pack leg + `validate-docs.sh` [SF] client leg) + tests.
- *(C1 = O9 residual empty-template leg + schema parser — FOLDED into Wave A; see §4.)*

### Wave D — governance + docs + prompts (parallel; disjoint files)
- **D1 (O15):** `project-template/{CLAUDE,AGENTS,GEMINI}.md` (trinity `## Document locations` + mirror prose → no-mirror; all 3 in ONE commit, trinity rule; cross-CLI normalization per ARCHITECTURE-BD-182 §4.1).
- **D2 (O16):** `project-template/docs/pack/PM-CHAT.md` (STATUS header `:328-334`; plan read/write/anchor model `:123,321-322,784,890,933,980,1022,1068`).
- **D3 (O17):** `project-template/docs/pack/prompts/*.md` (architect/coder/planner/reviewer/docs-researcher/pm-chat) + `HELP-FRAGMENT.md:39` + SETUP-NEW/SETUP_TEMPLATE/INSTALL-PROCEDURES/CLI-PM-SETUP.
- **D4 (O14):** `project-template/docs/pack/METHODOLOGY.md` (wholesale plan-model rewrite, 24 IMPLEMENTATION-PLAN refs).
- **D5 (O18):** `project-template/skills/audit-methodology/SKILL.md` (`:76-77`) + `project-template/skills/pm-startup/SKILL.md` (`:78,82,89-91`) masters → no-mirror; verify pack copies (`.claude/.codex/.gemini`) parity (BD-203 C-3 corrected copies → masters now match).
- **D6 (O21):** `supporting-docs/MIGRATION-v10-to-v11.md` (rewrite `## Per-entry decomposition`; STRIP `_format.md :338` + the `--force-overwrite-mirror` subsection `:384-413`; KEEP `:468` rename row + `:374` rollback).

### Wave E — tracker library reconciliation (dormant; parallel; independent of B/C/D)
- **E1 (O19):** `scripts/lib/tracker-agent-read.sh` (`:159,251-278` repoint M-mono fallback); `scripts/lib/tracker-doctor.sh` (`:128,173-174`); `scripts/lib/tracker-migrate-forward.sh` (`:1395` stale comment + `:1387-1432` mirror-only short-circuit M-mono prose — **A-2: do NOT touch the `:2142-2172` M-track staleness probe inside the `:1992-2253` KEEP range**); `scripts/lib/tracker-promote.sh` (`:251-271,463-624,915-923,1306-1360` project-monolith read/append); `scripts/lib/tracker-phase-task.sh` (`:7,24,140,487`); `scripts/tracker-migrate.sh` (project share). KEEP (M-track, untouched): `tracker-mirror.sh`, `tracker-header-snapshot.sh`, `tracker-migrate-reverse.sh:1604-1612`, the `tracker-config/*.toml` fixtures, `flat-file-mode.toml` (A-6). Tests: `scripts/tests/tracker-agent-read-test.sh` (`:34-78,190-192` MUST-A — the BD-214 C1 recurrence file) + any `tracker-doctor`/`tracker-promote`/`tracker-phase-task` test asserting a project-monolith read (`test-tracker-promote-direct.sh`, `test-tracker-phase-task.sh`).

### Downstream — BD-249 (separate `pack-only` commit, after BD-206)
- `scripts/lib/per-entry/_lib.sh` (remove pack `:85,99` constants, `pe_canonical_mirror_for_stream :150-152`, empty `mirror)` branch, header `:4-8,83`); **DELETE** `scripts/lib/per-entry/mirror-generate.sh`; `scripts/lib/per-entry/toc-regenerate.sh:15` (comment only); `scripts/tests/test-per-entry.sh` (Groups 3/4/8/9 + `:70,600` sourcing/smoke); `scripts/tests/test-validate-pack-checks-32-33-34.sh:274-278` (replace mirror fixture; KEEP `:281-288` TOC); `scripts/tests/test-init-project.sh:416,427` (Group-5 mirror-regen leg).

---

## 3. Task breakdown — 27 deliverables → 12–16 commits across 5 waves (+ BD-249)

Each commit: its deliverables, the exact files it touches, and its verification. Wave A is ONE atomic commit. All RW coder work runs in isolated worktrees off the §6 parallel map (the orchestrator schedules; the worktree mechanics are not this plan's concern).

### COMMIT A1 — Wave-A FOUNDATIONAL ATOMIC FULL-BATTERY-GREEN (serial, FIRST, alone)
**Deliverables:** O0(clarified) + O1 + O2 + O3 + O4(init-side) + O8 + O9(ALL validate-pack edits — see §4 fold) + O20-prose + O23 + O24 + O25 + O26 + the A-1 migrator copy-source fix + ALL Wave-A EXISTING-test inversions (EE-9 + A-1).
**Why ONE commit:** Wave-A green is measured against the FULL CI battery (validate-pack.py + `build.sh --all/--verify` + the shell shard battery), not validate-pack alone. Every surface that goes RED the instant the template / support-set / mirror-generation / `_format.md` / Check-43 allowlist / Check-29 flag / sidecar-source changes MUST land here so the FIRST commit is green across the entire battery (§15 binding + `verify-full-ci-suite`). Large-but-green beats small-but-red.
**Files (complete; see §2 Wave A for line anchors):**
1. Template sidecars: rebuild 3 `_intro.md` (O2) + 3 `_rules.md` (O3); confirm `changelog/_format.md` DELETED (O1).
2. `init-project.sh` (O1/O4/O8): install-map + self-doc + S11 copy + greenfield-mirror removal.
3. `migrate-v10-to-v11.sh:453` (A-1): drop `_format.md` from the changelog `support_basenames` branch + comments. *(The flag-wiring removal is Wave B / B1.)*
4. `_lib.sh:136` drop `_format.md`; `:123` add `_index.md` (O1, MUST-4).
5. `validate-pack.py` (O9 Check-43 + O25 Check-29 + O20-prose + the NEW empty-template leg + schema parser — the WHOLE validate-pack change, §4 fold).
6. `.operating-doc-history-allowlist.txt` / `.operating-doc-deferred-feature-allowlist.txt` / `.dangling-ref-allowlist.txt` (O26).
7. `tracker.toml.project-example` + `tracker.toml.pack-example` + `tracker-init.sh` emitter (O25/DR-4/MUST-B).
8. `test-fixtures/build.sh` round-trip removal + `README.md` prose (O23/BLOCKER-2/SHOULD-B).
9. Wave-A EXISTING-test inversions: `test-per-entry.sh:231-232`(+`_index.md` assert), `test-init-project.sh`(4.2-4.6), `contract-greenfield.sh`, `contract-migration.sh`(`_format.md` only — the mirror-comment `:475` is B1), `test-v11-realistic-ot.sh`(A.13), `test-validate-pack-check-43.sh`(O24), the 2 project-side-refs skeletons (O24), `tracker-config-schema-test.sh`(Test 7, BLOCKER-A), `tracker-init-test.sh`(3.5).
10. Doc refs: `README.md:149-154`, `PACK-AGENTS.md:213`, `MERGE-STRATEGY.md:270`+`:267-280` (O1 doc share, SHOULD-C).
**Verification (FULL battery; A-1):**
- `python3 scripts/validate-pack.py` exit 0 (all 14 Check 39/41 cleared + Check 29 + Check 43 + operating-doc check green + the new empty-template leg green).
- `bash test-fixtures/build.sh --all --clean` then `--verify` exit 0 (round-trip subject abolished; v11-realistic-ot + persona-contract fixtures build).
- FULL shell shard battery green, specifically re-run-to-green: `test-per-entry.sh`, `test-init-project.sh`, `test-validate-pack-check-43.sh`, `tracker-config-schema-test.sh`, `tracker-init-test.sh`, the persona-contract runner, `test-v11-realistic-ot.sh`, **AND the ENTIRE migration battery** `test-migrate-v10-to-v11{,-dry-run,-gates,-decompose}.sh` — ALL FOUR fully green at A1 (A-1: green restored once sidecars rebuilt + validate-pack clears — the migrator's internal Gate-2 runs validate-pack). **`test-migrate-v10-to-v11-decompose.sh` is FULLY GREEN at A1 (45/0), force-overwrite asserts INCLUDED** (`2.0b, 4.1a/b/c, 4.2a` PASS at A1 — the flag still exists at A1; O7 removes it in B1). MUST-1 correction (EE-C): there is NO "expected-RED-until-B1" decompose carve-out at A1; any RED decompose assert at the A1 commit is a REAL A1 regression, not a wave boundary. The decompose force-overwrite + `2.2a/b/c` inversions are a B1 SINGLE-WAVE break (lock-step with O7 in B1) — see §5.
- Grep gates (A-3, direct pathspecs): `_format.md` FORBIDDEN grep-zero is asserted JOINTLY across A+B+C+E (the `validate-docs.sh`/`.docs-gate-allowlist.txt` share is C2) — at A1 the reviewer asserts the Wave-A `_format.md` surfaces are clean, not the global zero (§7 GAP-1).
- Manifest (BD-228): A1's `project-template/*` + `test-fixtures/build.sh` + `scripts/*` edits are manifest inputs (and the D-wave's `supporting-docs/METHODOLOGY.md` + `INSTALL-PROCEDURES.md` are too — SHOULD-1) → at PUSH run `scripts/manifest-sync.sh` ONCE at the green final HEAD (expect exit 10 → regenerated `manifest.txt`); Check 62 + `build.sh --verify` enforce (§5 manifest note).
**Bounded review/fix cycle:** ≤2 review/fix pairs + 1 final reviewer (architect escalation if dirty after final).

### COMMIT B1 — migrator overhaul + DR-1 two-var divergence-gate removal + migration-test inversions (after A1; serial within)
**Deliverables:** O4(decompose-side) + O7 + DR-1 + the O7 migration-test inversions.
**Files:** `decompose.sh`, `migrator-core.sh`, `migrate-v10-to-v11.sh`(flag wiring — NOT the `:453` copy-source already done in A1), `test-migrate-v10-to-v11-decompose.sh`(2.2a/b/c + Group-4 force-overwrite asserts), `test-migrator-core.sh`, `test-v11-realistic-ot.sh`(Group B delete), `contract-migration.sh:475`. SERIAL within (the files interlock via the flag). O7 does NOT edit `mirror-generate.sh` (BD-249 deletes it).
**Verification:** FULL battery green; `test-migrate-v10-to-v11-decompose.sh` fully green on the NEW (no-flag) shape — it was already 45/0 at A1 (EE-C), and B1 INVERTS the `2.2a/b/c` + force-overwrite asserts (`2.0b, 4.1a/b/c, 4.2a`) lock-step with O7's flag removal so the suite stays green after the flag is gone; the two-var grep-zero gate partial (the `mirror-generate.sh` consumers remain until BD-249 — the gate reaches zero JOINTLY across B1+A1(build.sh)+BD-249). DR-1 gate (A-3, direct pathspecs): `git grep -nE '_MIGRATOR_FORCE_OVERWRITE_MIRROR|PE_FORCE_OVERWRITE_MIRROR|force-overwrite-mirror' -- ':!maintenance-docs/' ':!changelog/' ':!backlog/'` — B1 clears the `_MIGRATOR_*`/bridge/Group-4 hits; reviewer asserts B1's share clean, not global zero.

### COMMIT B1b — O22-proj project-side dead-mirror constants/asserts (after A1; [SF] with A1 + BD-249)
**Deliverables:** O22-proj.
**Files:** `_lib.sh:109,121,129` (remove project mirror constants) [SF — A1 first]; `test-per-entry.sh:219-221` (remove project mirror-filename asserts) [SF — A1 first; BD-249 after].
**Commit-framing flag (Check-36):** `test-per-entry.sh` is touched by A1 (O1, project) + B1b (O22-proj, project) + BD-249 (pack). A BD-206 commit touching `test-per-entry.sh` carries PROJECT edits; BD-249 carries PACK edits in a SEPARATE later commit. B1b is project-scoped → MAY use `project-only` IF it touches no pack-only paths; but `_lib.sh` is a shared lib (not under `project-template/`/`supporting-docs/`) so `project-only` would DENY it (Check-36 denies non-project paths under `project-only`). **RESOLUTION:** B1b uses NEUTRAL framing (no scope keyword) — it edits `_lib.sh` (shared) + `test-per-entry.sh` (shared test). FLAG for Pack Chat. (Same applies to A1, B1 — all neutral framing, since they span `scripts/` + `project-template/`.)
**Verification:** FULL battery green; `test-per-entry.sh` green (project mirror asserts removed).

### COMMIT B2 — O5 detect.sh/pack-help.sh client repoint (after A1; parallel to B1/B1b/B3)
**Deliverables:** O5 + O5b.
**Files:** `detect.sh`, `pack-help.sh`, `test-detect.sh`, `pack-help-test.sh`. `_SANCTIONED_PACK_SIDE_SHIPPED` + install map UNCHANGED (Check 47). Check 40 unaffected (new probe outside DENY-LIST-CONTENT markers).
**Verification:** FULL battery green; `test-detect.sh` + `pack-help-test.sh` green (client-tree probe asserted); Check 47 set-equality unchanged.

### COMMIT B3 — O6 recommendation.sh repoint (after A1; parallel)
**Deliverables:** O6.
**Files:** `recommendation.sh`, `recommendation-test.sh`.
**Verification:** FULL battery green; `recommendation-test.sh` green (per-entry-tree fixtures).

### COMMIT C2 — O10 client bash validator leg (after A1; FIRST in the serial Wave-C chain; reads A's schema)
**Deliverables:** O10.
**Files:** `project-template/scripts/validate-docs.sh` [SF — shared with C3/C4/C5 → SERIALIZE C2→C3→C4→C5, MUST-2], `project-template/scripts/.docs-gate-allowlist.txt` (the `_format.md`/mirror allowlist drops — completes the EE-6 grep-zero set on the client surface).
**Schedule (MUST-2):** FIRST of the serial Wave-C chain; its worktree bases at A1's landed HEAD. C3 bases at C2's landed HEAD, etc. Wave C runs PARALLEL to Waves B/D/E (cross-wave disjoint) but is INTERNALLY serial.
**Verification:** FULL battery green; `validate-docs.sh` validates a populated client tree (form-family + structured + no monolith + correct vocabulary), parsing the SAME `_rules.md` schema block as O9; `_format.md` FORBIDDEN grep-zero now clean on the client-validator surface.

### COMMIT C3 — O11 `_index.md` generator + MANDATORY validator + tests (after C2; serial Wave-C chain)
**Deliverables:** O11.
**Files:** new `scripts/lib/per-entry/` generator + validator scripts; the `_index.md` validation leg in `scripts/validate-pack.py` [SF — A1's O9 leg + C4/C5 legs → SERIALIZE A1→C3→C4→C5, MUST-2] (pack repo CI per DECISIONS §13 Item-7) AND in `project-template/scripts/validate-docs.sh` [SF — shared C2/C3/C4/C5] (client leg, "both repos"); tests. (`_lib.sh:123` admission already in A1.) Realizes the rule-8 chain (§8): in-code docstring naming the realized consumer + the architect-doc addendum cross-ref + the IMPL-REPORT link.
**Schedule (MUST-2):** bases at C2's landed HEAD; serializes with A1/C4/C5 on validate-pack.py and with C2/C4/C5 on validate-docs.sh.
**Verification:** FULL battery green; `_index.md` derives a topological seed from `Blockers`/`Unblocks`; validator enforces (1) hard-dependency-order consistency + (2) per-entry↔`_index.md` membership sync; gold conforms (zero violations on the impl-plan stream).

### COMMIT C4 — O12 changelog conformance + tests (after C3; serial Wave-C chain)
**Deliverables:** O12.
**Files:** the changelog conformance check in `scripts/validate-pack.py` [SF — A1/C3/C5 → SERIALIZE, MUST-2] (pack leg) AND `project-template/scripts/validate-docs.sh` [SF — C2/C3/C5] (client leg) — "both repos", parses changelog `_rules.md` `## Entry structure`; + tests.
**Schedule (MUST-2):** bases at C3's landed HEAD; serializes on both validator files.
**Verification:** FULL battery green; against the production changelog gold (55 entries): core `{Summary + Test count + ≥1 Files field}` present, doc-only exemption works, `entry-max-lines ≤ 180`, `summary-max-words ≤ 250` (gold max 130/243 → zero violations). Cheap deterministic counts (ci-runtime-compounding).

### COMMIT C5 — O13 naming-conformance guard + tests (after C4; LAST in the serial Wave-C chain)
**Deliverables:** O13.
**Files:** the §3.5 graceful naming guard in `scripts/validate-pack.py` [SF — A1/C3/C4 → SERIALIZE, MUST-2] (pack leg) AND `project-template/scripts/validate-docs.sh` [SF — C2/C3/C4] (client leg), "both repos"; + tests.
**Schedule (MUST-2):** bases at C4's landed HEAD; LAST validate-pack.py + validate-docs.sh writer in the Wave-C chain.
**Verification:** FULL battery green; guard fires only on a Phase-prefixed heading violating the template; gold conforms (zero violations); no forced refactor.

### COMMITS D1–D6 — governance/docs/prompts (after A1; mutually parallel, disjoint files)
- **D1 (O15):** trinity `## Document locations` + mirror prose → no-mirror. ALL 3 files (`project-template/{CLAUDE,AGENTS,GEMINI}.md`) in ONE commit (trinity rule); cross-CLI normalization per ARCHITECTURE-BD-182 §4.1. **MAJOR substantive rewrite → coder** (pack-chat-only file but `pack-chat-minor-edits-only` routes substantive landed-content rewrites to coder).
- **D2 (O16):** PM-CHAT plan model + STATUS header.
- **D3 (O17):** agent prompts + HELP-FRAGMENT + SETUP/INSTALL/CLI-PM.
- **D4 (O14):** METHODOLOGY wholesale plan-model rewrite.
- **D5 (O18):** skill masters → no-mirror + pack-copy↔master parity verify (reviewer diffs master vs each of `.claude/.codex/.gemini`).
- **D6 (O21):** MIGRATION-v10-to-v11.md no-mirror rewrite. COHERENCE GATE: O21 removes the `--force-overwrite-mirror` user narrative; it MUST land consistent with B1's flag removal (if the flag survived in code the doc would lie). Since B1 (flag removal) and D6 are both after A1 and the doc only DESCRIBES the flag, D6 may land in parallel with B1 — but the reviewer confirms code-vs-doc consistency at the batch end.
**Verification (each):** FULL battery green; operating-docs rule (zero history/deferred-feature, terse); trinity parity (D1); pack-copy parity (D5); doc-vs-code coherence (D6 ↔ B1).

### COMMIT E1 — O19 tracker library reconciliation + the Wave-E test inversion (after A1; independent of B/C/D)
**Deliverables:** O19.
**Files:** the dormant `tracker-*.sh` M-mono surfaces (§2 Wave E) — **A-2: the row-10 REPOINT is bounded to the genuine M-mono surfaces; the `:2142-2172` M-track staleness probe inside `:1992-2253` is KEEP, do NOT repoint it.** KEEP (untouched): `tracker-mirror.sh`, `tracker-header-snapshot.sh`, the reverse-emit, the `tracker-config/*.toml` fixtures, `flat-file-mode.toml` (A-6). Test lock-step (MUST-A): `tracker-agent-read-test.sh:34-78,190-192` (the BD-214 C1 recurrence file) + any `tracker-doctor`/`tracker-promote`/`tracker-phase-task` test asserting a project-monolith read.
**Verification:** FULL battery green; the dormant family carries no M-mono assumption; `tracker-agent-read-test.sh` + the other repointed tests green; `git grep -nE 'until BD-206' -- <tracker files> scripts/validate-pack.py` (A-3, direct pathspecs) reaches zero post-A1/E1; M-track KEEP surfaces UNTOUCHED (tracker stays gated OFF, BD-214).

### DOWNSTREAM COMMIT — BD-249 (O22-pack, `pack-only`, after BD-206 fully landed)
**Deliverables:** O22-pack (BD-249's deliverable, NOT a BD-206 wave).
**Files:** §2 Downstream. DELETE `mirror-generate.sh` (TERMINAL — after A1 removes its build.sh source line + B1 removes the migrator callers, only dead tests remain).
**Commit-framing:** `pack-only` (Check-36) — touches only `scripts/` pack-side. But `test-per-entry.sh` + `_lib.sh` are shared with BD-206 commits: BD-249 edits the PACK groups/constants in a SEPARATE later commit; the BD-206 commits already landed their project edits. The pack-only keyword is valid IFF BD-249's diff touches no `project-template/`/`supporting-docs/` paths (it does not).
**Verification:** FULL battery green; the JOINT mirror-subsystem grep-zero gate (A-3): `git grep -nE 'mirror-generate|per_entry_regenerate_mirror|pe_canonical_mirror_for_stream|mirror\) printf' -- ':!maintenance-docs/' ':!changelog/' ':!backlog/'` returns ZERO (currently 81 hits); the two-var force-overwrite gate returns ZERO (currently 103 hits); `per_entry_regenerate_toc` survives.

---

## 4. File-dependency + same-file serialization analysis (from the rule-10 map)

The rule-10 map (ARCHITECTURE-BD-206.md §6) gives the wave DAG; this section produces the concrete parallel-vs-serial COMMIT schedule and names every same-file collision and its resolution.

### Wave DAG (commit schedule)
```
A1  →  { B1, B1b, B2, B3 }        (Wave B — parallel EXCEPT B1b serializes with A1 on _lib.sh + test-per-entry.sh)
A1  →  C2 → C3 → C4 → C5           (Wave C — SERIAL, NOT parallel [MUST-2]: every C-commit edits validate-docs.sh, and C3/C4/C5 also edit validate-pack.py; read A1's schema; C1=O9 residual FOLDED into A1)
A1  →  { D1, D2, D3, D4, D5, D6 }  (Wave D — parallel, disjoint files)
A1  →  { E1 }                      (Wave E)
{ BD-206 fully landed }  →  BD-249 (downstream BD; terminal mirror-subsystem state)
```
Within Wave B, B1 is serial-internal (decompose/migrator-core/migrate-v10-to-v11 interlock via the flag). B2/B3/B1b are mutually parallel. **Wave C is INTERNALLY SERIAL (C2→C3→C4→C5; MUST-2): all four C-commits extend `validate-docs.sh` (the single 501-line client validator) and C3/C4/C5 also extend `validate-pack.py` (the single 11,799-line monolithic validator, 66 inline `check_*` funcs — a new check is a new inline function in this ONE file) — so they CANNOT be parallel worktree commits (they would collide at sequential patch-apply).** Wave D (disjoint docs) + Wave E (tracker libs) ARE mutually parallel with Wave B and with the Wave-C SERIAL CHAIN (D/E share no file with B or C — verified below). All worktree waves run in isolated worktrees off this map; each later C-commit bases its worktree at the prior C-commit's landed HEAD.

### Same-file collisions (named + resolved)

| File | Touching commits | Resolution |
|---|---|---|
| **`scripts/lib/per-entry/_lib.sh`** | A1 (O1 `:123` add `_index.md`, `:136` drop `_format.md`) → B1b (O22-proj `:109,121,129`) → BD-249 (pack `:85,99,150-152`, branch, header) | SERIALIZE A1 → B1b → BD-249. A1 lands first; B1b edits after A1; BD-249 is the last writer (separate later BD). NEVER parallel-write. |
| **`scripts/tests/test-per-entry.sh`** | A1 (O1 `:231-232` + `_index.md` assert) → B1b (O22-proj `:219-221`) → BD-249 (Groups 3/4/8/9, `:70,600`) | SERIALIZE A1 → B1b → BD-249. Commit-framing: BD-206 commits carry the project edits with NEUTRAL framing (the file is shared); BD-249 carries the pack edits as a separate `pack-only` commit. |
| **`scripts/validate-pack.py`** (single 11,799-line monolith; new check = new inline `check_*` func) | A1 (O9: Check-43 + O25 Check-29 + O20-prose + the NEW empty-template leg + schema parser; C1=O9-residual FOLDED into A1) → **C3 (O11 `_index.md` validation leg) → C4 (O12 changelog conformance leg) → C5 (O13 naming-guard leg)** | **TWO-PART resolution (MUST-2).** (1) O9's validate-pack.py edits FOLD into A1 (RECOMMENDED by design §6 C1 note: one-file, avoids a second-touch serialization; C1 disappears as a separate commit). (2) But O11/O12/O13 EACH "join the conformance enforcement, **both repos**" (design O11/O12/O13) → per DECISIONS §13 Item-7 the pack-repo leg IS `validate-pack.py`. So C3/C4/C5 ALSO touch validate-pack.py — they do NOT all fold into A1 (they are distinct Wave-C deliverables that READ A1's schema). **RESOLUTION: SERIALIZE A1 → C2 → C3 → C4 → C5.** Each C-commit's worktree bases at the prior C-commit's landed HEAD; NEVER parallel-write validate-pack.py. (Rationale for serialize-not-fold in §4 "MUST-2 resolution rationale" below.) |
| **`project-template/scripts/validate-docs.sh`** (single 501-line client bash validator) | **C2 (O10 client per-entry schema leg + `:88` `_format.md` drop) → C3 (O11 `_index.md` client leg) → C4 (O12 changelog conformance client leg) → C5 (O13 naming-guard client leg)** | **SERIALIZE C2 → C3 → C4 → C5 (MUST-2).** O10/O11/O12/O13 EACH ship a CLIENT leg = `validate-docs.sh` (the "both repos" client half; design O10 "extend `validate-docs.sh`", O11/O12/O13 "both repos"). All four Wave-C commits extend this ONE file → they CANNOT be parallel worktree commits. This is the SECOND shared-file constraint forcing the Wave-C serial chain (the first is validate-pack.py). The reviewer surfaced validate-pack.py (C3/C4/C5); validate-docs.sh adds C2 to the serial chain. |
| **`scripts/init-project.sh`** | A1 only (O1/O4/O8) | No collision — single commit. |
| **`scripts/migrate-v10-to-v11.sh`** | A1 (`:453` copy-source, A-1) + B1 (flag wiring) | SERIALIZE A1 → B1. The `:453` `support_basenames` edit is in A1 (it must land WITH the sidecar rebuild so the migrator copies the right set + the migration battery goes green). The flag-wiring removal (DR-1) is B1. Two disjoint regions of the same file; A1 first. |
| **`scripts/lib/migrate-v10-to-v11/decompose.sh` + `migrator-core.sh`** | B1 only (serial within B1) | B1-internal serialization (flag interlock). |
| **`scripts/lib/tracker-init.sh`** | A1 (O25 emitter drop, MUST-B) | A1 only — the emitter drop is lock-step with the Check-29 flip (both in A1). NOT touched in E1. |
| **`scripts/tests/tracker-config-schema-test.sh` + `tracker-init-test.sh`** | A1 only (Test 7 + 3.5, lock-step with Check-29) | A1 only. The dormant `tracker-*.sh` repoints + their tests are E1; the Check-29-surface tests are A1. Disjoint by lock-step subject. |
| **`project-template/{CLAUDE,AGENTS,GEMINI}.md`** (trinity) | D1 only (ONE commit, all 3) | Trinity rule: all 3 in the same commit. No cross-wave collision. |
| **`test-fixtures/build.sh`** | A1 (O23 round-trip block `:510-578`) | A1 only. The `mirror-generate.sh` SOURCE LINE goes in A1; the FILE deletes in BD-249 — no edit-then-dangle window (A1's removal precedes BD-249's deletion). A-5: the `:627-632` M-track block is KEEP (disjoint region). |
| **`scripts/tests/test-init-project.sh`** | A1 (O1/O4 4.2-4.6) + BD-249 (Group-5 mirror-regen leg `:416,427`) | SERIALIZE A1 → BD-249. A1 inverts/deletes the 4.2-4.6 mirror asserts; BD-249 removes the residual Group-5 mirror-regen sourcing leg. A1 first. |

### Disjointness verification (parallel waves do not collide)
- Wave B files (`decompose.sh`, `migrator-core.sh`, `detect.sh`, `pack-help.sh`, `recommendation.sh` + their tests) are DISJOINT from Wave C (`validate-pack.py` [C3/C4/C5 legs], `validate-docs.sh` [C2/C3/C4/C5 legs], new `_index.md`/conformance/naming scripts + tests), Wave D (docs/prompts/trinity), Wave E (tracker libs). VERIFIED: Wave B shares no file with Wave C/D/E. **WITHIN Wave C, however, the four commits are NOT disjoint** — they share `validate-pack.py` (C3/C4/C5) and `validate-docs.sh` (C2/C3/C4/C5), so Wave C is INTERNALLY SERIAL (MUST-2). Wave B ∥ Wave C-chain ∥ Wave D ∥ Wave E (cross-wave disjoint); only INSIDE Wave C is there serialization.
- The cross-wave shared files anchored at A1 (`_lib.sh`, `test-per-entry.sh`, `migrate-v10-to-v11.sh`, `test-init-project.sh`, `test-fixtures/build.sh`) serialize A1 → (B1b/B1/BD-249) as tabled — Wave D/E touch none of them. **`validate-pack.py` is the EXCEPTION the prior plan got wrong (MUST-2):** A1 carries O9's leg, AND C3/C4/C5 each carry a SECOND validate-pack.py leg (O11/O12/O13 pack half) → it serializes A1 → C3 → C4 → C5 (NOT "A1 only"). Likewise `validate-docs.sh` serializes C2 → C3 → C4 → C5. So the corrected claim is: **Wave D and Wave E touch none of the shared validator files; Wave C touches BOTH validator files across all four of its commits and is therefore internally serial.**

### MUST-2 resolution rationale — serialize the Wave-C enforcement chain (do NOT fold C3/C4/C5 into A1)
The adversarial review (MUST-2) proved O11(C3)/O12(C4)/O13(C5) each add a pack-side leg to the SINGLE monolithic `validate-pack.py`, so the prior §4 "parallel Wave-C + C/D/E touch none of validate-pack.py" claim was FALSE. Reconciliation surfaced a SECOND collision the review did not enumerate: `validate-docs.sh` is ALSO a single file, and O10/O11/O12/O13 each add a CLIENT leg to it ("both repos") → C2 joins the collision set too. Three options were offered (a) serialize C3→C4→C5; (b) fold the three new validate-pack.py checks into A1; (c) batch the three new checks into one Wave-C validate-pack.py commit + keep client-leg/test commits parallel.

**CHOSEN: (a), generalized to the whole Wave-C chain — SERIALIZE A1 → C2 → C3 → C4 → C5; each commit lands its FULL deliverable (pack leg in validate-pack.py + client leg in validate-docs.sh + its tests) atomically.** Rationale:
- **(b) fold-into-A1 REJECTED:** A1 is already the largest commit (the whole O0-O9-O23-O24-O25-O26 atomic set). The C1-into-A1 fold was justified because C1 is a *residual of O9* (same deliverable, already in A1). O11/O12/O13 are DISTINCT deliverables that READ A1's `_rules.md` schema and have NO pre-existing test to break — the design deliberately placed them in Wave C (after A). Folding three substantial new validators + their tests into A1 bloats an already-huge atomic commit and harms reviewability, with no correctness gain (serialize achieves the same no-collision result).
- **(c) batch-pack-leg-split-from-client-leg REJECTED:** it splits a single deliverable's pack leg (validate-pack.py) from its client leg (validate-docs.sh) into different commits, violating `enumerate-encoding-surfaces` (each surface lands with its lock-step encodings) and creating an intermediate state where the pack validates a schema the client does not yet (or vice versa). It ALSO does not help: the client legs still collide on validate-docs.sh, so "keep client-leg commits parallel" is itself false.
- **(a) generalized CHOSEN:** keeps each O10/O11/O12/O13 deliverable atomic + coherent (pack + client + tests together), honors worktree-isolation (no parallel writes to validate-pack.py or validate-docs.sh), keeps each commit independently reviewable, and keeps A1 bounded. **Trade-off accepted: Wave C loses internal parallelism (was never real — both validator files are shared by all four C-commits) in exchange for a bounded A1 + coherent deliverables.** The commit COUNT is unchanged (Wave C is still 4 commits C2/C3/C4/C5); only the SCHEDULE changes parallel→serial. Wave C still runs PARALLEL to Waves B/D/E (cross-wave disjoint).

### Why O25 is in Wave A, not Wave E
The client `[mirror]` drop is lock-step with the Check-29 `mirror_required` flip AND `tracker-config-schema-test.sh` Test 7 (which PINS the flag) AND the live emitter + `tracker-init-test.sh` 3.5. If any split across waves, the intermediate state is Check-29-RED or battery-RED (Test 7) or self-contradictory (emitter re-emits the dropped table). All FIVE land together in A1. The dormant `tracker-*.sh` repoints (O19) stay E1 — they carry their own Wave-E test break (`tracker-agent-read-test.sh`).

---

## 5. Per-wave verification strategy (A-1 BINDING) — empirically determined breaking-test set

> **A-1 mandate:** each wave's verification EMPIRICALLY runs the FULL battery (validate-pack.py + build.sh + ALL shell shards incl. the migration battery). I independently determined the COMPLETE per-wave breaking-test set (did NOT trust EE-9's sample). Empirical-Evidence blocks follow.

### EE-A — validate-pack.py at the partial Wave-A state (the 14 deleted-sidecar rows)
- **Command:** `python3 scripts/validate-pack.py 2>&1 | grep -E 'Check (39|41)|issue'`
- **Verbatim output:** Check 39 FAIL ×7 (`backlog/_intro.md`, `backlog/_rules.md`, `changelog/_format.md`, `changelog/_intro.md`, `changelog/_rules.md`, `implementation-plan/_intro.md`, `implementation-plan/_rules.md`); Check 41 FAIL ×7 (same paths); `FAILED — 14 issue(s) found`.
- **HEAD/date:** `66c8332`, 2026-06-26, working tree = 8 deletions.
- **Interpretation:** EXACTLY the EE-4 set — the dangling `cmd_update`/`_CLIENT_INSTALLED_FILES` references to the 7 hand-deleted sidecars. A1 rebuilds 6 + drops the 2 `_format.md` rows → clears all 14.
- **Conclusion: SUPPORTED.** A1 is the commit that clears validate-pack.

### EE-B — the migration battery is RED at the partial Wave-A state (A-1's specific concern; beyond EE-9's sample)
- **Command:** `bash scripts/tests/test-migrate-v10-to-v11-decompose.sh` / `…-v11.sh` / `…-dry-run.sh` / `…-gates.sh` (each, tail summary).
- **Verbatim output:** `test-migrate-v10-to-v11-decompose.sh → Passed: 33 Failed: 12`; `test-migrate-v10-to-v11.sh → Passed: 48 Failed: 6`; `test-migrate-v10-to-v11-dry-run.sh → Passed: 57 Failed: 4`; `test-migrate-v10-to-v11-gates.sh → Passed: 80 Failed: 7`. Failing asserts are uniformly `rc=0 — got 31` (EXIT_GATE_FAILED) plus, in decompose, `2.1a-f per-entry … missing` and the force-overwrite asserts (`2.0b`, `4.1a/b/c`, `4.2a`).
- **HEAD/date:** `66c8332`, 2026-06-26, 8 deletions.
- **Interpretation (root cause):** TWO causes. (1) The migrator copies its sidecar decompose-source from `project-template/docs/project/<stream>/{_rules.md,_intro.md,_format.md}` (`migrate-v10-to-v11.sh:443-462`); the 8 deletions removed those sources. (2) The migrator's Gate 2 RUNS `validate-pack.py` internally (confirmed: `test-migrate-v10-to-v11-gates.sh` fails `2.1 Gate 2 OK validate-pack` + `Gate 2 PASS rc=0 got 31`); the deleted sidecars make that internal validate-pack fail → rc=31 cascades through every migration test. So the rc=31 failures are DOWNSTREAM of EE-A.
- **Conclusion: SUPPORTED.** These four migration tests break at the SIDECAR-DELETION state and are RESTORED when A1 (a) rebuilds the 6 sidecars, (b) clears validate-pack (so the internal Gate 2 passes), and (c) fixes the migrator `:453` copy-source. **THEREFORE the migration battery is a Wave-A break for the SIDECAR-driven + Gate-2 asserts** — A1 must land the migrator `:453` fix + the sidecar rebuild together to restore them. The decompose force-overwrite asserts (`2.0b`, `4.1a/b/c`, `4.2a` — which name `PE_FORCE_OVERWRITE_MIRROR` / expect `rc=25`/`rc=0`) are RED at the partial-deletion state ONLY because of the SAME Gate-2 `rc=31` cascade (they too abort at the internal validate-pack before flag logic runs) — they are NOT a flag-behavior break at A1. **At the A1 COMMIT they are GREEN (sidecars rebuilt → Gate-2 passes; flag still present); they invert to RED only at B1 when O7 REMOVES the flag + the regeneration → the test asserts invert lock-step in B1, not A1** (proven by the EE-C clean-clone control: 45/0 at HEAD with sidecars + flag present).

### EE-C — `test-migrate-v10-to-v11-decompose.sh` is GREEN at the A1 COMMIT (45/0); the force-overwrite break is a B1 SINGLE-WAVE break (MUST-1 CORRECTED)
> **MUST-1 correction.** The prior plan classified decompose as a "dual-wave break" with the force-overwrite asserts "expected-RED-until-B1 at A1." That was a MISDIAGNOSIS. The asserts return `rc=31 = EXIT_GATE_FAILED` (`migrator-core.sh:74 readonly EXIT_GATE_FAILED=31`) — they fail at the migrator's INTERNAL Gate-2 (it runs `validate-pack.py`), which is RED ONLY at the partial-deletion working-tree state (the 8 deletions make the internal validate-pack fail → rc=31 cascades). The `--force-overwrite-mirror` flag is merely HOW the test invokes apply; the partial-state failure is the Gate-2 cascade, NOT flag behavior. At the A1 COMMIT (sidecars rebuilt → Gate-2 passes; `_format.md` eliminated; flag still present because O7 is B1) decompose is FULLY green, force-overwrite asserts included. There is NO dual-wave break and NO "pull O7 into A1" fallback.
- **Re-verification control (REQUIRED, run this reconciliation pass):** clone HEAD `66c8332` to a clean `/tmp` checkout (committed state = sidecars PRESENT + `_format.md` PRESENT + flag PRESENT = the A1 state MINUS O7's flag removal), then run decompose.
- **Command (verbatim):**
  ```
  $ SCRATCH="$(mktemp -d /tmp/bd206-reconcile-clone.XXXXXX)"
  $ git clone -q --no-hardlinks . "$SCRATCH" ; cd "$SCRATCH"
  $ git rev-parse --short HEAD            → 66c8332
  $ ls project-template/docs/project/changelog/   → _format.md  _intro.md  _rules.md   (sidecars + _format.md PRESENT)
  $ grep -c '_MIGRATOR_FORCE_OVERWRITE_MIRROR' scripts/lib/migrator-core.sh   → 3   (flag PRESENT)
  $ bash scripts/tests/test-migrate-v10-to-v11-decompose.sh | tail -3
  ```
- **Verbatim output:**
  ```
  === Summary ===
  Passed: 45
  Failed: 0
  All BD-165 decompose tests passed.
  ```
- **HEAD/date:** clean clone of `66c8332`, 2026-06-26, committed state (NO working-tree deletions).
- **Interpretation:** with sidecars present (Gate-2 passes) AND the flag present, ALL 45 decompose asserts pass — INCLUDING `2.0b, 4.1a/b/c, 4.2a`. The A1 COMMIT is exactly this state (sidecars rebuilt by A1; flag not yet removed — O7 is B1) → decompose is GREEN at A1. The asserts flip to RED ONLY at B1, when O7 removes the flag + the regeneration — precisely where the plan already schedules their inversion (§3 COMMIT B1).
- **Conclusion: SUPPORTED.** `test-migrate-v10-to-v11-decompose.sh` is a **SINGLE-WAVE break at B1**, not a dual-wave break. The prior plan conflated "RED at the pre-A1 working-tree-deletion state (Gate-2 cascade)" with "RED at the A1 commit HEAD." **A1 is FULLY battery-green INCLUDING decompose (45/0).** The B1 inversion of `2.2a/b/c` + the force-overwrite asserts (`2.0b, 4.1a/b/c, 4.2a`) lands lock-step with O7 in B1 (already correctly scheduled). The A1 reviewer asserts decompose 45/0 at A1; a RED decompose assert at the A1 commit is a REAL regression, NOT a tolerated wave boundary.

### EE-D — the non-migration Wave-A inversions are green-on-OLD-shape (flip when the source changes)
- **Command:** `bash scripts/tests/test-per-entry.sh` ; `…test-init-project.sh` ; `…test-validate-pack-check-43.sh` ; `…tracker-config-schema-test.sh` ; `…tracker-init-test.sh` ; `…tracker-agent-read-test.sh` (each summary).
- **Verbatim output (partial Wave-A state):** `test-per-entry.sh → PASS: 57 FAIL: 0`; `test-init-project.sh → Passed: 39 Failed: 25`; `test-validate-pack-check-43.sh → PASS: 9 FAIL: 0`; `tracker-config-schema-test.sh → PASS: 40 FAIL: 0`; `tracker-init-test.sh → Passed: 104 Failed: 0`; `tracker-agent-read-test.sh → Passed: 57 Failed: 0`.
- **Interpretation:** `test-init-project.sh` is RED NOW (25 fail — `_rules.md`/`_intro.md` missing, install `rc=30`) → a Wave-A break restored when A1 rebuilds the sidecars + fixes the installer. The others are GREEN-on-old-shape: `test-per-entry.sh:231-232` passes because `_lib.sh:136` still says `_format.md` (flips when O1 drops it); `test-validate-pack-check-43.sh` passes the old allowlist (flips when O9 edits it); the tracker tests pass the old shape (flip when O25 lands). So their inversions land WITH their deliverables in A1.
- **Conclusion: SUPPORTED.** A1's composition (the EE-9 inversions + A-1's migration battery) is the complete Wave-A breaking set. The A1 reviewer re-runs ALL of these to green (except decompose's force-overwrite asserts per EE-C).

### EE-E — build.sh fixture rebuild breaks at the partial Wave-A state (O23 is Wave A)
- **Command:** `bash test-fixtures/build.sh --all --clean` (then `--name v11-realistic-ot --clean`).
- **Verbatim output:** the rebuild builds `v10-minimal` + `v10-realistic-ot` OK, then STALLS at `── building v11-realistic-ot ──` (no completion within the timeout). The v11 fixture sources the working-tree pack HEAD (`PACK="$PACK_ROOT" init-project.sh`), which fails `rc=30` on the deleted sidecars (matching EE-D's `test-init-project.sh:3.1 rc=0 got 30`).
- **Interpretation:** the v11 fixture build depends on a working `init-project.sh` + the `mirror-generate.sh` round-trip. Both are Wave-A surfaces. O23's round-trip removal + A1's installer fix restore the build. NOTE `build.sh --verify` (no rebuild) is GREEN at the partial state (it checks committed SHAs); only `--all --clean` exercises the break — and CI runs `build.sh --all --clean` then `--verify` (`validate-pack.yml:193,195`).
- **Conclusion: SUPPORTED.** O23/build.sh is correctly Wave A.

### EE-F — the Wave-B/C/D/E breaks are localized to their deliverables (green-on-old-shape at A1)
- **Command:** `bash scripts/test-detect.sh` ; `…recommendation-test.sh` ; `…pack-help-test.sh` (the Wave-B client-read candidates).
- **Verbatim output (partial Wave-A state):** `test-detect.sh → pass (5/4 groups)`; `recommendation-test.sh → Passed: 53 Failed: 0`; `pack-help-test.sh → Passed: 21 Failed: 0`.
- **Interpretation:** these are GREEN at A1 — they don't depend on the sidecar deletion; they break only when O5/O6 repoint the client reads (B2/B3), and their inversions land WITH those deliverables. Likewise the tracker dormant tests (`tracker-agent-read-test.sh` 57/0 at A1) break only at E1 (O19). The Wave-C enforcement legs are NEW checks (no pre-existing test to break). Wave-D is docs (no test-break; operating-docs + trinity + parity checks).
- **Conclusion: SUPPORTED.** Each later wave carries its OWN breaking tests; none is a Wave-A break. Per-wave green = the FULL battery green after that wave's commit.

### Per-wave verification protocol (every commit)
Each commit's bounded review/fix cycle (≤2 review/fix pairs + 1 final reviewer; architect escalation if dirty after final) ends with the FULL CI battery run, EMPIRICALLY (not validate-pack alone):
1. `python3 scripts/validate-pack.py` exit 0.
2. `bash test-fixtures/build.sh --all --clean && bash test-fixtures/build.sh --verify` exit 0.
3. The auto-discovered shell shard battery (all `scripts/test*.sh` + `scripts/tests/*.sh` + `scripts/tests/fixture-dependent/*.sh`) green — including the migration battery (A-1) and the tracker family.
4. The wave's grep-zero / allowlist gates (A-3, direct git pathspecs).
The GREEN-AT-FIRST-WAVE guarantee for A1 (MUST-1 CORRECTED): after A1 the FULL battery is green — INCLUDING `test-migrate-v10-to-v11-decompose.sh` (45/0, force-overwrite asserts included; the flag still exists at A1, so they PASS — EE-C control). There is NO decompose carve-out at A1; any RED decompose assert at the A1 commit is a real A1 regression. The decompose force-overwrite + `2.2a/b/c` inversions are a B1 SINGLE-WAVE break (O7 removes the flag in B1; the asserts invert lock-step there). After B1 the decompose battery is fully green again on the new (no-flag) shape. After every other commit, its own breaking tests are green.

### CI-runtime-compounding (new checks O9-O13 must be cheap)
The NEW enforcement checks (empty-template leg O9, client leg O10, `_index.md` validator O11, changelog conformance O12, naming guard O13) run on EVERY validate-pack invocation (~155×/battery). KEEP them cheap: deterministic line/word counts (O12), template-shape match (O9/O13), parse-the-schema-once (O9/O10 reuse the `pe_supporting_files_admitted` awk grammar — no per-entry subprocess storm). The `_index.md` validator (O11) is structural (topological + membership-sync) — bounded to the impl-plan stream's phase files, not a whole-tree scan. FLAG: the O11 validator + O12 conformance MUST draw their candidate set from `git ls-files` (per ci-guard-measure-then-bound), never a raw filesystem walk; SKIP-lenient if git absent.

### Manifest interplay (BD-228 / Check 62) — A1 AND the D-wave perturb manifest inputs (SHOULD-1 RESOLVED)
`scripts/lib/manifest-inputs.sh` declares FOUR manifest-input globs relevant here (verified at HEAD `66c8332`): `project-template/*` (`:56`), `scripts/*` (`:57`), `test-fixtures/build.sh` (`:58`), `supporting-docs/METHODOLOGY.md` (`:59`), `supporting-docs/INSTALL-PROCEDURES.md` (`:60`). So MORE than just A1 perturbs the manifest:
- **A1** rebuilds the project-template sidecars (`project-template/*`) AND edits `test-fixtures/build.sh` (O23) AND edits `scripts/*` → all manifest inputs.
- **The D-wave ALSO perturbs manifest inputs (SHOULD-1):** O14/D4 rewrites `supporting-docs/METHODOLOGY.md` (a declared input at `:59`) and O17/D3 edits `supporting-docs/INSTALL-PROCEDURES.md` (a declared input at `:60`). (Note: these are the `supporting-docs/` SOURCE files — the manifest keys on the source, not the install-time `docs/pack/` copy.) So the manifest is perturbed by A1 AND the D-wave, NOT A1 alone.
- **This is NOT a correctness break.** The push-time `scripts/manifest-sync.sh` regenerates `test-fixtures/manifest.txt` from the WHOLE input set in one pass, regardless of WHICH wave changed an input — so it captures the A1 sidecar/build.sh changes AND the D-wave supporting-docs changes together at push (expect exit 10 → commit the regenerated manifest with user approval, per the active-state memory). Check 62 + `build.sh --verify` enforce manifest correctness at CI.

The manifest is regenerated ONLY at push, ONLY when a fixture input changed — NOT per-commit (regenerate-manifest-push-time). The orchestrator runs `manifest-sync.sh` ONCE at the green final HEAD (after A1 + B + C + D + E land), capturing every perturbed input. A broken build.sh (O23) would break Check 62, so O23's redesign must leave `build.sh --verify` green (verified achievable — the round-trip subject is abolished, not the build).

---

## 6. Commit plan (what goes in each commit, what order)

| # | Commit | Scope keyword | Deliverables | Depends on |
|---|---|---|---|---|
| 1 | **A1** Wave-A foundational atomic full-battery-green | NEUTRAL (spans `scripts/` + `project-template/`) | O0,O1,O2,O3,O4(init),O8,O9(ALL validate-pack),O20-prose,O23,O24,O25,O26 + A-1 migrator copy-source + all Wave-A test inversions | — (FIRST, alone) |
| 2 | **B1** migrator overhaul + DR-1 + migration-test inversions | NEUTRAL | O4(decompose),O7,DR-1 | A1 |
| 3 | **B1b** O22-proj project dead-mirror constants/asserts | NEUTRAL (`_lib.sh`/`test-per-entry.sh` shared) | O22-proj | A1 (SF `_lib.sh`,`test-per-entry.sh`) |
| 4 | **B2** detect.sh/pack-help.sh client repoint | NEUTRAL | O5 | A1 |
| 5 | **B3** recommendation.sh repoint | NEUTRAL | O6 | A1 |
| 6 | **C2** client bash validator leg | NEUTRAL | O10 | A1 (FIRST of serial Wave-C chain; SF `validate-docs.sh`) |
| 7 | **C3** `_index.md` generator + validator | NEUTRAL | O11 | C2 (serial; SF `validate-pack.py` + `validate-docs.sh`) |
| 8 | **C4** changelog conformance | NEUTRAL | O12 | C3 (serial; SF `validate-pack.py` + `validate-docs.sh`) |
| 9 | **C5** naming guard | NEUTRAL | O13 | C4 (LAST of serial Wave-C chain; SF `validate-pack.py` + `validate-docs.sh`) |
| 10 | **D1** trinity Document-locations | `pack-chat-only` (project-template trinity) OR neutral if mixed | O15 | A1 |
| 11 | **D2** PM-CHAT + STATUS + D3 prompts + D4 METHODOLOGY + D5 skills + D6 MIGRATION | `project-only` (all under `project-template/`+`supporting-docs/`) — verify per commit | O16,O17,O14,O18,O21 | A1 |
| 12 | **E1** tracker library reconciliation | NEUTRAL (`scripts/lib/tracker-*.sh`) | O19 | A1 |
| 13 | **BD-249** pack dead-mirror removal (SEPARATE BD) | `pack-only` | O22-pack | BD-206 fully landed |

**Commit-count note (authoritative; reconciled NIT-1 + MUST-2):** the fixed part of the count is A1(**1**) + Wave B(**4**: B1, B1b, B2, B3) + Wave C(**4**: C2, C3, C4, C5 — SERIAL per MUST-2, but still 4 commits) + Wave E(**1**: E1) = **10**. The ONLY variable is Wave D's granularity: D1 (trinity) MUST be its own commit; D2-D6 MAY land as ONE combined `project-only` commit (all under `project-template/`+`supporting-docs/`, disjoint files, no test break) OR as up to 6 separate commits. So:
- Wave D combined (D1 + one D2-D6) = **2** D-commits → BD-206 = 10 + 2 = **12 commits** (A1, B1, B1b, B2, B3, C2, C3, C4, C5, D1, D-combined, E1).
- Wave D fully split (D1..D6) = **6** D-commits → BD-206 = 10 + 6 = **16 commits**.

RECOMMENDED: D1 separate + D2-D6 each separate for clean review = the design's 6 D-commits → **16 BD-206 commits**; the orchestrator may combine the disjoint `project-only` D2-D6 into fewer (down to 12). Either way BD-249 = **+1** (separate BD). **The PLAN's authoritative minimum: A1 is ONE atomic commit; Wave C is 4 SERIAL commits; the rest are ≥1 commit per the §3 breakdown; same-file commits serialize.**

> AUTHORITATIVE COMMIT COUNT (reconciles §0; supersedes the prior "13"): the precise count depends ONLY on the orchestrator's Wave-D commit-granularity choice. **Range: 12 (A1 + B1/B1b/B2/B3 + C2/C3/C4/C5 + D1 + D-combined + E1) to 16 (D fully split: D1..D6) BD-206 commits, + 1 BD-249 commit.** MUST-2 does NOT change the count (Wave C is 4 SERIAL commits, not parallel). §3 is the SSOT on commit composition; §0 now states this same range.

**Commit-scope-keyword discipline (Check-36, CI-enforced):**
- A1, B1, B1b, B2, B3, C2, C3, E1 span `scripts/` (pack-side libs/tests) + sometimes `project-template/` → **NEUTRAL framing** (no scope keyword). A1 in particular touches both `project-template/docs/project/` sidecars AND `scripts/` → MUST be neutral (a `pack-only`/`project-only` keyword would trip Check-36).
- C2/C3/C4/C5 EACH ship BOTH a pack leg (`scripts/validate-pack.py` for C3/C4/C5) and a client leg (`project-template/scripts/validate-docs.sh` for all four) → every Wave-C commit touches `project-template/` AND (C3/C4/C5) `scripts/` → **NEUTRAL framing** (a `pack-only`/`project-only` keyword would trip Check-36). The coder/Pack-Chat confirms the actual diff before claiming a keyword (commit-subject-keyword-token-trap).
- D1 (trinity at `project-template/`) is `pack-chat-only`-PERMITTED (project-template trinity), but the SUBSTANTIVE rewrite routes to coder; the commit subject MAY be `pack-chat-only` OR `project-only` per the actual diff.
- D2-D6 (all under `project-template/`+`supporting-docs/`) → `project-only` valid IFF the diff touches no pack-only paths.
- **BD-249** is `pack-only` (only `scripts/`).
- FLAG for Pack Chat: every commit's keyword is verified against `git diff --name-only` BEFORE commit; mis-framing fails Check-36. Default to NEUTRAL when in doubt (Check-36 skipped).

**Approval gates (per the pipeline):**
1. Planner-to-coder gate: Pack Chat surfaces THIS plan to the user; explicit approval before spawning the A1 coder.
2. Per-commit: each commit's bounded review/fix cycle → Pack Chat triages reviewer findings → user approves the commit.
3. BD-249 is a SEPARATE BD with its own approval after BD-206 lands.

## 7. GAP / risk callouts + cross-doc consistency check

### GAPs surfaced (planner)
- **GAP-1 — the `_format.md` FORBIDDEN grep-zero gate spans waves (NOT Wave-A-alone).** EE-6's 23-file operational set includes `project-template/scripts/.docs-gate-allowlist.txt` + `validate-docs.sh` (Wave C / O10) AND `supporting-docs/MIGRATION-v10-to-v11.md` (Wave D / O21). So the GLOBAL `_format.md` grep-zero cannot be asserted at A1 — only A1's share is clean at A1. **Resolution:** the grep-zero gate (A-3, direct pathspecs: `git grep -nE '_format\.md' -- ':!maintenance-docs/' ':!backlog/' ':!changelog/'`) is asserted JOINTLY after A1+B1+C2+D6+E1 (i.e. when O1/O9/O10/O21/O24/O26 have all landed). Each commit's reviewer asserts ITS share clean; the FINAL BD-206 reviewer asserts the global zero. The design's §5b "_format.md FORBIDDEN" row implies a single grep-zero; this plan distributes it across the waves and asserts the global zero at batch end.
- **GAP-2 (RESOLVED — MUST-1) — `test-migrate-v10-to-v11-decompose.sh` is a B1 SINGLE-WAVE break, GREEN at A1.** The prior "dual-wave break / expected-RED-until-B1 at A1" framing was a misdiagnosis (the force-overwrite asserts fail at the partial-deletion state from the migrator's internal Gate-2 `rc=31` cascade, NOT flag behavior; a clean-clone control at HEAD ran decompose 45/0 — EE-C). At the A1 COMMIT decompose is FULLY green (45/0), force-overwrite asserts included. The decompose `2.2a/b/c` + force-overwrite (`2.0b, 4.1a/b/c, 4.2a`) inversions are a B1 single-wave break (lock-step with O7's flag removal in B1, already scheduled in §3 COMMIT B1). **The A1 reviewer asserts decompose 45/0 at A1; a RED decompose assert at A1 is a REAL regression, not a tolerated wave boundary.** There is NO GAP here — recorded as RESOLVED so a downstream reader does not re-introduce the carve-out.
- **GAP-3 — the migrator `:453` copy-source edit is NOT explicitly enumerated as a Wave-A edit in the design.** The design's O1 declares `_format.md` FORBIDDEN everywhere (the grep-zero backstop catches `:453`), and O7 (Wave B) covers the migrator — but neither names `migrate-v10-to-v11.sh:453`'s `support_basenames` changelog branch as a Wave-A edit. A-1's empirical census (EE-B) proves it MUST land in A1 (the migration battery's Gate-2 + copy-source asserts are A1 breaks). **Resolution:** the plan places the `:453` edit in A1 explicitly (§3 A1 file #3). CONFIRM2 S-2 flagged this same under-attestation; A-1 sanctions the fix.
- **GAP-4 — C3 `_index.md` is a NEW artifact with NO pre-existing test to break, but it has a hard MANDATORY-validator dependency on the schema SSOT (A1's `_rules.md`).** C3 reads A1's `_rules.md` schema block. If A1's `_rules.md` schema is wrong, C3's validator inherits the error. **Resolution:** C3 runs after A1 (DAG-enforced); the C3 reviewer validates the generator against the OT gold impl-plan stream (61 phase-epics, EE-2) for zero violations.

### Risks (named per the prompt)
- **Stale references:** the `_format.md`/mirror/`--force-overwrite-mirror`/project-monolith prose refs are distributed across 23+ operational files (EE-6) + the tracker family (EE-11). MITIGATION: the joint grep-zero gates (A-3) at batch end catch any missed surface; the FINAL BD-206 reviewer runs all four gates (`_format.md`, two-var force-overwrite, mirror-subsystem joint, `until BD-206`).
- **Trinity rule violation:** D1 edits `project-template/{CLAUDE,AGENTS,GEMINI}.md` — MUST be ONE commit, all 3, cross-CLI normalized (not byte-identical). MITIGATION: D1's reviewer asserts trinity parity (the three express the same rules); cross-CLI normalization per ARCHITECTURE-BD-182 §4.1.
- **CI breakage (the §15 green-at-first-wave):** A1 is large; if any Wave-A inversion is missed, the FIRST commit is battery-RED. MITIGATION: the A1 reviewer runs the FULL battery empirically (A-1); the EE-A..EE-E breaking set is the exhaustive Wave-A list; the bounded cycle catches a miss before commit.
- **Migration regression:** the v10→v11 migrator is the highest-blast surface (the migration battery + the OT gold). MITIGATION: B1 inverts the migration tests in lock-step; the OT gold (read-only) is the measure-then-bound target; `test-v11-realistic-ot.sh` (fixture-dependent, end-to-end) exercises the full chain.
- **Check-36 mis-framing:** A1 spans pack+project → a `pack-only`/`project-only` keyword would fail Check-36. MITIGATION: NEUTRAL framing for all mixed commits (§6); verify `git diff --name-only` before each commit (commit-subject-keyword-token-trap — a keyword token anywhere in the subject is a claim).
- **Manifest drift (BD-228):** the manifest inputs perturbed by this batch span A1 (`project-template/*` sidecars + `test-fixtures/build.sh` + `scripts/*`) AND the D-wave (`supporting-docs/METHODOLOGY.md` O14 + `supporting-docs/INSTALL-PROCEDURES.md` O17 — SHOULD-1), not A1 alone. MITIGATION: run `manifest-sync.sh` ONCE at PUSH at the green final HEAD — it regenerates from the WHOLE input set regardless of which wave changed an input (expect exit 10 → commit the regenerated manifest); Check 62 + `build.sh --verify` enforce.
- **A-2 boundary risk (M-track break in E1):** a Wave-E coder could repoint the `:2142-2172` M-track staleness probe, breaking the dormant tracker→file feature. MITIGATION: A-2 (§1) bounds row-10's REPOINT to the genuine M-mono surfaces; O19's prose carve-out KEEPs the M-track header management; the E1 reviewer confirms `tracker-mirror.sh` + the reverse-emit are UNTOUCHED.
- **BD-249 ordering risk:** `mirror-generate.sh` deletion is terminal; if BD-249 runs before A1 removes its build.sh source line + B1 removes the migrator callers, the deletion dangles. MITIGATION: BD-249 is DAG-gated after BD-206 fully lands (the file deletion is the terminal mirror-subsystem state).

### Cross-doc consistency check (plan ↔ design)
- **The plan PRESERVES every design decision** (O0–O26, the 5 waves, the BD-249 split, the rule-10 map, EE-1..EE-11, DR-1..DR-4). No design decision is re-opened.
- **The plan's deltas from the design-doc TEXT are EXACTLY the six approved amendments (§1), all SANCTIONED:** A-1 (the migration-battery breaking set + the migrator `:453` Wave-A edit + per-wave full-battery verification — supersedes EE-9's sample); A-2 (the `tracker-migrate-forward.sh:2142-2172` KEEP boundary — supersedes EE-11 row 10's overlap); A-3 (direct git pathspecs for grep-zero gates — supersedes EE-11 Command 2); A-4/A-5/A-6 (doc-precision NITs, no plan-material change). **No other plan/design drift exists.** A downstream agent comparing this plan to the design will find ONLY these six sanctioned deltas (§1) — anything else would be a planner defect, and I assert there is none. **Reconciliation note (the 4 adversarial findings resolved here introduce NO new design delta):** MUST-1 CORRECTS a planner mistake (the EE-C dual-wave misdiagnosis) back to the design's own position (design EE-9 treats Wave-A as fully battery-green, never claims decompose is red at A1) — it removes a wrong plan claim, it does not add a design deviation. MUST-2 corrects the plan's §4 Wave-C SCHEDULE (parallel→serial) to honor worktree-isolation against the single monolithic `validate-pack.py`/`validate-docs.sh` — the design's §6 itself flagged the validate-pack.py same-file constraint for C1; the plan now extends that same constraint to C3/C4/C5 (and adds validate-docs.sh for C2-C5), a planning refinement within the design's stated scheduling principle ("same-file edits serialize"). SHOULD-1 + NIT-1 are accuracy fixes. None re-opens an O0-O26 design decision.
- **The plan's commit-count + fold decisions are PLANNING choices the design INVITED:** the C1-into-A1 validate-pack.py fold is the design's own §6 recommendation; the Wave-D commit-granularity is left to the orchestrator (the design says "parallel within"). These are not design deviations.

---

## 8. Worktree-wave schedule note (RW coder waves)

The RW coder waves run in ISOLATED worktrees; the orchestrator schedules them off the §4 parallel-vs-dependent map (this plan's job is the map + serialization constraints, not the worktree mechanics). For completeness:
- **A1 first, alone** (serial, blocks everything). Its first coder CREATES the worktree (per-spawn `isolation:"worktree"`, `worktree.baseRef:"head"`); every fix-coder in A1's bounded cycle REUSES it; teardown only after A1 is CONFIRMED landed (exit 0).
- After A1 lands: the **Wave-B set (B1 serial-internal, B1b, B2, B3), the Wave-C SERIAL CHAIN (C2→C3→C4→C5), the Wave-D set (D1, D2-D6), and E1 may run as parallel worktree WAVES across waves** — EXCEPT (i) B1b serializes with A1's already-landed `_lib.sh`/`test-per-entry.sh` edits (A1 is committed, so B1b's worktree bases at the post-A1 HEAD — no live-write conflict); and (ii) **Wave C is INTERNALLY SERIAL (MUST-2): C2→C3→C4→C5 each base their worktree at the prior C-commit's landed HEAD, because all four extend `validate-docs.sh` and C3/C4/C5 also extend `validate-pack.py`.** Cross-wave, B/C-chain/D/E share no file (verified §4 disjointness), so the four waves parallelize freely; only INSIDE Wave C is there serialization.
- The patch is produced ONLY after a RO reviewer confirms each commit CLEAN; Pack Chat SendMessage-s the most-recent RW agent for the `git diff`, then applies + commits (user approval). Agents never commit.
- **BD-249** runs after ALL BD-206 commits land (a fresh worktree based at the post-BD-206 HEAD).

## 9. BD-249 coordination (the cross-BD dependency)

- **O22 splits:** O22-proj (project `_lib.sh:109,121,129` + `test-per-entry.sh:219-221`) is IN BD-206 (commit B1b); **O22-pack is BD-249** — a SEPARATE `pack-only` commit, sequenced DIRECTLY AFTER BD-206.
- **The dependency:** BD-249's `mirror-generate.sh` deletion is the TERMINAL mirror-subsystem state. It depends on: (a) A1 having removed the build.sh `mirror-generate.sh` source line (O23); (b) B1 having removed the migrator's `decompose.sh`/`init-project.sh` production callers (O4/O7). After those, the generator's only callers are dead tests → safe to delete. BD-249's own backlog entry (`backlog/BD-249.md`) records `Blockers: BD-206 — sequenced DIRECTLY AFTER`.
- **The JOINT grep-zero gate** (A-3, direct pathspecs) goes green ONLY after BOTH BD-206 O22-proj (B1b) AND BD-249 O22-pack land: `git grep -nE 'mirror-generate|per_entry_regenerate_mirror|pe_canonical_mirror_for_stream|mirror\) printf' -- ':!maintenance-docs/' ':!changelog/' ':!backlog/'` → ZERO (currently 81 hits). Within BD-206 ALONE the gate is NOT yet zero (the pack half is BD-249) — the BD-206 final reviewer asserts the PROJECT constants/asserts are gone, NOT the joint zero.
- **The two-var force-overwrite gate** is also JOINT: A1 (build.sh consumers, O23) + B1 (`_MIGRATOR_*` flag/bridge/Group-4, O7/DR-1) + BD-249 (`mirror-generate.sh` `PE_FORCE_OVERWRITE_MIRROR` consumers + Group-8 tests). Reaches ZERO (currently 103 hits) only after all three.
- **`per_entry_regenerate_toc` SURVIVES** (a separate function) — BD-249 cleans only `toc-regenerate.sh:15`'s stale doc-comment, never the function.

## 10. Rules-Applied Verification Block

| Rule | Verification evidence (measured) | Conclusion |
|---|---|---|
| **empirical-evidence-blocks** | §5 carries EE-A..EE-F, each with command + VERBATIM output + HEAD `66c8332`/2026-06-26 + interpretation + conclusion. **MUST-1 reconciliation control (re-run this pass, RECORDED in the corrected EE-C):** `git clone -q --no-hardlinks . $SCRATCH` of HEAD `66c8332` (clean state: sidecars + `_format.md` + flag present) → `bash scripts/tests/test-migrate-v10-to-v11-decompose.sh` → `Passed: 45 Failed: 0` (force-overwrite asserts INCLUDED), proving decompose is GREEN at the A1 commit. **MUST-2 structural evidence:** `wc -l scripts/validate-pack.py` = 11,799 (single monolith, 66 inline `check_*` defs); `wc -l project-template/scripts/validate-docs.sh` = 501 (single client validator); `manifest-inputs.sh:56-60` declares `project-template/*`/`scripts/*`/`test-fixtures/build.sh`/`supporting-docs/METHODOLOGY.md`/`supporting-docs/INSTALL-PROCEDURES.md` (SHOULD-1). Gate baselines re-verified: mirror-subsystem 81, two-var force-overwrite 103. The other A-1 battery numbers (14, 33/12, 48/6, 57/4, 80/7, 39/25, 57/0, 40/0, 104/0, 57/0) stand from the original plan + the adversarial reviewer's independent reproduction. | COMPLIANT |
| **verify-full-ci-suite** | §5 designs EVERY wave's verification against the FULL battery (validate-pack.py + `build.sh --all/--verify` + ALL shell shards incl. the migration battery + the tracker family), confirmed three-legged from `validate-pack.yml:192-202`. A-1 BINDING discharged: the migration battery (EE-B) was empirically run + each break assigned to its wave. **MUST-1 reconciliation:** the prior EE-C "dual-wave / expected-RED-until-B1" decompose claim was a misdiagnosis — re-verified by a clean-clone control (`git clone` HEAD `66c8332` → `bash test-migrate-v10-to-v11-decompose.sh` → `Passed: 45 Failed: 0`), proving decompose is FULLY GREEN at the A1 commit (it is a B1 SINGLE-WAVE break). The green-at-first-wave guarantee for A1 now asserts FULL battery green INCLUDING decompose 45/0; no force-overwrite carve-out. | COMPLIANT |
| **bounded-review-fix-cycle** | §3 + §6 specify each commit's cycle as ≤2 review/fix pairs + 1 final reviewer; architect escalation if dirty after final. | COMPLIANT |
| **enumerate-encoding-surfaces** | §2 + §3 list each surface WITH its lock-step encodings (tests/fixtures/validators/CI/allowlists/`.toml`/emitter) in the same commit: `_format.md` ⇔ `_lib.sh`+tests+fixtures+allowlists+docs (A1+C2+D6); Check-29 flip ⇔ Test 7 ⇔ emitter ⇔ 3.5 (A1); O19 repoints ⇔ tracker tests (E1); build.sh ⇔ manifest-input+README (A1). | COMPLIANT |
| **ci-check-runtime-compounding** | §5 "CI-runtime-compounding" flags the NEW checks (O9-O13) as cheap (deterministic counts, parse-schema-once, `git ls-files` candidate set, SKIP-lenient); no whole-tree scan / subprocess-per-entry storm. | COMPLIANT |
| **manifest-regen-push-time** | §5 "Manifest interplay" + §3 A1 verification: build.sh + project-template are manifest inputs (`manifest-inputs.sh:56,58`); `manifest-sync.sh` runs at PUSH (expect exit 10 → commit regenerated manifest); Check 62 + `build.sh --verify` enforce; NOT per-commit. | COMPLIANT |
| **deferral-is-scope-creep** | No deferral beyond the user-set boundaries (BD-185 full part-migration, BD-246 checksum, BD-247 pack form-family, BD-249 pack dead-mirror). The 6 amendments are FIX, in-plan (§1). The C1→A1 fold is a consolidation, not a defer. | COMPLIANT |
| **agents-never-commit / per-action-approval-sub-agents** | This reconciliation pass ran ONLY read-only git in the canonical checkout (`rev-parse`, `status --short`, `grep`, `ls-files`) + `git clone -q --no-hardlinks . /tmp/bd206-reconcile-clone.XXXX` (a NEW scratch clone — `clone` to a fresh /tmp dir creates no ref/index/worktree in the canonical repo) + read-only `bash` decompose test INSIDE that scratch clone + read-only `wc`/file Reads. Canonical HEAD UNCHANGED at `66c8332`; working tree still EXACTLY the 8 deletions (no source mutated, no commit, no stage, no worktree added to the canonical repo). SOLE write = this plan doc (Bash heredoc/python in-place edits). No state-changing git verb on the canonical repo; no destructive op. | COMPLIANT |
| **graph-first-context** | DISCOVERY ran the graph FIRST against the INJECTED path `/Users/david/Developer/optiquity-ai-agent-config-pack-v11-dev/graphify-out/graph.json` (`--backend claude-cli --budget 1500`, BFS depth=2, 15 nodes) for the validate-pack/_lib.sh/test-per-entry collision blast radius — surfaced the Check 39/40/43 fixture community, fully contained within the grep ground-truth. VERIFICATION via grep/Read (exact file:line/counts). G2 not needed. | COMPLIANT |
| **spawn-unique-naming** | Original author `planner-bd206-nomirror`; this reconciliation pass operates as `planner-bd206-reconcile` (FRESH instance per reconciliation-instance-independence — NOT the author, NOT the adversarial reviewer). Both recorded in the header + this row. | COMPLIANT |
| **rules-applied-verification-block / agents-read-rule-docs-in-full** | This block exists with per-rule measured evidence. READ-IN-FULL: ARCHITECTURE-BD-206.md (1276 lines, all pages); CONFIRM2-ARCH-REVIEW-BD-206.md (361 lines, full — the 6-amendment evidence); backlog/BD-206.md + BD-249.md (full); pack-root CLAUDE.md `## Pack memory` (in-context, full); the named memory files: `feedback_architect_planner_empirical_evidence.md`, `feedback_verify_full_ci_suite.md`, `feedback_review_fix_cycle.md`, `feedback_ci_check_runtime_compounding.md`, `feedback_manifest_regen_on_v11_surface.md`, `feedback_deferral_is_scope_creep.md` (ABSENT as standalone → relied on CLAUDE.md `deferral-is-scope-creep` rule text, as the prompt permitted), `feedback_agent_output_rules_applied_block.md`, `feedback_agents_read_rule_docs_in_full.md`, `feedback_enumerate_encoding_surfaces.md` (relied on CLAUDE.md `enumerate-encoding-surfaces` rule text). Anti-contamination: NO prior BD-206 PLAN/RESTART/FINAL, NO adversarial-PLANNER review, nothing under `/tmp/bd206-REJECTED-DO-NOT-READ/` opened. **RECONCILIATION PASS read-set (`planner-bd206-reconcile`):** the CURRENT `PLAN-BD-206.md` (edit target, read IN FULL — both pages); the CURRENT `ADVERSARIAL-PLAN-REVIEW-BD-206.md` (the 4 findings, read IN FULL); the APPROVED `ARCHITECTURE-BD-206.md` §5 (O9-O14/O17 deliverables) + §6 (rule-10 map, lines 1012-1141); `DECISIONS-BD-206-RESTART.md` (full, esp. §13 Item-7 + §15/§16); `backlog/BD-206.md` + `backlog/BD-249.md` (Status/Target/Blockers); pack-root CLAUDE.md `## Pack memory` (in-context, full) + the named memory files. Reconciler anti-contamination: did NOT read any OTHER BD-206 doc — NOT `PLAN-BD-206-RESTART.md`, NOT `ARCHITECTURE-BD-206-RESTART*.md`/`-FINAL.md`/`-RECONCILED.md`, NOT the architecture-side adversarial/CONFIRM reviews, nothing under `/tmp/bd206-REJECTED-DO-NOT-READ/`. | COMPLIANT |

**HEAD:** `66c833223c2c8e3b7657e3c24e7c4ddfb539a3d7` — **Date:** 2026-06-26 — **Author:** `planner-bd206-nomirror` — **Reconciled by:** `planner-bd206-reconcile` (MUST-1/MUST-2/SHOULD-1/NIT-1 folded in place; see §11 Reconciliation log) — **Plan:** `/Users/david/Developer/_tmp/pack-handoff-bd206-restart/PLAN-BD-206.md`

## 11. Reconciliation log (`planner-bd206-reconcile`; adversarial plan review's 4 findings → RESOLVED in place)

> All 4 findings were triaged FIX by the user. Each is folded INTO this single plan doc (no addendum). The plan was otherwise SOUND (all 27 deliverables mapped, grep baselines 81/103 correct, BD-249 sequencing + Check-36 framings + bounded cycle validated, the 6 §1 amendments untouched) — that is preserved; nothing else re-opened.

| Finding | Sev | Resolution (one line) | Plan section(s) now carrying the fix |
|---|---|---|---|
| **MUST-1** | MUST | DELETED the EE-C/GAP-2 "dual-wave / expected-RED-until-B1" decompose carve-out + the "pull O7 into A1" fallback. RE-STATED: **A1 is FULLY battery-green INCLUDING `test-migrate-v10-to-v11-decompose.sh` 45/0** (force-overwrite asserts included; flag present at A1). Decompose is a B1 SINGLE-WAVE break (O7 removes the flag in B1; the asserts invert lock-step there — already scheduled in B1). RE-VERIFIED by my own clean-clone control: `git clone` HEAD `66c8332` → `bash test-migrate-v10-to-v11-decompose.sh` → `Passed: 45 Failed: 0`. | §3 COMMIT A1 Verification (decompose 45/0, no carve-out); §3 COMMIT B1 Verification (B1 inverts on the new shape); §5 EE-B (force-overwrite asserts are Gate-2-cascade-RED at partial state, GREEN at A1); §5 EE-C (FULLY REWRITTEN as the corrected control block); §5 per-wave protocol (green-at-first-wave INCLUDES decompose); §7 GAP-2 (RESOLVED, no carve-out); §10 RAB verify-full-ci-suite row. |
| **MUST-2** | MUST | C3/C4/C5 each add a leg to the SINGLE monolithic `validate-pack.py` (+ reconciliation surfaced a 2nd collision: C2/C3/C4/C5 each add a leg to the SINGLE `validate-docs.sh`) → the §4 parallel Wave-C schedule + "C/D/E touch none of validate-pack.py" claim were FALSE. **CHOSEN resolution: option (a) generalized — SERIALIZE A1 → C2 → C3 → C4 → C5** (each commit lands its full deliverable = pack leg + client leg + tests atomically; each bases its worktree at the prior C-commit's landed HEAD). REJECTED (b) fold-into-A1 (bloats A1; C1-fold was a residual of O9, these are distinct deliverables) + (c) split-pack-from-client-leg (violates enumerate-encoding-surfaces; client legs still collide). **Commit count UNCHANGED (Wave C still 4 commits, schedule only).** | §2 Wave C heading (serial); §3 COMMIT C2/C3/C4/C5 (serial chain, both shared validator files, schedule notes); §4 Wave DAG (C serial); §4 post-DAG prose; §4 same-file table (validate-pack.py row rewritten + NEW validate-docs.sh row); §4 disjointness verification (corrected); §4 NEW "MUST-2 resolution rationale" subsection; §6 commit table C-rows (Depends-on = serial) + scope-keyword note; §8 worktree schedule (C internally serial). |
| **SHOULD-1** | SHOULD | ENUMERATED the D-wave manifest inputs: `manifest-inputs.sh:59-60` declares `supporting-docs/METHODOLOGY.md` (O14/D4) + `supporting-docs/INSTALL-PROCEDURES.md` (O17/D3) as inputs → the D-wave ALSO perturbs the manifest, not A1 alone. NOT a correctness break (push-time `manifest-sync.sh` regenerates from the WHOLE input set in one pass at the green final HEAD). | §5 "Manifest interplay" (rewritten to list all 5 input globs + the D-wave perturbation); §3 COMMIT A1 manifest verification line; §7 "Manifest drift" risk. |
| **NIT-1** | NIT | FIXED the §0 commit-count headline. The prior "13 commits (1+4+2+6+1)" was arithmetically wrong (1+4+2+6+1=14) + contradicted "C carries C2..C5". **Authoritative count: BD-206 ∈ {12 … 16} commits + 1 BD-249 commit** (12 if Wave-D combined; 16 if Wave-D fully split into D1..D6). Also corrected §6's stale "12–17" upper bound (was off by one; D fully split = 6 D-commits → 16, not 17) and the §3 heading. MUST-2 does NOT change the count. | §0 commit-count line (rewritten); §3 heading (12–16); §6 commit-count note (arithmetic fixed); §6 AUTHORITATIVE COMMIT COUNT line (12–16). |

**MUST-2 choice + rationale (restated for the orchestrator):** SERIALIZE the Wave-C enforcement chain A1 → C2 → C3 → C4 → C5 (NOT fold into A1, NOT split pack-leg-from-client-leg). The two shared validator files (`validate-pack.py` monolith for C3/C4/C5; `validate-docs.sh` for C2-C5) make Wave C inherently non-parallel under worktree-isolation; serializing keeps A1 bounded and each O10/O11/O12/O13 deliverable coherent. Wave C still runs PARALLEL to Waves B/D/E (cross-wave disjoint). Commit count unchanged.

**Final authoritative schedule (post-reconciliation):**
- **A1** (1, FIRST alone, NEUTRAL) → fully battery-green incl. decompose 45/0.
- **Wave B** (4, parallel after A1; B1 serial-internal; B1b serializes with A1 on `_lib.sh`/`test-per-entry.sh`): B1, B1b, B2, B3 — all NEUTRAL.
- **Wave C** (4, SERIAL CHAIN after A1: C2→C3→C4→C5 — all NEUTRAL; parallel to B/D/E).
- **Wave D** (2–6, parallel after A1: D1 trinity separate `pack-chat-only`/neutral; D2-D6 combined or split, `project-only`).
- **Wave E** (1, parallel after A1: E1 NEUTRAL).
- **BD-249** (1, SEPARATE BD, `pack-only`, after BD-206 fully lands).
- **BD-206 total: 12–16 commits** (variable = Wave-D granularity); **BD-249: 1 commit.**

### Anti-contamination attestation
**Author (`planner-bd206-nomirror`):** No prior BD-206 PLAN (RESTART/FINAL/RECONCILED), no adversarial-PLANNER review, nothing under `/tmp/bd206-REJECTED-DO-NOT-READ/` was opened. Inputs: the CURRENT ARCHITECTURE-BD-206.md (design SSOT) + the six binding amendments (this prompt) + CONFIRM2-ARCH-REVIEW-BD-206.md (amendment evidence only) + backlog/BD-206.md + BD-249.md + the live repo (read-only) + read-only battery runs.

**Reconciliation pass (`planner-bd206-reconcile`):** Inputs limited to the CURRENT `PLAN-BD-206.md` (edit target) + the CURRENT `ADVERSARIAL-PLAN-REVIEW-BD-206.md` (the 4 findings) + the APPROVED `ARCHITECTURE-BD-206.md` + `DECISIONS-BD-206-RESTART.md` + `INVESTIGATION` + `backlog/BD-206.md`/`BD-249.md` + the live repo (read-only) + a clean-clone scratch decompose re-run. Did NOT read any OTHER BD-206 plan/review doc (no `PLAN-BD-206-RESTART.md`, no `ARCHITECTURE-BD-206-RESTART*`/`-FINAL`/`-RECONCILED`, no architecture-side adversarial/CONFIRM reviews, nothing under `/tmp/bd206-REJECTED-DO-NOT-READ/`). FRESH reconciliation instance per `reconciliation-instance-independence` (not the author, not the adversarial reviewer).
