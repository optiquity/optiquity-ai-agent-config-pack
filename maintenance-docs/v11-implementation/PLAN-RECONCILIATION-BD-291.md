# PLAN-RECONCILIATION-BD-291 — adjudication of the adversarial plan review

- Agent: `pack-planner` RECONCILIATION instance (FRESH — neither the plan's author nor its adversarial reviewer), read-only; worktree `…/.claude/worktrees/agent-aa37fd679923b10de` @ HEAD `0c2709e7e59241a39b7892d31c219d3f5ec4cefd` (== injected canonical HEAD; `git worktree list` cross-checked: canonical + this worktree both at `0c2709e`; `git status --porcelain` clean); date 2026-08-25; `isolation_mode: full` per spawn contract.
- Inputs: plan `…/bd291-planner-20260825T223908Z/PLAN-BD-291.md` (383 lines, read in full); adversarial review `…/bd291-advplan-20260825T230325Z/ADVERSARIAL-PLAN-REVIEW-BD-291.md` (read in full); authority chain `…/bd291-reconcile-20260825T221529Z/ARCHITECTURE-BD-291.md` + `RECONCILIATION-BD-291.md`; `backlog/BD-291.md`; user decisions per spawn prompt (not re-opened).
- Deliverable: the RECONCILED plan `PLAN-BD-291.md` beside this file — full standalone edition, all accepted corrections folded. A coder can execute any wave from that document + the design alone.
- Every ruling below rests on measurements I ran MYSELF this session at `0c2709e` — not on either input's say-so.

## §1 Ruling summary

**7/7 findings ACCEPT (0 REJECT, 0 DRAFT-FOR-USER). Both adversarial open items (AOI-1, AOI-2) adjudicated and folded. The plan survives reconciliation; no re-planning; coder waves may start at the user's planner-to-coder gate.**

| Finding | Severity | Ruling | Fold location in the reconciled plan |
|---|---|---|---|
| F-1 contract-migration.sh omitted from c3 | MUST | **ACCEPT** | §1 c3 row; NEW §4.6.1; §4.7 gate; §5.3; §8.1; §10; PE-19/PE-23 |
| F-2 PE-7 evidence gap (H1/fence axis unmeasured) | SHOULD | **ACCEPT** | PE-7 rewording + NEW PE-20 (my own census) |
| F-3 §10 omits conditional `-gates.sh` ∩ BD-172 | SHOULD | **ACCEPT** | §10 conditional-contact block; §9.3 extended d1 note; §4.7; PE-21 |
| F-4 `test-fixtures/README.md` stale row, ∩ BD-171 | SHOULD | **ACCEPT** | §1 c3 row; NEW §4.6.2; §4.7 grep; §10; PE-21/PE-23 |
| F-5 exit-code range mis-stated (31 omitted) | NIT | **ACCEPT** | §5.1.2 corrected range; NEW PE-22 |
| F-6 run (ii) red path implicit | NIT | **ACCEPT** | §9.2 explicit red-path sentence |
| F-7 bare-date guard wiring under-specified | NIT | **ACCEPT** | §2.1.4 guard-wiring clause; P11 case 9 annotated |

## §2 Per-finding rulings (independent evidence per ruling)

### F-1 (MUST) — ACCEPT

**Independent measurements (all this session, `0c2709e`):**
- `test-fixtures/build.sh` L1134–1142 (`_materialize_for_contract` `migration` arm): `src="$THIS_DIR/v10-realistic-ot"` → `cp -R "$src" "$sandbox"` — the contract sandbox is a FULL copy of the exact persona P9 enriches. CONFIRMED.
- `scripts/persona-contracts/contract-migration.sh` (558 lines): L48 `MIGRATE_SH="$PACK_ROOT/scripts/migrate-v10-to-v11.sh"`; L102 `PACK="$PACK_ROOT" bash "$MIGRATE_SH" "$SANDBOX"` (bare = auto `--dry-run` then `--apply`, per the L85–99 comment); L122 `--resume` after `.resolved` sidecar auto-resolution — the REAL c3-changed pipeline runs end-to-end inside this contract. CONFIRMED, and STRONGER than the review stated: TWO comment blocks carry the falsified rationale, not one — L451–466 ("NOT asserted in the migration v11_artifacts array because the BD-165 decompose sub-op skips streams whose v10 monolithic INPUT is absent (the v10-realistic-ot fixture case)") AND L499–513 ("The current v10-realistic-ot fixture has no docs/project/{BACKLOG,IMPLEMENTATION-PLAN,CHANGELOG}.md input files, so the migrator legitimately produces no _toc.md files … Adding the _toc.md surface here would force the contract to fail"). Post-P9 both are FALSE.
- `grep -n "docs/project" scripts/persona-contracts/contract-migration.sh` → hits ONLY at L444–513 (installed-template asserts + the two comments). There is NO "asserts docs/project absence" case → the plan's §4.6 hedge predicate reaches NOTHING. CONFIRMED.
- Wrapper `scripts/tests/fixture-dependent/test-persona-contracts.sh` (87 lines, read in full): roster-only (`contracts=(…)` L41–46; dir check L36; zero fixture assertions) — the hedge also named the wrong file. CONFIRMED.
- Referencer census re-run: `grep -rln "v10-realistic-ot" scripts/ test-fixtures/ pack-ops/ supporting-docs/ .github/` → **15 files** (the review reported 11 — a classification-granularity delta, see §5 note N-1): 8 plan-covered, 5 name-only (`test-migrator-capability-translation.sh` L15 comment, `test-validate-pack-check-61.sh` L182 synthetic string, `ci-shard-plan.py` L104–106 comment, `v11-trinity-marker-prepped/README.md` L83, `dashboard-approvals/dashboard.html` rendered convenience view), leaving exactly the SAME TWO uncovered content-encoders the review found: `contract-migration.sh` + `test-fixtures/README.md`. The material conclusion is IDENTICAL under both counts.
**Ruling rationale:** the c3 "Files (complete)" list omitted a measured encoding surface whose committed rationale P9 falsifies, and the plan's own correction instrument could not fire (`enumerate-encoding-surfaces` violation; state was verifiable at plan time). Folded: file added to c3; §4.6.1 specifies BOTH comment-block reconciliations + a dedicated decompose-output assert block + the invariant-3d verify note (root `BACKLOG.md` untouched — PE-10 re-cited); §4.7 replaces the dead hedge with a direct obligation; §10/PE-23 close the census.

### F-2 (SHOULD) — ACCEPT

**Independent measurements:** `grep -rln "per_entry_decompose" scripts/ test-fixtures/build.sh` → 2 lib files + 6 test scripts + `build.sh` (matches the review's set); graph pass (BFS depth=2 from `per_entry_decompose()`, 23 nodes) surfaced NO consumer beyond it. I then ran my OWN programmatic census (python heredoc extractor over the 7 consumer files — 76 heredocs — plus `scripts/tests/fixtures/tracker-migrate/BACKLOG.md`): **0 fence lines; 0 `# ` lines after any entry anchor**; the 26 `# `-prefixed hits are all pre-anchor shell-comment lines inside NON-decompose-input heredocs (fake-`gh` bodies at `tracker-migrate-forward-test.sh` L536–537/L965–967, `tracker-migrate-roundtrip-test.sh` L116–119; probe-payload comments at `tracker-bd204-lossless-roundtrip-test.sh` L475–496/L583–618; `.gitignore`/user-`test.sh` bodies at `build.sh` L331/L636–644/L916/L975–980).
**Ruling rationale:** the review is right that PE-7's conclusion out-ran its measurement — the date-shape greps cannot see the H1/fence mechanism, which changes for EVERY stream. The claim itself is TRUE (my census independently re-proves it). Folded: PE-7 re-scoped to the naming axis with an explicit joint-conclusion pointer; NEW PE-20 carries my census as the H1/fence-axis evidence.

### F-3 (SHOULD) — ACCEPT

**Independent measurements:** `backlog/BD-172.md` read in full — `Status: Open`; Description: "Lands in v11.0"; File/Symbol line 3: "`scripts/tests/test-migrate-v10-to-v11-gates.sh` — nine new test cases (3 helpers × 3 cases each …)". Plan §4.6 (original) explicitly authorizes editing `-gates.sh` inside c3 on wording contact, yet §10 recorded only `test-dry-run-migration.sh` + d1 bookkeeping as plan-added paths.
**Ruling rationale:** a conditionally-authorized edit is blast radius for collision purposes. Folded: §10 now records the four conditional banner-sensitive files with the BD-172 COORDINATE note (BD-291 c3 lands first; BD-172's nine cases authored against the post-c3 S4a wording); §9.3's d1 BD-172 note is EXTENDED to carry the wording-contact sentence (durable anchor); §4.7 tells the coder to record any fired `-gates.sh` edit in the IMPL-REPORT.

### F-4 (SHOULD) — ACCEPT

**Independent measurements:** `test-fixtures/README.md` L29 (`v10-realistic-ot` row) read: "Fake-OT shape: project-name fills (`FakeOT`); x-prefixed custom agent …; 5-entry TD-* `BACKLOG.md`; trinity v10 self-label intact" — no docs/project monolith mention → stale after P9. `backlog/BD-171.md` (`Status: Open`) File/Symbol: "EXTEND `test-fixtures/README.md` table (note the live-clone harness pattern complements the persistent fixtures)" — a DIFFERENT edit to the SAME table. `scripts/lib/manifest-inputs.sh` L55–61: `test-fixtures/README.md` is NOT an input glob (only `build.sh`) — no manifest impact; `test-fixtures/` is not a project-side prefix — c3 stays `pack-only`.
**Ruling rationale:** the only fixture-description doc for the persona P9 rewrites must move in lock-step. Folded: §4.6.2 (one table-cell edit, neutral vocabulary), c3 file list, §4.7 grep, §10 BD-171 note-grade COORDINATE.

### F-5 (NIT) — ACCEPT

**Independent measurement:** `scripts/lib/migrator-core.sh` L68–79: `EXIT_PACK_INVALID=10` … `EXIT_ALREADY_MIGRATED=16`; L75–78 comment "Distinct from stage failure (codes 20..30)"; `readonly EXIT_GATE_FAILED=31`; `readonly EXIT_INTERNAL=99`. The plan's "framework 10..16/20..30/99" omitted 31; the CONCLUSION (8 collision-free) holds regardless. Folded: §5.1.2 states the measured set (10..16, fail_stage 20+N (20..30), 31, 99; local 0/2/4/5/6/7); NEW PE-22.

### F-6 (NIT) — ACCEPT

**Independent verification:** plan §9.2 (original) states the acceptance and the MEASURED-precondition role but no named action on a red run; §7.8 covers post-landing defects generically. A coder/orchestrator reading §9 alone had no red-path instruction. Folded: §9.2 now states — red run (ii) BLOCKS the flip (no d1); diagnose from the persisted report sections; land the correction via the §7.8 in-batch `fix:` path; RE-RUN (ii); precondition is rc==0 re-measured, never waived. No deferral introduced (the fix lands inside the batch).

### F-7 (NIT) — ACCEPT

**Independent measurement:** `scripts/lib/per-entry/decompose.sh` L161–176: the CURRENT project-changelog `anchor_re` is `^### (\d{4}-\d{2}-\d{2})(?: — Phase (\d+))?(?: — (.+?))?$` — BOTH suffix groups optional, so today a bare `### 2026-01-01` line IS an anchor (id = date, L173–174). Under the NEW strict regex `^### (\d{4}-\d{2}-\d{2}) — (.+)$` the same line matches NEITHER the anchor NOR `section_break_re` (`^## `, L176) — an `id_extract`-only guard would never execute and the line would silently change routing class (a BEHAVIORAL regression vs today, worse than the review framed it: currently the line is an anchor; naively it becomes span/ignore filler). P11 case 9 bites the outcome, but the spec invited a mis-wiring→test-failure→rework loop. Folded: §2.1.4 now mandates a SEPARATE date-prefix classification evaluated independently of the strict anchor regex, with the failure mode named; P11 case 9 annotated as the bite.

## §3 Adversarial open items — adjudicated

- **AOI-1 (F-1 correction scope: reconcile-only vs reconcile+assert) → ACCEPT (b), reconcile + assert, in c3.** Property-fit verified (`design-discipline-challenge`): the contract ALREADY runs the full migrator over the persona (PE-19 — dry-run→apply→resume), so five presence asserts (`_toc.md` ×3, `_index.md`, `MIGRATION-TRIAGE.md`) are O(1) stat calls on an execution that already happens; `enumerate-encoding-surfaces` favors symmetric coverage; the asserts convert accidental post-c3 pipeline execution into deliberate coverage. Fold refinement (mine): a DEDICATED assert block, NOT new `v11_artifacts` members — the array's header comment derives it from install rules, and the five paths are migration OUTPUTS; shoving them into the array would falsify its own derivation comment (the same defect class F-1 fixes). Plus the invariant-3d verify note (PE-10: no stage touches root `BACKLOG.md`; P9 adds under `docs/project/` only — if 3d breaks, blast radius exceeded spec: STOP).
- **AOI-2 (where the README row update lands: c3 vs BD-171) → ACCEPT (a), c3.** Deferring to BD-171 fails the deferral test (unblocked, one table-cell edit, no size/blocked/fit defense); the BD-171 contact is note-grade (different row/purpose — PE-21) and recorded in §10.

## §4 Consolidated user-gate list (for the planner-to-coder gate)

Decisions the USER ratifies or executes; everything else in the plan is agent-executable as specified. Items 1–7 ratify at the planner-to-coder gate; 8–9 at their named later gates; 10–12 are user actions at close-out.

1. **Reconciled-plan approval** (the standing planner-to-coder gate) — including the 7 folded findings; the two reconciliation adjudications AOI-1(b)/AOI-2(a) are overridable here (AOI-1 downgrade path: reconcile-only, drop the five asserts — NOT recommended, evidence §3).
2. **OI-P3** — P9 moved from Wave A1 into c3 (design-deviation ratification; evidence PE-6/PE-8).
3. **OI-P2** — m4/m5 hosted in the existing `test-dry-run-migration.sh` (design §4 map addition).
4. **OI-P7** — Wave C split into c4 (`pack-only`) + c5 (`project-only`).
5. **OI-P9** — strict §5.1(d) set-equality kept, NO pre-widened allowlist; a fixture-shell surprise at c4/T5.a is fixed inside c4's bounded cycle (contingency awareness).
6. **F-1 fold scope** — `contract-migration.sh` reconciliation + five presence asserts in c3 (per item 1's override note).
7. **OI-P4** — `chore:` manifest-commit subject shape: ratify the landed `fa06e36`/`3b75ca6` precedent at the FIRST manifest-chore approval (the CLAUDE.md format block enumerates `feat:`/`fix:`/`docs:` only; the restriction text targets invented `fix:` shapes — explicit ratification closes the governance record).
8. **Run (ii)** — per-action approval of the real-target `--apply-sandbox` run (target checkout READ-ONLY source; harness sandboxes into `$TMPDIR`); on red, the F-6 path runs and run (ii) repeats — each re-run is a fresh per-action approval.
9. **OI-P8** — preservation-copy actor at the d1 approval (Pack Chat mechanical copy per precedent vs a minimal coder spawn), together with **OI-P5** (census artifacts stay OUT of the repo — Check 93 leg-1 exposure measured by the plan; ratify at d1).
10. **d1 commit approval** — flip + preservation + BD-172/BD-254 notes (the extended BD-172 note text per F-3 fold is part of this approval).
11. **OI-A11 relay** — user relays the fidelity guarantee + verification command to the target-app PM chat and the second pack chat.
12. **Launch-gate note** — the public/GA flip opens on the USER-run real migration succeeding with the shipped instructions; user-owned, no pack-side encoding beyond d1.

## §5 Notes and reconciliation-added observations

- **N-1 (census count delta — no action).** The review's F-1 census reported 11 files for `grep -rln "v10-realistic-ot"` over the same five roots; my re-run returns 15. The 4-file delta is classification granularity (the review appears to have pre-filtered `manifest.txt`, `dashboard.html`, `ci-shard-plan.py`, `DRY-RUN-MIGRATION.md` as trivially-covered before counting). Under BOTH counts the uncovered content-encoder set is IDENTICAL: exactly `contract-migration.sh` + `test-fixtures/README.md`, both now in c3. Recorded for audit accuracy only; no plan impact beyond PE-23's full 15-file classification.
- **N-2 (F-7 severity — no action).** My measurement shows the naive mis-wiring is a behavioral REGRESSION vs today (a bare-date line is currently a valid anchor; under the new strict regex it silently becomes filler unless the guard is wired as specified) — arguably SHOULD-grade rather than NIT. Moot post-fold: the wiring clause + P11 case 9 close it either way.
- **BD-171 non-absorption:** preserved and re-verified — the plan's T5 legs stay in the fixture-mode path; BD-171's scratch-GH harness set has ∅ file overlap with the plan's edit set; the two same-table/same-file contacts are note-grade COORDINATEs (§10), not absorptions.
- **Nothing deferred out of v11.0:** all 7 folds land inside the existing c1–c5/m*/d1 batch; no new BD opened; no finding resolved by deferral.

## §6 Readiness verdict

**READY for the user's planner-to-coder gate.** The reconciled `PLAN-BD-291.md` is standalone and internally consistent: all 20 design rows + 2 plan-added fold rows map to commits with verifying instruments; every constant re-checked (72 = 61+7+4; carrier sets 4/0/13; exit set 10..16/20..30/31/99; framework/local collision-free 8); the collision EEB is complete (3 added edit files + 4 conditional contacts + bookkeeping, 2 note-grade COORDINATEs); every commit gate names both CI jobs + DEEP + the wired set + commit-specific legs; no stale rationale survives (the §4.6 hedge replaced; PE-7 re-scoped; §5.4→§5.3 internal ref fixed); the user-gate list above is the single consolidated decision surface.

## §7 Rules-Applied Verification Block

1. **agents-never-commit** — Evidence: git this session = `rev-parse HEAD`, `worktree list`, `status --porcelain`, all in this worktree (first tool call, output quoted in-transcript: both trees at `0c2709e`); zero state-changing verbs; no patch emitted (RO); the folded plan assigns every commit to orchestrator+user gates. **COMPLIANT**
2. **per-action-approval-sub-agents** — Evidence: writes this session = `PLAN-BD-291.md` (1 Write + 2 Edit-appends) + `PLAN-RECONCILIATION-BD-291.md` (this Write) in the owned dir `…/bd291-planrec-20260825T232131Z/` + one scanner script `heredoc_census.py` in the session scratchpad (an OS-temp root); all four input handoff dirs read-only; nothing deleted anywhere. **COMPLIANT**
3. **preflight-stop-means-stop** — Evidence: emitted `PREFLIGHT: plan reconciliation complete; about to Write PLAN-BD-291.md + PLAN-RECONCILIATION-BD-291.md to /Users/david/.local/state/optiquity-pack-handoff/bd291-planrec-20260825T232131Z` in the message immediately preceding the first Write; no stop message received; writes chunked (each call ≤~300 lines). **COMPLIANT**
4. **graph-first-context** — Evidence: my discovery-flavored question (walker consumers + fixture-content encoders) ran `graphify query … --graph /Users/david/Developer/optiquity-ai-agent-config-pack/graphify-out/graph.json --backend claude-cli --budget 1500` → 23 nodes (decompose test + lib + unrelated Check-40 README), NO consumer beyond the grep set — corroboration captured in PE-20; the injected path used verbatim, never recomputed; all other reads were P2 verification reads of surfaces the plan/review had already named (carve-out (i)) or SSOT field values (BD Status — carve-out (ii)). **COMPLIANT**
5. **empirical-evidence-blocks** — Evidence: every ruling in §2 quotes the command + captured output (file, line, verbatim text, counts) at HEAD `0c2709e`, 2026-08-25, from MY OWN runs (contract L48/L102/L122/L451–466/L499–513; build.sh L1134–1142; BD-171/BD-172 File/Symbol lines; README L29; migrator-core L68–79; decompose.sh L161–176; the 76-heredoc census; manifest-inputs globs; the 15-file referencer census); the folded plan additions are PE-19…PE-23. **COMPLIANT**
6. **ci-guard-measure-then-bound + declare-verify-backing** — Evidence: the folded specs keep every measured carrier set + bite obligation intact (verified §2/§3: no allowlist added anywhere — OI-P9's rejection carried; §4.6.1's asserts are presence checks against a measured post-c3 output state; F-7's fold is exactly an absence-of-backing repair — a guard whose host regex cannot see its trigger; F-1's fold repairs a hedge whose predicate reached nothing). **COMPLIANT**
7. **verify-full-ci-suite** — Evidence: re-checked each gate in the reconciled plan (§2.7, §3.3, §4.7, §5.1, §5.2, §9.1) — every one names validate-pack + DEEP + fixture build/`--verify` (with the declared divergence states) + the full wired set + commit-specific legs; the F-1 fold ADDED a gate leg (contract asserts + 3d + grep-zero) rather than diluting any; no vacuous gate line survives. **COMPLIANT**
8. **design-discipline-challenge** — Evidence: AOI-1 adjudicated on verified property-fit, with a structural refinement (dedicated assert block, NOT `v11_artifacts` members — the array's derivation comment would be falsified, quoted §3); F-7's severity was re-derived from mechanism, not accepted at face value (N-2); no split-the-difference ruling — 7/7 ACCEPT each on captured evidence. **COMPLIANT**
9. **operating-docs-no-history-no-bloat** — Evidence: the folded edit intents preserve the constraint — §4.6.1(a) mandates present-tense replacement text with zero history/BD-narration + a reviewer grep; §4.6.2 is a content-inventory cell; §8.2 item 9 extended to cover the reconciled comments; no fold adds narration to any operating doc. **COMPLIANT**
10. **dependency-direction-placement** — Evidence: both folded files are pack-side (`scripts/persona-contracts/`, `test-fixtures/`) — no install-map/`_SANCTIONED_PACK_SIDE_SHIPPED` contact, no dual-use file introduced; re-verified the plan's placements unchanged (accounting.sh pack-side; TRIAGE migrator-generated client output). **COMPLIANT**
11. **ci-check-runtime-compounding** — Evidence: folded legs stay in bounds — §4.6.1(b) = five `[[ -f ]]` stat calls on a contract that already runs the migrator; §4.6.2 = a doc cell; no validate-pack check added; T5's 3×-migrator cost note carried unchanged. **COMPLIANT**
12. **open-item-surfacing** — Evidence: §4 consolidates every open decision with context + options + an evidence-based recommendation (items 1–12; AOI adjudications in §3 with override paths); N-1/N-2 surface my own observations; nothing recommended from memory; nothing deferred to another/new BD. **COMPLIANT**
13. **memory-not-an-ssot** — Evidence: every rule/contract claim cites a live in-repo path read THIS run (`contract-migration.sh`, `test-persona-contracts.sh`, `build.sh`, `manifest-inputs.sh`, `migrator-core.sh`, `per-entry/decompose.sh`, `test-fixtures/README.md`, `backlog/BD-171.md`, `backlog/BD-172.md`, the pack-root `CLAUDE.md` rules text) or a handoff artifact by absolute path; zero claims from CLI memory. **COMPLIANT**
14. **public-bound-no-leak** — Evidence: both output docs live out-of-repo; the target app appears only as the injected read-only path literal and the internal `ot-` fixture shorthand quoted from committed files (`FakeOT`, `v10-realistic-ot`); the folded in-repo edit specs mandate neutral vocabulary (§4.6.2; §8.2 item 10). **COMPLIANT**
15. **no-deferral-without-user-direction + deferral-is-scope-creep** — Evidence: 7/7 folds land inside the existing v11.0 batch (F-1/F-4 in c3; F-2/F-3/F-5 as plan-text/ledger amendments; F-6/F-7 as spec clauses); AOI-2's defer-to-BD-171 option REJECTED with the deferral-test argument (§3); §5 confirms no new BD and no finding resolved by deferral. **COMPLIANT**
16. **rules-applied-verification-block** — Evidence: this block — 16/16 rules from the spawn prompt's "Rules in force" list, each with quoted evidence and a terminal conclusion; it closes the document. **COMPLIANT**
