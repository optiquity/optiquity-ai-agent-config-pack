# PLAN-BD-197 — Worktree isolation: execution plan (commit-sequenced, green-per-commit, Claude-only)

**Role:** pack-planner (fresh). **Mode:** plan-only (one doc written; everything else read-only).
**Repo:** optiquity-ai-agent-config-pack-v11-dev · **Branch:** v11-dev · **HEAD at THIRD-pass planning:** `05ad61b4ca86a743d27230ec86a8252a55c064d4` (`05ad61b`) — C0/C1/C2/C3 have ALREADY LANDED (e7cefbe / 220b6c7 / f6ee088 / 05ad61b); the remaining commits are C4, C5, C6a/b, C7a/b, C8a/b. The SECOND pass planned at `ae3d932`.
**Date:** 2026-06-14 (THIRD pass; SECOND pass 2026-06-13).
**Plans from:** `ARCHITECTURE-BD-197-WORKTREE-ISOLATION-RECONCILED.md` (THE authority; PLANNER-READY: YES, incl. the **Correction pass (2026-06-14)** mode-model re-documentation AND the **NEW §18** in-session spawn symmetry + unified backstop scope-correction). Design decisions are LOCKED — this plan sequences, decomposes to file-level tasks, and defines per-commit verification + coder spawn boundaries. It designs nothing.
**Pass:** THIRD UPDATE PASS (2026-06-14) — edit-in-place over the SECOND-UPDATE-PASS 12-commit plan. This pass folds the architect's **§18 in-session spawn symmetry + unified backstop** (RECONCILED design, scope-correction 2026-06-14) plus the **user-approved Guard-A′ extension**. The commit SEQUENCE is UNCHANGED (C0–C8b, 12 commits, 11 if Guard-C folds) — this is CONTENT-SHARPENING of C4 / C5 / C7a / C8a + a Guard-A′ token-set extension in C8b, no new commit. §18 corrects a PROJECT-side UNDER-SCOPE: BD-197's PRIMARY use case is the orchestrator spawning sub-agents IN-SESSION via the Agent/Task tool with `isolation:"worktree"` + `/tmp`-patch merge-back — for BOTH Pack Chat (pack) AND PM Chat (project) — but the prior plan documented the project in-session path only through the `agent-run.sh` launcher (the SECONDARY path). The unified in-session backstop is 3 layers (§18.2): (i) always-on PROSE deny-list (shipped); (ii) DOCUMENTED-OPTIONAL user `permissions.deny` recipe (NOT shipped; the in-session mechanical hard-deny; session-scoped + sub-agent-inherited per RESEARCH F2; PreToolUse hook noted SECONDARY/fails-open); (iii) launcher `--disallowedTools` (project `agent-run.sh`). J4 stays NO new shipped pack-side file (Check-47 untouched). **SECOND UPDATE PASS (2026-06-13)** had folded the §17 Check-36 manifest carve-out (C0) + restored the `project-only` keyword on the C6a/C7a/C8a DATA halves + the re-review M-2/S-1/S-2/N-1/N-2 fixes. This pass folds the architect's **Check 36 manifest carve-out** (RECONCILED design **§17**) as BD-197's NEW FIRST COMMIT (**C0**, pack-only) and uses it to RESTORE the clean `project-only` keyword on the C6a/C7a/C8a DATA halves — resolving the 2nd-adversarial BLOCKER B-1 / M-1 (a `project-only` commit that stages the regenerated `test-fixtures/manifest.txt` could not be `project-only` because the manifest is a pack-side path; the carve-out makes the manifest scope-neutral, so a `project-only` content commit + the carved manifest passes Check 36 AND `build.sh --verify`). It also folds the remaining `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-2.md` fixes (M-2 carve-out prose-coherence gate; S-1 Guard-A allowlist grows by all THREE PLAN-adversarial reviews — `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL.md`, `-2.md`, and `-3.md`; S-2 guard-half manifest diffs expected-EMPTY; N-1/N-2 count precision). The prior UPDATE PASS had already folded (a) the 11 user-approved decisions from `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL.md` and (b) the Correction-pass two-independent-mechanisms mode-model (background-session axis → BD-218). The sequence is now **12 commits** (C0 carve-out + C1–C5 pack + C6a/C6b, C7a/C7b, C8a/C8b project-data/guard splits), every commit single-surface with an exclusive CI-verified Check-36 keyword (C7b may fold → 11).

## Read-in-full + no-derivation attestation
**THIRD UPDATE PASS attestation (2026-06-14, HEAD `05ad61b`).** I read each NAMED authoritative input directly and in full (no skim, no summary, no crop, no derivation): the existing plan (this artifact, edited in place); the RECONCILED design — the **NEW §18** in-session spawn symmetry + unified backstop (§18.1 spawn symmetry, §18.2 the 3-layer backstop + layer-to-path map + J4=NO, §18.3 the §2.1 scrub, §18.4 the C4/C5/C7a/C8a commit-scope implications, §18.E EB-A..EB-D, §18.R), plus §2 (scrubbed UC-1), §4 (merge-back + RW/RO), §5 (git-permission contract), §9 (OPTIONAL-FEATURES both surfaces), §12.2/§12.3 (mechanism + docs homes); `maintenance-docs/v11-implementation/RESEARCH-BD-197-INSESSION-BACKSTOP.md` (the verified backstop facts F1–F5 + the availability matrix + the BOTTOM LINE: prose deny-list [load-bearing default] + documented-optional user `permissions.deny` [mechanical when activated, session-scoped + sub-agent-inherited, deny-first] + optional PreToolUse hook [defence-in-depth, `if`-matcher fails open]); `pack-ops/PACK-CHAT.md` + `project-template/docs/pack/PM-CHAT.md` (the spawn-instruction surfaces — §18 deliverables); `backlog/BD-197.md` (all notes incl. Notes 11/12/13) + `backlog/BD-218.md` (the scoped-out background-session axis); `CLAUDE.md ## Pack memory` (full); the memory file `project_bd197_user_design_direction.md` (full — PROBE FINDINGS 1–5 + the decision ledger). **MATERIAL re-measurement delta: HEAD ADVANCED `ae3d932` → `05ad61b`** — C0 (e7cefbe), C1 (220b6c7), C2 (f6ee088), C3 (05ad61b) have ALREADY LANDED; the remaining commits are C4/C5/C6a-b/C7a-b/C8a-b, and §18's content-sharpens (C4/C5/C7a/C8a/C8b) ALL target still-pending commits. The commit SEQUENCE is UNCHANGED. New live measurements at `05ad61b` (§F THIRD-pass blocks): battery **202** (was 186 — C0 NC-cases + Check-52 test added 16 invocations); highest Check **52** (was 51 — Guard-B pack Check 52 landed in C3); the prohibition matcher returns **25 files** (was 23 — the C1/C2 STRIP files [`CLAUDE.md`, `CONCEPTUAL-REVIEW-METHODOLOGY.md`, `PLAN-SKILL-DIMENSIONS.md`, `ARCHITECTURE-BD-196-S1`] are GONE, and 6 new BD-197 IMPL/PACK-REVIEW process docs from C0–C3 now quote the regex — exactly the re-measure-at-commit mandate realized); OPTIONAL-FEATURES `baseRef`/`bgIsolation` **0/0** AND `permissions.deny` **0/0** in BOTH files (the NEW Guard-A′ recipe-token baseline); `git checkout -- <path>` carve-out STILL **3** sites (C4 pending); Check-36 carve-out + Class column CONFIRMED landed (C0/C3); validate-pack exit **0**; PM-CHAT.md in-session spawn ABSENT (§18.E EB-A reproduced — 0 isolation/worktree/tmp/handoff/merge-back hits; only the `agent-run.sh` launcher + incidental "in-session" prose); PACK-CHAT.md in-session contract ABSENT (§18.E EB-B reproduced — the 1 hit is the benign "handoff at Batch" prose). The §18 fold scopes the in-session spawn instructions into C4 (PACK-CHAT.md) + C7a (PM-CHAT.md), the `permissions.deny` recipe into C5 (pack OPTIONAL-FEATURES) + C8a (project OPTIONAL-FEATURES), and the Guard-A′ token-set extension into C8b — all single-surface, no new commit, the C0–C8b sequence intact.

**SECOND UPDATE PASS attestation (2026-06-13).** I read each NAMED authoritative input directly and in full (no skim, no summary, no crop, no derivation): the existing plan (this artifact, edited in place), the RECONCILED architecture (878 lines, all pages — esp. the **NEW §17** Check 36 manifest carve-out, the **Correction pass (2026-06-14)**, and §1/§3/§9/§13), `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-2.md` (the re-review whose B-1/M-1/M-2/S-1/S-2/N-1/N-2 findings I fold), `backlog/BD-197.md` (all notes 1–11, incl. **Note 11** PROBE+SCHEMA CORRECTION), `backlog/BD-218.md` (the scoped-out background-session axis), `CLAUDE.md ## Pack memory` (full), and the memory file `project_bd197_user_design_direction.md` (full — PROBE FINDINGS + the decision ledger). The prior UPDATE PASS had folded `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL.md` (the first plan review's 11 decisions). I re-measured every load-bearing state-claim against live HEAD `ae3d932` (§F). **One material re-measurement delta vs the prior §F:** the prohibition-only matcher now returns **23** files (prior plan: 21, then 22 with the 2nd review) — the latest +1 is `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-3.md` (3 matches: it QUOTES the regex), exactly as the re-review's S-1 prediction + the re-measure-at-commit mandate foretold (the BD-197-process/review-doc carrier set grows with every review pass; all THREE PLAN-adversarial reviews now match — see §F EE-2 for the live per-file counts and the measure-at-C5-commit framing). No other measurement moved (battery=186, highest Check=51, OPTIONAL-FEATURES 0/0, pack=5 / project=16, READONLY_AGENTS=14, carve-out=3 sites, dangling-ref EXCISE=3, manifest clean, validate-pack green). **NEW carve-out measurement (§17 verification):** the forced-co-variant set a `project-template/`-only content edit pushes into the same commit is EXACTLY `{test-fixtures/manifest.txt}` — `bash test-fixtures/build.sh --all --clean` produced ZERO tracked changes at this HEAD (manifest regenerated byte-identical; the fixture dirs are gitignored), confirming design §17.3 EB-2/EB-3.

---

## A. Commit sequence (lead with this)

**Twelve commits, two surfaces kept strictly separate, EVERY commit single-surface with an exclusive CI-verified Check-36 keyword (user decision 6 — SPLIT over neutral framing; B-1/M-1 RESOLVED by the §17 carve-out).** **C0 (NEW first commit) = the Check 36 manifest carve-out (pack-only)** — it lands BEFORE C1 so every later commit is evaluated by a carve-out-aware Check 36. P2 is pack-only (C1/C2); P3 pack-side is C3/C4/C5; P3 project-side SPLITS each mixed project+validator commit into a **project-only DATA half then a pack-only GUARD half, data-first so the guard is green on arrival** (C6a/C6b, C7a/C7b, C8a/C8b). The DATA halves are cleanly `project-only` again (the prior plan was forced toward neutral framing by the manifest pincer; C0's carve-out makes `test-fixtures/manifest.txt` scope-neutral so a `project-only` content commit + the carved manifest passes Check 36). No commit carries neutral framing. Every commit leaves CI green (legs ordered so no intermediate red — see §C).

| # | Subject (repo format) | Surface | Phase | Check-36 keyword |
|---|---|---|---|---|
| C0 | `fix: v11 — BD-197 Check 36 manifest carve-out (scope-neutral test-fixtures/manifest.txt) — enabling commit` | pack-self (scripts/validate-pack.py + its test) | P3-enabling | `pack-only` |
| C1 | `docs: v11 — BD-197 P2 disposition non-rule worktree carriers (plan/research/methodology docs)` | pack-self (maintenance-docs + pack-ops) | P2 | `pack-only` |
| C2 | `feat: v11 — BD-197 P2 remove the worktree prohibition + bug-era guardrails (trinity + commit-discipline ×3)` | pack-self (trinity + skills + agents) | P2 | `pack-only` |
| C3 | `feat: v11 — BD-197 P3 pack RW/RO two-class model + Class column + Guard-B(pack)` | pack-self (PACK-AGENTS + 5 agents ×3 + validate-pack) | P3 | `pack-only` |
| C4 | `feat: v11 — BD-197 P3 pack in-session spawn + patch-handoff merge-back + git-permission hardening + commit-discipline redesign + backstop` | pack-self (PACK-CHAT in-session spawn + merge-back + skills ×3 + agents ×3 + rationale + backstop) | P3 | `pack-only` |
| C5 | `feat: v11 — BD-197 P3 pack OPTIONAL-FEATURES isolation section (+ permissions.deny backstop recipe) + Guard-A + Guard-C` | pack-self (pack-ops + validate-pack) | P3 | `pack-only` |
| C6a | `feat: v11 — BD-197 P3 project RW/RO two-class model + agent-run.sh projection (data)` | client (project-template only) | P3 | `project-only` |
| C6b | `feat: v11 — BD-197 P3 Guard-B(project) RW/RO consistency check (guard)` | pack-self (validate-pack + tests + yml) | P3 | `pack-only` |
| C7a | `feat: v11 — BD-197 P3 project in-session spawn + merge-back + git-permission hardening + agent-run.sh --disallowedTools (data)` | client (project-template only) | P3 | `project-only` |
| C7b | `feat: v11 — BD-197 P3 project verb-parity guard extension (guard)` | pack-self (validate-pack + tests + yml) | P3 | `pack-only` |
| C8a | `feat: v11 — BD-197 P3 project OPTIONAL-FEATURES isolation section (+ permissions.deny backstop recipe; client-authored doc)` | client (project-template only) | P3 | `project-only` |
| C8b | `feat: v11 — BD-197 P3 Guard-A′ OPTIONAL-FEATURES presence-check (baseRef+bgIsolation+permissions.deny recipe), both surfaces (guard)` | pack-self (validate-pack + tests + yml) | P3 | `pack-only` |

**Total = 12 commits** (11 if C7b folds — J3). Each carries an EXCLUSIVE Check-36 keyword (`pack-only` or `project-only`); no commit is neutral-framed. **C0 (the carve-out) is what MAKES the C6a/C7a/C8a `project-only` keyword achievable** — without it, those DATA halves stage the regenerated `test-fixtures/manifest.txt` (a pack-side path) and Check 36 would deny `project-only` (the 2nd-adversarial B-1 BLOCKER). The carve-out treats exactly `test-fixtures/manifest.txt` as scope-neutral in BOTH the `pack_only` and `project_only` offender branches, so the manifest no longer counts as an offender in either. The split is **data-first** in each pair (C6a→C6b, C7a→C7b, C8a→C8b): the project DATA lands first as `project-only`, then the pack GUARD lands as `pack-only` and is GREEN on arrival because the data it asserts is already present (measure-then-bound: the guard asserts the post-data state). Guard-C (C7b) is the CODER'S-CALL fold-vs-standalone (decision 8) — if the verb-parity check folds into an existing parity check inside a C7-adjacent pack edit, C7b may carry only the test/yml wiring; the coder records the fold-vs-standalone rationale in the IMPL-REPORT (§J3).

**§18 content-sharpen (THIRD pass, 2026-06-14 — NO new commit, NO sequence change).** The architect's §18 scope-correction SHARPENS the CONTENT of four already-planned commits + extends one guard; the C0–C8b sequence is intact. (1) **C4 (pack)** — PACK-CHAT.md gains the IN-SESSION orchestrator spawn procedure (Pack Chat spawns RW agents via the Agent tool with `isolation:"worktree"`, RO agents in-place, in the background, names the `/tmp` handoff dir), not only the post-return merge-back/apply. (2) **C7a (project)** — PM-CHAT.md gains the symmetric IN-SESSION Agent/Task-tool spawn instruction authored CLIENT-NATIVE (the 5 elements of §18.1: two spawn paths [in-session PRIMARY + `agent-run.sh` launcher SECONDARY], `isolation:"worktree"` for RW only, background, `/tmp`-patch merge-back, conflict/degradation at project homes) — CLOSING the project under-scope (the prior plan documented the project in-session path only via the launcher). (3) **C5 (pack OPTIONAL-FEATURES)** — adds the documented-optional user `permissions.deny` recipe (the in-session mechanical backstop; PreToolUse hook noted secondary/fails-open) alongside the existing `baseRef`/`bgIsolation` content. (4) **C8a (project OPTIONAL-FEATURES)** — adds the same `permissions.deny` recipe authored CLIENT-NATIVE (separate artifact, not a byte-copy). (5) **C8b (guard)** — the user-approved Guard-A′ (Check 54) EXTENSION: assert the `permissions.deny` recipe token is present in BOTH OPTIONAL-FEATURES files, in addition to the existing `baseRef`+`bgIsolation` presence assertion (measure-then-bound; the C8b coder sizes the token set to exactly what C5/C8a author + re-measures at commit-time). J4 stays NO new shipped pack-side file (Check-47 untouched — §18.2 EB-D). These folds are single-surface (C4/C5/C8b `pack-only`; C7a/C8a `project-only`) — no commit's Check-36 keyword changes.

**Why C0 first (the carve-out is the enabling commit).** The 2nd-adversarial re-review (B-1) proved the restored split is internally infeasible WITHOUT the carve-out: a `project-template/`-only content edit changes a v11 fixture's installed-HEAD SHA → a `test-fixtures/manifest.txt` row changes → the `regenerate-manifest-v11-surface` rule FORCES staging the pack-side `test-fixtures/manifest.txt` in the SAME commit (else `build.sh --verify` goes CI-RED on a stale manifest). Under the un-patched Check 36, that staged manifest is a `project-only` offender (`test-fixtures/manifest.txt` starts with neither `project-template/` nor `supporting-docs/`), so the commit can NEITHER be cleanly `project-only` NOR omit the manifest. **C0 resolves the pincer:** it adds a one-path scope-neutral carve-out (design §17) so `test-fixtures/manifest.txt` is filtered out of BOTH offender branches → a `project-only` content commit + the carved manifest passes Check 36, AND because the staged manifest is the build-faithful regeneration, `build.sh --verify` passes too. C0 must land FIRST so every later `project-only` commit (C6a/C7a/C8a) is evaluated by the carve-out-aware Check 36. C0 itself is cleanly `pack-only`: it edits only `scripts/validate-pack.py` + its test, neither of which projects into the client fixtures → the manifest diff is EMPTY for C0 → no manifest stage (no self-referential pincer).

**Why the split (user decision 6 — exclusive keyword on every commit; B-1 RESOLVED by C0).** Guard-B(project), Guard-C(project extension), and Guard-A′ live in `scripts/validate-pack.py` (pack-side). A commit that edits BOTH `project-template/` AND `scripts/validate-pack.py` CANNOT carry `project-only` (`scripts/` is outside `_PROJECT_SIDE_PATH_PREFIXES`). So each mixed pair splits **data-first**:
- **DATA half (`project-only`):** the `project-template/` edits only (PM-CHAT, agent files, agent-run.sh, OPTIONAL-FEATURES) + the carved-out `test-fixtures/manifest.txt`. No `scripts/` source touch → `project-only` is clean (the manifest is scope-neutral via C0; every other path is project-side).
- **GUARD half (`pack-only`):** the `scripts/validate-pack.py` guard + its new per-check test + the `validate-pack.yml` wiring only. No `project-template/` touch → `pack-only` is clean. It lands AFTER its data half, so the guard is GREEN on arrival (the data it asserts is already committed). Its manifest diff is expected-EMPTY (validate-pack.py does not project into client fixtures — S-2), so no manifest stage there.
This is strictly green-per-commit (the guard never lands before its data) AND keyword-exclusive (no neutral commit). The 2nd-adversarial's M-1 "unsatisfiable J1" finding is RESOLVED by C0: the exclusive `project-only` keyword IS achievable on the DATA halves because the carve-out neutralizes the only forced cross-prefix path.

Rationale for ordering: **C0 (the carve-out) before everything** — it is BD-197's enabling commit (design §17); it must land first so the carve-out-aware Check 36 evaluates every later `project-only` commit. Then P2 (clean slate) before P3 (builds the enabled model) — the BD's own phase order. Within P3, pack-side (C3–C5) before client-side (C6–C8) because the pack-side guards/patterns are the reference the client side is authored to mirror NATIVELY (not byte-copied), and the pack merge-back mechanism is the PRIMARY UC-1 deliverable. RW/RO model (C3 / C6a) before merge-back (C4 / C7a) because merge-back prompts/skills reference the two-class distinction. OPTIONAL-FEATURES (C5 pack / C8a project) last because **Guard-A′ (the OPTIONAL-FEATURES presence-check) ships ONCE in the FINAL pack-only guard half C8b** (decision 7) — by C8b BOTH OPTIONAL-FEATURES files carry the `baseRef` + `bgIsolation` keys, so the cross-surface presence-check is green on arrival. C5 does NOT ship Guard-A′; C5 verifies pack-side key presence by the coder's PREFLIGHT grep, not a cross-surface guard (decision 7). Within each split pair, the **DATA half precedes the GUARD half** (C6a→C6b, C7a→C7b, C8a→C8b) so each guard is green when it lands.

---

## B. File-level task breakdown per commit

### C0 — Check 36 manifest carve-out (enabling commit; pack-only)
BD-197's FIRST commit (design §17). It makes the C6a/C7a/C8a `project-only` keyword achievable by treating the forced-co-variant `test-fixtures/manifest.txt` as scope-neutral in Check 36. Edits the validator + its test IN LOCKSTEP (enumerate-encoding-surfaces).
- `scripts/validate-pack.py` — add the carve-out (design §17.4, exact edit):
  - **NEW constant** beside `_PROJECT_SIDE_PATH_PREFIXES` (measured at `:4126`): `_SCOPE_NEUTRAL_GENERATED_PATHS = frozenset({"test-fixtures/manifest.txt"})` — sized to EXACTLY the measured forced-co-variant set (§F EE-10; measure-then-bound — a frozenset of ONE path, NOT a `test-fixtures/` prefix, so the static `v11-trinity-marker-prepped/` snapshot + the recipe `build.sh`/`README.md` still count toward scope).
  - **NEW predicate** beside `_is_project_side_path` (measured at `:4250`): `def _is_scope_neutral_generated(path: str) -> bool: return path in _SCOPE_NEUTRAL_GENERATED_PATHS` (with the design §17.4 docstring naming §17 as the rationale).
  - **BOTH offender branches** (measured: `pack_only` at `:4313`, `project_only` at `:4323`): add `and not _is_scope_neutral_generated(p)` to each comprehension. The `pack_chat_only` branch (`:4333`) is UNCHANGED (out of scope, scope-deliverables-to-the-ask).
- `scripts/tests/test-validate-pack-checks-36-37-38.sh` — update IN THE SAME COMMIT (design §17.6):
  - **Group 0 (module-registration, measured `required` list at `:43-53`):** add `_is_scope_neutral_generated` to `required` (mirrors how `_is_project_side_path` is asserted at `:49`).
  - **Group 1 (scope-rule unit tests, after the `assert_pside` block ~`:172`):** add cases NC-1..NC-6 (design §17.6) — (NC-1) predicate admits the manifest; (NC-2) predicate rejects `scripts/validate-pack.py`; (NC-3) predicate rejects sibling `test-fixtures/v11-trinity-marker-prepped/CLAUDE.md` AND `test-fixtures/build.sh` (proves exact-string, not a prefix); (NC-4) a `project-only` commit = `["project-template/docs/pack/PM-CHAT.md","test-fixtures/manifest.txt"]` PASSES (offenders empty); (NC-5) a `pack-only` commit = `["pack-ops/PACK-CHAT.md","test-fixtures/manifest.txt"]` PASSES; (NC-6) a real cross-surface offender = `[...,"scripts/validate-pack.py","test-fixtures/manifest.txt"]` STILL FAILS (offenders = `["scripts/validate-pack.py"]`).
- **RUN the updated test → re-run the FULL battery, SAME commit.** Check 36's test already exists + is wired into `validate-pack.yml` (no run-before-wire NEW-test ceremony — this is an UPDATE to an existing wired test), but the coder MUST still RUN the updated `test-validate-pack-checks-36-37-38.sh` (quote exit 0) AND re-run the full battery to confirm no regression (verify-full-ci-suite).
- **PREFLIGHT (measure-then-bound):** the coder confirms (a) the carve-out admits ONLY `test-fixtures/manifest.txt` (the projected `project_only` predicate over `[<project path>, "test-fixtures/manifest.txt"]` → offenders empty; over `[<project path>, "scripts/validate-pack.py", "test-fixtures/manifest.txt"]` → offenders `["scripts/validate-pack.py"]` — design §17.5 EB-5), and (b) `git status --short test-fixtures/manifest.txt` is clean after `bash test-fixtures/build.sh --all --clean` (validate-pack.py does not feed client fixtures → manifest diff EMPTY → C0 stages NO manifest).

C0 touches `scripts/` → it IS a v11-surface dir, so the coder RUNS `bash test-fixtures/build.sh --all --clean`; but validate-pack.py + its test do NOT project into client fixtures → the manifest diff is EMPTY → NOTHING is staged (§G). Keyword `pack-only` (no `project-template/`/`supporting-docs/`). Green on arrival: the carve-out only ADDS a filter; it cannot make any existing commit red (a true cross-surface offender is still flagged — §17.5(a)).

### C1 — P2 disposition: non-rule worktree carriers (pack-only)
Plan/research/methodology docs that REFERENCE the prohibition or bug-era model but are not the trinity rule itself. Re-audit at commit-time per design §11 (fresh-audit mandate).
- `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md` — UPDATE the "no worktree isolation from non-main clones" digest to the enabled opt-in model (design §11.1 row 5).
- `maintenance-docs/v11-implementation/EXECUTION-PLAN-V11.0.md` §D — **UPDATE IN PLACE to the enabled model** (decision 9 — user chose update-in-place, NOT annotate-as-superseded). Updating in place also keeps this file OUT of Guard-A's allowlist (an annotated-superseded note that retained prohibition prose would self-match the prohibition gate; an in-place update to the enabled model does not). No coder update-vs-annotate call here — it is decided.
- `maintenance-docs/v11-implementation/PLAN-SKILL-DIMENSIONS.md` §4.8 + reproductions — **UPDATE IN PLACE to the enabled model** (decision 9 — NOT annotate-as-superseded). Same Guard-A-allowlist rationale as EXECUTION-PLAN above: an in-place enabled-model update removes the prohibition prose (measured 2 matches at HEAD) so the file drops out of the prohibition matcher entirely, rather than being allowlisted.
- `maintenance-docs/v11-implementation/PLAN-DOC-CONCISION-GUARDRAILS.md` — UPDATE the "NO worktree isolation" coder-spawn-template fragment (§11.1 row 8).
- `maintenance-docs/v11-implementation/ARCHITECTURE-BD-196-S1-CORPUS-CLASSIFICATION.md` row #24 — excise the stale `L348-357` line-range; reflect the rule change (§11.1 row 9). If the coder RETAINS the old rule-name as a classification record, this file stays in the Guard-A allowlist (coder records the disposition).
- `maintenance-docs/v11-research/RESEARCH-CLAUDE-REPOS-SURVEY.md` — UPDATE stale caveat + EXCISE the `feedback_worktree_isolation_broken_from_v11_clone` dangling-ref (§11.1 row 10).
- `maintenance-docs/v11-implementation/ARCHITECTURE-SKILL-AGENT-MAINTAINABILITY.md` — EXCISE the dangling-ref (§11.3; active non-process carrier, measured below).
- `maintenance-docs/v11-implementation/RESEARCH-19C-G-ITEMS-VERIFICATIONS.md` — EXCISE the dangling-ref (§11.3; active non-process carrier).
- `maintenance-docs/v11-implementation/PLAN-DEPLOYMENT-PYTHON-OBSERVABILITY.md` — VERIFY at commit-time (real prohibition vs incidental `baseRef` token); UPDATE or leave (§11.1 row 13).
- LEAVE-as-history (do NOT edit): `IMPLEMENTATION-REPORT-BD-196-C9.md`, `IMPL-REPORT-BD-214-C5a-BD197.md`, the 9 archive files (D4), and the BD-197-process artifacts.
- LEAVE (benign, still true): `pack-ops/PACK-CHAT.md` "multiple worktrees on the same clone" (§11.2).
- Pack-Chat-direct bookkeeping (NOT coder, NOT this commit's scope but flagged): the entry's stale "4 dangling refs" → the measured active EXCISE set = **3** non-process active carriers (`RESEARCH-CLAUDE-REPOS-SURVEY.md`, `ARCHITECTURE-SKILL-AGENT-MAINTAINABILITY.md`, `RESEARCH-19C-G-ITEMS-VERIFICATIONS.md` — §F EE-3, re-confirmed at this HEAD); Pack Chat updates `backlog/BD-197.md`. (The BD-197-process dangling-ref carriers — incl. this PLAN doc — are LEAVE, allowlisted; archive is LEAVE per D4.)

C1 touches NO `project-template/`, `scripts/`, `pack-ops/` product surface except `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md` → **v11-surface (pack-ops/) touched → manifest regen required** (§G). Keyword `pack-only` (no project-template/supporting-docs).

### C2 — P2 remove the prohibition + bug-era guardrails (pack-only)
The trinity rule + the highest-coupling skill edits. Routed through the PACK-CHAT rule-change propagation procedure for the `## Pack memory` edit (design §11.1 row 1, BD-197 process-note).
- `CLAUDE.md` `### Sub-agent behavior (Claude-only)` worktree bullet (measured line 325-334) — REPLACE the prohibition with the **enabled-model opt-in bullet from design §12.1(a)** (the design supplies the literal). **Mode-model (Correction pass 2026-06-14):** the bullet states subagents run IN-PLACE by default; a chat opts a subagent into isolation by passing the per-spawn Agent-tool **`isolation:"worktree"` PARAMETER (the TRIGGER; only valid value)**; the developer sets **`worktree.baseRef:"head"`** in settings so the worktree bases at local HEAD (unset/`fresh` bases at origin/main — a documented wrong-base degradation); RW agents emit a `/tmp` patch the orchestrator applies (agents never commit); the agent verifies its actual regime at runtime (pwd/HEAD ground-truth), never trusting settings; **`worktree.bgIsolation` governs background SESSIONS only (not subagents) → BD-218.** Do NOT reference any "9-cell matrix" or "bgIsolation-as-trigger" — those are removed from the design (§3). Preserve the existing Trinity-exemption framing (measured: the subsection is Claude-only and already carries a Trinity-exemption note at CLAUDE.md). **Trinity nuance:** because this subsection is Claude-only/trinity-exempt, the bullet does NOT need a parallel AGENTS.md/GEMINI.md root edit — measured AGENTS.md/GEMINI.md root carry 0 worktree refs. The coder MUST NOT add the rule to root AGENTS/GEMINI (that would break the Claude-only exemption). Flag for BD-217: Codex/Gemini get their own worktree story later.
- `.claude/skills/commit-discipline/SKILL.md` — REDESIGN regime-detecting (design §12.4): §1 pre-flight non-fatal both directions; §2 write-target regime-aware (keep the absolute ban on retargeting another agent's main checkout); §3 git-state ban carries the denylist + principle line; §6 retire the bug-era "/tmp = wrong path" anti-pattern. Measured asserts to remove: line 20 `pwd # Must end in worktree path`, line 22 `Must start with worktree-agent-`.
- `.codex/skills/commit-discipline/SKILL.md` — mirror of the above (quad/trinity mirror).
- `.gemini/skills/commit-discipline/SKILL.md` — mirror of the above.
- `.claude/.codex/.gemini/skills/implementation-report/SKILL.md` (×3) — REDESIGN regime-aware (§11.2): in-place ⇒ diff against parent base; isolated ⇒ the patch is the persisted /tmp artifact. Measured: 4 worktree refs each.
- `.claude/.codex/.gemini/agents/pack-coder.{md,toml}` (×3) — UPDATE "makes the file changes in its worktree" / "Branch + final HEAD SHA on your worktree" → "in its scoped working tree (or isolated worktree when opted-in); emits a patch + report" (§11.2). NOTE: the git-permission carve-out drop (`git checkout -- <path>`) is C4 work, not here — C2 is removal of the worktree-MODEL prose only. The carve-out removal is **per-CLI audience-correct, NOT a byte-identical excision** (decision 3 / S-1): the Codex `.toml` embeds the carve-out mid-sentence (`.codex/agents/pack-coder.toml:21`) in different prose from the `.claude`/`.gemini` `.md` phrasing (`:37`/`:39`) — see C4.

C2 touches `scripts/`? No. Touches `pack-ops/`? No (the trinity rule is at pack-root CLAUDE.md, not pack-ops/). Touches skills + agents (not in the four v11-surface dirs). **Manifest: verify — CLAUDE.md pack-root trinity is base-case EXEMPT (manifest memory), and `.claude/`/`.codex/`/`.gemini/` are not in {project-template, scripts, pack-ops, supporting-docs}. If the C2 diff touches ONLY those, NO manifest regen. The coder runs `bash test-fixtures/build.sh --all --clean` and stages manifest ONLY if the diff is non-empty (§G).** Keyword `pack-only`.

### C3 — P3 pack RW/RO two-class model + Guard-B(pack) (pack-only)
- `pack-ops/PACK-AGENTS.md` — extend the roster (measured: `## Pack agents` table, 5 rows, `Mode` column at line 13) with an explicit `Class` column (`RW`/`RO`); add a "## Two agent classes" subsection under `## Agent permission rules` (design §4.3 pack). Measured set: 1 RW (`pack-coder`) + 4 RO.
- `.claude/.codex/.gemini/agents/pack-{architect,coder,docs-researcher,planner,reviewer}.{md,toml}` (5 ×3 = 15) — per-agent prose mandate header reinforcement (`**Source-write within scope.**` RW / `**Read-only.**` RO) (§4.3).
- `scripts/validate-pack.py` — NEW Guard B (pack): set-equality {PACK-AGENTS roster `Class` cells} ↔ {agent-file PROSE mandate headers}. Bind to the PROSE header, NEVER `tools:` (design §13.2; `pack-reviewer` carries `Write,Edit` yet is RO). Next check number = **Check 52** (measured highest existing = Check 51). Measure-then-bound + runtime guard per §E.
- New per-check test `scripts/tests/test-validate-pack-check-52.sh` — **author the test → RUN it locally (quote exit 0) → wire it into `.github/workflows/validate-pack.yml` `tests` job → re-run the FULL battery, ALL in the SAME commit** (decision 2, run-before-wire; §D). Record Guard-B(pack)'s measured **wall-time against a stated budget** in the IMPL-REPORT (decision 4 — single whole-tree pass, no subprocess-per-entry, battery = 202 validate-pack invocations [re-measured at `05ad61b`, §F EE-1; was 186 at the 2nd pass]).

C3 touches `pack-ops/` + `scripts/` → **v11-surface → manifest regen required** (§G). Keyword `pack-only`.

### C4 — P3 pack in-session spawn + merge-back + git-permission hardening + commit-discipline redesign completion + backstop (pack-only)
- `pack-ops/PACK-CHAT.md` — codify the IN-SESSION orchestrator spawn procedure AND the merge-back procedure (design §18.1 pack-side + §4.1 + §6). **§18 sharpen (this is the C4 CONTENT clarification — measured ABSENT at HEAD `05ad61b`, §18.E EB-B / §F EE-11: PACK-CHAT.md carries NO `isolation`/`/tmp`/handoff/merge-back/`run_in_background` spawn contract today; the 1 "handoff" hit is the benign "handoff at Batch" prose).** The edit MUST be the orchestrator IN-SESSION spawn procedure, not ONLY the post-return apply: state that Pack Chat spawns **RW agents (coders) IN-SESSION via the Agent tool with `isolation:"worktree"`**, **RO agents IN-PLACE (no isolation)**, in the **BACKGROUND** (`run_in_background`, the existing pack default-background behavior), keyed off the PACK-AGENTS `Class` SSOT (C3) — RW ⇒ isolate; RO ⇒ in-place; and names the per-spawn absolute `/tmp` handoff dir + the IMPL-report path + the patch path. Then the merge-back half (same C4 edit): read the patch, `git apply --check`/`--3way`, conflict protocol (atomic per-patch check→apply→review→commit; STOP+surface+re-spawn fresh coder on conflict, no hand-merge) — the agent runs ZERO git-state changes (`git diff > /tmp/...` only); the orchestrator applies + commits. **No-platform-safety-net (FACT-4):** the spawn instruction states RW agents MUST be spawned isolated and the agents-never-commit + destructive-verb ban is LOAD-BEARING, not advisory.
- `CLAUDE.md` `## Pack memory` `agents-never-commit` bullet — amend to ENUMERATE the §5.1 denylist + the read-only-only principle line (design §5.3, §12.1(c)). Routed via the PACK-CHAT propagation procedure: corpus (1) → rationale (2) → references (4) + manifest (5) same commit → cache (3) upkeep → manifest regen (6). **Trinity:** this bullet is in `### Workflow`, NOT the Claude-only subsection → it IS a trinity rule → parallel edit in AGENTS.md + GEMINI.md `## Pack memory` `agents-never-commit` bullet in the SAME commit (trinity rule). Apply cross-CLI normalization (audience-correct, not byte-copy where a per-CLI value differs; the verb denylist is platform-neutral so it copies, but confirm).
- `pack-ops/PACK-MEMORY-RATIONALE.md` `## agents-never-commit` — update the verb list + record the principle (§5.3).
- `pack-ops/.spawn-rule-manifest.txt` — verify slug→canonical still resolves; add the `agent-two-class-model` rationale slug if §12.1(b) introduces it.
- `.claude/.codex/.gemini/skills/commit-discipline/SKILL.md` (×3) — §3 carries the full denylist + principle (add `restore --staged`, `apply`, `worktree`, `clean`, `branch -d/-D` if absent). (This completes the C2 regime-detect redesign with the C4 verb-list hardening; if the coder prefers, the verb-list hardening can fold into C2 — but C4 keeps it with the rest of the git-permission work for cohesion. Either is green; planner keeps it in C4 to bind the denylist edit to the rationale/cache propagation that lives in C4.)
- `.claude/.codex/.gemini/agents/pack-coder.{md,toml}` (×3) — add the denylist reinforcement AND **DROP the stale `git checkout -- <path>` carve-out** (measured present: `.claude/agents/pack-coder.md:37`, `.codex/agents/pack-coder.toml:21`, `.gemini/agents/pack-coder.md:39`). **Remove the carve-out PER-CLI, audience-correct (decision 3 / S-1 / cross-cli-reference-normalization) — NOT a byte-identical exact-string excision:** the Codex `.toml:21` embeds the clause mid-sentence in a longer read-only-verbs prose block (`... git checkout (except \`git checkout -- <path>\` to inspect file contents at a different ref). ...`), which differs from the `.claude`/`.gemini` `.md` phrasing. Excise the carve-out clause in EACH file in its own phrasing; the coder PREFLIGHT confirms ZERO carve-out remains in ANY of the three (`grep -c 'checkout -- <path>' ... == 0` across all 3). **M-2 PROSE-COHERENCE check (re-review M-2 — token-absence is NECESSARY but NOT SUFFICIENT):** `grep -c == 0` proves the TOKEN is gone but NOT that the surrounding sentence still reads correctly. The Codex `.toml:21` embeds the clause MID-SENTENCE in a longer read-only-verbs block (measured live: `"... git checkout (except \`git checkout -- <path>\` to inspect file contents at a different ref). These are forbidden ..."`) — a naive parenthetical excision leaves a dangling `(except )` fragment or a now-stale `git checkout` listing. So the C4 coder PREFLIGHT + reviewer MUST additionally READ BACK each of the 3 excised sentences and confirm: (a) no orphan `(except)` fragment; (b) `git checkout` is handled per design §5.1 — plain `checkout` of a path IS destructive, so it stays in the deny enumeration with NO exception (not silently dropped from the list). This is the enumerate-encoding-surfaces lock-step nuance the prior plan under-specified. Add the RW-emit step (emit patch + report to handoff dir).
- `.claude/.codex/.gemini/agents/pack-{architect,planner,reviewer,docs-researcher}.{md,toml}` — RO-emit step (name the report path; under /tmp handoff when isolated) per §4.2.
- Mechanical backstop (RECONCILED to design §18.2 — the in-session backstop model; J4 = NO new shipped pack-side file). The C4 pack-side IN-SESSION backstop is: (i) the always-on shipped PROSE deny-list (the §5.1 verb set + §5.2 principle, carried in the trinity/skill/agent/rationale surfaces this C4 already edits — NOT a new file); (ii) the in-session MECHANICAL hard-deny is the **documented-OPTIONAL user-configured `permissions.deny` recipe** (per F2 session-scoped + sub-agent-inherited; the pack DOCUMENTS it in OPTIONAL-FEATURES — authored in C5 — and ships NO settings file); (iii) a user-configured **PreToolUse hook is SECONDARY (fails-open, F2)** — also NOT shipped by the pack. **The pack ships NO PreToolUse hook and NO settings file for the in-session path** (§18.2 EB-D). Any such recipe/hook is VERB-PRECISE: deny `Bash(git apply:*)`, NEVER `Bash(git diff:*)`; confirm the `git diff > file` shell redirect is not tripped (design §5.1, §5.3, §18.2, G-4). The LAUNCHER mechanical layer (layer iii of §18.2) is the project `agent-run.sh --disallowedTools` extension, which lands project-side in C7a — NOT a pack-side file. **Pre-coding GATE (decision 5 / S-3 / dependency-direction-placement — HARD STOP, retained as a safety net):** if any backstop element can be expressed by EDITING existing config (a hook entry the USER adds to THEIR settings, `--disallowedTools` flags on an existing invocation, or documented-prose), it stays inline/documented. **But if any element would require a NEW pack-side FILE/SCRIPT, the coder MUST STOP and may NOT author it on its own authority** — Pack Chat escalates to **architect + user sign-off** BEFORE the coder writes it (the Check-47 `_SANCTIONED_PACK_SIDE_SHIPPED` allowlist is FROZEN to exactly `{scripts/lib/detect.sh, scripts/pack-help.sh}`; a net-new pack-side file that also ships would trip Check 47). Per §18.2 the F1–F5 model needs NO new pack-side file (J4 = NO), so this gate is expected to resolve as NO-new-file — but it PRECEDES the C4 coder spawn; resolve it at the C4 architect-check, not at commit-time by the coder (§J4).
- `test-fixtures/manifest.txt` regen (v11-surface: pack-ops/ + scripts if the hook touches scripts/).

C4 touches `pack-ops/` (+ possibly `scripts/`) → **manifest regen required** (§G). Keyword `pack-only`. NOTE: the trinity `agents-never-commit` parallel edit to AGENTS.md/GEMINI.md is pack-root trinity, which is base-case manifest-EXEMPT, but pack-ops/ is touched so manifest regen fires anyway.

### C5 — P3 pack OPTIONAL-FEATURES section + Guard-A + Guard-C (pack-only)
- `pack-ops/OPTIONAL-FEATURES.md` — new section "## Claude Code — Isolated parallel agents (worktree isolation)" modeled on the existing Agent-Teams section shape (measured: `## Claude Code — Agent Teams` at line 19). Content per design §9 + §12.3, **on the CORRECTED two-independent-mechanisms model (Correction pass 2026-06-14)**: status/what/when; **TRIGGER (per task) = the per-spawn Agent-tool `isolation:"worktree"` PARAMETER** (only valid param value — `head`/`none` are SETTINGS values, NOT param values; this RESOLVES the design's note-9 VERIFY, but the C5 coder STILL confirms empirically once the prohibition is lifted by C2, recording the confirmation in the IMPL-REPORT); **BASE (REQUIRED setting) = `worktree.baseRef:"head"`** in `settings.json` (per-project recommended OR global) so isolated worktrees branch from local HEAD — **with the explicit consequence: unset/`fresh` defaults to branching from `origin/<default>` (origin/main), the historical wrong-base degradation**; **`worktree.bgIsolation`** described ACCURATELY as the **background-SESSION gate** (enum `["worktree","none"]`, default `"worktree"`; blocks Edit/Write to main until `EnterWorktree`) that does NOT control subagents, **with a pointer to BD-218** (v11.1); caveats (#60588 version-sensitive, #38287 silent-delete, #39886 silent-fall-to-MAIN, the `fresh`=origin/main wrong-base); "pack ships NO settings file"; Trinity-exempt note (Codex/Gemini = BD-217); the manual-worktree one-liner (UC-low). **NO 9-cell matrix, NO "bgIsolation is the trigger" — those are REMOVED from the design (§3).** **§18.2(ii) sharpen — ADD the documented-optional `permissions.deny` recipe (the in-session mechanical backstop).** Alongside the `baseRef`/`bgIsolation` mode-setting content, add a subsection documenting the OPTIONAL user-configured `permissions.deny` block the developer may add to THEIR OWN `settings.json` (user or project scope — the pack ships NO settings file) to mechanically hard-deny the §5.1 destructive git verbs for ALL in-session sub-agents. Per RESEARCH F2 the block is SESSION-SCOPED + INHERITED by sub-agents (incl. background) + deny-first (not bypassed by `bypassPermissions`); it is the ONLY in-session mechanical layer (F1: agent-file `tools:` cannot deny a git sub-verb; no per-spawn deny param). The recipe lists the §5.1 verbs as scoped Bash rules — e.g. `"Bash(git commit:*)"`, `"Bash(git push:*)"`, `"Bash(git add:*)"`, `"Bash(git reset:*)"`, `"Bash(git restore:*)"`, `"Bash(git checkout:*)"`, `"Bash(git apply:*)"`, `"Bash(git worktree:*)"`, `"Bash(git clean:*)"`, … — VERB-PRECISE (deny `apply`, NEVER `diff` — the patch-emit). Note the PreToolUse hook (matcher `Bash`, `permissionDecision:"deny"`) as a SECONDARY defence-in-depth option whose `if`-matcher FAILS OPEN (F2) — so `permissions.deny` is the documented-primary mechanical layer. This is additive PROSE in the EXISTING file — **NO shipped settings file, NO new pack-side file (J4=NO; Check-47 untouched — §18.2 EB-D / §F EE-12 baseline: `permissions.deny` measured 0 in this file at HEAD).** **Coder PREFLIGHT (decision 7 + §18):** grep-confirm the pack file mentions `baseRef` AND `bgIsolation` AND the `permissions.deny` recipe token post-edit (the pack-side half of what the EXTENDED Guard-A′ will later assert in C8b — C5 verifies all three by PREFLIGHT grep, NOT by a cross-surface guard).
- `scripts/validate-pack.py` — NEW **Guard A** (Check 53): prohibition-stays-removed flip-block — PROHIBITION-ONLY signature (`no worktree isolation`, `Do not pass .*isolation.*worktree`), NEVER the bare `baseRef`/`bgIsolation` key names (design §11.5 gate (a), §13.1; 2nd-adversarial G-1/G-2). **NARROW self-exception (decision 1 / plan-review BLOCKER B-1 — distinct from the re-review's B-1 manifest pincer; use the Check-51 precedent verbatim):** the matcher otherwise SELF-MATCHES because `scripts/validate-pack.py` and the new test file QUOTE the regex literal. So Guard-A: (i) **self-skips the validator** via the existing `entry.name == "validate-pack.py"` pattern (precedent at `scripts/validate-pack.py:2169`); (ii) **allowlists ONLY the single new test file `scripts/tests/test-validate-pack-check-53.sh`** — NARROW, NOT the whole `scripts/tests/` dir; (iii) **allowlists the commit-time-MEASURED BD-197 doc carriers** (the per-file KEEP set, §F EE-2). **Guard A′ is NOT shipped in C5** — it ships in **C8b** (decision 7), the commit where BOTH OPTIONAL-FEATURES surfaces satisfy it. NEW **Guard C** (decision 8 / §13.3): verb-enumeration parity — **PREFER folding into an existing parity check; author a standalone Check 56 ONLY if folding over-complicates**; record the fold-vs-standalone rationale in the IMPL-REPORT. (If standalone, the verb-parity STANDALONE check + its test land here in C5 or in C7b per the coder's fold decision — §J3.) Measure-then-bound + runtime guard per §E. **PREFLIGHT (decision 1):** assert the prohibition matcher returns EXACTLY the measured legitimate set INCLUDING the just-authored validator (self-skipped) + the new check-53 test.
- New per-check test for Check 53 (+ Check 56 if Guard-C is standalone here) — **author → RUN locally (quote exit 0) → wire into `validate-pack.yml` `tests` job → re-run the full battery, ALL in the SAME commit** (decision 2). Record each new check's measured **wall-time vs a stated budget** in the IMPL-REPORT (decision 4 — single whole-tree `rg`, no subprocess-per-entry, battery = 202 invocations [re-measured at `05ad61b`, §F EE-1; was 186 at the 2nd pass]) (§H).

C5 touches `pack-ops/` + `scripts/` → **manifest regen required** (§G). Keyword `pack-only`. **Guard-A′ does NOT ship in C5** — it ships in **C8b** (decision 7), the FINAL pack-only guard half, by which point BOTH OPTIONAL-FEATURES surfaces carry the keys, so the cross-surface presence-check is green on arrival. C5 verifies pack-side key presence by the coder PREFLIGHT grep (above), NOT a cross-surface guard. This removes the prior plan's "Guard-A′ would be RED between C5 and C8" hazard entirely.

### C6a — P3 project RW/RO two-class model + agent-run.sh projection (DATA; project-only)
- `project-template/docs/pack/PM-CHAT.md` `## Permission profiles` table — keep as the authored SSOT (measured 14 RO + `coder` + `repo-ops`); add the two-class framing native to the client audience (PM Chat orchestrator, not Pack Chat) (design §4.3 project).
- `project-template/agent-run.sh` `READONLY_AGENTS` — confirm it remains the CI-checked projection (measured 14 entries, lines 38-53); fix the stale comment (measured lines ~92-94 "Edit/Write excluded at agent-definition level" — FALSE since BD-127 kept Write) per §4.3 / inventory §3.1.
- `project-template/.{claude,codex,gemini}/agents/*.{md,toml}` (16 ×3 = 48) — per-agent prose mandate header reinforcement (`**Write-capable (scoped).**` / `**Read-only.**`); `repo-ops` = RW sub-label "Write-capable (script)" (§4.3). Authored client-native; ZERO pack-self references (bd-pack-only rule).

C6a touches `project-template/` ONLY (no `scripts/` source) + the v11-surface manifest regen → stages `project-template/` content + the carved-out `test-fixtures/manifest.txt` → **`project-only`** (clean Check-36: project content is project-side, the manifest is scope-neutral via C0; nothing else). **C0's carve-out is load-bearing here** — without it the staged manifest would deny `project-only` (B-1). v11-surface (project-template/) → **manifest regen required + non-empty** (project-template/ projects into the v11 fixtures → SHAs drift → manifest changes) (§G). This DATA half lands the RW/RO declarations Guard-B(project) will assert in C6b.

### C6b — P3 Guard-B(project) RW/RO consistency check (GUARD; pack-only)
- `scripts/validate-pack.py` — NEW **Guard B (project)** (Check 55): set-equality {PM-CHAT RO rows} ↔ {`agent-run.sh READONLY_AGENTS`} ↔ {per-file RO PROSE headers} (design §13.2). Measured 14 RO + 2 RW. **Bind to the PROSE header, NEVER `tools:`** — Gemini files have no `tools:` (measured 0/16) so prose+array only.
- New per-check test `scripts/tests/test-validate-pack-check-55.sh` — **author → RUN locally (quote exit 0) → wire into `validate-pack.yml` `tests` job → re-run the full battery, ALL in the SAME commit** (decision 2). Record Guard-B(project) **wall-time vs budget** in the IMPL-REPORT (decision 4).

C6b touches `scripts/` ONLY (no `project-template/`) → **`pack-only`** (clean Check-36). v11-surface (scripts/) → **manifest regen RUN; stage ONLY if diff non-empty (expected EMPTY — S-2)** — `scripts/validate-pack.py` + a `scripts/tests/test-*.sh` do NOT project into the client fixtures, so no fixture SHA drifts and the manifest diff is empty; the RUN obligation fires (scripts/ is v11-surface) but the STAGE is a no-op (§G). GREEN on arrival: the RW/RO data it asserts is already committed in C6a (measure-then-bound).

### C7a — P3 project in-session spawn + merge-back + git-permission hardening + agent-run.sh --disallowedTools (DATA; project-only)
- `project-template/docs/pack/PM-CHAT.md` — **§18.1 sharpen — ADD the IN-SESSION Agent/Task-tool spawn instruction (this CLOSES the project under-scope; measured ABSENT at HEAD `05ad61b`, §18.E EB-A / §F EE-11: PM-CHAT.md documents spawning ONLY via the `agent-run.sh` launcher — ZERO `isolation`/`worktree`/`/tmp`/handoff/merge-back hits).** Author a new in-session-spawn subsection CLIENT-NATIVE, natural home alongside `## Permission profiles` (measured `:397`, where the only spawn mechanism today is the per-profile `agent-run.sh` flag block), covering the FIVE elements of §18.1: (1) **two spawn paths** — IN-SESSION via the Agent/Task tool from PM Chat (the new PRIMARY), and the existing `agent-run.sh <cli> --agent <name>` launcher (the documented SECONDARY); (2) **`isolation:"worktree"` for RW agents ONLY** (`coder`; `repo-ops` is RW-script) spawned in-session, RO agents (the 14 read-only profiles) WITHOUT isolation — keyed off the `## Permission profiles` RW/RO classification (the project SSOT, D2); (3) **background spawning** so PM Chat stays interactive (client-native phrasing per cross-CLI-reference-normalization — NO pack-self `run_in_background` rule citation); (4) the orchestrator **`/tmp`-patch merge-back** (PM Chat names a per-spawn absolute `/tmp` handoff dir + IMPL-report path + patch path; the RW agent edits, runs in-scope verification, emits a read-only `git diff` patch + report to the handoff dir, returns; PM Chat reads, runs the review/fix cycle, `git apply --check`/`--3way`, applies, commits with developer approval — agent does ZERO git-state changes); (5) **conflict + degradation** point at the project's OWN §6/§8 client-native homes (the PM-CHAT merge-back procedure + project OPTIONAL-FEATURES). **§4.1 authored CLIENT-NATIVE** (orchestrator = "PM Chat"; the project's own merge-back, NOT a reference to the pack flow). **NOT a byte-copy of PACK-CHAT.md** (pack-project-separation). **ZERO pack-self references** (no BD-NNN, `maintenance-docs/`, `pack-*`, Pack Chat, `pack-ops/`). **No-platform-safety-net (FACT-4):** the instruction states RW agents MUST be spawned isolated and the agents-never-commit/verb-ban is LOAD-BEARING. The launcher remains the documented SECONDARY path; the in-session path is the new PRIMARY documented path.
- `project-template/.{claude,codex,gemini}/agents/coder.*` — the RW-emit step (patch + report to handoff dir) (§12.2). **No platform safety net (Correction pass / FACT-4):** a non-isolated background subagent can write the parent checkout freely → the coder file MUST state RW agents are spawned with `isolation:"worktree"` and the agents-never-commit/verb-ban is load-bearing, not advisory.
- `project-template/skills/implementation/SKILL.md` — regime-aware report step. **AS-BUILT (2026-06-14, BD-197 Note 15(c) / C7a D-1, P-missed-7):** there is NO project-side `implementation-report` skill (that name exists only pack-side); the regime-aware report step landed at the project's real report-step SSOT `project-template/skills/implementation/SKILL.md` (+ the project coder agent files), NOT at the non-existent `project-template/skills/implementation-report/SKILL.md` this plan originally named.
- `project-template/agent-run.sh` — `--disallowedTools` hardening: add `Bash(git stash:*)`, `Bash(git reset:*)`, `Bash(git restore:*)`, `Bash(git checkout:*)`, `Bash(git apply:*)`, `Bash(git worktree:*)`, `Bash(git clean:*)` to `CLAUDE_READONLY_FLAGS` (measured: currently only `Bash(git commit:*)`/`push` at line 98; the dispatch-line copy at line 155 too) — VERB-PRECISE: deny `apply`, never `diff` (design §5.3, G-4). **Reconciliation nuance (§5.3):** the shipped `project-template/.claude/settings.json` ALLOWS `Bash(git add *)` for the human/PM (measured `:16`); the agent ban is enforced by the agent Hard rule + `agent-run.sh --disallowedTools`, NOT by the shipped settings. P3 must NOT add a `worktree` key to the shipped settings template (§7).
- Project trinity `## Project memory` "No destructive operations" rule (measured: project-template/CLAUDE.md/AGENTS.md/GEMINI.md carry it) — close any verb-enumeration gap surface-by-surface (enumerate-encoding-surfaces). **Trinity rule applies** — parallel edit across the 3 project trinity files in the SAME commit, cross-CLI normalized (GEMINI.md carries the extra approval-mode line per inventory C3 — preserve it).
- 48 agent files' Hard rules + Codex `## Permission profile` blocks — close any verb gap (measured: Codex already enumerates reset/stash; close remaining).
- **P3-launcher (UC-secondary) lives in C7a** — see the launcher note below; it is a `project-template/agent-run.sh` `--worktree` extension (project-only), probe-gated, not a `scripts/` edit.

C7a touches `project-template/` ONLY (no `scripts/` source) + the carved-out `test-fixtures/manifest.txt` → **`project-only`** (clean Check-36 via C0's manifest carve-out). v11-surface → **manifest regen required + non-empty** (§G). This DATA half lands the project verb-enumeration Guard-C(project) extension will assert in C7b (if standalone).

### C7b — P3 project verb-parity guard extension (GUARD; pack-only)
- `scripts/validate-pack.py` — Guard-A′(project) is NOT here (keys arrive in C8a; Guard-A′ ships in C8b). **Guard-C (decision 8 / §J3):** IF the coder chose a STANDALONE verb-parity check (vs folding into an existing parity check), extend/ship it here to cover the project verb-enumeration surfaces (trinity ×3, agent files, `agent-run.sh` flags) landed in C7a. **IF the coder folded Guard-C into an existing parity check**, that fold lands wherever the existing check lives (likely C5) and **C7b carries only the test/yml wiring delta for the project surface** (or is EMPTY and dropped — the coder records the fold-vs-standalone rationale + whether C7b is needed in the IMPL-REPORT).
- New/updated per-check test + workflow wiring — **author → RUN locally (quote exit 0) → wire → re-run full battery, SAME commit** (decision 2); **wall-time vs budget** in the IMPL-REPORT (decision 4).

C7b touches `scripts/` ONLY → **`pack-only`** (clean Check-36). v11-surface (scripts/) → **manifest regen RUN; stage ONLY if diff non-empty (expected EMPTY — S-2, same as C6b)** (§G). GREEN on arrival: the project verb data it asserts is committed in C7a. **If the coder folds Guard-C and C7b would be empty, C7b is DROPPED → 11 commits; the coder/Pack-Chat record this in the IMPL-REPORT.** (Plan numbers 12 with C7b present; the fold may reduce to 11 — a coder's-call per decision 8.)

### C8a — P3 project OPTIONAL-FEATURES isolation section (DATA; project-only)
- `project-template/docs/pack/OPTIONAL-FEATURES.md` — the SAME isolation section authored INDEPENDENTLY for the client audience (design §9, §12.3), **on the CORRECTED mode model**: client developer audience, client paths, "PM Chat" orchestrator; **TRIGGER = the per-spawn `isolation:"worktree"` param**; **BASE = `worktree.baseRef:"head"` REQUIRED** (with the unset/`fresh`=origin/main wrong-base consequence stated); **`worktree.bgIsolation` described as the background-SESSION gate with a BD-218 pointer** (NOT a subagent control); the project `agent-run.sh --worktree` launcher IF the §7 cwd-scoping probe passes ELSE the documented manual procedure (NEW-FORK-1 = gate-then-probe-then-degrade). Separate artifact; not a fallback for the pack version. ZERO pack-self references (no BD-NNN, no `maintenance-docs/`, no `pack-*`, no Pack Chat, no `pack-ops/`). **NO 9-cell matrix, NO bgIsolation-as-trigger.** **§18.2(ii) sharpen — ADD the documented-optional `permissions.deny` recipe authored CLIENT-NATIVE (separate artifact, NOT a byte-copy of the pack version).** Alongside the `baseRef`/`bgIsolation` mode-setting content, document the OPTIONAL `permissions.deny` block the downstream-project developer may add to THEIR `settings.json` to mechanically hard-deny the destructive git verbs for ALL in-session sub-agents (session-scoped + inherited + deny-first per RESEARCH F2); list the verbs as scoped Bash rules (VERB-PRECISE — deny `apply`, never `diff`); note the PreToolUse hook as a SECONDARY/fails-open option. Client audience, client paths, "PM Chat" orchestrator; ZERO pack-self references. **NO shipped settings file, NO new pack-side file (J4=NO; §F EE-12 baseline: `permissions.deny` measured 0 in this file at HEAD).** **Coder PREFLIGHT (§18 / C8b):** grep-confirm the project file mentions `baseRef` AND `bgIsolation` AND the `permissions.deny` recipe token (the project-side half of the EXTENDED Guard-A′, asserted in C8b).

C8a touches `project-template/` ONLY (no `scripts/` source) + the carved-out `test-fixtures/manifest.txt` → **`project-only`** (clean Check-36 via C0's manifest carve-out). v11-surface → **manifest regen required + non-empty** (§G). This DATA half lands the project-side `baseRef` + `bgIsolation` key mentions AND the `permissions.deny` recipe token the EXTENDED Guard-A′ asserts in C8b. **Coder PREFLIGHT:** grep-confirm the project file mentions `baseRef` AND `bgIsolation` AND the `permissions.deny` recipe token.

### C8b — P3 Guard-A′ OPTIONAL-FEATURES presence-check, BOTH surfaces (GUARD; pack-only)
- `scripts/validate-pack.py` — NEW **Guard A′** (Check 54 — ships ONCE, here; decision 7): OPTIONAL-FEATURES presence-check asserting **BOTH** surfaces (`pack-ops/OPTIONAL-FEATURES.md` from C5 + `project-template/docs/pack/OPTIONAL-FEATURES.md` from C8a) mention **`baseRef` AND `bgIsolation` AND the `permissions.deny` recipe token** (design §11.5 gate (b), §13.1a + the **user-approved §18.4 Guard-A′ EXTENSION**). **§18 sharpen (user-approved EXTENSION — measure-then-bound):** the design's §18.4 originally framed the `permissions.deny`-token assertion as an optional P3-architect call, but the **USER APPROVED extending Guard-A′ on 2026-06-14 (see `backlog/BD-197.md` Note 14)**, so it is BINDING here — and the reconciled design §18.4/§18.R/§13.1a now MANDATE it (design↔plan agree). Guard-A′ now asserts THREE tokens per file: `baseRef`, `bgIsolation`, AND the `permissions.deny` recipe token (the in-session backstop the user wants kept from silently vanishing). **Asserted tokens sized to EXACTLY what C5/C8a author** (measure-then-bound; the prose `isolation` param is NOT folded into the bounded check — §13.1a). The C8b coder RE-MEASURES at commit-time the exact recipe-token string C5/C8a landed (`rg -c 'permissions\.deny'` or the precise recipe heading both authored) and sizes the third asserted token to that — NOT a broader pattern. GREEN on arrival: by C8b BOTH OPTIONAL-FEATURES files carry all three tokens (pack from C5, project from C8a).
- New per-check test `scripts/tests/test-validate-pack-check-54.sh` — **author → RUN locally (quote exit 0) → wire into `validate-pack.yml` `tests` job → re-run the full battery, SAME commit** (decision 2). The test MUST cover all THREE tokens (`baseRef`, `bgIsolation`, `permissions.deny` recipe) on BOTH surfaces — a positive case (all three present in both → PASS) AND a negative case (the recipe token absent from one file → FAIL), so the extension is enforced not merely declared (enumerate-encoding-surfaces). Record **wall-time vs budget** in the IMPL-REPORT (decision 4 — six single-file `rg -c` reads [3 tokens × 2 files]; trivial).

C8b touches `scripts/` ONLY → **`pack-only`** (clean Check-36). v11-surface (scripts/) → **manifest regen RUN; stage ONLY if diff non-empty (expected EMPTY — S-2, same as C6b)** (§G).

**P3-launcher (UC-secondary, NEW-FORK-1 — RESOLVED = gate-then-probe-then-degrade).** The `agent-run.sh --worktree` launcher is a PROBE-GATED sub-deliverable inside **C7a** (project `agent-run.sh`, project-only — NOT a `scripts/` edit) and documented in **C8a** (project OPTIONAL-FEATURES): the coder runs the cwd-scoping probe (does `claude --agent` in a worktree keep git scoped there — #55708 class) at commit-time; PASS ⇒ ship the `--worktree` flag (project); FAIL ⇒ document the manual procedure only. **Launcher HEAD-basing is PROVEN settings-independent** (`git worktree add --detach <path> HEAD` bases at parent HEAD deterministically — Correction pass / FACT-1), so only the cwd-scoping leak remains to probe. It does NOT block UC-1 (C1–C5). Surfaced as a user-visible outcome in the IMPL-REPORT, not a blocker.

---

## C. Dependency graph + green-per-commit proof

```
C0 (Check-36 manifest carve-out, ENABLING) ─► C1 (P2 docs) ─► C2 (P2 trinity+skills) ─► C3 (pack RW/RO + Guard-B) ─► C4 (pack merge-back + hardening + backstop)
                                                                              │
                                              C5 (pack OPTIONAL-FEATURES + Guard-A + Guard-C[if standalone])
                                                                              │
   C6a (project RW/RO DATA, project-only) ─► C6b (Guard-B(project) GUARD, pack-only)
                                                                              │
   C7a (project merge-back + hardening DATA, project-only) ─► C7b (Guard-C project ext GUARD, pack-only [may drop if folded])
                                                                              │
   C8a (project OPTIONAL-FEATURES DATA, project-only) ─► C8b (Guard-A′ both-surfaces GUARD, pack-only)
```
**C0 enables the split:** the carve-out (design §17) lands first so the carve-out-aware Check 36 evaluates every later `project-only` DATA half. **Data-first within each split pair (C6a→C6b, C7a→C7b, C8a→C8b):** the `project-only` DATA half lands first; the `pack-only` GUARD half lands second and is green on arrival. Guard-A′ ships ONCE in C8b (decision 7), not C5.

**Why no commit is red (the load-bearing guard-ordering):**
- **C0** ADDS only a filter to Check 36's offender comprehensions — it cannot make any existing or future commit RED: a true cross-surface offender is still flagged (design §17.5(a), live-verified EB-5: `[<project>, scripts/validate-pack.py, manifest]` → offenders `["scripts/validate-pack.py"]`), and the manifest cannot smuggle content (a hand-edited manifest is caught by `build.sh --verify`, design §17.5(b)). C0 is `pack-only` (validate-pack.py + its test only) with an EMPTY manifest diff (no client-fixture change). validate-pack stays green on arrival (the updated Check-36 test passes; the full battery re-run is clean).
- **C1/C2** add NO new guard; they only EDIT existing content. validate-pack stays green (it has no worktree-prohibition guard yet). The prohibition removal cannot turn anything red because Guard-A does not exist until C5.
- **C3** ships Guard-B(pack) AND the Class-column data it checks in the SAME commit (pack RW/RO is a single pack-self surface — no surface split needed) → the guard is green on arrival (measure-then-bound: the guard asserts the post-edit state present in the same diff).
- **C4** edits prose/rationale/agents only; adds the mechanical backstop. No new validate-pack guard whose data is absent. Trinity `agents-never-commit` parallel edit keeps trinity-parity checks green.
- **C5** ships Guard-A (prohibition-stays-removed) — green because C2 already removed the prohibition and C1 dispositioned the doc carriers; the allowlist is sized to the commit-time-measured LEAVE set with the **NARROW self-exception** (decision 1): the validator self-skips itself (`entry.name == "validate-pack.py"`) + allowlists ONLY the new check-53 test + the measured BD-197 doc carriers (§F EE-2). **Guard-A′ does NOT ship in C5** (decision 7 — it ships in C8b). Guard-C, if standalone here, is green because C2/C4 enumerated the verbs on every surface it checks.
- **C6a** (project-only) lands the RW/RO data; **C6b** (pack-only) ships Guard-B(project) AFTER → green on arrival (the data is already committed in C6a). Each half is single-surface, keyword-exclusive.
- **C7a** (project-only) lands the merge-back + hardening data + the probe-gated launcher; **C7b** (pack-only) extends/ships Guard-C for the project verb surface AFTER → green on arrival. If the coder folded Guard-C, C7b may be EMPTY and dropped (decision 8 / §J3). Each half single-surface.
- **C8a** (project-only) authors the project OPTIONAL-FEATURES section (project keys); **C8b** (pack-only) ships Guard-A′ asserting BOTH surfaces AFTER → green on arrival because the pack keys are in C5 and the project keys are in C8a (both already committed). Guard-A′ ships ONCE, here (decision 7).

**Green-per-commit, all 12 commits (the load-bearing decisions):** (0) **C0 carve-out lands FIRST** (design §17) — the carve-out-aware Check 36 then evaluates every later `project-only` DATA half, so the staged manifest is no longer a `project-only` offender (the re-review's BLOCKER B-1 / M-1 RESOLVED). (1) **data-first split** — every `pack-only` GUARD half (C6b/C7b/C8b) lands AFTER its `project-only` DATA half (C6a/C7a/C8a), so a guard never lands before the data it asserts (no transient RED). (2) **Guard-A′ ships ONCE in C8b** (decision 7) — never in C5 asserting both surfaces (which would have RED'd C5 onward, project keys absent until C8a). C5's pack-side key presence is verified by the coder's PREFLIGHT grep, not a cross-surface guard. (3) **Guard-A NARROW self-exception** (decision 1 — distinct from the re-review's B-1; this is the plan-review's narrow self-skip decision) — C5's prohibition guard does not self-RED on the validator source or its own check-53 test. These four together make the battery green at every commit boundary AND keep every commit keyword-exclusive (Check-36 clean).

---

## D. Verification strategy per commit (FULL CI battery — name every script)

Per `verify-full-ci-suite`: a green `validate-pack.py` is NOT a green commit. EVERY coder PREFLIGHT and EVERY reviewer pass runs EVERY script wired in `.github/workflows/validate-pack.yml`, quoting each EXIT status. Sampling is the defect. **Run-before-wire mandate (decision 2 / M-3): every commit that ADDS a new check (C3 Check-52, C5 Check-53 [+56 if standalone], C6b Check-55, C7b Guard-C ext, C8b Check-54) MUST, in the SAME commit: (1) author the new per-check test → (2) RUN it locally and QUOTE its exit 0 in the IMPL-REPORT → (3) wire it into `.github/workflows/validate-pack.yml` `tests` job → (4) re-run the FULL battery.** A brand-new test is not in the prior battery, so the coder MUST run its own new test before wiring it; never defer the run to a later commit. **C0 is the EXCEPTION to run-before-wire ceremony but NOT to the run obligation:** C0 UPDATES the ALREADY-WIRED `scripts/tests/test-validate-pack-checks-36-37-38.sh` (no new test to wire), so the coder runs the UPDATED test (quote exit 0 on its 6 new NC-cases) + re-runs the FULL battery in the same commit — no yml wiring delta needed for C0. The complete wired set (measured from the yml at HEAD `ae3d932`):

**`validate` job (2):**
- `python3 scripts/validate-pack.py`
- `PACK_VALIDATE_DEEP=1 python3 scripts/validate-pack.py`

**`tests` job (every script, in yml order):**
`scripts/test-detect.sh`; `scripts/tests/tracker-provider-test.sh`, `tracker-config-test.sh`, `tracker-init-test.sh`, `tracker-agent-read-test.sh`, `tracker-migrate-forward-test.sh`, `tracker-migrate-reverse-test.sh`, `tracker-migrate-roundtrip-test.sh`, `test-tracker-phase-task.sh`, `test-tracker-links.sh`, `test-tracker-cycle-check.sh`, `tracker-errors-test.sh`, `tracker-config-schema-test.sh`, `recommendation-state-schema-test.sh`, `test-per-entry.sh`, `test-validate-pack-checks-32-33-34.sh`, `test-validate-pack-checks-36-37-38.sh`, `test-validate-pack-check-39.sh`, `test-validate-pack-check-40.sh`, `test-validate-pack-check-41.sh`, `test-validate-pack-check-18.sh`, `test-validate-pack-check-16.sh`, `test-validate-pack-check-19.sh`, `test-validate-pack-check-42.sh`, `test-validate-pack-check-43.sh`, `test-validate-pack-check-44.sh`, `test-validate-pack-check-45.sh`, `test-validate-pack-check-46.sh`, `test-validate-pack-check-removed-doc-advisory.sh`, `test-validate-pack-check-49-field-faithfulness.sh`, `test-validate-pack-check-50-codec-single-source.sh`, `test-validate-pack-check-51-flip-block.sh`, `tracker-deferral-gate-test.sh`, `tracker-bd129-gh-repo-test.sh`, `tracker-bd130-doctor-wired-test.sh`, `tracker-bd132-race-test.sh`, `tracker-bd133-header-preservation-test.sh`, `tracker-bd134-close-retry-test.sh`, `recommendation-test.sh`, `pack-help-test.sh`, `test-customization-preserve.sh`, `test-init-project.sh`, `test-migrate-v10-to-v11.sh`, `test-migrate-v10-to-v11-dry-run.sh`, `test-migrate-v10-to-v11-gates.sh`, `test-migrate-v10-to-v11-decompose.sh`; `scripts/test-migrator-core.sh`, `scripts/test-migrator-manifest.sh`, `scripts/test-migrator-capability-translation.sh`; `test-fixtures/build.sh --all --clean` then `git checkout HEAD -- test-fixtures/manifest.txt` then `test-fixtures/build.sh --verify`; `scripts/tests/test-v11-realistic-ot.sh`; `scripts/test-migrator-skills.sh`; `scripts/test-persona-contracts.sh`; `scripts/tests/template-translations-test.sh`, `template-version-test.sh`, `test-issue-forms.sh`. PLUS the NEW per-check tests this BD adds (Check 52 [C3], 53 [C5], 55 [C6b], 54 [C8b], and Check 56 / Guard-C-ext [C5 or C7b — coder's fold-vs-standalone call, decision 8]).

**Per-commit emphasis (which tests are MOST likely to catch this BD's regressions — but run ALL regardless):**
- C0: `test-validate-pack-checks-36-37-38.sh` (the UPDATED Check-36 test with NC-1..NC-6 — RUN + quote exit 0 same commit); the FULL battery (the carve-out touches the offender comprehensions every `pack-only`/`project-only`-claiming test exercises); `test-v11-realistic-ot.sh` (validator output pins). Manual: PREFLIGHT the projected predicate (`[<project>, manifest]` → empty; `[<project>, scripts/validate-pack.py, manifest]` → `[scripts/validate-pack.py]`).
- C1/C2: `test-v11-realistic-ot.sh` (pins validator OUTPUT/banners — the BD-203/BD-214 trap); `test-validate-pack-check-*` for any check asserting CLAUDE.md/skill content; `template-translations-test.sh` (trinity/skill parity).
- C3 / C6a / C6b: the NEW Check-52 (C3) / Check-55 (C6b) per-check tests (author+run+wire SAME commit); `template-translations-test.sh` (agent-file parity ×3 CLIs); `test-issue-forms.sh` if any form references agents. (C6a is project DATA — emphasis on `template-translations-test.sh` + agent-file parity; C6b is the GUARD — emphasis on the new Check-55 test.)
- C4 / C7a / C7b: `test-v11-realistic-ot.sh` (banner pins); `pack-help-test.sh` (if /pack-help describes the feature); the trinity-parity checks inside validate-pack (agents-never-commit bullet ×3 [C4]; project trinity No-destructive ×3 [C7a]); the Guard-C ext test (C7b, if standalone).
- C5 / C8a / C8b: NEW Check-53 test (C5) / Check-54 test (C8b) — author+run+wire SAME commit; `test-v11-realistic-ot.sh`. (C8a is project DATA — `template-translations-test.sh`; C8b is the GUARD — the new Check-54 test.)

**Manual checks per commit:** (a) grep audit — Guard-A's prohibition-only matcher returns ONLY the commit-time-measured allowlist INCLUDING the self-skipped validator + the new check-53 test (decision 1; C5 + every later commit); (b) dangling-ref token absent from active non-process tree (C1 onward — the 3 measured EXCISE targets gone); (c) `git diff --name-only` matches the claimed Check-36 keyword on EVERY commit — verify each split half is SINGLE-surface (C6a/C7a/C8a touch ONLY `project-template/`; C6b/C7b/C8b touch ONLY `scripts/`); (d) trinity parity manual diff for any trinity edit (C2 Claude-only exemption preserved — NOT propagated to root AGENTS/GEMINI; C4 agents-never-commit + C7a project No-destructive parallel edits present); (e) the per-CLI carve-out is ZERO across all 3 pack-coder files after C4 (decision 3 — `grep -c 'checkout -- <path>'` = 0 in `.claude`/`.codex`/`.gemini`).

---

## E. New CI guards — measure-then-bound steps the coder runs

For EACH guard, the coder executes the 5-step contract AGAINST THE LIVE TREE before authoring, and bounds runtime (battery runs validate-pack 202× — re-measured at `05ad61b` `grep -rcE 'validate-pack\.py' scripts/tests/*.sh | awk -F: '{s+=$2} END{print s}'` = 202 [§F EE-1; was 186 at the 2nd pass]; a heavy per-invocation check compounds into hours).

**C0 — Check 36 manifest carve-out (design §17; measure-then-bound, NOT a new check):**
1. **Measure 1 — the current Check 36 logic** (live-confirmed): `_PROJECT_SIDE_PATH_PREFIXES` at `:4126`; `_is_project_side_path` at `:4250`; offender branches `pack_only` `:4313` / `project_only` `:4323` / `pack_chat_only` `:4333` (the last UNCHANGED).
2. **Measure 2 — the FULL forced-co-variant set** (the question measure-then-bound demands): which pack-side path(s) does a `project-template/`-only CONTENT change FORCE into the same commit? Live measurement (§F EE-10): `bash test-fixtures/build.sh --all --clean` → `git status --porcelain` over all five v11-surface dirs = EMPTY (manifest regenerates byte-identical; every built fixture dir is gitignored — design §17.3 EB-2/EB-3). The rule names EXACTLY one artifact: `test-fixtures/manifest.txt`.
3. **Size the carve-out to EXACTLY `{test-fixtures/manifest.txt}`** — a `frozenset` of one path, NOT a `test-fixtures/` prefix (a prefix would wrongly exempt the static `v11-trinity-marker-prepped/` snapshot + the recipe `build.sh`/`README.md`, which are real pack-side content that SHOULD count toward scope — design §17.4).
4. **VERIFY not weakened** (design §17.5 / EB-5, live-reproducible): the projected `project_only` predicate over `["project-template/docs/pack/PM-CHAT.md","test-fixtures/manifest.txt"]` → offenders `[]` (split feasible); over `[...,"scripts/validate-pack.py","test-fixtures/manifest.txt"]` → offenders `["scripts/validate-pack.py"]` (real cross-surface offender STILL caught). No content-smuggling channel: a hand-edited manifest is independently caught by `build.sh --verify`.
5. **Runtime (ci-check-runtime-compounding):** ONE `frozenset` membership test per offender candidate — O(1), on an already-materialized path-set; no new subprocess, no whole-tree scan; negligible across the 202 validate-pack invocations.

**Guard A — Check 53 (prohibition-stays-removed, design §13.1) — with the NARROW self-exception (decision 1 / plan-review BLOCKER B-1):**
1. Measure: `rg -l --hidden --no-ignore 'no worktree isolation|Do not pass .*isolation.*worktree' -g '!.git' -g '!test-fixtures'` against the post-C2/post-C1 tree. **Live at this HEAD this matcher returns 23 files** (§F EE-2) — the matcher hits files that QUOTE the regex (the validator source, the new test, the BD-197 docs), NOT only prohibition prose. `scripts/` IS in scope, so `scripts/validate-pack.py` (once Guard-A lands) and `scripts/tests/test-validate-pack-check-53.sh` SELF-MATCH. **This 23-file count + the KEEP set below are MEASURED AT C5 COMMIT-TIME (illustrative as-of-now; the BD-197-process/review-doc set grows with every pass — this plan, each PLAN-adversarial review, any future review keeps matching). The C5 coder RE-MEASURES live at commit-time and allowlists EVERY then-existing BD-197-process/review doc that quotes the regex, plus the validator self-skip + the single check-53 test. Do NOT anchor on this static number.**
2. Categorize KEEP vs STRIP (post-P2 STRIP must be empty — C1/C2 stripped them). **KEEP includes the SELF-MATCH set, handled by the NARROW exception (decision 1, Check-51 precedent):** (i) **self-skip the validator** via `entry.name == "validate-pack.py"` (precedent `scripts/validate-pack.py:2169`); (ii) **allowlist ONLY the single new test file `scripts/tests/test-validate-pack-check-53.sh`** — NARROW, NOT the whole `scripts/tests/` dir; (iii) **allowlist the commit-time-MEASURED BD-197 doc carriers** (per-file KEEP, §F EE-2 — as-of-now this includes all three `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL{,-2,-3}.md` reviews; the C5 coder re-measures and allowlists every then-existing BD-197-process/review doc that matches — NOT a static list). **The narrow exception stays NARROW: validator self-skip + ONLY the single check-53 test file + the measured doc carriers — do NOT widen to the whole `scripts/tests/` dir.**
3. Fix-recipe: any STRIP remaining ⇒ the C1/C2 edit was incomplete; fix before C5.
4. Allowlist = the measured KEEP set ONLY — NEVER the bare `baseRef`/`bgIsolation` key names (G-1/G-2); NEVER the whole `scripts/tests/` dir (NARROW per decision 1). Size to KEEP, no broader.
5. Verify post-fix: the matcher returns exactly the allowlist. **PREFLIGHT (decision 1):** the C5 coder RE-MEASURES the matcher LIVE at commit-time (do NOT trust this plan's as-of-now 23-file count) and asserts the matcher returns EXACTLY the then-measured legitimate set INCLUDING the just-authored validator (self-skipped) + the new check-53 test (allowlisted) + every then-existing BD-197-process/review doc that quotes the regex (the set keeps growing).
- **Runtime:** scope to active tree (exclude archive/test-fixtures), SINGLE whole-tree `rg`, per-check runtime guard (time the check; WARN/FAIL on budget overrun per ci-check-runtime-compounding). No subprocess-per-entry. **Record the measured wall-time vs a stated budget in the IMPL-REPORT (decision 4).**

**Guard A′ — Check 54 (OPTIONAL-FEATURES presence — `baseRef` + `bgIsolation` + the `permissions.deny` recipe token; design §13.1a + the user-approved §18.4 EXTENSION) — ships ONCE in C8b (decision 7):**
1. Measure baseline: `rg -c 'bgIsolation|baseRef|permissions\.deny' pack-ops/OPTIONAL-FEATURES.md project-template/docs/pack/OPTIONAL-FEATURES.md` (re-measured at HEAD `05ad61b` = `baseRef`/`bgIsolation` 0/0 AND `permissions.deny` 0/0 in BOTH files — §F EE-4 + EE-12; all three tokens absent pre-C5/C8a).
2. Categorize: presence-check (positive), not absence — no STRIP set.
3. Assert BOTH files mention **`baseRef` AND `bgIsolation` AND the `permissions.deny` recipe token** post-P3 (the user-approved §18.4 EXTENSION). **Tokens sized to EXACTLY those THREE tokens** (measure-then-bound; the prose `isolation` param is NOT folded into the bounded check — design §13.1a; the third token = the exact `permissions.deny` recipe string C5/C8a author, RE-MEASURED at C8b commit-time, NOT a broad pattern). On the corrected mode model all three legitimately appear in each file (`baseRef`=required base; `bgIsolation`=background-session gate / BD-218 pointer; `permissions.deny` recipe=the documented-optional in-session mechanical backstop §18.2(ii)). Turns green only when BOTH surfaces are authored: pack in C5, project in C8a → **ship the guard in C8b** (the FIRST commit where both are present). Decision 7 — Guard-A′ ships ONCE, in C8b; NOT in C5.
4. Allowlist: N/A (positive check).
5. Verify: green in C8b (pack tokens from C5 + project tokens from C8a all committed — all three per file). **C5/C8a verify their own-side tokens by coder PREFLIGHT grep, NOT by this guard (decision 7).**
- **Runtime:** six single-file `rg -c` reads (3 tokens × 2 files); trivial. **Record wall-time in the IMPL-REPORT (decision 4).**

**Guard B — Check 52 (pack) / Check 55 (project) (RW/RO consistency, design §13.2):**
1. Measure: pack = 5 agents (1 RW + 4 RO); project = 16 agents (2 RW + 14 RO; READONLY_AGENTS=14). (Re-confirmed live: §F evidence.)
2. Categorize: each agent's PROSE mandate header vs roster/array cell.
3. Fix-recipe: any mismatch ⇒ the C3 (pack) / C6a (project) header/roster edit is wrong; fix before shipping.
4. Bound: bind to PROSE header, NEVER `tools:` (pack-reviewer Write,Edit-yet-RO; Gemini no `tools:` field).
5. Verify: set-equality holds.
- **Runtime:** single-pass file reads, no per-agent subprocess storm; per-check runtime guard. **Record wall-time vs budget in the IMPL-REPORT (decision 4).** (Guard-B(pack)=C3; Guard-B(project)=C6b.)

**Guard C — folded OR standalone Check 56 (verb-enumeration parity, design §13.3; decision 8 — CODER'S CALL):** measure current enumeration coverage across trinity/commit-discipline×3/pack-coder×3/project Hard rules/agent-run.sh flags; size the assertion to the measured surface set. **PREFER folding verb-enumeration parity into an EXISTING parity check** (fewer checks); author a standalone Check 56 ONLY if folding over-complicates. **Record the fold-vs-standalone rationale in the IMPL-REPORT (decision 8 / §J3).** If folded into an existing pack-side check, that fold lands in C5 (the pack guard commit) and the C7b project-surface extension may be EMPTY → dropped (→ 11 commits; record in IMPL-REPORT). If standalone, ship Check 56 + its test in C5 (pack surfaces) and extend it for project surfaces in C7b. **Record wall-time vs budget in the IMPL-REPORT (decision 4).**

---

## F. Empirical-Evidence Blocks (the state-claims this sequencing depends on)

**THIRD UPDATE PASS re-measurement (2026-06-14, HEAD `05ad61b`).** All blocks re-run live at the ADVANCED HEAD. **Material DELTAS vs the SECOND pass §F (HEAD moved `ae3d932` → `05ad61b`; C0/C1/C2/C3 LANDED):** (1) **battery 186 → 202** (EE-1 — C0's NC-cases + the Check-52 test added ~16 validate-pack invocations); (2) **highest Check 51 → 52** (EE-6 — Guard-B pack Check 52 landed in C3; the remaining new checks are 53 [C5], 55 [C6b], 54 [C8b], 56 [Guard-C if standalone]); (3) **prohibition matcher 23 → 25** (EE-2 — the C1/C2 STRIP files [`CLAUDE.md`, `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md`, `PLAN-SKILL-DIMENSIONS.md`, `ARCHITECTURE-BD-196-S1`] are now GONE [C1/C2 stripped the prohibition prose, exactly as planned], and SIX new BD-197 process docs from the C0–C3 commits now QUOTE the regex [`IMPL-REPORT-BD-197-C1.md`, `IMPL-REPORT-BD-197-C2.md`, `IMPL-REPORT-BD-197-FIX-SHOULDS.md`, `PACK-REVIEW-BD-197-C1.md`, `PACK-REVIEW-BD-197-C2.md`, `PACK-REVIEW-BD-197-FIX-SHOULDS.md`] — the re-measure-at-commit mandate realized); (4) **C0 carve-out + C3 Class column CONFIRMED landed** (EE-10/EE-5). Unchanged: OPTIONAL-FEATURES `baseRef`/`bgIsolation` 0/0 (EE-4), project=16 / READONLY_AGENTS=14 (EE-5), `git checkout -- <path>` carve-out 3 sites (EE-8 — C4 pending), manifest clean (EE-9), validate-pack exit 0 (EE-6). **NEW THIRD-pass blocks:** **EE-11** measures the §18 in-session-spawn ABSENCE on BOTH surfaces (PACK-CHAT.md + PM-CHAT.md); **EE-12** measures the `permissions.deny` recipe baseline (0/0) for the EXTENDED Guard-A′. (The SECOND pass had folded the §17 carve-out as EE-10 = `{test-fixtures/manifest.txt}`; that holds, C0 landed it.)

**EE-1 — HEAD + battery invocation count.**
- Command: `git rev-parse HEAD`; `git rev-parse --abbrev-ref HEAD`; `grep -rcE 'validate-pack\.py' scripts/tests/*.sh | awk -F: '{s+=$2} END{print s}'`.
- Output: `05ad61b4ca86a743d27230ec86a8252a55c064d4`; `v11-dev`; `202`.
- HEAD/date: `05ad61b` / 2026-06-14.
- Interpretation: HEAD ADVANCED `ae3d932` → `05ad61b` (C0/C1/C2/C3 landed); battery now invokes validate-pack **202×** (was 186 — C0's NC-cases + the Check-52 test added ~16). The runtime-compounding budget is set against 202.
- Conclusion: SUPPORTED — re-measurement was necessary; counts updated (battery 186 → 202).

**EE-2 — prohibition-only matcher (Guard-A allowlist sizing).**
- Command: `rg -l --hidden --no-ignore 'no worktree isolation|Do not pass .*isolation.*worktree' -g '!.git' -g '!test-fixtures'`.
- **THIRD-pass re-measurement (HEAD `05ad61b`, 2026-06-14): the matcher now returns 25 files.** Delta vs the SECOND-pass 23: the four C1/C2 STRIP files (`CLAUDE.md`, `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md`, `PLAN-SKILL-DIMENSIONS.md`, `ARCHITECTURE-BD-196-S1-CORPUS-CLASSIFICATION.md`) NO LONGER MATCH (C1/C2 stripped the prohibition prose, as planned), and SIX new BD-197 process docs from the C0–C3 commits now QUOTE the regex and JOINED the carrier set: `IMPL-REPORT-BD-197-C1.md`, `IMPL-REPORT-BD-197-C2.md`, `IMPL-REPORT-BD-197-FIX-SHOULDS.md`, `PACK-REVIEW-BD-197-C1.md`, `PACK-REVIEW-BD-197-C2.md`, `PACK-REVIEW-BD-197-FIX-SHOULDS.md`. Net: 23 − 4 STRIP-gone + 6 new = 25 (9 archive + 16 active-KEEP; STRIP now empty). This is the re-measure-at-commit mandate realized — the C5 coder RE-MEASURES live and sizes Guard-A's allowlist to the then-current set (every BD-197 IMPL/PACK-REVIEW/design/research/plan doc that quotes the regex + the validator self-skip + the single check-53 test). **Do NOT trust either the 23 or the 25 static count.** The SECOND-pass per-file enumeration (at `ae3d932`) is preserved below for history.
- Output (SECOND-pass, **23 files** at `ae3d932` — re-measured live, untracked included): 9 archive (`maintenance-docs/archive/v11/*`: ARCHITECTURE-CLEANUP-BATCH-19B-V2, IMPLEMENTATION-REPORT-BD-180-FIX-2, IMPLEMENTATION-REPORT-BD-181-PRECONDITION, IMPLEMENTATION-REPORT-CLEANUP-BATCH-19B-19b-1, PACK-REVIEW-BD-181-PRECONDITION, PACK-REVIEW-CLEANUP-BATCH-19B-19b-1, PACK-REVIEW-V10.1-BACKPORT, PLAN-BD-175-PHASE-5, PLAN-CLEANUP-BATCH-19B) + 14 active. **Per-file `rg -c` match counts (live, this HEAD):** `CLAUDE.md`=1, `ARCHITECTURE-BD-196-S1-CORPUS-CLASSIFICATION.md`=1, `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md`=1, `PLAN-SKILL-DIMENSIONS.md`=2 (these 4 are STRIP); `ARCHITECTURE-BD-197-WORKTREE-ISOLATION-RECONCILED.md`=6, `ARCHITECTURE-BD-197-ADVERSARIAL-REVIEW-2.md`=6, `RESEARCH-BD-197-P1-WORKTREE-ISOLATION.md`=5, `ARCHITECTURE-BD-197-WORKTREE-ISOLATION.md`=3, `RESEARCH-BD-197-AGENT-PERMISSION-INVENTORY.md`=1, `IMPLEMENTATION-REPORT-BD-196-C9.md`=1, `PLAN-BD-197-WORKTREE-ISOLATION.md`=4 (this plan), `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL.md`=8 (the 1st plan review), `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-2.md`=3 (the 2nd/re-review), `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-3.md`=3 (the 3rd/final plan review — NEW since the prior plan; the re-measure-at-commit mandate realized) (these 10 are KEEP). **EXCLUDED (0 matches — confirmed `rg -c`):** `backlog/BD-197.md`=0, `ARCHITECTURE-BD-197-ADVERSARIAL-REVIEW.md` (1st adversarial)=0, `IMPL-REPORT-BD-214-C5a-BD197.md`=0.
- HEAD/date (SECOND-pass enumeration above): `ae3d932` / 2026-06-13. THIRD-pass count: `05ad61b` / 2026-06-14 = 25.
- Interpretation (SECOND-pass): **23 = 9 archive + 4 STRIP + 10 active-KEEP.** THIRD-pass: **25 = 9 archive + 16 active-KEEP (STRIP now empty post-C1/C2).** The prior plan measured 21, then 22 (the 2nd review); the latest +1 is `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-3.md` (3 matches), which now exists and QUOTES the regex — exactly as the re-measure-at-commit mandate predicts (the carrier set grows each review pass). **STRIP** (C1/C2 rewrite the prohibition prose → non-matching post-P2): `CLAUDE.md`, `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md`, `PLAN-SKILL-DIMENSIONS.md`, `ARCHITECTURE-BD-196-S1` (if the row label is stripped). **KEEP allowlist (per-file, MEASURED AT C5 COMMIT-TIME — illustrative as-of-now, NOT a static list; decision 1 / re-review S-1):** `IMPLEMENTATION-REPORT-BD-196-C9.md` (history) + the BD-197-process prohibition-prose carriers (RECONCILED, 2nd adversarial design-review, RESEARCH-P1, first design, RESEARCH-AGENT-PERMISSION, this PLAN doc, **all three PLAN-adversarial reviews — the 1st, the 2nd/re-review, AND the 3rd/final review**) + 9 archive — PLUS the **NARROW self-exception** (decision 1 / plan-review B-1): the validator self-skip (`entry.name == "validate-pack.py"`) + the single new `scripts/tests/test-validate-pack-check-53.sh` (NOT the whole `scripts/tests/` dir). The C5 coder RE-MEASURES live and allowlists every then-existing BD-197-process/review doc that matches (any future review keeps matching). **`backlog/BD-197.md` and the 1st DESIGN adversarial = 0 matches → EXCLUDED from the allowlist.**
- Conclusion: SUPPORTED (THIRD-pass: 25 at `05ad61b`; SECOND-pass: 23 at `ae3d932`) — Guard-A's allowlist sizes to the COMMIT-TIME-MEASURED per-file KEEP set (now incl. all three PLAN-adversarial reviews — the 1st, 2nd, and 3rd) + the narrow validator/test self-exception; **re-measure at C5 commit-time — this 23-file count + KEEP set are illustrative as-of-now, NOT static** (the process-artifact set grows with each pass — THIS very plan + every review pass + any future review will keep matching; the coder re-measures and sizes the allowlist to the then-current set — never trusting this static enumeration). The narrow exception stays NARROW (validator self-skip + ONLY the check-53 test + the measured doc carriers — not the whole `scripts/tests/` dir).

**EE-3 — dangling-ref token (C1 excision targets — RE-MEASURED at `05ad61b`; C1 LANDED).**
- Command: `rg -l 'feedback_worktree_isolation_broken_from_v11_clone' -g '!.git'` (THIRD-pass live re-measure at `05ad61b`).
- Output (THIRD-pass, HEAD `05ad61b`, 2026-06-14 — 15 total = 4 archive + 11 active, ALL BD-197-process/allowlist; ZERO active non-process EXCISE targets remain): the 11 active hits are `ARCHITECTURE-BD-197-WORKTREE-ISOLATION-RECONCILED.md`, `ARCHITECTURE-BD-197-WORKTREE-ISOLATION.md`, `ARCHITECTURE-BD-197-ADVERSARIAL-REVIEW.md`, `ARCHITECTURE-BD-197-ADVERSARIAL-REVIEW-2.md`, `RESEARCH-BD-197-P1-WORKTREE-ISOLATION.md`, `PLAN-BD-197-WORKTREE-ISOLATION.md` (this plan), `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-2.md`, `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-3.md`, `IMPL-REPORT-BD-197-C1.md`, `PACK-REVIEW-BD-197-C1.md`, `backlog/BD-197.md` (all BD-197-process / allowlist LEAVE) + 4 archive (D4 LEAVE: `PLAN-CLEANUP-BATCH-19B.md`, `IMPLEMENTATION-REPORT-CLEANUP-BATCH-19B-19b-6.md`, `ARCHITECTURE-CLEANUP-BATCH-19B.md`, `ARCHITECTURE-CLEANUP-BATCH-19B-V2.md`). The 3 SECOND-pass active non-process EXCISE targets are now ABSENT (per-file `rg -l | grep -c`: `ARCHITECTURE-SKILL-AGENT-MAINTAINABILITY.md` = 0, `RESEARCH-19C-G-ITEMS-VERIFICATIONS.md` = 0, `RESEARCH-CLAUDE-REPOS-SURVEY.md` = 0).
- HEAD/date: `05ad61b` / 2026-06-14. (SECOND-pass enumeration was at `ae3d932`: 8 process/allowlist + 3 active non-process EXCISE targets, all 3 since excised by C1.)
- Interpretation: **C1 (landed, 220b6c7) excised all 3 active non-process carriers** (design §11.3) — at `05ad61b` the dangling-ref matcher returns ONLY BD-197-process/allowlist carriers (the live LEAVE set, which grows per pass per the re-measure-at-commit mandate) + the 4 archive (D4 LEAVE). The dangling-ref matcher is SEPARATE from the prohibition matcher (this token = the deleted-memory-pointer, not prohibition prose).
- Conclusion: SUPPORTED — **0 active non-process EXCISE targets remain at `05ad61b`** (C1 done); the SECOND-pass "3 EXCISE targets" figure is SUPERSEDED by this measured 0. No C1 action remains; this block is now an audit-honest record of the post-C1 state.

**EE-4 — OPTIONAL-FEATURES presence baseline (Guard-A′).**
- Command: `rg -c 'bgIsolation|baseRef' pack-ops/OPTIONAL-FEATURES.md project-template/docs/pack/OPTIONAL-FEATURES.md`.
- Output: exit 1 (no matches in either file; both files exist — measured `## Claude Code — Agent Teams` present in pack file).
- HEAD/date: `ae3d932` / 2026-06-13.
- Interpretation: `baseRef`/`bgIsolation` absent pre-P3 in both surfaces (re-confirmed 0/0 at `05ad61b`); Guard-A′ (Check 54, EXTENDED) ships in **C8b** and is green only after the pack tokens (C5) + the project tokens (C8a) are both committed (decision 7). **The `permissions.deny` recipe baseline is measured in NEW block EE-12 (also 0/0) — the EXTENDED Guard-A′ adds it as a third asserted token.**
- Conclusion: SUPPORTED — matches design §11.5 gate (b) baseline (0/0); the THIRD token baseline is EE-12.

**EE-5 — RW/RO counts + agent inventory.**
- Command: `ls .claude/agents .codex/agents .gemini/agents`; `ls project-template/.claude/agents/*.md | wc -l`; `grep -nA20 'READONLY_AGENTS=' project-template/agent-run.sh`.
- Output: pack = 5 agents ×3 CLIs (pack-architect/coder/docs-researcher/planner/reviewer); project = 16 agents; READONLY_AGENTS = exactly 14 entries (architect, reviewer, planner, tester, docs-researcher, grpc-schema, auditor, auditor-architecture/-code/-docs/-security/-tests/-ui/-ops); coder+repo-ops absent ⇒ 2 RW.
- HEAD/date: `ae3d932` / 2026-06-13.
- Interpretation: pack 1RW+4RO; project 2RW+14RO — matches design §4.3 / §13.2 + inventory §1.
- Conclusion: SUPPORTED — Guard-B sized to 5 / 16.

**EE-6 — highest existing Check number + validate-pack green baseline.**
- Command: `grep -oE 'Check [0-9]+' scripts/validate-pack.py | grep -oE '[0-9]+' | sort -n | tail -1`; `python3 scripts/validate-pack.py; echo $?`.
- Output: `52`; `PASSED — all checks clean`, exit `0`.
- HEAD/date: `05ad61b` / 2026-06-14.
- Interpretation: **Check 52 (Guard-B pack) is ALREADY LANDED (C3, at this HEAD)**; the REMAINING new guards become **53 (Guard-A, C5), 55 (Guard-B project, C6b), 54 (Guard-A′ EXTENDED, C8b), 56 (Guard-C if standalone — C5 pack + C7b project ext; decision 8)**. Note Check 54 (Guard-A′) lands LAST commit-wise (C8b) despite a lower number than 55 — number ≠ commit order; numbers are assigned at authoring, Guard-A′ keeps 54 from the design. validate-pack is green at the THIRD-pass baseline (no pre-existing red to fight).
- Conclusion: SUPPORTED — highest Check 51 → 52 (Guard-B pack landed in C3); remaining numbering + clean baseline confirmed.

**EE-7 — trinity surface placement (P2 = pack-CLAUDE-only).**
- Command: `grep -nc 'worktree' AGENTS.md GEMINI.md project-template/CLAUDE.md project-template/AGENTS.md project-template/GEMINI.md`; `grep -n 'Spawn all sub-agents with no worktree' CLAUDE.md`; `grep -n '### Sub-agent behavior' CLAUDE.md AGENTS.md GEMINI.md`.
- Output: root AGENTS.md=0, GEMINI.md=0; project trinity all 0; prohibition at `CLAUDE.md:325`; `### Sub-agent behavior (Claude-only)` only in CLAUDE.md (line 323).
- HEAD/date: `ae3d932` / 2026-06-13.
- Interpretation: the prohibition lives ONLY in pack-root CLAUDE.md's Claude-only/trinity-exempt subsection; NOT in root AGENTS/GEMINI, NOT in any project trinity, NOT in any client surface. P2 is pack-only and Claude-only; the C2 coder must NOT propagate the bullet to AGENTS/GEMINI (preserves the exemption). P3 client work is net-new/additive.
- Conclusion: SUPPORTED — confirms design §11 (pack-side ONLY) + §0/§8 (client carries no prohibition).

**EE-8 — pack-coder checkout carve-out (C4 excision target — located by SYMBOL/string; line numbers illustrative).**
- Command: `grep -c 'checkout -- <path>' .claude/agents/pack-coder.md .codex/agents/pack-coder.toml .gemini/agents/pack-coder.md` (the C4 coder re-locates each site by the `git checkout -- <path>` carve-out STRING at commit-time — line numbers DRIFT and are NOT used as anchors).
- Output: present in all 3 (one match each) — `.claude/agents/pack-coder.md` (carve-out clause `git checkout -- <path>` in the pack-coder read-only-git carve-out sentence), `.codex/agents/pack-coder.toml` (the same clause embedded MID-SENTENCE in the longer Codex read-only-verbs prose block — a different phrasing from the `.md` files), `.gemini/agents/pack-coder.md` (carve-out clause in the Gemini phrasing). Total = 3. **Illustrative line numbers as last measured (drift expected; NOT anchors): `.claude` ~`:37`/`:47`, `.codex.toml` ~`:21`/`:23`, `.gemini` ~`:39`/`:49`** — the coder ignores these and re-locates by the string.
- HEAD/date: `05ad61b` / 2026-06-14 (count re-confirmed live this pass; the SECOND-pass enumeration cited `.claude:37`/`.codex.toml:21`/`.gemini:39` at `ae3d932` — superseded by the by-symbol locator, line numbers drift).
- Interpretation: the stale `git checkout -- <path>` carve-out exists in pack-coder ×3; C4 DROPS it (design §5.3, G-4) — **per-CLI audience-correct removal located by SYMBOL/string, NOT a byte-identical exact-string excision and NOT line-number-anchored (decision 3 / S-1):** the Codex `.toml` embeds the clause mid-sentence in different prose from the `.claude`/`.gemini` `.md` phrasing. The C4 coder PREFLIGHT re-locates each site by the carve-out STRING and confirms `grep -c 'checkout -- <path>'` = 0 in ALL three files post-edit. Enumerate-encoding-surfaces lock-step.
- Conclusion: SUPPORTED — 3 excision sites confirmed by STRING (line numbers illustrative, re-located at commit-time).

**EE-9 — manifest baseline clean.**
- Command: `git status --short test-fixtures/manifest.txt`.
- Output: empty (clean).
- HEAD/date: `ae3d932` / 2026-06-13.
- Interpretation: manifest is in sync at baseline; each v11-surface commit regenerates + stages it (§G).
- Conclusion: SUPPORTED.

**EE-10 — Check 36 carve-out forced-co-variant set (the §17 carve-out sizing) + Check 36 loci.**
- Command: `cp test-fixtures/manifest.txt /tmp/...` → `bash test-fixtures/build.sh --all --clean` → `git status --porcelain test-fixtures/ scripts/ project-template/ pack-ops/ supporting-docs/` → restore manifest (no git-state change); PLUS `grep -n` for the Check 36 loci.
- Output: build exit `0`; post-build `git status --porcelain` over all five v11-surface dirs = **EMPTY** (manifest regenerated byte-identical at this HEAD; every built fixture dir is gitignored). Check 36 loci confirmed live: `_PROJECT_SIDE_PATH_PREFIXES` at `:4126` = `("project-template/", "supporting-docs/")`; `_is_project_side_path` at `:4250`; offender branches `pack_only` `:4313`, `project_only` `:4323`, `pack_chat_only` `:4333`; `check_commit_scope_honesty` at `:4264`; `_commit_paths` at `:4194`; Check-51 self-skip precedent `entry.name == "validate-pack.py"` at `:2169`; `_SANCTIONED_PACK_SIDE_SHIPPED` at `:4429` = `{scripts/lib/detect.sh, scripts/pack-help.sh}`. `_SCOPE_NEUTRAL_GENERATED_PATHS`/`_is_scope_neutral_generated` ABSENT (the carve-out is C0's to author). The Check-36 test `scripts/tests/test-validate-pack-checks-36-37-38.sh` exists + is wired; its `required` symbol list is at `:43-53` (asserts `_is_project_side_path` at `:49`); the `assert_pside` unit-test block is at `:158-172`.
- HEAD/date: `ae3d932` / 2026-06-13.
- Interpretation: a `project-template/`-only content edit FORCES exactly ONE pack-side tracked path into the same commit — `test-fixtures/manifest.txt` (design §17.3 EB-2/EB-3 reproduced; the fixture dirs are gitignored, the static `v11-trinity-marker-prepped/` snapshot has no build recipe). So the carve-out is sized to the frozenset `{test-fixtures/manifest.txt}` — measure-then-bound, no broader. All Check-36 loci the C0 coder edits are confirmed present at the design's stated lines (which the coder re-locates by symbol, as line numbers drift).
- Conclusion: SUPPORTED — the §17 carve-out target is exactly `test-fixtures/manifest.txt`; C0's edits land at confirmed loci.

**EE-11 — §18 in-session-spawn ABSENCE, BOTH surfaces (the C4/C7a deliverables).**
- Command: `grep -ciE "isolation|/tmp|handoff|merge-back|run_in_background" pack-ops/PACK-CHAT.md` and `grep -ciE "isolation|worktree|/tmp|handoff|merge-back" project-template/docs/pack/PM-CHAT.md`; plus `grep -niE "agent-run\.sh|Task tool|Agent tool" project-template/docs/pack/PM-CHAT.md`.
- Output (HEAD `05ad61b`, 2026-06-14): PACK-CHAT.md = **1** (the lone hit is "deliberate handoff at Batch" — benign batch-handoff prose, §11.2 LEAVE; ZERO `isolation`/`/tmp`/merge-back/`run_in_background` spawn-contract hits). PM-CHAT.md = **0** for `isolation`/`worktree`/`/tmp`/handoff/merge-back; the only spawn-mechanism hits are the three `agent-run.sh` flag-profile blocks (`:455` Claude, `:484` Codex, `:501` coder) + incidental "in-session" prose (`:568`). `## Permission profiles` at `:397` (the §18.1 in-session-subsection home).
- HEAD/date: `05ad61b` / 2026-06-14.
- Interpretation: reproduces design §18.E EB-A/EB-B at the new HEAD — NEITHER surface documents the in-session Agent/Task-tool `isolation:"worktree"` + RW/RO + background + `/tmp`-patch contract. PACK-CHAT.md is the C4 deliverable (§18.1 pack-side); PM-CHAT.md is the C7a deliverable (§18.1 project-side, CLOSING the under-scope). Both still pending (C4/C7a unlanded).
- Conclusion: SUPPORTED — the in-session spawn instruction is genuinely absent on both surfaces; C4 (PACK-CHAT.md) + C7a (PM-CHAT.md) ADD it; this is a CONTENT clarification of already-planned commits, not a new commit.

**EE-12 — `permissions.deny` recipe baseline (the EXTENDED Guard-A′ third token).**
- Command: `grep -c 'permissions\.deny' pack-ops/OPTIONAL-FEATURES.md project-template/docs/pack/OPTIONAL-FEATURES.md`.
- Output (HEAD `05ad61b`, 2026-06-14): pack `0`, project `0` (both files exist; neither mentions the recipe yet).
- HEAD/date: `05ad61b` / 2026-06-14.
- Interpretation: the `permissions.deny` recipe token is absent in BOTH OPTIONAL-FEATURES files pre-C5/C8a — the baseline for the user-approved §18.4 Guard-A′ EXTENSION. C5 authors the pack-side recipe, C8a the project-side recipe; the EXTENDED Guard-A′ (Check 54, C8b) then asserts all three tokens (`baseRef`, `bgIsolation`, `permissions.deny`) in both files and is green on arrival. The C8b coder RE-MEASURES the exact recipe-token string C5/C8a land and sizes the third asserted token to it (measure-then-bound).
- Conclusion: SUPPORTED — `permissions.deny` 0/0 baseline; the EXTENDED Guard-A′ keeps the documented backstop recipe from silently vanishing.

---

## G. Manifest regen flags (per commit)

v11-surface = diff touches `project-template/`, `scripts/`, `pack-ops/`, or `supporting-docs/`. Pack-root trinity (CLAUDE/AGENTS/GEMINI) is base-case EXEMPT; `.claude/`/`.codex/`/`.gemini/` skill+agent dirs are NOT in the four v11-surface dirs.

| Commit | Surface | Touches v11-surface dir? | Manifest regen? |
|---|---|---|---|
| C0 | pack-self | YES (`scripts/`) | RUN build; stage ONLY if diff non-empty (expected EMPTY — validate-pack.py + its test do not project into client fixtures) |
| C1 | pack-self | YES (`pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md`) | YES — `bash test-fixtures/build.sh --all --clean`, stage if diff non-empty |
| C2 | pack-self | NO (only pack-root CLAUDE.md [exempt] + `.claude/.codex/.gemini/` skills+agents) | RUN build to confirm; stage ONLY if diff non-empty (expected empty) |
| C3 | pack-self | YES (`pack-ops/PACK-AGENTS.md` + `scripts/`) | YES |
| C4 | pack-self | YES (`pack-ops/` + possibly `scripts/` IF the backstop edits an existing script — §J4 gate) | YES |
| C5 | pack-self | YES (`pack-ops/` + `scripts/`) | YES |
| C6a | client | YES (`project-template/`) | YES |
| C6b | pack-self | YES (`scripts/`) | RUN build; stage ONLY if diff non-empty (expected EMPTY — S-2: validate-pack.py + test do not project into fixtures) |
| C7a | client | YES (`project-template/`) | YES |
| C7b | pack-self | YES (`scripts/`) IF the commit lands (may be dropped if Guard-C folded) | RUN build; stage ONLY if diff non-empty (expected EMPTY — S-2) |
| C8a | client | YES (`project-template/`) | YES |
| C8b | pack-self | YES (`scripts/`) | RUN build; stage ONLY if diff non-empty (expected EMPTY — S-2) |

Every commit RUNS `bash test-fixtures/build.sh --all --clean` and stages `test-fixtures/manifest.txt` in the SAME commit ONLY when the manifest diff is non-empty (manifest-regen memory). **Where the manifest STAGE actually fires (N-1 precision):** only the THREE project-content DATA halves — **C6a / C7a / C8a** — change a fixture SHA (project-template/ projects into the v11 fixtures), so only those three STAGE a non-empty manifest. **The pack-side commits (C0, C1, C2, C3, C4, C5, C6b, C7b, C8b) have an EXPECTED-EMPTY manifest diff** — validate-pack.py, its tests, the trinity/skill/agent edits, the pack-ops doc edits, and the pack OPTIONAL-FEATURES section do NOT project into the client fixtures (C1's `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md` is a pack-ops doc, not a fixture-projected file; C3/C5 touch `pack-ops/PACK-AGENTS.md`/`OPTIONAL-FEATURES.md`, also not fixture-projected). The RUN obligation fires on every v11-surface-touching commit (all 12), but the STAGE is a no-op on the 9 pack-side commits and a real stage on exactly C6a/C7a/C8a. The carved manifest (via C0) is what lets those three stage it under a `project-only` keyword. Each split half is single-surface, so its manifest handling is scoped to that surface's diff.

---

## H. Enumerate-encoding-surfaces (per surface changed → its validators + tests + CI refs + cross-ref docs, lockstep)

| Surface changed | Validator(s) | TEST file(s) | CI workflow ref | Cross-ref docs |
|---|---|---|---|---|
| Check 36 carve-out (C0): `scripts/validate-pack.py` `_SCOPE_NEUTRAL_GENERATED_PATHS` + `_is_scope_neutral_generated` + both offender branches | Check 36 (`check_commit_scope_honesty`) — IS the validator | `test-validate-pack-checks-36-37-38.sh` (UPDATED in lockstep — Group 0 `required` += `_is_scope_neutral_generated`; Group 1 NC-1..NC-6) — ALREADY WIRED (run + re-run battery, SAME commit) | validate-pack.yml validate + tests jobs (no wiring delta — test pre-wired) | design §17; the C6a/C7a/C8a `project-only` keyword + the `regenerate-manifest-v11-surface` rule |
| CLAUDE.md worktree bullet (C2) | trinity-parity checks in validate-pack (Claude-only exemption — no AGENTS/GEMINI parity required) | `test-v11-realistic-ot.sh` (banner/output pins); any `test-validate-pack-check-*` asserting CLAUDE.md memory content | validate-pack.yml validate job | OPTIONAL-FEATURES pointer (C5); EXECUTION-PLAN §D + PLAN-SKILL-DIMENSIONS §4.8 updated-in-place (C1) |
| commit-discipline SKILL ×3 (C2/C4) | `template-translations-test.sh` (skill parity ×3 CLIs) | `template-translations-test.sh`; `test-v11-realistic-ot.sh` | tests job | implementation-report skill (regime-aware, C2) |
| PACK-CHAT.md in-session orchestrator spawn + merge-back (C4 — §18.1 pack) | (no dedicated guard — prose contract) | `test-v11-realistic-ot.sh` (banner/output pins); `pack-help-test.sh` if /pack-help describes it | validate/tests jobs | design §18.1 pack-side + §4.1 + §6; PACK-AGENTS `Class` SSOT (C3); pack-coder RW-emit step (C4) |
| PM-CHAT.md in-session Agent-tool spawn instruction (C7a — §18.1 project; CLOSES under-scope) | (no dedicated guard — prose contract; client-native) | `template-translations-test.sh`; `test-v11-realistic-ot.sh` | tests job | design §18.1 project-side; PM-CHAT `## Permission profiles` RW/RO SSOT (D2); `agent-run.sh` launcher (SECONDARY path); project coder RW-emit step (C7a) |
| pack agent files ×3 (C3/C4) | Guard-B Check 52; `template-translations-test.sh` | `test-validate-pack-check-52.sh` (NEW, run-before-wire C3); `template-translations-test.sh` | tests job | PACK-AGENTS roster Class column; pack-coder per-CLI carve-out removed ×3 (C4) |
| PACK-AGENTS Class column (C3) | Guard-B Check 52 | `test-validate-pack-check-52.sh` | tests job | per-agent prose headers ×15 |
| trinity agents-never-commit verb enum (C4) | trinity-parity/bijection checks; Guard-C | `test-v11-realistic-ot.sh`; Guard-C per-check test (if standalone) | validate job | PACK-MEMORY-RATIONALE; .spawn-rule-manifest; commit-discipline ×3 |
| pack OPTIONAL-FEATURES section + `permissions.deny` recipe (C5) | (PREFLIGHT grep of `baseRef`+`bgIsolation`+`permissions.deny`, NOT a guard in C5 — EXTENDED Guard-A′ asserts all three in C8b) | `pack-help-test.sh` if /pack-help describes it; `test-v11-realistic-ot.sh` | tests job | corrected mode model (design §3/§9); §18.2(ii) backstop recipe + RESEARCH F2/F3; CLAUDE.md pointer; BD-218 pointer |
| Guard-A flip-block (C5) | (is the validator; NARROW self-exception decision 1) | `test-validate-pack-check-53.sh` (NEW, run-before-wire C5) | tests job | design §13.1; the validator self-skip + check-53-test allowlist |
| Guard-C verb-parity (C5 fold/standalone; C7b project ext) | folded into existing parity check OR standalone Check 56 (decision 8) | Guard-C per-check test (if standalone, run-before-wire); `template-translations-test.sh` | tests/validate | design §13.3; commit-discipline ×3; pack-coder ×3; project Hard rules; agent-run.sh flags |
| project RW/RO data: agent files ×3 + PM-CHAT + agent-run.sh READONLY_AGENTS (C6a) | (asserted by Guard-B Check 55 in C6b) | `template-translations-test.sh`; `test-init-project.sh`/`pack-help-test.sh` if they exercise agent-run.sh | tests job | PM-CHAT permission table |
| Guard-B(project) Check 55 (C6b) | (is the validator) | `test-validate-pack-check-55.sh` (NEW, run-before-wire C6b) | tests job | design §13.2; the C6a RW/RO data |
| project merge-back + git-hardening + --disallowedTools data (C7a) | (project verb data asserted by Guard-C ext in C7b) | `template-translations-test.sh`; `test-v11-realistic-ot.sh` (banner pins) | tests/validate | project CLAUDE/AGENTS/GEMINI No-destructive ×3; project coder/skill |
| Guard-C project ext (C7b, if standalone) | standalone Check 56 ext OR EMPTY/dropped if folded | Guard-C ext test (run-before-wire) | tests job | the C7a project verb data |
| project OPTIONAL-FEATURES section + `permissions.deny` recipe (C8a) | (asserted by EXTENDED Guard-A′ Check 54 in C8b — all 3 tokens) | `template-translations-test.sh`; `test-v11-realistic-ot.sh` | tests job | pack OPTIONAL-FEATURES (separate artifact, client-native); corrected mode model; §18.2(ii) backstop recipe |
| EXTENDED Guard-A′ Check 54 both surfaces (C8b) | (is the validator; tokens=`baseRef`+`bgIsolation`+`permissions.deny` recipe — user-approved §18.4 extension) | `test-validate-pack-check-54.sh` (NEW, run-before-wire C8b — positive [all 3 present both files] + negative [recipe absent → FAIL]) | tests job | design §13.1a + §18.4 extension; C5 pack tokens + C8a project tokens (all 3 each) |

The NEW per-check tests (52 [C3], 53 [C5], 55 [C6b], 54 [C8b], + Guard-C 56 [C5/C7b if standalone]) MUST be authored + RUN locally (quote exit 0) + added to `.github/workflows/validate-pack.yml` `tests` job in the SAME commit as the check (run-before-wire, decision 2; asymmetric coverage = audit gap). **C0 is the exception that proves the lockstep rule:** Check 36's test ALREADY exists + is wired, so C0 has NO wiring delta — but the validator edit + the test UPDATE (the 6 NC-cases) STILL land in the SAME commit (enumerate-encoding-surfaces: a Check-36 logic change with no matching test change would be an audit gap), and the updated test is RUN + the full battery re-run before the IMPL-REPORT.

---

## I. Coder spawn map + rules-in-force per commit

Each commit = one fresh `pack-coder` spawn (fresh per commit — agent-team lifecycle), C0 FIRST, followed by the BOUNDED review/fix cycle: coder → reviewer → Pack-Chat triage → fix-coder → reviewer-2 (max 2 fix pairs + 3 reviewers per commit; architect escalation if still dirty after the final reviewer). Pack Chat does NO fixes itself. All agents READ-ONLY git; merge-back of any RW work is via /tmp patch → orchestrator applies (agents-never-commit RETAINED for ALL agents incl. RW). **No platform safety net (Correction pass / FACT-4):** a non-isolated background subagent can write the main checkout FREELY → every RW agent MUST be spawned with `isolation:"worktree"`, and the agents-never-commit + destructive-verb ban is LOAD-BEARING, not advisory. The RW merge-back is the orchestrator applying the agent's `/tmp` patch (`git apply` is orchestrator-only; the agent runs only read-only `git diff > /tmp/...`).

| Commit | Coder spawn | Rules-in-force the prompt MUST enumerate (inline, literal) |
|---|---|---|
| C0 | pack-coder (Check 36 manifest carve-out; `pack-only`) | ci-guard-measure-then-bound (**carve-out sized to EXACTLY `frozenset({"test-fixtures/manifest.txt"})` — design §17.3 measured; a frozenset of ONE path, NOT a `test-fixtures/` prefix; PREFLIGHT the projected `project_only` predicate: `[<project>,manifest]`→empty, `[<project>,scripts/validate-pack.py,manifest]`→`[scripts/validate-pack.py]` per §17.5 EB-5**); enumerate-encoding-surfaces (validator + `test-validate-pack-checks-36-37-38.sh` change IN LOCKSTEP, same commit — Group 0 `required`+= `_is_scope_neutral_generated`, Group 1 NC-1..NC-6); ci-check-runtime-compounding (O(1) set-membership; negligible across 202 invocations); verify-full-ci-suite (RUN the updated Check-36 test + re-run FULL battery — existing wired test, no run-before-wire wiring delta); empirical-evidence (re-confirm the forced-co-variant set + Check-36 loci at commit-time); commit-subject-keyword-token-trap (`pack-only` — validate-pack.py + test only; manifest diff EMPTY → no manifest stage); regenerate-manifest-v11-surface (RUN build, expected-empty diff → stage nothing); rules-applied-block; preflight-stop-means-stop; agents-never-commit |
| C1 | pack-coder (P2 docs; `pack-only`) | empirical-evidence (re-audit at commit-time); ci-guard-measure-then-bound (dangling-ref + prohibition matchers; 3 measured EXCISE targets); **update-in-place EXECUTION-PLAN §D + PLAN-SKILL-DIMENSIONS §4.8 to the enabled model, NOT annotate-superseded (decision 9)**; bd-pack-only-operational-rule; pack-project-separation; regenerate-manifest-v11-surface; rules-applied-block; preflight-stop-means-stop; agents-never-commit |
| C2 | pack-coder (trinity + skills; `pack-only`) | trinity rule + Claude-only exemption (do NOT propagate to root AGENTS/GEMINI); **CORRECTED mode model in the enable bullet — `isolation:"worktree"` param=TRIGGER, `baseRef:"head"`=base [`fresh`=origin/main consequence], `bgIsolation`=background-session/BD-218; NO 9-cell matrix / NO bgIsolation-as-trigger**; PACK-CHAT rule-change propagation procedure; edit-in-place-not-rewrite; verify-full-ci-suite (test-v11-realistic-ot banner pins); cross-cli-reference-normalization; enumerate-encoding-surfaces; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C3 | pack-coder (pack RW/RO + Guard-B; `pack-only`) | ci-guard-measure-then-bound + ci-check-runtime-compounding (Guard-B sized to 5; bind to PROSE header not tools; **record wall-time vs budget — decision 4**); **run-before-wire Check-52 test (author→run→wire→re-run battery, SAME commit — decision 2)**; enumerate-encoding-surfaces (Class column + 15 headers + Check-52 test + yml wiring); empirical-evidence; verify-full-ci-suite; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C4 | pack-coder (pack in-session spawn + merge-back + hardening + backstop; `pack-only`) | **§18.1 IN-SESSION ORCHESTRATOR SPAWN PROCEDURE in PACK-CHAT.md (the C4 CONTENT clarification, measured ABSENT §F EE-11): Pack Chat spawns RW agents IN-SESSION via the Agent tool with `isolation:"worktree"`, RO agents IN-PLACE, in the BACKGROUND, keyed off the PACK-AGENTS `Class` SSOT, naming the `/tmp` handoff dir — NOT only the post-return apply**; trinity rule (agents-never-commit parallel ×3 — this bullet is NOT in the Claude-only subsection); PACK-CHAT propagation order; **in-session backstop per §18.2 (pack ships NO PreToolUse hook, NO settings file): (i) shipped PROSE deny-list [the trinity/skill/agent/rationale edits this C4 makes] + (ii) documented-OPTIONAL user `permissions.deny` [authored in C5 OPTIONAL-FEATURES, NOT shipped] + (iii) SECONDARY user PreToolUse hook [fails-open, NOT shipped]; the launcher `--disallowedTools` layer is project-side C7a, not pack-side**; verb-precise backstop (deny apply, not diff; confirm `diff > file` not tripped); **DROP `git checkout -- <path>` carve-out ×3 PER-CLI audience-correct, NOT byte-identical [decision 3 / 1st-review S-1]; PREFLIGHT grep=0 in all 3 PLUS M-2 PROSE-COHERENCE read-back (Codex `.toml:21` embeds it mid-sentence — confirm no orphan `(except )` fragment + `git checkout` stays in the deny list with NO exception per §5.1; grep=0 is necessary-not-sufficient — re-review M-2)**; **NEW-pack-side-script GATE [decision 5 / S-3]: per §18.2 the F1–F5 model needs NO new pack-side file (J4 = NO); but if any backstop element WOULD need a NEW pack-side FILE, STOP — architect+user sign-off BEFORE coding (Check-47 frozen allowlist); coder may NOT author it on its own authority**; no-platform-safety-net (RW must be spawned isolated; verb-ban load-bearing); enumerate-encoding-surfaces; cross-cli-normalization; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C5 | pack-coder (pack OPTIONAL-FEATURES + Guard-A + Guard-C; `pack-only`) | ci-guard-measure-then-bound (Guard-A allowlist = measured KEEP only, NEVER baseRef/bgIsolation key names — G-1/G-2; **NARROW self-exception decision 1: validator self-skip `entry.name=="validate-pack.py"` + ONLY the new check-53 test, NOT whole `scripts/tests/`; PREFLIGHT asserts the matcher returns exactly the measured legitimate set incl. the just-authored validator+test**); ci-check-runtime-compounding (single rg, runtime guard, scoped to active tree, ×202 budget; **wall-time vs budget — decision 4**); **CORRECTED OPTIONAL-FEATURES content (isolation param TRIGGER, baseRef:head required+fresh consequence, bgIsolation=bg-session/BD-218; NO 9-cell matrix)**; **§18.2(ii) ADD the documented-optional `permissions.deny` recipe (in-session mechanical backstop; §5.1 verbs as scoped Bash rules, VERB-PRECISE deny `apply` not `diff`; session-scoped + sub-agent-inherited + deny-first per RESEARCH F2; PreToolUse hook SECONDARY/fails-open; NO shipped settings file, NO new pack-side file — J4=NO); PREFLIGHT grep `baseRef`+`bgIsolation`+`permissions.deny` recipe token present (pack-side half of the EXTENDED Guard-A′)**; **VERIFY-AT-IMPLEMENTATION (confirm `isolation:"worktree"` empirically once C2 lifts the prohibition; record in IMPL-REPORT)**; **Guard-C fold-vs-standalone CODER'S CALL (decision 8) — prefer fold, record rationale; Guard-A′ does NOT ship here (decision 7)**; **run-before-wire Check-53 [+56 if standalone] test (decision 2)**; empirical-evidence; enumerate-encoding-surfaces; verify-full-ci-suite; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C6a | pack-coder (project RW/RO DATA; `project-only`) | pack-project-separation (PM-CHAT/PM-Chat, client-native, NOT byte-copy of pack); bd-pack-only-operational-rule (ZERO pack-self refs in project-template/ — no BD-NNN/maintenance-docs/pack-*/Pack-Chat/pack-ops); client-ref-delete-or-forward-look; enumerate-encoding-surfaces; **single-surface: `project-template/` ONLY (no `scripts/`) → `project-only` clean**; verify-full-ci-suite; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C6b | pack-coder (Guard-B(project) GUARD; `pack-only`) | ci-guard-measure-then-bound + ci-check-runtime-compounding (Guard-B project sized to 16; Gemini no tools:; bind to PROSE header; **wall-time vs budget — decision 4**); **run-before-wire Check-55 test (decision 2)**; enumerate-encoding-surfaces (Check-55 test + yml); **single-surface: `scripts/` ONLY → `pack-only` clean; GREEN on arrival (C6a data committed)**; empirical-evidence; verify-full-ci-suite; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C7a | pack-coder (project in-session spawn + merge-back + hardening DATA; `project-only`) | **§18.1 IN-SESSION AGENT/TASK-TOOL SPAWN INSTRUCTION in PM-CHAT.md, CLIENT-NATIVE, the 5 elements (two paths [in-session PRIMARY + `agent-run.sh` launcher SECONDARY]; `isolation:"worktree"` for RW only; background; `/tmp`-patch merge-back; conflict/degradation at project homes) — CLOSES the project under-scope (measured ABSENT §F EE-11); ZERO pack-self refs**; pack-project-separation; bd-pack-only-operational-rule; trinity rule (project trinity parallel ×3, cross-cli-normalized — preserve GEMINI approval-mode line); verb-precise --disallowedTools (deny apply not diff); shipped-settings reconciliation (do NOT add worktree key to shipped settings.json; ban enforced by Hard rule + agent-run.sh flags not settings); no-platform-safety-net (RW must be spawned isolated); NEW-FORK-1 cwd-scoping probe (gate the `--worktree` launcher; HEAD-basing PROVEN; degrade to manual if probe fails); **single-surface: `project-template/` ONLY → `project-only` clean**; enumerate-encoding-surfaces; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C7b | pack-coder (Guard-C project ext GUARD; `pack-only`; MAY be dropped if folded) | ci-guard-measure-then-bound + runtime-compounding; **Guard-C project-surface extension IF standalone; if folded into an existing check (decision 8) this commit is EMPTY → DROPPED (→11 commits; record in IMPL-REPORT)**; run-before-wire (decision 2); **wall-time vs budget — decision 4**; **single-surface: `scripts/` ONLY → `pack-only` clean; GREEN on arrival (C7a data committed)**; enumerate-encoding-surfaces; verify-full-ci-suite; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C8a | pack-coder (project OPTIONAL-FEATURES DATA; `project-only`) | **§18.2(ii) ADD the `permissions.deny` recipe authored CLIENT-NATIVE (separate artifact, NOT a byte-copy of the pack version; client paths/audience; PreToolUse hook SECONDARY; NO shipped settings file, NO new pack-side file — J4=NO)**; pack-project-separation (client-authored, separate artifact, not a fallback for the pack version); bd-pack-only-operational-rule (ZERO pack-self refs); **CORRECTED mode model in the client section (isolation param TRIGGER, baseRef:head required+fresh consequence, bgIsolation=bg-session/BD-218; NO 9-cell matrix)**; client-ref-delete-or-forward-look; NEW-FORK-1 launcher doc (probe-gated); **PREFLIGHT grep: `baseRef`+`bgIsolation`+`permissions.deny` recipe token present (EXTENDED Guard-A′ asserts all 3 in C8b)**; **single-surface: `project-template/` ONLY → `project-only` clean**; enumerate-encoding-surfaces; verify-full-ci-suite; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |
| C8b | pack-coder (EXTENDED Guard-A′ both-surfaces GUARD; `pack-only`) | ci-guard-measure-then-bound (**user-approved §18.4 EXTENSION — Guard-A′ tokens sized to EXACTLY THREE: `baseRef`+`bgIsolation`+the `permissions.deny` recipe token; the C8b coder RE-MEASURES at commit-time the exact recipe string C5/C8a authored and sizes the third token to it — NOT a broad pattern; the `isolation` param prose is NOT folded into the bounded check; test covers positive [all 3 in both files] + negative [recipe absent → FAIL]**); ci-check-runtime-compounding (two `rg -c`; **wall-time — decision 4**); **Guard-A′ ships ONCE here, both surfaces; GREEN on arrival (C5 pack keys + C8a project keys committed) — decision 7**; **run-before-wire Check-54 test (decision 2)**; **single-surface: `scripts/` ONLY → `pack-only` clean**; enumerate-encoding-surfaces; verify-full-ci-suite; regenerate-manifest; rules-applied-block; preflight; agents-never-commit |

---

## J. Decision ledger (the §17 carve-out + the 11 review-decisions + the re-review folds + the mode-correction) + the few remaining coder-call / user-visible items

**RESOLVED by the §17 carve-out + the 11 user-approved review-decisions + the re-review folds (folded into §A–§I — recorded here for traceability, NOT re-opened):**
- **J-resolved-0 (the manifest pincer — re-review BLOCKER B-1 / MUST M-1) — RESOLVED by the §17 carve-out (C0, the new FIRST commit).** The re-review proved the restored split was internally infeasible: a `project-only` DATA half (C6a/C7a/C8a) is FORCED to stage the regenerated `test-fixtures/manifest.txt` (a pack-side path), which Check 36 flags as a `project-only` offender → the commit could neither be `project-only` nor omit the manifest (`build.sh --verify` would go red). **C0** adds a one-path scope-neutral carve-out (design §17) treating `test-fixtures/manifest.txt` as scope-neutral in BOTH offender branches → a `project-only` content commit + the carved manifest passes Check 36 AND `build.sh --verify`. M-1's "J1 is unsatisfiable" is now FALSE: the exclusive `project-only` keyword IS achievable. (§A C0 / §B C0 / §C / §E carve-out / §F EE-10 / §G / §H / §I C0.)
- **J-resolved-1 (C6/C7 Check-36 framing) — RESOLVED by decision 6 (SPLIT) + the C0 carve-out.** The prior plan flagged C6/C7 as mixed-surface needing neutral framing. The user chose SPLIT: each mixed pair becomes a `project-only` DATA half + a `pack-only` GUARD half (C6a/C6b, C7a/C7b, C8a/C8b). Every commit carries an exclusive Check-36 keyword; no neutral commit. (§A.)
- **J-resolved-2 (Guard-A′ ship point) — RESOLVED by decision 7.** Guard-A′ ships ONCE in **C8b** (both OPTIONAL-FEATURES surfaces present by then). C5 verifies pack-side key presence by PREFLIGHT grep, not a cross-surface guard. (§C / §E / §I C5/C8b.)
- **J-resolved-5 (Guard-A self-match) — RESOLVED by decision 1.** Guard-A uses the Check-51 NARROW self-exception: validator self-skip + ONLY the new check-53 test (NOT the whole `scripts/tests/` dir) + the measured BD-197 doc carriers; PREFLIGHT asserts the matcher returns exactly the measured legitimate set. The matcher returns 23 live as-of-now (§F EE-2; +1 over the prior 22 = the 3rd/final review doc, S-1) — but the C5 coder re-measures at commit-time and sizes the allowlist to the then-current set (the count is illustrative, not static). (§E / §F EE-2 / §I C5.)
- **J-resolved-6 (run-before-wire) — RESOLVED by decision 2.** Every new-check commit authors → runs (quote exit 0) → wires → re-runs the full battery, SAME commit. (§D / §I C3/C5/C6b/C7b/C8b.)
- **J-resolved-7 (mode-model + VERIFY param) — RESOLVED by the Correction pass + decision (b).** The `isolation` param value is `"worktree"` (resolves the design's note-9 VERIFY); the C5 coder still confirms empirically. OPTIONAL-FEATURES sets `baseRef:"head"` (with `fresh`=origin/main consequence), documents the `isolation` param as the per-spawn trigger, and describes `bgIsolation` as the background-session gate with a BD-218 pointer. No 9-cell matrix / no bgIsolation-as-subagent-trigger anywhere. (§B C2/C5/C8a / §E / §I.)
- **J-resolved-8 (carve-out per-CLI) — RESOLVED by decision 3.** The `git checkout -- <path>` carve-out removal is per-CLI audience-correct, NOT a byte-identical excision; PREFLIGHT confirms zero carve-out in all 3 pack-coder files. (§B C2/C4 / §F EE-8 / §I C4.)
- **J-resolved-9 (superseded-docs update-in-place) — RESOLVED by decision 9.** EXECUTION-PLAN-V11.0 §D + PLAN-SKILL-DIMENSIONS §4.8 are UPDATED IN PLACE to the enabled model (NOT annotated-as-superseded) — which also keeps them out of Guard-A's allowlist. (§B C1 / §I C1.)
- **J-resolved-10 (runtime wall-time) — RESOLVED by decision 4.** Coders record each new check's measured wall-time vs a stated budget in the IMPL-REPORT (single whole-tree pass, no subprocess-per-entry, battery = 202 invocations [re-measured at `05ad61b`, §F EE-1; was 186 at the 2nd pass]). (§D / §E / §I.)

- **J-resolved-11 (re-review M-2 carve-out prose-coherence) — FOLDED into §B C4 + §I C4.** The `git checkout -- <path>` removal gate is now `grep -c == 0` PLUS a manual prose read-back (no orphan `(except )` fragment; `git checkout` stays denied with no exception per §5.1) — token-absence is necessary-not-sufficient.
- **J-resolved-12 (re-review S-1 Guard-A allowlist) — FOLDED into §F EE-2 + §K.** The Guard-A KEEP allowlist now names all THREE PLAN-adversarial reviews (`PACK-REVIEW-BD-197-PLAN-ADVERSARIAL.md` 1st, `-2.md` 2nd/re-review, and `-3.md` 3rd/final — the latter now matches at 3); the matcher returns 23 live as-of-now; the re-measure-at-C5 mandate is restated (the set keeps growing — the coder allowlists the then-current set).
- **J-resolved-13 (re-review S-2 guard-half manifest) — FOLDED into §B C6b/C7b/C8b + §G.** The guard halves' manifest diffs are expected-EMPTY (validate-pack.py does not project into client fixtures); the RUN obligation fires but the STAGE is a no-op.
- **J-resolved-14 (re-review N-1/N-2 count precision) — FOLDED into §G + §D.** §G now states the manifest STAGE fires on exactly C6a/C7a/C8a (the 3 project-content halves), all other commits expected-empty (N-1); the battery-vs-design-~155 delta is recorded (N-2; the 2nd-pass figure was 186, now re-measured to 202 at `05ad61b` per §F EE-1 — the runtime budget is set against 202).

- **J-resolved-15 (§18 in-session spawn symmetry + unified backstop + the user-approved Guard-A′ extension) — FOLDED (THIRD pass, 2026-06-14) into §A / §B C4/C5/C7a/C8a/C8b / §E / §F EE-11+EE-12 / §H / §I; NO new commit, sequence intact.** The architect's §18 scope-correction is content-sharpening of four already-planned commits + one guard extension: (a) **C4** PACK-CHAT.md gains the IN-SESSION orchestrator spawn procedure (RW via `isolation:"worktree"`, RO in-place, background, `/tmp` handoff) — not only post-return apply; (b) **C7a** PM-CHAT.md gains the symmetric in-session Agent/Task-tool spawn instruction authored CLIENT-NATIVE (5 elements), CLOSING the project under-scope (the prior plan documented the project in-session path only via the launcher); (c) **C5** pack OPTIONAL-FEATURES gains the documented-optional `permissions.deny` recipe (in-session mechanical backstop, RESEARCH F2/F3); (d) **C8a** project OPTIONAL-FEATURES gains the same recipe client-native; (e) **C8b** the user-approved Guard-A′ EXTENSION asserts the `permissions.deny` recipe token in BOTH OPTIONAL-FEATURES files in addition to `baseRef`+`bgIsolation` (measure-then-bound, sized to exactly what C5/C8a author, re-measured at commit-time). J4 stays NO new shipped pack-side file (Check-47 untouched — §18.2 EB-D). The mode-model is the CORRECTED two-independent-mechanisms model (subagent trigger = `isolation:"worktree"` param; `bgIsolation` = background-session gate → BD-218). HEAD re-measured `ae3d932` → `05ad61b` (C0/C1/C2/C3 landed; the §18 targets are all still-pending C4/C5/C7a/C8a/C8b).

**Standing GATES + CODER-CALL / user-visible items (the few that remain):**

**J1 — SPLIT structure (decision 6, STANDING constraint; re-review M-1 RESOLVED by C0).** Every commit MUST carry an exclusive CI-verified Check-36 keyword via the C0 carve-out + the data-first split (12 commits; possibly 11 if Guard-C folds — J3). No commit may be neutral-framed. **The re-review's M-1 objection ("J1 is unsatisfiable for the project DATA halves because the manifest forces a cross-prefix stage") is RESOLVED by C0** — the carve-out makes the manifest scope-neutral, so the exclusive `project-only` keyword IS achievable. This is a binding constraint on the coder + Pack Chat, not an open question.

**J2 — Guard-A′ in C8b (decision 7, STANDING; EXTENDED per user-approved §18.4).** Guard-A′ ships once, in C8b, asserting both surfaces (green on arrival). **It now asserts THREE tokens per file — `baseRef`, `bgIsolation`, AND the `permissions.deny` recipe token (MANDATED — user-approved 2026-06-14; see `backlog/BD-197.md` Note 14; reconciled design §18.4/§18.R/§13.1a)** — sized measure-then-bound to exactly what C5/C8a author (re-measured at C8b commit-time). Do not ship it in C5.

**J3 — Guard-C fold-vs-standalone (decision 8 — CODER'S CALL).** PREFER folding verb-enumeration parity into an existing parity check; author a standalone Check 56 ONLY if folding over-complicates. Record the fold-vs-standalone rationale in the IMPL-REPORT. **If folded, C7b is EMPTY and DROPPED → 11 commits** (the coder/Pack-Chat record this). Surfaced so the user knows a Check 56 + a C7b commit may or may not appear.

> **AS-BUILT RECONCILIATION (2026-06-14, BD-197 Note 15(a)/(b) — supersedes the contingent J3 / §A-§I "Guard-C fold / Check 56 ext / may-drop-to-11" framing throughout this plan).** Guard-C was realized as **TWO STANDALONE checks**, confirmed by `grep` of `scripts/validate-pack.py`: **Check 56** = Guard-C pack (`check_destructive_git_verb_parity`, shipped in C5 — the full §5.1 28-verb set across 10 pack surfaces) and **Check 57** = Guard-C project (`check_project_destructive_git_verb_parity`, shipped in C7b — an 8-verb intersection + a trinity-only catch-all across 52 project surfaces). The C7b coder chose standalone (NOT folded, NOT a Check-56 extension) because the pack and project surface families are structurally different (different canonical verb set + a trinity-only catch-all) — the decision-8 escape hatch. So **C7b was PRESENT, NOT dropped — the realized count is 12 commits (not 11)**; the "may-drop-to-11-if-folded" branch did NOT apply, and the project guard is **Check 57**, not a "Check 56 extension." The realized full check-number map: Guard-B-pack=Check 52 (C3), Guard-A=Check 53 (C5), Guard-A′=Check 54 (C8b), Guard-B-project=Check 55 (C6b), Guard-C-pack=Check 56 (C5), Guard-C-project=Check 57 (C7b). Wherever this plan says "Check 56 ext (C7b)," "Guard-C if standalone," "may drop to 11 commits," or "C7b may be dropped," read the as-built outcome here. See RECONCILED design §13.3 + `IMPL-REPORT-BD-197-RECONCILIATION.md`.

**J4 — Mechanical backstop NEW-pack-side-script GATE (decision 5 — HARD STOP, pre-coding).** Per design §18.2 (EB-D), the in-session backstop is "shipped PROSE deny-list + documented-OPTIONAL user `permissions.deny` recipe" — the pack ships NO PreToolUse hook and NO settings file, and the F1–F5 model needs NO new pack-side file (**J4 = NO**; Check-47 untouched). So C4's backstop is expressed by EDITING existing config / DOCUMENTING the optional user recipe (the trinity/skill/agent/rationale edits + the C5 OPTIONAL-FEATURES recipe + the project-side C7a `--disallowedTools` launcher flags) — the coder does it inline. The gate is RETAINED as a safety net: if any backstop element WOULD require a NEW pack-side FILE/SCRIPT, the coder MUST STOP — Pack Chat escalates to **architect + user sign-off** BEFORE the coder writes it (Check-47 `_SANCTIONED_PACK_SIDE_SHIPPED` is FROZEN to exactly `{scripts/lib/detect.sh, scripts/pack-help.sh}`). The coder may NOT author a new pack-side script on its own authority. Resolve at the C4 architect-check, NOT at commit-time by the coder.

**J5 — Probe outcomes the user should SEE (not silent coder choices).** (a) C5 records the empirical confirmation of `isolation:"worktree"` in the IMPL-REPORT (the param is established per FACT-5, but the coder confirms once the prohibition is lifted by C2). (b) The C7a/C8a NEW-FORK-1 cwd-scoping probe RESULT — does the `--worktree` launcher SHIP or DEGRADE to a manual procedure — is a user-visible C7a/C8a outcome (launcher HEAD-basing is PROVEN settings-independent per FACT-1; only cwd-scoping remains to probe). It does NOT block UC-1 (C1–C5).

---

## K. Out-of-scope issues surfaced (not silently fixed)

- **Codex/Gemini worktree support = BD-217 (v11.1).** P1 found both have EXISTS-BUT-IMMATURE worktree support. This plan keeps the trinity-exemption clean (C2 does NOT add the rule to root AGENTS/GEMINI; C5/C8a carry the Trinity-exempt note) but designs/implements NOTHING for Codex/Gemini isolation. Do not pull BD-217 work into this plan.
- **Background-session isolation (`worktree.bgIsolation` + EnterWorktree/ExitWorktree) = BD-218 (Deferred, v11.1).** The Correction pass established that subagent isolation (the `isolation:"worktree"` param × `baseRef`) and background-session isolation (`bgIsolation` + EnterWorktree) are TWO INDEPENDENT mechanisms. This plan ships ONLY the subagent axis (BD-197). `bgIsolation` is DOCUMENTED (in C5/C8a OPTIONAL-FEATURES) accurately as the background-session gate with a BD-218 pointer — it is NOT implemented or treated as a subagent control. Do not pull BD-218 work into this plan. (This is the only deferral; user-authorized 2026-06-14 per BD-218 + Note 11.) **The in-session mechanical backstop's HONEST LIMIT (RESEARCH F3) is NOT a deferral — it is a documented design property, all v11.0.** The `permissions.deny` recipe (layer ii) is USER-ACTIVATED: out-of-box (no user settings) the only active deny layers for in-session sub-agents are the shipped PROSE deny-list (layer i) + the agents-never-commit behavioral contract; the mechanical hard-deny exists and is reachable but the user must add it to THEIR settings.json (the pack ships NO settings file — BD-197 hard constraint). This is fully documented in C5/C8a OPTIONAL-FEATURES (both surfaces, client-native) and is NOT scoped out — only background-session isolation (BD-218) is deferred.
- **The PLAN doc itself + this plan's BD-197-process docs (incl. all THREE PLAN-adversarial reviews) will match the dangling-ref / prohibition matchers once committed (re-review S-1).** The C1/C5 coder MUST re-measure at commit-time and allowlist the then-current BD-197-process artifact set — which now includes `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL.md` (1st, 8 matches), `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-2.md` (2nd/re-review, 3 matches), AND `PACK-REVIEW-BD-197-PLAN-ADVERSARIAL-3.md` (3rd/final, 3 matches) as KEEP carriers (the design's re-measure mandate, §11/§11.5) — never trust this plan's static enumerations. Any FUTURE review doc that quotes the regex joins the set; the coder's allowlist is sized to the LIVE measurement, not this list.
- **HEAD ADVANCED `ae3d932` → `05ad61b` since the SECOND pass; THIS THIRD UPDATE PASS re-measured live.** C0/C1/C2/C3 LANDED — the prohibition matcher now returns **25** (was 23; the C1/C2 STRIP files are gone, 6 new C0–C3 IMPL/PACK-REVIEW process docs joined — §F EE-2), battery is **202** (was 186 — §F EE-1), highest Check is **52** (was 51 — Guard-B pack landed in C3, §F EE-6), `permissions.deny` baseline is 0/0 (NEW §F EE-12), and PACK-CHAT.md/PM-CHAT.md in-session spawn is ABSENT (NEW §F EE-11). The C5/C8a/C8b coders re-measure AGAIN at commit-time (the process-artifact set keeps growing; the `permissions.deny` recipe-token string is sized at C8b commit-time to what C5/C8a actually authored; never trust this plan's static enumerations).

---

## Rules-Applied Verification Block

**THIRD UPDATE PASS (2026-06-14) — the rules-in-force from the calling prompt.** This block supersedes the SECOND-pass block. The 11-lettered-section map (§A–§K, plus the attestation + this block) is INTACT and in unchanged order (verified `grep -n '^## '`: attestation, A, B, C, D, E, F, G, H, I, J, K, Rules-Applied). Folding §18 (in-session spawn symmetry + unified backstop) + the user-approved Guard-A′ extension is IN-SECTION content-sharpening (table-row + bullet edits in §A/§B C4/C5/C7a/C8a/C8b/§E/§H/§I/§J/§K + two NEW §F blocks EE-11/EE-12), NOT section deletion and NOT a new commit. The commit SEQUENCE is UNCHANGED (C0–C8b, 12 commits, 11 if Guard-C folds).

| # | Rule (as named in prompt) | Verification evidence (quoted/measured) | Conclusion |
|---|---|---|---|
| 1 | edit-in-place-not-full-rewrite | All edits were targeted `old→new` replacements via Python read-modify-write (each chunk asserting exactly 1 occurrence before substituting; the write is end-of-script per chunk). NO wholesale rewrite. Section map re-counted AFTER editing (`grep -n '^## '`): attestation + the 11 lettered headings `## A–## K` + Rules-Applied present, unchanged order (see final-message §5). §A/§B/§E/§F/§H/§I/§J/§K sharpened IN-SECTION (C4/C5/C7a/C8a/C8b content; two NEW §F blocks EE-11/EE-12), no lettered section removed; §B commit-block count still 12 (C0 + C1–C5 + C6a/b + C7a/b + C8a/b); the C0–C8b sequence intact. | COMPLIANT |
| 2 | empirical-evidence-blocks | §F re-measured live at HEAD `05ad61b` (2026-06-14): EE-1 battery **202** (was 186); EE-6 highest Check **52** (was 51, Guard-B pack landed C3) + validate-pack exit 0; EE-2 prohibition matcher **25** (was 23 — C1/C2 STRIP gone, 6 C0–C3 process docs joined); EE-4 OPTIONAL-FEATURES `baseRef`/`bgIsolation` 0/0; NEW EE-11 in-session-spawn ABSENT both surfaces (PACK-CHAT.md 1 benign hit / PM-CHAT.md 0; `## Permission profiles` `:397`); NEW EE-12 `permissions.deny` 0/0; EE-5 project 16 / READONLY_AGENTS 14; EE-8 carve-out 3 sites; EE-10 §17 carve-out + Class column CONFIRMED landed. Each block carries command + verbatim output + HEAD-SHA + date + interpretation + SUPPORTED conclusion. | COMPLIANT |
| 3 | pack-project-separation-of-concerns | §18 folds keep the surfaces strictly separate: C4 (PACK-CHAT.md) + C5 (pack OPTIONAL-FEATURES) + C8b (guard) = `pack-only`; C7a (PM-CHAT.md) + C8a (project OPTIONAL-FEATURES) = `project-only`. §B C7a/C8a + §I rows MANDATE client-native authoring ("PM Chat" orchestrator, client paths, "NOT a byte-copy of the pack version", "separate artifact, not a fallback"); the in-session spawn instruction + the `permissions.deny` recipe are authored independently per surface (§18.1/§18.2). The Guard-A′ extension (C8b, pack-side validator) asserts both surfaces but is itself single-surface `pack-only`. | COMPLIANT |
| 4 | bd-pack-only-operational-rule | §B C7a (PM-CHAT.md in-session instruction) + C8a (project recipe) + §I rows explicitly require ZERO pack-self refs (no BD-NNN, `maintenance-docs/`, `pack-*`, Pack Chat, `pack-ops/`) in any `project-template/` edit; the §18.1 element-3 background-spawn note is client-native ("NO pack-self `run_in_background` rule citation"). | COMPLIANT |
| 5 | verify-full-ci-suite | §D's wired-script enumeration is unchanged + still authoritative; §B C8b + §H require the EXTENDED Guard-A′ test `test-validate-pack-check-54.sh` authored → RUN (quote exit 0) → wired into `validate-pack.yml` → full battery re-run in the SAME commit (run-before-wire), with positive + negative cases for all 3 tokens. Battery re-measured to 202 (§F EE-1). | COMPLIANT |
| 6 | ci-guard-design-measure-then-bound | §B C8b + §E + §I C8b: the user-approved Guard-A′ EXTENSION is sized to EXACTLY THREE tokens (`baseRef`+`bgIsolation`+the `permissions.deny` recipe token), the third RE-MEASURED at C8b commit-time to the exact string C5/C8a authored (NOT a broad pattern); §F EE-12 establishes the 0/0 baseline; §E step 4 keeps the bounded check sized to the legitimate token set (the `isolation` param prose is NOT folded in). C5/C8a author the recipe; C8b measures-then-bounds the guard to it. | COMPLIANT |
| 7 | ci-check-runtime-compounding | §B C8b + §E: the EXTENDED Guard-A′ stays single-pass/cheap — six single-file `rg -c` reads (3 tokens × 2 files), trivial, negligible across the 202 validate-pack invocations (§F EE-1); coder records wall-time vs budget in the IMPL-REPORT (decision 4). No subprocess-per-entry, no whole-tree scan added. | COMPLIANT |
| 8 | enumerate-encoding-surfaces | §H adds the in-session spawn surfaces (PACK-CHAT.md C4, PM-CHAT.md C7a) + the `permissions.deny` recipe rows (pack/project OPTIONAL-FEATURES) + the EXTENDED Guard-A′ + its test, each with validators/tests/CI-refs/cross-ref docs in lockstep; §D + §I move in step (the Guard-A′ test covers all 3 tokens, positive + negative). The new spawn instructions + recipes + the guard extension + its test are all reflected in §D/§H/§I in lockstep. | COMPLIANT |
| 9 | regenerate-manifest-v11-surface | §G's per-commit table is unchanged + still correct: C7a/C8a touch `project-template/` (v11-surface) → manifest STAGE fires (carved-out via C0, so still `project-only`); C4/C5/C8b are pack-side (`pack-ops/`/`scripts/`) → RUN build, STAGE only if non-empty (expected-empty; pack docs + validate-pack do not project into fixtures). §G re-confirmed; the §18 folds do not change which commits stage the manifest. | COMPLIANT |
| 10 | cross-cli-reference-normalization | §B C7a + §I C7a: PM-CHAT.md is a SINGLE client file (not trinity) — the in-session instruction is authored CLIENT-NATIVE, background-spawn phrasing client-native (no pack-self rule citation). No new trinity implications from §18 (the in-session spawn instructions live in PACK-CHAT.md/PM-CHAT.md, not the trinity CLAUDE/AGENTS/GEMINI files; the C2 Claude-only worktree bullet already landed). Flagged: no trinity edit added by this pass. | COMPLIANT |
| 11 | agents-never-commit + destructive-verb ban (ALL agents incl RW) | §B C4/C7a + §I intro/C4/C7a: the in-session spawn instructions STATE that RW agents are spawned isolated (`isolation:"worktree"`) and the verb-ban is LOAD-BEARING (no platform safety net — FACT-4); the §18.2(ii) `permissions.deny` recipe is the documented mechanical reinforcement (session-scoped + sub-agent-inherited, F2). The orchestrator alone applies + commits; agents run only `git diff > /tmp/...`. **This THIRD UPDATE PASS ran ZERO state-changing git verbs** — only `git rev-parse`/`git log`/`git branch` reads + read-only `rg`/`grep`/`python3 validate-pack.py`; the only writes are to the plan doc via Python read-modify-write. | COMPLIANT |
| 12 | no-deferral-without-user-direction | All §18 work (in-session spawn for BOTH surfaces + the prose deny-list + the documented-optional `permissions.deny` recipe + the launcher `--disallowedTools`) stays v11.0 (C4/C5/C7a/C8a/C8b); the in-session backstop's user-activated honest-limit (F3) is a documented design property, NOT a deferral (§K); ONLY background-session isolation (`bgIsolation`/EnterWorktree) remains deferred — BD-218 (user-authorized 2026-06-14). | COMPLIANT |
| 13 | bounded-review-fix-cycle | §I intro unchanged: each commit = one fresh pack-coder + max 2 fix pairs + 3 reviewers per commit; architect escalation if dirty after the final reviewer; Pack Chat does no fixes. The coder spawn map is unchanged by this pass. | COMPLIANT |
| 14 | scope-deliverables-to-the-ask | This pass folded ONLY §18 (in-session spawn symmetry + unified backstop + the §2.1-scrub-driven content) + the user-approved Guard-A′ extension — no other change. The C0–C8b sequence, the C0 carve-out, the data-first split, the mode-model, the Guard-A self-exception, run-before-wire, the per-CLI carve-out drop, J3/J4/J5 are all PRESERVED unchanged. No redesign; no new commit. | COMPLIANT |
| 15 | rules-applied-verification-block | This THIRD-pass block addresses every rules-in-force rule from the calling prompt with quoted/measured evidence; no empty cell; no AMBIGUOUS terminal state. | COMPLIANT |

---

**PLAN-READY: YES.** All NAMED inputs read in full (the existing plan; the RECONCILED design incl. the NEW §18 + §2-scrubbed + §4 + §5 + §9 + §12.2/§12.3; `RESEARCH-BD-197-INSESSION-BACKSTOP.md` F1–F5; `pack-ops/PACK-CHAT.md` + `project-template/docs/pack/PM-CHAT.md`; `backlog/BD-197.md` incl. Notes 11–13 + `backlog/BD-218.md`; `CLAUDE.md ## Pack memory`; the user-direction memory). All sequencing-load-bearing state-claims re-measured against live HEAD `05ad61b` (§F — HEAD ADVANCED `ae3d932` → `05ad61b`, C0/C1/C2/C3 LANDED; battery 186 → 202; highest Check 51 → 52; prohibition matcher 23 → 25; OPTIONAL-FEATURES `permissions.deny` 0/0 NEW baseline; in-session spawn ABSENT both surfaces). The commit SEQUENCE is UNCHANGED — **12 commits** (11 if Guard-C folds — J3), C0–C8b; C0/C1/C2/C3 already landed, the §18 folds target still-pending C4/C5/C7a/C8a/C8b. The §18 fold SHARPENS content: C4 = PACK-CHAT.md in-session orchestrator spawn procedure; C7a = PM-CHAT.md in-session Agent/Task-tool spawn instruction (CLOSES the project under-scope); C5/C8a = the documented-optional `permissions.deny` recipe (pack + project, client-native); C8b = the user-approved Guard-A′ EXTENSION (3-token assertion incl. the recipe). J4 stays NO new shipped pack-side file (Check-47 untouched). Green-per-commit holds (the §18 content lands inside already-green commits; the extended Guard-A′ is green on arrival because C5/C8a author all three tokens before C8b). The mode-model is the CORRECTED two-independent-mechanisms model (subagent `isolation:"worktree"` param × `baseRef`; background-session `bgIsolation` → BD-218). The standing items are unchanged: coder-call (J3 Guard-C fold — may reduce to 11) + a pre-coding GATE (J4 new-pack-side-script, owed before C4 codes; §18 confirms J4 = NO new file) + user-visible probe outcomes (J5).

*End of PLAN-BD-197-WORKTREE-ISOLATION.md (THIRD UPDATE PASS 2026-06-14 — §18 in-session spawn symmetry + unified backstop folded into C4/C5/C7a/C8a + the user-approved Guard-A′ extension into C8b; sequence C0–C8b UNCHANGED; SECOND PASS 2026-06-13 §17 carve-out as C0)*
