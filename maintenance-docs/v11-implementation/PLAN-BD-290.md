# PLAN-BD-290 — Executable implementation plan, waves W1–W4 (RECONCILED)

**Agent:** planner-bd290-planrec (fresh pack-planner reconciliation instance, RO; neither the plan's author nor its adversarial reviewer). **Date:** 2026-08-25.
**Tree:** /Users/david/Developer/optiquity-ai-agent-config-pack (canonical checkout; `pwd` verified). **HEAD:** 34e8de7377d28f751479891f0d7d4ab33ce028ec (re-pinned at reconciliation; the sole delta file vs the v1 anchor 3cf436e is `pack-ops/dashboard-approvals/dashboard.html` — 34e8de7 `docs: v11 — dashboard re-render at BD-290 stage 2 (pack-only)`). Working tree: ` M backlog/BD-290.md` (user-approved AC amendment, applied) + ` M pack-ops/session-state.json` (orchestrator-owned) — both treated as given.
**Design (authoritative):** `/Users/david/.local/state/optiquity-pack-handoff/bd290-reconcile-20260825T163255Z/ARCHITECTURE-BD-290.md` (reconciled; §4 edit map, §5 hooks+tests, §6 guards, §10 wave map). Adjudication log beside it.
**Reconciliation provenance:** adversarial review `…/bd290-advplan-20260825T171636Z/ADVERSARIAL-PLAN-REVIEW-BD-290.md` (0 BLOCKER / 2 MUST / 4 SHOULD / 5 NIT) adjudicated in `…/bd290-planrec-20260825T173350Z/PLAN-RECONCILIATION-BD-290.md` — all accepted corrections folded below, plus one reconciliation-found gap (RP-1, new-file patch conveyance, §6/OI-6).
**Gate evidence:** W0 `…/bd290-w0-20260825T165603Z/IMPL-REPORT.md` — all four gated shapes ALLOWED at the pinned workspace root (plan on the PRIMARY design; the §3 fallback is dead). W-V1 deny-path + allow-path both proven live; pinned-def leg MEASURED: on the Agent-tool subagent_type path, a scratch def pinning `isolation: worktree` in frontmatter, spawned WITHOUT the per-spawn parameter, ran IN-PLACE (mailbox channel) — the pin is NOT honored; only the per-spawn `isolation:"worktree"` parameter isolates (other invocation paths unmeasured). Affirms the design's OI-D no-pin direction.
**Stage-1 evidence (cited, not re-derived):** census `…/bd290-census-20260825T125529Z/IMPL-REPORT.md`; probes `…/bd290-probe-20260825T125529Z` and `…/bd290-probe2-20260825T154529Z`.
**User decisions in force:** OI-B mode-invariant workspace; OI-F honest-bounds caveat, `permissions.deny` stays optional; OI-G W1–W3's own cycles run the §3 workspace ritual; OI-H AC amendment applied.

---

## §0 Planner evidence ledger (Empirical-Evidence Blocks)

Measurements marked (v1) ran at HEAD 3cf436e and were re-verified valid at 34e8de7 by the adversarial pass and/or this reconciliation (the sole inter-HEAD delta file is `dashboard.html`); measurements marked (rec) ran fresh THIS reconciliation run at HEAD 34e8de7, 2026-08-25.

- **EEB-PL1 — HEAD/tree/worktrees (rec).** `pwd && git rev-parse HEAD && git status --porcelain` → canonical path, `34e8de7377d28f751479891f0d7d4ab33ce028ec`, exactly the two expected modifications. `git worktree list` → ONE row (the canonical checkout); `ls ${HOME}/.local/state/optiquity-pack-workspaces/` → empty. **SUPPORTED** — clean slate for the per-commit workspaces.
- **EEB-PL2 — retired-phrase carriers at verified locators (v1; carrier files re-measured rec).** Pattern-1 tracked carriers at 34e8de7 (rec): `git grep -lE "then .?cd.?(-s it)? to the target tree"` → **9 files** — `pack-ops/OPERATING-MODES.md:42`, `.claude/.codex/.agents` `pack-isolation-mode/SKILL.md:21`, `project-template/docs/pack/PM-OPERATING-MODES.md:43`, `project-template/skills/pm-isolation-mode/SKILL.md:25`, `project-template/docs/pack/PM-CHAT.md:532`, `backlog/BD-290.md` (record stream), **plus `pack-ops/dashboard-approvals/dashboard.html`** (rendered mirror; the phrase survives the stage-2 re-render because its CLAUDE.md source is pre-W1 — cleared by C4, and Check-53-invisible: `.html` is outside `_CHECK_53_SCAN_SUFFIXES`, `discipline_parity.py:695`). Reuse-family (v1, locators re-spot-checked): `pack-ops/PACK-CHAT.md:313,:321,:409`; `pack-ops/OPTIONAL-FEATURES.md:123,:130,:138,:147,:261`; `.claude/agents/pack-coder.md:43`; `project-template/docs/pack/OPTIONAL-FEATURES.md:108,:140–143`; `project-template/docs/pack/PM-CHAT.md:518,:523,:539–541,:607`; `project-template/.claude/agents/coder.md:62–63`; `…/repo-ops.md:44–46`; commit-discipline SKILL `:22,:24,:34,:45–46,:53,:78,:104`; implementation-report SKILL `:30`; `project-template/docs/pack/prompts/reviewer.md:48–51`. `cd-REUSE` (rec): 5 files (pack-coder ×3, `pack-ops/OPTIONAL-FEATURES.md`, `pack-ops/PACK-CHAT.md`). **SUPPORTED.**
- **EEB-PL3 — agent-def enumeration (v1).** Pack RO defs = 4 names × 3 dirs = **12 files** (E-RODEF); pack-coder = 3 files + `RUNTIME-SUBAGENT-PATTERN.md` (E-RWDEF); client coder/repo-ops = 2 names × 3 dirs = **6 files** (E-C11). Full paths in §2/§3. **SUPPORTED.**
- **EEB-PL4 — check anatomy current (v1; re-verified rec).** `discipline_parity.py`: `_CHECK_53_PROHIBITION_PATTERNS` (2 patterns, :690–693), `_CHECK_53_SCAN_SUFFIXES = (".md", ".txt", ".py", ".sh", ".toml")` (:695), `_CHECK_53_ALLOWLIST_DIR_PREFIXES = ("maintenance-docs/archive/", "maintenance-docs/v11-implementation/")` (:700–703), `_CHECK_53_SELF_SOURCE` (:734); `_CHECK_54_OPTIONAL_FEATURES_SURFACES` = 2 files; Check 54 RUNTIME comment says "exactly TWO single-file reads"; **`__all__` at :2818 under the :2810 header "every Cluster B symbol (the facade re-exports via `import *`)", with `_CHECK_54_OPTIONAL_FEATURES_SURFACES` listed at :2847** (rec). `core.py`: `_CHECK_54_REQUIRED_TOKENS` at :391–395; `__all__` header :479–483 mandates every underscore seam be listed; `"_CHECK_54_REQUIRED_TOKENS"` at :519; `CHECK_REGISTRY_EXPECTED_COUNT = 91` at :210 with the "(Next free numeric ID = 95.)" ledger line at :209. **SUPPORTED.**
- **EEB-PL5 — test anatomy (CORRECTED at reconciliation, AP-5).** `test-validate-pack-check-53.sh` (rec, header :26–39 + Group 0 :58–89 read verbatim): **Group 0** = module import + symbol registration (`hasattr` presence only — NO pattern-count sizing, so H1's tuple append is Group-0-safe); **Group 1** = synthetic-tree end-to-end with the per-pattern FAIL model — A (pattern 1) / A2 (pattern 2, "the second matcher branch") — plus B/B2/C/D/E/F; **Group 2** = end-to-end exit-status on HEAD; **Group 3** (:38–39) = the two lenient-SKIP branches (git absent / not a work tree), "each pinned with a fixture that carries the prohibition prose" — these are per-SKIP-branch pins proving LENIENCY, not the per-pattern bite mechanism. `test-validate-pack-check-54.sh` (264 lines): cases A–G incl. absent-file FAIL (F) and exact-sizing PASS (G); **Group 0 (:54–88) loads `validate-pack.py` as a module and asserts `hasattr(mod, n)` + EXACT tuple equality on `_CHECK_54_OPTIONAL_FEATURES_SURFACES` / `_CHECK_54_REQUIRED_TOKENS` — the facade-reachability idiom the H5 extension follows** (rec). **SUPPORTED.**
- **EEB-PL6 — propagation procedure + manifest (v1).** `pack-ops/PACK-CHAT.md:552` § "Keeping CLAUDE.md, AGENTS.md, GEMINI.md, and PACK-AGENTS.md current"; `:562` rule-change propagation table (manifest step 6 = push-time `scripts/manifest-sync.sh`, NOT per-commit). `pack-ops/.spawn-rule-manifest.txt` slugs: 9, none isolation-specific; its `references:` lines resolve against the five preserved section titles (Check 46). `pack-ops/PACK-MEMORY-RATIONALE.md` `## agents-never-commit` (:28) body restates the old cycle flow (:30–36). **SUPPORTED.**
- **EEB-PL7 — trinity exemption holds (v1).** `grep -nE "worktree|isolation" AGENTS.md GEMINI.md` → 4 hits each; NONE is a retired-family token. **SUPPORTED** — W1's CLAUDE.md edits need no AGENTS/GEMINI parallels IF confined per EEB-PL8.
- **EEB-PL8 — CLAUDE.md family hits all inside the exempt section (v1; boundaries re-verified rec).** Family hits :476, :479, :480, :485 — all within `### Sub-agent behavior (Claude-only)` (:468–579; next heading `### Pack Chat scope` at :580) — plus :904 "the caller's target tree" (`ci-check-runtime-compounding` rule — DIFFERENT sense, MUST NOT be touched). **SUPPORTED.**
- **EEB-PL9 — CI battery shape (v1).** `.github/workflows/validate-pack.yml` → job `validate` (`python3 scripts/validate-pack.py` + `PACK_VALIDATE_DEEP=1 python3 scripts/validate-pack.py`) + job `tests` (sharded wired `scripts/tests/*.sh` via `scripts/lib/ci-shard-plan.py`, fixture build where a shard needs it) + `tests-result` coverage assert. **SUPPORTED.**
- **EEB-PL10 — Guard-2 token zero-state (rec).** `git grep -in "only in its own worktree"` at 34e8de7 → **zero output lines**. The amended AC text does NOT contain the token bytes (no leading "only" — diff read verbatim). **SUPPORTED** — post-W1/W2 occurrences remain exactly the authored set.
- **EEB-PL11 — session config + gitignore (rec).** `cat pack-ops/session-config.json` → `"isolation_mode": "read-write-only"`; gitignored. **SUPPORTED** — W-V2 flips to `full` and restores `read-write-only` (measured value, not assumed).
- **EEB-PL12 — preservation conventions + uniqueness (v1; uniqueness re-verified rec).** `maintenance-docs/v11-implementation/` naming precedent `ARCHITECTURE-BD-*.md` / `PLAN-BD-288.md` / `RESEARCH-BD-*.md`; `git ls-files | grep -i "bd-290"` → only `backlog/BD-290.md` ⇒ every §5 preserved basename is repo-unique by construction. Check 53 allowlist already covers `maintenance-docs/v11-implementation/` (EEB-PL4). **SUPPORTED.**
- **EEB-PL13 — Check-68 scope vs the W3 in-code design reference (CORRECTED at reconciliation, AP-1; supersedes the v1 claim).** Measured (rec): `boundary_refs.py:4257` `_CHECK_68_INCLUDE_TREES = ("project-template", "supporting-docs")`; `:4267–4274` `_CHECK_68_EXCLUDE_PREFIXES` includes `"maintenance-docs/"`; `_CHECK_OPERATING_DOC_FAMILIES` (:3466–3510) contains NO `scripts/` glob; scope build (:4364–4376) = operating docs ∪ `README.md` ∪ the two include trees. Consequence: `scripts/lib/validate_checks/discipline_parity.py` is NEVER in Check 68's scope and `maintenance-docs/` paths are excluded — the H1 comment's path reference is never CI-resolved, tracked or not; C3's battery cannot red on it. The C3 fold-in of H6/H7 stands on `architect-doc-reality-reconciliation` co-landing coherence (design §12.14: check-header comment + addendum home + IMPL-REPORT cross-refs land WITH the realized consumer; a C5-time copy would leave the C3 comment citing a repo path that exists only two commits later), NOT on CI force. The §4.1/§7 gate is therefore a direct existence assertion, not a Check-68 behavior. **SUPPORTED.**
- **EEB-PL14 — collision-scan delta (rule 6) (v1).** Files this plan ADDS beyond the design's §9 blast-radius set: `backlog/_toc.md` (regen at flip; `backlog/_rules.md:89–91`), `maintenance-docs/v11-implementation/<9 new BD-290 basenames>` (§5), `pack-ops/session-state.json` (already in the design's ORCH row). BD-247 claims the backlog ENTRY SCHEMA + `_rules.md`, not the generated `_toc.md`; no open BD claims any new BD-290 basename. **Intersection delta = NONE — verdict unchanged (3 COORDINATE, 0 blockers, per design §9).**
- **EEB-PL15 — graph presence + graph-first record (rec).** `ls -la graphify-out/graph.json` → exists (11,721,659 bytes, refreshed 2026-08-25 13:18 — the runtime-injection posture absorbs refreshes) ⇒ the orchestrator injects `--graph /Users/david/Developer/optiquity-ai-agent-config-pack/graphify-out/graph.json` into every §6 spawn. The planning and reconciliation runs raised NO new discovery question — the carrier universe is closed by design EEB-10 (graph-first attempt on record in design EEB-16: three graph-empty instances for this vocabulary ⇒ sanctioned G2 fallback); every read was a verification read of a named surface (fall-through (i)/(ii)/(iv)). **SUPPORTED.**
- **EEB-PL16 — sweep-family audit bounds (rec; AP-4 fold).** `git grep -n "fix-coder reuse"` → **6 carriers**: `project-template/.claude/agents/coder.md:63`, `…/repo-ops.md:46`, `project-template/.agents-plugin/optiquity-agents/agents/coder.md:65`, `…/repo-ops.md:50`, `project-template/.codex/agents/coder.toml:31`, `…/repo-ops.toml:21` — ALL inside the G5–G10 edit set (client RW defs ×6; the two `.toml` carriers also state the now-refuted "a pin would force a new worktree per spawn" rationale, replaced by the measured no-pin wording). `git grep -n "worktree-agent"` full set captured and categorized — EDIT-set carriers: commit-discipline ×3 (:22/:24/:34/:45/:53 regime-DETECT sense KEPT; :78/:104 write-target sense REWRITTEN), implementation-report ×3 (:30 rewritten), `pack-ops/OPTIONAL-FEATURES.md:264` (self-detect, re-grounded by F5-b); legitimate survivors: record streams (`backlog/BD-197.md`, `maintenance-docs/v11-implementation/*`), display filters (`scripts/dashboard-render.py:416`, `project-template/scripts/pm-dashboard-render.py:429`, `pack-ops/dashboard-approvals/dashboard-shell.html:335`), `pack-ops/DASHBOARD-SPEC-PACK.md:777`, `scripts/lib/validate_checks/singletons.py:249` (H3 reconciles to name both kinds), `scripts/tests/test-validate-pack-check-4.sh` fixtures. **SUPPORTED** — the §2.2/§3.1/§8 family audits are sized to this measured bound.
- **EEB-PL17 — C3's new files cannot ride the `git diff` patch (rec; RP-1, reconciliation-found).** H6/H7 are the ONLY new-file additions in the coder commits (C1's 31 and C2's 12 are all edits of existing tracked files; `git ls-files | grep -i bd-290` → only `backlog/BD-290.md`). Plain `git diff` emits tracked-file modifications only — an untracked new file appears in NO patch, and the coder cannot stage one (`git add`, including `-N`, is a denied verb; the documented merge-back ritual `pack-ops/PACK-CHAT.md:395–431` is `git diff > changes.patch` → `git apply`). Under the v1 §6 steps 7–8 the C3 patch silently omits H6/H7; the orchestrator's targeted `git add maintenance-docs/v11-implementation/…` then fails LOUDLY (path absent in the canonical tree) — caught, but unplanned. Consequence: §6 carries a C3-specific new-file conveyance step (OI-6). **SUPPORTED.**

---

## §1 Plan summary + wave/commit table

BD-290 lands as **six commits** (C0 recommended + C1–C5), realizing design waves W1–W4. W0 and W-V1 have already run and PASSED — no plan step repeats them. Every commit runs the full battery (§7), lands via the §6 workspace ritual (coder commits C1–C3) or Pack-Chat-direct/orchestrator mechanics (C0, C4, C5), and is user-approved at the commit gate.

| Commit | Wave | Actor | Scope (file count) | Subject (proposed) | Scope keyword | Depends on |
|---|---|---|---|---|---|---|
| **C0** (recommended — OI-1) | pre-W1 | Pack Chat direct | `backlog/BD-290.md` (already-applied AC amendment) + `backlog/_toc.md` regen (expected byte-identical — OI-1/AP-11) + `pack-ops/session-state.json` | `docs: v11 — BD-290 acceptance-criteria amendment (pack-only)` | pack-only | user approval of this plan |
| **C1** | W1 | pack-coder (workspace WS-1) | 31 pack-side files (§2) | `fix: v11 — BD-290 W1 pack-side isolation-subsystem contract repair (pack-only)` | pack-only | W0 PASS (done); C0 |
| **C2** | W2 | pack-coder (workspace WS-2, parallel with C1's cycle) | 12 project-side files (§3) | `fix: v11 — BD-290 W2 project-side isolation-subsystem contract repair (project-only)` | project-only | W0 PASS (done); C0. Parallel with C1 (file sets disjoint — §6); lands after C1 (fixed order for determinism) |
| **C3** | W3 | pack-coder (workspace WS-3, fresh) | 5 code/test files + 2 preserved design docs (§4) | `fix: v11 — BD-290 W3 Check 53/54 guard extensions + tests (pack-only)` | pack-only | C1 AND C2 landed (§6 atomicity: the guards' fix-recipes and tokens are authored by W1/W2) |
| **C4** | W4 | Orchestrator (no coder) | `pack-ops/dashboard-approvals/dashboard.html` re-render | `docs: v11 — BD-290 W4 dashboard re-render (pack-only)` (prefix per OI-2) | pack-only | C1 (its CLAUDE.md § Pack memory mirror source); ordered after C3 with W4 |
| **C5** | W4 | Pack Chat direct | remaining preserved artifacts (7 files, §5) + `backlog/BD-290.md` Resolved flip + `_toc.md` regen + `pack-ops/session-state.json` | `docs: v11 — BD-290 design artifacts preserved + Resolved (pack-only)` | pack-only | C3 landed; W-V2 acceptance probe PASS; C4 |
| — | W4 | Orchestrator | W-V2 acceptance probe + channel observation + full battery on canonical HEAD; `scripts/manifest-sync.sh` before `git push` | (no commit; probe + push mechanics) | — | C3 landed (probe exercises the repaired contract docs’ regime) |

Atomicity rationale (design §6, load-bearing): Check 53's two new patterns would FAIL on the pre-W1/W2 tree (7 + 5 live STRIP carriers), and Check 54's new leg would FAIL until the six surfaces carry the minted token — so **C3 lands strictly after C1+C2**, and neither guard is merged into a doc commit (each intermediate commit stays battery-green: C1/C2 strip carriers with the guards not yet live; C3 activates guards against an already-clean tree, green-by-construction).

Check 71 constraint (load-bearing in C1): the three skill triples touched by W1 (`pack-isolation-mode`, `implementation-report`, `commit-discipline`) each land **byte-identical across `.claude/` / `.codex/` / `.agents/` in C1 itself** — a partial triple reds the C1 battery.

---
## §2 W1 — pack-side contract repair (commit C1, 31 files)

**Binding cross-file instructions (from design §4, restated for the coder prompt):**

- **Phrase-sweep rule.** Mapped line numbers are LOCATORS, not bounds. For EVERY file below, sweep the WHOLE file for the retired-token family — `then cd to the target tree` (any punctuation/`cd`-s variant), `cd in + verify`, `cd into that/it … worktree`, `cd-REUSE`, `REUSES the/that … worktree`, `fix-coder reuse`, `worktree-agent-*`-as-cycle-workspace, stale `live worktree` (vocabulary rule) — and rewrite every occurrence. The sweep must be **wrap-aware**: prose wraps mid-phrase (e.g. CLAUDE.md's rule-9 "live worktree" splits across lines), so grep for the component words too, or sweep section-by-section by eye. Known under-runs the sweep must catch: commit-discipline carriers at `:78`/`:104` (outside the `:22–:53` locator). After every OPTIONAL-FEATURES edit, re-verify Check 54's three EXISTING tokens (`baseRef`, `bgIsolation`, `permissions.deny`) survive.
- **Vocabulary rule.** One term for the shared cycle tree: the **commit workspace** (an orchestrator-created out-of-prefix worktree). "the commit's live worktree"/"live worktree" → "the commit's live workspace"; rule-9 trigger reads "while a live commit workspace with uncommitted work exists". "Worktree" stays correct for the agent's OWN platform worktree. Do NOT sweep non-family senses: CLAUDE.md:904 "the caller's target tree" (runtime-compounding rule), AGENTS/GEMINI's four hits (EEB-PL7), `boundary_refs.py:4670` "REUSES" (unrelated sense), the large-BD-pipeline "parallel worktree coder waves" wording (not in the family; touching it would trigger trinity-parity obligations W1 does not carry), the regime-DETECT sense of `worktree-agent-*` (EEB-PL16 — the agent's OWN platform worktree; only the cycle-workspace/write-target sense is retired).
- **Guard-2 token bytes.** The literal lowercase byte sequence **`only in its own worktree`** appears VERBATIM in every authored `full` contract below (F1, F6–F8 here; G1, G3 in W2). Design-internal cross-refs like "(§3)" in the §2 invariant are NOT copied onto repo surfaces — replace with the surface-local pointer (e.g. the "How `full` executes" paragraph, or `pack-ops/OPERATING-MODES.md` § Isolation mode).
- **No-pin wording (binds F4, F5, F28–F30; client twins in W2).** Surfaces state ONLY the measured claim, WITHOUT evidence citations (no "proven live"/"W-V1" provenance — `operating-docs-no-history-no-bloat`): "Isolation is the per-spawn caller's choice — the enforce hook keys on the per-spawn `isolation` parameter in the tool payload; a def-frontmatter pin is not a substitute for passing it per-spawn." The W-V1 pinned-def leg HAS measured (header, gate evidence): the pin is not honored on the subagent_type spawn path. The authored no-pin text therefore ADDS this measured second sentence, stated as current behavior, narrowly scoped, zero provenance: "A def-frontmatter pin is not honored on this spawn path — only the per-spawn `isolation` parameter isolates." Do NOT author the design's draft hook-payload corollary ("a def-level pin is invisible to the payload check") — that mechanism-level claim remains UNMEASURED (the scratch def's unknown class fail-opens the hook, so the payload view was never probed; the design's no-pin note keys the corollary on measuring the corollary claim itself); the honored/not-honored behavior above is exactly what the leg proved (OI-5, resolved).
- **History discipline.** Every rewrite: zero version markers, bug numbers ("#73647" never appears), "Before v2.1.x" narration, BD references, or provenance. Platform facts as live constraints ("the platform refuses…").

### §2.1 Per-file edit specs

**F1. `pack-ops/OPERATING-MODES.md`** — (E-P1-a) REPLACE the `full` row at :42 with the design's authored text, verbatim: "All agents — read-only included — spawn `isolation:"worktree"`. An isolated agent runs git only in its own worktree or the orchestrator-injected commit workspace; the platform refuses git aimed at the main checkout or any tree under its path. Canonical facts (HEAD, dirty summary) arrive orchestrator-injected; target-tree files are read by absolute path. Clean-channel opt-in: isolated spawns return on the async completion channel." (E-P1-b) INSERT a short "How `full` executes" paragraph after the mode table, ≤8 lines, covering design S3–S6: injected canonical HEAD + dirty summary; committed-state git in the agent's own worktree; `git worktree list` as the sanctioned base cross-check; wrong-base degradation = report + Read-tool fallback, never cross-tree git.

**F2. `CLAUDE.md`** — (E-P2, pack-chat-only file scoped INTO the coder prompt — the supported path per `pack-chat-minor-edits-only`). ALL edits confined to `### Sub-agent behavior (Claude-only)` (:468–579); the section is trinity-exempt, so NO AGENTS.md/GEMINI.md parallel (EEB-PL7/PL8) — the coder verifies post-edit that both still carry zero family tokens. Six targeted edits: (a) "Isolation mode (read-at-spawn)" sub-bullet: replace "ALL agents — RO included — spawn isolated (`isolation:"worktree"`) then `cd` to the target tree" with the §2-invariant summary (isolated agents run git only in their own worktree or the orchestrator-injected commit workspace; target-tree files by absolute path; canonical facts injected); read-at-spawn config mechanics unchanged. (b) RW-class bullet (:476 area): replace first-coder-CREATES/REUSES text with the workspace model — the ORCHESTRATOR creates the commit workspace out-of-prefix (`git worktree add --detach <WS> HEAD` under the workspaces root); every cycle agent (first coder, reviewers, fix-coders) targets `<WS>` by injected absolute path, fusing `cd <WS> && …` per call; spawn params unchanged (RW always `isolation:"worktree"`). (c) RO-class bullet (:479–480): "the commit's live workspace when the work is still uncommitted there (target it per-call; verify pwd/HEAD in the workspace)". (d) "No up-front patch" bullet: patch step reads `cd <WS> && git diff > <handoff>/changes.patch`. (e) Lifecycle/teardown bullet: orchestrator `git worktree remove <WS>` ONLY after the commit is confirmed landed; the platform sweep never removes orchestrator-created workspaces; failed commit KEEPS the workspace. (f) Rule-9 wording per the vocabulary rule.

**F3. `pack-ops/PACK-MEMORY-RATIONALE.md`** — (E-P2 lockstep, DEFINITE) `## agents-never-commit` body (:28–36): "held in the commit's isolated worktree" → "held in the commit workspace"; the patch-flow sentences updated to the workspace wording in lockstep with F2(d). Whole-file sweep. No slug added/removed ⇒ C3-bijection (slug-set equality) unaffected.

**F4. `pack-ops/PACK-CHAT.md`** — (E-P3, pack-chat-only scoped in) (a) § "How Pack Chat spawns" (:307–342): RW bullet :313 ("REUSES the same worktree — never a new worktree for a fix-coder") and :321 (frontmatter-pin/cd-REUSE rationale) → workspace-creation ritual (`git worktree add --detach <WS> HEAD` at `${XDG_STATE_HOME:-$HOME/.local/state}/optiquity-pack-workspaces/<bd-or-batch>-<ts>/`), WS-path injection into every cycle spawn, no-pin rule reworded per the no-pin note. (b) RO bullet + isolation-mode preflight (:335–340 "the RO agent `cd`s to the work's tree"): under `full` ⇒ pass the param + inject the canonical-facts block (HEAD SHA, `git status --porcelain` summary, workspace path when a cycle is live). (c) § "Merge-back" (:395–431): :409 reuse text → cycle-in-workspace; step 5 teardown = `git worktree remove <WS>` after the landed commit. (d) **Section titles verbatim-preserved**: "How Pack Chat spawns", "Merge-back (orchestrator-only; agents never apply or commit)", "In-session sub-agent spawn + merge-back (worktree isolation)", "Behavioral rules", "Role" — `pack-ops/.spawn-rule-manifest.txt` references resolve against them (Check 46). No canonical imperative text restated verbatim (Check 46 anti-restate).

**F5. `pack-ops/OPTIONAL-FEATURES.md`** — (E-P4-a) :302–312 Hook-1 prose: align the fail-open sentence to fold-to-default code semantics — absent/malformed config FOLDS to `read-write-only` and is enforced; only unparseable payload / unknown class / internal error are blanket-allow. (E-P4-b) :147 + :261–266: replace the "silent fall-to-main" premise with the live enforcement model (the platform actively refuses cross-tree git from isolated agents); the runtime pwd/HEAD self-detect RULE stays, re-grounded on surviving cases (non-Claude CLIs without worktree support; wrong-base worktree; belt-and-suspenders). (E-P4-c) :253–260 auto-removal caveat: auto-removal only when the subagent made no changes; worktree locked while running; sweep preserves work; explicit-teardown rule now REQUIRED for orchestrator-created workspaces. (E-P4-d) NEW "Platform worktree guard" honest-bounds paragraph: the guard's four checks as live constraints + two measured bounds — in-prefix linked worktrees count as protected target; out-of-prefix linked worktrees are NOT guard-covered (workspace trust = prose ban + the documented-optional `permissions.deny` recipe, per user decision OI-F). (E-P4-e) Caveats: the clean-channel effect of isolated spawns is not platform-documented; re-verify after CLI upgrades — one-line recipe (spawn one isolated + one non-isolated named probe; compare delivery). (E-P4-f) :117–140 cycle prose (:123 REUSES, :130 cd-in+verify, :134–139 no-pin paragraph) → workspace model mirroring G4's client treatment; no-pin per the wording note. Verify `baseRef`/`bgIsolation`/`permissions.deny` all survive (Check 54).

**F6–F8. `pack-isolation-mode` skill ×3** — `.claude/skills/pack-isolation-mode/SKILL.md`, `.codex/skills/pack-isolation-mode/SKILL.md`, `.agents/skills/pack-isolation-mode/SKILL.md` — (E-P9) rewrite the `full` bullet (:20–21 in the `.claude` copy) to the F1 row text, short form, token bytes verbatim. **Byte-identical, all three in C1 (Check 71).**

**F9–F11. `implementation-report` skill ×3** — `.claude/.codex/.agents` copies — (E-IR) :29–31: the isolated-regime change-base is the injected commit workspace's HEAD (detached at the parent HEAD), not "the `worktree-agent-*` checkout's HEAD"; regime-detection wording in lockstep. **Byte-identical ×3 in C1 (Check 71).**

**F12. `pack-ops/PACK-AGENTS.md`** — (E-P12, pack-chat-only scoped in) § "Two agent classes" (:158): one-line qualifier — under `isolation_mode: full` RO agents spawn isolated and follow `pack-ops/OPERATING-MODES.md` § Isolation mode (pointer only, no restatement). Heading text unchanged (internal :23 cross-ref + manifest-referenced sibling titles).

**F13–F15. `commit-discipline` skill ×3** — `.claude/.codex/.agents` copies — (E-CD, whole-file sweep; carriers :22, :24, :34, :45–46, :53 AND :78, :104) (a) RW runtime-regime text: your workspace is the injected `<WS>` path — fuse `cd <WS> &&` into each call; verify pwd + workspace HEAD vs the injected SHA; Writes/Edits target `<WS>/…` ABSOLUTE paths (:78/:104 currently direct writes under the platform-worktree `pwd` — rewrite both). (b) RO text: default mode = the work's tree; isolated regime = own-worktree git only, injected facts, Read-tool cross-tree. (c) OI4 twin: replace "(a platform fall-to-main, or a CLI without worktree support)" (:45–46) with the surviving degradation cases. (d) Vocabulary rule at :40/:73/:95. The regime-DETECT mentions of `worktree-agent-*` (:22/:24/:34/:53 sense — the agent's OWN platform worktree) KEEP the token in the detect sense (EEB-PL16). **Byte-identical ×3 in C1 (Check 71).**

**F16–F27. Pack RO defs ×12** — `.claude/agents/pack-architect.md`, `pack-planner.md`, `pack-reviewer.md`, `pack-docs-researcher.md`; `.codex/agents/pack-architect.toml`, `pack-planner.toml`, `pack-reviewer.toml`, `pack-docs-researcher.toml`; `.agents-plugin/pack-agents/agents/pack-architect.md`, `pack-planner.md`, `pack-reviewer.md`, `pack-docs-researcher.md` — (E-RODEF) mechanical one-sentence extension of each "RO placement" paragraph: when your spawn prompt marks you worktree-isolated, run git only in your own worktree or the injected workspace; cross-tree git into the main checkout's path is platform-refused; use the injected canonical facts. Vocabulary rule on "the commit's live worktree". Per-CLI format preserved (`.toml` copies carry the paragraph as plain text — same sentence, CLI-native formatting; `skill-agent-maintenance-mechanical`: mechanical, complete, all 12 in C1).

**F28–F30. `pack-coder` def ×3** — `.claude/agents/pack-coder.md` (:23, :42–44), `.codex/agents/pack-coder.toml`, `.agents-plugin/pack-agents/agents/pack-coder.md` — (E-RWDEF) replace cd-REUSE text with the workspace model; no-pin rationale per the wording note.

**F31. `.agents-plugin/pack-agents/RUNTIME-SUBAGENT-PATTERN.md`** — (E-RWDEF) :88 carrier: same workspace-model rewrite.

**NO-EDIT set (coder must NOT touch, verify-only):** `scripts/hooks/modes-enforce.py`, `scripts/hooks/modes-commit-gate.py`, `.claude/settings.json`, `scripts/tests/test-modes-enforce.sh`, `scripts/install-modes-hook.sh` (hook logic param-intent-only, unchanged); `pack-startup`/`pack-refresh`/`pack-intervention-mode`/`pack-review-mode` skills, `pack-ops/HELP-FRAGMENT-PACK.md` (zero phrase hits); `.gitignore`, `pack-ops/.dangling-ref-allowlist.txt`, `pack-ops/.spawn-rule-manifest.txt` (no renames/new tracked files; titles preserved via F4-d); dashboard render machinery (`scripts/dashboard-render.py`, `pack-ops/DASHBOARD-SPEC-PACK.md`, `dashboard-shell.html` — W4 handles the rendered mirror); `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md:189` (survives as default-mode truth — wording glance only, not contract-bearing); AGENTS.md / GEMINI.md (trinity-exempt section, EEB-PL7).

### §2.2 W1 verification gate (inside WS-1, before PREFLIGHT)

1. Sweep-zero audits (wrap-aware): `git grep -nE "then .?cd.?(-s it)? to the target tree"` → pack-side hits = ∅ (client hits + `dashboard.html` remain until W2/C4 — expected, Check 53 pattern not yet live and `.html` unscanned); `git grep -n "cd-REUSE"` → pack-side ∅; `git grep -nE "REUSES th|cd in \+ verify"` → pack-side operating surfaces ∅.
2. Family audits (AP-4, bound per EEB-PL16): `git grep -n "worktree-agent" -- .claude .codex .agents pack-ops` → remaining hits are regime-DETECT sense only (commit-discipline detect lines; OPTIONAL-FEATURES self-detect re-grounded); ZERO hits in the cycle-workspace/write-target sense (the :78/:104 class is gone).
3. Token presence: `git grep -c "only in its own worktree"` → present in F1 + F6–F8 (≥4 pack-side; F2/F4 summaries may add occurrences — permitted, Guard 2 asserts presence not exclusivity).
4. Check 71 pre-check: `cmp` each skill triple pairwise — byte-identical.
5. Check 54 existing tokens: all three survive in `pack-ops/OPTIONAL-FEATURES.md`.
6. Full battery in WS-1 (§6 command shapes): `validate-pack.py` plain + DEEP + every wired `scripts/tests/*.sh` (fixture build first where needed).
7. Trinity check: AGENTS.md/GEMINI.md untouched (`cd <WS> && git status --porcelain` shows only the 31 F-files).

---
## §3 W2 — project-side contract repair (commit C2, 12 files)

Mirror-but-customize (design §4.2; P-missed-7 satisfied — every concept maps to an existing client SSOT). Client vocabulary throughout: `coder`/`repo-ops`, PM chat, `docs/project/pm-session-config.json`, client workspace root `${XDG_STATE_HOME:-$HOME/.local/state}/optiquity-pm-workspaces/<task>-<timestamp>/` (the client persistent-state analog of the measured `optiquity-pm-handoff` convention at PM-CHAT.md:589 — mirror-but-customize, never the pack root). The §2 phrase-sweep, vocabulary, token-bytes, no-pin, and history rules all bind here identically.

**G1. `project-template/docs/pack/PM-OPERATING-MODES.md`** — (E-C1) REPLACE the `full` row (:43) with the client twin of F1's authored text (client vocabulary; KEEP the existing clean-channel clause and the PM-CHAT pointer); INSERT the client "How `full` executes" paragraph (twin of F1's, ≤8 lines). Token bytes verbatim.

**G2. `project-template/docs/pack/PM-CHAT.md`** — (E-C2) (a) :513–545 placement + `full` note + no-pin: :518 (cycle REUSES), :523 ("live worktree … cd into"), :532 ("then cd-s it to the target tree"), :539–541 (no-pin) → workspace model (PM-chat-created out-of-prefix workspace under the client workspace root above), `full` paragraph rewritten to the §2 contract, no-pin per the wording note. (b) :581–625 merge-back: :607 (cd-into + REUSES) → cycle-in-workspace + teardown wording; the :589 handoff-root convention text is UNTOUCHED. (c) **KEEP :547–549** ("no platform safety net" for a NON-isolated RW agent — still true).

**G3. `project-template/skills/pm-isolation-mode/SKILL.md`** — (E-C6) rewrite the `full` bullet (:24–25) to the G1 row text, short form, token bytes verbatim. Single copy (distributed at install by `stage_s4_skills()` — no mirror-triple obligation client-side).

**G4. `project-template/docs/pack/OPTIONAL-FEATURES.md`** — (E-C8) client twins of F5-b/c/d/e: :108 (fix-coder REUSES) and :140–145 (no-pin/frontmatter text incl. :143 "cd into and REUSE") → workspace model + no-pin wording note; the :250–300 feature-mechanics block gets the honest-bounds paragraph twin + channel-caveat twin; the `isolation_mode` paragraph (:285–296) gets the §2 one-line contract summary. **KEEP :291's fail-open sentence as written** (accurate error-behavior-only claim — OI2 needs NO client edit). Verify the three Check 54 tokens survive.

**G5–G10. Client RW defs ×6** — `project-template/.claude/agents/coder.md` (:40–66; :62–63 no-pin), `project-template/.claude/agents/repo-ops.md` (:28–48; carrier runs to :46–48), `project-template/.codex/agents/coder.toml`, `repo-ops.toml`, `project-template/.agents-plugin/optiquity-agents/agents/coder.md`, `repo-ops.md` — (E-C11) client twin of F28–F30: workspace wording; no-pin per the wording note; per-CLI format preserved. ALL SIX carry the "fix-coder reuse" phrase and the `.toml` pair additionally states the now-refuted "a pin would force a new worktree per spawn" rationale (EEB-PL16) — the no-pin rewrite replaces both. Client RO defs and client skills carry NO cd-in/reuse text (design EEB-10) — no other def edits.

**G11. `project-template/docs/pack/prompts/reviewer.md`** — (E-C12) REPLACE the :48–51 placement constraint with the design's authored text, verbatim: "When the coder's work is still uncommitted, the calling prompt injects the commit workspace's absolute path — target it per-call (`cd <WS> && …`), verify the workspace HEAD against the injected SHA, and read the in-progress work there, not the main checkout." Other prompt templates carry no contract text (design EEB-10 closure) — no edits.

**G12. `supporting-docs/METHODOLOGY.md`** — (E-SD) :766–778 light wording: coder waves run in per-commit workspaces; mechanics stay single-sourced in PM-CHAT. The :715 PM-CHAT pointer stays. Family sweep of :768/:771 worktree mentions per the vocabulary rule.

**NO-EDIT set:** `project-template/scripts/pm-modes-enforce.py`, `pm-modes-commit-gate.py`, client `.claude/settings.json`, `scripts/tests/test-pm-modes-enforce.sh` (param-intent enforcement unchanged); `project-template/scripts/pm-dashboard-render.py` (render machinery — the `worktree-agent-*` mention at :429 is a cosmetic branch-display filter, design §4.1 NO-EDIT row; AP-7); `pm-startup`/`pm-status`/`pm-refresh`/selector skills, `HELP-FRAGMENT.md`, `PLATFORM-SKILLS.md:480`, client trinity (zero contract text — design EEB-8/C13), `project-template/skills/implementation/SKILL.md:43–44` (regime-recording, stays true).

### §3.1 W2 verification gate (inside WS-2)

1. Sweep-zero audits: the §2.2 greps now return ∅ tree-wide on operating surfaces. Expected residual pattern-1 carriers at this gate (NOT failures): `backlog/BD-290.md` (record stream, allowlisted at C3) and `pack-ops/dashboard-approvals/dashboard.html` (rendered mirror — `.html` is outside `_CHECK_53_SCAN_SUFFIXES`; cleared by C4, per §8 item 5). A reviewer seeing exactly these two hits treats the gate as PASS.
2. Family audits (AP-4, bound per EEB-PL16): `git grep -n "fix-coder reuse"` → ∅ (all 6 measured carriers are in the G5–G10 edit set); `git grep -n "worktree-agent" -- project-template supporting-docs` → only `project-template/scripts/pm-dashboard-render.py:429` (display filter) + `project-template/skills/implementation/SKILL.md:43–44` (regime-recording) remain.
3. Token presence: `only in its own worktree` present in G1 + G3 (the two client Guard-2 surfaces).
4. **Client doc-gate:** run the SHIPPED `project-template/scripts/validate-docs.sh` full-scan against the workspace's project-template tree (no history/provenance tokens) + confirm Check 43 (bare cross-refs carry dir qualifiers) green in the battery.
5. Check 54 existing tokens survive in `project-template/docs/pack/OPTIONAL-FEATURES.md`.
6. Full battery in WS-2 (§6 shapes).
7. Scope check: `cd <WS> && git status --porcelain` shows only the 12 G-files (all under `project-template/` + `supporting-docs/` — the `project-only` keyword holds for Check 36).

---

## §4 W3 — CI guards + tests (commit C3, 5 code/test files + 2 preserved docs)

**H1. `scripts/lib/validate_checks/discipline_parity.py`** —
- (Guard 1) APPEND to `_CHECK_53_PROHIBITION_PATTERNS` (:690), exactly: `re.compile(r"then .?cd.?(-s it)? to the target tree")` and `re.compile(r"cd-REUSE")`. APPEND to `_CHECK_53_ALLOWLIST_DIR_PREFIXES`: `"backlog/"` and `"changelog/"` — extend the sizing comment to name the record-stream class (same legitimacy argument as the two `maintenance-docs/` prefixes; loosening safety measured: zero original-pattern hits in either stream, design EEB-13). No scan-loop change (both patterns ride the existing single `git ls-files` pass + per-file regex loop — `ci-check-runtime-compounding` preserved).
- (Guard 2) NEW module-local tuple: `_CHECK_54_MODE_CONTRACT_SURFACES = ("pack-ops/OPERATING-MODES.md", ".claude/skills/pack-isolation-mode/SKILL.md", ".codex/skills/pack-isolation-mode/SKILL.md", ".agents/skills/pack-isolation-mode/SKILL.md", "project-template/docs/pack/PM-OPERATING-MODES.md", "project-template/skills/pm-isolation-mode/SKILL.md")`. **ADD `"_CHECK_54_MODE_CONTRACT_SURFACES"` to THIS module's `__all__`** (the :2818 list, beside `"_CHECK_54_OPTIONAL_FEATURES_SURFACES"` at :2847, module-definition order) — the :2810 "every Cluster B symbol (the facade re-exports via `import *`)" contract; without the export the H5 Group-0 facade assertion fails (`hasattr(mod, '_CHECK_54_MODE_CONTRACT_SURFACES')` → False) (AP-2). EXTEND `check_optional_features_presence()` (Check 54 — NO new check number; `CHECK_REGISTRY_EXPECTED_COUNT = 91` and the ":209 Next free numeric ID = 95" ledger line UNTOUCHED, preserving BD-289's assigned IDs) with the mode-contract leg: for each of the 6 surfaces, read the file (absent file ⇒ FAIL — absence-of-backing) and assert each `_CHECK_54_MODE_CONTRACT_TOKENS` member as a literal substring; failure message follows the existing Check 54 message shape. Import the new core seam in the :58–65 import block. Update the docstring + the RUNTIME comment ("exactly TWO single-file reads" → the true count: 2 + 6 bounded single-file reads, no subprocess, no walk).
- (Reconciliation chain, `architect-doc-reality-reconciliation`) Check-header comments name the design by file+symbol, no line numbers: `_CHECK_53_PROHIBITION_PATTERNS` / `_CHECK_54_MODE_CONTRACT_TOKENS` per `maintenance-docs/v11-implementation/ARCHITECTURE-BD-290.md` §6.

**H2. `scripts/lib/validate_checks/core.py`** — NEW seam beside `_CHECK_54_REQUIRED_TOKENS` (:391–395), with its own cross-module-seam comment block: `_CHECK_54_MODE_CONTRACT_TOKENS = ("only in its own worktree",)` — lowercase, exactly these bytes. ADD `"_CHECK_54_MODE_CONTRACT_TOKENS"` to `__all__` (:519 area — the seam-export contract; the :479–483 header mandates every moved/underscore seam be listed).

**H3. `scripts/lib/validate_checks/singletons.py`** — `_check_4_dev_worktree_branch` docstring only: reconcile the `worktree-agent-<id>` mention (:249) to name BOTH worktree kinds (platform agent worktrees AND orchestrator-created detached-HEAD commit workspaces). Behavior unchanged (the check is name-agnostic — design EEB-17).

**H4. `scripts/tests/test-validate-pack-check-53.sh`** — extend the existing case machinery (synthetic throwaway git repos, tracked fixtures). Anchoring (AP-5): the per-pattern FAIL model is **Group 1's A/A2 cases** (A = pattern 1, A2 = "the second matcher branch"); Group 3 (:38–39) is the two lenient-SKIP pins — leniency proof, NOT the bite mechanism. For EACH new pattern — (i) FAIL leg: a NEW Group-1 case (A3/A4-style) with a tracked fixture in an active-surface path (e.g. `pack-ops/`) carrying the retired phrase (`then cd to the target tree` variant incl. the `cd`-s form / the literal `cd-REUSE`) that must FAIL, asserting the check's FAIL text names the pattern+file (mutation-bite: the FAIL text appears ONLY when the pattern is live — the pre-extension check against the same fixture must not produce it); (ii) PASS leg: the SAME string in a `backlog/`-pathed fixture PASSes (new allowlist prefix); (iii) one `changelog/`-pathed PASS leg for one of the two patterns (covers the second new prefix). Optionally thread one new-pattern prose fixture into Group 3's SKIP pins (keeps the leniency legs representative). Group 0 needs no sizing change (it asserts symbol presence only — EEB-PL5). Existing cases A–F unchanged.

**H5. `scripts/tests/test-validate-pack-check-54.sh`** — extend: (0) Group-0 facade assertions (AP-2): add `'_CHECK_54_MODE_CONTRACT_SURFACES'` and `'_CHECK_54_MODE_CONTRACT_TOKENS'` to the `required` hasattr list and assert EXACT tuple equality on both (`mod._CHECK_54_MODE_CONTRACT_SURFACES` = the 6-surface tuple in H1's order; `mod._CHECK_54_MODE_CONTRACT_TOKENS == ('only in its own worktree',)`) — the existing :54–88 idiom; this passes only with BOTH `__all__` exports (H1 + H2) in place. (i) baseline PASS — synthetic tree now provisions all 6 mode-contract surfaces carrying the token (plus the existing 2 OPTIONAL-FEATURES files with the 3 tokens); (ii) FAIL — token removed from `pack-ops/OPERATING-MODES.md`; (iii) FAIL — token removed from a MIRROR copy (e.g. `.codex/skills/pack-isolation-mode/SKILL.md`), proving the leg asserts per-file (defense-in-depth beyond Check 71); (iv) FAIL — one mode-contract surface file absent entirely (absence-of-backing); (v) sizing PASS (case-G analog) — a surface carrying the token but none of the OTHER §2-contract vocabulary still PASSes (the leg is sized to exactly 1 token × 6 files). Existing cases A–G unchanged.

**H6–H7. Preserved design docs (fold-in per OI-4; grounds corrected per AP-1, evidence EEB-PL13):** copy `ARCHITECTURE-BD-290.md` and `RECONCILIATION-BD-290.md` from the reconcile handoff dir into `maintenance-docs/v11-implementation/` IN C3 (basenames repo-unique, EEB-PL12; the dir is Check-53-allowlisted so their retired-phrase quotations are legitimate), and APPEND to the preserved ARCHITECTURE copy the reconciliation addendum (design §12.14-b): a short "Realized consumers" section naming `scripts/lib/validate_checks/discipline_parity.py` (`_CHECK_53_PROHIBITION_PATTERNS` extension, `check_optional_features_presence` mode-contract leg) and `scripts/lib/validate_checks/core.py` (`_CHECK_54_MODE_CONTRACT_TOKENS`) — file+symbol, no line numbers. The fold-in keeps the whole `architect-doc-reality-reconciliation` chain (in-code comment, addendum, IMPL-REPORT cross-refs) landing ATOMICALLY with the realized consumer — Check 68 does NOT scan `.py` sources and excludes `maintenance-docs/` (EEB-PL13), so this is co-landing coherence, not a CI requirement. **Conveyance (RP-1, EEB-PL17):** these two files are NEW — they cannot ride the `git diff` patch; §6 step 7's C3-specific copy step conveys them. The C3 IMPL-REPORT cross-references both ways (§12.14-c).

### §4.1 W3 verification gate (inside WS-3)

1. Both extended tests green: `bash scripts/tests/test-validate-pack-check-53.sh` and `…-54.sh` (0 FAIL) — incl. the new Group-0 facade assertions and Group-1 per-pattern FAIL legs.
2. Guard post-fix clean proof on the real tree: `python3 scripts/validate-pack.py` — Check 53 green (pattern-1 tracked hits = {`backlog/BD-290.md`} ⊂ allowlist — the H6/H7 copies are untracked in WS-3 and outside the `git ls-files` candidate set until they land; pattern-2 hits = allowlisted/self-source only) and Check 54 green (6/6 surfaces carry the token — authored by C1/C2).
3. Preserved-doc existence (replaces the v1 Check-68 gate line — AP-1): `test -f <WS-3>/maintenance-docs/v11-implementation/ARCHITECTURE-BD-290.md && test -f <WS-3>/maintenance-docs/v11-implementation/RECONCILIATION-BD-290.md` both succeed, and the ARCHITECTURE copy contains the "Realized consumers" addendum section (grep). POST-LAND (§6 step 8 battery on canonical HEAD): `git ls-files maintenance-docs/v11-implementation/ARCHITECTURE-BD-290.md` → non-empty (both files tracked).
4. Registry invariants: `CHECK_REGISTRY_EXPECTED_COUNT` still 91; ledger line unchanged (guards BD-289's IDs).
5. Full battery in WS-3 (plain + DEEP + every wired test).
6. Scope check: only H1–H7 files modified/added; commit keyword `pack-only` holds (all pack-side paths).

---
## §5 W4 — acceptance + close-out (orchestrator + Pack Chat; commits C4, C5)

**Step W4-1 — W-V2 end-to-end `full`-mode acceptance probe** (per the AMENDED AC, `backlog/BD-290.md:12`, verbatim in the working tree: "a read-only agent spawns isolated, verifies its regime against the orchestrator-injected canonical HEAD, runs read-only git verbs in its own worktree, reads a canonical file by absolute path, and writes its report to the handoff dir — no failure, async-task channel preserved"):
1. Orchestrator writes `isolation_mode: full` via the selector (gitignored `pack-ops/session-config.json` — EEB-PL11; same mechanics W-V1 already exercised).
2. Spawn one RO probe, named `probe-bd290-wv2` (session naming precedent), `isolation:"worktree"`, prompt injecting: canonical HEAD SHA, one canonical absolute file path to Read, the handoff dir `${XDG_STATE_HOME:-$HOME/.local/state}/optiquity-pack-handoff/bd290-wv2-<ts>/`, the graph path, the rules-in-force block.
3. Probe executes S3 (pwd = agent worktree; own `git rev-parse HEAD` = injected SHA; `git worktree list` cross-check), S4 (committed-state git in its OWN worktree — e.g. `git log -1`, one `git grep`), S6 (Read the canonical file by absolute path), S8 (Write report to the handoff dir). No cross-tree git at any point.
4. Orchestrator records the **channel observation** (S9): delivery arrived as a clean async completion notification, boilerplate-free — recorded in the W-V2 handoff record; this doubles as the §8 channel canary at landing time and the user's post-upgrade re-check recipe.
5. Orchestrator restores `isolation_mode: read-write-only` (the measured pre-probe value, EEB-PL11).
PASS = all four AC legs + channel record. FAIL on any leg ⇒ STOP; spawn `pack-architect` to diagnose (bounded-cycle rule) — do NOT proceed to the Resolved flip.

**Step W4-2 — full battery on canonical HEAD** (post-C3): both jobs' surfaces locally — `python3 scripts/validate-pack.py`, `PACK_VALIDATE_DEEP=1 python3 scripts/validate-pack.py`, every wired `scripts/tests/*.sh` (fixture build where needed) — output redirected per §6.

**Step W4-3 — commit C4, dashboard re-render:** orchestrator runs `python3 scripts/dashboard-render.py` (build→verify→rename semantics per its header); the regenerated `pack-ops/dashboard-approvals/dashboard.html` mirrors CLAUDE.md § Pack memory POST-C1, dropping the :110 "REUSES that worktree, NEVER a new one" carrier AND the embedded pattern-1 phrase (both measured surviving the stage-2 re-render at 34e8de7 — EEB-PL2; `.html` is outside Check 53's scan suffixes, so only the re-render clears them). Verify: `grep -a -o "REUSES[^.]*" pack-ops/dashboard-approvals/dashboard.html` → no retired-contract sense; `grep -a -c "then .cd" …/dashboard.html` → 0; **plus one plain `python3 scripts/validate-pack.py` run at this gate** (Check 86 — dashboard-approvals file cap — targets exactly the touched dir; AP-9). Stage exactly the `pack-ops/dashboard-approvals/` delta; user approval; commit (subject per §1, prefix per OI-2).

**Step W4-4 — commit C5, preservation + Resolved flip (Pack Chat direct; pack-chat-only content):**
1. Derive the preservation dir AT RUNTIME from the README version table (current major v11 ⇒ `maintenance-docs/v11-implementation/` — never hardcoded ahead of the read).
2. Copy the remaining handoff artifacts with repo-unique basenames (EEB-PL12; renames required — every source is an `IMPL-REPORT.md` collision otherwise), proposed set + names per OI-3: `PLAN-BD-290.md` (THIS reconciled doc — the executed plan); `RESEARCH-BD-290-ISOLATION-CENSUS.md` (census); `PROBE-BD-290-1-CROSS-TREE-GIT.md` (probe 1); `PROBE-BD-290-2-PREFIX-SCOPE.md` (probe 2/U2); `PROBE-BD-290-W0-WORKSPACE-GATE.md` (W0); `PROBE-BD-290-WV1-HOOK-BITE.md` (W-V1, incl. the measured pinned-def leg: pin not honored on the subagent_type path); `PROBE-BD-290-WV2-ACCEPTANCE.md` (W-V2 + channel record).
3. Flip `backlog/BD-290.md` `Status: Open` → `Status: Resolved`, fill the `Resolved:` line (date + one-line outcome citing the W-V2 probe + channel observation) — flip-in-place, no section move; gated on the AC amendment being LANDED (C0, or C5 itself under OI-1 option b).
4. Regenerate `backlog/_toc.md` via `per_entry_regenerate_toc pack-backlog /backlog` (never hand-edit — `backlog/_rules.md:89–91`). At this commit the flip does not change the toc row (ID + title only), so the regen is expected byte-identical — run it regardless.
5. Update `pack-ops/session-state.json` (REPLACE fields: BD-290 leaves the queue; no in-flight agents; boundary commit = C5).
6. Stage exactly this file set; show staged; user approval; commit.

**Step W4-5 — push mechanics:** before `git push`, orchestrator runs `scripts/manifest-sync.sh` (push-time manifest reconciliation — regenerates `test-fixtures/manifest.txt` iff a fixture input changed; none of C0–C5 touches `test-fixtures/`, so expect a no-op). Push with user approval; watch the `Validate Pack` run (both jobs) to completion.

---

## §6 Per-commit cycle mechanics — the §3 workspace ritual (user decision OI-G in force)

Applies to C1, C2, C3 (coder commits). C0/C4/C5 are Pack-Chat-direct/orchestrator mechanics (no coder, no workspace).

**Per commit N (WS-N = `${XDG_STATE_HOME:-$HOME/.local/state}/optiquity-pack-workspaces/bd290-w<N>-<ts>/`):**
1. **Create (orchestrator, canonical checkout):** `git worktree add --detach <WS-N> HEAD`. Create AFTER C0 lands so every workspace bases at the amended-backlog HEAD.
2. **Coder spawn** (fresh instance per commit; `run_in_background: true`): name per `spawn-unique-naming` — C1 `coder-bd290-packdocs`, C2 `coder-bd290-clientdocs`, C3 `coder-bd290-guards`; `isolation:"worktree"` (RW, mode-independent); record in the spawn registry. Prompt MUST carry: the task scope = EXACTLY that wave's file list (§2/§3/§4 — the disjointness is load-bearing: C1 ⊂ {pack-ops/, pack root, `.claude`/`.codex`/`.agents`/`.agents-plugin` pack dirs}; C2 ⊂ {project-template/, supporting-docs/}; C3 ⊂ {scripts/, maintenance-docs/v11-implementation/}); the injected WS-N absolute path ("your workspace; fuse `cd <WS-N> && …` into every Bash call; Writes/Edits to `<WS-N>/…` absolute paths"); the injected canonical HEAD SHA; the handoff dir `…/optiquity-pack-handoff/bd290-w<N>-<ts>/`; the graph path (EEB-PL15); the design-doc path + this plan's path; markdown-only outputs, chunked Writes over ~300 lines; the rules-in-force block (enumerated inline); PREFLIGHT + STOP-MEANS-STOP. **C1-specific prompt elements:** the trinity propagation procedure noted per the design §10 W1 row (`pack-ops/PACK-CHAT.md` § "Keeping … current"; the spawn-rule manifest needs no change — EEB-PL6) (AP-8), and the W-V1 pinned-def measurement stated as the definitive basis for the §2 no-pin wording (pin NOT honored on the subagent_type spawn path; per-spawn parameter only — the OI-5 fire-branch) (AP-10). NO solutions beyond the design/plan references; no prior review reports.
3. **Battery invocations (W0 anomaly-1 recommendation, adopted as spec):** every battery/test run redirects at invocation time so exit + tail are always inline: `cd <WS-N> && PACK_VALIDATE_DEEP=1 python3 scripts/validate-pack.py > <handoff>/battery-deep.log 2>&1; echo "EXIT=$?"; tail -n 5 <handoff>/battery-deep.log` (same shape for the plain run and each wired test; keep workspace commands simple, one action per call — the shape-matcher practice note).
4. **Reviewer spawn** (RO; `reviewer-bd290-<facet>`): placement per the live `isolation_mode` read at spawn from `pack-ops/session-config.json` (currently `read-write-only` ⇒ in-place spawn; under `full` ⇒ `isolation:"worktree"`); EITHER way the prompt injects WS-N and directs the G11-shape constraint: target WS-N per-call, verify workspace HEAD vs injected SHA, review the WIP there, run the FULL battery in WS-N. References: design + plan only.
5. **Triage gate:** Pack Chat reads the review, triages each finding (default FIX-ALL), presents to the user; user may override per finding.
6. **Fix-coder** (fresh instance, `fixcoder-bd290-<facet>[-2]`, RW isolated): targets the SAME WS-N. Bounded cycle: max 2 review/fix pairs + 1 final reviewer; dirty after that ⇒ STOP, spawn `pack-architect`.
7. **Patch (review-clean only):** SendMessage the most-recent RW agent: `cd <WS-N> && git diff > <handoff>/changes.patch`. **C3 addition (RP-1, EEB-PL17):** the same message also directs `cp <WS-3>/maintenance-docs/v11-implementation/ARCHITECTURE-BD-290.md <WS-3>/maintenance-docs/v11-implementation/RECONCILIATION-BD-290.md <handoff>/` — the two NEW files cannot ride `git diff` (untracked; the agent cannot stage), so the reviewed-clean copies travel via the handoff dir (a plain copy into the agent's owned dir — no git verb, no deletion).
8. **Land (orchestrator):** `git apply --check <patch>` then `git apply <patch>` in the canonical checkout; **C3 addition:** then `cp <handoff>/ARCHITECTURE-BD-290.md <handoff>/RECONCILIATION-BD-290.md maintenance-docs/v11-implementation/` (WS-3 remains the fallback source — it is kept until the commit lands); stage EXACTLY the wave's file list (targeted `git add <paths>`; under OI-1 option (a) the tree is otherwise clean except orchestrator-owned session-state — if session-state rides the commit it stays within the pack-only claim); `git status` + show staged; user approval (the approval message carries the next-steps list per the standing rule); commit; verify the battery on the new canonical HEAD (for C3 this includes the §4.1 gate-3 post-land `git ls-files` assertion).
9. **Teardown:** `git worktree remove <WS-N>` ONLY after the commit is confirmed landed (exit 0). Failed/aborted commit KEEPS WS-N as recovery fallback.

**Parallelization (rule-10 map):** C1 and C2 cycles run in PARALLEL (WS-1 ∥ WS-2; file sets disjoint by path family — verified §2/§3; reviews may also overlap). Commits LAND serially, C1 then C2 (fixed order; C2's patch applies cleanly to the post-C1 tree because the sets are disjoint). C3 is serial after both. W4 is serial after C3.

**Live-worktree ASK gate (rule 9):** while WS-1/WS-2 hold uncommitted work, any spawn OUTSIDE those commits' own cycles requires the user placement+disposition ask.

---

## §7 Dependency / verification matrix

| Commit | Depends on | Verification gate (all must PASS pre-approval) | Rollback posture |
|---|---|---|---|
| C0 | user approval | `python3 scripts/validate-pack.py` (plain) — the amendment is record-stream text; `_toc.md` regen RUN (`backlog/_rules.md:89–91`) but expected byte-identical (the amendment edits only the entry body's :12; the toc row carries ID + title only — AP-11); staged set = **2 files (3 only if the regen changes `_toc.md`)** | trivial (single-commit revert candidate; no workspace) |
| C1 | W0 PASS (done); C0 | §2.2 gate: sweep-zero (pack), family audits, token presence, Check 71 `cmp` triples, Check 54 tokens, FULL battery in WS-1 (plain + DEEP + all wired tests, redirected per §6.3), scope check 31 files; post-land battery on canonical HEAD | failed commit KEEPS WS-1; canonical untouched until `git apply` |
| C2 | W0 PASS; C0 (lands after C1 by fixed order) | §3.1 gate: sweep-zero (tree-wide operating surfaces, with the two expected residual carriers — record stream + dashboard.html), family audits, client doc-gate (`validate-docs.sh` full-scan) + Check 43, token presence (G1, G3), FULL battery in WS-2, scope check 12 files (`project-only` holds); post-land battery | failed commit KEEPS WS-2 |
| C3 | C1 AND C2 LANDED (guard atomicity, design §6) | §4.1 gate: test-53/54 green with the Group-0 facade assertions + Group-1 mutation-bite legs, real-tree Check 53/54 green, preserved-doc existence in WS-3 (`test -f` ×2 + addendum grep — AP-1 gate replacement), registry invariants (91 / ID-95 ledger untouched), FULL battery in WS-3; **land step includes the RP-1 new-file copy**; post-land battery + `git ls-files maintenance-docs/v11-implementation/ARCHITECTURE-BD-290.md` non-empty | failed commit KEEPS WS-3 |
| W-V2 | C3 landed | AC legs S3/S4/S6/S8 + channel record; mode restored to `read-write-only` | probe is read-only; re-runnable |
| C4 | C1 (mirror source); after C3 + W-V2 | re-rendered HTML passes the renderer's built-in verify; greps confirm BOTH retired texts gone (`REUSES…` + the pattern-1 phrase — EEB-PL2); one plain `validate-pack.py` run (Check 86 targets the touched dir — AP-9); staged set = dashboard-approvals only | re-render is idempotent; re-run on failure |
| C5 | C3, C4, W-V2 PASS; AC amendment landed | `validate-pack.py` plain + DEEP on canonical (Check 53 allowlist covers the new preserved docs; Check 68 basename/qualified refs resolve); `_toc.md` regen RUN (expected byte-identical); staged set exact | trivial; no workspace |
| push | C0–C5 | `scripts/manifest-sync.sh` (expected no-op), then push; `Validate Pack` workflow BOTH jobs green | worktree-free; CI failure ⇒ fix-forward per standing rules |

"FULL battery" everywhere = the `validate` job's two invocations (plain + `PACK_VALIDATE_DEEP=1`) + the `tests` job's complete wired set (`scripts/lib/ci-shard-plan.py` enumerates; fixture build first where a suite needs it) — per `verify-full-ci-suite` (EEB-PL9).

---

## §8 Cross-doc consistency checklist (every §4 design row × §7 OI resolutions)

Run by the C3 reviewer (tree-wide, post-C1+C2) and re-run by Pack Chat before C5:

1. **OI2:** pack `pack-ops/OPTIONAL-FEATURES.md` Hook-1 prose states fold-to-default (F5-a applied); client `:291` fail-open sentence UNCHANGED (`git diff` of C2 must not touch it).
2. **OI3 / claim-G symmetry:** `git grep -n "clean-channel"` now returns pack-side hits (`pack-ops/OPERATING-MODES.md` row + `pack-ops/OPTIONAL-FEATURES.md` caveat) in addition to the 3 client hits — asymmetry closed.
3. **OI4:** `git grep -n "fall-to-main"` → zero on operating surfaces (F5-b, F13–F15-c, G4 applied); the pwd/HEAD self-detect RULE still present everywhere it lived, re-grounded.
4. **OI5 / Guard 2:** all 6 mode-contract surfaces carry `only in its own worktree` (Check 54 leg green); Check 71 byte-locks the skill mirrors; narrative restatements (PACK-CHAT/PM-CHAT) point at the SSOT, no delimited-region drift guard (accepted residual, recorded in the design).
5. **Retired-family closure:** pattern-1 tracked hits = {`backlog/BD-290.md`} ∪ {`maintenance-docs/v11-implementation/ARCHITECTURE-BD-290.md`, `RECONCILIATION-BD-290.md`, and at C5 `PLAN-BD-290.md` + probe/census copies} — ALL under allowlisted prefixes; pattern-2 tracked hits on scanned suffixes = self-source/self-test only; `REUSES th`/`cd in + verify` = zero on operating surfaces; `dashboard.html` cleared by C4.
6. **Check 54 legacy tokens:** `baseRef`/`bgIsolation`/`permissions.deny` present in BOTH OPTIONAL-FEATURES files.
7. **Check 46:** manifest-referenced section titles intact (EEB-PL6 list); anti-restate clean.
8. **Trinity:** AGENTS.md/GEMINI.md byte-untouched by C1–C3 (`git log --stat` per commit); client trinity untouched by C2.
9. **History discipline:** `git grep -nE "73647|v2\.1\.2|Before v2" -- <the 43 edited operating files>` → zero (record streams exempt).
10. **no-leak (Check 93):** green in every battery — the authored prose is abstract (it is already, in the design's row texts).
11. **Vocabulary:** `git grep -n "live worktree"` on operating surfaces → zero ("live workspace" replaces it); "worktree" survives only for agents' OWN platform worktrees and git-mechanics text.
12. **Hook NO-EDIT proof:** `git diff C0..C5 -- scripts/hooks/ project-template/scripts/pm-modes-enforce.py project-template/scripts/pm-modes-commit-gate.py` → empty.
13. **OI-F honest-bounds landed (AP-6):** `git grep -n "out-of-prefix"` → present in BOTH `pack-ops/OPTIONAL-FEATURES.md` (E-P4-d paragraph) and `project-template/docs/pack/OPTIONAL-FEATURES.md` (E-C8 twin) — the one user decision that previously had no landing gate.
14. **Sweep-family audits (AP-4, bound per EEB-PL16):** `git grep -n "fix-coder reuse"` → ∅ on operating surfaces; `git grep -ln "worktree-agent"` → exactly the legitimate-survivor set — record streams (`backlog/BD-197.md`, `maintenance-docs/v11-implementation/*`), display filters (`scripts/dashboard-render.py`, `project-template/scripts/pm-dashboard-render.py`, `pack-ops/dashboard-approvals/dashboard-shell.html`), `pack-ops/DASHBOARD-SPEC-PACK.md`, `scripts/lib/validate_checks/singletons.py` (H3 wording), `scripts/tests/test-validate-pack-check-4.sh`, plus regime-DETECT-sense survivors in the commit-discipline/implementation-report triples and `pack-ops/OPTIONAL-FEATURES.md` — with ZERO cycle-workspace/write-target-sense hits anywhere.

---
## §9 Open items (context + options + recommendation each)

- **OI-1 — the two uncommitted working-tree files vs the staging ritual. USER DECISION (cheap).** Context: `backlog/BD-290.md` (AC amendment) and `pack-ops/session-state.json` are modified but uncommitted (re-verified at 34e8de7 — the mid-stream dashboard re-render resolved the transient third file; C0's list needs NO delta); the documented staging ritual is `git add -A && git status` (CLAUDE.md § Rules), which at C1 time would silently sweep both into a coder commit; the design's ORCH row assigns them to Pack Chat bookkeeping. Options: (a) land them FIRST as commit C0 (`docs:` form, pack-only) — the amendment is already user-approved governance, so C0 is pure bookkeeping, and every later commit starts from a clean tree where `git add -A` is safe; (b) carry them uncommitted through C1–C4 with targeted `git add <paths>` at every commit, landing the amendment inside C5. Recommendation: **(a)** — evidence: three coder commits × two carried files = six chances for accidental inclusion; the workspace worktrees base at HEAD either way, so (a) also makes the workspaces see the amended AC; the flip-gate (design §11 OI-H) requires the amendment LANDED before the C5 flip in any case. Staged-set expectation per AP-11: 2 files (the `_toc.md` regen is byte-identical for this amendment — toc row is ID + title only, measured).
- **OI-2 — C4 commit-message prefix. USER DECISION (at the C4 gate).** Context: the CLAUDE.md commit-format block enumerates `feat:`/`fix:`/`docs:`; the dashboard re-render is a mechanical regeneration. Precedents at the current HEAD's own history (updated per AP-3): the NEWEST and most-exact analog is **34e8de7 `docs: v11 — dashboard re-render at BD-290 stage 2 (pack-only)`** — exactly a dashboard re-render, landed mid-stream by the orchestrator; the BD-288 close-out used `chore:` for a mechanical regen (3b75ca6) beside a `docs:` preservation commit (7d43b01). Options: (a) `docs: v11 — BD-290 W4 dashboard re-render (pack-only)` per the newest exact analog (and an enumerated prefix); (b) `chore: v11 — BD-290 W4 dashboard re-render (pack-only)` per the 3b75ca6 regen precedent; (c) fold the re-render into C5's `docs:` commit (single close-out commit). Recommendation: **(a)** — evidence: 34e8de7 is a live, user-approved precedent for EXACTLY this action under an enumerated prefix; the user approves the exact subject at the commit gate regardless.
- **OI-3 — preservation set + renamed basenames. USER DECISION (at the C5 gate).** Context: the handoff artifacts are all named `IMPL-REPORT.md`, colliding with each other and with nothing tracked (EEB-PL12); the preservation dir convention uses typed prefixes (`ARCHITECTURE-`/`PLAN-`/`RESEARCH-`). Options: (a) preserve the full 9-artifact set with the §5 proposed unique names (the preserved `PLAN-BD-290.md` = THIS reconciled plan, the executed one); (b) preserve only design + reconciliation + plan (minimum reconciliation-chain set) and leave probe/census reports in the handoff dirs. Sub-question surfaced at reconciliation: whether the plan-review chain (`ADVERSARIAL-PLAN-REVIEW-BD-290.md`, `PLAN-RECONCILIATION-BD-290.md`) also preserves — precedent (7d43b01/BD-288) preserved design-side artifacts; review reports have no preservation precedent and their substance is folded into this plan. Recommendation: **(a)** with the review-chain docs NOT preserved (their content is folded here; the user may override at the gate) — evidence: BD-290's AC says "root cause established with reproducible captured evidence" — the probe reports ARE that evidence, and handoff dirs are per-clone transient state outside the repo; all proposed basenames verified repo-unique (EEB-PL12).
- **OI-4 — design-doc preservation split across C3/C5 (a refinement of the design's W4 row; rationale corrected at reconciliation per AP-1).** Context: H1's in-code comment cites `maintenance-docs/v11-implementation/ARCHITECTURE-BD-290.md`. Check 68 does NOT make this reference CI-resolvable — `discipline_parity.py` is outside its scan scope and `maintenance-docs/` is an exclude prefix (EEB-PL13, corrected) — so the fold-in is a sequencing choice, not a CI requirement. It stands on `architect-doc-reality-reconciliation` co-landing coherence: the chain (in-code comment naming the design, the preserved doc + "Realized consumers" addendum, the IMPL-REPORT cross-refs) lands ATOMICALLY with the realized consumer; a C5-time copy would leave C3's comment citing a repo path that exists only two commits later, and would split the reconciliation chain across the review boundary. Options: (a) fold the ARCHITECTURE + RECONCILIATION copies (+ addendum) into C3 (H6/H7), preserving the rest in C5; (b) keep all preservation in C5 (the comment's path reference stays equally CI-unverified either way). Recommendation: **(a)** — the chain-coherence ground above; the cost is two extra files in C3 conveyed per RP-1. This moves a W4 item one wave earlier — no work leaves BD-290 and every commit stays green, so it is sequencing, not scope change; flagged here because it adjusts the design's wave map.
- **OI-5 — W-V1 pinned-def leg (RESOLVED).** Context: at plan v1 the leg was in flight. It has since MEASURED: on the Agent-tool subagent_type path, a scratch def pinning `isolation: worktree` in frontmatter, spawned WITHOUT the per-spawn parameter, ran IN-PLACE with delivery on the mailbox channel — the pin is NOT honored; only the per-spawn parameter isolates. Other invocation paths unmeasured — every surface scopes the claim to this path. Resolution folded into §2's no-pin binding instruction (the measured second sentence authored UNCONDITIONALLY) and injected into the C1 coder prompt (§6 step 2 — AP-10). The design's hook-payload-visibility corollary stays UNWRITTEN: the design keys it on measuring the corollary claim itself, and the leg measured honored/not-honored BEHAVIOR while the hook fail-opened on the scratch def's unknown class (the payload view was never probed) — the adversarial suggestion to author it is REJECTED on this evidence. No user decision needed.
- **OI-6 — C3 new-file conveyance (RP-1, reconciliation-found; surfaced for the gate, orchestrator mechanics).** Context: H6/H7 are the only NEW files in the coder commits; plain `git diff` cannot carry untracked files and the coder cannot stage (EEB-PL17), so the documented patch ritual alone would drop them (caught loudly at the targeted `git add`, but unplanned). Options: (a) orchestrator copies the two files out of WS-3 at land time (workspace is kept until the commit lands); (b) the RW agent, at the patch step, also copies the two reviewed-clean files into its handoff dir; orchestrator applies the patch then copies them from the handoff into the canonical tree (WS-3 remains the fallback). Recommendation: **(b)** — the handoff dir is the agent's owned output surface and survives independent of workspace lifecycle; the copy is a plain file write with no git verb; the reviewed-clean provenance is preserved (the copies are taken AFTER the review-clean gate, in the same message as the patch). Folded into §6 steps 7–8.
- **No other open items.** Every design §4 row is mapped (§2/§3/§4/§5); no row is deferred; the W0 fallback is dead (all legs PASSED); no size/blocked/fit concern surfaced.

---

## §10 Rules-Applied Verification Block (reconciled plan)

1. **agents-never-commit** — Evidence: git inventory across the planning + reconciliation runs: `git rev-parse HEAD`, `git status --porcelain`, `git worktree list`, `git log`, `git show --stat`, `git grep` (family/token/audit measurements), `git ls-files | grep`, `git diff` (read-only patch-emit family), `git check-ignore` — read-only verbs only; zero state-changing git verbs; working tree left exactly as received (` M backlog/BD-290.md`, ` M pack-ops/session-state.json`). Conclusion: **COMPLIANT**.
2. **per-action-approval-sub-agents** — Evidence: reconciliation writes = `PLAN-BD-290.md` + `PLAN-RECONCILIATION-BD-290.md` in the owned dir `/Users/david/.local/state/optiquity-pack-handoff/bd290-planrec-20260825T173350Z/`; zero deletions/overwrites anywhere; all input handoff dirs read-only. Conclusion: **COMPLIANT**.
3. **preflight-stop-means-stop** — Evidence: the PREFLIGHT line emitted immediately before the reconciliation Writes; no stop/halt message received. The §6 spawn specs carry PREFLIGHT + STOP-MEANS-STOP into every coder prompt. Conclusion: **COMPLIANT**.
4. **graph-first-context** — Evidence: EEB-PL15 — graph exists (11,721,659 bytes, path injection specified for every §6 spawn); neither run raised a NEW discovery question (carrier universe closed by design EEB-10; its graph-first attempt recorded in design EEB-16 — three graph-empty instances ⇒ sanctioned G2 fallback); all reads were verification reads of named surfaces (fall-through (i)/(ii)/(iv)). Conclusion: **COMPLIANT**.
5. **empirical-evidence-blocks** — Evidence: §0 EEB-PL1..PL17, each with the actual command, captured output, HEAD (34e8de7 for (rec) entries; 3cf436e for (v1) entries with the inter-HEAD delta bounded to dashboard.html), interpretation, verdict. Conclusion: **COMPLIANT**.
6. **cross-bd-collision-scan** — Evidence: EEB-PL14 — the plan's added-file delta intersected against the §9 open-BD surface sets; delta verdict NONE-new; the design's 3-COORDINATE/0-blocker conclusion stands (no new file added at reconciliation — RP-1 changes conveyance, not the file set). Conclusion: **COMPLIANT**.
7. **operating-docs-no-history-no-bloat** — Evidence: §2's binding "History discipline" instruction carries the constraint into every coder-facing edit intent; the no-pin wording strips all provenance; §8 item 9 makes it a grep-audited gate; the reconciliation folds added NO provenance to any authored surface text. Conclusion: **COMPLIANT**.
8. **dependency-direction-placement** — Evidence: every §2/§3 file list keeps pack and client copies separate (F6–F8 vs G3; F9–F11 pack-side; G11 client-side); no dual-use file; the only NEW tracked files (H6/H7 + the C5 preservation set) are pack-side `maintenance-docs/`; `_SANCTIONED_PACK_SIDE_SHIPPED` untouched. Conclusion: **COMPLIANT**.
9. **ci-check-runtime-compounding** — Evidence: §4 H1 preserves the design's bounds — Guard 1 rides the existing single `git ls-files` pass; Guard 2 adds 6 bounded single-file substring reads; the RUNTIME comment update documents the true cost. Conclusion: **COMPLIANT**.
10. **verify-full-ci-suite** — Evidence: §7 defines "FULL battery" as both workflow jobs' surfaces per EEB-PL9 and every coder-commit row names it; §6.3 fixes the redirected invocation shape; C4's gate gains the plain validate-pack run (AP-9); no vacuous gate line survives (the v1 Check-68 line is replaced by direct assertions — AP-1). Conclusion: **COMPLIANT**.
11. **open-item-surfacing** — Evidence: §9 OI-1..OI-6 each carry context + options + an evidence-based recommendation; none recommends from memory; none defers work out of BD-290. Conclusion: **COMPLIANT**.
12. **memory-not-an-ssot** — Evidence: every rule/contract claim cites a live in-repo path read at planning or reconciliation (file:line throughout §0–§8) or a named handoff artifact by path; the config value used (`isolation_mode`) was read live (EEB-PL11). Conclusion: **COMPLIANT**.
13. **public-bound-no-leak** — Evidence: this doc names no target-app or domain-vocabulary token; all quoted strings are pack-generic contract text; the doc lives in the out-of-repo handoff dir; §8 item 10 keeps Check 93 in every gate. Conclusion: **COMPLIANT**.
14. **no-deferral-without-user-direction + deferral-is-scope-creep** — Evidence: every design §4 row lands in C1–C5; the §9 items adjust sequencing/mechanics only; the sole wave-map change (OI-4) moves work EARLIER; all reconciliation folds land inside C0–C5 in v11.0. Conclusion: **COMPLIANT**.
15. **filename-uniqueness-heuristic** — Evidence: EEB-PL12 — `git ls-files | grep -i "bd-290"` → only `backlog/BD-290.md`; all new preserved basenames carry the BD-290 token and are repo-unique by construction. Conclusion: **COMPLIANT**.
16. **rules-applied-verification-block** — Evidence: this block — 16 entries with quoted/measured evidence and terminal conclusions; no AMBIGUOUS state. Conclusion: **COMPLIANT**.

**Deliverable:** `/Users/david/.local/state/optiquity-pack-handoff/bd290-planrec-20260825T173350Z/PLAN-BD-290.md` (RO agent; owned-dir write; no repo edits; no patch). Adjudication log beside it: `PLAN-RECONCILIATION-BD-290.md`.
