# Apple app plus Python server template

This template is for repositories that keep Apple client code and Python server code together.

## Suggested layout

- Apple app code at repo root or in `App/`
- Python server code under `server/`
- tests under `server/tests/`
- repo automation under `scripts/`

## Recommended commands

```bash
./scripts/bootstrap.sh
./scripts/validate.sh
```
