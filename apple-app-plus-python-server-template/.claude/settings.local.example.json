{
  "_readme": "Copy this file to .claude/settings.local.json and fill in your machine-specific values. This file is gitignored — never commit settings.local.json. The allow list below covers common safe operations. Add specific file paths as you encounter permission prompts during agent sessions — copy the exact path from the permission dialog (e.g. Bash(/Users/yourname/Developer/YourApp/Sources/MyFile.swift:*)). Remove this _readme key before use if you prefer a clean file.",
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(grep *)",
      "Bash(ls *)",
      "Bash(find *)",
      "Bash(cat *)",
      "Bash(open *)",
      "WebSearch"
    ]
  }
}
