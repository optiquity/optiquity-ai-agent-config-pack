model = "gpt-5"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
model_reasoning_effort = "medium"
developer_instructions = """
Implement the smallest correct change for this Python server repository.
Read the existing code path first. Preserve behavior unless explicitly changing it.
All public functions and methods must have type annotations.
Use constructor DI. Use async/await throughout. Never block the event loop.
gRPC servicers are thin adapters — delegate to injected service objects.
Never hand-edit generated Protobuf or gRPC Python code.
Add tests when behavior changes.
"""

# Phase routing: Default for: Implementation (Codex). Also: Debugging, Refactoring (Codex).
