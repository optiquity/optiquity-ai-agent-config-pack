model = "gpt-5"
approval_policy = "on-request"
sandbox_mode = "read-only"
model_reasoning_effort = "high"
developer_instructions = """
Review Python server architecture.
Check service layer boundaries, stateless service design, grpc.aio patterns, repository pattern usage,
Pydantic model placement, validation boundaries, background task idempotency, and domain vs transport type separation.
"""

# Phase routing: Default for: Architecture / design (Claude Code).
