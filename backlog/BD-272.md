<!-- per-entry source: /backlog/BD-272.md; contract: /backlog/_rules.md -->
**BD-272 — Eradicate the `pack td` boundary violation (mis-homed, client-advertised, never-shipped project-side TD-promotion workflow)**
Type: fix — boundary-violation eradication; precursor to BD-257. `pack td` (BD-107) wired a PROJECT-side workflow (TD-entry → phase promotion) through the pack `pack <noun>` CLI namespace, placed the backing pack-side (`scripts/pack-td.sh` + `scripts/lib/tracker-promote.sh`, marked `# pack-internal: true`), advertised it on the CLIENT surface, but never shipped the backing (`init-project.sh` `stage_s5_scripts` copies only `project-template/scripts/*`). The pack repo has no TD entries; the verb is dead in practice (no `pack` dispatcher, no external caller, only its own 3 tests exercise it). Design verdict (docs-researcher → architect → adversarial → reconciliation, 2026-07-23): an oversight on the ship axis, UNIQUE (only live-advertised-yet-unshipped verb), DEAD → eradicate.
Status: Open
Blockers: none.
Position: PRECURSOR to BD-257 (the project-side slash-command sibling — direct surface collision on `project-template/docs/pack/{HELP-FRAGMENT.md,PM-CHAT.md}` + the client command surface); lands BEFORE BD-257 design. BD-136 note-level (removing a normal H2 leaves the marker grammar untouched).
Unblocks: a clean client command surface for BD-257 (no mis-homed / unshipped verb advertised); boundary restored.
File/Symbol:
  - DELETE: `scripts/pack-td.sh`, `scripts/lib/tracker-promote.sh`, `scripts/tests/test-tracker-promote-path1.sh`, `scripts/tests/test-tracker-promote-path2.sh`, `scripts/tests/test-tracker-promote-direct.sh`, `scripts/tests/fixtures/tracker-promote/`
  - EDIT (client): `project-template/docs/pack/HELP-FRAGMENT.md`, `project-template/docs/pack/PM-CHAT.md`, `project-template/scripts/.docs-gate-allowlist.txt`
  - EDIT (shipped/pack): `supporting-docs/METHODOLOGY.md`, `README.md`, `pack-ops/CONCEPTUAL-REVIEW-METHODOLOGY.md`, `scripts/ci-shard-weights.tsv`, `scripts/tests/pack-help-test.sh`, `scripts/lib/validate_checks/help_fragments.py`, `scripts/lib/tracker-edit.sh`, `scripts/lib/tracker-migrate-forward.sh`
Resolution gate (user, 2026-07-23): NOT resolved until a COMPLETE fresh audit (grep-to-zero census of the LIVE surfaces) confirms NO `pack td` residue remains AND the user explicitly approves the audit. The bounded review/fix cycle on the eradication commit does not satisfy this — a separate post-commit audit + user sign-off is required.
Description: Execute the reconciled eradication manifest. **Delete** the dead machinery (dispatcher, orchestration lib, 3 promotion tests, fixtures dir). **Strip** the `pack td` advertisement from the client surfaces and reword the stale GH-issue label mechanics (`promoted-to:` / `derived-from:` / `folded-into:`) to flat-file terms while KEEPING the verb-independent TD-resolution decision model (OI-2 **Option X**) in `HELP-FRAGMENT.md`, `PM-CHAT.md`, and the shipped `supporting-docs/METHODOLOGY.md`. **Reconcile** the CI interlocks so the battery stays green — the `scripts/ci-shard-weights.tsv` rows for the deleted tests, the `pack-help-test.sh` `2.1`/`2.2` assertions (the client-rows-PRESENT `2.2` reconciled this session at `97277d6` flips to ABSENT), the client docs-gate allowlist liveness (`.docs-gate-allowlist.txt` rows whose sole corpus backing is the removed `PM-CHAT.md` lines), the `README.md` `scripts/` listing, the `help_fragments.py` cosmetic exclusion, and the dangling comment cites in `tracker-edit.sh` / `tracker-migrate-forward.sh`. **CLASS-3 historical record** (`backlog/`, `maintenance-docs/`) is UNTOUCHED — scrubbing the record is a defect; the tracked `pack-ops/dashboard-approvals/dashboard.html` is a generated artifact that regenerates and legitimately embeds BD-107's historical text (never hand-edited). One atomic cross-surface commit, no scope keyword (spans pack + client). **Delete-now** (OI-1); if a clean project-side TD-promotion verb is ever wanted it is authored fresh under BD-257 as a proper shipped project deliverable — never a `pack td` revival. Design chain (this session, 2026-07-23): the pack td blast-radius research + eradication architect + adversarial + reconciliation docs (moved into `maintenance-docs/v11-implementation/` in the paired report commit).
Resolved:
