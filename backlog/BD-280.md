<!-- per-entry source: /backlog/BD-280.md; contract: /backlog/_rules.md -->
**BD-280 — Dashboard doc-index staleness: the spec-embedded `maintenance-docs/` list references deleted docs (BD-210 fallout) — drop stale entries or make it live-derived**
Type: chore — pack-dev tooling fix (dashboard convenience view; never SSOT, not CI-gated).
Status: Open
Target: v11.0 (surfaced during BD-210 v11.0 cleanup 2026-08-02; NON-BLOCKING — the dashboard is a pack-dev convenience view, never source of truth, not CI-gated). Lands in v11.0 per no-deferral unless the user defers.
Blockers: None.
Unblocks: an accurate dashboard doc-index with no dead links to deleted `maintenance-docs/`.
File/Symbol (candidates — architect/coder confirms): the dashboard doc-index list (rendered at `pack-ops/dashboard-approvals/dashboard.html` ~L110) is SPEC-EMBEDDED — authored in `pack-ops/dashboard-approvals/dashboard-shell.html` and/or specified in `pack-ops/DASHBOARD-SPEC-PACK.md`; `scripts/dashboard-render.py` if the list should become live-derived.
Description:
  **Problem:** the pack dashboard embeds a curated doc-index (~18 `maintenance-docs/` paths) that is SPEC-EMBEDDED in the fingerprinted shell, NOT live-derived from the tree. BD-210 deleted 861 `maintenance-docs/`; ~10 doc-index entries now point at deleted files (dead links). A re-render does NOT fix it — the shell is reused unless the `spec-sha` changes. Cosmetic (convenience view, never SSOT, not CI-gated) but untidy.
  **Goal:** the doc-index references only existing docs.
  **Options (architect/coder decides):** (a) drop the deleted entries from the spec's curated list — quick, but re-staleable on the next deletion; (b) make the doc-index LIVE-DERIVED so the renderer scans the actual `maintenance-docs/` tree and auto-stays-current — more robust, the preferred long-term shape. Recommend (b) if cheap, else (a).
  **Provenance:** surfaced by the BD-210 census (§4-A) + the BD-210 fix-reviewer; deferred from BD-210 as out-of-scope (BD-210 = maintenance-docs cleanup, not a dashboard-spec change).
Acceptance criteria: the dashboard doc-index contains NO reference to a non-existent `maintenance-docs/` file; if made live-derived, a subsequent deletion auto-updates it without a manual spec edit; `validate-pack` green; the dashboard re-renders + verifies clean.
References: BD-210 (the deletion that surfaced this; its census §4-A "dashboard.html:110 cosmetic doc-index"); `pack-ops/DASHBOARD-SPEC-PACK.md`; `scripts/dashboard-render.py`; `pack-ops/dashboard-approvals/dashboard-shell.html`.
Resolved: n/a
Position: v11.0 — cosmetic pack-dev tooling follow-up; surfaced during BD-210 (2026-08-02); NON-BLOCKING (the dashboard is a convenience view). Slot whenever convenient (after BD-205/093 or opportunistically when the dashboard spec is next touched).
