<!-- per-entry source: /backlog/BD-231.md; contract: /backlog/_rules.md -->
**BD-231 — Dangling `.mcp.json.example` references: the file is absent from `project-template/` (add-file vs. repoint is a user decision)**
Type: fix — broken-reference repair. Multiple pack and client-shipped files reference `.mcp.json.example`, but that file does not exist in `project-template/` (the Antigravity successor `.agents/mcp_config.json.example` does). Repair the dangling references; whether to ADD the missing file or REPOINT to the existing one is the user's call. Opened 2026-06-18 (user direction).
Status: Open
Target: v11.0 — broken-reference correctness; must be resolved before launch. Surfaced as out-of-scope finding F-3 during the BD-221 conversion cluster (C11 review, supporting-docs scope); converted to a tracked BD per user direction 2026-06-18 rather than fixed inline.
Blockers: none.
File/Symbol (measured 2026-06-18, HEAD `ef74053`):
  - Absent target: `project-template/.mcp.json.example` does NOT exist. `project-template/.agents/mcp_config.json.example` DOES exist (the Antigravity MCP example shipped by the BD-201/BD-221 conversion). `.mcp.json.example` exists ONLY under `test-fixtures/v10-*` (legacy fixtures), not as a live `project-template/` deliverable.
  - References that resolve to the absent file: `README.md:139` (Repository Layout, `project-template/` block); `project-template/README.md:20`; `project-template/docs/pack/PM-CHAT.md:824`; `supporting-docs/INSTALL-PROCEDURES.md:633`, `:793`, `:798`; `supporting-docs/SETUP-NEW.md:309` (`cp .mcp.json.example .mcp.json`).
  - (Out of scope: `changelog/`, resolved-BD bodies, `maintenance-docs/`, `scripts/` test/lib code, and `test-fixtures/` — these are history, internal, or fixture references, not live client deliverable instructions. The fix-coder confirms the exact live-reference set at fix time.)
Description:
  **Problem:** The pack instructs clients to use `.mcp.json.example` (e.g. `cp .mcp.json.example .mcp.json`) and lists it in the layout, but the template no longer ships it under `project-template/`. A client following `SETUP-NEW.md:309` would hit a missing-file error. This is a genuine broken reference, not cosmetic.
  **Decision required (USER):** add-file vs. repoint, because the two MCP example files serve different tools — `.mcp.json.example` is the Claude-Code-side MCP template (Claude reads `.mcp.json`), while `.agents/mcp_config.json.example` is the Antigravity-side template. Options: (a) ADD a `project-template/.mcp.json.example` (Claude-side template; if the pack intends Claude clients to have an MCP example, this restores the documented file); (b) REPOINT the references to `.agents/mcp_config.json.example` (if the Antigravity file is now the single shipped MCP example and the Claude-side example was intentionally dropped); (c) a mix — keep both files and disambiguate each reference to the correct per-CLI template. The correct choice depends on whether the pack still intends to ship a Claude-side MCP example — a deliverable-intent question only the user can settle. The eventual fix applies the user-chosen option.
  **Solution:** per the user-chosen option, either add the missing template or repoint every live reference to the correct existing template, so no client-facing instruction or layout entry names a non-existent file; per-CLI reference normalization applies where a reference is CLI-specific (`cross-cli-reference-normalization`).
Acceptance criteria: every live client-facing reference to an MCP example file resolves to a file that exists in `project-template/`; no `cp`/setup step names a missing file; the layout entry matches reality; per-CLI references name the audience-correct template; validate-pack green; a grep gate confirms no dangling `.mcp.json.example` reference remains in live deliverable docs.
References: surfaced as F-3 (out-of-scope finding, BD-221 cluster C11 review); BD-201 / BD-221 (the Antigravity MCP relocation that produced `.agents/mcp_config.json.example`); `cross-cli-reference-normalization` (per-CLI canonical reference substitution); `client-ref-delete-or-forward-look` (forward-look a real project asset); the `out-of-scope-fix-immediately` rule (the origin-mode that this BD-conversion deviates from, by explicit user direction 2026-06-18).
Resolved: n/a
Position: v11.0 — broken-reference repair; independent of BD-229 and BD-230. Needs a user option decision (add vs. repoint vs. mix) before the fix-coder runs.
