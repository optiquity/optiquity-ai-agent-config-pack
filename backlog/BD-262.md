<!-- per-entry source: /backlog/BD-262.md; contract: /backlog/_rules.md -->
**BD-262 — Groupings stream foundation: format contract + per-entry tuple + toc axis + groupings-lib.sh (BD-189 CBD-1)**
Type: feat — STRUCTURAL project-side (client deliverable). The BD-189 Wave-1 foundation child: the grouping doc format contract, the fourth per-entry stream, and the shared library every other child consumes. Opened 2026-07-05 at C0 per PLAN-BD189-FOLDED.md F1 (the user-approved folded plan).
Status: Open
Target: v11.0 — Wave 1 (solo; the API-freeze foundation for the Wave-2 five-way parallel).
Blockers: none (opened unblocked at C0).
Unblocks: BD-263, BD-264, BD-265, BD-266, BD-267, BD-268.
File/Symbol:
  - NEW `project-template/docs/project/groupings/_rules.md`
  - NEW `project-template/docs/project/groupings/_intro.md`
  - `scripts/lib/per-entry/_lib.sh` (`PE_STREAM_KEYS` + attr arms)
  - `scripts/lib/per-entry/toc-regenerate.sh` (project-groupings axis)
  - NEW `project-template/scripts/groupings-lib.sh`
  - `scripts/tests/test-per-entry.sh` (tuple + toc coverage)
  - NEW `scripts/tests/test-groupings-lib.sh`
  - `scripts/tests/test-validate-docs-target-coherence.sh` (the C1 extension: lib golden + rider-1 geometry + third-reader legs)
  - `project-template/scripts/.docs-gate-allowlist.txt` (ONLY iff the two new shipped docs need exact-sized records — measure at implementation against the four-allowlist baseline)
Description:
  Scope (verbatim from PLAN-BD189-FOLDED.md F1/BD-262): D1 grammar as amended (GQ-1 bold-pair; GQ-2 fixed 10-enum; GQ-3 field set; F9 byte grammar; F17 opaque `Doc-links:`); D2 tree (`_rules.md` + `_intro.md`; generated `_toc.md`; NO `_kinds.md`); D2.2 stream tuple; D2.3 toc axis; RS-1 (full reserved-GRP-000 contract incl. `reserved-id:` key); D3.0 lib incl. RS-4 (`grp_scan` reserved flag + `grp_real`), the A7 hardcode + TIGHTENED entry regex `^GRP-(\d{3}|[1-9]\d{3,})\.md$`, the three-function derived-status API (`grp_phase_status_map` / `grp_rollup_map` / `grp_rollup`) computing the §T-1 B′ table with the pinned flag predicates; the cascade lib function (item-5 s1) — sources = phases with `Status:` ∈ {deferred, superseded}; reversed-edge transitive closure over the FOUR-field grammar (`_conf_index_edges` semantics, validate-docs.sh:833-835); per-phase attribution; grouping annotation via `grp_reverse_map`; the S-4 status-aware join (superseded-excluded counts for M / moment-3 PENDING / K — K = declared-stays-ungrouped LIVING phases); the target surface (§T-2): `grp_phase_target_map` (EPIC-ONLY pin) + `grp_implied_target_map` (no-fourth-parse pin; `via=-` pin) + the amended row grammar (`tgt=`/`t=` under the G-1-confirmed NON-DONE AND NON-SUPERSEDED declarer set) + the G-12c ALL-ACTIVE-incl-done poison + the F13 order-stability note (lib header comment: token relative-order freeze; fixture asserts lib-ordinal == contract declaration order); lib + tuple + toc tests incl. the B′ golden set, the target golden set, and the agreement-fixture EXTENSION.
  The agreement-test extension (Q1-arm-A; the landed contract at test :15/:31-33 — "the gate golden is never re-authored"): EXTENDS `scripts/tests/test-validate-docs-target-coherence.sh` with (a) the `grp_implied_target_map` row-set golden derived from the SAME normative table; (b) rider 1 — one new geometry: a conflict pair + NO `_index.md`; the golden asserts BOTH the conflict line AND the missing-index FAIL (the :1089-1094 placement's first committed bite); (c) the third reader — `target-sweep.sh` reads the same fixture tree (tool vs gate vs lib — the cross-reader triangle closed). Fixture pins: every fixture entry carries an explicit `Entry-Type`; the fixture header states the documented sweep-vs-gate untyped-entry divergence and scopes agreement claims to GATE-GREEN trees; C1 authors NO new present-but-empty `Status:` entries IN GATE-ASSERTED fixtures (lib-only fixtures MAY carry one where the B′ poison rows require an unreadable input — documented in the test header; no gate assertion rides on it; the ES geometry is BD-264's owned refresh — this BD's gate-facing goldens stay stable across BD-264's A2 close).
  Acceptance (verbatim): the shipped `_rules.md` schema block parses under the BASH twin-parser leg (test-asserted; the Python leg's agreement is BD-264's Check-84 bite probe); the lib API (ALL functions named + signatures + the amended pinned row grammar + typed errors) is FROZEN by its tests — the Wave-2 contract; rollup unit tests pass the §T-1 B′ table row-for-row INCLUDING flag bytes and the S=∅ → `unknown` row; the target goldens: existing rows gain `tgt=- t=0`; mixed-target; future-unassigned drag; zero-target; present-illegal poison incl. ILLEGAL-ON-DONE-MEMBER; deferred-member-loose-target (deferred targets still dominate); done-member-loose-target (excluded from max); superseded-member-loose-target (excluded — the G-1 pin frozen here); all-done → `tgt=- t=0` beside `complete`; the implied-map golden byte-exact incl. `via=-` rows; the cascade-vs-`grp_deps` agreement fixture; the cross-parser Status fixture; the reserved-ID cross-agreement line; lib-ordinal == contract order; empty tree seeds `(empty — no entries)` `_toc.md`; full battery green.
Resolved: n/a
