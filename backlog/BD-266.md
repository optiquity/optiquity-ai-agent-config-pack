<!-- per-entry source: /backlog/BD-266.md; contract: /backlog/_rules.md -->
**BD-266 — status-generate.sh: marker-delimited never-SSOT dashboard generator + --check wiring + BD-105 title-link fold + target columns (BD-189 CBD-5)**
Type: feat — client tooling (project-side deliverable). The BD-189 Wave-2 dashboard child. Opened 2026-07-05 at C0 per PLAN-BD189-FOLDED.md F1.
Status: Resolved
Target: v11.0 — Wave 2 (parallel; disjoint file sets).
Blockers: BD-262 (the lib API: `grp_reverse_map`, `grp_rollup_map`, `grp_phase_status_map`, `grp_phase_target_map`).
Unblocks: BD-268; BD-105's resolution (G1).
File/Symbol:
  - NEW `project-template/scripts/status-generate.sh`
  - `project-template/scripts/validate.sh` (one wiring line)
  - NEW `scripts/tests/test-status-generate.sh`
Description:
  Scope (verbatim from PLAN-BD189-FOLDED.md F1/BD-266; the (e) fork COLLAPSED to arm (ii)): class-1a Status cell sources the `Status:` FIELD via `grp_phase_status_map` (one source inside one generated file; in-file emoji conventions untouched; the D3.3 amendment recorded in THIS BD's entry + the IMPL-REPORT per architect-doc-reality-reconciliation); class-1a Target column (the phase's own field; `—` when absent); class-1a Groupings cell three-state (RS-5); class-1b table header pinned `Grouping | Kind | Status | % complete | Target | Member phases` — Status + percent + Target (bare derived token) from `grp_rollup_map`; the generator consumes `grp_phase_status_map` + `grp_phase_target_map` + `grp_rollup_map` — NO third parse point; moment-3 PENDING excludes superseded phases (the S-4 filter; deferred orphans stay listed); class-2 frontier from `pm-session-state.json` (SOURCE only); class-3 hand section seeded once; marker-less REFUSE; `--check` class-1-only + marker integrity + SKIP-lenient; BD-105's flat-file title-link cell; one-line `validate.sh` wiring.
  Acceptance (verbatim): create-if-absent seeds class 3; regen ×2 byte-identical; hand section preserved; `--check` bites on class-1 drift, ignores class-2, SKIPs on absent/marker-less; the blocked-renders-blocked golden (a `blocked` phase renders `blocked` in class-1a — the emoji-invisible state made visible); class-1b header golden carries `% complete` AND `Target`; the cross-consumer parity probe (`groupings.sh list -q` values == class-1b values from ONE fixture tree, both via `grp_rollup_map`); a superseded member renders per B′ + flags in class-1b; Check 70 untouched; full battery green.
Resolved: 2026-07-05 — Landed: the marker-delimited never-SSOT STATUS.md generator (three generated classes + the preserved hand section; class-1a Status FIELD-sourced per the (e) arm-(ii) ruling with the D3.3 amendment chain recorded; the class-1a Target column; the pinned class-1b header incl. `% complete` + `Target`; the S-4 filter both directions; the RULED four-state Groupings cell incl. `none (superseded)`; class-2 SOURCE-only; marker-less REFUSE; `--check` class-1-only; BD-105's flat-file title-link cell) + the validate.sh wire (bites end-to-end) + the 74-assert suite. Reviewed CLEAN + the 3-fix re-review CLEAN (the presence-keyed parity leg self-activates at BD-265's landing; the pair-disjointness assert; the banner). The D3.3 amendment: class-1a sources the `Status:` field, superseding the emoji-sourced FINAL text — recorded here per architect-doc-reality-reconciliation.
