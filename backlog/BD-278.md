<!-- per-entry source: /backlog/BD-278.md; contract: /backlog/_rules.md -->
**BD-278 — Client `validate.sh` enforcement: opt-in pre-push hook installer + `/pm-startup` detect-and-suggest (client deliverable)**
Type: feat — client / project-template dev-workflow enforcement (shipped in `project-template/`). Give client projects a real, client-appropriate ENFORCEMENT point for the shipped `validate.sh` quality gate (which fans out to every validator leg, including the BD-277 mktemp guard) WITHOUT presuming a CI platform: an OPT-IN local git pre-push hook that runs `validate.sh`, plus a `/pm-startup` (and `/pm-refresh`) step that DETECTS whether `validate.sh` is already enforced (a CI workflow OR a git hook) and, if not, SUGGESTS the user let the PM chat install the hook.
Status: Open
Blockers: SEQUENCING (not technical). Coordinate with BD-277 — `validate.sh` gains the new shell-portability leg there; the hook runs `validate.sh` (all legs) so BD-277's guard rides along. No hard technical blocker; runs after/alongside BD-277 in this mini-batch.
Position: client deliverable; mini-batch with BD-277. The architect ASSESSES: the hook mechanics (pre-push recommended — gates before code leaves the machine; vs pre-commit which re-runs the full validator battery on every commit; vs post- which cannot gate), the `/pm-startup` detection design (how to detect a workflow that runs `validate.sh` vs an installed git hook), and client-appropriateness (P-missed-7: SUGGESTION-not-auto-install, user-consent-gated — installing a git hook is a persistent config change requiring explicit user approval; client-audience language only).
Unblocks: client projects get a real, opt-in enforcement point for `validate.sh` (all legs) that needs no hosted CI; the developer is prompted at PM-chat startup when `validate.sh` is unenforced, instead of relying solely on the agent coder loop honoring the mandated gate.
File/Symbol (architect refines after the assessment; provisional surface family):
  - a NEW opt-in installer `project-template/scripts/install-validate-hook.sh` (filename confirmed unique) — a client-authored pre-push hook installer MODELED on the existing `project-template/scripts/install-immutable-hook.sh` (opt-in, advisory, refuses if a hook already exists, never modifies the tree, user-consent-gated).
  - `project-template/skills/pm-startup/SKILL.md` — a NEW detect-and-suggest step: detect whether `validate.sh` is enforced (a `.github/workflows/` that invokes it OR a local git pre-push/pre-commit hook); if neither, SUGGEST the user authorize the PM chat to install the pre-push hook. Mirror of the pack `/pack-startup` Step 5 hook-detect-and-suggest (which detects the graphify pre-push hook + suggests its installer).
  - `project-template/skills/pm-refresh/SKILL.md` — likely a mirrored re-verify/offer step (mirror of the pack `/pack-refresh` Step 2b hook re-heal), architect confirms.
  - possibly `project-template/docs/pack/PM-CHAT.md` and/or the client PM prompt — the PM chat's role in installing the hook ON explicit user OK.
  - SEPARATE client copies (no-dual-use); client-audience only; OPT-IN + user-consent-gated (never auto-installed).
Description: Census (2026-07-30, `v11-dev`): the client PM chat does NOT currently run `validate.sh` (only the agent coder loop does, mandated by `docs/pack/prompts/coder.md`); nothing catches a human commit/push or an agent that skips the mandated gate; and the client ships NO CI workflow (`.github/workflows/` is absent BY DESIGN — the pack ships the quality tooling + mandates it in the agent loop, and leaves CI-platform wiring to the developer, documenting the pattern in `OPTIONAL-FEATURES.md`). This BD adds a client-appropriate enforcement layer: an opt-in pre-push hook (precedent: `install-immutable-hook.sh`) suggested at PM-chat startup when unenforced (precedent: the pack's own `/pack-startup` Step 5 hook detect-and-suggest). It does NOT ship a CI workflow (user-declined 2026-07-30).
  **Out of scope:** shipping a client CI workflow (user-declined); the BD-277 mktemp guard logic itself (BD-277).
Design provenance: user idea (2026-07-30) raised during BD-277 — rather than ship a CI workflow, give `validate.sh` a client-appropriate enforcement point via an opt-in push hook suggested at PM-chat startup ("making it a suggestion when that slash command runs is much simpler"). Precedents cited: `install-immutable-hook.sh` (client opt-in pre-commit hook for the immutable check) + `/pack-startup` Step 5 (pack detect-and-suggest for the graphify pre-push hook).
Resolved: n/a
