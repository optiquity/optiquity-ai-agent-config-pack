<!-- per-entry source: /backlog/BD-214.md; contract: /backlog/_rules.md -->
**BD-214 — Tracker-deferral cleanup: strip tracker artifacts, block the mode flip (pack + project)**
Type: fix — STRUCTURAL cleanup of the BD-204 tech debt under the tracker-deferral decision (user 2026-06-12). Full pipeline: docs-researcher census → architect (keep-dormant vs delete line) → planner → coder batches with bounded review cycles.
Status: Open
Target: v11.0.
Blockers: None on the census; execution follows the architect + planner passes and runs TOGETHER with the restarted Track-2 re-baseline (one unified execution plan).
Unblocks: a clean flat-file-only v11.0 — no tracker residue in entry content, no reachable tracker flip on either surface, no doc/help surface advertising tracker use.
Problem: BD-204's C-8 dogfood flip was abandoned and tracker support deferred indefinitely (user 2026-06-12). The repo carries tracker tech debt: tracker artifacts edited into BD entry content during BD-204 (gz64/base64 blob machinery use, tracker-purpose HTML comments), a reachable tracker opt-in/flip path on both pack and project surfaces, a startup recommendation system that advertises the opt-in, doc/help/validator surfaces describing tracker mode as usable, and 213 inert GH Issues on the pack repo created by the abandoned flip.
Scope: (1) ENTRY CLEANUP NOW — remove every tracker artifact the census finds in BD entry content (gz64/base64 blobs, tracker-purpose HTML comments); entries return to clean per-entry flat-file form in the EXISTING format, content and intent preserved (the format itself does not change here — that is BD-215, future). (2) FLIP-BLOCK — disable the ability to switch to tracker mode on BOTH surfaces (pack side and project side): the `pack tracker init`/opt-in path refuses with a clear deferred message; the D-19 recommendation system stops recommending tracker opt-in (pack-startup Step 8 and project analogs); no user-facing path reaches tracker mode. (3) KEEP-DORMANT VS DELETE — tracker support will return in a future release, so well-designed reusable code (TrackerProvider abstraction, migrator framework, lib structure) is retained DORMANT (kept, blocked from activation, documented as parked); genuine cruft is deleted. The ARCHITECT draws this line per census occurrence, AFTER the docs-researcher census — no maximal rip-out. (4) SURFACE SWEEP — update every surface that describes tracker mode as usable: trinity (pack root + project-template), `_rules.md` mode sections, PACK-CHAT.md mode sections, PACK-AGENTS.md, agent definitions, skills (pack-startup, pack-help), user-facing help output, README, supporting-docs, project-template docs/scripts, validators (Checks 29″/32′/mirror rules and tracker-specific checks), tests, fixtures, `.gitignore` entries, CI workflows. (5) GH-ISSUES DISPOSITION — decide and execute the disposition of the 213 inert GH Issues (architect proposal, user decides; the PAT cannot delete — archive/close vocabulary only). (6) LOCAL-STATE GUARD — local tracker state (`tracker.toml`, `.pack-tracker/`) was deleted from the maintainer checkout 2026-06-12 (backups in /tmp); ensure no repo surface recreates it.
Out of scope: the canonical entry-format redesign and full re-author (BD-215, deferred); removing the dormant tracker code wholesale (retained for the future release); resuming any BD-204/BD-207 migration work.
Acceptance criteria: census-complete artifact inventory with every occurrence dispositioned (keep-dormant / block / delete) and exact counts reconciled; zero tracker artifacts in entry content (grep-zero gate over the documented artifact patterns); the tracker flip is unreachable on both surfaces with a clear deferred message; no doc/help/validator/test surface presents tracker mode as usable; the dormant-code line is architect-documented; GH-Issues disposition decided by the user and executed; full CI battery green (validate-pack + integration tests).
References: BD-204 (deferred pack tracker; the tech-debt source); BD-207 (deferred project tracker); BD-215 (the future format redesign this cleanup does NOT do); BD-203 (the per-entry flat-file standard that remains the sole mode); the restarted Track-2 re-baseline (unified execution).
Resolved: n/a
Position: v11.0; executes together with the restarted Track-2 re-baseline work (user 2026-06-12).
Note (2026-06-12, user decisions on scope item 5): GH-Issues disposition DECIDED — DELETE ALL 213 issues; execution deliberately HELD ("hold off on the issues — we can deal with them later"), so the architect designs the deletion mechanics only (BD-212 research: GraphQL deleteIssue, owner-only, ≥1s pacing) and execution waits for an explicit user go. The five archived rehearsal scratch repos (4× pack-bd204-oracle-*, 1× tmp-bd212-delete-probe) were manually DELETED by the user 2026-06-12; AI-CLI retained.
Note (2026-06-13, C1–C5b landed; BD-214 implementation complete bar the held deletion): the cleanup train landed — C1 flip-block code + Check 51 legs 1/2/4 + Node-24 bump (2d3f3d0; C1 CI hotfix bd06a96); C2 pack-side surface sweep (c994d82); C3 project-side + installers + Check 51 legs 3-5 + atomic install-map removal (c2559fa); C4 deleted 93 superseded BD-204/MODE3 churn docs (cdfe87d); C5a Track-2 entry re-scopes + BD-216 authoring + BD-197 fold (6d5ba2d); C5b this bookkeeping commit (status flips + dated notes). REMAINING before Resolved: the HELD deletion of the 213 inert GH issues + 49 pack-managed labels (decided DELETE-ALL per US-8; execution gated on explicit user go) — BD-214's FINAL step (US-1), after which this entry flips Resolved.
