<!-- per-entry source: /backlog/BD-213.md; contract: /backlog/_rules.md -->
**BD-213 — `pack tracker reset` + 3-level recovery, project-side application (supersedes BD-103 with BD-212)**
Type: feat — project-side application of the BD-212 reset design to client assets. Supersedes the project half of BD-103 (Deprecated).
Status: Deferred
Target: none — no release version; lands with the tracker-resumption release (the deferred cluster {BD-204, BD-207, BD-215, BD-216, BD-188, BD-212, BD-213}). (BD-214 Track-2 US-5, 2026-06-13; supersedes the 2026-06-11 v11.0 target.)
Blockers: BD-212 (the pack-side verb + design must land first) — both deferred; sequenced with/after BD-207 (client reset presupposes the client tracker mode BD-207 delivers) — also deferred. BD-213 is transitively deferred by its blockers.
Unblocks: clients get the full 3-level recovery story (soft / hard / nuclear) with the reset verb packaged, documented, and walkthrough-integrated.
File/Symbol: `project-template/docs/pack/PM-CHAT.md` (recovery routing prose), `OPTIONAL-FEATURES.md` § "Failure modes" (the canonical 3-level recovery documentation home per BD-103), `supporting-docs/MIGRATION-v10-to-v11.md` (recovery cross-link), client HELP-FRAGMENT surface (`docs/pack/HELP-FRAGMENT.md` lineage), client-fixture verification.
Description: Apply the BD-212 design to the project side WITHOUT redesign —
  the verb and lib are already shared-layer (`scripts/` ships to clients) and
  surface-parameterized by BD-212's GENERALIZABLE constraint, so this BD is
  the client-surface remainder: client-facing documentation (PM-CHAT.md
  recovery routing; OPTIONAL-FEATURES.md "Failure modes" 3-level recovery —
  soft reverse+fix+re-forward, hard reset+re-forward, nuclear
  restore-from-backup; MIGRATION cross-links; client HELP-FRAGMENT verb
  help), walkthrough integration, and verification on client fixtures
  (the `v11-*` persona fixtures + a scratch-clone tracker-mode exercise per
  the BD-174-class harness where applicable).
  TRACKER-AGNOSTIC prose: the client-facing recovery documentation
  describes reset in PROVIDER terms (capability-declared delete; the
  close + void-label fallback where a backend cannot delete) — never as
  GitHub-specific behavior.
  P-missed-7 applies in full: every client-doc edit starts from the
  project-side SSOT (PM-CHAT.md / OPTIONAL-FEATURES.md), never from pack-ops
  mechanisms. Commits are project-side; use the `project-only` keyword where
  the diff qualifies (Check 36-enforced), neutral framing otherwise.
  Boundary note inherited from BD-212: `pack-ops/HELP-FRAGMENT-TRACKER.md`
  is client-installed at S11 — whichever side the BD-212 architect assigns
  its verb-help edit to, this BD verifies the installed client copy renders
  the reset verb correctly.
Position: deferred — no release version; lands with the tracker-resumption release. With/after BD-207; after BD-212. (BD-214 Track-2 US-5, 2026-06-13.)
Resolved: n/a
Note (2026-06-13, BD-214 Track-2 US-5): Status flipped Open → Deferred, no release version. The project-side reset application rides BD-212 (pack verb) + BD-207 (client tracker mode) — both deferred — so BD-213 is transitively deferred and lands with the tracker-resumption cluster. Not abandonment — deferred-with-the-cluster.
