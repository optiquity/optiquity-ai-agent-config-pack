<!-- per-entry source: /backlog/BD-221.md; contract: /backlog/_rules.md -->
**BD-221 — Convert Gemini CLI support to Antigravity (full Gemini → Antigravity transition)**
Type: feat — STRUCTURAL, cross-cutting. Replace the pack's Gemini-CLI support with Antigravity (Gemini CLI's successor) across ALL surfaces (pack-self + client `project-template/`): the GEMINI.md trinity member + per-CLI agent/command/skill files + `.gemini/` config dir + MCP config + setup/migration docs + validators + install plumbing. This is the broad "Gemini → Antigravity support transition" that BD-201 explicitly anticipated. Opened 2026-06-15 (user direction).
Status: Open
Target: **v11.0 — LAUNCH GATE** (user direction 2026-06-15: pulled into v11.0 because of the impending Antigravity GA deadline — shipping v11.0 with Gemini support just as Gemini is superseded by Antigravity is unacceptable; v11.0 must target the supported successor). Supersedes BD-201's earlier "NOT v11.0 / v11.x minor" framing.
Blockers (CORRECTED 2026-06-15 by the BD-217 research — the earlier "GA-blocked ~06-18" framing was WRONG): Antigravity is NOT GA-blocked — it shipped at Google I/O 2026-05-19 and is **publicly inspectable NOW** (`antigravity.google/docs`, public preview, free for individuals); **2026-06-18 is Gemini's DEPRECATION date, not an Antigravity gate.** So there is NO external GA blocker — the refreshed Antigravity research STARTED 2026-06-15 (RESEARCH-BD-221-ANTIGRAVITY-TRANSITION.md). The only real design caveat: Antigravity is public PREVIEW with a STILL-SETTLING config-dir/agent-file layout + open bugs (worktree-leak, `extensions.worktreeConfig`) — the design must treat unstable paths as forward-looking (verify empirically; expect some post-launch churn) rather than hard-code preview specifics. v11.0's coupling is to the CONVERSION WORK (a large cross-cutting trinity change), NOT to any GA date. Other v11.0 work (BD-185/206/171/210/205) proceeds in parallel.
Unblocks: pack + client projects target Antigravity (the supported successor) instead of the superseded Gemini CLI — correct config paths, agent/subagent invocation model, MCP config (`mcp_config.json`), worktree/parallel support, and docs.
Description:
  **Problem:** Google's Gemini CLI is being superseded by Antigravity. The pack currently ships full Gemini-CLI support (GEMINI.md trinity member, `.gemini/` config, per-CLI agent/command/skill files, MCP via `settings.json`, setup/migration docs, validators). As Antigravity becomes the supported tool, Gemini support must migrate wholesale to Antigravity — designed against the real GA product, not patched piecemeal.
  **Scope (full transition; refined by the refreshed research):**
    - **Refreshed Antigravity research** (`verify-availability-not-just-existence`): re-run against the GA product (the user's prior session is uncaptured/stale). Cover the agent/subagent model, config-file layout + paths, MCP config (`mcp_config.json` — the BD-201 relocation), worktree/parallel-agent support (coordinate with BD-217), command/skill invocation, and any feature NOT yet at parity with Gemini.
    - **Convert every Gemini surface to Antigravity** across pack-self + `project-template/`: the trinity GEMINI.md member, agent/command/skill files, `.gemini/` → the Antigravity config dir, MCP config, setup/migration docs, validators, manifest/install plumbing — per the `cross-cli-reference-normalization` rule (audience-correct, NOT a byte-copy of the Claude/Codex text).
    - **Decide the Gemini deprecation/removal posture** (hard replace vs transitional dual-support + a client migration step) at design time.
  **Coordinated / subsumed work:**
    - **BD-201** (MCP `settings.json` → `mcp_config.json` relocation) is the MCP-config COMPONENT of this transition — fold it in / resolve it under this BD.
    - **BD-217** (cross-CLI worktree isolation) now includes Antigravity worktree research (2026-06-15 expansion) — coordinate so worktree/parallel support targets Antigravity, not the departing Gemini.
  **Out of scope:** Claude + Codex support (unchanged); v11.0 (this is a later v11.x minor); the worktree-isolation mechanism itself (BD-217).
  **Hard constraints:** trinity propagation discipline (GEMINI.md is a trinity member — parallel edits across the trinity per the rule); the `cross-cli-reference-normalization` audience-correct substitution; no settings file shipped/auto-written; graceful migration for existing client projects.
  **Pipeline:** researcher (refresh) → architect → planner → coder → bounded review/fix. Full trinity propagation.
  **Acceptance criteria (provisional — refined by research):** all Gemini surfaces converted to Antigravity (pack + client); config paths + MCP + agent/subagent model correct per the GA product; docs + validators updated; trinity parity preserved; existing client projects have a graceful Gemini→Antigravity migration step; validate-pack green.
References: BD-201 (the MCP-config-relocation component + the "group with the broader transition" direction); BD-217 (cross-CLI worktree isolation, now incl. Antigravity); the user's prior Antigravity research session (uncaptured / stale — to be refreshed); the `cross-cli-reference-normalization` rule (ARCHITECTURE-BD-182 §4.1); the trinity rule.
Resolved: n/a
Position: **v11.0 launch gate** (user 2026-06-15) — NOT GA-blocked (Antigravity inspectable now); refreshed research STARTED 2026-06-15 → architect → planner → coder. The broad Gemini → Antigravity transition BD-201 anticipated, pulled forward to v11.0 because Gemini deprecates 2026-06-18. Worktree/parallel-agent isolation for Antigravity is OUT of this BD's v11.0 base-support scope → BD-217 (v11.1), built on the config/agent foundation this BD establishes (user 2026-06-15).
