<!-- per-entry source: /backlog/BD-251.md; contract: /backlog/_rules.md -->
**BD-251 — Remove born-dead BD-164 per-entry back-pointer helpers (`pe_first_line_is_backpointer` + `pe_ensure_backpointer`)**
Type: feat — pack-side dead-code removal. Surfaced 2026-06-27 from the BD-249 adversarial-architect orphan sweep (pre-existing, mirror-unrelated orphans); user-approved open.
Status: Open
Target: v11.0
Blockers: BD-249 — SEQUENCED AFTER BD-249. Both edit `scripts/lib/per-entry/_lib.sh` (same file ⇒ serialize per the rule-10 parallelization map); BD-249's `_lib.sh` orphan strips sit adjacent to these removals, so landing this after BD-249's commit avoids a patch collision. SEQUENCING dependency, not logical.
Unblocks: a fully orphan-free per-entry `_lib.sh` back-pointer helper surface (no born-dead functions).
File/Symbol:
  - `scripts/lib/per-entry/_lib.sh` — remove `pe_first_line_is_backpointer()` (def ~:327) and `pe_ensure_backpointer()` (def ~:353). Both are BORN-DEAD: introduced UNUSED by BD-164 (commit `ab51d76`, 2026-05-15) and never wired into any consumer — ZERO call sites across all git history.
  - **KEEP — LIVE, do NOT remove:** `pe_backpointer_line()` (~:303; called by `tracker-migrate-reverse.sh` + `test-per-entry.sh`) and `pe_strip_backpointer_stdin()` (~:338; called by `tracker-migrate-forward.sh`/`tracker-promote.sh` + the tracker round-trip test suites + `test-per-entry.sh`). The `_lib.sh` Public-API header (`~:40-41`) lists only these two LIVE helpers — confirm the removals do not touch the header block.
  - **grep-zero completeness gate:** after the removal, `git grep -nF 'pe_first_line_is_backpointer'` and `git grep -nF 'pe_ensure_backpointer'` (excluding `maintenance-docs/`) MUST return zero.
Description: BD-164 introduced the `scripts/lib/per-entry/` helper library; two of its back-pointer helpers — `pe_first_line_is_backpointer` (first-line predicate) and `pe_ensure_backpointer` (idempotent prepend) — were defined but never wired into any consumer. The per-entry back-pointer line is produced by `decompose.sh` / `pe_backpointer_line` instead, so these two functions have been dead since the commit that created them. BD-249's adversarial-architect orphan sweep surfaced them as pre-existing dead code UNRELATED to the mirror subsystem (correctly kept out of BD-249's scope). This BD removes exactly those two functions. Pack-only; ONE commit; SMALL/mechanical (base flow — coder + bounded review/fix cycle; no architect/planner pass needed). Verification: the grep-zero gate + `validate-pack` green + `test-per-entry.sh` green (the LIVE back-pointer helpers and their tests are untouched).
  **Out of scope:** the LIVE `pe_backpointer_line` / `pe_strip_backpointer_stdin` helpers (KEEP); the mirror subsystem (BD-249); the back-pointer line itself (stays — produced elsewhere).
Resolved: n/a
Position: v11.0 — after BD-249 is committed (same-file `_lib.sh` serialization). A small mechanical dead-code cleanup.
