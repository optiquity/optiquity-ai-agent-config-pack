<!-- per-entry source: /backlog/BD-110.md; contract: /backlog/_rules.md -->
**BD-110 — Pack-side `pack-auditor` agent**
Type: TODO(version)
Status: Open
Blockers: BD-074
Unblocks: None
File/Symbol: `.claude/agents/pack-auditor.md`, `.codex/agents/pack-auditor.toml`, `.gemini/agents/pack-auditor.md` (per-CLI per §6.M), `PACK-CHAT.md` Audit cadence section (cadence now anchors to BD-205's final readiness audit, not the deprecated BD-100 checkpoints), `scripts/tests/test-pack-auditor.sh`
Description: New pack-side ongoing-state-audit agent peer to `pack-reviewer`
  per V3.3 D-23. Distinct role: review = pre-commit change-scoped;
  pack-auditor = ongoing-state surface-scoped. RE-SCOPE 2026-06-13 (BD-214
  Track-2): the audit surface is the per-entry tree (flat-file only — the
  `/backlog/` + `/changelog/` per-entry trees as SSOT); the tracker-mode-health
  leg is DROPPED (tracker deferred indefinitely, BD-214), and the dead BD-100
  CP-prompt dependency is removed (BD-100 is Deprecated/merged into BD-205,
  US-7). Audit scope: per-entry backlog dependency graph, BD entry semantic
  consistency, drift over time, pack-product/pack-ops separation, version-table
  consistency. Per-CLI replicated to match existing pack-side layout per
  §6.M (a). Loads `audit-methodology` always; `architecture-review`
  conditionally for layer-discipline findings. Skill provenance via §6.N
  audit at land-time (BD-074 vs BD-110). Cadence anchors to BD-205 (the final
  readiness audit) per the re-enumerated launch-gate set.
Resolved: n/a
