<!-- per-entry source: /backlog/BD-207.md; contract: /backlog/_rules.md -->
**BD-207 — Project-side per-entry ↔ GH-Issues reversible tracker (apply the tracker standard to client assets)**
Type: feat — STRUCTURAL. The PROJECT analog of BD-204: the client-shipped per-entry → GH-Issues (tracker Mode 3) conversion + its REVERSE (Mode 3 → per-entry), made REVERSIBLE/round-trippable + no-mirror + entry-preserving per the corrected standard. Project-side product change. Full pipeline. Designed in/with the BD-203 architect pass (per the architect's together-vs-separate decision); implemented later.
Status: Deferred
Target: none — deferred indefinitely, no release version (user 2026-06-12; supersedes the 2026-06-05 launch-gate fixing).
Blockers: Follows BD-206 (project per-entry trees must exist first) + the corrected standard (BD-203). Designed alongside BD-203/BD-206 per the architect's together-vs-separate decision (user 2026-06-04). BD-215 (tracker-agnostic entry-format redesign) blocks any resumption (user 2026-06-12).
POST-BD-204 REFRESH (anchor, user 2026-06-05): once BD-204 (the pack analog) is IMPLEMENTED, this entry MUST be rewritten to be correct + compatible with the AS-BUILT BD-204 design — the SSOT/mirror model, no-monolith stance, overflow-carrier + Mode-3 write-model decisions, Pack Feedback two-lane separation, and the surface-generalizable machinery — realized on the project side. Deferred now (BLOCKED on BD-204 implementation; the design may drift during build) to avoid baking in a guess; anchored here so the refresh is not lost. SCOPE ADDITION (user 2026-06-06): BD-207 MUST also include the DELETION of `scripts/lib/tracker-sidecar.sh` + `scripts/lib/tracker-header-snapshot.sh` — BD-204 leaves them DORMANT (the client/project branch still consumes them, so BD-204 could not delete them and stay pack-only); BD-207 removes them when it retires the client-side sidecar/header-snapshot use (fail-loud delete once the last consumer is gone).
DEFERRED TECH-DEBT (BD-204 C-4.5 review, 2026-06-07 — carry into the POST-BD-204 REFRESH; surfaced + deferred per the bounded review/fix cycle, no live risk, out of C-4.5 scope): (1) the gz64 size-budget safety margin (`TMF_SIZE_SAFETY_MARGIN`, default 2048) computes a NEGATIVE budget for a hypothetical sub-2KB-limit `raw_text` provider and would fail every entry — when BD-207 wires non-GH backends, clamp `margin = min(margin, limit // 2)` (or warn) so a small-limit provider degrades gracefully; no live risk today (GH 65536 / Trello 16384 / Jira 32767 all clear the ~43 KB design floor). (2) test-hygiene: the benign PRE-EXISTING `line 376: issue: command not found` stderr artifact in `scripts/tests/tracker-migrate-forward-test.sh` (fails no assertion) should be cleaned up. Tracked here so neither is forgotten.
Unblocks: client projects convert their per-entry trees ↔ GH Issues REVERSIBLY (lossless round-trip), no monolithic mirror, every entry preserved — the same standard as the pack (BD-204).
REVERSIBILITY (HARD REQUIREMENT, user 2026-06-04): the client per-entry ↔ GH-Issues conversion MUST round-trip LOSSLESSLY (per-entry → GH-Issues → per-entry == original); not a one-way push.
Scope: the client-shipped tracker feature (Mode 2↔3) corrected to no-mirror + REVERSIBLE + entry-preserving; the reverse-migration path; client tracker docs/rules/validators. Same standard as BD-204, applied to project assets.
Out of scope: the pack tracker (BD-204); the project monolith→per-entry conversion (BD-206); the pack conversion (BD-203).
Acceptance criteria (full correctness audit): the client per-entry ↔ GH-Issues round-trips LOSSLESSLY (verified); NO monolithic mirror; every entry preserved; reversibility proven; validate-pack green.
References: `project_pack_self_migration_launch_gate` (pack memory); BD-204 (pack analog); BD-206 (project per-entry prerequisite); BD-203 (corrected standard + design); the tracker Mode 1/2/3 feature (BD-060 TrackerProvider).
Resolved: n/a
Position: deferred indefinitely — out of the v11.0 plan (user 2026-06-12); resumption blocked on BD-215 + the POST-BD-204 REFRESH.

Note (2026-06-12, USER DECISION — indefinite deferral): BD-207 is DEFERRED INDEFINITELY with no release version, in lockstep with BD-204 (the pack analog was abandoned at its C-8 dogfood flip after repeated round-trip churn). The project-side ability to flip to tracker mode is BLOCKED by BD-214 (tracker-deferral cleanup + flip-block, both surfaces). The POST-BD-204 REFRESH anchor above remains valid but now additionally requires BD-215 (tracker-agnostic canonical entry-format redesign) to land first — format before tracker. The deferred tech-debt items recorded above stay anchored here for the future resumption.
Note (2026-06-13, BD-214 Track-2 US-5 cluster semantic): BD-207's "no release version" means it lands with the tracker-resumption release as part of the deferred cluster {BD-204, BD-207, BD-215, BD-216, BD-188, BD-212, BD-213} — not permanent abandonment. Resumption additionally requires BD-215 (format-first) per the POST-BD-204 REFRESH + the BD-204 note.
