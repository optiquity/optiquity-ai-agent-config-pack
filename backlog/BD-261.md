<!-- per-entry source: /backlog/BD-261.md; contract: /backlog/_rules.md -->
**BD-261 — Client phase target-version system: the relative release-cycle vocabulary (`Target:` field, derivation, release-boundary lifecycle) — prerequisite for BD-189 groupings implementation**
Type: feat — STRUCTURAL project-side schema + semantics (client deliverable). Adds an optional `Target:` field to client phase entries carrying a CLOSED, pack-defined, ordinally-ordered release-cycle vocabulary; grouping targets derive from member targets by maximum over that fixed scale. Opened 2026-07-04 (user ruling δ at the BD-189 (b) gate, item 7).
Status: Open
Target: v11.0 — sequenced BEFORE BD-189's implementation waves (user 2026-07-04): the groupings children must consume `Target:` natively (rollup, goldens, display, checks target-aware from birth, never retrofitted). BD-189's plan HOLDS for this BD's landing.
Blockers: none on repo state (BD-189's approved design + rulings provide the consuming context; this BD lands first by ruled order).
Unblocks: BD-189's implementation waves (target-aware groupings children); the grouping-target-by-proxy display.
Provenance: user direction 2026-07-04 — the BD-189 (b)-gate target-version addendum, resolved as option δ after the trio's analysis (the "latest member target" propagation was unverifiable over opaque client version strings; the user's closed relative vocabulary dissolves the correctness-oracle problem — the order is intrinsic to the pack-defined enum, and the pack never parses or ranks a client version string).
The vocabulary (USER CONTENT, verbatim semantics — the ordinal scale `current < next-patch < next-minor < next-major < future-unassigned`):
  - `current` — sometime before the current version is released.
  - `next-patch` — sometime after the current version is released but before the next patch, minor, or major version is released.
  - `next-minor` — sometime after the current version is released but before the next minor or major version is released.
  - `next-major` — sometime after the current version is released but before the major version is released.
  - `future-unassigned` — anytime; unconstrained and not release-blocking; may also carry a specific user comment about the version in the phase description text (human-only prose; tooling never interprets it).
  Targets are derivational metadata ONLY: they never change the mechanics of `Blockers`/`Unblocks`/dependencies (user-stated).
File/Symbol (candidate surfaces — the researcher/architect produce the definitive set):
  - `project-template/docs/project/implementation-plan/_rules.md` — the `Target:` field admission + the enum + its semantics (the schema SSOT both validators parse)
  - `project-template/scripts/validate-docs.sh` — the enum-validation leg (unknown value FAIL; the Kind-validation precedent)
  - `scripts/lib/validate_checks/` — the pack-side twin assertion per the twin-parser model
  - the BD-189 groupings consumption seam — grouping target = max(member targets) over the ordinal scale; the deferred interaction (the user's addendum: a deferred phase's target defers the grouping to the latest target in the graph); display columns (the detail header / STATUS.md)
  - `supporting-docs/METHODOLOGY.md` — authoring + lifecycle procedure text
  - `docs/project/pm-session-state.json` (ASSESS — see the suggestion below)
Description:
  Problem: deferred (and all) phases need a WHEN that respects client-owned versioning without the pack ever ordering client version strings; groupings need a derivable target-by-proxy; the semantics must be deterministic, verifiable, and cheap.
  Goal: the five-value relative vocabulary as an optional phase field; deterministic grouping-target derivation; validation + tests + procedures; BD-189's children consume it natively.
  Design charges (the architect resolves; ask-don't-assume):
  - **The release-boundary lifecycle (the hard part):** the enum is relative to a MOVING reference (the client's current release). Design the boundary behavior: what happens to unlanded `current` targets when the client ships (a re-target sweep procedure? triggered by whom? what does validation say post-boundary?).
  - **The absent-field sub-pin:** is an absent `Target:` ≡ `future-unassigned`, or a distinct "no claim"?
  - **The reference-point mechanism:** where "the current release" authoritatively lives. USER SUGGESTION (explicitly suggestion-status, not a decision): `docs/project/pm-session-state.json` could hold the current work-in-progress client project version identifier — assess against the snapshot's grammar axis and the SSOT question.
  - The grouping derivation + deferred interaction; display; checks/tests; the BD-227 milestone-mapping seam (constraint (b) forward-awareness).
  Process: the standard pipeline (researcher census → architect → adversarial → reconciliation → user gates → planner stages → coder waves), compact where the evidence permits. Cross-BD coordinates: BD-189 (the consumer; its plan holds), BD-227 (tracker milestones), BD-185 (phase-schema co-edit), BD-171 (migration expectations), BD-257 (display surface).
Resolved: n/a
