<!-- per-entry source: /backlog/BD-267.md; contract: /backlog/_rules.md -->
**BD-267 — Impl-plan ordering + lifecycle contract: grouping-affinity placement, deferral-deprioritization ordering, four-field dependency sentence, terminal-supersession sentence, groupings-contract pointer (BD-189 CBD-6)**
Type: feat — contract prose (ONE file; five sentence-level edits in landed sections). The BD-189 Wave-2 ordering/lifecycle child. Opened 2026-07-05 at C0 per PLAN-BD189-FOLDED.md F1.
Status: Open
Target: v11.0 — Wave 2 (parallel; disjoint file sets).
Blockers: BD-262 (concept anchor + the pointer's target contract).
Unblocks: BD-268.
File/Symbol:
  - `project-template/docs/project/implementation-plan/_rules.md`
Description:
  Scope (verbatim from PLAN-BD189-FOLDED.md F1/BD-267): (1) `## Ordering — _index.md` gains the OQ-7 affinity sentence-pair (same-grouping phases contiguous wherever Blockers/Unblocks permit; never constrains parallelization; "grouping-mates" over real groupings — GRP-000 excluded); (2) STACKED beneath it, the item-6(2) deferral-deprioritization ordering rule (completable groupings' phases order ahead of every-membership-poisoned phases wherever dependencies permit — deterministic, same mechanism class); (3) the DEFSTATUS COMMON four-field repair at :100-101 (the topological-seed sentence names `Blockers` / `Unblocks` / `Dependencies` / `Prerequisite`); (4) the item-3 terminal-supersession sentence in `## Lifecycle states admitted` (:117-126): supersession is TERMINAL — a superseded phase is frozen, never revertible to any open state; revival = a NEW phase; (5) the §P2 item-6(c) pointer: the `## Target semantics` max sentence (:79-80) gains its pointer to the NOW-EXISTING groupings contract. F6 RULED EXPLICIT (user 2026-07-05, superseding the fold's default): the shipped declarer sentence names BOTH terms INLINE — "non-done and non-superseded" — no definitional-only cross-reference; the groupings `_rules.md` + the lib spec carry the same explicit pair (the G-1 pin sites). Operating-doc clean: zero BD refs, zero history.
  Acceptance (verbatim, F6-amended): the schema block bytes UNCHANGED in this BD's own diff (absolute — the :79-80 sentence lives in `## Target semantics` PROSE, outside the schema block); the :79-80 sentence gains the pointer + the explicit non-done-and-non-superseded qualifier (its own acceptance item); no test re-pins on the section's prose; docs gate green; full battery green.
Resolved: n/a
