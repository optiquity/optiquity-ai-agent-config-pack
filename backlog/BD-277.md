<!-- per-entry source: /backlog/BD-277.md; contract: /backlog/_rules.md -->
**BD-277 — Client-side (project-template) BSD-mktemp portability: sweep + guard assessment (client deliverable analog of BD-276)**
Type: feat — client / project-template portability hardening (shipped in `project-template/`). The client deliverable analog of BD-276: give CLIENT projects the same cross-platform (macOS BSD + Linux GNU) `mktemp` parity + a regression guard. SEPARATE CLIENT copies per the `dependency-direction-placement` (c) no-dual-use rule (own client guard, own client rule text) — NEVER the pack's Check 92.
Status: Resolved
Blockers: SEQUENCING (not technical). Follows BD-276 (the pack-side sweep + guard + root-cause fix land first; this mirrors the client-appropriate subset).
Position: client deliverable; after BD-276. The architect ASSESSES where a client-side guard runs before any coder — the client ships validators (`validate.sh` / `validate-docs.sh`) but NO `.github/workflows/`, so guard placement is a P-missed-7 project-side-SSOT investigation (a leg in an existing client validator vs a new client check vs developer-run-only), not a copy of the pack's CI check.
Unblocks: client projects get cross-platform `mktemp` parity + a client-appropriate regression guard against re-introducing the non-portable forms.
File/Symbol (architect refines after the assessment; census 2026-07-30 ground-truth):
  - the SOLE client occurrence: `project-template/agent-run.sh:347` (`mktemp -d -t agy-auditor-XXXXXX`) -> the portable full-path-template form `mktemp -d "${TMPDIR:-/tmp}/agy-auditor.XXXXXX"`.
  - a client-side guard (SEPARATE client copy, no-dual-use): assess placement across the client validators (`project-template/scripts/validate.sh` / `validate-docs.sh`); covers the same portability classes the pack Check 92 covers (`-t <prefix>XXXXXX` incl. bundled `-dt`/`-qt`; GNU-only `--tmpdir` + `-p`).
  - NO client teaching-surface fix needed (census-confirmed): the client skills `pm-startup` / `pm-refresh` already use plain portable `mktemp` (no `-t`); there is no client fixtures README; the `verification-harness` skill is pack-only.
Description: The BD-276 pack-side sweep + guard surfaced exactly ONE non-portable `mktemp` in the client deliverable — `agent-run.sh:347` — and NO client-side root-cause teaching surface (unlike pack-side, whose `verification-harness` skill + `test-fixtures/README.md` taught the buggy form). This BD fixes the one client invocation and assesses/adds a client-appropriate regression guard so a client CI (or developer validate run) catches re-introductions, mirroring BD-276's intent at the client's much smaller scale. Client-audience only — zero pack-governance terms in shipped client surfaces.
  **Out of scope:** the pack-side sweep/guard/skill (BD-276, done); any pack-ops surface.
Design provenance: surfaced during the BD-276 pack-side work (2026-07-30) as the client companion; scope split ratified with the user (client-only — the pack `verification-harness` skill root-cause fix stays in BD-276 pack-side). Census (2026-07-30, `v11-dev`): 1 client occurrence, zero `--tmpdir`/`-p`, no client teaching surface.
Resolved: 2026-07-30 — client BSD-mktemp portability shipped project-only. (1) FIX: `project-template/agent-run.sh:347` `mktemp -d -t agy-auditor-XXXXXX` -> portable `mktemp -d "${TMPDIR:-/tmp}/agy-auditor.XXXXXX"` (the sole client non-portable invocation; the tmpdir is used only as an internal path prefix). (2) GUARD: NEW client-authored bash `project-template/scripts/validate-shell.sh` (mode 100755) shipped as a NEW always-on leg of `project-template/scripts/validate.sh` -- a SEPARATE client copy (no-dual-use, never the pack Check 92), client-audience (ZERO pack vocabulary, CI-enforced by Check 37/43). Scans `git ls-files '*.sh'` (O(chars), SKIP-lenient off git, empty allowlist), flags the same 3 classes the pack guard covers (`-t <prefix>XXXXXX` incl. bundled `-dt`/`-qt`/`-dqt`; GNU-only `--tmpdir`; GNU-only `-p`) with quote-aware comment stripping (whole-line + trailing, parity with the pack `_strip_shell_comment`) + a `--self-test` (client-side bite proof, no pack test file). Ships + wires to clients AUTOMATICALLY via the glob install machinery (init-project S5 glob, cmd_update find-glob, always-on by absence from the S9 conditional roster) -- ZERO enumeration edits; verified end-to-end by a scratch init-project install (SHIPPED+EXEC+WIRED+RUNS+UPDATE-OK). (3) No client teaching-surface fix (census-confirmed clean: client skills use plain portable `mktemp`; `verification-harness` is pack-only). Deterministic pipeline: architect (client-guard placement, P-missed-7) -> user design gate -> planner (provisioning/install map) -> user plan gate -> coder -> review (1 MUST: trailing-comment parity gap) -> fix (quote-aware strip port) -> re-review CLEAN. Full battery + DEEP green; project-only. The client `validate.sh` ENFORCEMENT layer (opt-in pre-push hook + pm-startup detect-and-suggest) is the companion BD-278. Fixture manifest regenerated at push-time (orchestrator job, separate pack-only commit).
