<!-- per-entry source: /backlog/BD-109.md; contract: /backlog/_rules.md -->
**BD-109 — Project-side `auditor-issue-tracking` sub-agent**
Type: TODO(version)
Status: Open
Blockers: None on critical path; recommended sequencing pairs with BD-082 ext (Check 28) at step 23a/23b
Unblocks: None
File/Symbol: `project-template/.claude/agents/auditor-issue-tracking.md`, `.codex/agents/auditor-issue-tracking.toml`, `.gemini/agents/auditor-issue-tracking.md`, `auditor.md` parent extension × 3 CLIs, `audit-methodology/SKILL.md` × 3 CLIs, `scripts/tests/test-auditor-issue-tracking.sh`
Description: New 8th cluster under existing `auditor` parent fan-out per
  V3.3 D-23. Audits issue-tracking-surface health: dependency-graph
  integrity, syntax conformance (including the BD-211 entry-ID grammar —
  canonical `BD-\d+` / no letter suffix, sub-parts as in-body sections),
  semantic consistency, drift detection.
  Read-only. Trinity-replicated × 3 CLIs in one commit (agent trinity
  symmetry enforced by Check 11; Check 28 is PM-startup per-CLI parity, not
  agent-trinity replication — numbering corrected 2026-06-13).
  Skip rule (flat-file, per-entry): brand-new project — an empty project
  backlog stream (no `docs/project/backlog/` entries) AND an empty project
  implementation-plan stream (no `docs/project/implementation-plan/` entries)
  — skips this cluster. (Restated in per-entry flat-file terms; the former
  monolithic `BACKLOG.md` / `IMPLEMENTATION-PLAN.md` are readable mirrors
  until BD-206.)
Resolved: n/a
