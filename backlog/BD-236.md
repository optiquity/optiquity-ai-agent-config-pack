<!-- per-entry source: /backlog/BD-236.md; contract: /backlog/_rules.md -->
**BD-236 — Project-side agent-def discipline-parity guards are incomplete (Check 57 covers 8 of the 13 enumerated git verbs; Check 11 cross-surface symmetry is informational-only) — investigate + design a coherent project-side parity-enforcement strategy**
Type: design investigation → CI guard-coverage (project-side agent-def discipline parity). Investigate-then-implement (architect investigation → user decision → planner → coder). The architect maps the full project-side discipline-parity enforcement landscape and recommends a coherent guard strategy; no code lands until the user approves the design.
Status: Open
Target: v11.0 (user-confirmed 2026-06-19). NOT strictly a launch blocker — the shipped content is CORRECT today (all 13 denylist verbs are present in every def; the verb set is intact; the general ban is anchored by the trinity catch-all + the "No state-changing git operations" phrase present everywhere). The gap is in GUARD COVERAGE (protection against future enumeration drift), not in current content. Lands in v11.0 because it is unblocked work surfaced during v11.0 development (no-deferral-without-user-direction); user confirmed Target.
Blockers: none hard. Independent of BD-235's parked skills decision. Logically should land BEFORE BD-205's final readiness sweep so BD-205 VERIFIES the strengthened guards rather than re-discovering the gap (BD-205 explicitly excludes net-new guard features, so it is not the home for BUILDING this coverage).
Unblocks: provable cross-surface parity of the project-side agent discipline — drift-proof enumerations across the 48 agent defs (16 agents × 3 CLI surfaces), closing the path where the 5 most-consequential git verbs and the broader hand-duplicated discipline block can silently diverge with CI green.
Provenance: surfaced during BD-235's pack-architect investigation (HEAD `0281ec3`). The investigation's PRIMARY recommendation (a hybrid project-side Tier-0 `agent-discipline` skill) was REJECTED by the user as a Frankenstein design that patched a symptom without whole-ecosystem understanding; BD-235's real skills question is PARKED pending a full-landscape redesign. THIS BD is the SEPARATE, real parity hazard the investigation's travels surfaced — it is NOT a continuation of the skills question and does NOT centralize or extract anything; it is purely about making the EXISTING duplication provably consistent via guard coverage.
File/Symbol (measured 2026-06-19, HEAD `0281ec3`):
  - **Check 57** (`check_project_destructive_git_verb_parity`, `scripts/validate-pack.py` ~L9801) — the project analog of pack Check 56. Asserts `_CHECK_57_CANONICAL_VERBS` is PRESENT in every project surface: project trinity ×3 (`project-template/{CLAUDE,AGENTS,GEMINI}.md`) + the 48 agent Hard-rules (16 agents × 3 CLIs) + `project-template/agent-run.sh` `--disallowedTools`. The canonical set is the **measured 8-verb intersection** `{checkout, clean, merge, rebase, reset, restore, stash, worktree}`.
  - **The coverage gap:** the agent defs ENUMERATE 13 verbs (`add, commit, push, tag, rebase, merge, reset, restore, stash, checkout, clean, apply, worktree`); Check 57 guards only 8 of them. The 5 NOT guarded are `add, commit, push, tag, apply` — i.e. the most consequential (commit/push/add). They were excluded by design ("measure-then-bound — sized to the measured-consistent verb set") because the surfaces express them INCONSISTENTLY (the trinity uses an open catch-all rather than enumerating commit/push; `agent-run.sh --disallowedTools` and the trinity do not carry every verb as a `git <verb>` token), so naively widening Check 57 to all 13 would FAIL today and is not a one-line change.
  - **Check 11** (`check_pack_agent_trinity`, `scripts/validate-pack.py` ~L1076) runs `scripts/compare-agent-trinity.py --all` over the 3 PROJECT surfaces (the script reads `project-template/.claude/agents/`, `.codex/agents/`, `.agents-plugin/optiquity-agents/agents/`) in **lenient / informational mode** — divergences are reported ("informational; not a failure"), never block CI.
  - **Check 27** (`scripts/validate-pack.py` ~L1749) scans all 48 project defs but asserts only representative-phrase PRESENCE (`COMMON_CANONICAL_PHRASES` = `"No state-changing git operations"`, `"Chunk long writes"`, `"Symbol references in reports"`, `"Trinity rule"`, etc.) — not the verb set and not cross-surface content identity.
  - **Live drift instance (confirmed, currently cosmetic):** for the SAME agent across surfaces, `project-template/.codex/agents/coder.toml` and `reviewer.toml` list `… git reset, git stash, git restore, …` while `.claude` and `.agents-plugin` list `… git reset, git restore, git stash, …` (`stash`/`restore` transposed); `repo-ops` does NOT transpose (matches Claude/Antigravity). The verb SET is intact on all surfaces (no safety gap today), but the inconsistency proves hand-maintained drift is already occurring and is unguarded by the failing checks.
  - **Contrast (pack side is stricter):** pack `_CHECK_56_CANONICAL_VERBS` carries 19+ verbs incl. `commit`/`push`; the project guard lags the pack guard.
  - **NOT a problem (rule it out):** the lead-in phrasing differs across surfaces ("You MAY NOT run …" on Claude/Codex vs "Forbidden: …" on Antigravity) — this is allowed audience-phrasing per `cross-cli-reference-normalization`, NOT a rule divergence; it is explicitly out of scope.
Description:
  **Problem:** the project-side universal-discipline content is hand-duplicated across 48 agent defs and is NOT provably parity-guarded. Three findings compound: (1) Check 57 enforces only an 8-verb intersection, leaving the 5 most-consequential git verbs present-but-unguarded in the defs; (2) the only cross-surface CONTENT-parity comparator (`compare-agent-trinity.py` via Check 11) runs informational-only and never fails CI, so drift is reported, not blocked; (3) Check 27 is presence-only, so no discipline element (git verbs, chunk-writes, symbol-refs, output policy) has enforced full cross-surface content parity. The git-verb denylist is merely the most-guarded tip of a systemic enumeration-parity-enforcement gap.
  **Goal:** a coherent, measured project-side discipline-parity ENFORCEMENT strategy — decide, with evidence, how each duplicated discipline element should be guarded across the 52 project surfaces (trinity ×3, 48 defs, `agent-run.sh`) so future drift is caught at CI rather than reported informationally, WITHOUT a redesign of where discipline lives (no skill extraction, no Tier-0 changes — that is BD-235's parked question).
  **Scope (in):** the CI guard-coverage of project-side agent-def discipline parity — Check 57 verb-set sizing (and whether to widen it / how to reconcile the format differences across trinity/def/launcher surfaces), whether Check 11 should be promoted from informational to enforcing (and the "trinity-asymmetry-by-design" marker convention that promotion would require, so legitimate tool-specific asymmetry is still permitted), and whether any content-parity check is warranted for the broader discipline block.
  **Non-goals (explicit):** (a) NOT a skill extraction / Tier-0 / `commit-discipline`-style redesign — that is BD-235's PARKED question and was rejected; (b) NOT a one-off "add the 5 missing verbs to Check 57" patch — the measure-then-bound rationale for the 8-verb intersection must be understood and addressed as a whole, not papered over; (c) NOT a change to WHERE discipline lives (defs stay authoritative per PM-CHAT.md); (d) the lead-in phrasing variance is out of scope (allowed).
  **Process (required flow):**
    1. `pack-architect` (read-only) maps the full landscape with Empirical-Evidence Blocks: every duplicated discipline element × every project surface × its current guard (enforced / presence-only / informational / none); why Check 57 is bounded to 8 (the format differences across surfaces); the cost of each candidate enforcement option (per `ci-check-runtime-compounding`); and a recommended strategy following `ci-guard-design-measure-then-bound` (measure the tree first, KEEP/STRIP each occurrence, size the allowlist to the legitimate set). Output: a design doc.
    2. User reviews + approves the architect design (the planner-to-coder gate). If "no change" is the right answer for any sub-part, that is a legitimate recorded outcome.
    3. If implementation is approved: `pack-planner` → `pack-coder` apply the guard changes + any surface edits needed to reach parity, updating ALL encoding surfaces in lock-step per `enumerate-encoding-surfaces` (the check, its per-check test, the CI workflow wiring, cross-ref docs), under the bounded review/fix cycle.
  **Why a separate BD (not folded into BD-205):** BD-205 is a whole-repo readiness GATE that explicitly excludes net-new features and verifies the repo against EXISTING guards; building new guard coverage is feature work outside its charter. Keeping this separate lets BD-205 later VERIFY the strengthened guards pass.
Acceptance criteria: an architect design doc maps the project-side discipline-parity enforcement landscape with measured evidence (each discipline element × surface × current guard; the Check-57 8-verb-intersection rationale; the format differences across trinity/def/launcher) and recommends a coherent enforcement strategy following measure-then-bound; a user decision is recorded (including "no change" for any sub-part that is justified); if implementation is approved, the planner→coder scope updates every encoding surface in lock-step (check + per-check test + CI wiring + cross-ref docs); `validate-pack` green; the live `stash`/`restore` ordering drift is resolved or explicitly accepted-with-rationale; NO client-install/boundary regression; the lead-in phrasing variance remains untouched (out of scope).
References: BD-235 (the investigation during which this surfaced; its skills recommendation was rejected and its skills question is PARKED — this BD is the separate parity hazard, not a continuation); BD-226 F-9 (the regime/patch decoupling that first surfaced the project/pack discipline-home asymmetry); BD-197 (origin of Check 56/57 the destructive-git-verb parity guards); `scripts/validate-pack.py` Check 57 / Check 56 / Check 27 / Check 11; `scripts/compare-agent-trinity.py`; Pack memory `ci-guard-design-measure-then-bound`, `enumerate-encoding-surfaces`, `ci-check-runtime-compounding`, `cross-cli-reference-normalization`, `skill-agent-maintenance-mechanical`; BD-205 (final readiness audit — verifies, does not build, this coverage).
Resolved: n/a
Position: v11.0 — project-side CI guard-coverage investigation; surfaced during BD-235 (2026-06-19). Sequenced directly AFTER BD-222 (its anti-drift CI-guard sibling), before BD-223/224/210/205 — slot #6 in the v11.0 substance queue (user 2026-06-19). Independent of BD-235's parked skills question; the agent-def + `validate-pack` surface it touches is already settled (BD-221/BD-226 Resolved), so there is no churn-avoidance constraint beyond landing before BD-205's final readiness sweep (which then VERIFIES the strengthened guards). Not a launch blocker (shipped content is correct today), but Target v11.0 per user confirmation + no-deferral.
