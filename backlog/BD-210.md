<!-- per-entry source: /backlog/BD-210.md; contract: /backlog/_rules.md -->
**BD-210 — v11.0 pre-launch maintenance-docs cleanup: delete superseded design/research docs**
Type: chore — CLEANUP, pack-only. DELETE superseded / completed-design and research docs from `maintenance-docs/v11-implementation/` + `maintenance-docs/v11-research/` (possibly whole directories) before v11.0 launch, per the delete-superseded rule (archive/"prison" RETIRED 2026-06-05 — agents read archives anyway; NO information is better than incorrect information). Dangling references created by the deletions are ACCEPTABLE and desired (fail-loud — a break surfaces the ref for fixing).
Status: Open
Target: v11.0 — pre-launch process (user 2026-06-05).
Blockers: Runs LATE in v11.0 — AFTER the remaining implementation BDs land (BD-204, BD-206, BD-207, BD-197, BD-185) so their design/research docs are genuinely complete + superseded before deletion; deleting a doc still consumed by active work would break it.
Unblocks: agents + Pack Chat stop encountering stale / superseded design + research docs; `maintenance-docs/` (an agent-read surface) carries LIVE content only.
Scope: enumerate every doc under `maintenance-docs/v11-implementation/` + `maintenance-docs/v11-research/`; categorize each as LIVE-still-referenced-by-active-work vs SUPERSEDED/COMPLETE; DELETE the superseded set (whole directories where wholly superseded). SUBSUMES the §11.3 v8-archive staleness in the still-active `ARCHITECTURE-PER-ENTRY-SPLIT-INTEGRATION.md` (delete that doc if superseded by then, else reconcile its stale element in place per the active-doc-with-stale-element exception). Decide whether `maintenance-docs/archive/` itself is deleted (the prison convention is retired).
Out of scope: shipped/client surfaces (`project-template/`, `supporting-docs/`, `scripts/`); the live trinity + `pack-ops/` governance docs; the per-entry SSOT trees.
Acceptance criteria: no superseded design/research doc remains agent-readable under `maintenance-docs/`; every LIVE doc preserved; validate-pack GREEN (any dangling-ref break from the deletions either fixed or accepted per fail-loud, none left silently wrong); a final scan confirms no agent-read surface cites deleted content as current.
References: `fail-loud-delete-old-source-on-migration` (pack memory — the delete-superseded rule); `project_pack_self_migration_launch_gate` (pack memory — flags this pre-launch item); BD-203 (defers its §11.3 staleness here); BD-205 (possible fold-in — final readiness audit).
Resolved: n/a
Position: v11.0 launch gate; LATE — after the implementation BDs, near/with BD-205 (final readiness audit).
