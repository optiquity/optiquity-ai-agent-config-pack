<!-- per-entry source: /backlog/BD-171.md; contract: /backlog/_rules.md -->
**BD-171 — Real-OT scratch-clone FLAT-FILE v10→v11 migration test harness**
Type: TODO(version) — surfaced 2026-05-15 from session discussion of test fixture thoroughness pre-public-release
Status: Open
RE-SCOPE 2026-06-13 (BD-214 Track-2): the harness validates the real-OT v10.3 → v11.0 FLAT-FILE migration only. ALL tracker-toggle legs (flat → GH Issues → flat → GH Issues) are DROPPED — tracker (GH Issues) integration is deferred indefinitely (BD-214). Scratch disposal is archive-only (the user's PAT cannot delete repos). OT re-pinned to v10.3 (latest real-OT tag). KEEP v11.0 — this validates the flagship flat-file migration the launch ships.
Blockers: None on critical path. (Was: pairs with BD-102 Batch-23 dog-food to share live-GH infra; BD-102 is Deprecated, US-7 — the shared-infra sequencing is moot. The harness now runs standalone, folded into BD-205's flat-file readiness gate.)
Unblocks: Empirical validation of the v10.3 → v11.0 FLAT-FILE migration on a real OT clone (not the synthetic `v10-realistic-ot` fixture); reusable harness pattern for v11 → v12 real-clone testing in future major versions; closes pre-public-release gap identified 2026-05-15 (synthetic fixture coverage is necessary but not sufficient for real-client validation).
File/Symbol:
  - NEW `scripts/tests/test-real-ot-migration.sh` (harness: provision scratch GH repo via `gh` CLI per pack-memory test-infra rule, clone real OT into `/tmp` at the v10.3 tag, push to scratch, run `migrate-v10-to-v11.sh`, verify the FLAT-FILE migration outcome incl. customization preservation + trinity invariants, archive-only disposal of the scratch repo with per-step approval — no tracker-mode round-trip leg, deferred per BD-214).
  - NEW `test-fixtures/v10-real-ot-snapshot/README.md` (documentation of the harness pattern + provenance; the live-clone fixture itself is not committed since it's a runtime pattern not a static fixture).
  - EXTEND `.github/workflows/validate-pack.yml` (gated CI run with `if: github.event_name == 'workflow_dispatch'` so the test runs on manual trigger only; auto-CI shouldn't provision scratch repos every push).
  - EXTEND `test-fixtures/README.md` table (note the live-clone harness pattern complements the persistent fixtures).
Description: Real-OT FLAT-FILE migration test that complements the
  `v10-realistic-ot` synthetic fixture (BD-113). Provisions a scratch GH repo
  via `gh repo create`, clones the real OT project into `/tmp` at the v10.3 tag,
  pushes to scratch, runs the actual v10.3 → v11.0 FLAT-FILE migration
  end-to-end, verifies preservation invariants (BD-088 customization survival;
  BD-136 marker round-trip if applicable), then disposes of the scratch repo
  archive-only (the user's PAT cannot delete repos). Original OT repo is
  read-only; no destructive operations on the source. Per the pack-memory rule
  "Test infra is self-provisioned" (`CLAUDE.md` ## Pack memory →
  [rationale: test-infra-self-provisioned]; the bare
  `feedback_test_infra_self_provisioned.md` filename was a dead ref —
  corrected 2026-06-13): "provision scratch GH repos via gh CLI with
  per-step approval; clean up after; never touch existing real repos." Pattern reusable for v11 → v12 real-clone testing in future versions.
  Closes pre-public-release gap: synthetic coverage is good but cannot
  substitute for real-client-shape validation (real OT may have customization
  patterns the synthetic fixture doesn't model — history depth, file-removal
  patterns, conflicting `[CONDITIONAL]` block edits, edge `x-`-prefix
  conventions).

  RE-SCOPE 2026-06-13 (BD-214 Track-2): the FULL multi-toggle tracker pattern
  (flat-file → GH Issues → flat-file → GH Issues) is DROPPED — tracker (GH
  Issues) integration is deferred indefinitely (BD-214). The harness validates
  the flat-file migration outcome only: customization preservation, trinity
  invariants, per-entry backlog survival. (The multi-toggle leg would land with
  the tracker-resumption release if BD-171 is revived for tracker testing then.)

  Position: standalone, folded into BD-205's flat-file readiness gate (was
  "position 2 of the Batch-23 live-GH test trio after BD-174 and before BD-102";
  BD-174 and BD-102 are Deprecated, US-7 — the trio sequencing is moot).
Resolved: n/a
