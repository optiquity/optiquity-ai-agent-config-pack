<!-- per-entry source: /backlog/BD-259.md; contract: /backlog/_rules.md -->
**BD-259 — Bring shipped `project-template/` operating docs into compliance with the shipped client doc-gate (`validate-docs.sh` full-scan is RED at baseline)**
Type: fix — STRUCTURAL project-side (client-deliverable quality). The shipped client operating docs fail the shipped client doc-gate's OWN full-scan, so a fresh client install has docs that fail their own gate.
Status: Open
Target: v11.0 — pre-launch client-deliverable quality gate. A fresh client install must not fail its own shipped doc-gate on first run.
Blockers: none on repo state. COORDINATE (shared surfaces, sequence to avoid churn): BD-245 (renames the trinity rules heading + strips per-CLI memory endorsements) and BD-253 (adds a `target: docs/project/session-state.json` record to the SAME `.docs-gate-allowlist.txt`, and adds a rule to the trinity) both co-edit the trinity + the allowlist. BD-253 is intentionally NOT blocked by this BD: BD-253's own verification probe is dangling-axis-SCOPED to its new artifact (not a full-scan), so it does not trip on this pre-existing RED.
Unblocks: the shipped client deliverable passes its own doc-gate on a clean install; a standing guard prevents silent regression when future BDs edit `project-template/` operating docs.
Provenance: surfaced 2026-07-01 during BD-253's plan-reconciliation (its S2 `project-template/` full-scan probe). Root cause: pack CI never runs the shipped `validate-docs.sh` full-scan against `project-template/` (the gate is designed to run in the CLIENT's installed repo), so template drift accumulated silently. The client gate was built + tightened across BD-243 (created the gate), BD-206 (added the conformance / dangling / deferred axes + the allowlist), and BD-258 (reworded the pipeline content) WITHOUT ever running the gate full-scan against the shipped template + completing the allowlist — i.e. the `ci-guard-measure-then-bound` + `enumerate-encoding-surfaces` discipline was applied to the pack gate but NOT to the CLIENT gate against the shipped template.
Problem: `bash project-template/scripts/validate-docs.sh` (full scan) is RED at baseline (exit 1; 10 violations) against the shipped `project-template/` docs. Three failure classes:
  - **REAL bloat (3) — a doc defect:** the BD-258 "Large-phase pipeline standard (size-tiered)" bullet is 984 chars (cap 700) in all three trinity files — `project-template/CLAUDE.md:428`, `AGENTS.md:407`, `GEMINI.md:425`. The bullet must be TIGHTENED (lock-step ×3, trinity parity) or, if genuinely irreducible, allowlisted with a `reason:`.
  - **Deferred-axis false-positives (5) — a gate-config gap, docs are correct:** legitimate status-enum DOCUMENTATION lines that contain the vocabulary word "Deferred"/"deferred" — `docs/project/backlog/_rules.md:52` (`status-enum: … Deferred …`), `:66` (`Unblocked — … between Open and Deferred`), `:67` (`Deferred — deliberately postponed`); `docs/project/implementation-plan/_rules.md:44` (`status-enum: … deferred …`), `:66` (`… / deferred / …`). These are rule self-references defining the status vocabulary; the gate's own remediation text says to allowlist self-references. FIX = `.docs-gate-allowlist.txt` snippet records with `reason:`, NOT doc edits.
  - **Dangling runtime-generated (2) — a gate-config gap, docs are correct:** `docs/project/backlog/_rules.md:21` and `docs/project/changelog/_rules.md:21` reference `…/_toc.md`, which is RUNTIME-generated (absent in the pre-install template), so the ref dangles. FIX = `.docs-gate-allowlist.txt` `target:` records (the same will-exist-at-runtime mechanism BD-253 uses for `session-state.json`).
Net: 3 of 10 are a real doc defect (tighten the bullet); 7 of 10 are a legitimate-but-unallowlisted set (add records) — the gate is correct in principle but its allowlist was never sized against the shipped template.
File/Symbol:
  - `project-template/CLAUDE.md` — the 984-char "Large-phase pipeline standard" bullet (tighten; trinity parity)
  - `project-template/AGENTS.md` — same bullet (lock-step)
  - `project-template/GEMINI.md` — same bullet (lock-step)
  - `project-template/scripts/.docs-gate-allowlist.txt` — 5 deferred-axis snippet records + 2 dangling `target:` records (measure-then-bound; sized to the legitimate set)
  - `project-template/docs/project/backlog/_rules.md` — the flagged status-enum + `_toc` lines (allowlisted, likely no edit)
  - `project-template/docs/project/implementation-plan/_rules.md` — the flagged status-enum lines (allowlisted, likely no edit)
  - `project-template/docs/project/changelog/_rules.md` — the flagged `_toc` line (allowlisted, likely no edit)
  - a NEW standing guard (pack CI wiring and/or a `validate-pack` check) that runs the shipped `validate-docs.sh` full-scan against `project-template/` so future template-doc edits are held to the client gate
  - `supporting-docs/INSTALL-PROCEDURES.md` — 9 install-tree dangling fixes (pack-clone-path rewords + records; WIDENED scope per the design-gate ruling)
  - `supporting-docs/METHODOLOGY.md` — 6 install-tree dangling fixes (same ruling)
  - the guard's install-tree leg + the L3 target-liveness corpus amendment (WIDENED scope)
Description:
  Problem: (above) — the shipped client operating docs fail the shipped client doc-gate's own full-scan at baseline, so a fresh client install fails its own gate; the drift was silent because pack CI never full-scanned `project-template/`.
  Goal: `validate-docs.sh` full-scan GREEN against the shipped `project-template/` — (1) tighten the BD-258 pipeline bullet ×3 (trinity parity; preserve meaning) or allowlist-if-irreducible; (2) add allowlist records for the 5 legitimate status-enum self-references + the 2 runtime-generated `_toc.md` targets, each with a `reason:`, sized EXACTLY to the legitimate set (measure-then-bound — no over-wide record); (3) add a STANDING guard so the template cannot silently regress (a pack CI / `validate-pack` full-scan of `project-template/` — the BD-253 S2 probe generalized). Zero false-suppression; trinity parity preserved.
  Process: size-tiered large-BD standard (likely LARGE — cross-surface: trinity + 3 `_rules.md` + allowlist + a NEW guard; a NEW CI check is a structural signal). docs-researcher (MEASURE every current gate hit; categorize each KEEP→allowlist vs STRIP→fix — the measure-then-bound census) → architect (per-hit disposition + the standing-guard design + the BD-245/BD-253 shared-surface sequencing) → adversarial → reconcile-if-findings → user design gate → planner → coder waves. Confirm the exact pipeline when it runs. Design-gate rulings (2026-07-03, on `DESIGN-BD259-FINAL.md`): SCOPE WIDENED per the user — the fresh-INSTALL tree's 15 dangling violations (pack-side path leaks in `supporting-docs/INSTALL-PROCEDURES.md` + `supporting-docs/METHODOLOGY.md`; 4 measured classes with end-to-end-verified per-class recipes) are IN scope, incl. the install-tree guard leg + the L3 target-liveness corpus amendment; OQ-1 RULED YES — the client gate's `--self-test` becomes load-bearing in pack CI (guard leg); the trinity bullet is TIGHTENED to the verified 681-char draft (parity ×3), not allowlisted.
Acceptance criteria: `bash project-template/scripts/validate-docs.sh` (full scan) exits 0 against `project-template/`; the BD-258 pipeline bullet is ≤700 chars in all three trinity files (or allowlisted-irreducible with a `reason:`); the 5 status-enum self-references + 2 runtime-`_toc.md` refs are allowlisted with `reason:`/`target:` records sized to the legitimate set (no over-wide match); a standing guard runs the `project-template/` full-scan in pack CI (or `validate-pack`) so a future regression FAILS loudly; trinity parity preserved; `validate-pack` + the full wired battery green.
Resolved: n/a
Position: v11.0 — surfaced by BD-253's reconciliation (the S2 probe); a pre-launch client-deliverable quality fix. Coordinate with BD-245 + BD-253 on the shared trinity + `.docs-gate-allowlist.txt` surfaces.
