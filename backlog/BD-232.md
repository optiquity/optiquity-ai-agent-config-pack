<!-- per-entry source: /backlog/BD-232.md; contract: /backlog/_rules.md -->
**BD-232 — Reorganize + reduce Pack Chat's out-of-repo memory cache (architect-designed reorg, coder-rewritten content; Pack Chat installs only)**
Type: chore — Pack Chat operating-state maintenance. Reorganize and shrink Pack Chat's OUT-OF-REPO memory cache so it holds fewer, well-organized memories — process & operations rules only, no project state. An architect designs the reorganization; a coder rewrites the content; Pack Chat only installs the reviewed result. Opened 2026-06-18 (user direction).
Status: Open
Target: v11.0 — resequenced to run directly after BD-234 (user 2026-06-19; was originally right after BD-225/229/230/231).
Blockers: none — independent of repo state; sequenced per the user's queue.
Boundary (absolute): the ONLY artifacts touched are the out-of-repo Pack Chat memory cache files at `~/.claude/projects/-Users-david-Developer-optiquity-ai-agent-config-pack/memory/*.md` + that directory's `MEMORY.md` index. EXPLICITLY NOT the trinity `## Pack memory` sections, NOT `CLAUDE.md`/`AGENTS.md`/`GEMINI.md`, NOT any pack-repo content. The trinity SSOT and all repo files are untouched. (This BD's own entry + standard design/impl process docs are the only repo artifacts; the memory cache itself is out-of-repo and produces no repo commits.)
File/Symbol:
  - Target set: the out-of-repo memory directory (54 `*.md` memory files + `MEMORY.md` as of 2026-06-18).
  - Staging/handoff: an out-of-repo directory the coder writes the rewritten set into (e.g. `/tmp/handoff-bd232-memory/`); Pack Chat installs from there after review-clean.
Description:
  **Problem:** The memory cache holds 54 files — too many, not well organized, and it contains project state that does not belong in memory.
  **Goal:** fewer, well-organized memories; PROCESS & OPERATIONS rules only; ZERO project state. The reorganization and rewrite are done by an architect + coder — NOT by Pack Chat (user does not want Pack Chat designing or rewriting its own memories).
  **Process (required flow):**
    1. `pack-architect` (read-only) reads every current memory file + `MEMORY.md` and designs the reorganization: which to DELETE, which to MERGE, which to EDIT, the new organization/grouping, the new memory-governance rules (what qualifies as a memory, dedup discipline, a count/size discipline), and the target count. Project state is removed — the architect first confirms each `project_*` memory's content is already represented in the backlog (the user's system), so nothing is lost. `reference_*` operational facts are classified (keep/merge/edit), NOT treated as project state. Output: a design doc.
    2. User reviews + approves the architect design (the planner-to-coder gate).
    3. `pack-coder` rewrites the new, reduced, organized memory SET (the rewritten memory files + a regenerated `MEMORY.md` index) into the out-of-repo staging directory, preserving the required memory frontmatter format (`name` / `description` / `metadata.type`). The coder does NOT mutate the live memory directory. Output: an impl report.
    4. Standard bounded review/fix cycle (`pack-reviewer`) against the architect design (≤2 review/fix pairs + 1 final reviewer).
    5. After review-clean + user approval, Pack Chat INSTALLS the reviewed set into the live out-of-repo memory directory (old set → new set). This install is the only step Pack Chat performs on the content.
  **Why architect + coder (not Pack Chat):** user-directed — Pack Chat is not trusted to organize/rewrite its own memories; the architect/coder split provides an independent designer and an independent author, with the user approving both gates.
Acceptance criteria: memory count materially reduced and organized; every retained memory is a process/operations rule or an operational reference; ZERO project-state memories and no "Active project state" section in `MEMORY.md`; `MEMORY.md` regenerated with all pointers resolving (no dangling); required frontmatter format preserved on every file; trinity + all repo files provably untouched (out-of-repo scope only); the reorganization is architect-designed and the content is coder-rewritten (not Pack-Chat-authored); user approves the architect design and the final install.
References: user direction 2026-06-18 (this BD's scope + the architect→coder→Pack-Chat-install flow); the out-of-repo memory directory; the manual deletion of 8 stale/project-state memories earlier in the 2026-06-18 session (the precedent for "too many / stale / project-state-doesn't-belong"); the pack-architect-spawn-protocol (architect-first when work touches memory files).
Resolved: n/a
Position: v11.0 — out-of-repo Pack Chat operating-state maintenance; sequenced directly after BD-234 (user-resequenced 2026-06-19; previously right after BD-225/229/230/231). Produces no memory-file repo commits (out-of-repo); only the BD entry + standard design/impl process docs are repo artifacts.
Note (2026-06-20, user direction — scope STRENGTHENED; SUPERSEDES the original "reduce + retain process/ops rules as memories" framing): caching pack/project RULES in out-of-repo memory is a FAILURE MODE — the pack cannot version or control out-of-repo memory, so a cached rule copy goes stale silently when the in-repo rule changes, and Pack Chat would act on the wrong version with no audit trail the user could trace. NEW TARGET — for memories ABOUT pack/project-side rules there are EXACTLY THREE (meta-governance only): (1) NEVER put a pack- or project-related RULE in a memory; (2) RE-READ the pack + project rules from the in-repo SSOT at the START OF EVERY COMMIT; (3) save unsaved STATE or notes about pack/project rules ONLY in a BD or a new doc — never a memory. Every other memory that encodes a pack/project rule or project state is REMOVED (not merely "reduced"). SAFE REMOVAL: before deleting any memory, VERIFY its content is captured in the in-repo SSOT (trinity `## Pack memory`, `PACK-CHAT.md`, `PACK-AGENTS.md`, the `/backlog/` + `/changelog/` trees + `_rules.md`, `README`); MIGRATE any orphan (memory-only) rule into the in-repo SSOT FIRST (reconcile with BD-238's adversarial-memory reconciliation) so no rule is lost. This EXPANDS BD-232's boundary beyond out-of-repo-only to ALSO codify the three meta-rules in the pack (`PACK-CHAT.md` / trinity) so they are pack-controlled + enforced, and to perform the orphan migration. Genuine NON-pack environment facts (e.g. `gh` PAT can't-delete, GitHub-MCP variant, Free-plan branch-protection) are classified separately at the architect design gate (keep as runtime-verifiable, or drop + re-verify at runtime). Pack Chat OPERATES OFF THE IN-REPO SSOT EFFECTIVE IMMEDIATELY (sources rules from the live in-repo files, not the cache). Sequencing UNCHANGED (after BD-234).
