<!-- per-entry source: /backlog/BD-232.md; contract: /backlog/_rules.md -->
**BD-232 — Reorganize + reduce Pack Chat's out-of-repo memory cache (architect-designed reorg, coder-rewritten content; Pack Chat installs only)**
Type: chore — Pack Chat operating-state maintenance. Reorganize and shrink Pack Chat's OUT-OF-REPO memory cache so it holds fewer, well-organized memories — process & operations rules only, no project state. An architect designs the reorganization; a coder rewrites the content; Pack Chat only installs the reviewed result. Opened 2026-06-18 (user direction).
Status: Resolved
Target: v11.0 — resequenced to run directly after BD-234 (user 2026-06-19; was originally right after BD-225/229/230/231). ADJACENCY UPDATE (user 2026-06-23): BD-222 is now inserted directly after BD-234, so BD-232 follows BD-222 (still after BD-234).
Blockers: none — independent of repo state; sequenced per the user's queue.
Boundary: TWO surfaces. (1) OUT-OF-REPO — the Pack Chat memory cache at `~/.claude/projects/-Users-david-Developer-optiquity-ai-agent-config-pack/memory/*.md` + that directory's `MEMORY.md` index, reduced to the 3 meta-governance memories + genuine environment facts (out-of-repo; produces no repo commits). (2) IN-REPO — the destination boundary is EXPANDED (user re-scope 2026-07-01, decision D; supersedes the earlier "governance surfaces only [trinity `## Pack memory` + `PACK-CHAT.md`]" framing) to the FOUR destination families a keeper may home in, each by BEST-FIT AUDIENCE: (a) the trinity `## Pack memory` + `pack-ops/PACK-MEMORY-RATIONALE.md` (the `[rationale:]` bijection); (b) `pack-ops/PACK-CHAT.md` / `pack-ops/PACK-AGENTS.md`; (c) the pack AGENT defs `.claude/agents/pack-*.md`; (d) the pack SKILL files `.claude/skills/*/SKILL.md`. All four families were AUTHORIZED as homes; the agent-def + skill families were authorized but this BD homed NOTHING there — C1-C3 used only the trinity `## Pack memory` + `PACK-MEMORY-RATIONALE.md` (the `memory-not-an-ssot` meta-rule + the 7 new spawn-rule slugs + 8 rationale-prose consolidations) and `pack-ops/PACK-CHAT.md` (the 11 orchestrator behavioral bullets). No pack-repo content OUTSIDE those four families (+ this BD's own entry + standard design/impl process docs) is touched. State persistence is governed by BD-252 (anything worth saving goes to docs or `pack-ops/session-state.json`, never memory — memory is brittle + does not persist across machines).
File/Symbol:
  - Target set: the out-of-repo memory directory (54 files = 53 topic `*.md` + `MEMORY.md`, as of 2026-06-18/measured 2026-07-01).
  - Staging/handoff: an out-of-repo directory the coder writes the rewritten set into (e.g. `/tmp/handoff-bd232-memory/`); Pack Chat installs from there after review-clean.
  - Dispositions (locked at the 2026-07-01 re-scope; the 53 topic memories partition exactly; `MEMORY.md` regenerated separately):
    - DELETE 22 (no migration) = 14 already-backed in the in-repo SSOT (pure delete) + 1 project-state removed per BD-232's zero-project-state mandate (git-real BD `Status:`-backed, stale state not a durable rule) + 4 rare user-judged-not-useful pure deletes (`dormant_subsystem_blast_radius`, `external_rules_census_before_design`, `progress_marker_for_multi_step_phases`, `no_prefix_chars`) + 3 deferred-feature preserved in `maintenance-docs/` (`tracker_carrier_no_sidecar`, `tracker_portability`, `groupings_design_principles`).
    - INSTALL 27 = migrate-then-delete into a best-fit in-repo home per the placement design, then delete the memory (SAFE-REMOVAL: grep-present at its home BEFORE deletion).
    - LEAVE 4 (user decision B) = the local environment/setup facts `reference_branch_protection_unavailable`, `reference_gh_pat_no_delete`, `reference_github_mcp_availability`, `reference_sendmessage_uuid_addressing` — NEITHER deleted NOR migrated (env/setup facts, not pack rules); they stay in the cache as-is and remain indexed by the regenerated `MEMORY.md`.
    - Arithmetic: 22 DELETE + 27 INSTALL + 4 LEAVE = 53 topic memories.
    - End-state = 8 files = 4 LEAVE env facts + 3 NEW meta-governance pointer memories + the regenerated `MEMORY.md`.
Description:
  **Problem:** The memory cache holds 54 files — too many, not well organized, and it contains project state that does not belong in memory.
  **Goal:** fewer, well-organized memories; PROCESS & OPERATIONS rules only; ZERO project state. The reorganization and rewrite are done by an architect + coder — NOT by Pack Chat (user does not want Pack Chat designing or rewriting its own memories).
  **Process (deterministic LARGE flow — BD-232 is LARGE: cross-surface [trinity `## Pack memory` + `PACK-CHAT.md`] + a required memory census; the standing large-BD standard governs; reconciled from the pre-BD-258 lighter draft):**
    1. `pack-docs-researcher` (ALWAYS first) — internal census: reads every current memory file + `MEMORY.md`; classifies each (meta-governance / process-ops rule / project-state / genuine environment-fact); for each, records whether its content is BACKED by the in-repo SSOT (trinity `## Pack memory`, `PACK-CHAT.md`, `PACK-AGENTS.md`, the `/backlog/`+`/changelog/` trees + `_rules.md`, `README`) with the backing location, or is ORPHAN (memory-only); and records the 3 meta-rules' current in-repo coverage. Output: a census report (facts only — no DELETE/MERGE/EDIT verdicts, no design).
    2. `pack-architect` designs the reorganization from the census: the DELETE/MERGE/EDIT verdicts, the new organization/grouping, the memory-governance rules (what qualifies as a memory, dedup + count/size discipline), the target count, the in-repo codification of the 3 meta-rules (trinity `## Pack memory` + `PACK-CHAT.md`), and the orphan-migration plan. Project state is removed only where the census confirms it is already in the backlog (nothing lost); `reference_*` operational facts are classified keep/merge/edit. Output: a design doc.
    3. Adversarial architect review (ALWAYS) → reconciliation architect (a FRESH independent instance, iff the adversarial returned findings).
    4. User reviews + approves the architect design (design gate).
    5. `pack-planner` sequences the work (staging/install steps + the in-repo codification/migration commits + the verification/review strategy) → adversarial planner (ALWAYS) → reconciliation planner (FRESH, iff findings) → user planner-to-coder gate.
    6. `pack-coder` waves + bounded review/fix cycle (`pack-reviewer`, ≤2 review/fix pairs + 1 final reviewer per commit): (a) rewrite the reduced, organized memory SET (rewritten files + regenerated `MEMORY.md`) into an out-of-repo staging directory, preserving the required frontmatter (`name` / `description` / `metadata.type`), WITHOUT mutating the live memory dir; (b) apply the in-repo codification + orphan migration.
    7. After review-clean + user approval: Pack Chat COMMITS the in-repo codification/migration and INSTALLS the reviewed memory set into the live out-of-repo directory (old → new). The install is the only step Pack Chat performs on the memory content.
  **Why architect + coder (not Pack Chat):** user-directed — Pack Chat is not trusted to organize/rewrite its own memories; the architect/coder split provides an independent designer and an independent author, with the user approving both gates.
Acceptance criteria: memory count materially reduced and organized; every retained memory is a process/operations rule or an operational reference; ZERO project-state memories and no "Active project state" section in `MEMORY.md`; `MEMORY.md` regenerated with all pointers resolving (no dangling); required frontmatter format preserved on every file; the in-repo codification (the 3 meta-rules in `pack-ops/PACK-CHAT.md` + the trinity `## Pack memory`) and any orphan-rule migration into the SSOT are complete + committed, and no pack-repo file OUTSIDE those governance surfaces is touched; the reorganization is architect-designed and the content is coder-rewritten (not Pack-Chat-authored); user approves the architect design and the final install. PLACEMENT-DESIGN acceptance (2026-07-01 re-scope): each of the 27 INSTALL keepers is homed in a versioned in-repo surface (one of the four destination families by best-fit audience) and is grep-present at its home BEFORE its memory is deleted (the SAFE-REMOVAL migration-before-deletion gate — no content-loss window); NO rule is lost; NO CI guard trips (validate-pack + `PACK_VALIDATE_DEEP=1` + the full battery green across C1-C3); the 3 meta-governance pointer memories carry `metadata.node_type: memory` frontmatter; the regenerated `MEMORY.md` indexes exactly the 3 meta + 4 env facts (7 topic entries) with NO "Active project state" section; the retained cache = exactly 8 files (4 LEAVE env + 3 meta + `MEMORY.md`).
References: user direction 2026-06-18 (this BD's scope + the architect→coder→Pack-Chat-install flow); BD-252 (pack-side session-state mechanism — GOVERNS this BD's state persistence: anything worth saving goes to docs or `pack-ops/session-state.json`, never memory); the out-of-repo memory directory; the manual deletion of 8 stale/project-state memories earlier in the 2026-06-18 session (the precedent for "too many / stale / project-state-doesn't-belong"); the pack-architect-spawn-protocol (architect-first when work touches memory files).
Resolved: 2026-07-01 (v11.0) — out-of-repo Pack-Chat memory cache reduced 54→8 files (kept the 4 environment/setup facts + 3 new meta-governance pointer memories + regenerated `MEMORY.md`; ZERO rule/project-state memories, no "Active project state"). Landed via the full deterministic large-BD pipeline: docs-researcher census → architect placement design → adversarial → reconcile → user design gate → planner → adversarial → reconcile → user planner-to-coder gate → 4 coder waves (fresh reviewer per commit, all CLEAN) → reviewed out-of-repo install. In-repo: 27 keepers migrated across 3 commits — C1 codified the `memory-not-an-ssot` meta-rule (never cache a rule in memory; re-read rules from the in-repo SSOT at commit start; state only in a BD/doc/`session-state.json`) across the trinity `## Pack memory` + `PACK-MEMORY-RATIONALE.md` + `PACK-CHAT.md` op-leg + spawn-rule-manifest; C2 migrated 11 orchestrator disciplines to `PACK-CHAT.md § Behavioral rules`; C3 added 7 spawn-relevant trinity slugs + 8 rationale-prose consolidations (bijection 32→39); C4a recorded the dispositions + expanded destination boundary in this entry. 22 memories deleted (14 already-backed + 1 project-state + 4 rare-not-useful + 3 deferred-feature preserved in `maintenance-docs/`); 4 environment facts left as-is (out of pack/project scope, user decision). validate-pack + `PACK_VALIDATE_DEEP=1` + the 97-test wired battery green at each commit.
Position: v11.0 — out-of-repo Pack Chat operating-state maintenance; sequenced directly after BD-234 (user-resequenced 2026-06-19; previously right after BD-225/229/230/231). Produces no memory-file repo commits (out-of-repo); only the BD entry + standard design/impl process docs are repo artifacts.
Note (2026-06-20, user direction — scope STRENGTHENED; SUPERSEDES the original "reduce + retain process/ops rules as memories" framing): caching pack/project RULES in out-of-repo memory is a FAILURE MODE — the pack cannot version or control out-of-repo memory, so a cached rule copy goes stale silently when the in-repo rule changes, and Pack Chat would act on the wrong version with no audit trail the user could trace. NEW TARGET — for memories ABOUT pack/project-side rules there are EXACTLY THREE (meta-governance only): (1) NEVER put a pack- or project-related RULE in a memory; (2) RE-READ the pack + project rules from the in-repo SSOT at the START OF EVERY COMMIT; (3) save unsaved STATE or notes about pack/project rules ONLY in a BD, a doc, or `pack-ops/session-state.json` (governed by BD-252) — never a memory (memory is brittle + does not persist across machines). Every other memory that encodes a pack/project rule or project state is REMOVED (not merely "reduced"). SAFE REMOVAL: before deleting any memory, VERIFY its content is captured in the in-repo SSOT (trinity `## Pack memory`, `PACK-CHAT.md`, `PACK-AGENTS.md`, the `/backlog/` + `/changelog/` trees + `_rules.md`, `README`); MIGRATE any orphan (memory-only) rule into the in-repo SSOT FIRST (reconcile with BD-238's adversarial-memory reconciliation) so no rule is lost. This EXPANDS BD-232's boundary beyond out-of-repo-only to ALSO codify the three meta-rules in the pack (`PACK-CHAT.md` / trinity) so they are pack-controlled + enforced, and to perform the orphan migration. Genuine NON-pack environment facts (e.g. `gh` PAT can't-delete, GitHub-MCP variant, Free-plan branch-protection) are classified separately at the architect design gate (keep as runtime-verifiable, or drop + re-verify at runtime). Pack Chat OPERATES OFF THE IN-REPO SSOT EFFECTIVE IMMEDIATELY (sources rules from the live in-repo files, not the cache). Sequencing UNCHANGED (after BD-234).
