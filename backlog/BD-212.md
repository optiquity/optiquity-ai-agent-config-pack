<!-- per-entry source: /backlog/BD-212.md; contract: /backlog/_rules.md -->
**BD-212 — `pack tracker reset` verb + 3-level recovery (pack side; supersedes BD-103 with BD-213)**
Type: feat — pack-side implementation of the reset verb + pack-surface recovery documentation. Supersedes the pack half of BD-103 (Deprecated); the project-side application is BD-213.
Status: Open
Target: v11.0
Blockers: None (all BD-103 blockers — BD-066/067/070/076/084/098 — are Resolved).
Unblocks: BD-213 (project-side application reuses this BD's design unchanged); hard-level recovery (reset + re-forward) for the BD-204 dogfooded pack tracker.
File/Symbol: `scripts/pack-tracker.sh`, `scripts/lib/pack-tracker/reset.sh` (final lib placement per architect), `scripts/tests/test-tracker-reset.sh`, `pack-ops/HELP-FRAGMENT-TRACKER.md` (verb help; NOTE: pack-ops/ but client-installed at S11 — boundary disposition is an architect item), `pack-ops/PACK-CHAT.md` (pack-surface recovery prose).
Description: Friction-by-design bulk-delete for pack-marked GH issues, per the
  locked design (V1 §6.K) ADAPTED to the BD-204 as-built machinery: confirm
  flag verbatim `--confirm-i-have-admin-and-want-to-delete-all-pack-issues`;
  admin-permission check; marker-scoped delete (ONLY issues carrying
  `<!-- pack-id: -->` match — user-created and inbound-lane issues are
  untouchable); throttled (≥ the provider's min-write interval; honor
  retry-after). 3-level recovery documented for the PACK surface: soft
  (reverse + fix + re-forward), hard (reset + re-forward), nuclear
  (restore-from-git/backup).
  PIPELINE (user 2026-06-11): researcher → architect → planner → coder, each
  gate per standing rules. Researcher-first is REQUIRED (external-rules
  census): enumerate the official GH issue-deletion rule set — `deleteIssue`
  mutation shape via live schema introspection, permission/role requirements,
  token-type behavior (classic PAT vs fine-grained), rate/abuse limits — and
  live-probe deletability on a SCRATCH repo (per-step user approval;
  archive-only disposal) BEFORE the architect. Architect is REQUIRED
  (PACK-CHAT.md prose is an operating-doc trigger; and the §6.K-era design
  predates BD-204's as-built provider/id-map/oracle reality and must be
  reconciled, per architect-doc-vs-reality).
  TRACKER-AGNOSTIC (HARD, same property as BD-204): reset routes through
  the BD-060 TrackerProvider abstraction — a provider-routed delete/reset
  op with a DECLARED capability (providers differ: GH needs repo-admin;
  some trackers forbid or soft-delete only). NO GH assumption in the
  agnostic layer; all `deleteIssue`/PAT/introspection specifics live in
  `tracker-provider-gh.sh` + its capability block. A backend that cannot
  delete declares so and reset FAILS LOUD with the documented fallback
  (close + void-label), never assuming GH semantics. The researcher's
  census is GH-scoped (the canonical backend); the architect defines the
  agnostic op + capability contract.
  GENERALIZABLE (HARD, same property as BD-204): the verb and lib are
  surface-parameterized, never pack-hardwired; researcher + architect are
  PROJECT-AWARE (cover both surfaces' requirements) but implementation here
  is PACK-SIDE ONLY — NO project-template/ or client-doc edits (that is
  BD-213, by design, so client adaptation is mechanical later). Pack-side
  commits carry `pack-only` (Check 36-enforced).
  Verification: scratch-repo test leg via the proven BD-204 C-7 oracle
  pattern — provision scratch, forward a fixture, plant a DECOY user-issue
  (no pack-id marker) + a decoy inbound-lane issue, run reset with the
  confirm flag, assert pack-marked deleted + decoys survive + throttle
  observed + admin check fails loud on insufficient permission; archive
  scratch at end (tool never deletes repos; manual-delete recommendation).
Position: After BD-204 C-8 for implementation; the RESEARCHER step MAY run pre-C-8 as a C-8 de-risk (user decision pending at authoring time). Before BD-213.
Resolved: n/a
