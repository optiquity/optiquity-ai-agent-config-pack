<!-- per-entry source: /backlog/BD-265.md; contract: /backlog/_rules.md -->
**BD-265 — groupings.sh query CLI: the five locked verbs over groupings-lib.sh + derived-status/target columns + deps --deferral cascade view (BD-189 CBD-4)**
Type: feat — client CLI (project-side deliverable). The BD-189 Wave-2 query-surface child. Opened 2026-07-05 at C0 per PLAN-BD189-FOLDED.md F1.
Status: Resolved
Target: v11.0 — Wave 2 (parallel; disjoint file sets).
Blockers: BD-262 (the lib API).
Unblocks: BD-268.
File/Symbol:
  - NEW `project-template/scripts/groupings.sh`
  - NEW `scripts/tests/test-groupings-cli.sh`
Description:
  Scope (verbatim from PLAN-BD189-FOLDED.md F1/BD-265): the 5 locked verbs; FIVE-code typed-error enum (`unknown-id`/`bad-ref`/`no-tree`/`parse`/`reserved`); `shared-with GRP-000` → `ERROR(reserved)`; `-q` rows = the §T-2 AMENDED machine grammar verbatim; `list` columns: ID, Kind, derived status, percent, Target (bare token), Title, members + the GRP-000 tail line; the grouping-arm detail header composed `GRP-NNN — <Title> — <Kind> — <derived> D/A (P%) [flags] — <derived-target> t=K/N` (suffix rules §T-2: `— unknown` bare on poison; nothing on `-`; N = A−D; GRP-000 header stays count-only); `deps --deferral` — the item-5 s1 cascade presentation: the deferral-poisoned set with per-phase edge attribution + the §T-2 target annotation rows (`tgt= impl= via=`) + per-affected-grouping `poisoned-max=`; `grp_rollup GRP-000` → `ERROR(reserved)` surfaced verbatim. Dependency-grammar citations = the four-field code (validate-docs.sh:833-835) — never the comment layer.
  Acceptance (verbatim): golden outputs per verb incl. empty-tree `(no groupings)` exit 0; typed-error goldens all FIVE codes; goldens authored ONCE, target-aware and B′-aware from birth (the 59-token/120-fraction behavioral reach lands here as first-authored bytes, never re-authored); the `poisoned-max=` worked rows; the composed detail header golden incl. Kind token + `t=K/N` suffix and its poison/absent arms; `deps --deferral` tension row for an untargeted DEFERRED blocker with an implied bound; no Check-22 fire (PM-CHAT.md untouched); full battery green.
Resolved: 2026-07-05 — Landed (the last Wave-2 child): the client query CLI — the five locked verbs as a thin lib-consuming layer (zero re-derivation; the parity probe byte-equal), the five-code typed-error enum, the composed detail header with all suffix arms, `deps --deferral` with the target annotations + `poisoned-max=`. The full bounded cycle ran to its ceiling: review (the M1 thinness breach — the CLI-local enum parse — empirically proven) → the 4-fix pass (read_enum as a statement-for-statement lib mirror; fail-loud; the empty-tree asserts; the posture normalization) → re-review (CLEAN + 2 observations) → the 2-residual micro-fix (the MEASURED Unicode separator-class mirror `libsep()`, locale-safe, claim bounded to the proven plane; `tv=unknown`) → the final pass-3 check (CLEAN — own-character verification). Suite 57/57; at landing the generator's presence-keyed parity leg went LIVE and passed (the cross-consumer triangle closed). The landing subject's scope keyword was corrected pre-push (mixed pack+project — no keyword).
