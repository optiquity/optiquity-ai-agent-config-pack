<!-- per-entry source: /backlog/BD-241.md; contract: /backlog/_rules.md -->
**BD-241 — Make previously-spawned still-alive agents discoverable: unique spawn NAMES + a durable spawn REGISTRY + a name→agentId→message-id lookup precedence (Claude-only mechanism; pack + project)**
Type: feat — process / agent-orchestration discoverability (trinity `## Pack memory`, Claude-only sub-section; pack-side + project-side surfaces). External + internal docs researcher → architect FIRST, then planner → coder.
Status: Resolved
Target: v11.0 — small process discoverability fix (user 2026-06-20). Not a launch blocker; sequence after/with BD-240 at user discretion.
Blockers: none — independent of repo state (surfaced alongside BD-240 on 2026-06-20).
Unblocks: the orchestrator (Pack Chat pack-side; the project orchestrator project-side) can RELIABLY re-find + re-engage a still-alive previously-spawned agent without transcript archaeology — and the user never has to supply the method.
Provenance: 2026-06-20 — re-engaging the BD-206 docs-researcher required digging its `agentId` (`a7202065c22979cf5`) out of the session JSONL because nothing made it discoverable; the user had to TELL Pack Chat the re-find method. User direction (verbatim intent): (1) all spawned agents UNIQUELY NAMED; (2) find by NAME first, else agentId/UUID, else message-id, else whatever works; (3) a durable REGISTRY the orchestrator consults (no transcript archaeology); (4) Claude-only mechanism (Codex/Antigravity have no peer-messaging — architect determines their applicability, likely BD-217); (5) KEEP the fresh-agent-default policy — this fixes HOW-to-find, not WHEN-to-reengage.
File/Symbol (measured 2026-06-20, HEAD `ef8d6ff`):
  - The Agent tool's `name` parameter ("Makes it addressable via SendMessage({to: name}) while running") is NOT currently used as a discipline — spawns carry only a `description`; the `agentId` is auto-generated (e.g. `a7202065c22979cf5`) and only recoverable from the spawn `tool_result` in the session transcript. SendMessage `to` accepts the `agentId` to resume a completed-but-warm agent ("resumed from transcript").
  - Existing rules to RECONCILE (no contradiction): `reference_sendmessage_uuid_addressing` (UUIDs work; prefer naming at spawn) — this BD REFINES it into a registry + precedence; `feedback_fresh_agent_default_no_sendmessage` (fresh agent by default; re-engaging needs user approval) — UNCHANGED; this BD gates under it.
  - Natural home: trinity sub-section "Sub-agent behavior (Claude-only)" (SendMessage / Agent-Teams / resume-by-agentId are Claude-only per the existing trinity exemption — Codex/Antigravity confirmed absent).
  - Project-side surfaces (mechanism Claude-only): the project orchestrator docs — `project-template/docs/pack/METHODOLOGY.md` / `PM-CHAT.md` / the project trinity Project-memory — the architect determines exact homes.
Description:
  **Problem:** a still-alive previously-spawned agent is re-findable ONLY by transcript archaeology (digging the `agentId` out of the session JSONL); spawns are not uniquely named, and there is no registry — so the orchestrator (and the user) cannot reliably re-engage a warm agent. The fresh-agent-default policy is correct; the gap is pure DISCOVERABILITY.
  **Goal (user-decided scope):** (1) every spawned agent gets a UNIQUE, descriptive NAME at spawn (Agent-tool `name`), so it is addressable by name; (2) a DURABLE spawn REGISTRY the orchestrator records each spawn into (name, agentId, purpose, status) and CONSULTS to re-find agents — no transcript archaeology; (3) a documented LOOKUP PRECEDENCE: by NAME → agentId/UUID → message-id → whatever works. The find/re-engage MECHANISM is Claude-only on BOTH pack + project sides (the architect determines Codex/Antigravity applicability — likely deferred to BD-217); the unique-naming discipline applies wherever agents spawn. KEEP `fresh-agent-default` + re-engagement-needs-user-approval UNCHANGED — BD-241 fixes HOW-to-find, not WHEN-to-reengage.
  **Investigation (user-mandated, researchers-then-architect):** an EXTERNAL docs researcher (Claude Code Agent / SendMessage / Agent-Teams naming + resume semantics, agentId/message-id addressing, liveness/resumability against the actual CLI — verify-availability, not assume) AND an INTERNAL docs researcher (current spawn-construction surfaces, the two overlapping rules, where a durable registry would live, the project-side orchestrator surfaces) → ARCHITECT (design the naming convention + registry mechanism + lookup precedence + rule reconciliation, pack + project, Claude-only) → planner → coder.
Acceptance criteria: a rule + mechanism such that the orchestrator re-finds a still-alive spawned agent by NAME (registry-backed) with a documented fallback precedence (name → agentId → message-id → other); all spawns are uniquely named going forward; the registry is durable + consulted (no transcript archaeology); the Claude-only mechanism is documented on BOTH pack + project surfaces with Codex/Antigravity applicability determined (likely deferred BD-217) and the unique-naming discipline applied wherever agents spawn; reconciled with `reference_sendmessage_uuid_addressing` (refined) + `feedback_fresh_agent_default_no_sendmessage` (unchanged) with no contradiction; `validate-pack` green (rule↔rationale bijection, anti-restate, trinity parity, spawn-rule manifest); investigated by external + internal docs researcher then architect-designed (not Pack-Chat-authored).
References: `reference_sendmessage_uuid_addressing` + `feedback_fresh_agent_default_no_sendmessage` (to reconcile); trinity "Sub-agent behavior (Claude-only)" + the Claude-only trinity exemption; BD-217 (Codex/Antigravity cross-CLI coordination — the cross-CLI-applicability anchor); BD-240 (sibling 2026-06-20 process fix). Worked example: re-finding agent `a7202065c22979cf5` required JSONL archaeology.
Resolved: 2026-06-20 — codified 3 corpus rules (`spawn-unique-naming` + `spawn-registry-find` + `reconciliation-instance-independence`) + corrected the stale "confirmed-absent" cross-CLI rationale across 4 STRIP surfaces (incl. the SHIPPED `supporting-docs/METHODOLOGY.md`) + project-side surfaces. Scope evolved during design: Claude registry/find mechanism v11.0 + cross-CLI naming discipline now; Codex/Antigravity registry+find DESIGN deferred to v11.1/BD-217 (handoff spec); precedence name→agentId (no message-id primitive). Pipeline: ext+int researchers → architect → adversarial → fresh reconciliation → [D3 reconciliation-instance rule added (user) via a fresh architect + compatibility analysis (ZERO contradictions) + L418 correction architect] → planner → adversarial planner → fresh reconciliation planner → coder (isolated worktree) → reviewer (CLEAN+1 NIT) → fix-coder → post-fix reviewer (CLEAN). Commits `decc0bb` (pack-only: rules+rationale+S1/S4/S2) + `77fdae1` (cross-surface: METHODOLOGY S3 + project surfaces) + `40d2b88` (manifest regen). CI green (run 27883245779). The 2 out-of-repo memory edits originally scoped were CANCELLED (user 2026-06-20: no caching pack rules in memory — see BD-232). BD-217 updated as the cross-CLI discovery anchor.
Position: v11.0 — small process discoverability fix; surfaced 2026-06-20 (the user had to supply the re-find method). Not a launch blocker; sequence after/with BD-240 at user discretion.
