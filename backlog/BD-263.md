<!-- per-entry source: /backlog/BD-263.md; contract: /backlog/_rules.md -->
**BD-263 — Groupings provisioning on every v11.0+ path: S11 fourth block, cmd_update rows, migrator skeleton+toc seed, immutable manifest 3→4 (BD-189 CBD-2)**
Type: feat — provisioning (pack-side installers building the project-side deliverable). The BD-189 Wave-2 provisioning child. Opened 2026-07-05 at C0 per PLAN-BD189-FOLDED.md F1.
Status: Open
Target: v11.0 — Wave 2 (parallel with BD-264/265/266/267; disjoint file sets re-verified at the fold).
Blockers: BD-262.
Unblocks: BD-268; G1b if the split fires; the BD-171 harness seam.
File/Symbol:
  - `scripts/init-project.sh`
  - `scripts/immutable-manifest.sh`
  - `scripts/migrate-v10-to-v11.sh`
  - `scripts/lib/migrator-core.sh`
  - `scripts/tests/test-init-project.sh`
  - `scripts/tests/test-migrate-v10-to-v11.sh`
  - `test-fixtures/` EXPECTED surfaces via `test-fixtures/build.sh` (manifest at PUSH only)
  - NOT touched: `project-template/scripts/verify-immutable.sh`; `scripts/lib/validate_checks/boundary_refs.py`
Description:
  Scope (verbatim; unchanged from the held plan — no settlement, fold-spec, or audit delta targets this child): D6.0-D6.3 + F10 + F13; the OQ-9 ACK (`IMMUTABLE_PROJECT_RELS` 3→4); the #16 fixture legs as runtime-built test legs.
  Acceptance (verbatim): fresh S11 install → groupings docs present, `_format.md` absent, empty-seed `_toc.md`, manifest 4 rows, installed `verify-immutable.sh` rc=0; `--update` seeds + idempotent; user `GRP-*.md` never overwritten; migrator installs skeleton + toc seed; Checks 9/25/39/41/49/50 + DEEP green; full battery green.
Resolved: n/a
