<!-- per-entry source: /backlog/BD-245.md; contract: /backlog/_rules.md -->
**BD-245 — Strip per-CLI project memory from project deliverables (all 3 trinity CLIs); rename `## Project memory` → `## Project rules`; codify a CLI-symmetric no-project-memory prohibition (sibling of BD-232)**
Type: feat — STRUCTURAL project-side cleanup + governance rule (client deliverable). Removes every endorsement of a per-CLI project/session memory FEATURE — across ALL THREE trinity CLIs (Claude, Codex, Antigravity/Gemini), symmetrically — from the shipped project surfaces; renames the overloaded `## Project memory` trinity rules-codification section (NOT the feature) to `## Project rules`; and codifies an express, CLI-symmetric prohibition that project tasks never use a per-CLI project/session memory feature (the repo is the authoritative SSOT). The project-side sibling of BD-232 (the pack-side memory-cache reduction). Large-BD standard: researcher → architect (+ adversarial) → planner (+ adversarial) → coder waves.
Status: Resolved
Target: v11.0 — directly after BD-232 (user 2026-06-23); INFORMED BY BD-232 (do BD-232 first, then apply the analogous treatment to the project/client side).
Blockers: none on repo state. Sequencing: runs directly AFTER BD-232 (its pack-side sibling) so the project-side treatment is informed by what BD-232 codified pack-side (the three meta-rules: never cache a rule in memory; re-read rules from the in-repo SSOT; save state/notes in a BD, a doc, or the session-state file, never a memory). Not strictly bound — BD-232 is a starting point for the project-side adaptation, not a ceiling.
Unblocks: the shipped client deliverable no longer suggests or assumes ANY CLI's per-project/session memory feature; the term "project memory" is reserved EXCLUSIVELY for a CLI memory feature (which is forbidden for project tasks); the project's codified rules live in a clearly-named non-"memory" trinity section (`## Project rules`).
Provenance: user direction 2026-06-23 — discovered while mapping BD-239's project-side surfaces. The shipped project deliverable (a) ENDORSES per-CLI memory features — Claude (`project-template/docs/pack/PM-CHAT.md` "Per-project Claude memory cache (Claude-only) … projects may use per-project memory at `~/.claude/projects/<slug>/memory/`") and Antigravity/Gemini (`project-template/GEMINI.md` "Cross-session memory: persist facts to `~/.gemini/GEMINI.md`"; `supporting-docs/CLI-PM-SETUP.md`; PM-CHAT cross-session-memory passages) — which contradicts the user's standing rule that per-project memory is a failure mode (a cached rule goes stale silently; the repo cannot version/control it); and (b) carries an OVERLOADED `## Project memory` trinity section name that is a rules-codification section, not the feature. User direction: the prohibition is SYMMETRIC across all three trinity CLIs — Claude, Codex, AND Antigravity/Gemini all have (or may have) a CLI project/session memory feature, and none may be used for project tasks.
File/Symbol (candidate surfaces, measured 2026-06-23 @ HEAD `7caff91` — researcher/architect produce the DEFINITIVE per-CLI set when this BD runs):
  - **FEATURE-endorsement STRIP set (all 3 CLIs):** Claude — `project-template/docs/pack/PM-CHAT.md` "Per-project Claude memory cache" block (~L889-891). Antigravity/Gemini — `project-template/GEMINI.md` Cross-session memory (~L509); `project-template/docs/pack/PM-CHAT.md` `### Cross-session memory` (~L981) + Antigravity memory-write passage (~L987); `supporting-docs/CLI-PM-SETUP.md` Cross-session memory (~L161-166). Codex — the deliverable currently CLAIMS "Codex CLI … has no equivalent per-project memory" (`PM-CHAT.md` ~L899); per user direction all three CLIs can use a memory feature, so the researcher RE-VERIFIES Codex's current memory mechanism against authoritative docs and the strip/prohibition covers it symmetrically (do NOT assume Codex is exempt). Also `supporting-docs/METHODOLOGY.md` (~L165 memory-invocation phrasing — review).
  - **SECTION RENAME set (`## Project memory` → `## Project rules`):** the trinity heading ×3 — `project-template/CLAUDE.md` (~L360), `AGENTS.md` (~L339), `GEMINI.md` (~L357) — PLUS every reference, in lock-step: `project-template/docs/pack/PM-CHAT.md` (~L35 "see `## Project memory` in CLAUDE.md…"), `supporting-docs/METHODOLOGY.md` (~L145, ~L1611, ~L1615 "§ Project memory"), and the SHIPPED client gate `project-template/scripts/validate-docs.sh` (the bloat AXIS keyed on the literal string `"## Project memory"` ~L18/L210/L255-259/L309/L356/L453/L463). Renaming the section REQUIRES updating this gate in lock-step (`enumerate-encoding-surfaces`).
  - **KEEP (do NOT strip):** the aligned "the repo is the authoritative memory, not session history; agents have no persistent memory" framing (`PM-CHAT.md` ~L1067, `supporting-docs/METHODOLOGY.md` ~L81/L336, `CLI-PM-SETUP.md` ~L43/L72/L195) — this is the correct message and stays.
  - **NEW prohibition rule:** the renamed `## Project rules` trinity section ×3 + the project-side governance docs (`PM-CHAT.md` / METHODOLOGY) — an express, CLI-SYMMETRIC rule that project tasks never use a per-CLI project/session memory feature (Claude / Codex / Antigravity-Gemini alike); the repo SSOT is the authority. Mirrors BD-232's pack-side meta-rules, project-audience, generalized to all three CLIs.
Description:
  Problem: the shipped client deliverable endorses per-CLI memory features (a stale-silently failure mode the user forbids) for at least two CLIs and asymmetrically exempts a third (Codex), AND overloads the term "project memory" with a rules-codification trinity section — so a client agent on ANY of the three CLIs could be told to use per-project memory, and could confuse the rules-section with the feature.
  Goal: (1) STRIP every per-CLI project/session-memory-feature endorsement from the shipped project surfaces, SYMMETRICALLY across all three trinity CLIs (Claude, Codex, Antigravity/Gemini); (2) RENAME the `## Project memory` trinity section to `## Project rules` (keeping its rules) + update every reference incl. the shipped `validate-docs.sh` gate, in lock-step; (3) CODIFY an express, CLI-symmetric project-side prohibition (no per-CLI project/session memory; the repo is the authoritative SSOT), reserving the term "project memory" for the (forbidden) CLI feature alone; (4) KEEP the aligned repo-is-authoritative-memory framing. Zero loss of the legitimate rules currently under the section.
  Process: large-BD standard — `pack-docs-researcher` (definitive per-CLI feature-endorsement census across all three CLIs incl. re-verifying Codex's current memory mechanism, + the section-reference census; both surfaces: `project-template/` ∪ project-side `supporting-docs/`) → `pack-architect` (+ adversarial) → `pack-planner` (+ adversarial) → `pack-coder` waves → `pack-reviewer` (grep-zero on the old section name outside feature-references; per-CLI symmetry verified; no-behavior-change on the kept rules). Informed by BD-232's pack-side result. User approves the prohibition wording. Design-gate rulings (2026-07-02, on `DESIGN-BD245-FINAL.md`): Q1 wording — the user revised the re-read cadence: rules are RE-READ from the repo at the START OF EVERY COMMIT (per-session reads go stale; never a cached copy) — final text measured under the 700-cap matcher at implementation; Q2 (frozen `v11-trinity-marker-prepped` fixtures LEAVE, heading owned by BD-136) stands as the evidence-closed decision; Q3 CONFIRMED — `supporting-docs/CLI-PM-SETUP.md` is in the strip set (serve-only docs still teach the forbidden pattern); Q4 CONFIRMED — prohibition placement = trinity `## Project rules` ×3 + a PM-CHAT mirror, NOT METHODOLOGY; Q5 CONFIRMED — the migrated-project heading transform routes to BD-136 (BD-245 does the literal grep/comment swap only). Sequencing RULED: BD-255 Parts A/B/C → BD-259 → BD-245 → BD-255 Part D (rename a clean section, not a flagged one); the test-82 live-coupling decoupling lands first as its own batch fix.
Acceptance criteria: no shipped project surface endorses or assumes ANY of the three CLIs' per-project/session memory feature (grep-zero on feature-endorsement phrasing, per-CLI symmetric — no CLI exempted, Codex included); the `## Project memory` trinity section is renamed to `## Project rules` across all 3 files with every reference (incl. the shipped `validate-docs.sh` AXIS) updated in lock-step (grep-zero on the old `## Project memory` string except where it names the forbidden feature); the legitimate rules formerly under the section are preserved under `## Project rules`; an express CLI-symmetric no-per-project-memory prohibition is codified project-side (mirroring BD-232's meta-rules, project vocabulary, generalized across Claude/Codex/Antigravity-Gemini); the repo-is-authoritative-memory framing is kept; the term "project memory" appears ONLY in reference to the (forbidden) CLI feature; validate-pack + the shipped `validate-docs.sh --self-test` green; researcher-then-architect-designed (not PM-chat/Pack-Chat-authored).
References: BD-232 (the pack-side sibling — memory-cache reduction + the three meta-rules; do FIRST, informs this; pack-side is Claude-centric since pack dev runs in Claude, whereas this project-side sibling is CLI-symmetric because the deliverable ships to all three CLIs); BD-253 (project-side session-state mechanism — GOVERNS this BD's project-side state persistence: state → docs or the project session-state file (per BD-253), never memory; the project analog of BD-232→BD-252); BD-239 (surfaced this during its project-side location census); BD-243 (the shipped `validate-docs.sh` client gate whose `## Project memory` AXIS this BD updates); the pack-side `## Pack memory` section (the analogous pack rules-section — KEPT under its name; only the project side renames, per user 2026-06-23).
Resolved: 2026-07-03 — Delivered via the full pipeline (census → architect → adversarial → reconciliation → user rulings → a parallel design-revalidation audit [verdict: no design-level drift] → planner → adversarial → reconciliation → three reviewed-clean commits + one approved follow-on). (C1) STRIPPED all five per-CLI memory-feature endorsement passages from the shipped project surfaces — deletions only, the stale Codex 'no equivalent' claim erased, the repo-is-authoritative KEEP framing intact. (C2) RENAMED the trinity section `## Project memory` → `## Project rules` across EVERY encoding surface in ONE lock-step commit (both SSOT constants; three headings with bodies byte-unchanged; every literal incl. the hyphenated and line-wrapped traps; both Check-66 test pins; the migrator regex/msg pair) — the BD-259 standing guard's heading-sync leg proved the lock-step mechanically (7/7), and reviewer counterfactuals proved it load-bearing both directions. (C3) CODIFIED the user-signed-off CLI-symmetric prohibition byte-exact: the trinity bullet ×3 (688 chars folded; Claude `~/.claude/projects/<slug>/memory/`, Codex `~/.codex/memories/`, Gemini/Antigravity `~/.gemini/GEMINI.md` all named; rules re-read from the repo at the start of every commit, never a cached copy) + the distinctly-worded PM-CHAT operational mirror (734; cap-exempt there, proven). (OBS-1 follow-on, user-approved) the pack-root GEMINI cross-session-memory endorsement replaced with a rule-conformant prohibition (the census proved it the sole pack-root endorsement). Acceptance held: the term 'project memory' survives ONLY inside the prohibition blocks + the two accurate pack-root KEEP statements; the corrected multiline sweep EMPTY; the pack `## Pack memory` negative control intact. Verified per commit: the client gate full scan + self-test, the 7-leg guard, validate-pack plain+DEEP, the full sharded battery, shard coverage. Review triage: zero findings on C1/C3/OBS-1; one accepted NIT on C2 (accurate test-header provenance, out of the plan row set). Docs: CENSUS-BD245.md + DESIGN-BD245-FINAL.md + REVALIDATE-BD245.md + PLAN-BD245-FINAL.md (out-of-repo handoff). Forward notes: BD-136 inherits the frozen-fixture heading + the migrated-project transform (user rulings Q2/Q5); BD-255 Part D is now UNGATED (all four quartet prerequisites Resolved) and adopts the renamed section + the `pm-session-state.json` name in its audit.
Position: v11.0 — directly after BD-232 (user 2026-06-23); project-side sibling of the pack-side BD-232 memory work; cross-surface client-deliverable cleanup + a CLI-symmetric governance prohibition; large-BD pipeline.
