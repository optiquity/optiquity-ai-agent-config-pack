<!-- per-entry source: /backlog/BD-233.md; contract: /backlog/_rules.md -->
**BD-233 — Codex + Antigravity Graphify graph-first parity: verify + enable cross-CLI pack-dev agent graph-querying (the cross-CLI half of BD-225)**
Type: feat — cross-CLI pack-development agent tooling parity (PACK-OPS; not client-shipped). Verify whether Codex `.toml` pack agents and the Antigravity plugin pack agents actually consume the pack-root `AGENTS.md` / `GEMINI.md` graph-first rule the way Claude subagents consume `CLAUDE.md` — and, where they don't, design + implement the per-CLI enablement so a Codex/Antigravity pack-development session gets the same graph-first benefit. Opened 2026-06-18 (user direction, from BD-225 OQ-9).
Status: Deferred
Target: post-v11.0 — large cross-CLI verification + enablement effort; NOT needed for the v11.0 release. Deferred with no scheduled position (user-authorized 2026-06-18). Sequencing decided when picked up.
Blockers: BD-225 (the Claude-side Graphify pack integration + the pack-root trinity graph-first rule must land first; this BD extends that integration to the Codex/Antigravity agent surfaces).
Independence (explicit): NOT bound to BD-217. BD-217 is the cross-CLI half of worktree-isolation (BD-197); this BD is the cross-CLI half of Graphify (BD-225). They are UNRELATED concerns and must not be coupled or co-scheduled — each cross-CLI track stands alone.
File/Symbol (subject to whatever BD-225 lands first):
  - `AGENTS.md` (pack-root, Codex) + `GEMINI.md` (pack-root, Antigravity) — the graph-first rule BD-225 places here for trinity parity; verify it is actually consumed by Codex/Antigravity pack agents at spawn.
  - `.codex/agents/pack-*.toml` (5 Codex pack agents) — verify they inherit/consume the pack-root `AGENTS.md` rule and can run the deterministic `graphify` query CLI; enable if not.
  - `.agents-plugin/pack-agents/agents/pack-*.md` (5 Antigravity pack agents) + the plugin roster — verify they consume the pack-root `GEMINI.md` rule and can query the graph; enable if not.
Description:
  **Problem:** BD-225 wires Graphify graph-first usage into the pack via the pack-root trinity. It is verified that pack-root `CLAUDE.md` auto-loads into Claude subagents — so every Claude pack agent gets the graph-first rule and can query the graph. It is NOT verified whether Codex `.toml` pack agents or the Antigravity plugin pack agents consume `AGENTS.md` / `GEMINI.md` the same way, so a pack-development session run via Codex or Antigravity may not actually benefit from the graph. BD-225 ships the rule in all three trinity files for parity (harmless inert text where unconsumed), but the cross-CLI EFFECTIVENESS is unverified.
  **Goal:** (1) verify, in the ACTUAL Codex and Antigravity environments, whether their pack agents consume the pack-root graph-first rule and can run the deterministic `graphify` query CLI; (2) where they don't, design + implement the per-CLI enablement so a Codex/Antigravity pack-development session gets the same graph-first benefit Claude has.
  **Boundary (absolute):** PACK-OPS ONLY — this concerns pack-DEVELOPMENT agents (the `.codex/` / `.agents-plugin/` pack-* agents), not client deliverables; nothing ships to clients; no setup artifact in `project-template/` (P-missed-7). Mirrors BD-225's boundary.
  **Why deferred:** pack development runs through Claude (Pack Chat is a Claude session; pack agents are spawned via Claude), so the realized BD-225 benefit does not depend on Codex/Antigravity. Verifying + enabling the other two CLIs is a large effort requiring those environments and is not needed for the v11.0 release. User-authorized deferral 2026-06-18 (size + logical-fit: a distinct cross-CLI track, parallel to but independent of BD-217).
Acceptance criteria: in the actual Codex and Antigravity environments, each pack-* agent either (a) is verified to consume the pack-root graph-first rule + query the graph, or (b) is enabled to do so; the graph-first benefit is confirmed working cross-CLI; boundary held (pack-ops only; nothing client-side; rule never in `project-template/`); no coupling to BD-217.
References: BD-225 (the Claude-side Graphify pack integration this extends — OQ-9 is the origin); BD-221 (Gemini→Antigravity conversion — the Antigravity agent surfaces); BD-217 (the SEPARATE cross-CLI worktree-isolation track — cited only to state this BD is INDEPENDENT of it). The graph-first rule's home is the pack-root trinity, never `project-template/`.
Resolved: n/a
Position: Deferred — post-v11.0; unscheduled (user-authorized 2026-06-18). Prerequisite: BD-225 lands first. Independent of BD-217; do not co-schedule.
