<!-- per-entry source: /backlog/BD-254.md; contract: /backlog/_rules.md -->
**BD-254 — Implement the Graphify cost/benefit improvement recommendations (vocab-expansion query flow, save-result feedback loop, freshness tightening, cost ledger, stale-report regen)**
Type: chore — pack-ops tooling improvement (PACK-OPS; not client-shipped). The IMPLEMENTATION leg of the BD-234 Graphify assessment: BD-234 delivered the measured cost/benefit assessment; this BD enacts its ranked improvement recommendations. Surfaced 2026-06-28 from the Graphify cost/benefit report; user-approved open.
Status: Open
Target: v11.1 (user direction 2026-06-28 — the current Graphify settings are sufficient for the v11.0 launch; these are realized-benefit improvements, NOT launch blockers, so they land post-launch).
Blockers: none hard. Builds on BD-225 (Graphify integration) + BD-234 (the assessment that produced these recommendations). Sequenced into v11.1.
Unblocks: closes the realized-vs-potential benefit gap the assessment measured — better natural-language query recall, less mid-task staleness, a tracked spend ledger, and the currently-unused value-multipliers turned on.
Source: `maintenance-docs/graphify-assessments/GRAPHIFY-COST-BENEFIT-2026-06-28.md` (the measured assessment + its ranked recommendations + a reproducible Methodology section). Re-run that report's methodology AFTER these changes land to measure the actual improvement (before/after comparison).
File/Symbol (architect to detail; candidate surfaces — ranked by the report's recommendation order):
  - **Vocab-expansion query flow (biggest recall win, ~0 cost):** the pack-root trinity `## Pack memory` `graph-first-context` rule (`CLAUDE.md` / `AGENTS.md` / `GEMINI.md`) — teach agents the constrained vocab-expansion step Graphify's `query` requires (the binary does case-folded substring + IDF only; raw natural-language queries degrade recall — demonstrated in the report).
  - **`save-result` feedback loop:** wire the `graphify save-result … → graphify-out/memory/` Q&A loop into the agent/orchestrator query path so good answers feed the next refresh (the loop is unused today; `memory/` is empty).
  - **Freshness tightening:** the refresh mechanism (`scripts/hooks/graphify-pre-push.sh` + `scripts/install-graphify-hook.sh`) is push-cadence → the graph goes stale mid-task → agents fall back to grep/Read. Evaluate a free AST-only `graphify update .` on code-only changes (0 LLM tokens) and/or `check-update` / `watch` to cut the fallback.
  - **Cost ledger:** establish a cumulative spend record — the `claude-cli` build path emits no `cost.json`; snapshot the build-log token line + `graphify-out/.graphify_analysis.json` per refresh so spend is tracked over time.
  - **Stale `GRAPH_REPORT.md`:** regenerate it on refresh (it currently lags `graph.json`).
  **Out of scope:** re-tuning the index scope / cadence / build knobs settings (BD-234 confirmed the current settings are sufficient — left AS-IS, not this BD's job); the cross-repo `global` graph (a separate evaluation if ever wanted); anything client-side (PACK-OPS only — the Graphify rule lives in the pack-root trinity, never `project-template/`).
References: BD-234 (the assessment leg — Resolved; this BD is its implementation leg) and the BD-234 cost/benefit report `maintenance-docs/graphify-assessments/GRAPHIFY-COST-BENEFIT-2026-06-28.md` — the implementer MUST consult the report's measured results (benefit/cost/cadence figures, the qualitative accuracy findings, the §process review) and its ranked recommendations during design, and re-run its Methodology section for a before/after comparison. The report is the authoritative input + the success yardstick for this BD; also BD-225 (the Graphify integration whose process this tunes) and BD-237/BD-240 (prior Graphify fixes/reframes).
Process: size-tiered large-BD standard (changes pack-ops rules + the refresh mechanism = structural) → researcher-if-needed → architect → adversarial-if-large → planner → coder + bounded review/fix cycle. Runs in v11.1.
Resolved: n/a
Position: v11.1 — the implementation leg of BD-234 (whose assessment leg is complete + Resolved). Sits in the v11.1 scope alongside the other deferred v11.1 work; not a v11.0 launch blocker.
