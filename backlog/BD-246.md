<!-- per-entry source: /backlog/BD-246.md; contract: /backlog/_rules.md -->
**BD-246 — Non-mutation integrity check for pack-shipped immutable files (per-version content checksums + CI-red verify)**
Type: feat — surfaced 2026-06-25 from the BD-206 design-gate G-10 decision; user-approved open
Status: Resolved
Target: v11.0 (LAUNCH-BLOCKING for the core)
Blockers: BD-206 — this BD hashes BD-206's FINAL immutable set (the rewritten `_rules.md`, no `_format.md`, the new schema block, `_intro.md` reclassified modifiable, and BD-206's O14 fix to the stale `PACK-AGENTS.md:213` immutability statement). Building the manifest before BD-206 finalizes those files would hash files about to change — so this lands AFTER BD-206.
Unblocks: makes BD-206 G-10's (A) force-overwrite SAFE — recovers the loud tamper-detection signal that G-10's silent reassert otherwise loses (loud detection between version bumps + automatic correction on bump).
File/Symbol:
  - **Immutable-set declaration** — an explicit `immutable` classifier (a new column on the `_CLIENT_INSTALLED_FILES` install-map rows in `scripts/init-project.sh`, OR a frozen `_IMMUTABLE_SHIPPED` constant parallel to `_SANCTIONED_PACK_SIDE_SHIPPED`). Launch-scope = the 3 `_rules.md` files (the clear §1 immutables). EXCLUDE derived files (`_toc.md`/`_index.md` — regenerator output) and `cmd_update`-merged files (the trinity — designed to drift); `_intro.md` is modifiable (excluded). This explicit declaration is the one real prerequisite (mutability-class is NOT machine-encoded today).
  - **Content-hash manifest** — per-file `sha256`, version-keyed header `# pack-version: vN.M` (version from `scripts/lib/detect.sh:128 detect_pack_version` + `scripts/lib/template-version.sh`). Mirrors the proven fixture-manifest PATTERN (`test-fixtures/manifest.txt` + `scripts/build.sh --verify` + validate-pack Check 62) but is a DISTINCT instance (content sha256, NOT git-tree SHAs); regenerated at release (a `--regen` mode); shipped alongside the immutable files + tracked by `_CLIENT_INSTALLED_FILES` (Check 41). G-10 force-overwrite installs new files + new manifest together → clean post-bump by construction.
  - **CI-red verify (the enforcement spine)** — a new `validate-pack.py` check (Check 73; next free after BD-206 takes 72) verifies the pack's own `project-template/.../_rules.md` against the manifest; a client-side verify leg (a `validate-docs.sh`-adjacent script) verifies the INSTALLED immutable files; both cheap (sha256 of ~3 small files; honor `ci-check-runtime-compounding`), SKIP-lenient when the manifest is absent.
  - **Optional pre-commit commit-ban** — an OPT-IN sample hook (advisory, force-overridable; CI is the real gate), NOT auto-installed (local-only, not server-enforceable on the Free plan). [User 2026-06-25: same BD; not auto-installed; user reserves the right to revisit auto-install during this BD's design.]
Description: Pack-shipped immutable files (per §1, starting with the 3 `_rules.md`) must not be silently mutated. BD-206 G-10 chose (A) force-overwrite for the immutable `_rules.md` on existing-project re-run (robust to skipped-version upgrades), which is SILENT on client tampering. This BD recovers the loud signal — a per-version content-checksum manifest + CI-red-on-mutation verify (pack side + client side) + an optional opt-in commit-ban hook — giving "loud detection + automatic correction." The architect feasibility assessment (2026-06-25) found it ACCEPTABLE and launch-worthy for the core, with the three refinements captured in File/Symbol. It is the integrity guarantee that makes G-10's silent force-overwrite safe.
  **Out of scope:** classifying the mutability of `cmd_update`-merged files (trinity, etc.) beyond declaring the launch set; tracker work.
Resolved: 2026-06-28 — non-mutation integrity check shipped: frozen `_IMMUTABLE_SHIPPED` (the 3 `_rules.md`) + per-version sha256 `immutable-manifest.txt` + `immutable-manifest.sh` regen tool; pack-side Check 76 (content-verify + version-header gate + wired-assertion) + client `verify-immutable.sh` wired into `validate.sh`; install-map row; fresh-install manifest-ship fix + generalized Check 41 copy-site guard (every hand-enumerated per-file row across all stages); `PACK-AGENTS.md` `_intro.md` immutability prose corrected; optional opt-in `install-immutable-hook.sh` (advisory, force-overridable, NOT auto-installed). Commits `b6c5ecc`..`673e685`. Full large-BD pipeline incl. adversarial architect + adversarial planner + reconciliations; the mid-implementation scratch-install test caught + fixed a launch-blocking fresh-install manifest-ship defect before it shipped.
