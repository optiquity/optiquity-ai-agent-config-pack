<!-- per-entry source: /backlog/BD-229.md; contract: /backlog/_rules.md -->
**BD-229 — README Repository-Layout pack-agent count is stale: `(4)` → `(5)` on all three agent-surface rows**
Type: fix — docs accuracy. The `README.md` Repository Layout section states each pack-agent surface holds 4 agents; the tree actually holds 5. Correct the count on all three rows. Opened 2026-06-18 (user direction).
Status: Open
Target: v11.0 — small, self-contained doc-accuracy fix; must be correct before launch. Surfaced as out-of-scope finding POQ-C9-1 during the BD-221 conversion cluster (C9 review); converted to a tracked BD per user direction 2026-06-18 rather than fixed inline.
Blockers: none.
File/Symbol:
  - `README.md:178` — `.claude/agents/` row: `Pack-specific Claude agents (4)` → `(5)`.
  - `README.md:179` — `.codex/agents/` row: `Pack-specific Codex agents (4)` → `(5)`.
  - `README.md:180` — `.agents-plugin/pack-agents/agents/` row: `Pack-specific Antigravity agent bundle (4)` → `(5)`.
Description:
  **Problem:** The README Repository Layout claims `(4)` pack agents on each of the three agent surfaces. Measurement (2026-06-18, HEAD `ef74053`) shows each surface ships exactly 5: `pack-architect`, `pack-coder`, `pack-docs-researcher`, `pack-planner`, `pack-reviewer` (`.claude/agents/*.md`, `.codex/agents/*.toml`, `.agents-plugin/pack-agents/agents/*.md` — 5 files each). The `(4)` predates `pack-docs-researcher` and was never updated. README is the authoritative layout reference (per CLAUDE.md "Repo structure"), so a stale count there is a correctness defect, not a cosmetic one.
  **Solution:** change `(4)` → `(5)` on the three rows above. No other surface asserts this count (the v1 history row at `README.md:83` "5 agents" is unrelated historical text and is already correct). Verify post-fix that no validator or test encodes a `4`-agent expectation for these surfaces.
Acceptance criteria: all three Repository-Layout rows read `(5)`; the count matches `ls` of each agent surface (5 each); no other pack-agent-count assertion is left stale; validate-pack green.
References: surfaced as POQ-C9-1 (out-of-scope finding, BD-221 cluster C9 review); `enumerate-encoding-surfaces` (verify no validator/test encodes the count); the `out-of-scope-fix-immediately` rule (the origin-mode that this BD-conversion deviates from, by explicit user direction 2026-06-18).
Resolved: n/a
Position: v11.0 — small doc-accuracy fix; independent of the other two converted findings (BD-230, BD-231).
