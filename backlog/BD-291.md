<!-- per-entry source: /backlog/BD-291.md; contract: /backlog/_rules.md -->
**BD-291 — Per-entry conversion field-fidelity: confirm, root-cause, and fix the missing-field defect; prove the OT migration end-to-end on v11.0 (launch-gating)**
Type: bug — migration / per-entry decompose subsystem.
Status: Open
Target: v11.0
Blockers: BD-290 (user precondition: isolation `full` mode working on both sides before this work starts).
Unblocks: the public launch — GA/public flip is gated on the target app working correctly on v11.0 (user decision); also closes the field-fidelity gap in the monolith→per-entry conversion for every future client migration.
File/Symbol: `scripts/lib/per-entry/decompose.sh`; `scripts/lib/per-entry/_lib.sh`; `scripts/migrate-v10-to-v11.sh` (decompose dispatch); `test-fixtures/` v10/v11 personas; the per-check tests; `/backlog/_rules.md` + project-side `_rules.md` (field-faithful contract); the pack's own `/backlog/` + `/changelog/` trees (audit vs git-history monoliths); `supporting-docs/MIGRATION-v10-to-v11.md` (instructions deliverable) — docs-researcher census completes the set.
Description:
  The target app's PM chat and a second pack chat observed missing fields in some per-entry flat files; the field-faithful contract requires every top-level field preserved verbatim, extension fields included. The target-app migration has NOT yet run. Read-only recon measured concrete risk in the target app's monoliths: 113 backlog entries, with rare extension fields (`Pending developer decisions:`, `Design reference:`, `Audit scope expansion:`) and prose-colon field-grammar hazards in the implementation-plan monolith.
  Scope: (1) architect confirms and root-causes the defect (researcher-first LARGE deterministic pipeline); (2) fix the conversion code; (3) audit and repair the pack's OWN converted trees against their pre-conversion monoliths in git history (same decompose code produced them); (4) prove the target-app migration end-to-end on a read-only scratch clone; (5) ship precise post-migration instructions for any manual/synthetic field fill where mechanical preservation is impossible — executed by the target app's PM chat + user, never by pack agents. The real migration is run by the target app's PM chat + user (the migrator's designed consumption model); the target app checkout is READ-ONLY for all pack work (scratch clones only). BD-171 stays SEPARATE (its harness and disposition are untouched by this entry; it may be Deprecated independently).
Acceptance criteria: root cause confirmed with per-field evidence; decompose proven 100% field-faithful by a field-census — every monolith field accounted for in the per-entry output, zero unaccounted drops — across all streams, on fixtures and the target-app scratch clone; the pack's own converted trees verified and repaired where fields were dropped; the migrator runs end-to-end green on the target-app scratch clone; manual-fill instructions shipped with the migration docs where mechanical preservation is impossible; full CI battery green (both jobs + `PACK_VALIDATE_DEEP=1`).
Position: launch-gating — GA/public flip is gated on the target app working correctly on v11.0 (user decision); work STARTS only after BD-290 is Resolved.
