<!-- per-entry source: /backlog/BD-216.md; contract: /backlog/_rules.md -->
**BD-216 — Phase-parts tracker representation + tracker-mode execution ordering (tracker legs of BD-185)**
Type: feat — STRUCTURAL. The TRACKER counterpart of BD-185: the GH-Issues / TrackerProvider representation of the phase-parts hierarchy and tracker-mode execution ordering. Split out of BD-185 on 2026-06-13 (BD-214 Track-2, US-4) so the flat-file half (BD-185) can ship in v11.0 while the tracker half defers with the rest of the deferred tracker cluster.
Status: Deferred
Target: none — no release version; lands with the tracker-resumption release (the deferred cluster {BD-204, BD-207, BD-215, BD-216, BD-188, BD-212, BD-213}). (user 2026-06-12; cluster semantic US-5 2026-06-13.)
Blockers: BD-185 (semantic source — the flat-file phase-part structure this represents in the tracker MUST be locked + deterministically serializable first); BD-215 (the canonical entry format that phase/part/task entries serialize through — format before tracker); BD-204 + BD-207 (the pack-side and project-side tracker machinery this rides on, both deferred). No work begins until the tracker cluster resumes.
Unblocks: tracker-mode users can express phase splits, mid-work phase-to-parts expansion, and explicit execution ordering in the tracker — round-tripping the BD-185 flat-file structure losslessly, with no flat-file artifact serving as the ordering SSOT in tracker mode.
File/Symbol (tracker legs — deferred):
  - `project-template/.github/ISSUE_TEMPLATE/work-item.yml` — Part field + `part:M` label namespace (NEW) per the BD-068 form-family rules + the BD-069 `template_version` delta.
  - `scripts/lib/tracker-provider-*.sh` (BD-060 TrackerProvider) — bi-directional sync of part membership + execution order across forward (flat→tracker) and reverse (tracker→flat) operations.
  - any v11.x flat→tracker migrator — initialize the tracker-native ordering mechanism from current implementation order (the tracker leg of the migration; the flat-file whole-number pass-through is BD-185's).
  - `scripts/validate-pack.py` — tracker-side check(s) enforcing part-membership + ordering invariants in tracker mode (architect determines specifics; complements BD-185's flat-file invariant check).
Description: BD-204 demonstrated that tracker-mode execution ordering and phase-part containment have no native GH-Issues mechanism (issue numbers reflect creation order; Blockers give only partial order; sub-issues give containment, not sibling order; a regenerated IMPLEMENTATION-PLAN mirror does not preserve flat-file execution notes). BD-185 solves the FLAT-FILE half (the canonical, deterministically-serializable phase→part→task structure + flat-file execution-notes ordering). BD-216 is the TRACKER half: it represents that same structure in the tracker (form-family Part field + label namespace), syncs part membership + execution order bi-directionally via the TrackerProvider, and gives tracker mode a native execution-ordering mechanism that does not depend on a flat-file artifact or abuse GH Projects as a single-phase ordering substitute.
  **Symbiosis with BD-185 (HARD — interdependent, must be elegant not forced):** BD-216's tracker representation MUST round-trip BD-185's flat-file structure losslessly. BD-185 owns the structure + its deterministic serialization; BD-216 owns the tracker projection of that structure. Neither redefines the other's half; the tracker form is a faithful projection of the flat-file canonical form (readable form == tracker form, the BD-215 property).
Success Criteria (tracker legs — moved here from BD-185 on 2026-06-13):
  - SC6. The tracker form-family (`work-item.yml` Part field + `part:M` label namespace + `template_version` per BD-069) supports parts and ordering with the smallest template_version delta consistent with the BD-068 form-family rules.
  - SC7. Bi-directional sync (BD-060 TrackerProvider, mirror semantics) preserves part membership and execution order across forward (flat→tracker) and reverse (tracker→flat) operations.
  - SC4-tracker. Execution ordering of phases is expressible in tracker mode WITHOUT depending on any flat-file artifact and WITHOUT using GH Projects as a single-phase ordering substitute.
  - SC8-tracker. Any flat→tracker forward-migration initializes the tracker-native ordering mechanism from the current implementation order, without manual intervention.
  - SC-RT. The tracker representation round-trips the BD-185 flat-file phase-part structure losslessly (per-entry → tracker → per-entry == original).
Out of scope: the flat-file phase-parts structure, lifecycle, and execution-notes ordering (BD-185, v11.0); the canonical entry-format redesign (BD-215); any tracker migration EXECUTION (BD-204/BD-207 — this BD only supplies the phase-parts representation those migrations carry); GH Projects integration (v11.1, reserved); tracker backends other than github (reserved); letter-suffix phase forms (rejected).
Acceptance criteria: tracker mode expresses phase splits, mid-work phase-to-parts expansion, and explicit execution ordering; part membership + execution order round-trip losslessly through the TrackerProvider (forward + reverse); the BD-185 flat-file structure round-trips through the tracker unchanged; no flat-file artifact is the tracker-mode ordering SSOT; validate-pack green incl. the tracker-side invariant check.
References: BD-185 (semantic source — the flat-file phase-part structure + deterministic serialization this projects into the tracker; BD-185 → blocks → BD-215 → blocks resumption); BD-215 (canonical entry format — phase/part/task entries serialize through it); BD-204 + BD-207 (the deferred tracker machinery this rides on); BD-060 (TrackerProvider abstraction); BD-068 (form-family rules) + BD-069 (template_version); BD-189 (groupings — the v11.1 grouping projection is adjacent but separate).
Resolved: n/a
Position: deferred — no release version; lands with the tracker-resumption release. Blocked on BD-185 (structure locked + deterministically serializable) + BD-215 (canonical format) + BD-204/BD-207 (tracker machinery). The tracker half of BD-185's split (user 2026-06-13, US-4).
