<!-- per-entry source: /backlog/BD-288.md; contract: /backlog/_rules.md -->
**BD-288 — CI guards that do not bite: Check 40's bareness coverage gap, Check 68's name-only path resolution, Check 81's matcher decoupled from its data, and raw-rglob candidate sets that walk agent worktrees (pack gates + client twin)**
Type: fix — CI guard coverage (prevention; the defect class already shipped a real 12-reference instance that survived to the v11.0 RC1 cut and was caught only by hand).
Status: Open
Target: v11.0 (surfaced during the BD-093 release cut, deliberately left unfixed there on the fix-coder's `ci-guard-measure-then-bound` recommendation; the no-new-v11.0-BD constraint that applied during the cut has been lifted by the user).
Blockers: None. COORDINATE: co-edits `scripts/lib/validate_checks/boundary_refs.py` with no other open BD (measured — no active-BD `File/Symbol` collision at open time); the client-twin leg co-edits `project-template/scripts/validate-docs.sh`.
Unblocks: a bare or stale cross-reference authored into any live pack doc is caught by CI at push instead of by a human reviewer at a release cut.
File/Symbol (architect confirms): `scripts/lib/validate_checks/boundary_refs.py` — Check 40 (`check_bare_pack_ops_refs`, walks the non-recursive `pack-ops/*.md` glob), Check 43 (`check_project_side_bare_internal_refs`, walks `_iter_client_installed_files()`), Check 68 (`check_dangling_file_refs`, the qualified-path + basename-index resolution and `_CHECK_68_INCLUDE_TREES` / `_CHECK_68_EXCLUDE_PREFIXES`), plus the shared `_build_basename_index` / `_CHECK_40_ALLOWLIST` / `_CHECK_40_ANCHOR_PHRASES` / `_iter_operating_docs` seams; `pack-ops/.dangling-ref-allowlist.txt` (the Check-68 KEEP set); `scripts/validate-pack.py` (the check registry + the header check-count prose); `project-template/scripts/validate-docs.sh` (the client twin's `dangling` axis — the `base in basenames` fallback is the same defect, mirror-but-customize); `scripts/init-project.sh` (the `_CLIENT_INSTALLED_FILES` inventory that defines Check 43's walk); `.github/workflows/validate-pack.yml` (Check-42 test wiring); `scripts/tests/` (the per-check test file); `supporting-docs/` and `README.md` (the uncovered live surfaces); `CLAUDE.md`, `AGENTS.md`, `GEMINI.md` (the pack-root trinity, also uncovered); `scripts/lib/validate_checks/cross_bd.py` — Check 81 (`_check_81_active_bd_ids`, the inert dict-only `active[]` matcher) and Check 82 (the collision map that consumes the same trigger); `scripts/dashboard-render.py` (the competing string-form consumer of `active[]`); `pack-ops/DASHBOARD-SPEC-PACK.md` (the user-owned spec that documents the string form); `pack-ops/session-state.json` (the surface both consumers read); `scripts/lib/validate_checks/discipline_parity.py` — Check 53 (`check_worktree_isolation_prohibition_flip_block`, whose raw `REPO_ROOT.rglob("*")` candidate set is defect (4)) and its wired test `scripts/tests/test-validate-pack-check-53.sh` (whose synthetic fixtures must be a throwaway git repo for the negative legs to keep their teeth against a tracked candidate set); session-hygiene item 5 (the RO write-capability grant) touched `.claude/agents/pack-architect.md`, `.claude/agents/pack-planner.md`, `.claude/agents/pack-docs-researcher.md`, `.codex/agents/pack-architect.toml`, `.codex/agents/pack-planner.toml`, `.codex/agents/pack-docs-researcher.toml`, `.codex/agents/pack-reviewer.toml` (the fourth Codex RO definition, brought in so all four carry one accurate byte-identical sandbox comment), and `pack-ops/PACK-AGENTS.md` (the RO-class parenthetical in § "Two agent classes"), with the NIT-1 sibling-comment corrections landing in `scripts/lib/validate_checks/discipline_parity.py` (Check 52 block comment + docstring), `scripts/validate-pack.py` (the Check 52 registry comment), and `scripts/tests/test-validate-pack-check-52.sh` (the header comment).
Description:
  **Problem — two distinct defects on one family of checks.**

  **(1) The BARENESS axis has a walk-scope hole.** Two checks gate cross-references for *bareness* (a filename reference carrying no directory qualifier): Check 40 walks `pack-ops/*.md` — a NON-RECURSIVE glob, 11 files — and Check 43 walks `_iter_client_installed_files()` = `project-template/**` plus only those `supporting-docs/` entries listed in `scripts/init-project.sh`'s `_CLIENT_INSTALLED_FILES` block (`_SANCTIONED_PACK_SIDE_SHIPPED` is frozen empty). Measured at HEAD: exactly 2 of the 10 `supporting-docs/` files are client-installed (`METHODOLOGY.md`, `INSTALL-PROCEDURES.md`), so the other 8 are walked by NEITHER bareness check. The hole is wider than `supporting-docs/`: the repo-root `README.md`, the pack-root trinity (`CLAUDE.md` / `AGENTS.md` / `GEMINI.md`), and the ~150-file per-CLI operating-doc set (`.claude/` + `.codex/` + `.agents/` agent and skill definitions) are ALSO walked by neither.

  **(2) The EXISTENCE axis is broad but blind to stale-but-resolvable paths.** Check 68 (`_CHECK_68_INCLUDE_TREES = ("project-template", "supporting-docs")` + `README.md` + the 158-doc operating-doc IN set, which DOES include the pack-root trinity) covers nearly the whole bareness hole — but only for *existence*. Its resolution is: a qualified path resolves if the path exists, ELSE if `Path(token).name in index` — a BASENAME-INDEX FALLBACK. So a qualified path that is wrong but whose basename exists somewhere in the tree resolves clean, and a bare basename resolves clean whenever the basename exists anywhere. Check 68 therefore cannot see a reference that points at a file which MOVED. The identical fallback is present in the client twin `project-template/scripts/validate-docs.sh` (`if norm in relpaths or base in basenames: continue`), so the client gate carries the same blindness.

  **(3) Check 81's active-BD matcher is decoupled from the data it gates — same class, different surface (folded in at BD-open by user direction).** Check 81 (`check_open_bd_structured_surface_field`) gates that every BD in active design carries a structured `File/Symbol` path list, and its trigger is membership in `pack-ops/session-state.json` `active[]`. But `_check_81_active_bd_ids()` accepts a member ONLY when it is a DICT carrying a `bd` key matching `^BD-\d+$`; a plain-string member is silently skipped. The FAIL leg does not fire against the form `active[]` actually carries today (`["BD-093"]`, `["BD-287"]`, `["BD-205"]`, ...) — the check registers, prints OK, and gates nothing.
  **CORRECTION to this entry's BD-open text (measured 2026-08-23; full survey of all 158 commits touching `pack-ops/session-state.json`, classifying each committed `active[]` value).** As opened, this entry claimed "Measured across the snapshot's entire committed history, EVERY `active[]` value has been a list of plain strings" and that the FAIL leg "has never fired once since the check was authored." Both are FALSE — the original survey was truncated. The measured distribution is 32 commits with DICT members (2026-06-29 through 2026-07-05), 91 with STRING members (continuous from 2026-07-16 to HEAD), and 35 with an empty list. The matcher therefore WORKED against the dict form the surface produced at the time it was authored, and went inert when the data drifted to the string form. The correct characterization is DATA-DRIFT DECOUPLING — a matcher still asserting against a shape its surface no longer emits — not a matcher that never worked. The same wrong claim reached the commit message for `cfd5b02`, which cannot be rewritten; this entry body is the correction of record. The defect and its remedy are unchanged.
  **The data shape is correct; the matcher is the defect.** Two consumers of the same field disagree, and the string form is the load-bearing one: `scripts/dashboard-render.py` does `" ".join(session.get("active", []) or [])`, which raises on dict members, and the user-owned verbatim spec `pack-ops/DASHBOARD-SPEC-PACK.md` states explicitly that `active[]` may hold long descriptive strings. So the fix direction is NOT to migrate the data to dicts on the check's say-so; the architect decides, with the renderer and the spec as binding constraints. Check 82 consumes an adjacent trigger and must be re-verified for the same inertness.
  **Why it belongs here and not in a new entry.** This is the SAME defect class as (2): a records-style check that stays green because its matcher never reaches the reality it claims to verify (`declare-verify-backing`). BD-288's remit is that class, not one check family.

  **(4) A guard's candidate set is a raw filesystem walk, not the repo — same class, fourth instance (surfaced during BD-288 implementation).** Check 53 (`check_worktree_isolation_prohibition_flip_block`, the BD-197 Guard-A prohibition flip-block) enumerated its candidate files with `REPO_ROOT.rglob("*")` — a raw filesystem walk. A raw walk reports whatever is ON DISK rather than what the repo CONTAINS, so it descended into live sub-agent worktrees at `.claude/worktrees/agent-*/` and re-reported this repo's own files, seen through a second checkout, as fresh violations; it also read gitignored build output (`graphify-out/`) the repo does not own. Because a worktree must survive until its commit lands, the pack's gate-direct-commits-on-validate-green rule was unsatisfiable for the whole duration of any RW agent cycle. This IS an instance of the entry's defect class: the guard's candidate set was not the reality it claimed to check — the inverse polarity of (2) and (3) (it FAILED on non-reality rather than passing on it), but the same decoupling between what a check enumerates and what it asserts about. Measured at fix time: on a clean tree the tracked-file set and the raw walk select an IDENTICAL 854 scanned files (1108 tracked / 1426 on-disk paths before filtering), so the correction is behavior-preserving on the repo and changes only the environment-artifact exposure. Fixed by drawing the candidate set from `git ls-files` with a lenient SKIP off a git work tree, per `ci-guard-measure-then-bound` and the established Check 63 / Check 69 idiom — NOT by path-excluding `.claude/worktrees/`, which would suppress the symptom and leave the defect.

  **Why this is not theoretical — the mechanism, verified in code.** `supporting-docs/MIGRATION-v10-to-v11.md` carried 12 BARE `MERGE-STRATEGY.md` references after that file moved to `pack-ops/MERGE-STRATEGY.md`. Check 68 resolved all 12 (the basename existed) and stayed green; Check 40 never walked the file because it is not client-installed; Check 43 never walked it for the same reason. The references survived undetected until a reviewer found them by hand during the release cut, and were qualified to `pack-ops/MERGE-STRATEGY.md` in the RC1 cut commit. Both halves of the mechanism are confirmed against the check bodies at HEAD.

  **Measured exposure (the measure-then-bound census input, run with Check 40's own extraction + allowlist + anchor-phrase + same-dir rules).** Raw occurrence counts on the live uncovered surfaces, classified by candidate-set size, NOT yet triaged KEEP-vs-STRIP: the 8 non-installed `supporting-docs/` files — 18 zero-candidate / 43 single-candidate / 7 multi-candidate; the 6 repo-root markdown files (README + trinity) — 11 / 26 / 5; the 65 per-CLI operating-doc files (`.claude/`, `.codex/`, `.agents/`, `.agents-plugin/`) — 0 / 31 / 4. Roughly 145 occurrences on live surfaces requiring individual KEEP-or-STRIP classification. A large share are legitimate KEEPs — e.g. the trinity's bare `BACKLOG.md` / `IMPLEMENTATION-PLAN.md` / `CHANGELOG.md` describe a CLIENT-side artifact with no pack-repo file, and `SKILL.md` has ~100 candidates by ecosystem convention (the `filename-uniqueness-heuristic` structural-collision exemption). Every zero-candidate reference measured in the 8 `supporting-docs/` files is ALREADY on `pack-ops/.dangling-ref-allowlist.txt` as an intentional KEEP, which is why the existence axis is green: the uncovered axis is BARENESS, not non-existence.

  **Standing scope constraints (user-set at BD-open).**
  - **Out of scope, permanently:** `maintenance-docs/`, `backlog/`, `changelog/`, `test-fixtures/`, and `scripts/tests/fixtures/` are NOT to be brought under a bareness gate. They are historical / deliberate-fixture records under the accurate-history model (~480 files, ~1100 zero-candidate occurrences), already excluded from Check 68 by `_CHECK_68_EXCLUDE_PREFIXES`, and already carrying the Check-48 soft-advisory removed-doc scan. This is a constraint handed to the architect, not an open design question.
  - **Scope grows into THIS entry, never into a new one.** Anything the architect, planner, coder, or reviewer surfaces during BD-288 is folded into BD-288 and done now. Opening a follow-on BD for work discovered here is tech debt and is prohibited; a deferral requires explicit user authorization per the `deferral-is-scope-creep` and `no-deferral-without-user-direction` rules.
  - **Prefix-keyed allowlist fragility — recorded for the architect, NOT acted on (surfaced during the defect-(4) fix).** `_check_53_is_allowlisted` matches on exact repo-relative path strings plus `str.startswith` directory prefixes. When Check 53's candidate set yielded paths under an unexpected prefix (`.claude/worktrees/agent-*/`), every allowlist key missed and the guard's OWN sanctioned carriers were re-reported as offenders — measured at 2 false offenders against the canonical checkout, both of them the check's own self-exception files. Defect (4)'s tracked-set fix removes the only known way to produce such prefixes, so this is LATENT, not live: with `git ls-files` the candidate paths are repo-relative by construction. The open question for the architect is whether the sibling prefix-keyed allowlists already in this entry's `File/Symbol` — `_CHECK_40_ALLOWLIST`, `_CHECK_68_EXCLUDE_PREFIXES`, and the adjacent `_CHECK_64_EXCLUDE_PREFIXES` — carry the same fragility and want the same reasoning applied. The implementation recommendation carried forward is to leave the allowlists alone for now (an assertion would guard an unreachable state and cost a per-candidate check against `ci-check-runtime-compounding`); the architect may overrule.
  - **Design questions left to the architect (not pre-decided):** whether the fix is a widened Check-40 walk, a new check, or a generalization of Check 68; whether the Check-68 basename-fallback hardening lands in this BD (it touches currently-green surfaces — `project-template/` and `pack-ops/` — and drags the client twin along under mirror-but-customize); how a fourth walk over ~150 additional files is kept cheap under `ci-check-runtime-compounding`.
  **Session-hygiene items folded in — NOT instances of this entry's defect class.**
  Five items surfaced during BD-288 work. None is a CI guard, and none is an
  instance of the class this entry exists to fix (a check that is registered, runs,
  prints OK, and does not reach the reality it claims to verify). They are recorded
  here rather than in new entries solely under the standing constraint above — scope
  grows into THIS entry — and they have no bearing on the acceptance criteria below.
  - **Stale push rule in `pack-ops/PACK-CHAT.md`.** The bullet governing pushes still
    described the v11-dev development phase and forbade pushing to `main` from Pack
    Chat. That phase has ended: `main` carries the v11 content, the local `v11-dev`
    branch no longer exists, and Pack Chat commits on `main` — so the rule as written
    forbade every ordinary push. Rewritten to state the post-cut reality (`main` is
    the working branch; approved commits push normally) while keeping the protections
    that still bind: no force-push of `main`, no rewrite of landed history. The "No
    commit without explicit approval" rule is untouched and unweakened.
  - **Agent worktree directory showing as untracked.** Sub-agent worktree isolation
    materializes checkouts under `.claude/worktrees/`, which appeared as an untracked
    path in the working-tree view Pack Chat inspects before every commit — noise
    adjacent to a staging step. Added `.claude/worktrees/` to `.gitignore`, scoped to
    that subdirectory so the 24 tracked `.claude/` files (`agents/`, `skills/`,
    `settings.json`) stay tracked.
  - **The same worktree directory was still visible to the knowledge-graph builder.**
    `.graphifyignore` is read INSTEAD of `.gitignore` (its own one-file-or-the-other
    rule), so the `.gitignore` entry above does not propagate to it; left unexcluded,
    a graph rebuild during a live agent run would index a full second checkout and
    duplicate every node. Added `.claude/worktrees/` there as a plain literal — the
    matcher is fnmatch, where `*` crosses `/` and `**` is not special.
  - **Two client setup guides pinned the install to refs that do not resolve.**
    `supporting-docs/SETUP-NEW.md` sent readers to a `v11-dev` branch and to `v11.0` /
    floating `v11` tags — none of which exist, so the command failed loudly.
    `supporting-docs/SETUP-EXISTING.md` pinned `git checkout v10.0` while describing
    itself as the v11.0 guide throughout; that ref DOES exist, so it succeeded and
    silently installed v10 pack content (and its `v10` floating alternative resolves
    to v10.1 — a different commit again). Both now list the available `v11*` tags,
    pin to `v11.0-RC1`, and offer tracking `main` as the alternative. Client-facing
    surface: no pack-only mechanism was introduced into either guide.
  - **Read-only pack agent definitions declared a file write their tool grant did not
    permit.** `pack-architect`, `pack-planner`, and `pack-docs-researcher` each instruct
    the agent that its single permitted file write is the one caller-specified document
    and that emitting it as a chat message instead is a defect — but their Claude
    `tools:` lines carried no `Write`/`Edit` and their Codex `sandbox_mode` was
    `read-only`, so the declaration was unbacked on both CLI surfaces that encode a
    capability. Measured effect in one session: two architect instances fell back to
    Bash heredocs, while a third read the definition's own fallback clause as forbidding
    that, declined to write, and transmitted a finished ~700-line review over chat in six
    chunks. Fixed by granting `Write, Edit` (Claude) and `workspace-write` (Codex) to the
    three, each scoped by the same prose sentence `pack-reviewer` already carried. The
    Codex sandbox comment was then corrected and made byte-identical across all FOUR
    Codex RO definitions (`pack-reviewer.toml` included — it carried the original
    overclaim the other three were copied from): `workspace-write` neither reaches the
    out-of-workspace handoff path nor narrows writes, since no pack-side
    `.codex/config.toml` sets `writable_roots`, so on Codex the RO guarantee is the
    prose policy rather than a sandbox barrier — stated in `pack-ops/PACK-AGENTS.md`
    § "Two agent classes" as well. The
    `**Read-only.**` mandate header that Check 52 binds to is unchanged, so the two-class
    model is untouched. The Antigravity bundle carries no capability field for any of
    the five agents — including the RW `pack-coder`, which does write — so it was left
    unedited. That rests on an ASSUMPTION, recorded here as one: that a bundle file
    with no capability field is unrestricted rather than restricted-by-default. What
    backs it is the measured 0/5 field count and the RW agent sharing the same shape;
    what does NOT back it is Antigravity's documented behaviour, which the bundle's own
    line-1 marker (`RE-VERIFY at impl: plugin agents/ inner template schema +
    frontmatter field set`, present in all five files) records as unverified. If that
    re-verification finds a capability mechanism, this leg needs revisiting. Not an
    instance of this entry's defect class in
    the CI-guard sense — no check is involved — but the same underlying failure: a
    declared mapping whose load-bearing reality was never verified
    (`declare-verify-backing`).
Acceptance criteria: the bareness axis covers every LIVE pack doc surface — the 8 non-installed `supporting-docs/` files, the repo-root `README.md`, the pack-root trinity, and the per-CLI operating-doc set — with the exclusion set above intact and code-enforced; the guard was designed measure-then-bound (the full occurrence list captured, every occurrence classified KEEP or STRIP, fix-recipes stripping every STRIP occurrence, the allowlist sized EXACTLY to the measured legitimate set and each entry carrying a one-line rationale, and the guard verified clean against the projected post-fix state); the guard draws its candidate file set from git-TRACKED files and skips lenient when git is unavailable; the Check-68 basename-index fallback no longer resolves a qualified path whose own path does not exist (or the architect records an evidence-based rationale for leaving it, with the blast radius on the currently-green surfaces measured); the client twin `project-template/scripts/validate-docs.sh` is reconciled under mirror-but-customize (a separate project-side copy, never a shared file); a per-check test file is authored and wired into `.github/workflows/validate-pack.yml` so Check 42 passes; the `scripts/validate-pack.py` header check-count prose and the `README.md` version-table check-count claim are updated in lock-step per `enumerate-encoding-surfaces`; a regression test reproduces the original mechanism (a bare reference to a file that exists at a DIFFERENT path must FAIL); Check 81's FAIL leg demonstrably FIRES against the `active[]` form that `pack-ops/session-state.json` actually carries, proven by a test that puts an active BD with a bare `File/Symbol` in `active[]` and asserts a non-zero exit, with `scripts/dashboard-render.py` and `pack-ops/DASHBOARD-SPEC-PACK.md` left consistent; Check 82's trigger is re-verified against the same reality and fixed if it shares the inertness; full battery green — both CI jobs plus `PACK_VALIDATE_DEEP=1` plus `build.sh --verify` plus the shard plan.
References: the originating findings are `maintenance-docs/v11-implementation/FIX-REPORT-BD-093-1.md` (OI-2) and `maintenance-docs/v11-implementation/PACK-REVIEW-BD-093-FINAL.md`; BD-179 authored Check 40, BD-173 (H.14) authored Check 43, BD-243 (Gate 3) authored Check 68 and `pack-ops/.dangling-ref-allowlist.txt`, BD-271 authored the Check-85 narration-twin parity guard that documents the pack↔client twin relationship; BD-180 (G) owns the `_CLIENT_INSTALLED_FILES` inventory Check 43's walk derives from; BD-206 will retire the project-side monolithic mirror the trinity's bare `BACKLOG.md` / `IMPLEMENTATION-PLAN.md` / `CHANGELOG.md` references describe.
Position: v11.0 — prevention hardening on the CI guard family, opened after the RC1 tag. LARGE by the size test: cross-surface (`supporting-docs/` + repo-root trinity + per-CLI operating docs + the client twin — 2 or more families), blast-radius (a census is required by `ci-guard-measure-then-bound`, and the encoding surfaces are 3 or more: check body, allowlist, per-check test, CI workflow wiring, the validate-pack header count, the README count), and structural (a new or changed CI check). Launch-gate does NOT fire — RC1 is cut and the affected surface is clean on the existence axis. Runs the deterministic LARGE pipeline starting at `pack-architect`.
