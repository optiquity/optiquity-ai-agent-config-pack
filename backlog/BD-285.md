<!-- per-entry source: /backlog/BD-285.md; contract: /backlog/_rules.md -->
**BD-285 — Fresh-install into existing code: close the untested collision-sidecar branch (C1) and replace the existing-`CLAUDE.md` dead-end STOP with a guided keep/replace/merge branch (C2)**
Type: feat/test — install correctness + UX (existing-source collision coverage + existing-trinity onboarding; C1 small, C2 guided-merge LARGE).
Status: Resolved
Target: v11.0 (user chose "turnkey it all in v11.0" including the guided existing-`CLAUDE.md` merge, 2026-08-11).
Blockers: None hard. C2's guided branch reuses the shared `scripts/lib/prompt.sh` (BD-284). COORDINATE: edits `init-project.sh` (also touched by BD-284) — sequence/serialize.
Unblocks: a repo with code + a hand-written `CLAUDE.md` (never had the pack) onboards via a guided merge instead of a hard STOP; the reachable install collision path gains test coverage.
File/Symbol: **C1** — a new `existing-source`-with-a-colliding-non-trinity-file fixture (`test-fixtures/build.sh` + `test-fixtures/manifest.txt`) + a `contract`/`test-init-project.sh` assertion exercising the `existing_classifier_copy` → `.pack-template` sidecar branch (the reachable one, 7 call sites). **C2** — `scripts/init-project.sh` (`classify_project_state` / `detect_ai_config` → `already-configured` STOP at ~L1562-1577, and `stage_s7_trinity` ~L736-748); `scripts/lib/detect.sh` (`detect_ai_config`).
Description:
  **GAP-1 (C1, test-only):** the reachable fresh-install collision-sidecar branch (`existing_classifier_copy` → project-shadows-new-pack → `.pack-template`, 7 non-trinity call sites) has ZERO positive test coverage. `contract-mid-dev.sh` asserts the NO-collision case (its fixture ships no colliding files). Close the hole: a fixture whose `existing-source` tree OWNS a file colliding with a pack name (e.g. a pre-existing `scripts/<name>` or `docs/pack/<name>`), asserting the sidecar is created + the user file preserved.
  **GAP-2 (C2, behavior):** a repo with code + a hand-written `CLAUDE.md`/`AGENTS.md`/`GEMINI.md` (never had the pack) hits `already-configured` STOP (`detect_ai_config` counts a bare trinity file) and is told to "remove or archive" it — NO guided merge; the user's content is lost unless they hand-merge. The `stage_s7_trinity` `.pack-template` branch that LOOKS like it handles this is UNREACHABLE dead code (the STOP fires upstream). This is the exact "existing repo, has code, never had the pack" persona the user named.
  **Goal:** replace the bare trinity STOP with a guided branch — detect a hand-written trinity file that is NOT pack-installed, and prompt (via `prompt.sh`) `keep yours / replace with pack / merge (install pack trinity, preserve your content as a sidecar for hand-merge)`; the STOP stays only for a genuinely pack-managed or ambiguous config. Preserve the safe non-TTY default (decline/STOP unless `--yes`/explicit choice). Remove or wire-in the now-reachable S7 collision branch accordingly.
  **Design caution (pack-boundary HIGH):** C2 mutates the `classify → STOP` contract, a launch-gate-adjacent behavior — the architect designs the detection (pack-installed vs hand-written trinity) + the guided branch + its non-TTY degradation carefully; the safe default must never clobber a user's `CLAUDE.md`.
Acceptance criteria: **C1** — a fresh install against an `existing-source` repo owning a colliding non-trinity file creates the `.pack-template` sidecar, leaves the user file byte-identical, and a test asserts it. **C2** — a repo with a hand-written (non-pack) trinity file is offered keep/replace/merge (interactive) rather than a bare STOP; "merge" installs the pack trinity and preserves the user's content as a recoverable sidecar; the non-TTY/`--no-interactive` default is safe (never silently overwrites); a genuinely pack-managed config still routes to the migrator. `INSTALL-PROCEDURES.md` + any classify docs updated in lock-step; `validate-pack` (both jobs + `PACK_VALIDATE_DEEP=1`) + persona-contract suite green; existing greenfield/mid-dev contracts unregressed.
References: the onboarding first-run UX strategy exploration (2026-08-11, GAP-1/GAP-2 + EEB-GAP1/EEB-GAP2/EEB-AICONFIG/EEB-STOP); BD-284 (shared `prompt.sh`); `contract-mid-dev.sh` / `contract-greenfield.sh` (persona contracts); `scripts/lib/detect.sh`.
Resolved: 2026-08-20 — C1 + C2 (with the folded UNIVERSAL 2-way no-BASE onboarding merge) shipped via the deterministic LARGE pipeline (planner → adversarial planner [1 BLOCKER: the F1 ordered helper crashed the flagship lone-`CLAUDE.md`-starter persona under `set -euo pipefail` by stashing absent siblings; 1 MUST: the Wave-E check numbered an already-assigned ID; 4 SHOULD] → reconciliation planner [all 7 folded] → user planner-to-coder gate → 5 parallel/serial worktree coder waves, each bounded-review + fix CLEAN, per-commit full battery green). **Wave A** (`7d7b8fb`) C1: the `existing-project-collision` fixture + `contract-existing-source.sh` (F-1 byte-identity + `.pack-template` sidecar, proven to BITE) + a doc-currency sweep. **Wave B** (`ec82ff0`) C2 core: `init-project.sh` classify refactor + provenance-gated three-way dispatch (pack→migrator STOP / ambiguous STOP / hand-written→guided) keyed on the new `detect_trinity_provenance` + `handle_handwritten_trinity` (`--trinity=keep|replace|merge`, evaluated independent of `prompt_should_interact`, null-choice STOP, non-TTY-safe) + the per-file-GUARDED F1 never-lose floor (stash `<f>.user-orig` → verify byte-equal → overwrite; absent siblings plain-install with no sidecar/row) + the `.pack-install-reconcile` `merge-2way` writer + the structured key-union (driven off `customization_classify`, throwaway probe, F6 non-silent fallback, incl. the reviewer-caught guided-branch silent-overwrite fix) + the P0 commit-first `EXIT_DIRTY` message; `detect.sh` `detect_trinity_provenance` + the P1 config-shape guard (populated foreign roster → STOP; incl. `.gemini/agents|skills`). **Wave C** (`46ecd76`) the client `resolve-merge-conflicts` skill **Case 3**: a 2-way no-BASE trinity fold (OURS=`.user-orig`, THEIRS=live pack trinity) behind the empty-BASE Regime-B round-trip gate + a THEIRS-keyed completeness lower bound + an honest disclosure (the line-level leg is a lower bound; `.user-orig` is the floor) + KEEP-`.user-orig`-on-success (unlike Case 2) + a deterministic no-LLM fold-gate test (ACCEPT + 4 REJECT variants + `.user-orig` survival). **Wave D** (`2f79a30`) the install-merge docs (MERGE-STRATEGY.md base-absent matrix + F2/F6/state-dir-lifecycle disclosures + the OI-C realized-consumer cross-ref; SETUP-EXISTING/SETUP-NEW/INSTALL-PROCEDURES merge-vs-replace + `.user-orig` recovery + the P0 commit-your-starter-first caveat) + the BD-243 concision ceiling re-derivation 557→696 (`ceil(605×1.15)`, the check's own convention). **Wave E** (`f35c66b`) validate-pack **Check 94** (the install-merge token wire BITES both directions — init writes `merge-2way` ↔ the SKILL.md Case-3 selector reads it — plus the negation-aware Case-3-On-success KEEP-`.user-orig` static backstop; ROI-2 one entry), count 90→91 with the README count-twin in lock-step. The never-lose SAFETY FLOOR: a byte-identical `.user-orig` sidecar captured before overwrite and KEPT on success — even a buggy fold cannot lose the user's content. Replaces the existing-trinity dead-end STOP with a guided keep/replace/merge branch and folds the BD-287 auto-merge machinery into a universal onboarding merge (v10.x update, non-pack existing project, greenfield-with-starter). Launch-gate satisfied; a leg of the turnkey-onboarding program. Every wave green on the full wired battery (both `validate-pack` jobs + `PACK_VALIDATE_DEEP=1` + `build.sh --verify` + persona contracts + shard). BD-172 rebases Gate 2 onto the merged/sidecar rows next; BD-093 release pin last.
Position: v11.0 — C1 (cheap coverage) + C2 (LARGE guided-merge, contract-mutating). C2 runs the deterministic LARGE pipeline. Sequence with BD-284 (`prompt.sh` + `init-project.sh`); a launch-gate item ahead of BD-093.
