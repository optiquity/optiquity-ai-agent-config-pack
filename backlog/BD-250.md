<!-- per-entry source: /backlog/BD-250.md; contract: /backlog/_rules.md -->
**BD-250 — Extend the anti-history/bloat durable guards (Check 65/66/67) to `supporting-docs/` operating docs + strip residual violations (measure-then-bound)**
Type: feat — surfaced 2026-06-27 by the BD-206 history/bloat audit; user-approved open. CI-guard coverage extension (BD-243 follow-up).
Status: Resolved
Target: v11.0
Blockers: BD-206 — sequenced AFTER BD-206 finishes, with the post-BD-206 set (BD-249/246/248); exact order TBD when the post-206 queue is sequenced. It extends BD-243's guards and strips a residual in `supporting-docs/MIGRATION-v10-to-v11.md` (the same file BD-206 D2-6 edited) — landing after BD-206 avoids churning that file concurrently.
Unblocks: regression-proof anti-history/bloat enforcement across the newly-operating `supporting-docs/` set (METHODOLOGY + INSTALL-PROCEDURES) — closes their coverage hole so history/bloat can no longer creep back into those 2 install-shipped operating docs unseen. The other `supporting-docs/*.md` (MIGRATION / SETUP-NEW / SETUP_TEMPLATE / CLI-PM-SETUP) stay REFERENCE (BD-243 EXEMPT) and are deliberately NOT guarded under this BD's option-b scope.
File/Symbol:
  - `scripts/validate-pack.py` `_CHECK_OPERATING_DOC_FAMILIES` (~`:8190`) — the IN-set for Check 65 (no-history) / Check 67 (no-deferred-feature); ADD exactly 2 point literals: `supporting-docs/METHODOLOGY.md` + `supporting-docs/INSTALL-PROCEDURES.md` (point family-members, NOT a glob; Check 69 `_CHECK_OPERATING_DOC_SCANNED_TREES` is UNCHANGED). Check 66 is N/A — its surface is the trinity `## Pack memory` bullets, not the families IN-set.
  - Operating-vs-reference split (architect-confirmed per the BD-243 taxonomy): OPERATING = METHODOLOGY.md + INSTALL-PROCEDURES.md (re-classified REFERENCE→OPERATING, the targeted reversal). REFERENCE (stay EXEMPT, NOT guarded) = MIGRATION-v10-to-v11.md, SETUP-NEW.md, SETUP_TEMPLATE.md, CLI-PM-SETUP.md, and the remaining `supporting-docs/*.md`.
  - Strip recipe (measure-then-bound, 3 STRIP hits in the 2 newly-operating files): METHODOLOGY.md `; tracker integration is deferred` (deferred-tracker clause), METHODOLOGY.md `schedule a single-source consolidation in a future cleanup batch` (future-batch roadmap), INSTALL-PROCEDURES.md `the addition lands in a future / pack version` (future-pack-version roadmap). 14 KEEP hits allowlisted (C4 live TD-deferral workflow), sized exactly.
  - CLIENT-gate parity (CG-CLIENT) — CLOSED: add 11 `doc:` KEEP records to `project-template/scripts/.docs-gate-allowlist.txt` for the installed `docs/pack/METHODOLOGY.md` + `docs/pack/INSTALL-PROCEDURES.md`, plus a 2C per-check test exercising the client DEFERRED axis against the real shipped files.
Description:
  Problem: BD-243 stripped historical/audit-trail text + bloat from the operating subset of `supporting-docs/` (one-time) and added the durable guards Check 65/66/67 — but the guards' IN-set (`_CHECK_OPERATING_DOC_FAMILIES`) does NOT include `supporting-docs/`, so those operating docs carry NO regression guard (history/bloat can creep back unseen), and a residual deferred-feature violation already persists at `MIGRATION-v10-to-v11.md:762-763` (invisible to Check 67 only because of the coverage hole). Surfaced 2026-06-27 by the BD-206 history/bloat audit, which found BD-206 itself CLEAN — these two gaps are pre-existing, not BD-206 regressions.
  Goal: close the coverage hole — extend Check 65/66/67's IN-set to the operating subset of `supporting-docs/` (measure-then-bound: add the surfaces → run the guards → strip whatever they newly flag, including the `MIGRATION:762-763` residual; size any allowlist exactly to legitimate KEEPs) — so anti-history/bloat enforcement covers ALL operating docs going forward, zero meaning/functionality lost.
  Process: size-tiered large-BD standard (it changes a CI check = structural): researcher-if-needed → architect (the operating-vs-reference split for `supporting-docs/` + the guard extension + the measure-then-bound strip recipe) → planner → coder + bounded review/fix cycle; validate-pack green.
  **Out of scope:** BD-206 (CLEAN per the audit); the one-time strip BD-243 already performed (this is the COVERAGE extension + residual cleanup for the 2 newly-operating files); operating docs already in the guard IN-set; the `supporting-docs/MIGRATION-v10-to-v11.md` "Phase B is deferred. Tracker integration is deferred indefinitely" residual — MIGRATION stays REFERENCE under option b (NOT scanned / NOT allowlisted / NOT stripped), and that residual is the migration narrative of the standing `project_tracker_deferred_indefinitely` user decision (2026-06-12, non-negotiable), so it is INTENTIONALLY retained; the 7 reference `supporting-docs/*.md` (MIGRATION / SETUP-NEW / SETUP_TEMPLATE / CLI-PM-SETUP / and the rest) — they stay REFERENCE/EXEMPT, deliberately NOT guarded.
Resolved: 2026-06-28 — option b: re-classified ONLY `METHODOLOGY.md` + `INSTALL-PROCEDURES.md` as operating docs (2 point literals → `_CHECK_OPERATING_DOC_FAMILIES`; Check 69 scanned-trees unchanged); stripped 3 residual deferred-feature mentions; Check-67 allowlist sized to exactly 14 KEEP; client-gate parity = 11 `doc:` KEEP records in `.docs-gate-allowlist.txt` + a 2C per-check test. ONE neutral commit `2236788`; count stays 74 (no new check). The `MIGRATION-v10-to-v11.md` residual is intentionally retained (REFERENCE; `project_tracker_deferred_indefinitely`). Full large-BD pipeline (researcher → architect → adversarial → reconcile → planner → adversarial planner → reconcile → re-validation vs post-BD-246 HEAD); review CLEAN.
