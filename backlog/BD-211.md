<!-- per-entry source: /backlog/BD-211.md; contract: /backlog/_rules.md -->
**BD-211 — Canonicalize the per-entry header grammar: eliminate suffix sub-entry forms + normalize title parentheticals; validator-enforced (BD-204 prerequisite)**
Type: feat — STRUCTURAL. Canonicalize the per-entry BD/TD header grammar to `<ID>-NNN — Title` (no letter-suffix sub-entry form; no parenthetical between the ID and the em-dash), simplify the shared per-entry engine + validator + `_rules.md` to that canonical form, and make the validator ENFORCE it so drift is caught going forward. Adapts the BD-203-built shared grammar. SCOPE (pack vs cross-surface) to be set by the architect — the engine/_rules/validator are shared, so this likely spans pack + the shared layer (NOT assumed pack-only; the commit subject carries the architect-determined keyword or none).
Status: Open
Target: v11.0 — BD-204 PREREQUISITE (must land before BD-204 resumes at C-6 / the C-8 dogfood flip, so the migration carries a canonical, suffix-free backlog).
Blockers: interrupts BD-204 (paused after C-5). Adapts BD-203 (which built the shared per-entry grammar with suffix support).
Unblocks: BD-204 proceeds on a clean canonical backlog (all entries parse; the forward migrator drops nothing); the rules + structure are simplified (no suffix special-case); the validator guards the canonical grammar against future drift.
Problem: the forward migrator's header regex (`(?:BD|TD)-\d{3}\s*[—-]`) silently drops 3 of 212 live entries — `BD-167b`, `BD-169b` (the grandfathered suffix sub-entries) and `BD-195` (a `(Code Red 3)` parenthetical between the ID and the em-dash). The suffix form is admitted by the shared engine grammar (`BD-[0-9]+[a-z]*`), `_rules.md`, and `validate-pack.py` — a special-case the user has decided to retire. Without canonicalization, BD-204's dogfood migration is lossy (3 entries) and the structure stays needlessly complex.
Scope: (a) ELIMINATE the suffix sub-entries — fold `BD-167b`→`BD-167` and `BD-169b`→`BD-169` as in-body sections (per the no-letter-suffix rule "a sub-part is a section"), delete the 2 files, repoint the active `BD-169` cross-ref, regen `_toc.md`; (b) NORMALIZE the `BD-195` title parenthetical to the canonical position; (c) SIMPLIFY the shared per-entry engine (`scripts/lib/per-entry/*`) + `_rules.md` grammar to canonical `<ID>-NNN — Title` (remove suffix admission); (d) make `validate-pack.py` ENFORCE the canonical header (a guard that fails any non-canonical header — suffix or pre-em-dash parenthetical); (e) update the affected tests + the trinity no-letter-suffix rule/memory (grandfathering now moot); (f) disposition the ~50 references — ~10 active (update) + ~40 historical maintenance-docs (accurate-history → advisory Check-48 WARNs, NOT rewritten).
Out of scope: BD-204's tracker-migration code (separate); rewriting historical maintenance-docs (accurate-history).
Acceptance criteria (full audit): all live entries parse via the canonical header (count == live count, currently 210 post-fold); no suffix entry/grammar remains in the active engine/_rules/validator; the validator FAILS a non-canonical header (suffix or pre-em-dash parenthetical) — verified with a negative test; the no-letter-suffix rule/memory reflect the retired grandfathering; historical refs are advisory WARNs only; validate-pack green + full CI; BD-203's shared grammar adapted with no project-side regression.
References: BD-203 (built the shared per-entry grammar); BD-204 (the prerequisite consumer); `feedback_no_bd_letter_suffix`; `feedback_rename_plans_measure_then_bound`; `feedback_researcher_maps_blast_radius_before_architect`.
Resolved: n/a
Position: v11.0; interrupts BD-204 between C-5 and C-6 (user 2026-06-06).
