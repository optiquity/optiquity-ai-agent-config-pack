<!-- per-entry source: /backlog/BD-225.md; contract: /backlog/_rules.md -->
**BD-225 — Add Graphify knowledge-graph support for Pack Chat + pack agents' own work (PACK-OPS; not shipped to clients)**
Type: feat — pack-ops tooling / agent token-efficiency. Wire the Graphify knowledge-graph tool (the `graphify` CLI; installed release 0.8.39) into THIS repo so Pack Chat + the pack-* agents can query a compact repo subgraph instead of re-reading the file tree — for pack self-maintenance AND the `project-template/` deliverables they maintain. Opened 2026-06-16 (user direction).
Status: Open
Target: v11.0 (user 2026-06-16). Orthogonal pack-ops (does NOT change what ships to clients), pulled into v11.0 to accelerate pack development. Sequenced BEFORE BD-226 (user 2026-06-17).
Blockers: none — BD-221's Gemini→Antigravity trinity conversion (the prior blocker) is Resolved 2026-06-17, so the graph-first rule lands on the converted pack-root trinity.
Unblocks: token-efficient repo context for Pack Chat + every pack agent it spawns (the pack is doc/reference/agent-heavy — Graphify's sweet spot); fewer full-tree reads per query.
Description:
  **Problem:** Pack Chat + the pack-* agents repeatedly re-read the file tree for repo context, which is token-expensive on a doc/reference/agent-heavy repo.
  **Goal:** wire Graphify into this repo PACK-SIDE so Pack Chat + the pack-* agents query the graph for context during tasks, and the graph is kept usefully fresh.
  **Design status: NOT yet designed.** Every aspect of Graphify usage — building the graph, keeping it FRESH, how spawned agents AND Pack Chat query/use it during tasks, ignore handling, hooks, the graph-first rule placement + agent enablement, output/git hygiene, and the safety foot-guns — is to be designed by a pack-architect as an options + pros/cons menu, with the USER deciding each. (The earlier "pre-adjudicated/locked" decisions were built against an older Graphify version and are superseded.)
  **Pre-architect gate (REQUIRED):** before the initial architect is spawned, the USER must provide additional goals, requirements, and success criteria to guide and focus that architect. The architect is NOT spawned until the user has supplied them.
  **Boundary (absolute):** PACK-OPS ONLY. The graph may INDEX the whole repo (incl. `project-template/`, so agents get context), but every setup artifact lives PACK-SIDE and nothing ships in the config pack; the graph-first rule goes in the PACK-ROOT trinity, NEVER the `project-template/` trinity (P-missed-7).
References: the source guides `dshanenyc-mac-setup/docs/graphify/{REPO-QUICKSTART,PLAN}.md` + the installed skill `~/.claude/skills/graphify/SKILL.md` (release 0.8.39); `feedback_bd_pack_only_operational_rule` / P-missed-7 (boundary).
Resolved: n/a
Position: v11.0 — pack-ops token-efficiency tooling; sequenced before BD-226 (user 2026-06-17). Design pending (architect options → user decisions).
