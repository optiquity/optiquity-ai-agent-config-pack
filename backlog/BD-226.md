<!-- per-entry source: /backlog/BD-226.md; contract: /backlog/_rules.md -->
**BD-226 — Sub-agent worktree-isolation overhaul: RW agents in per-commit isolated worktrees, RO agents in the work's tree, in-worktree review/fix cycle, patch-only-after-review-clean — generalized to pack-ops + project ops docs (Claude-only)**
Type: feat — agent-execution model / workflow. Replace the current "sub-agents run in-place by default; isolation is opt-in" rule with the model developed live during BD-221. The model is stated GENERALLY (orchestrator + agents) and then re-applied to two surfaces: pack-ops (Pack Chat + pack-* agents) AND the client project's own workflows (its operations docs). Opened 2026-06-17 (user direction, mid-BD-221); rules refined with the user 2026-06-17.
Status: Open
Target: v11.0 — FAST FOLLOW-ON directly after BD-221 (user 2026-06-17: "change the default behavior for isolation after this BD"). The model is followed LIVE throughout BD-221; this BD codifies it permanently afterward so the rule change does not perturb behavior mid-cluster.
Blockers: BD-221 — codify AFTER BD-221 completes (the model is in active live use during it).
Unblocks: a safe default where read-write agents never expose buggy/unreviewed work to a canonical tree (pack or client), and a clean, repeatable orchestration contract for every future multi-commit effort on both surfaces.
Description:
  **Provenance:** developed live during BD-221 after the in-place-coder pollution incident; this BD makes the agreed model the standing default rather than a per-session improvisation.
  **STATE IT GENERALLY (orchestrator + its agents), then re-apply to each surface.** The user's explicit instruction: generalize the rules, then apply them to the project's workflows for project use — do NOT write a pack-only rule.

  **THE GENERALIZED MODEL (the rules, as refined with the user 2026-06-17):**
    1. **Default by agent class.** **Read-WRITE agents** (coders, fix-coders — anything that writes/mutates) run in an **isolated worktree**. The **FIRST coder of a commit establishes (creates) a fresh isolated worktree**; **every subsequent read-write agent in that same commit's cycle — fix-coders included — REUSES that same worktree. NEVER create a new worktree for a fix-coder.** **Read-ONLY agents** (reviewers, architects, planners, auditors, docs-researchers) run **in whatever tree their target work lives in**: the **main/canonical tree when that work is on HEAD/committed**, the **isolated worktree when the work is still uncommitted there**. (Read-only is NOT "always in-place" — it goes to where the work is.)
    2. **The governing bias.** NEVER apply, patch, or commit known-buggy or not-yet-reviewed work to the canonical tree (the pack repo's main checkout, or the client project's tree).
    3. **Per-commit worktree + in-worktree review/fix cycle.** Each new commit's first coder gets a fresh isolated worktree (rule 1). The **ENTIRE review/fix cycle for that commit runs inside that one worktree** — the read-only reviewer(s) and the fix-coder(s) all operate there. Nothing reaches the canonical tree mid-cycle.
    4. **Patch only after review-clean.** Read-write agents do **NOT** emit a patch up front (the work may be wrong). The patch is produced **only after a read-only reviewer (or other RO agent) confirms the work CLEAN**, by **SendMessage-ing the most-recent read-write agent** in that cycle to produce it. The orchestrator then applies that reviewed-clean patch to the canonical tree and commits (with the human's approval). → the canonical tree only ever receives reviewed-clean work, at commit time.
    5. **Bounded cycle (unchanged).** ≤2 review/fix pairs + 1 final review per commit; architect escalation if still dirty after the final pass. Only WHERE the cycle runs changed (the commit's worktree), not the cycle limits.
    6. **Agent spawning.** Each spawn is a fresh, clean-context agent UNLESS SendMessage is used (the deliberate exception that re-engages an existing agent — e.g., the post-review-clean patch step in rule 4).
    7. **Worktree lifecycle.** Fresh worktree per commit's first coder; **after a commit lands, explicitly REMOVE every orphaned worktree from that commit's cycle** — *after* the commit (safe by then), **not** right-after-use (it may be needed again mid-cycle), and **never** by relying on auto-removal.
    8. **Mechanics.** Isolated worktrees ALWAYS base at local HEAD (`worktree.baseRef:"head"`). An agent operating in a pre-existing worktree (the reviewer/fix-coder in the first coder's worktree) `cd`s into it and VERIFIES pwd/HEAD at runtime, operating only there. Agents NEVER commit/stage/apply — only the orchestrator applies the final clean patch and commits, with human approval.

  **APPLICATION A — PACK-OPS (Pack Chat + pack-* agents).** Surfaces to update: the pack-root trinity `## Pack memory` "Sub-agent behavior (Claude-only)" section (flip the default + the in-worktree-cycle + patch-after-review-clean + worktree-lifecycle rules); `pack-ops/PACK-CHAT.md` + `pack-ops/PACK-AGENTS.md` (the orchestrator's run-the-cycle-in-the-worktree contract); the `feedback_worktree_isolation_mergeback_ops` memory (revise the merge-back: NO up-front patch; SendMessage-the-RW-agent-after-review-clean; explicit worktree removal after commit).
  **APPLICATION B — CLIENT/PROJECT (the project's own orchestrator + agents).** Re-apply the SAME generalized model to the project's operations docs: the project-template PM-chat rules (`project-template/.../PM-CHAT.md` or equivalent), `METHODOLOGY`, and other project ops docs that define how the project's agents are spawned and how its review/fix cycle runs. The generalized rules are restated for the project's audience (its orchestrator + its agents), not copied verbatim from the pack wording.

  **SCOPE / CLI:** **Claude-only on BOTH surfaces for BD-226.** Codex and Antigravity have no worktree-parallel isolation yet — **they run SERIALLY** until handled separately in **BD-217**. (The pack-side sub-agent-behavior section is trinity-exempt/Claude-only; the project-side update is likewise Claude-only here.)
  **Out of scope:** Codex/Antigravity worktree parity (BD-217); background-session isolation (BD-218).
  **Pipeline:** a rule/workflow change across operating docs + trinity Pack-memory + project ops docs → architect → planner → coder (per the pack-architect-spawn protocol + skill-agent-maintenance-mechanical: a structural/rule change escalates to architect, not a convenience edit). Architect spawn requires user approval at that time.
  **Acceptance criteria (provisional — refined by design):** the generalized model (rules 1–8) is stated once and re-applied to both surfaces; pack-ops surfaces (trinity `## Pack memory`, PACK-CHAT.md, PACK-AGENTS.md, the mergeback memory) and the project ops docs (PM-chat rules, METHODOLOGY, others) all express it audience-correctly; fix-coders are explicitly forbidden a new worktree (must reuse the commit's worktree); read-only agents are routed to the work's tree (not "in-place"); the patch is produced only after review-clean via SendMessage; orphaned worktrees are explicitly removed after their commit; Claude-only, Codex/Antigravity serial (BD-217); `validate-pack` green; NO client-install/boundary regression.
References: BD-197 (removed the prohibition, enabled opt-in isolation — this BD flips the default); BD-218 (background-session isolation, deferred); BD-217 (Codex/Antigravity worktree parity, deferred — they run serially until then); Pack memory `feedback_worktree_isolation_mergeback_ops` (the merge-back ops this BD revises); BD-221 (the live transition during which this model was developed + refined); `feedback_skill_agent_maintenance_mechanical` (rule changes escalate to architect).
Resolved: n/a
Position: v11.0 — FAST FOLLOW-ON directly after BD-221 (user 2026-06-17). Codifies the agent-execution model (generalized; applied to pack-ops + project ops docs) the team is using live in BD-221.
