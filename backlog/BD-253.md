<!-- per-entry source: /backlog/BD-253.md; contract: /backlog/_rules.md -->
**BD-253 — Project-side resumable session-state mechanism (PM-Chat; reconcile with existing `_index.md` / `STATUS.md` state; same transferability + no-CLI-memory rules as BD-252)**
Type: feat — STRUCTURAL (client-shipped). Surfaced 2026-06-28 (user-directed sibling of BD-252); user-approved open.
Status: Resolved
Target: v11.0 (default per no-deferral-without-user-direction; client-shipped; sequenced after BD-252 — its blocker — in the v11.0 launch queue, i.e. the BD-252/253 pair lands after BD-234, user-confirmed)
Blockers: BD-252 — sequenced AFTER BD-252 (the pack side designs the resumable-snapshot pattern first; the project side adapts it). This is NOT a copy: the project side already ships state mechanisms (`docs/project/implementation-plan/_index.md`, `STATUS.md`, and other project state) that this BD must RECONCILE with, not duplicate.
Unblocks: a client PM-Chat session that is fully transferable + restartable — survives /pm-startup, machine transfer, and CLI transfer (Claude ↔ Codex ↔ Antigravity) — with nothing necessary-to-resume lost.
Problem: the project-side (client) PM-Chat has the SAME live-state resilience gap as Pack Chat (BD-252) — live orchestration state that does not transfer — BUT the project side ALREADY ships partial state mechanisms (the impl-plan `_index.md`, `STATUS.md`, etc.). Those were not designed for full cross-machine/cross-CLI resumability and may lean on CLI memory or volatile context for the live-orchestration layer. This BD brings the project side to the same bar as BD-252 by RECONCILING/EXTENDING the existing mechanisms — not by bolting on a parallel duplicate.
Requirements (SAME firm rules as BD-252, applied to the client surface):
  - Repo-committed (the client's repo); CLI-agnostic / trinity-agnostic; CLI memory FORBIDDEN for state on every CLI.
  - Transferable + restartable across machine + CLI; `/pm-startup` reads + sets the resume state.
  - Pause/resume = RE-SPAWN from the snapshot; last commit = durable boundary.
  - Parallelization mode + queue order captured; current-snapshot-ONLY, never history (history stays in the existing project mechanisms, isolated).
  - Consistency / no-stale enforcement via the shipped client doc-gate (`project-template/scripts/validate-docs.sh`) + `validate-pack` parity checks; nothing may go stale.
Architect-to-decide (design charges):
  - **Reconciliation:** what `_index.md` (impl-plan topological order + membership), `STATUS.md` (the convenience view, already "never source of truth"), and any other existing project state ALREADY cover, vs the live-orchestration gap this BD fills; whether the resumable snapshot is a NEW artifact or an EXTENSION of an existing one — avoid a duplicate/competing SSOT.
  - **State boundary:** what must persist to resume vs what is recreatable (mirrors BD-252's Q2 charge for the client surface).
  - **Immutability model — RULED (user decision, final):** the immutable-file requirement is CLIENT-side ONLY — client projects must be prevented from modifying the immutable `_rules.md` set; pack-side chats and agents can ALWAYS modify them (ongoing pack development requires it). Mechanism: the client manifest is NOT a tracked/shipped file — `scripts/init-project.sh` (fresh + `--update`) and the v10→v11 migrator GENERATE `docs/project/immutable-manifest.txt` at install time by hashing the files actually installed; the shipped `verify-immutable.sh` (mechanism unchanged) checks the client tree against that install-time manifest. Pack side carries ZERO immutability machinery: Check 76 + its test + the tracked `project-template/docs/project/immutable-manifest.txt` are DELETED (fail-loud — every reference corrected in the same change); no freeze, no regen discipline, no version gating ever binds pack development. BD-093's release-cut regen step is removed (nothing to regen). Reference-integrity note stands: Check 68 (pack; `pack-ops/.dangling-ref-allowlist.txt`), Check 43, and the shipped `validate-docs.sh` dangling axis (`project-template/scripts/.docs-gate-allowlist.txt`) all police citations — the qualified-citation discipline from the FINAL design remains in force.
  - **Priority guard (user-stated):** the pack exists to build a framework for OTHER projects — the PROJECT side is the primary goal; pack-side gates/guards are subordinate enablers. Where a pack-side guard interaction adds friction to the client deliverable, prefer simplifying the guard over adding protective complexity to the pack repo.
File/Symbol:
  - `docs/project/pm-session-state.json` — the client-authored resumable snapshot (runtime-authored; NOT shipped; schema-as-contract). Name `pm-session-state.json` is USER-DECIDED (standing): avoids the basename collision with the pack-internal `pack-ops/session-state.json` (Check 43 keys on basenames).
  - `project-template/scripts/validate-docs.sh` — shipped client doc-gate: new `# AXIS: session-state` leg (struct/fresh/grammar) + self-test
  - `scripts/lib/validate_checks/boundary_refs.py` — `_CHECK_70_AXIS_MARKERS` pack-side parity twin (Check 70)
  - `scripts/tests/test-validate-pack-check-70.sh` — the Check-70 count guard (asymmetry-guard-1 third leg; B1)
  - `project-template/scripts/.docs-gate-allowlist.txt` — dangling-axis `target:` record for the runtime-authored artifact (M1)
  - `project-template/skills/pm-startup/SKILL.md` — new Step 3 (read+prime resume frontier) + renumber + report line
  - `project-template/CLAUDE.md` — trinity no-CLI-memory-for-state rule (overwrite-never-append)
  - `project-template/AGENTS.md` — trinity no-CLI-memory-for-state rule (overwrite-never-append)
  - `project-template/GEMINI.md` — trinity no-CLI-memory-for-state rule (overwrite-never-append)
  - `project-template/docs/pack/PM-CHAT.md` — client PM-Chat operational rule (distinctly worded; overwrite-never-append)
  - `project-template/docs/project/implementation-plan/_rules.md` — cross-reference: runtime mode/queue live in the snapshot, not `_index.md`
  - `project-template/docs/project/changelog/_rules.md` — REVIEW only (confirm history stays isolated; read-only likely)
  **Out of scope:** the pack-side mechanism (BD-252); history persistence (existing project mechanisms, kept isolated).
Process: RESTART from the beginning (user-directed; fresh instances at every stage) — docs-researcher (exhaustive enforcement-surface census per the immutability charge above, plus the standard internal census) → architect → adversarial architect → reconcile (if findings) → user design gate → planner → adversarial planner → reconcile (if findings) → user planner-to-coder gate → worktree coder waves + bounded review/fix cycle. User design-gate rulings: OQ-1 = (a) struct+grammar axis depth; immutability = install-time manifest (RULED above); OQ-2 = the `_index.md`-vs-snapshot distinction goes in `implementation-plan/_rules.md` ONLY, per the user's placement principle — a rule specific to ONE per-entry tree goes in that tree's `_rules.md` only; a rule specific to ALL per-entry trees and nothing else goes in every tree's `_rules.md`; a generic rule that merely also applies to per-entry trees goes in a project operations doc, NEVER in `_rules.md` (so trinity/PM-CHAT carry only the generic state rules — no-CLI-memory + the overwrite-never-append operational duty — and do NOT duplicate the `_index.md` distinction). ADDITIONALLY (user-directed): codify the placement principle itself as a STANDING project-side rule — per its own clause #3 it is a generic rule, so its home is a project operations doc (exact surface: planner/coder spec; trinity likely), never a `_rules.md`. OQ-3 = flip the README row to `v11.0 (work)` (cosmetic honesty marker only — nothing gates on it, and it must never re-enter as a correctness lever; BD-093 drops it to bare at the release cut). OQ-4 CLOSED as decided: fold the snapshot read into `/pm-startup` Step 2 + a final-report `Resume:` line (no new step, no renumber). OQ-5 = order (i): BD-253 fully first, BD-245 immediately after — BD-245 adjusts the trinity text after BD-253 finishes (carries the rule through its rename; adopts the `pm-session-state.json` name in its prose); BD-255 Part B drops Check 76 from its audit list when it runs; BD-259 serializes on the shared allowlist after BD-253. An M3-amendment architect pass precedes the planner (complete: `DESIGN-BD253-M3-AMENDMENT.md`). Tracked (2026-07-02, review F4): reconcile `backlog/BD-260.md`'s justification prose — its Provenance/Blockers/Target/Unblocks/Problem/Out-of-scope fields cite the superseded release-state-freeze design (the `work` qualifier deliverable itself stands) — a coder-applied entry edit (MAJOR: multi-field rewrite of a Resolved entry) before BD-253 resolves. SATISFIED pre-resolution (coder-applied, reviewed clean). The prior attempt's designs/plans/reviews/blocker analyses + the reverted patches are archived out-of-repo (`pack-handoff-bd253/archive-attempt-1/`) as EVIDENCE inputs for the fresh pipeline — the agents get the entire picture (both discovered blockers, the standing decisions) but design from scratch. File/Symbol above is the prior attempt's surface hypothesis; the restart pipeline re-derives it.
Resolved: 2026-07-02 — Shipped the client PM-Chat resumable session-state mechanism via the RESTART pipeline (census → architect → adversarial → reconciliation → user rulings + M3 amendment → planner → adversarial → reconciliation → six commits, every review cycle closing CLEAN). Landed: (M3) the RULED immutability model — install-time client-manifest generation in `scripts/init-project.sh` (fresh + `--update`) + the v10→v11 migrator (fixing the latent no-manifest-after-migration gap); pack-side machinery deleted (Check 76 + its test + the tracked manifest; registry 81→80; grep-provable zero-reader invariant); (A) the `# AXIS: session-state` struct+grammar leg in the shipped `validate-docs.sh` (OQ-1a; SKIP-lenient) + the Check-70 three-surface lockstep (5→6) + one carve record per allowlist + the revived `per TD-N`/`per BD-N` narration patterns in both twins (+ test-79 T9); (F) the `_index.md`-vs-snapshot distinction in `implementation-plan/_rules.md` (its ONLY home per OQ-2); (C) the trinity no-CLI-memory-for-state rule + the CODIFIED placement principle ×3 (byte-parity); (E) `/pm-startup` Step-2 snapshot read + the Step-6 `Resume:` line (OQ-4, no renumber); (D) the PM-CHAT operational upkeep rule; plus the BD-260 entry supersession reconciliation and the changelog-`_rules.md` history-isolation confirmation (read-only, no edit needed). README carries the user-owned honest `v11.0 (work)` marker (nothing gates on it; BD-093 drops it at the cut). Verified per commit: validate-pack plain+DEEP, the full sharded battery, shard coverage, install/tamper + axis bite probes; CI green at origin through the full stack. Docs: `DESIGN-BD253-RESTART-FINAL.md` + `DESIGN-BD253-M3-AMENDMENT.md` + `PLAN-BD253-RESTART-FINAL.md`. Forward notes: BD-245 carries the two trinity bullets through its rename + adopts the artifact name; BD-255 Part B drops Check 76 from its audit list; BD-259 serializes on the shared allowlist.
