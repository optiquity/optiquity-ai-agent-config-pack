<!-- per-entry source: /backlog/BD-257.md; contract: /backlog/_rules.md -->
**BD-257 — Add net-new + review/refresh existing client (project-side) slash commands + their execution foundation (brainstorm-gated; project-side analog of BD-224)**
Type: feat — client/project-template CLI UX / command surface. Add net-new client slash commands (shipped via `project-template/skills/`) AND review/update/expand the existing client commands so developers using the pack in their own projects invoke common project workflows easily — INCLUDING the execution foundation each command runs (the skill body's concrete steps + any client-side scripts in `project-template/scripts/`), not merely the command text. Opened 2026-06-30 (user direction). Pipeline: brainstorm/discovery (researcher + architect) → user adjudicates the set → architect design → planner → coder → bounded review/fix.
Status: Resolved
Target: v11.0 — near closeout alongside BD-224 (pack-side sibling), after the surface-changing v11.0 work settles (notably BD-221 Antigravity conversion, which reshapes the client command/skill trinity).
Blockers: SEQUENCING (not technical). Coordinate with BD-224 (pack `/pack-*` surface — SEPARATE artifact, same design window) and BD-221 (client command/skill trinity conversion). Build against the settled client surface; land before BD-205 (final readiness audit).
Unblocks: faster, more discoverable CLIENT project operation — client slash commands for common workflows, each backed by a real, working execution foundation; and an existing client command surface that reflects the current system rather than drifting stale.
File/Symbol (architect refines after the brainstorm freezes the set; provisional surface family):
  - `project-template/skills/<command>/SKILL.md` — the net-new AND refreshed command skills (the execution steps each command runs).
  - `project-template/scripts/` — any client-side scripts the commands invoke (execution foundation; `dependency-direction-placement` — client deliverables default project-side).
  - the per-CLI client command wiring (Claude / Codex / Antigravity command surfaces) + the `project-template/` trinity (`CLAUDE.md` / `AGENTS.md` / `GEMINI.md`) references.
  - the validate-pack skill-consistency checks (e.g. Check 31 skill-cell) that assert the client skills' shape — confirm parity after the additions/refresh.
  - the client-dashboard render-cache surface family (OI1(a), 2026-07-18; foundation/framework — the project-side transplant of the BD-224 render-cache surfaces): a client render skill under `project-template/skills/<name>/SKILL.md`, a client-owned build-spec + a client approvals dir under `project-template/`, and a client-side `project-template/` dangling-ref allowlist token — concrete project-specific assets authored WHEN a client dashboard is actually built under this BD, not now.
Scope note (user decision 2026-07-03, at the BD-189 design gate): whatever BD-189 builds that this BD relies on or wraps — the groupings query scripts, procedures, and help/command-surface presentation — MAY be modified by this BD to add functionality; that modification IS in this BD's scope. No actor may claim it is out of scope or requires a new BD. EXTENDED (user 2026-07-04, BD-189 GQ-6): this authority explicitly covers reworking the two BD-189-landed `HELP-FRAGMENT.md` groupings rows and anything related, unconstrained by arbitrary rules or test pins; scope objections against that rework are pre-overruled.
Scope expansion (user decision 2026-07-18, OI1(a) — at the BD-224 render-cache design gate): this BD is CONFIRMED to cover ALL project-side slash commands — the project-side analogs of the pack `/pack-*` set — INCLUDING a CLIENT DASHBOARD slash command (the project-side analog of `/pack-dashboard`). The client dashboard adopts the SAME render-cache design foundation ratified for BD-224 — a git-tracked, spec-fingerprinted, state-independent HTML shell REUSED on a content-hash match and REGENERATED on spec change, plus live-state payload thinning (FULL in-window records, MINIMAL out-of-window) — adapted to project-side pipelines and elements: TD entries + phases in place of BD-224's BD entries + waves, and a client-owned build-spec + client approvals dir + client-side dangling-ref allowlist token in place of the pack-side surfaces, with the client shipping its OWN copy of the generic render skill (`dependency-direction-placement` — no pack-side file is a runtime dependency of the client render). This expansion is a FOUNDATION / FRAMEWORK scope ONLY: it fixes the parity requirement and the transplant surface map; it does NOT author any project-specific dashboard assets (those are unknown/unpredictable until a concrete client project is in view, and are built WHEN a client dashboard is actually implemented under this BD). Status stays Open.
Stage-1 adjudicated + design/plan approved (user, 2026-07-24): brainstorm set FROZEN — 10 client `/pm-*` commands (the pack `/pack-*` mirror + `/pm-groupings` + `/pm-target-sweep`). Implementation = 7 commits `A1→B1→B2→B4→B3→C1→C2` (design + plan adversarially reviewed + reconciled). Load-bearing decisions anchored here (`deferred-work-tracked-anchor`): OI-B EMPTIES the pack→client ship-allowlist — commit **A1** sets `_SANCTIONED_PACK_SIDE_SHIPPED = ()`, de-ships `pack-help.sh` + `detect.sh`, reconciles the Check-47 code/comments to the strengthened `dependency-direction-placement` (c) hard line, AND adds a Check-47 empty-invariant assertion so any regrowth FAILS CI (the code-enforced backing of the (c) rule). The strengthened (c) `dependency-direction-placement` rule lands SEPARATELY first (Seq-1); A1 owns the matching CODE reconciliation. OI-A ships BOTH isolation values (client `full` opt-in addendum); `/pack-help` → `/pm-help` gets its OWN script + fragment (no dual-use). Full design + plan live in the paired `maintenance-docs/v11-implementation/` records landed with the BD-257 waves.
Description:
  **Problem:** the client slash-command surface (`project-template/skills/`) covers language/framework/domain skills but has gaps for common PROJECT-WORKFLOW commands a developer would want, and some existing client commands (incl. help/startup) may have drifted from the current system. BD-224 addresses the PACK-side `/pack-*` surface; this BD is its project-side analog — the client surface is a SEPARATE artifact (`pack-project-separation-of-concerns`) needing its own scoping. Per the 2026-07-18 scope expansion (above), that project-side set explicitly INCLUDES a client dashboard slash command (the analog of `/pack-dashboard`) built on the BD-224 render-cache foundation (spec-fingerprinted shell reuse + payload thinning), as a foundation/framework parity requirement adapted to project-side pipelines/elements (TD entries + phases in place of BD entries + waves).
  **Execution-foundation requirement (the core of this BD):** adding OR updating a client slash command is NOT merely adding/editing command text in a skill/doc/config. Each net-new command REQUIRES building its full execution foundation — the skill body (`project-template/skills/<name>/SKILL.md`) with the concrete steps it runs, PLUS any client-side scripts/actions it invokes (default home `project-template/scripts/`). A command with no working execution foundation is not shippable.
  **Existing-command refresh (IN SCOPE):** review, update, and where useful EXPAND the EXISTING client slash commands (the shipped `project-template/skills/` set, including help/startup) to reflect the current client-project state — correct stale steps/outdated references, close coverage gaps, and refresh each command's execution foundation where it drifted from the shipped scripts/flows. The shipped command surface must be accurate, not only the net-new additions.
  **Stage 1 — BRAINSTORM / DISCOVERY (decision gate; user adjudicates the set):** researcher inventories the current client command/skill surface (`project-template/skills/` + per-CLI wiring) + surveys client-workflow command patterns; architect brainstorms candidate net-new client commands AND flags existing commands needing refresh, each with the workflow it serves AND the execution foundation it needs. Pack Chat surfaces the menu; user freezes the set. NO design/impl until frozen. (Mirrors BD-224 / BD-221 Phase-0.)
  **Stage 2 — design → plan → implement** the chosen net-new commands + the existing-command refresh + their execution foundation, under the trinity rule (cross-CLI parity across the client's Claude/Codex/Antigravity surfaces per BD-221) and the skill `x-` contract (`skill-agent-maintenance-mechanical`).
  **Out of scope:** the pack-side `/pack-*` surface (BD-224 — separate artifact); the BD-221 conversion itself.
  **Hard constraints:** each new command ships its execution foundation (skill body + scripts), not just text; existing client commands (incl. help/startup) reviewed + brought current, not just net-new commands added; trinity/cross-CLI parity for every command; no boundary leak between client and pack `/pack-*` surfaces (`pack-project-separation-of-concerns`); client scripts default to `project-template/scripts/` (`dependency-direction-placement`); brainstorm set FROZEN before Stage-2; designed against the settled client surface.
  **Acceptance criteria (provisional — refined by design):** a user-adjudicated set of net-new client slash commands, each with a working execution foundation; existing client commands reviewed/updated to current state (stale steps/references corrected, execution foundation refreshed) with help/coverage expanded; the client-dashboard render-cache FOUNDATION (spec-fingerprinted shell reuse + payload thinning, TD/phases analog) is in scope as a framework parity requirement, with NO project-specific dashboard assets authored until a concrete client dashboard is built under this BD; cross-CLI parity; `validate-pack` green; full CI battery green; no client-install/boundary regression.
References: BD-224 (pack-side command-surface sibling — coordinate, separate artifact); BD-221 (client command/skill trinity conversion — build after it settles); BD-205 (final readiness audit — land before it); `project-template/skills/` (the client command surface); the trinity + `skill-agent-maintenance-mechanical` + `pack-project-separation-of-concerns` + `dependency-direction-placement` rules.
Resolved: 2026-07-28 — all 10 client `/pm-*` commands + execution foundations shipped across the 7 planned waves, each reviewed CLEAN via the bounded cycle: A1 (`/pack-help`→`/pm-help` client separation with its own script/fragment + de-ship `pack-help.sh`/`detect.sh` + `_SANCTIONED_PACK_SIDE_SHIPPED = ()` + Check-47 empty-invariant assertion, the code-enforced backing of the strengthened `dependency-direction-placement` (c) rule); B1 (client mode subsystem — review/intervention/isolation — + 2 FLAT client enforcement hooks + canaries + `PM-OPERATING-MODES.md`); B2 (`/pm-status` + `/pm-refresh`); B4 (`/pm-groupings` + `/pm-target-sweep` promotions); B3 (`/pm-dashboard` schema-retargeted render-cache framework + spec + engine test); C1 (HELP-FRAGMENT 8 `/pm-*` rows + Claude-only-enforcement markers + `pm-startup` Modes readiness step); C2 (Check 90 client `/pm-*`↔skill parity + registry count 86→87 + test + README). ALSO fixed a language-detection regression B3 introduced (the 3 shipped client PM `.py` scripts tripped the weak-evidence detectors in `init-project.sh` + `detect.sh`, mis-detecting python on every non-python client) via an anchored `scripts/` exclusion + a folded-in dot-path anchoring, and regenerated the v11 fixture manifest. Full deterministic LARGE-BD pipeline. CI green through the detection fix.
Position: v11.0 — near closeout alongside BD-224, after BD-221 settles, before BD-205. Brainstorm-gated. The project-side analog completing "both surfaces" slash-command coverage (net-new + execution foundation + existing-command refresh).
