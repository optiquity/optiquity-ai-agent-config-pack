<!-- per-entry source: /backlog/BD-255.md; contract: /backlog/_rules.md -->
**BD-255 — Systemic multi-surface-consistency defect-class fix: doc↔constant drift prevention + declare-verify hardening + cross-BD collision detection + memory/session-state quartet verification (single pack+project BD)**
Type: feat — STRUCTURAL root-cause class fix (new CI check(s) + new convention + new pipeline rule + count bump; cross-surface pack + project). THE single consolidated fix BD for the class — no separate umbrella / fix BD (user-directed 2026-06-29: "the fix is BD-255; I don't need a million fix BDs; whack-a-mole is forbidden"). Subsumes the prior narrow "quartet seamlessness verification" draft (now Part D) plus the systemic prevention the BD-253↔BD-245 collision + the live Check-36 RED exposed.
Status: Open
Target: v11.0 — launch-blocking. A live Check-36 RED sits on HEAD now (instance 1), and the defect class threatens the v11.0 surface; per no-deferral-without-user-direction the full fix targets v11.0.
Blockers: split by part. **Parts A/B/C (class PREVENTION) have NO blockers** — they should land EARLY in the v11.0 tail, BEFORE BD-232/245/253's remaining pipelines run, so Part C's scan governs them (highest-value timing). **Part D (quartet SEAMLESSNESS verification) is gated** on BD-232 + BD-252 + BD-245 + BD-253 all landing (BD-252 Resolved; BD-232/245/253 later in the tail). So the BD lands in two logical stages within one entry: prevention-mechanism first (unblocked), quartet-verification last (after the four land). Serializes on `scripts/validate-pack.py` with any other open check-adding BD (re-measure live status at plan time — BD-250 is Resolved, NOT a constraint).
Unblocks: the multi-surface-consistency defect CLASS is closed by construction (not whacked per-instance): every current + future doc↔constant twin co-varies; every declare-style check verifies its load-bearing backing; cross-BD shared-surface collisions surface at design time, not adversary time. Plus the concrete confidence that the pack-side (BD-232 + BD-252) and project-side (BD-245 + BD-253) memory/session-state work integrates seamlessly across all three trinity CLIs.
Root cause (the class, one sentence): **multi-surface representation without a co-variance guarantee** — a single piece of truth (a permitted-set, a section name, an install mapping, a content baseline, an edit-surface ownership) is encoded on ≥2 surfaces with nothing FORCING them to change together, so drift is the default outcome of any one-surface edit and review catches it only reactively/late. Three structurally distinct sub-types (A/B/C below). Four measured instances: (1) pack-chat-only doc↔constant RED [live]; (2) Check-41 install-row declare-without-copy-site [point-fixed, BD-246]; (3) BD-253↔BD-245 cross-BD collision [missed until adversary]; (4) the project-side parallels. NOT a new class to the repo — it is PARTIALLY solved (Checks 45/52/47/70 are the proven set-equality answer applied per-twin = itself slow whack-a-mole); this BD generalizes the answer so coverage is complete-by-construction and adds the first structural answer for sub-type C.
Scope (single BD — pack + project verified/fixed TOGETHER, NOT split, per user direction):
  - **Part A — doc↔constant drift prevention (sub-type A: representation twin).** A generic doc↔constant twin-sync mechanism (the Check-45/52 set-equality idiom generalized) driven by a measure-then-bound registry, + COLLAPSE (derive one surface) where clean (e.g. A5 `_CHECK_66_BULLET_SURFACE` reads the trinity heading so BD-245's `## Project memory`→`## Project rules` rename auto-propagates). Resolves the live Check-36 RED (add `pack-ops/session-state.json` to `_PACK_CHAT_ONLY_PERMITTED_PATHS`) AS the mechanism's first row — content correction + structural guarantee land together, never a bare patch. The exhaustive re-census (F1) + the completeness meta-check (F2) are part of this scope (see Reconciliation targets).
  - **Part B — declare-verify hardening (sub-type B: declaration with unverified backing).** A design-time rule that any check RECORDING a mapping (install row, wiring claim, surface↔surface link) MUST verify the LOAD-BEARING reality (ships / is reachable / bites), not a necessary-but-insufficient property (target exists); + a one-time meta-audit of all record-style checks. Sharpen `ci-guard-measure-then-bound`; name the enforcement seam (F7). No new runtime check (no uniform backing signal across heterogeneous declarations).
  - **Part C — cross-BD collision detection (sub-type C: concurrent edit, no lock).** A mandatory design-time open-backlog shared-surface scan (with an Empirical-Evidence Block) keyed on a STRUCTURED surface set, not the free-text `File/Symbol` field (F4) — re-based on the researcher blast-radius set or gated on a structured-field prerequisite so it actually catches the instance-3 shape; + an optional cheap advisory CI backstop. Pack + project symmetric (the client receives the scan as a documented workflow step, not the pack check — pack-side-project-concepts-deliverable-only).
  - **Part D — memory/session-state quartet seamlessness verification.** The original BD-255 charter: a one-time cross-family audit that BD-232 (pack memory-cache reorg) + BD-252 (pack session-state) + BD-245 (project per-CLI-memory strip/rename) + BD-253 (project session-state) integrate cleanly — and the FIRST real consumer/proof of the Part A/C mechanisms on a real multi-BD family. Concrete checks:
    - BD-245's `## Project memory`→`## Project rules` rename cleanly carried BD-253's session-state/overwrite rule (no orphan, duplicate, or stale section ref outside the forbidden-feature context).
    - BD-253's `validate-docs.sh` `# AXIS: session-state` edit + BD-245's rename/bloat-key edit coexist; `validate-docs.sh --self-test` green; neither clobbered the other.
    - The pack `## Pack memory` carries BD-252's overwrite rule + BD-232's memory-reorg meta-rules without contradiction (BD-232's "save state in a doc, never memory" points at BD-252's snapshot).
    - The pack `pack-ops/session-state.json` and project `docs/project/session-state.json` snapshots + their checks interoperate, create NO competing SSOT (with each other or `_index.md`/`STATUS.md`), and the CLI-memory-forbidden-for-state rule is symmetric + consistent pack↔project and across Claude/Codex/Antigravity.
    - grep-zero on orphaned section names / dangling references across all four BDs' surfaces; reconcile any seam the audit finds (Part D MAY edit, not just report).
  - **Resolves the live Check-36 RED** (HEAD's `pack-chat-only` commit touches `session-state.json` ∉ the constant) as Part A's first application — not as a standalone fix.
Reconciliation resolution (RESOLVED 2026-06-29): the systemic design was reconciled in DESIGN-RECONCILED.md and APPROVED at the user design gate — D1/D2/D3 as recommended, D4 = fold the live Check-36 RED into Part A, D7 = land all parts in v11.0 (no deferral). F1–F8 resolved as recorded below (kept for the planner):
  - **F1 (MUST):** the §2 census is INCOMPLETE — it missed `_CHECK_56_CANONICAL_VERBS` ↔ the trinity `agents-never-commit` verb denylist, ALREADY divergent (trinity lists `fetch` at CLAUDE.md:181; constant does not; Check 56 stays green because it is ONE-WAY constant→doc). Re-run the census over the FULL `_CHECK_NN` constant population (~120); classify each as doc-twin/not and bijection/one-way/unguarded; audit A2/A3/A4/A6 individually (do not assert "proven structural" en bloc); size Part A to the COMPLETE re-census; fold the `fetch` reconciliation in.
  - **F2 (MUST):** the twin registry is opt-in (hand-registered = same drift surface it replaces). Add a completeness meta-check (the Check-69/Check-59 registry-completeness idiom) that flags an unregistered doc-mirror constant, OR justify opt-in against the no-whack-a-mole mandate.
  - **F4 (MUST):** Part C's load-bearing scan cannot catch instance-3 keyed on `File/Symbol` (it was `TBD`/prose for BD-253/BD-245 at design time). Require a structured surface field as a prerequisite, or re-base the scan on the researcher blast-radius set; re-derive that it flags BD-253↔BD-245 (`validate-docs.sh` + the trinity section).
  - **F3 (SHOULD):** A-ii creates a 3-surface twin (prose | fenced | constant) with the prose↔fenced pair unguarded. Prefer collapse for A1, or add a prose↔fenced assertion.
  - **F5 (SHOULD):** the C census mischaracterizes BD-252↔BD-232 as a "caught" shared-surface case — BD-232 is out-of-repo-only (BD-232.md:7), so the empirical record is 0-for-1 on REAL collisions. Re-label; re-argue C recall on the real (missed) BD-253↔BD-245 instance + the actual blast-radius mechanism that caught it.
  - **F6 (SHOULD):** stale sequencing — BD-250 is Resolved, not pending. Remove the BD-250 serialization constraint; re-derive the validate-pack.py serialization risk from the live backlog.
  - **F7 (NIT):** name Part B's enforcement seam — a tagged pack-memory rule (`[roles: architect coder]`) so the spawn-prompt assembler selects it + it is Rules-Applied-verified.
  - **F8 (NIT):** registry max-number is 79 (count 77); the next check is numbered 80, not 78 — note it so a reviewer does not read a header collision.
File/Symbol: union, definitive set produced by the reconciled design + planner — `scripts/validate-pack.py` (new check(s) + `CHECK_REGISTRY_EXPECTED_COUNT` bump + per-check test(s) + the registry/meta-check + the Part A registry + Check-56 `fetch` reconciliation + A5 collapse); `pack-ops/PACK-AGENTS.md` (machine-readable fenced region for A1 if A-ii kept); the pack trinity `## Pack memory` + `pack-ops/PACK-CHAT.md` + `PACK-MEMORY-RATIONALE.md` (Part B/C rules + rationale bijection); the per-CLI client deliverables (`project-template/scripts/validate-docs.sh`, PM-CHAT/methodology) for the project-side analogs; the large-BD pipeline rule surfaces for Part C's mandatory scan; the quartet's union surfaces for Part D.
Process: size-tiered large-BD standard — LARGE (launch-gate: live RED + launch-quality; ≥2 signals: cross-surface pack+project, structural [new check + new convention + new pipeline rule + count bump]). Design = the systemic `DESIGN.md` (architect) + `ADVERSARIAL-ARCHITECT-REVIEW.md` (NEEDS-REWORK; F1–F8) + a FRESH reconciliation pass (`DESIGN-RECONCILED.md`; F1–F8 resolved) → user design gate [PASSED 2026-06-29] → planner (next) → adversarial planner → reconcile → user planner-to-coder gate → coder waves + bounded review/fix cycle. Parts map to parallelizable-vs-serial waves (Part A serial on validate-pack.py; Part B doc/memory-only parallel; Part C partly memory-only parallel + partly validate-pack.py serial). Part D is a final cross-family audit pass that runs after BD-232/252/245/253 land. The C1-class untracked-new-file blindspot applies to new per-check test files: stage-then-validate at merge-back.
  **Out of scope:** re-implementing BD-232/252/245/253's core work (Part D VERIFIES + seam-fixes their integration, does not rebuild them); a separate umbrella / fix BD of any kind (explicitly forbidden — this IS the single consolidated fix BD); reopening BD-252 (it surfaced instance 1; this fixes the twin it exposed and prevents recurrence — a backward note names BD-252 as the surfacing case, audit-trail in this entry only, per operating-docs-no-history).
Resolved: n/a
Position: v11.0 — Parts A/B/C land EARLY in the remaining v11.0 tail (before BD-232/245/253 resume, so Part C's scan governs them); Part D runs after BD-232/252/245/253 all land (slot after BD-245, before the BD-205 broad final audit). One BD, two sequencing stages.
