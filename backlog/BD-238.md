<!-- per-entry source: /backlog/BD-238.md; contract: /backlog/_rules.md -->
**BD-238 — Codify the PACK-SIDE large-BD development pipeline as an official, size-tiered standard (optional researchers → architect → adversarial architect → planner → adversarial planner, + reconciliation rounds + parallel worktree coder waves)**
Type: chore — process / agent-development-lifecycle codification (PACK-SIDE). Promote the rigorous large-BD flow (run in practice, only partially + scatter-documented) into ONE official in-repo standard with size-tiering. Architect-first (it touches rules + operating docs + trinity) → planner → coder.
Status: Resolved
Target: v11.0 — small process-doc codification; timely while BD-206 runs the flow; NOT a launch blocker. (v11.0 confirmed by user 2026-06-20.) RE-SEQUENCED (user 2026-06-23): now LEADS the v11.0 queue — runs FIRST, ahead of BD-206 (order: BD-238 → BD-239 → BD-242 → BD-206 → …).
Blockers: none — independent of repo state. Sequence at user discretion (does not block BD-206, which already runs the flow ad hoc).
Unblocks: BD-239 — the PROJECT-SIDE companion standard re-applies this pipeline to the project's phase-based lifecycle + the project's own agents, and DEPENDS ON this entry being Resolved first.
Provenance: user direction 2026-06-20 during BD-206 pipeline planning — the user wants the architect → adversarial architect → planner → adversarial planner flow (+ an optional researcher first step, + reconciliation rounds, + parallel worktree coder waves) as a DEFAULT STANDARD for large BDs, with the adversarial reviews + reconciliation OPTIONAL for smaller BDs. Worked precedent: BD-237 ran the full flow end-to-end; BD-206 is running it.
File/Symbol (measured 2026-06-20, HEAD `62805a8` — the flow is SCATTERED + the adversarial stages are UNDOCUMENTED in-repo):
  - `grep -rln adversarial CLAUDE.md AGENTS.md GEMINI.md pack-ops/*.md supporting-docs/*.md` → NO matches. "adversarial" lives ONLY in two OUT-OF-REPO Pack-Chat memory files (`feedback_adversarial_architect_review_on_major_gap`, `feedback_adversarial_planner_review_major_plans`), framed SITUATIONALLY ("on a major gap" / "major plans"), NOT as a documented standard tier.
  - The BASE pipeline IS in trinity `## Pack memory`: "Researcher-first pipeline" (`pack-docs-researcher` → `pack-architect` → `pack-planner` → `pack-coder`), the "Pack-architect spawn protocol", "Planner output → user review → coder spawn" (planner-to-coder gate); the worktree parallelization (rule 10) + bounded review/fix cycle + merge-back/conflict protocol are in trinity + `pack-ops/PACK-CHAT.md`.
  - GAP: no consolidated, size-tiered "large-BD standard pipeline" chaining the optional researchers → architect → adversarial architect → [reconciliation] → planner → adversarial planner → [reconciliation] → parallel coder waves; and no large-vs-small tiering rule.
Description:
  **Problem:** the rigorous large-BD development flow (BD-237 ran it end-to-end; BD-206 is running it) is not codified as an official standard — its adversarial-review + reconciliation stages exist only as out-of-repo memories (situational), and the full chain + the size-tiering are uncodified, so a fresh session/agent cannot rely on the standard.
  **Goal:** codify the PACK-SIDE flow as the DEFAULT STANDARD for LARGE BDs, with the two adversarial reviews + reconciliation OPTIONAL (user-elective) for SMALLER BDs.
  **The standard (full flow, with options):**
    1. **Optional researcher set — FIRST step, before the first architect.** Zero, one, or both of: an **INTERNAL** researcher (repo/codebase inventory + blast-radius census) and an **EXTERNAL** researcher (CLI / tool / framework / API docs verification against authoritative sources). "May be needed" — invoked per the BD; optional-by-need at ANY size.
    2. **Architect** → design, including the REQUIRED rule-10 parallel/dependency map.
    3. **Adversarial architect review** (fresh, clean-context instance) → Pack-Chat triage of findings → **[reconciliation architect — only if the review returns NEEDS-REWORK]**; loop until READY.
    4. **User design review** (the design gate).
    5. **Planner** → coder-ready plan, including its OWN rule-10 parallel/dependency map.
    6. **Adversarial planner review** (fresh instance) → triage → **[reconciliation planner — only if NEEDS-REWORK]**.
    7. **User planner-to-coder gate.**
    8. **Parallel worktree coder waves** — scheduled off the rule-10 map: disjoint-file commits run as concurrent coders, each in its own isolated worktree; same-file ⇒ serialize. Each commit's bounded review/fix cycle (≤2 review/fix pairs + 1 final) runs IN its worktree; the patch is produced only after review-clean; patches are applied to the canonical tree SEQUENTIALLY (atomic per patch) with the conflict protocol (STOP + re-spawn fresh, never hand-merge); commits land with user approval. Superseded design/plan docs are DELETED as the pipeline iterates (anti-contamination); the audit set is preserved into `maintenance-docs/v<major>-implementation/` (Constraint 3).
    The two adversarial passes are the MINIMUM for a large BD; ADDITIONAL architect/planner rounds are added when larger gaps are found.
  **Size-tiering (pack uses BDs):** **LARGE BD** = the full pipeline by default (the two adversarial reviews + reconciliation as the minimum). **SMALL BD** = the base flow (optional researcher → architect → planner → coder + the bounded review/fix cycle) with the two adversarial reviews + reconciliation OPTIONAL at user election. The architect defines the large-vs-small criterion measure-then-bound (e.g. launch-gate / cross-surface / blast-radius / structural — concrete, not a vibe).
  **Where it lives:** trinity `## Pack memory` is the SSOT, propagated per the "Keeping CLAUDE.md, AGENTS.md, GEMINI.md, and PACK-AGENTS.md current" rule-change procedure (corpus ×3 trinity + `[rationale:]` + `PACK-MEMORY-RATIONALE.md` bijection + `.spawn-rule-manifest.txt` + reference surfaces); + a consolidated lifecycle section in `pack-ops/PACK-CHAT.md` / `pack-ops/PACK-AGENTS.md` as the architect designs. The two existing OUT-OF-REPO adversarial memories are reconciled to point at the in-repo standard (trinity-wins).
  **Process:** architect-first per the pack-architect-spawn-protocol (touches rules + operating docs + trinity) → planner → coder; per the very standard it documents, this BD MAY itself warrant the adversarial passes if the architect deems it large.
Acceptance criteria: the pack-side large-BD pipeline is documented as ONE official, size-tiered standard (the full chain incl. the optional internal/external/both researcher first step + the large/small-BD criterion + adversarial-as-minimum-for-large / optional-for-small + reconciliation rounds + parallel-worktree coder waves keyed off the rule-10 map); it lives in the in-repo SSOT (trinity `## Pack memory` + the full propagation surfaces), NOT just out-of-repo memory; the two existing adversarial memories are reconciled to point at the in-repo standard (no contradiction, trinity-wins); no conflict with the existing researcher-first / spawn-protocol / planner-to-coder / worktree-isolation rules; `validate-pack` green (C3 rule↔rationale bijection, anti-restate, trinity parity, spawn-rule manifest); architect-designed (not Pack-Chat-authored).
References: BD-239 (PROJECT-SIDE companion standard — depends on this entry); out-of-repo memories `feedback_adversarial_architect_review_on_major_gap` + `feedback_adversarial_planner_review_major_plans` (to consolidate in-repo); trinity `## Pack memory` "Researcher-first pipeline" / "Pack-architect spawn protocol" / "Planner output → user review → coder spawn" / "Sub-agent behavior (Claude-only)" worktree rules (incl. rule 10) / "Keeping CLAUDE.md… current"; `pack-ops/PACK-CHAT.md` § "In-session sub-agent spawn + merge-back (worktree isolation)" (Parallelization map + Conflict protocol); BD-237 (worked end-to-end example) + BD-226 (worktree-isolation model the coder waves use); BD-206 (currently running the flow).
Resolved: 2026-06-23 — Codified the pack-side large-BD pipeline as ONE official, size-tiered standard: a new `large-bd-pipeline-standard` umbrella rule in trinity `## Pack memory` (byte-identical ×3, 1289 chars, under the Check-66 cap) + a `PACK-MEMORY-RATIONALE.md` section + a `.spawn-rule-manifest.txt` record + PACK-CHAT/PACK-AGENTS reference pointers. The umbrella REFERENCES (never re-authors) the 5 existing pipeline rules (researcher-first, planner-to-coder gate, reconciliation-instance-independence, pack-architect-spawn-protocol, bounded review/fix cycle); the execution half stays in `PACK-CHAT.md`. Size-tiering: a BD is LARGE (the two adversarial reviews + reconciliation are the MINIMUM) iff launch-gate fires alone OR ≥2 of {launch-gate, cross-surface, blast-radius, structural} fire; else the base flow (adversarial passes OPTIONAL at user election) — validated against precedent: BD-244=SMALL, BD-243=LARGE. A pack-root `## Pack memory` body-parity CI check was evaluated and DROPPED on measure-then-bound evidence (a correct check would ADD a divergence-allowlist discipline surface rather than remove one; pack-internal non-shipping surface; 23/26 rules already byte-identical, 0 drift) — NOT deferred; the per-edit SAFEGUARD-1 (planner byte-parity diff) + SAFEGUARD-2 (coder PREFLIGHT ×3 attestation) are the sufficient protection; registry count stays 69. Full large-BD pipeline run on itself: docs-researcher (location census) → architect → adversarial architect (NEEDS-REWORK, 2 major) → fresh reconciliation → parity-check architect (NOT-NECESSARY verdict) → user design gate → planner → adversarial planner (READY, 3 minor) → coder → reviewer (CLEAN) → C3 archival (coder + reviewer CLEAN). Commits `3d1cf34` (C1 standard, 7 files) + `d720873` (C3 archive 9 audit docs + §10.1 defer→dropped). C2: the 2 out-of-repo adversarial memories DELETED (their rule migrated in-repo; user 2026-06-23). validate-pack default+DEEP + full battery 84/84 green; `Validate Pack` CI green (run 28059201792). v11.0.
Position: v11.0 — pack-side process-standard codification; surfaced during BD-206 planning (user 2026-06-20). Not a launch blocker. SEQUENCED FIRST in v11.0 (ahead of BD-206) per user 2026-06-23 — the standard lands before BD-206's coder waves.
Note (2026-06-23, user direction): BD-238 IS a large BD → it runs the FULL large-BD pipeline (researcher(s) → architect → adversarial architect → reconciliation → planner → adversarial planner → reconciliation → parallel worktree coder waves), not the base/small flow. SUPERSEDES the "this BD MAY itself warrant the adversarial passes if the architect deems it large" hedge in Description above. Companion BD-239 is also large and uses BD-238 as a STARTING POINT, not a ceiling — see BD-239's 2026-06-23 note.
