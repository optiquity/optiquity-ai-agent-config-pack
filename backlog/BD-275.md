<!-- per-entry source: /backlog/BD-275.md; contract: /backlog/_rules.md -->
**BD-275 — test-v11-realistic-ot.sh C.1 couples the fixture test to the whole validate-pack exit code (repo-state flake; not order-dependent)**
Type: chore — pack-dev test hardening (PACK-OPS; test infra only, NOT client-shipped). Assertion C.1 in `scripts/tests/fixture-dependent/test-v11-realistic-ot.sh` judges the test on the GLOBAL exit code of the whole `validate-pack.py` battery (invoked with no check selection), coupling this fixture-integration test to repo state orthogonal to the fixture — the HEAD-driven Check 36 and every live-working-tree content check. When the tree is mid-work/dirty, C.1 flips to FAIL while the fixture assertions (Groups A/B) stay green: a repo-STATE dependency surfaced through an over-broad assertion, NOT a test-order / shared-`test-fixtures/` dependency.
Status: Resolved
Blockers: none — discharged. The "reproduce-in-fresh-clone first" blocker is met: two independent fresh-worktree runs (HEADs 04351ef, 691ac71) reproduced non-flake at a clean HEAD, and the C.1 coupling was reproduced LIVE via the `PACK_CHECK_36_RANGE` env lever (no commit) — 32/33 with C.1 the sole FAIL while Groups A/B stay green.
Unblocks: a clean serial local full-suite signal — no spurious flake masking a real regression (as nearly happened when the BD-257 detection regression was first mis-attributed to test flakiness).
Position: pack-dev test hardening; independent. Sequence AFTER the BD-257 detection fix + the BD-274 client waves (not blocking them).
File/Symbol:
  - `scripts/tests/fixture-dependent/test-v11-realistic-ot.sh` — the Group C `C.1` assertion. Replace the global `assert_eq "…" "0" "$VALIDATOR_RC"` (whole-battery exit-0 judgment) with a SCOPED assertion that the captured Check 32′/33/34 banner-delimited sections report no `FAIL`; keep running `validate-pack` once for its output but judge only the three checks the test owns.
Description: C.1 asserts the ENTIRE `validate-pack.py` battery exits 0. That battery validates the PACK (REPO_ROOT from `__file__`), not the fixture, and includes checks driven by repo state unrelated to the fixture: Check 36 reads `git log … HEAD`, and every content check reads the live working tree. So a mid-work HEAD (scope-keyword mismatch) or a dirty tree turns the battery red → C.1 FAIL while Groups A/B (the fixture) stay green. The original hypothesis — a preceding fixture-shard test mutating shared `test-fixtures/` and flipping a manifest/fixture check (61/62) — is REFUTED: Check 62 is a structural screen (row count / names / 40-hex shape, no SHA-correctness), Check 61 is a location screen, neither reads per-run mutable state; two serial runs (119 predecessors + a high-risk subset) showed zero tree mutation and the target green at both first and last position. Fix: decouple C.1 — assert only that the Check 32′/33/34 sections carry no FAIL, retaining the owned-check tripwire while dropping the coupling to unrelated repo state.
  **Out of scope:** the product (the v11 realistic-OT migration itself is correct); the systemic mktemp portability class (BD-276); the BD-257 detection regression (a BD-257 fix); the `tracker-migrate-forward-test.sh` fake-sleep real-tree-write hygiene (a SEPARATE test-hygiene fix, not this BD).
Resolved: 2026-07-31 — root cause corrected to C.1's coupling to the whole `validate-pack` exit code (repo-state), NOT test order / shared `test-fixtures/` state (original premise refuted by two independent fresh-worktree reproductions + a live `PACK_CHECK_36_RANGE` coupling demo). C.1 replaced with a scoped "no FAIL in the Check 32′/33/34 sections" assertion in `test-v11-realistic-ot.sh`, decoupling the test from Check 36 and every unrelated content check while retaining the owned-check tripwire; test stays 33/33.
