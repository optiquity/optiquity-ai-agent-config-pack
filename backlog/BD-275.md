<!-- per-entry source: /backlog/BD-275.md; contract: /backlog/_rules.md -->
**BD-275 — Order-dependent test-v11-realistic-ot.sh (serial-suite flake; passes isolated)**
Type: chore — pack-dev test hardening (PACK-OPS; test infra only, NOT client-shipped). `scripts/tests/test-v11-realistic-ot.sh` fails in a serial full-suite run but passes in CI's isolated per-shard runs — an order / shared-state dependency, not a product bug.
Status: Open
Blockers: REPRODUCTION-blocked (not technical): the test's built fixture is gitignored/absent in an isolated worktree, so the flake is not reproducible without a fresh-clone build. Reproduce-in-fresh-clone precedes the fix.
Unblocks: a clean serial local full-suite signal — no spurious flake masking a real regression (as nearly happened when the BD-257 detection regression was first mis-attributed to test flakiness).
Position: pack-dev test hardening; independent. Sequence AFTER the BD-257 detection fix + the BD-274 client waves (not blocking them).
File/Symbol (architect/coder refines after reproduction; provisional):
  - `scripts/tests/test-v11-realistic-ot.sh` — the order-dependent test.
  - `test-fixtures/` + the shard's whole-pack `validate-pack.py` (Checks 61/62 manifest/fixture) — the suspected shared-state vector: a preceding fixture-shard test may mutate `test-fixtures/`, flipping a manifest/fixture check that this test then reads.
Description: In a serial 100+-test full-suite run, `test-v11-realistic-ot.sh` fails; in CI's isolated per-shard runs it passes. The shard invokes each test via `bash "$t"` (subshell — env cannot leak between tests), so the only cross-test vector is the shared pack tree / `test-fixtures/` that a whole-pack check reads. Hypothesis (UNVERIFIED — needs a fresh-clone reproduce): a preceding fixture-mutating test leaves `test-fixtures/` in a state that flips a manifest/fixture check (61/62) this test then reads. The fix requires reproduce-in-fresh-clone FIRST (the fixture is gitignored), then either isolate the shared state or add a per-test reset.
  **Out of scope:** the product (the v11 realistic-OT migration itself is correct); the systemic mktemp portability class (BD-276); the BD-257 detection regression (a BD-257 fix).
Resolved:
