<!-- per-entry source: /backlog/BD-247.md; contract: /backlog/_rules.md -->
**BD-247 — Pack-side backlog form-family compliance + drift-prevention workflows (real pack/project symmetry)**
Type: feat — surfaced 2026-06-25 from the BD-206 design-gate G-5 discussion; user-approved open
Status: Open
Target: v11.1
Blockers: BD-206 — adopts the project-side form-family + per-entry + conformance-enforcement model that BD-206 establishes; the pack backlog is brought into the SAME compliance afterward.
Unblocks: real pack/project symmetry. Until the pack backlog tree is form-family-compliant + drift-enforced (the same as the project side BD-206 delivers), there is NO genuine pack/project symmetry — this is why G-5 ruled the "bold-pair-for-symmetry" argument moot.
File/Symbol:
  - The pack `/backlog/` per-entry tree — bring its ENTRY SCHEMA into the form-family model. (The pack backlog is ALREADY per-entry [BD-203] and bold-pair-anchored, but uses the v10-grammar `Type:` field — NOT the Entry-Type/Marker/Scope/Severity form-family decomposition.)
  - The pack `/backlog/_rules.md` — the form-family schema SSOT, mirroring the BD-206 project-side model. The exact form-family shape for pack BD entries (vs the project TD/phase entries) is THIS BD's design.
  - The drift-prevention WORKFLOWS — a pack-side form-family conformance check (rules + tests + CI), the analogue of BD-206's project-side enforcement, so the pack backlog cannot drift.
  - Assess the pack `/changelog/` tree similarly (already per-entry; structured-format compliance).
Description: G-5 (BD-206 design gate) established that real pack/project symmetry requires the PACK backlog to ALSO use the form families + per-entry structure + the compliance-maintaining / drift-preventing workflows — not just the project side. BD-206 brings the project side into compliance + enforces it; the pack side is NOT (v10-grammar `Type:` field, no form-family decomposition, no form-family conformance enforcement). This BD applies the BD-206 form-family + drift-prevention model to the pack backlog (and assesses the pack changelog), so the two surfaces are genuinely symmetric. v11.1.
  **Out of scope:** tracker work; the project-side conversion (BD-206); changes to the BD-NNN numbering / lifecycle states (this is about the entry SCHEMA + enforcement, not renumbering).
Resolved: n/a
