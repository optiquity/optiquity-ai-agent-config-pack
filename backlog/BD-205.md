<!-- per-entry source: /backlog/BD-205.md; contract: /backlog/_rules.md -->
**BD-205 — v11.0 final repo readiness audit + full test/audit/fix cycle (the last gate before launch)**
Type: feat/audit — STRUCTURAL. The comprehensive pre-launch readiness gate: a full test → audit → fix cycle, iterated until clean (auditor/reviewer agents + fix-coders per the loop).
Status: Open
Target: v11.0 (launch-gate item — the FINAL one, user 2026-06-04).
Blockers: runs only once every other launch-gate item is Resolved (user 2026-06-04). RE-ENUMERATED gate set (2026-06-13, BD-214 Track-2): the launch-gate BDs are BD-214 (+ its Track-2 entry applications), BD-197, BD-206 (v11.0 flat-file-only, US-6), BD-210 (pre-launch maintenance-docs cleanup), and then BD-093 (release pin) — plus the already-Resolved BD-195/BD-200/BD-203/BD-204-cleanup. (BD-204/BD-207 tracker work is DEFERRED indefinitely per BD-214 and is NOT a v11.0 launch-gate item. BD-185 (flat-file half) is likewise deferred out of v11.0 with the tracker-redesign group and is NOT a v11.0 launch-gate item.) It is the LAST gate before launch.
Unblocks: v11.0 launch — a verified-ready, correct repo.
File/Symbol (whole-repo readiness audit — key surfaces; refined by the Phase-0 census): `scripts/validate-pack.py`, `scripts/tests/`, `test-fixtures/`, `test-fixtures/build.sh`, `scripts/migrate-v10-to-v11.sh`, `scripts/dry-run-migration.sh`, `pack-ops/`, `project-template/`, and the trinity `CLAUDE.md` / `AGENTS.md` / `GEMINI.md`; the real-project resettable testbed sourced read-only from the developer's real target application repo (a private repo whose identity is kept OUT of this public-bound pack repo — recorded only in the local, uncommitted Phase-0 handoff); branch reconciliation across `main` and `v11-dev`.
Scope: run ALL repo tests (every test suite + validate-pack); a whole-repo v11.0 readiness audit (every launch-gate BD landed correctly; whole-repo correctness; the pack self-migration is sound; no contamination/regressions); the live-GH FLAT-FILE dog-food + milestone-audit scope (incorporating the prior Batch 22 (BD-100) milestone-audit scope — MERGED here per US-7 — and the prior Batch 23 trio residue); iterate test → audit → fix until ZERO findings.
  **DROP tracker legs from audit scope (2026-06-13, US-6/US-5):** tracker (GH Issues) integration is deferred indefinitely (BD-214), so the flat→tracker multi-toggle dog-food legs of the prior Batch 23 trio (BD-174 scratch-clone multi-toggle, BD-171 tracker-toggle, BD-102 flat→tracker dog-food) are OUT of v11.0 audit scope. BD-102 and BD-174 are Deprecated (US-7); BD-171 is re-scoped to the flat-file v10→v11 migration harness (tracker-toggle legs dropped). What remains in scope is the flat-file v10→v11 migration validation (the flagship migration the launch ships).
  **ABSORBED from BD-100 (MERGED, US-7) — the three Batch-21c retro carry-forwards (verbatim, from PACK-REVIEW-BD-116-RETRO §5; verify in this final milestone audit):** (a) `scripts/validate-pack.py` Check 23 (`pack-internal: true` marker enforcement) recurses into `scripts/persona-contracts/` — BD-116 retro F5 added the markers but Check 23 currently only scans top-level `scripts/`; (b) `scripts/persona-contracts/contract-greenfield.sh` carries an inline note mapping its Assertion-N numbering to `init-project.sh stage_sN_*` source-stage numbering (currently mismatched cosmetically — Assertions 1-7 vs Stages S2/S4/S5/S6/S7/S8/S11); (c) `scripts/persona-contracts/contract-mid-dev.sh` carries an inline rationale comment explaining the deliberate absence of S6/S8/S11 mirror coverage (mid-dev verifies user-domain preservation only; greenfield owns install-verification surface — splitting prevents redundant assertion duplication).
  **BD-102/171/174 residue (US-7):** BD-102 (real-pack dog-food) and BD-174 (scratch-pack-clone multi-toggle) are Deprecated — their flat-file-relevant intent (final ship-decision validation on the real pack repo + safe-env pack-on-pack code validation) is folded into this readiness gate's flat-file dog-food. BD-171's flat-file v10→v11 migration validation is the surviving live-migration leg.
  **Pre-merge `main`→`v11-dev` reconciliation (user 2026-07-30) — the merge-strategy prep so the eventual `v11-dev`→`main` merge is a DECISIVE clean full overwrite:** `main` has DIVERGED from this branch — 13 commits live on `main` that are NOT on `v11-dev` (snapshot 2026-07-30; **RE-DERIVE the live `git log --oneline v11-dev..main` set at reconciliation time** — it may have grown). For EACH `main`-only commit, adjudicate KEEP vs DROP **WITH USER APPROVAL per commit** (each potential cherry-pick/merge goes through the user; NOT everything is retained — some are intentionally superseded by v11 work). The KEPT commits are cherry-picked/merged **FROM `main` INTO `v11-dev` FIRST**, so that when `v11-dev` is finally merged back to `main` it is a clean full OVERWRITE with **nothing on `main` silently lost** (everything worth keeping already lives in `v11-dev`; the rest was user-approved as superseded). Starting inventory (`v11-dev..main`, 2026-07-30): `7ccbba9` Gemini→Antigravity reference-doc; `4234cde` Antigravity CLI docs snapshot; `a047174` gitignore `.playwright-mcp/`; `d174fb6` v11.1 GitHub-Projects parking-lot; `fa81704` v10.1 README/CHANGELOG row; `6afb926` CLAUDE.md "spawn all agents without isolation" (LIKELY SUPERSEDED by the v11 `isolation_mode` model — flag); `ec7aef1`/`11998b1`/`092f8d7` v10.1 RAG reconciliation + manifest; `0134dd2` v10.1 Check 21 (per-agent canonical-phrase); `50395d3` v10.1 PM-CHAT Permission-profiles; `532e883` project-trinity Project-memory + proto-gen row; `4258440` v10.1 per-agent permission profiles ×48. Adjudication precedes the final merge; capture each keep/drop decision + its rationale.
  **Test-environment architecture + coverage visibility (user 2026-08-02) — the audit runs against REAL, isolated, RESETTABLE environments, and its coverage is made VISIBLE before the fan-out:** (1) **Coverage matrix + environment inventory** (Phase-0 deliverable, USER-APPROVED before any audit runs): every verified item × the environment it is verified against (synthetic fixture / pack-self / real-project testbed / live-GH scratch), plus per-environment PROVENANCE (synthetic recipe vs `cloned from <the real target project repo> @ <SHA>`) + ISOLATION guarantee — gaps surfaced on one page for a user decision. (2) **Real-project resettable golden-baseline testbed** (the flagship stress environment, ON TOP OF the existing synthetic-fixture + pack-self dog-food, NOT replacing them): three tiers, one immutable — the developer's real target application repo (a private repo; identity kept OUT of this public-bound pack repo, in the local handoff) is a read-only clone SOURCE seeded ONCE + never touched again; an IMMUTABLE golden baseline (bare mirror / tagged snapshot) is the pinned start point + reset source; a WORKING testbed (the mutable copy) is migrated + worked + committed + PUSHED, then RESET to the baseline at the START of every iteration. Loop: reset → stress-test (v10→v11 migration + real work + commits + pushes) → collect findings → FIX THE PACK → reset → re-run — pack under real use, not a smoke test. (3) **Isolation guarantees (hard environment-integrity gate):** baseline seeded by read-only `git clone` (never writes the source); the testbed's `origin` RE-POINTED to a scratch target (local bare mirror OR a PRIVATE GitHub testbed repo — archive-not-delete on cleanup per the PAT) so a `git push` can NEVER reach the real target project remote; the real target project `HEAD` + `git status` snapshotted BEFORE/AFTER + asserted byte-identical (the untouched proof); every script verifies `pwd` is the testbed, never the real target project repo. (4) **Open substrate decision (Phase-0/architect):** all-local bare mirror (zero GitHub exposure; no real remote-push/CI fidelity) vs PRIVATE GitHub testbed repo (real push/PR/CI fidelity; the real target project code on GitHub → must be private) vs both — decided at Phase 0 with the coverage/fidelity tradeoff surfaced.
Out of scope: net-new features (this is a readiness GATE, not a build phase); any fix it surfaces is applied in-cycle, not deferred.
Acceptance criteria: every repo test green; the readiness audit CLEAN (no outstanding findings after the final pass); the live-GH dog-food passes; the Phase-0 coverage matrix + environment inventory are produced + USER-APPROVED, and the real-project resettable-testbed stress-test passes with the environment-integrity proof (the real target project `HEAD` + `git status` byte-identical before/after; no push reached the real target project remote); the pre-merge `main`→`v11-dev` reconciliation is COMPLETE (every `main`-only commit user-adjudicated keep/drop; all keepers merged into `v11-dev`; the final `v11-dev`→`main` merge verified to be a clean, lossless overwrite); no launch blockers remain; the repo is verified v11.0-ready and correct.
Note: this incorporates/supersedes the stale `EXECUTION-PLAN-V11.0.md` Batch 22 (BD-100 final audit) + Batch 23 (live-GH trio) ordering, repositioned as the single final gate after the remaining live launch-gate items. BD-100 is MERGED here (Deprecated, US-7); its milestone-audit scope + the three Batch-21c retro carry-forwards are absorbed above. BD-102/BD-174 are Deprecated (US-7) and their tracker-toggle legs dropped; only flat-file dog-food/migration validation remains in scope.
References: `project_pack_self_migration_launch_gate`; `project_batch23_test_coverage_gaps` (pack memory); `EXECUTION-PLAN-V11.0.md` Batch 22/23; BD-100 (Deprecated, merged here), BD-102 (Deprecated), BD-174 (Deprecated), BD-171 (flat-file migration harness); BD-214 (tracker deferral — defines the re-enumerated gate set).
Resolved: n/a
Position: v11.0 launch gate — the FINAL item; after the remaining launch-gate items; immediately before launch (user 2026-06-04).
Note (2026-06-11, anchored from BD-204 close-reason review-2 NIT-1): test-hygiene item for this audit — the suite-wide prefix-vulnerable `assert_contains "closed:     N"` idiom (forward/roundtrip summaries) can false-match larger counts sharing a prefix; mitigated today by exact-count legs. Assess a pattern-wide exact-match idiom during the full test/audit cycle.
