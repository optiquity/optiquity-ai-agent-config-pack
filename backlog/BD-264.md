<!-- per-entry source: /backlog/BD-264.md; contract: /backlog/_rules.md -->
**BD-264 — Groupings validation: Check 72 extension + new focused Check 84 + validate-docs conformance leg incl. GRP-000 reserved branch + exclusivity + toc-sync + empty-Status close + part-member close (BD-189 CBD-3)**
Type: feat — validation (twin-parser model: pack checks + client conformance). The BD-189 Wave-2 validation child. Opened 2026-07-05 at C0 per PLAN-BD189-FOLDED.md F1.
Status: Resolved
Target: v11.0 — Wave 2 (parallel; disjoint file sets).
Blockers: BD-262 (reads the shipped contract; the coherence-test file lands the BD-262 extension first).
Unblocks: BD-268.
File/Symbol:
  - `scripts/lib/validate_checks/discipline_parity.py`
  - `scripts/lib/validate_checks/core.py`
  - `scripts/validate-pack.py`
  - `project-template/scripts/validate-docs.sh`
  - `scripts/tests/test-validate-docs-target-coherence.sh` (ES golden refresh)
  - `scripts/lib/per-entry/index-generate.sh` (:10-11 comment, four names)
  - `scripts/tests/test-index-generate.sh` (:7-8 comment, four names)
  - `scripts/tests/test-validate-pack-check-73.sh` (:10-11 comment, four names)
  - NEW `scripts/tests/test-validate-pack-check-84.sh`
  - NEW `scripts/tests/test-validate-docs-groupings-conformance.sh`
Description:
  Scope (verbatim from PLAN-BD189-FOLDED.md F1/BD-264, the landed anchors): Check 72 tuple + forbidden `GROUPINGS.md`; NEW focused Check 84 (registry 80→81); client `_conf_check_grouping_entry` (full D7.3 + F9 byte grammar + RS-2 reserved branch AS AMENDED (IFF-1 disabled BOTH directions on GRP-000; field FORBIDDEN at any count) + RS-3 exclusivity + F12 toc-sync + dangling member refs + the G-2 part-member close: ONE predicate — a member token resolving to an `Entry-Type: phase-part` entry FAILs, same class as dangling, the message naming the member file + directing to the parent phase (parts inherit membership by containment) — + one negative fixture + one `--self-test` case, specced before the golden freeze); the A2 close at the :686 guard (present-but-empty `Status:` on a phase-epic → FAIL; census ZERO at HEAD — no allowlist); the two landed-bytes sub-items: (a) the pin-6 docstring carve-out RETIREMENT at validate-docs.sh:954-960; (b) the ES golden refresh in the coherence test (the new A2 FAIL line joins its expected set and the zero-assert flips); the A7 TIGHTENED `_CONF_ENTRY_REGEX["groupings"]` (lock-step with BD-262's `_lib.sh` arm); message fixes at :1116-1121 + :1199; the DEFSTATUS COMMON comment repairs (validate-docs.sh:802 + discipline_parity.py:1749 three-name → four-name) plus the +3 comment-only rider files; `--self-test` groupings + empty-Status + part-member cases; negative fixture battery incl. `GRP-0000.md` FAILS.
  Acceptance (verbatim): every negative FAILS naming file + problem (incl. `GRP-0000.md`; present-but-empty Status; a part-typed member ref — message directs to the parent phase); positives + empty tree + empty/single-member GRP-000 (no exception field) PASS; Check 84 bite-proven incl. the schema-key/lib cross-agreement line; the ES geometry green under the refreshed golden; the :954-960 docstring carries no stale carve-out sentence; registry 81 green plain + DEEP; full battery green.
Resolved: 2026-07-05 — Landed: the groupings validation layer — Check 72 extension (tuple + the forbidden `GROUPINGS.md`), NEW Check 84 (registry 80→81; the schema↔lib reserved-id cross-agreement, load-bearing-proven), the client conformance legs (the full grammar + the RS-2 amended reserved branch + RS-3 exclusivity + F12 toc-sync + dangling members + the G-2 part-member close with the ruled message shape + the A2 empty-Status close, census zero), the pin-6 docstring retirement, the ES golden refresh, the message fixes, the four-name comment repairs (+3 riders), and the two NEW tests (the 29-negative conformance battery; the check-84 bites). Reviewed CLEAN (own 13 scratch probes incl. the count-2 exception arm; D-1/D-2 deviations ACCEPTED on measured merits; the IN_GLOBS POQ routed to BD-268 rider 3). Battery at landing: plain+DEEP at registry 81, coverage 102, coherence 55/55 under the refreshed goldens, the guard 7/7.
