<!-- per-entry source: /backlog/BD-105.md; contract: /backlog/_rules.md -->
**BD-105 — STATUS.md phase-row link rendering (flat-file; tracker dual-link half deferred)**
Type: TODO(version)
Status: Resolved
RE-SCOPE 2026-06-13 (BD-214 Track-2): tracker (GH Issues) integration is deferred indefinitely (BD-214), so the TRACKER dual-link half of this entry (the ` · [#N](issue-URL)` enrichment and the reverse strip) DEFERS WITH the tracker work (the deferred cluster). The v11.0 flat-file scope is the STATUS.md phase-row entry/phase link rendering only — the `[Phase Title](#anchor)` single-link form against the per-entry implementation-plan stream. (Was: dual-mode links in the BD-206/207 orbit; tracker half voided by the deferral, confirmed flat-file-only here.) Orbit = BD-206.
Blockers: BD-065, BD-067, BD-068, BD-066, BD-084 (flat-file half). The tracker dual-link half is additionally blocked on tracker resumption (BD-215 → BD-204/207).
Unblocks: None
File/Symbol (flat-file half): `scripts/lib/tracker-doctor.sh` (dead-path fix — was `scripts/lib/pack-tracker/doctor.sh`, which does not exist), STATUS.md phase-row renderer, MIGRATION-v10-to-v11.md. TRACKER half (deferred): `scripts/lib/tracker-migrate-forward.sh`, `tracker-migrate-reverse.sh`, `scripts/tests/tracker-migrate-roundtrip-test.sh`.
Description: FLAT-FILE half (v11.0): STATUS.md phase-row titles render as a single
  link to the phase entry/anchor in the project implementation-plan stream:
  `[Phase Title](#anchor)`.
  TRACKER half (DEFERRED with the tracker work, BD-214): when tracker mode is
  active, phase-row titles would render in Option A middle-dot inline format
  `[Phase Title](#anchor) · [#N](issue-URL)`; reverse strips ` · [#N](URL)` to
  restore single-link form; 4 edge cases dispositioned (orphan / multi-epic /
  direct edits / closed); bidirectionality contract honored as tracker-only
  enrichment. This half lands with the tracker-resumption release; it is NOT
  v11.0.
Resolved: 2026-07-05 — The flat-file half is DELIVERED by BD-266's landed generator: STATUS.md phase-row titles render as `[Phase Title](#anchor)` single links (the generated class-1a cell). The TRACKER dual-link half + the corrected doctor citation (`scripts/lib/tracker-doctor.sh` — tracker-exclusive, measured) route to BD-227 per the BD-189 GQ-4 ruling (2026-07-04): BD-227 owns the generator's tracker-mode enrichment (the #14 tracker legs); no work is lost — the deferred half rides its existing cluster anchor.
