<!-- per-entry source: /backlog/BD-269.md; contract: /backlog/_rules.md -->
**BD-269 — Claude Artifacts dashboard subsystem (pack-side)**
Type: feat — pack-side dashboard subsystem (adapters → collect → render → template pipeline, a freshness gate, a /pack-publish command + registry, the pack-session-state /2 machine cursor, and an auto-render trigger). Opened during v11.0 development alongside BD-270.
Status: Deprecated
Blockers: none.
Unblocks: n/a.
File/Symbol: n/a — the entire subsystem was reverted out of the repository.
Description: A shareable Claude-Artifacts dashboard rendered deterministically from repo state
  (backlog / plan frontier / session cursor / in-flight work), with a plan-freshness gate, a
  render+publish pipeline and sinks, a `/pack-publish` command, a per-session discovery registry,
  and the `pack-session-state/2` machine cursor with its validator checks. Implemented across
  roughly 65 commits before deprecation.
Resolution: 2026-07-13, deprecated — the entire BD-269 dashboard subsystem was reverted out of
  the repository per user direction: `v11-dev` was reset to the pre-BD-269 baseline and every
  BD-269 commit was deleted and gc-pruned (a true history removal — NOT reversed-and-recommitted).
  The work is not present in the tree and will NOT be part of any release. The interleaved,
  independent BD-271 (narration-twin Check 85 + client-gate session-state tests) was preserved.
  BD-270 (the project-side companion) is deprecated together with this entry. Any future dashboard
  effort starts fresh under a new BD, not by reusing this one.
