<!-- per-entry source: /backlog/BD-276.md; contract: /backlog/_rules.md -->
**BD-276 — Systemic BSD-mktemp portability sweep + Check 92 3-class regression guard + root-cause teaching-surface fix**
Type: chore — pack-dev portability hardening (PACK-OPS + runtime scripts; NOT a client deliverable). The `mktemp [-d] -t <prefix>.XXXXXX` form is BSD-vs-GNU non-portable (BSD treats the `-t` argument as a PREFIX and appends its own random suffix, leaving a literal `XXXXXX` in the path); sweep to the portable full-template form.
Status: Resolved
Blockers: none (the affected sites currently PASS — they are not path-sensitive today).
Unblocks: cross-platform (macOS BSD + Linux GNU) parity for every mktemp site; removes a latent portability class that already surfaced once — it masked the BD-257 detection regression as a false "mktemp bug" (a real product regression was nearly written off as platform flakiness).
Position: pack-dev portability hardening; independent. Sequence AFTER the BD-257 detection fix + BD-274 (not blocking).
File/Symbol (coder enumerates the exact set at implementation):
  - ~50+ files / 200+ sites of `mktemp [-d] -t <prefix>.XXXXXX` across `scripts/tests/*.sh` + runtime libs (`scripts/lib/*.sh`, `immutable-manifest.sh`, `migrate-v10-to-v11.sh`, etc.).
  - Portable target form: `mktemp -d "${TMPDIR:-/tmp}/<prefix>.XXXXXX"` (full-path template — both BSD and GNU expand the `XXXXXX`).
Description: The BSD/GNU `mktemp -t` divergence leaves a literal `XXXXXX` in temp paths on macOS. Most sites are not path-sensitive (they pass), but the class is latent AND already caused a misdiagnosis: the BD-257 detection regression was first mistaken for a mktemp bug because `test-activate-capability.sh` uses this form. A VERIFIED sweep replaces every buggy `-t`-prefix site with the portable full-template form and confirms no test/behavior regresses. VERIFY each site is genuinely the `-t`-prefix form before rewriting (some `mktemp` calls are already portable — do not churn those).
  **Out of scope:** the order-dependent test flake (BD-275); the BD-257 detection regression (a BD-257 fix, not a mktemp issue); the SOLE client-side occurrence `project-template/agent-run.sh:347` (this is a pack-only BD — the client sweep + client guard are the companion BD-277).
Resolved: 2026-07-30 — pack-side BSD-mktemp portability hardening shipped. (1) SWEEP: 318 real non-portable `mktemp` invocations across 67 pack-side `.sh` rewritten to the portable full-path-template form `mktemp [-d] "${TMPDIR:-/tmp}/<prefix>.XXXXXX"` (BSD + GNU expand `XXXXXX` identically); already-portable calls left un-churned. (2) GUARD: new `validate-pack.py` Check 92 (`scripts/lib/validate_checks/mktemp_portability.py`; count 88->89) — a git-tracked pack-side `.sh` scan, O(lines), invocation-vs-mention aware (comments/docs spared), allowlist EMPTY (measure-then-bound), covering THREE portability classes: the `-t <prefix>XXXXXX` literal-XXXXXX form incl. bundled getopt clusters (`-dt`/`-qt`/`-dqt`), and the GNU-only tmpdir-specifiers `--tmpdir`/`--tmpdir=DIR` + short synonym `-p DIR` (BSD lacks them, errors out); per-check test `test-validate-pack-check-92.sh` (T2c-T2f bite cases self-assemble the X-run + a `mkdir -p` spare-regression case). (3) ROOT-CAUSE teaching surfaces fixed: the pack `verification-harness` coder-skill x3 (`.claude`/`.codex`/`.agents`, byte-identical) + `test-fixtures/README.md` both TAUGHT the buggy form with a false "works on both" note — corrected to the portable form + an accurate note. (4) `ARCHITECTURE-BD-219-CI-FAILURE-DIAGNOSIS.md` stale "portable" claim reconciled in place (its CI-cause conclusion preserved). Deterministic pipeline: coder -> guard review (5 findings, all user-FIX incl NIT-4 `--tmpdir` + the surfaced `-p` synonym) -> re-review CLEAN -> two reviewer-surfaced teaching/doc one-liners -> final review CLEAN. Full CI battery + DEEP green; count 89. Client companion (agent-run.sh:347 + client guard assessment) tracked as BD-277 (mirror-but-customize, no-dual-use).
