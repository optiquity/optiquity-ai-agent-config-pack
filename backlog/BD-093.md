<!-- per-entry source: /backlog/BD-093.md; contract: /backlog/_rules.md -->
**BD-093 — v11.0 release pin (tag, README, CHANGELOG, MIGRATION cross-link)**
Type: TODO(version)
Status: Resolved
Blockers: the live v11.0 launch-gate set — BD-242 (the locked versioning scheme v11.0 GA launches under), BD-214 (+ its Track-2 entry applications), BD-197, BD-206, BD-210, BD-205 (final readiness audit). BD-093 is the release-cut commit and runs LAST, after BD-205 passes. (Tracker work BD-204/BD-207 is deferred indefinitely per BD-214 and is NOT a v11.0 launch-gate blocker. BD-185 (flat-file half) is deferred out of v11.0 with the tracker-redesign group and is NOT a v11.0 launch-gate item.)
Unblocks: None
File/Symbol: `README.md`, `/changelog/v11.md` (pack changelog stream; the former monolithic `CHANGELOG.md` was deleted at BD-203 — flat-file/no-mirror), git tags, `maintenance-docs/archive/v11/EXECUTION-PLAN-V11.0.md` (final Pattern B sweep target)
Description: The release-cut commit. Tag operations follow BD-242's locked
  versioning scheme and require Pack Chat / user approval — the user decides
  every tag (this entry specifies no tag mechanics; BD-242 is the scheme's
  home). validate-pack must pass on the tagged commit;
  MIGRATION-v10-to-v11.md references reflect the as-shipped state.
  **Changelog audit-artifacts consolidation:** consolidate the v11.0
  audit-artifacts blocks in `/changelog/v11.md` into one canonical position
  at the v11.0 changelog tail. **Final Pattern B sweep:** move
  `maintenance-docs/archive/v11/EXECUTION-PLAN-V11.0.md` and the
  accumulated workflow artifacts to `maintenance-docs/archive/v11/` via
  `git mv`. **Immutable manifest:** nothing to regen at the cut — the client
  manifest is generated at install time (per BD-253's ruled immutability
  model); no tracked manifest exists in the pack repo.
Resolved: 2026-08-22 — v11.0 (RC1) cut. Tag `v11.0-RC1` created at commit `3da7f79` and pushed; `Validate Pack` CI green on both the branch and the tag push. Check 4 confirms `README.md version v11.0 (RC1) matches git tag` — a real tag match, not the dev-branch allowance — satisfying this entry's stated condition that validate-pack pass on the tagged commit. SHIPPED: `/changelog/v11.md` release-candidate block closing a 125-entry v11-era Resolved-BD coverage gap + the two audit-artifacts blocks consolidated to one canonical position at the v11.0 tail; the final Pattern B sweep (`EXECUTION-PLAN-V11.0.md` + 7 spent `RESEARCH-BD-*` docs -> `maintenance-docs/archive/v11/` as 100%-similarity renames, the 9 live-referenced docs retained, referrers updated in lock-step); `README.md` version row -> `v11.0 (RC1)`; `MIGRATION-v10-to-v11.md` verified as-shipped + citations qualified. BEYOND the entry's original scope, surfaced by review: Check 4 (`check_readme_version`) REPAIRED — it selected the LAST README version row while the table is newest-first, so it compared `v1` (permanently tagged) and passed unconditionally; the README<->tag guard had never functioned, and a new `scripts/tests/test-validate-pack-check-4.sh` pins the newest-row selection and proves the guard BITES. Check 39's reverse leg widened to the migrator adapter hooks (sourced through the pre-existing shared heredoc parser rather than a second brittle one) + a dead manifest row removed; Check 65 gained an advisory unmatched-record warning making the allowlist's "sized to the KEEP set EXACTLY" claim load-bearing; ~44 references to BD-210/BD-150-deleted architecture docs swept from code comments and prose with rationale preserved; `no_leak.py` comments record that the internal-surface exemption is PERMANENT (single work repo, public with history intact, no separate scrubbed copy). Process: 1 implementation pass, 3 reviewer passes, 3 fix passes; final review CLEAN at 0 BLOCKER / 0 MUST. Verified at the cut: validate-pack 0 (default + DEEP), 132/132 wired tests, fixtures 7/7, shard coverage exact. Reports at `maintenance-docs/v11-implementation/{IMPLEMENTATION-REPORT,PACK-REVIEW,FIX-REPORT}-BD-093*.md`. Remaining launch steps are USER-owned: force `main` to `v11-dev`, verify CI on `main`, transfer to the target account, flip public.
