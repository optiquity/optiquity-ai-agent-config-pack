<!-- per-entry source: /backlog/BD-093.md; contract: /backlog/_rules.md -->
**BD-093 — v11.0 release pin (tag, README, CHANGELOG, MIGRATION cross-link)**
Type: TODO(version)
Status: Open
Blockers: the live v11.0 launch-gate set — BD-214 (+ its Track-2 entry applications), BD-197, BD-206, BD-210, BD-185, BD-205 (final readiness audit). BD-093 is the release-cut commit and runs LAST, after BD-205 passes. (Tracker work BD-204/BD-207 is deferred indefinitely per BD-214 and is NOT a v11.0 launch-gate blocker.)
Unblocks: None
File/Symbol: `README.md`, `/changelog/v11.md` (pack changelog stream; the former monolithic `CHANGELOG.md` was deleted at BD-203 — flat-file/no-mirror), git tags, `maintenance-docs/v11-implementation/EXECUTION-PLAN-V11.0.md` (final Pattern B sweep target)
Description: The release-cut commit. Pack maintainer rule: tag operations
  only after Pack Chat approval. Delete `v11` if present; recreate `v11.0`
  and `v11`; push. validate-pack must pass on the tagged commit;
  MIGRATION-v10-to-v11.md references reflect as-shipped state.
  **Changelog audit-artifacts consolidation pass (carried from BD-150
  PACK-REVIEW §2.1 advisory nit, 2026-05-12):** the v11.0 Scope C
  audit-artifacts subsection (in the v11.0 changelog content at BD-150 ship
  time) is wedged between "Carried over to future work" and the v10 entry
  instead of consolidated with the other audit-artifacts blocks. Consolidate
  all v11.0 audit-artifacts blocks into one position at the v11.0 changelog
  tail (or another single canonical position) as part of the release-pin
  changelog polish (now in `/changelog/v11.md`). **Final Pattern B sweep:** move
  `maintenance-docs/v11-implementation/EXECUTION-PLAN-V11.0.md` and the
  BD-150 IMPL/REVIEW reports (and any other workflow artifacts that
  accumulated during the safe-window batches) to
  `maintenance-docs/archive/v11/` via `git mv` — BD-150's Pattern B sweep
  intentionally retained EXECUTION-PLAN and BD-150's own reports because
  they were still being referenced by the remaining safe-window batches.
Resolved: n/a
Note (2026-06-20, user direction): BD-093 is additionally GATED ON BD-242 — v11.0 GA must launch under the new semantic-versioning + Apple-style RC/GA scheme (BD-242), so BD-242 MUST be Resolved before this release-cut. The tag/README/changelog operations here will use the new scheme: v11.0 ships publicly as `v11.0.0 (RC1)` … `(RCn)` → `(GA)` (with the display↔tag-safe encoding BD-242 defines), deliberately avoiding version-number inflation. The "bare major tag floats to latest minor" + tag-move conventions referenced above are superseded by whatever BD-242 lands.
