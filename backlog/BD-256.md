<!-- per-entry source: /backlog/BD-256.md; contract: /backlog/_rules.md -->
**BD-256 — Modularize validate-pack.py — split the 80-check monolith into per-category modules so isolated check-BDs stop colliding on one file and can parallelize**
Type: chore — pack-internal CI tooling refactor (PACK-OPS; not client-shipped). Splits the monolithic `scripts/validate-pack.py` (14,489 lines; 80 registry checks; run serially in 1.78s) into separately-editable modules so genuinely-isolated check work no longer collides on one file and can be developed in parallel.
Status: Resolved
Target: v11.0 (surfaced 2026-06-30 during v11.0 development; no deferral authorized — `no-deferral-without-user-direction`). The BD-256-vs-BD-222 SEQUENCING (modularize-first vs BD-222-on-monolith-first) is decided at the architect design gate once blast radius is measured — a sequencing decision, not a version deferral.
Blockers: none hard. Interacts with every open check-touching BD: BD-222 (adds Check 83 — ON HOLD at its design gate pending this investigation), BD-253 (extends Check 70), BD-255-D1 (Part D verification quartet). These are the BDs the single-file collision serializes today.
Unblocks: dissolves the root cause of check-BD serialization — when each check lives in its own file, two BDs adding/editing DIFFERENT checks no longer collide on `scripts/validate-pack.py`, so the BD-255 cross-BD collision scan (Check 82) stops forcing them serial and they parallelize. (Caveat the architect must preserve: BDs that edit a SHARED constant/helper still collide — this removes the every-check-add-collides floor, not all coupling.)
File/Symbol (architect refines the exact module boundaries; the in-scope blast-radius surfaces):
  - `scripts/validate-pack.py` — the monolith: the 80 `check_*` functions, `_build_check_registry()`, `run_check()`, `main()`, the cross-check shared constants (`_PACK_CHAT_ONLY_PERMITTED_PATHS`, `_TRACKER_BACKENDS`, `_CHECK_54_REQUIRED_TOKENS`, `_CHECK_56_CANONICAL_VERBS`, `_DOC_CONSTANT_TWINS`, `CHECK_REGISTRY_EXPECTED_COUNT`, the path/strip helpers, `_session_state_load`) — split into modules over a shared core.
  - a new check-module package under `scripts/lib/` (e.g. `scripts/lib/validate-checks/`) — the home for the extracted per-category/per-check modules + the assembled registry + the shared core.
  - the invariants that MUST survive the split: Check 59 (`CHECK_REGISTRY_EXPECTED_COUNT == len(registry)`), Check 80 (`_DOC_CONSTANT_TWINS` twin-bijection), Check 81/82 (active-design `File/Symbol` + collision scan), Check 60 (shard coverage).
  - `scripts/tests/test-validate-pack-check-*.sh` — the per-check test files (how they invoke the checks after the split; in-process vs subprocess).
  - `.github/workflows/validate-pack.yml` — the `validate` job entrypoint (interface unchanged; confirm it still runs the full registry).
  - `test-fixtures/manifest.txt` — push-time regen iff a module move changes a fixture input.
  - `scripts/lib/ci-shard-plan.py` — the existing 4-shard TEST-battery partitioner (BD-219); confirm the refactor does not disturb it (it shards the wired-test battery, NOT validate-pack's internal checks — adjacency, not in scope).
References: BD-219 (the existing CI test-battery sharding via `scripts/lib/ci-shard-plan.py` — the precedent that already parallelized the wired-test wall-clock; this BD is the ANALOGOUS dev-time-isolation refactor for validate-pack's internal checks). BD-255 (the cross-BD collision scan, Check 82, whose forced serialization this BD's root-cause fix relaxes). BD-222 / BD-253 (the in-flight check-touching BDs whose sequencing this BD's design gate resolves). The run-model facts (1.78s serial; no `multiprocessing`; full `validate` job not sharded) were measured 2026-06-30 at HEAD `24b95bb`.
Process: size-tiered large-BD standard — LARGE (structural: a NEW module convention + a tree-shape change + a registry-assembly migration + it touches the count/bijection invariants). Pipeline: pack-docs-researcher (internal census — the full check/constant/helper dependency map + the recent check-BD collision history + the run model) → pack-architect (modularization design + a BD-222 sequencing recommendation) → adversarial architect review → reconcile-if-needed → user design gate (decide sequencing vs BD-222) → planner → adversarial planner → reconcile-if-needed → planner-to-coder gate → coder waves + bounded review/fix cycle.
Resolved: 2026-06-30 (v11.0) — validate-pack.py modularized into the `scripts/lib/validate_checks/` package (core.py spine + 8 cross-module seams, 12 cluster modules, singletons.py) behind a thin re-exporting facade at the frozen `scripts/validate-pack.py` CI entrypoint. 16 waves (W0-W15) via the large-BD pipeline; registry-80 preserved (identical (number,label) order), behavior byte-identical (plain + DEEP), full wired battery green. Capstone end-of-batch review CLEAN: both self-referential check self-exemptions (Check 35 folded-into on boundary_refs.py; Check 53 Guard-A on discipline_parity.py) are measure-then-bound-exact, proven by live guard-hole probes. W15 was verification-only (W16-absorbed, no empty commit). Batch-close doc-cleanup reconciled ARCHITECTURE-BD-256.md §8 + agents_skills.py docstring to post-W14 reality.
Position: v11.0 — a foundational dev-velocity refactor surfaced mid-v11.0; its sequencing relative to BD-222 and the remaining check-touching queue is set at the architect design gate.
