<!-- per-entry source: /backlog/BD-234.md; contract: /backlog/_rules.md -->
**BD-234 — Assess Graphify real usage + cost stats after burn-in; revisit the graph's cadence / knobs / index scope**
Type: chore — pack-ops tooling assessment + tuning (PACK-OPS; not client-shipped). After Graphify (BD-225) has been in real use for a burn-in period, review the ACTUAL usage + cost stats and adjust the graph's refresh cadence, query/build knobs, and index scope based on measured numbers rather than the pre-build estimates BD-225 launched with. Opened 2026-06-18 (user direction, from BD-225 OQ-5).
Status: Resolved
Target: v11.0 — positioned AFTER BD-206 (user 2026-06-18); requires a burn-in period of real Graphify usage before it can run meaningfully.
Blockers: BD-225 (Graphify must be implemented AND in real use to produce stats) + a burn-in period of actual usage (so `cost.json` and observed behavior are meaningful).
File/Symbol:
  - `graphify-out/cost.json` (per-clone, gitignored) — the cumulative token-cost tracker; the primary measured-cost source.
  - **Correction (2026-06-18, measured at the initial build):** the `graphify extract --backend claude-cli` build path (the pack's chosen build mechanism) does NOT emit `cost.json`. The measured-cost sources are instead the build-log token line (`[graphify extract] tokens: N in / M out`) plus `graphify-out/.graphify_analysis.json`. Initial-build figures: **7,899,177 tokens in / 2,040,684 out, ~$0 API cost** (Claude subscription path). When this assessment runs, read those sources in place of every `cost.json` reference in this entry.
  - The BD-225 settings this re-tunes: the semantic-refresh cadence (OQ-5 = doc-gated Option 2 + force-on-removal), the query knobs (`--budget` per use case), the build/perf knobs (BD-225 design §9), and the index scope (OQ-6 = whole-repo-minus-exclusions; the "start big, edit down later" promise).
Description:
  **Problem:** BD-225 wires Graphify in with PROVISIONAL settings — cadence, knobs, and whole-repo index scope — locked on the architect's evidence-based recommendations plus a PRE-BUILD cost projection, NOT on measured real-world cost/performance. The real per-refresh + cumulative subscription-quota cost is only knowable from `graphify-out/cost.json` and observed behavior after actual use.
  **Goal:** after a burn-in period of real Graphify usage, assess the measured usage + cost stats (`cost.json`; observed semantic-refresh frequency/duration; subscription-quota impact; query hit-rate / answer usefulness) and adjust as needed:
    - the semantic-refresh **cadence** (revisit OQ-5's doc-gated Option 2 — keep, tighten, or loosen);
    - the **query knobs** (`--budget` per use case: human / Pack Chat prompt-construction / spawned agent);
    - the **build/perf knobs** (BD-225 §9);
    - the **index scope** — honor the "start big, edit down later" direction: narrow the whole-repo corpus (e.g. additional exclusions) if it proves too costly or noisy in practice.
  **Boundary (absolute):** PACK-OPS ONLY — assesses pack-development Graphify usage; nothing client-side.
Acceptance criteria: measured usage + cost stats reviewed (`cost.json` + observed behavior) after a real burn-in; each provisional BD-225 setting (cadence, knobs, scope) either CONFIRMED as-is or ADJUSTED with documented reasoning; any adjustments land via the normal review/fix cycle.
References: BD-225 (the Graphify integration whose provisional settings this re-tunes — OQ-5 cadence + OQ-6 scope + design §9 knobs are the provisional choices to revisit); the "start big, edit down later" index-scope direction (user 2026-06-18); the user's "OK with the quota burn if backed by a good estimate" stance — this BD supplies the measured estimate the pre-build phase could not.
Resolved: 2026-06-28 — ASSESSMENT LEG COMPLETE. The measured Graphify cost/benefit assessment was delivered as the tracked report `maintenance-docs/graphify-assessments/GRAPHIFY-COST-BENEFIT-2026-06-28.md` (benefit: 56× token reduction / ~0-cost queries; cost: ~4.23M-token semantic-build floor over the burn-in on the `claude-cli` subscription path; qualitative accuracy + a process review; a reproducible Methodology for future comparison). REVISIT DECISION (user 2026-06-28): the current Graphify settings (cadence / query+build knobs / index scope) are SUFFICIENT for the v11.0 launch and are left AS-IS, not re-tuned now — focus stays on the remaining v11.0 launch requirements. The IMPLEMENTATION LEG (enacting the report's improvement recommendations — vocab-expansion query flow, save-result feedback loop, freshness tightening, cost ledger, GRAPH_REPORT regen) is moved to **BD-254 (v11.1)**. Both legs of this BD are thus discharged: assessed = yes (report delivered); revisit = settings confirmed as-is for v11.0, improvements deferred to BD-254.
Position: v11.0 — RESOLVED. Assessment delivered (the report); the revisit confirmed current settings as-is for launch; the improvement-implementation leg is tracked as BD-254 (v11.1).
