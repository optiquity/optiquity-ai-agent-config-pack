<!-- per-entry source: /backlog/BD-271.md; contract: /backlog/_rules.md -->
**BD-271 — Narration-twin hardening: client-gate session-state behavioral coverage + a new Check 85 regex-CONTENT parity guard (BD-252 / BD-253 / BD-269 follow-on)**
Type: feat — STRUCTURAL (pack-side CI + test coverage). Surfaced 2026-07-10 (BD-269 freshness work + the cF0b review POQ); user-approved open.
Status: Resolved
Blockers: none. The Check-85 guard must land only after both narration-pattern twins already match — they do (BD-252 pack twin + BD-253 client twin, both landed case-sensitive). Independent of BD-269 cF1 (the twins are aligned; Check 85 is regression protection, not a cF1 prerequisite).
Unblocks: n/a — regression-protection hardening; no downstream BD gated on it.
File/Symbol:
  - `scripts/tests/test-validate-docs-session-state.sh` (D1 — client doc-gate session-state behavioral coverage)
  - `scripts/lib/validate_checks/session_state.py` — `check_narration_twin_content_parity` (Check 85, D2)
  - `scripts/validate-pack.py` — Check 85 registry row + `CHECK_REGISTRY_EXPECTED_COUNT` bump
  - `scripts/lib/validate_checks/core.py` — Twin A source `_SESSION_STATE_NARRATION_PATTERNS`
  - `scripts/tests/test-validate-pack-check-85.sh` (22 cases)
Problem: two related gaps surfaced while BD-269's post-commit-hook vocabulary tripped the then-case-insensitive Check-79 narration guard. (1) COVERAGE: the client doc-gate's session-state validation (`project-template/scripts/validate-docs.sh` — `run_session_state`, `_SS_NARRATION_PATTERNS`, `_ss_iter_string_values`) shipped in BD-253 with ~zero behavioral tests (the cF0b review POQ). (2) DRIFT-DETECTION: no check compares the two narration-pattern twins — Twin A `_SESSION_STATE_NARRATION_PATTERNS` (`scripts/lib/validate_checks/core.py`) vs Twin B `_SS_NARRATION_PATTERNS` (`project-template/scripts/validate-docs.sh`). Check 70 (`check_client_doc_gate_parity`) is axis-marker STRUCTURAL parity only ("NOT behavioral" per its own docstring) and never inspects the narration lists; Check 79 CONSUMES Twin A but does not reconcile the pair. So a one-side flag / anchor / character-class / absence drift between the twins has zero coverage today. The earlier BD-269 note's "Check-70 strengthened" phrasing was shorthand — the correct encoding is a NEW sibling check, not an edit to Check 70.
Process: base flow (large-bd-pipeline-standard — one STRUCTURAL signal: a new CI check). D1: coder + reviewer already run this session (both clean) — lands directly as a pack-only commit. D2: architect (done — `ARCH-BD253-check70-content-parity.md`) → planner → user planner-to-coder gate → coder + bounded review/fix cycle. Design doc lands to `maintenance-docs/v11-implementation/` at resolution. Serializes with BD-269 c11 on `core.py` only (different lines: the count constant vs the P4a schema comment) — same-file order, no semantic conflict.
Position: v11.0; independent of BD-269 cF1 (twins already aligned; Check 85 is regression protection). D1 lands now; D2 after its planner→coder cycle.
Resolved: 2026-07-10 — Shipped both deliverables (both commits reviewed CLEAN; validate-pack plain+DEEP green). D1: `scripts/tests/test-validate-docs-session-state.sh` — behavioral coverage of the client doc-gate's session-state leg (`run_session_state` / `_SS_NARRATION_PATTERNS`; 8/8; drift-proof live-axis reads). D2: new **Check 85** (`check_narration_twin_content_parity` in `scripts/lib/validate_checks/session_state.py`) enforcing narration-twin regex-CONTENT parity across the module/embedded-.sh boundary — integer-value flag canonicalization, `bd`↔`td` fold bounded by a fold-reach + directionality guard, count+duplicate asserts, symmetric fail-loud, heredoc-select-by-assignment; + registry row + `CHECK_REGISTRY_EXPECTED_COUNT` 81→82 + honest module docstring + `scripts/tests/test-validate-pack-check-85.sh` (22 cases) + the `DESIGN-BD-243-CLIENT-GATE.md` §C.3 addendum (architect-doc reconciliation, 3-leg chain). Design vetted via adversarial + reconciliation architect passes (7 findings resolved). Check 70 unmodified (axis-marker structural parity only).
