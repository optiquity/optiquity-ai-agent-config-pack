<!-- per-entry source: /backlog/BD-243.md; contract: /backlog/_rules.md -->
**BD-243 — Strip historical/audit-trail text + bloat from operational docs (pack + project); add anti-bloat governance rule**
Type: feat — STRUCTURAL cross-surface cleanup (pack-ops + project-template). Removes historical/audit-trail text and general bloat from ALL operational documents on BOTH surfaces; adds ONE forward-looking governance rule. Large-BD standard: docs-researcher → architect → planner → coder → reviewer, with adversarial passes.
Status: Open
Target: v11.0 — user 2026-06-21 ("do it immediately"); the active priority while BD-206 is paused. (Open: hard gate on the BD-093 cut? — defaulting to land-in-v11.0.)
Blockers: none — independent of repo state. Coordination: overlaps BD-206 (PAUSED) on operating-doc edits touching the deleted monolith references — BD-243 handles historical/audit + bloat in operating docs; BD-206 handles the per-entry no-mirror FUNCTIONAL conversion; when BD-206 resumes it coordinates with BD-243's as-landed docs.
Unblocks: None
Provenance: user direction 2026-06-21 — discovered during BD-206 that operating docs (incl. pack-root CLAUDE.md / PACK-CHAT.md) carry historical/audit-trail justifications ("BD-NNN did X", dated notes, "until BD-NNN" clauses). Per-BD inline provenance + the memory/learnings framing accreted over dozens of BDs with no terseness enforcement, bloating the most-read, must-be-terse operating docs.
Decisions (user 2026-06-21):
  - GOAL: remove ALL historical + audit-trail references from operating docs (both surfaces). They serve no operational purpose; nothing reads them; tech debt. DELETE — never relocate (the real history already lives in changelog/backlog/maintenance-docs; a copy in an operating doc is redundant; relocating only fragments/preserves bloat).
  - EXPANDED bloat scope: also remove wordiness, verbosity, non-actionable text, and unstructured text that should be structured -> terse/structured operating text WHEREVER POSSIBLE. Bar: AGGRESSIVE.
  - TARGETS: operating rules/instructions + task-performing docs (chat-session docs, agent definitions, prompts, skills). NOT operating docs: output/helper/reference docs (e.g. README.md), the history trees (changelog/backlog entries), maintenance-docs, IMPL reports.
  - SCRIPTS EXEMPT: historical/audit text MAY remain in script COMMENTS (ignored at runtime). The BD targets operating DOCUMENTS, not scripts.
  - TAXONOMY: the docs-researcher + architect DEFINE the operating-doc vs reference-doc taxonomy + exempt list and PRESENT it to the user for approval, asking when unclear. Anchors: PACK-MEMORY-RATIONALE.md = operating (IN; no historical/audit text needed for its work); README.md = output/helper, NOT operating (OUT; its rules are self-governance, not system-wide operating instructions). Criterion: an operating doc issues system-wide operating instructions to chat sessions/agents; output/helper/self-governance docs do not.
  - SUCCESS = NO MEANING OR FUNCTIONALITY LOST. Docs get leaner; behavior unchanged — EXCEPT the one intended change below. Architect SETS the terseness/structure bar; reviewer VERIFIES no behavior change.
  - THE ONE FUNCTIONAL CHANGE: a NEW governance rule that (a) prohibits historical/audit-trail text in ANY operating doc going forward and (b) limits bloat (terse/structured operating text). It PERMITS that text in IMPL reports + reference docs (their purpose). Lives in ALL trinity files on BOTH surfaces (pack-root + project-template CLAUDE/AGENTS/GEMINI.md); architect decides additional locations.
File/Symbol (candidate ~150 operating docs, measured 2026-06-21 @ cd2927c — docs-researcher produces the DEFINITIVE list):
  - Pack (~34): root trinity; pack-ops/*.md (PACK-CHAT, PACK-AGENTS, MERGE-STRATEGY, PACK-MEMORY-RATIONALE, BOUNDARY-DEFINITION, ...); backlog/changelog _rules.md/_intro.md; .claude/skills/*/SKILL.md (11); .claude/agents/pack-*.md (5). [README.md OUT.]
  - Project (~118, shipped): project-template trinity; docs/pack/*.md + prompts/*.md (16); skills/*/SKILL.md (36); agent defs x3 CLI families (~47); docs/project/*/_rules.md/_intro.md/_format.md (7); supporting-docs/*.md (operating subset).
  - New-rule surface: 6 trinity files (+ architect-decided locations).
Description:
  Problem: operating docs — the most-read, must-be-terse rules/instructions for chat sessions + agents — accreted historical/audit-trail text + verbosity. Bloat that costs context every read, buries the actual rule, and has no operational purpose.
  Goal: sweep ALL operating docs (both surfaces) to DELETE historical/audit-trail text and aggressively reduce bloat to terse/structured operating text — zero meaning/functionality loss — and add a governance rule prohibiting it going forward.
  Process: large-BD standard. (1) pack-docs-researcher -> definitive blast radius + operating-vs-reference taxonomy + exempt list (confirm/replace the candidate); present taxonomy for user approval. (2) pack-architect (+ adversarial) -> sweep approach, terseness/structure bar, new rule (text + locations), per-doc delete/rewrite recipes preserving meaning + behavior. (3) pack-planner (+ adversarial) -> sequencing + worktree parallelization map (trinity-locked sets serialize). (4) pack-coder waves -> pack-reviewer (no-behavior-change + grep-zero on historical/audit patterns) under the bounded review/fix cycle. User approves at the planner-to-coder gate.
Acceptance criteria: every operating doc on both surfaces is free of historical/audit-trail text (grep-zero on agreed patterns) + reduced to terse/structured operating text; NO meaning/functionality lost (reviewer-verified) except the new rule; the new anti-bloat/no-history rule is codified in all 6 trinity files (+ architect-decided locations) and permits the text in IMPL reports + reference docs; the operating-vs-reference taxonomy + exempt list are user-approved; delete-only (nothing relocated); validate-pack green; full researcher->architect->planner->coder->reviewer workflow with adversarial passes.
References: BD-206 (PAUSED — overlapping operating-doc edits; coordinate on resume); /tmp/pack-handoff-bd206-research/CENSUS-BD-206-TECHDEBT.md (starting input — its STRIP-LIVE rows already classify historical refs in operating docs); BD-238 (large-BD pipeline standard); the trinity rule.
Resolved: n/a
Position: v11.0 — cross-surface operating-doc cleanup + governance rule; surfaced 2026-06-21 during BD-206; active priority while BD-206 paused.
