<!-- per-entry source: /backlog/BD-249.md; contract: /backlog/_rules.md -->
**BD-249 — Pack-side dead-mirror-subsystem removal (mirror-generate.sh + mirror constants + accessor)**
Type: feat — surfaced 2026-06-26 from the BD-206 design-gate DR-2 decision (SS-5); user-approved open
Status: Resolved
Target: v11.0
Blockers: BD-206 — sequenced DIRECTLY AFTER BD-206. The project-side half of the dead-mirror-subsystem removal (ARCHITECTURE-BD-206.md O22-proj) lands in BD-206's waves; this BD lands the PACK-side half (O22-pack) as a separate `pack-only` commit. The `mirror-generate.sh` file deletion is the TERMINAL state after BD-206's O7 in-file edits to that file, so it must follow. (Exact ordering among the several post-BD-206 BDs is TBD — to be decided when the post-206 queue is sequenced.)
Unblocks: genuine pack/project symmetry of the CORRECT (no-mirror) solution — both surfaces clean of the dead mirror subsystem, not symmetric in dead vestiges.
File/Symbol:
  - `scripts/lib/per-entry/_lib.sh` — remove the pack `mirror) printf` constants (pack-backlog + pack-changelog — the last 2 `mirror)` arms; the 3 project streams carried none post-BD-206) + their BD-203 comments; remove `pe_canonical_mirror_for_stream` (sole reader of the `mirror` key); reword the stale file-header / API-doc / constant-table / attr-index comments. ALSO remove the orphan helpers exposed by the file delete: `pe_supporting_files_effective` (sole callers `mirror-generate.sh` + `test-per-entry.sh` Group 7), `pe_warn`, `pe_is_interactive` (each sole-called from `mirror-generate.sh`; none Public API).
  - **DELETE** `scripts/lib/per-entry/mirror-generate.sh` entirely — the monolith-mirror generator (only dead test coverage remained post-BD-206).
  - `scripts/lib/per-entry/decompose.sh` — clean the stale `mirror-generate is retained …` clause (function survives).
  - `scripts/lib/per-entry/toc-regenerate.sh` — clean the stale `mirror-generate` cross-ref comment ONLY. **`per_entry_regenerate_toc` is a SEPARATE, surviving function — it stays.**
  - Dead pack test coverage:
    - `scripts/tests/test-per-entry.sh` — remove the `:70` sourcing, the `:604` smoke clause, and Groups **3/4/5/6/7/8** (mirror round-trip / idempotency / determinism / empty-tree / supporting-file / divergence-routing). **KEEP Group 9 (the TOC regenerator) — relocate the `$PB_DIR`+`$PB_ROOT` decompose scaffold into it.** (The original "Groups 3/4/8/9" was WRONG — Group 9 is KEEP.)
    - `scripts/tests/test-migrate-v10-to-v11-decompose.sh` (omitted from the original entry) — remove the `mirror_sha` helper, Group 3 (divergence routing, incl. `probe_mirror_routing`), the Group-4 tombstone, Group 5 (backward-compat) + the header-docstring descriptions; reword the `force-overwrite-mirror` prose. KEEP Groups 1/2 incl. the 4 negative-assert needles proving the BD-206 flag is gone.
    - `scripts/tests/test-validate-pack-checks-32-33-34.sh` — replace the mirror-generated `BACKLOG.md` fixture with a static `cat >` stub (the TOC-fixture block + the Check 32′ calls KEEP).
  - `test-fixtures/build.sh` + `scripts/tests/fixture-dependent/test-v11-realistic-ot.sh` — reword the 2 no-mirror-invariant comments to drop the `mirror-generate.sh` literal.
  - **grep-zero completeness gate (widened):** `git grep -nE 'mirror-generate|per_entry_regenerate_mirror|pe_canonical_mirror_for_stream|pe__mirror|mirror_build_temp|pe__normalize_trailing_blank|pe__effective_contains|mirror\) printf|PE_FORCE_OVERWRITE_MIRROR|force-overwrite-mirror' -- ':!maintenance-docs/' ':!changelog/' ':!backlog/'` returns only the 4 allowlisted B2 negative-assert needles, grep-zero for all else (audit-history exempt).
Description: After BD-203 deleted the pack monoliths (`pack-ops/BACKLOG.md` / `CHANGELOG.md`), the pack `mirror` subsystem became dead code — the `mirror) printf` constants are vestigial deletion-target references and `mirror-generate.sh` is never called for the pack streams. BD-206 (DR-2 resolved to option C) establishes the both-sides removal of the dead subsystem; the project-side half (O22-proj) lands in BD-206, and this BD lands the pack-side half (O22-pack) as the separate `pack-only` commit required by the Check-36 scope-keyword discipline (a project+pack mix in one commit would trip the gate). It removes the pack mirror constants, the never-called generator, the `pe_canonical_mirror_for_stream` accessor, and the dead pack test coverage; it preserves `per_entry_regenerate_toc` (a separate, surviving function). This realizes — and extends to a full both-sides retirement — the existing `mirror-generate.sh` in-code `TODO(v11.0): retire mirror-generate project-side at BD-206`. The fix is symmetry of the correct no-mirror solution on both surfaces: scope decides which commit (this `pack-only` one), never whether the pack side is fixed.
  **Out of scope:** the project-side removal (BD-206 O22-proj); the `per_entry_regenerate_toc` TOC regenerator (survives); pack-side backlog form-family compliance (BD-247).
Resolved: 2026-06-27 — dead M-mono mirror subsystem removed pack-side; 9 files (mirror-generate.sh deleted + _lib.sh mirror constants/accessor/3 orphans + dead test coverage), pack-only commit 61b01df. Widened grep-zero gate → 4 allowlisted needles → 0. Full large-BD pipeline incl. adversarial architect + adversarial planner + reconciliations; review CLEAN.
