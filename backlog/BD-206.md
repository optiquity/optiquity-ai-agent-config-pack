<!-- per-entry source: /backlog/BD-206.md; contract: /backlog/_rules.md -->
**BD-206 — Project-side per-entry no-mirror application (apply the corrected standard to client assets)**
Type: feat — STRUCTURAL. Apply the no-monolithic-mirror per-entry STANDARD (the rule corrected in BD-203) to the CLIENT-shipped per-entry feature + its assets, so the shipped product matches its own corrected convention. Project-side product change. Full pipeline.
Status: Resolved
Target: v11.0 — CONFIRMED flat-file-only (BD-214 Track-2, US-6 RESOLVED 2026-06-13). Launch-coherence item. The user deferred the project-side IMPLEMENTATION timing (2026-06-04: "later, whatever's easiest"); the implementation itself is Track-B work (see RE-SCOPE note) — this entry carries the re-scoped TEXT only. RE-SEQUENCED (user 2026-06-23): the BD-238 → BD-239 → BD-242 lead block now runs BEFORE BD-206.
RE-SCOPE 2026-06-13 (BD-214 Track-2, US-6): CONFIRMED v11.0, FLAT-FILE-ONLY. Tracker (GH Issues) integration is deferred indefinitely (BD-214), so this entry drops all tracker mode-conditional folds (the ops-contract R1-R8 mode-conditional legs that pivoted on tracker mode). The flat-file no-mirror conversion is the whole scope. SAME treatment as BD-203 — DELETE the project monoliths (`BACKLOG.md` / `IMPLEMENTATION-PLAN.md` / `CHANGELOG.md`) for converted streams, with NO regenerated mirror. `_index.md` FINDING (renamed from the former `_order` sidecar 2026-06-13, BD-214 — scope broadened, see ADD (US-6)): the per-entry index sidecar `_index.md` is PREDESIGNED but UNBUILT (zero such files exist in the tree — measured); BD-206 must CREATE it for the project implementation-plan stream (phase order is NOT numerically recoverable from filenames the way `/backlog/` BD-NNN ordering is — execution-ordering is ONE of the indexes `_index.md` may carry); if the predesign conflicts with the BD-203 as-built per-entry shape, the predesign is UPDATED to match BD-203 (BD-203 as-built wins). IMPLEMENTATION is Track B — its own docs-researcher → architect → planner → coder pipeline AFTER the BD-214 Track-2 cleanup; this entry carries the re-scoped scope + constraints + pointers TEXT only.
Blockers: Follows BD-203 (which corrects the shared standard + converts the pack). LAUNCH-COHERENCE: BD-203 corrects the shared trinity convention (which SHIPS to clients) to "no monolithic mirror," but the client per-entry tooling still GENERATES mirrors + `supporting-docs/MIGRATION-v10-to-v11.md` still says "monolith becomes a mirror." Shipping the corrected convention WITHOUT this implementation would ship an INCOHERENT product (convention says no-mirror; client feature makes mirrors) — so BD-206 is v11.0 (US-6).
POST-BD-204 REFRESH (anchor, user 2026-06-05): the BD-204 GH-Issues tracker work is now DEFERRED indefinitely (BD-214). The original "refresh against the AS-BUILT BD-204 tracker design" obligation is therefore MOOT for v11.0 — this entry is flat-file-only and does not depend on the deferred tracker machinery. Retained as history; the surface-generalizable no-monolith machinery realized on the project side is the flat-file scope below.
Unblocks: the client per-entry feature matches the corrected standard — client projects' per-entry trees (+ TOC) are the SSOT + readable form; no monolithic client mirror; client conversions preserve every entry (same standard as the pack).
Scope (flat-file-only, US-6): the client per-entry tooling behavior (stop generating monolithic client mirrors; per-entry + TOC is the readable form); `supporting-docs/MIGRATION-v10-to-v11.md` + any client per-entry doc / `_rules.md` corrected to the no-mirror standard; the client-side validators/checks (incl. `scripts/validate-pack.py` Check-43's project-side mirror-skip basenames + project-mirror prose, which BD-203 fix-2 correctly KEPT as accurate-for-now and which retire/update when the client mirror is removed).
  **KEEP (US-6):** the OT-v10.3 census prerequisite; the generalized-only guard; the scrubbed fixtures; the `scripts/lib/detect.sh` client-surface repoint; the client `[mirror]` retirement in Check 29; the `tracker-mirror.sh` CLIENT legs. (These are flat-file-relevant or surface-generalizable and survive the re-scope; only the tracker mode-conditional folds are dropped.)
  **ADD (US-6) — monolith DELETE, no mirror:** SAME treatment as BD-203 — for every converted client stream, DELETE the project monolith (`docs/project/BACKLOG.md`, `docs/project/implementation-plan/IMPLEMENTATION-PLAN.md`, `docs/project/changelog/CHANGELOG.md`) and ship NO regenerated mirror. The per-entry tree + generated `_toc.md` is the sole SSOT + readable form on the project side (matching the pack side).
  **ADD (US-6) — `_index.md` create/reconcile:** CREATE the per-entry index sidecar `_index.md` for the project implementation-plan stream. `_index.md` is a sidecar alongside `_intro.md` / `_rules.md` / `_toc.md` that may contain ONE OR MORE indexes or graphs for the per-entry flat files — including their ORDER or GROUPINGS, and OPTIONALLY a dependency graph (which MAY reference entries in another directory, e.g. TD entries depending on phase/implementation-plan entries). The dependency graph is NOT a default — it is created only if needed. The flat-file execution-ordering index is ONE of the possible indexes `_index.md` carries (required for the implementation-plan stream because phase order is NOT numerically recoverable from filenames the way `/backlog/` BD-NNN ordering is, so an index is required). `_index.md` is PREDESIGNED but UNBUILT (zero such files exist in the tree today — measured; the predesign, authored under the former name `_order` (the predesigned `.md` sidecar), lives in `ARCHITECTURE-BD-185-V2.md` §5.3, `ARCHITECTURE-BD-185-V2-ORDERING-ADDENDUM.md` §A-1, and `ARCHITECTURE-BD-203-V3-AMENDMENT.md` §F.3). If the predesign conflicts with the BD-203 as-built per-entry shape (flat tree + generated `_toc.md`, audience+purpose meta-doc governance per `ARCHITECTURE-BD-203-V3-AMENDMENT.md` §F), the predesign is UPDATED to match BD-203 (BD-203 as-built wins).
  **DEFERRED-SWEEP ANCHOR (2026-06-13, BD-214):** the `_order`→`_index.md` rename (the former `.md` sidecar basename) is applied to BD entries only (2026-06-13); the predesign chain (`ARCHITECTURE-BD-185-V2.md` + its `-ORDERING-ADDENDUM` + `ARCHITECTURE-BD-203-V3-AMENDMENT.md`), the BD-214 design/plan/report docs, the archived BD-185 pipeline records, the unbuilt `_order-generate.sh` generator, and any pack-ops/scripts involvement still carry the old `_order` name and REQUIRE a thorough researcher + architect sweep when `_index.md` usage + operations are implemented (census: `RESEARCH-ORDER-MD-RENAME-CENSUS.md` = 56 refs / 14 files; pure text, no built file / no validator-test hardcode).
  **DROP (US-6):** the ops-contract R1-R8 tracker mode-conditional folds (anything that pivoted client behavior on tracker mode) — tracker is deferred (BD-214); flat-file is the sole supported mode.
  **COMPLETE the CLIENT-surface half of the dual-use `_SANCTIONED_PACK_SIDE_SHIPPED` scripts (user 2026-06-04): BD-203 repoints ONLY the pack-surface branch of `scripts/lib/detect.sh` (`detect_pack_surface`) + `scripts/pack-help.sh`, leaving the CLIENT-surface branch reading the client monolith (`docs/project/BACKLOG.md` `^**TD-`). When BD-206 converts clients to per-entry trees that monolith vanishes — so BD-206 MUST repoint the client-surface detection/read to the client `/backlog/` tree so these dual-use files are COMPLETE (both surfaces tree-aware), with `_SANCTIONED_PACK_SIDE_SHIPPED` + the install map UNCHANGED (CI Check 47).** ALSO COMPLETE the PROJECT-SIDE SKILL MASTERS (user 2026-06-04): BD-203 (C-3) corrects only the pack COPIES (`.claude/.codex/.gemini`) of the boundary-investigation / pack-startup / etc. skill prose; the `project-template/skills/*/SKILL.md` MASTERS are project-side (denied by `pack-only`) and pending — BD-206 corrects the masters to the no-mirror standard so pack-copy↔master parity is restored (closes the G-4 divergence before launch). Same standard as BD-203, applied to project assets.
  **TRACK B:** the conversion + `_index.md` IMPLEMENTATION is Track-B work (own docs-researcher → architect → planner → coder pipeline after the BD-214 Track-2 cleanup); this entry carries the scope + constraints + pointers TEXT only.
Out of scope: the pack-side conversion (BD-203); GH Issues / tracker mode (BD-204, deferred indefinitely per BD-214 — no tracker mode-conditional behavior in this entry); the Track-B IMPLEMENTATION pipeline (this entry is re-scope TEXT only).
Acceptance criteria (full correctness audit): the client per-entry feature produces NO monolithic mirror; the project monoliths (`BACKLOG.md` / `IMPLEMENTATION-PLAN.md` / `CHANGELOG.md`) are DELETED for converted streams with no regenerated mirror (SAME as BD-203); the project implementation-plan stream carries an `_index.md` index sidecar (carrying at least the execution-ordering index) reconciled to the BD-203 as-built shape; client docs/rules state the no-mirror flat-file standard; a client conversion preserves every entry; the dual-use scripts (`detect.sh`/`pack-help.sh`) DETECT + READ a converted client project's per-entry tree with NO reliance on a client monolith (no half-repointed dual-use file remains); the `project-template/skills/*/SKILL.md` masters match the corrected standard (pack-copy↔master parity restored); NO client doc-vs-implementation gap; validate-pack green (Check 47 unchanged).
References: `project_pack_self_migration_launch_gate` (pack memory); BD-203 (corrects the standard + converts the pack); the client per-entry feature (Mode 1/2) + `supporting-docs/MIGRATION-v10-to-v11.md`.
Resolved: 2026-06-27 — Project-side per-entry, no-monolith-mirror, form-family conversion + full enforcement landed across 12 wave commits (A1; Wave B B1/B1b/B2/B3; Wave C C2/C3/C4/C5; Wave D D1 + D2-6-combined; Wave E E1 — `3fd576d..6b09cba`), plus the superseded-research-doc deletion (`5ee79a5`) and the PM-CHAT no-mirror NIT (`46738ae`). Delivered: per-entry schema + sidecar rebuild + Check 72 empty-template (A1); decompose/migrator emit per-entry with NO mirror (Wave B); form-family + conformance enforcement BOTH repos — Check 73 `_index.md` generator+validator (O11), Check 74 changelog conformance (O12 — narrative `Summary`|`Scope` + size caps required, `Test count`/`Files` advisory per the user-approved O12 measure-then-bound reconciliation), Check 75 graceful phase-naming guard (O13) (Wave C, serial on `validate-pack.py`+`validate-docs.sh`); client docs/prompts/skills/trinity repointed to no-mirror (Wave D — O14 METHODOLOGY, O15 trinity, O16 PM-CHAT, O17 prompts/SETUP/INSTALL, O18 skill masters [parity restored], O21); tracker reconciliation — per-entry→monolith M-mono mirror removed, tracker→file M-track `tracker-mirror.sh` kept (E1). Project monoliths DELETED (no regenerated mirror); `_index.md` created for the impl-plan stream; dual-use `detect.sh`/`pack-help.sh` client-surface repointed to the per-entry tree. Pipeline: census → architect (+2 adversarial + reconcile) → user design gate → planner (+ adversarial + reconcile) → user planner-to-coder gate → parallel worktree coder waves (each per-commit bounded review/fix) → cross-surface history/bloat audit (CLEAN, 0 findings) → end-of-batch cross-commit coherence review (CLEAN, 0 BLOCKER/MUST/SHOULD). Full battery green at HEAD `6b09cba` (validate-pack 73 checks; build 6/6; shard + migration batteries). Deferred-with-anchor: O22-pack pack-side dead-mirror removal → BD-249; non-mutation integrity manifest → BD-246; persistent handoff dir → BD-248; `supporting-docs/` anti-history/bloat guard-coverage gap → BD-250. v11.0.
Position: after BD-203; v11.0 launch-coherence (CONFIRMED flat-file-only, US-6). Implementation is Track B (after the BD-214 Track-2 cleanup).
