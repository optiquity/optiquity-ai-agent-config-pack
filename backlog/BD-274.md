<!-- per-entry source: /backlog/BD-274.md; contract: /backlog/_rules.md -->
**BD-274 — Project-side (client) agent-scratch ownership isolation (client deliverable analog of BD-273)**
Type: feat — client / project-template dev-workflow safety (shipped in `project-template/`). The client deliverable analog of BD-273: ship the same agent-scratch ownership-isolation model to CLIENT projects so a developer's client-agent workflow (the shipped `project-template` agents + the BD-257 B1 client mode/isolation subsystem) has the same protection — a client sub-agent cannot delete another agent's work or repo-bound-but-not-yet-tracked content. SEPARATE CLIENT copies per the `dependency-direction-placement` (c) no-dual-use rule (own client hook, own client convention, own client rule text) — never the pack's.
Status: Resolved
Blockers: SEQUENCING (not technical). Follows BD-273 (the pack-side model lands first; this mirrors it client-side). Coordinate with BD-257 (the client mode subsystem + hooks already land in `project-template/.claude/settings.json`) and BD-221 (client CLI surfaces).
Position: client deliverable; after BD-273. The architect ASSESSES whether the client agent workflow actually spawns sub-agents with scratch that needs isolation, then designs the client-appropriate realization (or surfaces why a lighter form suffices — evidence-based).
Unblocks: client projects get the same cross-agent scratch-deletion protection the pack gains in BD-273.
File/Symbol (architect refines after the assessment; provisional surface family):
  - the client ownership convention (how the client PM-Chat orchestrator assigns owned scratch dirs to client sub-agents).
  - a client PreToolUse[Bash] deletion-boundary hook shipped FLAT in `project-template/scripts/` + wired in `project-template/.claude/settings.json` (alongside the B1 mode hooks), Claude-only, path-scoped, fail-open — a SEPARATE client copy of the BD-273 pack hook (no dual-use).
  - the client rule text (the `project-template/` trinity `## Project rules` + `project-template/docs/pack/PM-CHAT.md`) stating the client agent scratch-ownership discipline.
Design provenance: designed in the SAME fresh architect pass as BD-273 — the shared ownership-isolation model with a pack realization (BD-273) and a client realization (BD-274).
Resolved: 2026-07-28 — client (project-side) agent-scratch ownership isolation shipped as a no-dual-use mirror of BD-273 across C1 (`project-template/scripts/pm-deletion-boundary.py` client hook + own `project-template/.claude/settings.json` PreToolUse element + pack-side test + pm-startup/pm-refresh canary extensions), C2 (merge-safety fixture proving the client hook wires as a SEPARATE PreToolUse[Bash] element so the structured settings merge produces NO duplicate commit-gate — bite proven through the real `customization_preserve` dispatch — plus a migrator dry-run merge assertion), C3 (client trinity "No destructive operations" bullet byte-identical ×3 + PM-CHAT ownership + owned-dirs-record clauses). Full deterministic LARGE-BD pipeline. Combined BD-273/274 end-of-batch audit CLEAN-WITH-ONE-SHOULD; the two client NITs fixed: NIT-1 — completed the pack→pm mirror by renaming the client handoff root `optiquity-pack-handoff` → `optiquity-pm-handoff` (hook + PM-CHAT + client-hook test, lock-step; pack side legitimately keeps "pack"; read-path == documented write-path) — and NIT-2 (stale README case-count). Mirror-but-customize + no-dual-use (`_SANCTIONED_PACK_SIDE_SHIPPED` frozen empty) + P-missed-7 boundary discipline + fresh-install AND v10→v11 migrate coverage of the client artifacts all verified by the audit. Full CI battery green.
