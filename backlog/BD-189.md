<!-- per-entry source: /backlog/BD-189.md; contract: /backlog/_rules.md -->
**BD-189 — Flat-file groupings implementation (architect/planner/coder cycle)**
Type: TODO(version) — surfaced 2026-05-23 from BD-186 sidecar wrap; live forward-pointing anchor for the FLAT-FILE groupings core implementation per pack memory `feedback_deferred_work_tracking`. RE-SCOPED 2026-06-17 (user-directed split): the tracker-PROJECTION legs split out to BD-227; this BD is the FLAT-FILE-ONLY half.
Status: Open
Target: v11.0 — sequenced directly after BD-206 (the project-side per-entry no-mirror application). REVERSIBLE: this v11.0 placement was set by user direction 2026-06-17 and may be moved out again later if scheduling warrants.
Blockers: BD-206 (sequenced directly after it). Flat-file is the sole supported mode, so the flat-file groupings core has no tracker dependency.
Unblocks: Per-capability flat-file implementation BDs that the groupings planner produces (BD-A through BD-K speculative per REQUIREMENTS-GROUPINGS-V11.md §5 BD landscape); the user-facing flat-file groupings feature. (Tracker-mode projection of groupings = BD-227, deferred.)
File/Symbol:
  - NEW `maintenance-docs/v11-implementation/ARCHITECTURE-GROUPINGS.md` (architect deliverable)
  - NEW `maintenance-docs/v11-implementation/PLAN-GROUPINGS.md` (planner deliverable)
  - Per-BD implementation surfaces TBD by planner (spans 17 capabilities)
  - PRIMARY INPUTS (read-only):
    - `maintenance-docs/v11-research/HANDOFF-V11.1-ARCHITECT.md` (READ FIRST — orientation; 155 lines)
    - `maintenance-docs/v11-research/REQUIREMENTS-GROUPINGS-V11.md` (primary input; 908 lines; 17 capabilities + design principles + scope decisions)
    - `maintenance-docs/v11-research/RESEARCH-TRACKER-GROUPING-PRIMITIVES-PER-BACKEND.md` (verified per-backend facts; 1762 lines; Pass-1 + Pass-2; §7 0-5 graded matrix)
    - `maintenance-docs/v11-research/IMPLEMENTATION-REPORT-RESEARCH-TRACKER-PRIMITIVES.md` (research methodology + open questions; 390 lines)
    - `maintenance-docs/v11-research/TOUCH-POINT-INVENTORY-GROUPINGS-V2.md` (constraint baseline; 910 lines; already triaged but useful for deeper detail per touch-point)
    - `maintenance-docs/v11-research/V11.1-DISCUSSION-GITHUB-PROJECTS.md` (original brainstorm; superseded but §10 grouping doc shape + §14 open questions remain useful context)
    - `maintenance-docs/v11-research/EXTERNAL-RESEARCH.md` §1-§5 + §6-§8 + §10-§12 (canonical for GitHub Issues / gh CLI / MCP / Codex / Gemini integration patterns + token cost + migration prior art + OSS abstraction patterns + failure modes + lesser-known features + CLI version verification; tracker primitive details superseded per staleness redirect at top of that doc)
    - `/backlog/BD-186.md` (Resolved) + `/backlog/BD-187.md` (parking-lot, dependency: external-tool instruction doc) + `/backlog/BD-188.md` (parking-lot, dependency: sprint view) for adjacent-work context and dependency-chain visibility (repointed 2026-06-13 — `pack-ops/BACKLOG.md` was deleted at BD-203; the `/backlog/` per-entry tree is the sole SSOT)
    - `CLAUDE.md` `## Pack memory` (pack-repo-trinity rules; standard pack-agent context)
Description: Live forward-pointing anchor for the FLAT-FILE groupings core implementation work. The flat-file legs of the groupings feature (per REQUIREMENTS-GROUPINGS-V11.md §4) land here; this BD captures the umbrella work that the architect / planner / coder pipeline will break into per-capability BDs. The tracker-PROJECTION legs (tracker-native grouping primitives / tracker-mode projection) split out to BD-227 (deferred); this entry is flat-file-only.

  Without this BD, the deferred work would lack a live forward-pointing anchor per pack memory `feedback_deferred_work_tracking` (BD-186 is Resolved, not a live anchor; the requirements artifact alone is "input" not a forward-pointing surface). This BD-189 satisfies the rule by providing the umbrella entry until the architect/planner work decomposes it.

  **Inbound deferral (from BD-195 audit, 2026-05-31):** P-31l — `INTAKE-GROUPINGS-V11.md` self-flags unverified fidelity (a quality caveat, not contamination). Deferred out of the BD-195 v11.0 sweep as legit groupings scope; address here (review fidelity, or rely on `REQUIREMENTS-GROUPINGS-V11.md`, canonical, which wins on conflict). [Flat-file core item; carried into v11.0 by the 2026-06-17 split.]

  **Pipeline (per REQUIREMENTS-GROUPINGS-V11.md §6):**
  1. Architect pass produces ARCHITECTURE-GROUPINGS.md
  2. User review
  3. Planner pass produces PLAN-GROUPINGS.md with per-BD breakdown
  4. User review
  5. Coder cycles per BD with reviewer cycles per pack memory `feedback_review_fix_one_cycle`
  6. **Migration architect/planner/coder pass is SEPARATELY scoped** per REQUIREMENTS-GROUPINGS-V11.md #16 SC16.10; the architect will likely open it as a sibling BD (architect/planner/coder rigor required; NOT ad-hoc file copies)
  7. End-of-batch reviewer + per-BD status flips + MIGRATION-v11.0-to-v11.x.md landing + release pin

  **Scope boundary:** This BD covers the FLAT-FILE CORE groupings implementation per capabilities #1-#17, limited to the flat-file legs of each capability. Of #11 (tracker capability matrix), NOTHING lands here except tracker-INFORMED schema design: the per-backend primitives research is consumed as INPUT to the flat-file field/shape choices so the later BD-227 tracker integration lands cleanly — zero tracker dependency in this BD (OQ-10 ruling, 2026-07-03). The tracker-PROJECTION legs of those capabilities (tracker-native grouping primitives / tracker-mode projection) are NOT in this BD — they split out to BD-227 (deferred). BD-187 (entry-type instruction doc) and BD-188 (sprint view) are SIBLING parking-lot BDs for ADJACENT future work; they are NOT subsumed by BD-189.

  **No-tracker constraint (2026-06-13, BD-214; split 2026-06-17):** tracker (GH Issues) integration is deferred indefinitely (BD-214). The split makes this explicit: the groupings flat-file CORE = THIS BD (BD-189, v11.0; flat-file is the sole supported mode), and the tracker-PROJECTION legs of groupings (tracker-native grouping primitives) = BD-227 (Deferred; lands with the tracker-resumption cluster, degrading gracefully per the C7 graceful-tracker-degradation design principle). BD-188 (Iteration/Project primitive) is tracker-dependent end-to-end and is Deferred no-version (US-5); the BD-188↔BD-227 dependency is decided later (not yet asserted).

  **BD-210 LIVE-classification constraint (2026-06-13):** the architect/planner inputs above include superseded/research docs; BD-210 (pre-launch maintenance-docs cleanup) classifies docs LIVE vs SUPERSEDED and may DELETE some inputs before this BD fires. Treat the input list as LIVE-at-read-time; re-verify each input still exists before consuming it.


  **Design-gate rulings (user, 2026-07-03, on CENSUS-BD189.md S6; the census header carries the user's binding constraints a/b/c + ask-don't-assume):**
  - **OQ-1:** the #16 upgrade-migration leg SURVIVES — invariant: EVERY project on v11.0+ gets groupings (fresh installs via S11; v10 projects via the v10→v11 migrator; already-installed v11.0 projects via the upgrade leg — standalone script vs `init-project.sh --update` fold is architect space). SC16.13(d)/SC16.14 tracker legs stay BD-227.
  - **OQ-2:** the May #14 generator decision STANDS — BD-189 builds the deterministic, flat-file-only, never-SSOT STATUS.md generator in the generated-sections shape (marker-delimited): class 1 = per-entry-tree tables (phase table + groupings table; BD-105's flat-file phase-row links fold in as a phase-table cell); class 2 = the snapshot-derived current-frontier section (`docs/project/pm-session-state.json` as SOURCE only — the snapshot stays pure); class 3 = the hand-authored PM-judgment section (marker-preserved; regen never touches it; no machine source exists for judgment prose by design).
  - **OQ-3:** option 3 — the groupings nudge rides EXISTING machinery at three moments (the pm-startup report line; the phase-creation authoring step; the STATUS.md ungrouped-visibility cell); no dormant-seam revival (the D-19/`recommendation.sh` wiring stays with the tracker cluster). PLUS the durable non-membership requirement: a deliberate "this phase stays ungrouped" ruling is TRACKED and honored by suggestion machinery and dashboard alike — never relitigated; mechanism is an architect charge bounded by the explicit-membership SSOT model.
  - **OQ-4:** ALL groupings mechanics (query verbs, reverse lookups, the generator) are client-shipped + client-executed (`project-template/scripts/` + PM-Chat procedures; the exact split is architect space). Nothing enters the frozen pack-side ship allowlist. No command-surface freezes — BD-257 wraps/extends later per queue order. The pack repo's role: ship the templates, provision via installer/migrator, validate template copies in CI.
  - **OQ-5:** #13 validation — the approved decision's INTENT binds (split focused checks; extend-don't-duplicate); the check-number specifics are RE-DERIVED against the landed BD-206 twin-parser model.
  - **OQ-6:** SC12.4's flat-file leg ROUTES to BD-257 (its existing client-command-surface remit); BD-189 ships the mechanics + the #12 procedures (METHODOLOGY workflow; PM-CHAT orchestration) that reference them; tracker rows stay BD-227. See the BD-257 entry's modification-authority note (user 2026-07-03).
  - **OQ-7:** (a) "3 mandatory supporting files" is satisfied by the live shipped-2 (`_rules.md` + `_intro.md`) + generated `_toc.md` pattern; (b) NO ordering sidecar (`_index.md`) for the groupings stream — groupings are orderless by design (SC2.5); presentation order is architect space. ADDENDUM (grouping-affinity ordering): membership is an INPUT to the phase-order derivation — same-grouping phases are placed contiguously in the implementation-plan index whenever Blockers/Unblocks permit; interleaving happens only where cross-group blockers force it; the preference NEVER constrains parallelization (an affinity tie-break among topologically valid orders; SC2.5 intact — groupings still declare no order).
  - **OQ-8:** the current queue order is authoritative (BD-189 → 224 → 257 → 136 → 236 → 210 → 205 → 093); BD-210's stale "directly after BD-224" line reconciled in its entry.
  - **OQ-9:** the groupings tree gets the SAME mechanics as the other per-entry trees — shipped sidecars enter the install-time immutable manifest like every stream's sidecars; user-authored `GRP-NNN.md` entries are never-overwrite user content; SC16.5's generic-merge routing is superseded by conformance to the landed pattern.
  - **OQ-10 (+ the user's doc-shape principles):** zero tracker dependency (see the corrected Scope boundary). A grouping is a LIST OF PHASES — pure structure (ID, Kind, title, the explicit member-phases list, declared exception field, cross-reference links). NO free-floating prose: a doc link or a comment is permitted ONLY as a declared OPTIONAL structured field, opaque to tooling (carried faithfully, never interpreted); governing principle — no requirement or dependency on anything non-deterministic. The format has ONE canonical, deterministically machine-parseable AND machine-writable serialization (the BD-185 SC-SER analog, applied to groupings).


  **Gate-1 rulings (user, 2026-07-04, on ARCHITECTURE-BD189-FINAL.md §C + GRP000-VALIDATION.md — the FINAL design is APPROVED and is THE design contract; this approval also constitutes the explicit user half of the OQ-9 immutable-manifest sign-off, `IMMUTABLE_PROJECT_RELS` 3 → 4 rows):**
  - **GQ-1:** the bold-pair header `**GRP-NNN — <Title>**` (the ID-keyed-entry precedent; impl-plan's H2 form is the phase-document precedent, not applicable); Kind lives SOLELY in its `Kind:` field — no `(<kind>)` title suffix (double-encoding forbidden by determinism).
  - **GQ-2:** a FIXED 10-value Kind enum — the 9 defaults + **`unassigned`** as the catch-all — declared solely in the immutable `_rules.md`. NO client extension mechanism (no `_kinds.md` sidecar; `_rules.md` stays immutable); augmentation arrives only via pack version bumps. SC4.2 (per-project extensibility) KNOWINGLY SUPERSEDED. `unassigned` carries no recognition-table row (never suggested, only hand-chosen).
  - **GQ-3:** required-`Description` DROPPED; the optional opaque `Comment:` is the sole prose carrier (description content may live there). META-PRINCIPLE (settles the class): the user's enumerations are PRINCIPLE-statements, not exhaustive field lists — any structured field required for the system to work appropriately and deterministically (`Entry-Type`, the back-pointer, future analogs) is admissible without a gate question; the violations are OMITTING something required, or carrying something required in an UNSTRUCTURED or NON-DETERMINISTIC form.
  - **GQ-4:** option (a), condition verified (the dead-path item is tracker-exclusive entry bookkeeping; `tracker-doctor.sh` has zero STATUS.md involvement) — BD-105 resolves when CBD-5 lands (its flat-file scope delivered by the generator); the tracker dual-link half routes to BD-227 (which already owns the #14 tracker legs), the corrected doctor citation traveling with it; NO new BD. The BD-105/BD-227 entry edits land as backlog governance when CBD-5 ships.
  - **GQ-5:** the phase-side `Ungrouped:` field REJECTED (decentralizes grouping information); a new sidecar REJECTED (complexity for minor value); **`GRP-000` ADOPTED** as the reserved ungrouped-phases grouping per GRP000-VALIDATION.md (VIABLE; 4 real forks): a single `reserved-id: GRP-000` schema key in the immutable `_rules.md`; GRP-000 alone is legal empty or single-member (the `Single-member exception:` field FORBIDDEN on it); EXCLUSIVITY — membership in GRP-000 ∩ any other grouping = FAIL (replaces the cross-stream contradiction check; net-cheaper); CREATED ON FIRST USE, never pre-created (absence ≡ zero rulings); allocation starts at GRP-001 (the minimum can never re-mint); the lib carries the reserved/real set split (`list-membership GRP-000` IS the declared-ungrouped ledger query); affinity ordering EXCLUDES GRP-000; its Kind is pinned `unassigned`. SC2.1's letter stays fully intact. Mitigations: per-phase rationale via GRP-000's own `Comment:` line (NEVER duplicated phase-side); a deleted phase's stale row is the standard SC3.7 dangling-member fail-loud case + the pre-deletion `list-membership phase-N` procedure step. Grouping completion NEVER touches membership — a finished grouping stands as a permanent snapshot; "complete" is a derived display fact.
  - **GQ-6:** option (i) — BD-189 lands the TWO minimal HELP-FRAGMENT rows in the same commit as the PM-CHAT orchestration text (Check 22 is same-commit-atomic). BD-257's authority EXTENDED: authorized to rework these rows and anything related to implement the command surface correctly, unconstrained by arbitrary rules or test pins; scope objections pre-overruled (see the BD-257 entry note).
  - **ADDENDUM — derived grouping status (user, 2026-07-04):** a grouping's status is DERIVED from its member phases' `Status:` values — COMPUTED, NEVER STORED (the grouping file stays lifecycle-free: present or absent). One rollup implementation in `groupings-lib.sh` (SC9.9), consumed by the query verbs (`list`/`show` status column) and the STATUS.md generator (groupings-table derived-status + percent-complete columns). Natural mapping: all `done` → complete; all `not-started` → not-started; any other mix → in-progress; percent = done ÷ members. The planner PINS and presents at the plan gate: the `blocked` rollup treatment and the `deferred`/`superseded` denominator question. GRP-000 exempt (reserved-set).


  **Gate-2 rulings (user, 2026-07-04, on PLAN-BD189-FINAL.md §P7 — five of six ruled; (b) OPEN in a user-directed architect trio):**
  - **(a) B4:** derived `blocked` only when the grouping is fully stalled; the `[N blocked]` flag always renders when any member is blocked. (The ledger-(b) trio may propose an amendment to the trigger per the user's mixed-deferred discussion — pending.)
  - **(b) OPEN — user-directed trio in flight:** the user's deferred/superseded discussion (superseded orphan-removal; all-deferred → `deferred`; mixed-deferred → `blocked`; the cross-grouping-unblock exception; the dependency-graph/cascade/cycle capability; work-halt guidance; β1 denominator; the target-version addendum — phases may need a `Target:` concept, groupings inherit by proxy, client versioning respected) is DISCUSSION-not-decision, under architect → adversarial → reconciliation assessment. The plan approval is HELD until (b) settles (C1 would freeze the affected goldens).
  - **(c) Variant B (poison):** any unreadable member ⇒ derived `unknown` (display↔gate honesty; the fix path is identical under both variants — the label is the alarm). The per-variant fold sets select B; the S=∅ repair is moot-if-B at the vacuous-truth site (B defines S=∅ directly).
  - **(d) Option 1:** the ruling's "show" = the `list-membership <GRP-NNN>` grouping-arm detail header (`GRP-NNN — <Title> — <Kind> — <derived> D/A (P%) [flags]` above the member rows; `-q` machine rows unchanged; GRP-000 count-only). NO sixth verb; verb aesthetics stay BD-257's later call.
  - **(e) Arm (ii):** the STATUS.md phase-table (class-1a) Status cell sources the `Status:` FIELD via `grp_phase_status_map` — one source inside one generated file (the GQ-1 double-encoding principle); in-file emoji conventions untouched; the D3.3 amendment recorded in the generator child's entry (provisional CBD-5) at implementation; the blocked-renders-blocked golden added.
  - **(f) JOIN:** `project-template/docs/project/groupings/` joins `_PACK_CHAT_ONLY_PERMITTED_PREFIXES` like its three siblings — the five-surface lockstep (core.py constant + the generated PACK-AGENTS.md region + the trinity small-set line ×3) lands as commit G3.

  **Resolution:** This BD resolves when the planner has produced per-capability flat-file BDs and the umbrella role is no longer needed (i.e., the children carry the work forward). Alternatively, it could resolve once all children Resolved. Architect/planner decides at scheduling time.

  **Position:** v11.0 — sequenced directly after BD-206 (project-side per-entry no-mirror application). Re-scoped 2026-06-17 (user-directed split) from the former v11.1+ deferred framing; the flat-file half is now a v11.0 keeper. REVERSIBLE: the user may move this v11.0 placement out again later. References: BD-227 (the tracker-projection half this was split from), BD-206 (the entry it sequences after), BD-186 (Resolved requirements), BD-214 (no-tracker constraint).
Resolved: n/a
