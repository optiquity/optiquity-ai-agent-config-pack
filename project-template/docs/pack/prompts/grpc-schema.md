---
agent: grpc-schema
variants: []
---

# grpc-schema — prompt templates

Placeholder. No standard variants ship for this agent yet. Projects
that need a `grpc-schema` prompt may add variants here (pack) or add
custom variants via the `x-<name>.md` custom-agent mechanism documented
in PM-CHAT.md.
