---
name: pm-help
description: Show all project commands and colloquial mappings (semantic trigger — "what project commands exist", "how do I run X", or a quick reference for `pm-startup`, `init-project.sh`, `agent-run.sh`, or any top-level project verb).
allowed-tools: Bash
---

Run `scripts/pm-help.sh` (it lives at the project root under `scripts/`) and
present its output to the user verbatim. For full docs see `docs/pack/PM-CHAT.md`,
`docs/pack/INSTALL-PROCEDURES.md`, and `docs/pack/OPTIONAL-FEATURES.md`.
