# Synthetic fixture — Check 40 PASS path (allowlist)

This fixture exercises the `_CHECK_40_ALLOWLIST` exemption tier.
Every bare ref below MUST be on the allowlist.

- See `README.md` for the landing-page docs.
- The trinity (`CLAUDE.md`, `AGENTS.md`, `GEMINI.md`) carries the
  pack-memory rules.
- Project commands documented in `QUICKSTART.md`.
- Memory cache lives at `MEMORY.md` (external to pack repo).
- Concept nouns: `tracker.toml`, `id-map.json`, `report.md`,
  `manifest.txt`.
- Per-entry filename patterns: `BD-NNN.md`, `TD-NNN.md`,
  `phase-N.md`.
- Feedback memory cache: `feedback_review_fix_one_cycle.md`.
- Byte-identical mirror exception: `HELP-FRAGMENT.md` (resolves at
  client-installed location).
